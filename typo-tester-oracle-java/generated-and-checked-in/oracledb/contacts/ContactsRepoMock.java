/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package oracledb.contacts;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilderMock;
import typo.dsl.UpdateParams;

public record ContactsRepoMock(
    java.util.function.Function<ContactsRowUnsaved, ContactsRow> toRow,
    HashMap<ContactsId, ContactsRow> map)
    implements ContactsRepo {
  public ContactsRepoMock(java.util.function.Function<ContactsRowUnsaved, ContactsRow> toRow) {
    this(toRow, new HashMap<ContactsId, ContactsRow>());
  }
  ;

  public ContactsRepoMock withToRow(
      java.util.function.Function<ContactsRowUnsaved, ContactsRow> toRow) {
    return new ContactsRepoMock(toRow, map);
  }
  ;

  public ContactsRepoMock withMap(HashMap<ContactsId, ContactsRow> map) {
    return new ContactsRepoMock(toRow, map);
  }
  ;

  @Override
  public DeleteBuilder<ContactsFields, ContactsRow> delete() {
    return new DeleteBuilderMock<>(
        ContactsFields.structure(),
        () -> new ArrayList<>(map.values()),
        DeleteParams.empty(),
        row -> row.contactId(),
        id -> map.remove(id));
  }
  ;

  @Override
  public Boolean deleteById(ContactsId contactId, Connection c) {
    return Optional.ofNullable(map.remove(contactId)).isPresent();
  }
  ;

  @Override
  public Integer deleteByIds(ContactsId[] contactIds, Connection c) {
    var count = 0;
    for (var id : contactIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
        ;
      }
      ;
    }
    ;
    return count;
  }
  ;

  @Override
  public ContactsId insert(ContactsRow unsaved, Connection c) {
    if (map.containsKey(unsaved.contactId())) {
      throw new RuntimeException("id " + unsaved.contactId() + " already exists");
    }
    ;
    map.put(unsaved.contactId(), unsaved);
    return unsaved.contactId();
  }
  ;

  @Override
  public ContactsId insert(ContactsRowUnsaved unsaved, Connection c) {
    return insert(toRow.apply(unsaved), c);
  }
  ;

  @Override
  public SelectBuilder<ContactsFields, ContactsRow> select() {
    return new SelectBuilderMock<>(
        ContactsFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  }
  ;

  @Override
  public List<ContactsRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  }
  ;

  @Override
  public Optional<ContactsRow> selectById(ContactsId contactId, Connection c) {
    return Optional.ofNullable(map.get(contactId));
  }
  ;

  @Override
  public List<ContactsRow> selectByIds(ContactsId[] contactIds, Connection c) {
    var result = new ArrayList<ContactsRow>();
    for (var id : contactIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
        result.add(opt.get());
      }
      ;
    }
    ;
    return result;
  }
  ;

  @Override
  public Map<ContactsId, ContactsRow> selectByIdsTracked(ContactsId[] contactIds, Connection c) {
    return selectByIds(contactIds, c).stream()
        .collect(Collectors.toMap((ContactsRow row) -> row.contactId(), Function.identity()));
  }
  ;

  @Override
  public UpdateBuilder<ContactsFields, ContactsRow> update() {
    return new UpdateBuilderMock<>(
        ContactsFields.structure(),
        () -> new ArrayList<>(map.values()),
        UpdateParams.empty(),
        row -> row);
  }
  ;

  @Override
  public Boolean update(ContactsRow row, Connection c) {
    var shouldUpdate =
        Optional.ofNullable(map.get(row.contactId()))
            .filter(oldRow -> !oldRow.equals(row))
            .isPresent();
    if (shouldUpdate) {
      map.put(row.contactId(), row);
      ;
    }
    ;
    return shouldUpdate;
  }
  ;

  @Override
  public ContactsRow upsert(ContactsRow unsaved, Connection c) {
    map.put(unsaved.contactId(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public List<ContactsRow> upsertBatch(Iterator<ContactsRow> unsaved, Connection c) {
    var result = new ArrayList<ContactsRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.contactId(), row);
      result.add(row);
    }
    ;
    return result;
  }
  ;
}
