/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.warehouses;

import static typo.runtime.Fragment.interpolate;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import typo.dsl.DeleteBuilder;
import typo.dsl.Dialect;
import typo.dsl.SelectBuilder;
import typo.dsl.UpdateBuilder;
import typo.runtime.Fragment;
import typo.runtime.MariaTypes;

public class WarehousesRepoImpl implements WarehousesRepo {
  @Override
  public DeleteBuilder<WarehousesFields, WarehousesRow> delete() {
    return DeleteBuilder.of("`warehouses`", WarehousesFields.structure(), Dialect.MARIADB);
  }
  ;

  @Override
  public Boolean deleteById(WarehousesId warehouseId, Connection c) {
    return interpolate(
                Fragment.lit("delete from `warehouses` where `warehouse_id` = "),
                Fragment.encode(WarehousesId.pgType, warehouseId),
                Fragment.lit(""))
            .update()
            .runUnchecked(c)
        > 0;
  }
  ;

  @Override
  public Integer deleteByIds(WarehousesId[] warehouseIds, Connection c) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : warehouseIds) {
      fragments.add(Fragment.encode(WarehousesId.pgType, id));
    }
    ;
    return Fragment.interpolate(
            Fragment.lit("delete from `warehouses` where `warehouse_id` in ("),
            Fragment.comma(fragments),
            Fragment.lit(")"))
        .update()
        .runUnchecked(c);
  }
  ;

  @Override
  public WarehousesRow insert(WarehousesRow unsaved, Connection c) {
    return interpolate(
            Fragment.lit(
                "insert into `warehouses`(`code`, `name`, `address`, `location`, `service_area`,"
                    + " `timezone`, `is_active`, `contact_email`, `contact_phone`)\n"
                    + "values ("),
            Fragment.encode(MariaTypes.char_, unsaved.code()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.varchar, unsaved.name()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.varchar, unsaved.address()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.point, unsaved.location()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.polygon.opt(), unsaved.serviceArea()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.varchar, unsaved.timezone()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.bool, unsaved.isActive()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.varchar.opt(), unsaved.contactEmail()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.varchar.opt(), unsaved.contactPhone()),
            Fragment.lit(
                ")\n"
                    + "RETURNING `warehouse_id`, `code`, `name`, `address`, `location`,"
                    + " `service_area`, `timezone`, `is_active`, `contact_email`,"
                    + " `contact_phone`\n"))
        .updateReturning(WarehousesRow._rowParser.exactlyOne())
        .runUnchecked(c);
  }
  ;

  @Override
  public WarehousesRow insert(WarehousesRowUnsaved unsaved, Connection c) {
    ArrayList<Fragment> columns = new ArrayList<>();
    ;
    ArrayList<Fragment> values = new ArrayList<>();
    ;
    columns.add(Fragment.lit("`code`"));
    values.add(interpolate(Fragment.encode(MariaTypes.char_, unsaved.code()), Fragment.lit("")));
    columns.add(Fragment.lit("`name`"));
    values.add(interpolate(Fragment.encode(MariaTypes.varchar, unsaved.name()), Fragment.lit("")));
    columns.add(Fragment.lit("`address`"));
    values.add(
        interpolate(Fragment.encode(MariaTypes.varchar, unsaved.address()), Fragment.lit("")));
    columns.add(Fragment.lit("`location`"));
    values.add(
        interpolate(Fragment.encode(MariaTypes.point, unsaved.location()), Fragment.lit("")));
    unsaved
        .serviceArea()
        .visit(
            () -> {},
            value -> {
              columns.add(Fragment.lit("`service_area`"));
              values.add(
                  interpolate(Fragment.encode(MariaTypes.polygon.opt(), value), Fragment.lit("")));
            });
    ;
    unsaved
        .timezone()
        .visit(
            () -> {},
            value -> {
              columns.add(Fragment.lit("`timezone`"));
              values.add(interpolate(Fragment.encode(MariaTypes.varchar, value), Fragment.lit("")));
            });
    ;
    unsaved
        .isActive()
        .visit(
            () -> {},
            value -> {
              columns.add(Fragment.lit("`is_active`"));
              values.add(interpolate(Fragment.encode(MariaTypes.bool, value), Fragment.lit("")));
            });
    ;
    unsaved
        .contactEmail()
        .visit(
            () -> {},
            value -> {
              columns.add(Fragment.lit("`contact_email`"));
              values.add(
                  interpolate(Fragment.encode(MariaTypes.varchar.opt(), value), Fragment.lit("")));
            });
    ;
    unsaved
        .contactPhone()
        .visit(
            () -> {},
            value -> {
              columns.add(Fragment.lit("`contact_phone`"));
              values.add(
                  interpolate(Fragment.encode(MariaTypes.varchar.opt(), value), Fragment.lit("")));
            });
    ;
    Fragment q =
        interpolate(
            Fragment.lit("insert into `warehouses`("),
            Fragment.comma(columns),
            Fragment.lit(")\nvalues ("),
            Fragment.comma(values),
            Fragment.lit(
                ")\n"
                    + "RETURNING `warehouse_id`, `code`, `name`, `address`, `location`,"
                    + " `service_area`, `timezone`, `is_active`, `contact_email`,"
                    + " `contact_phone`\n"));
    ;
    return q.updateReturning(WarehousesRow._rowParser.exactlyOne()).runUnchecked(c);
  }
  ;

  @Override
  public SelectBuilder<WarehousesFields, WarehousesRow> select() {
    return SelectBuilder.of(
        "`warehouses`", WarehousesFields.structure(), WarehousesRow._rowParser, Dialect.MARIADB);
  }
  ;

  @Override
  public List<WarehousesRow> selectAll(Connection c) {
    return interpolate(
            Fragment.lit(
                "select `warehouse_id`, `code`, `name`, `address`, `location`, `service_area`,"
                    + " `timezone`, `is_active`, `contact_email`, `contact_phone`\n"
                    + "from `warehouses`\n"))
        .query(WarehousesRow._rowParser.all())
        .runUnchecked(c);
  }
  ;

  @Override
  public Optional<WarehousesRow> selectById(WarehousesId warehouseId, Connection c) {
    return interpolate(
            Fragment.lit(
                "select `warehouse_id`, `code`, `name`, `address`, `location`, `service_area`,"
                    + " `timezone`, `is_active`, `contact_email`, `contact_phone`\n"
                    + "from `warehouses`\n"
                    + "where `warehouse_id` = "),
            Fragment.encode(WarehousesId.pgType, warehouseId),
            Fragment.lit(""))
        .query(WarehousesRow._rowParser.first())
        .runUnchecked(c);
  }
  ;

  @Override
  public List<WarehousesRow> selectByIds(WarehousesId[] warehouseIds, Connection c) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : warehouseIds) {
      fragments.add(Fragment.encode(WarehousesId.pgType, id));
    }
    ;
    return Fragment.interpolate(
            Fragment.lit(
                "select `warehouse_id`, `code`, `name`, `address`, `location`, `service_area`,"
                    + " `timezone`, `is_active`, `contact_email`, `contact_phone` from `warehouses`"
                    + " where `warehouse_id` in ("),
            Fragment.comma(fragments),
            Fragment.lit(")"))
        .query(WarehousesRow._rowParser.all())
        .runUnchecked(c);
  }
  ;

  @Override
  public Map<WarehousesId, WarehousesRow> selectByIdsTracked(
      WarehousesId[] warehouseIds, Connection c) {
    HashMap<WarehousesId, WarehousesRow> ret = new HashMap<WarehousesId, WarehousesRow>();
    selectByIds(warehouseIds, c).forEach(row -> ret.put(row.warehouseId(), row));
    return ret;
  }
  ;

  @Override
  public Optional<WarehousesRow> selectByUniqueCode(String code, Connection c) {
    return interpolate(
            Fragment.lit(
                "select `warehouse_id`, `code`, `name`, `address`, `location`, `service_area`,"
                    + " `timezone`, `is_active`, `contact_email`, `contact_phone`\n"
                    + "from `warehouses`\n"
                    + "where `code` = "),
            Fragment.encode(MariaTypes.char_, code),
            Fragment.lit("\n"))
        .query(WarehousesRow._rowParser.first())
        .runUnchecked(c);
  }
  ;

  @Override
  public UpdateBuilder<WarehousesFields, WarehousesRow> update() {
    return UpdateBuilder.of(
        "`warehouses`", WarehousesFields.structure(), WarehousesRow._rowParser, Dialect.MARIADB);
  }
  ;

  @Override
  public Boolean update(WarehousesRow row, Connection c) {
    WarehousesId warehouseId = row.warehouseId();
    ;
    return interpolate(
                Fragment.lit("update `warehouses`\nset `code` = "),
                Fragment.encode(MariaTypes.char_, row.code()),
                Fragment.lit(",\n`name` = "),
                Fragment.encode(MariaTypes.varchar, row.name()),
                Fragment.lit(",\n`address` = "),
                Fragment.encode(MariaTypes.varchar, row.address()),
                Fragment.lit(",\n`location` = "),
                Fragment.encode(MariaTypes.point, row.location()),
                Fragment.lit(",\n`service_area` = "),
                Fragment.encode(MariaTypes.polygon.opt(), row.serviceArea()),
                Fragment.lit(",\n`timezone` = "),
                Fragment.encode(MariaTypes.varchar, row.timezone()),
                Fragment.lit(",\n`is_active` = "),
                Fragment.encode(MariaTypes.bool, row.isActive()),
                Fragment.lit(",\n`contact_email` = "),
                Fragment.encode(MariaTypes.varchar.opt(), row.contactEmail()),
                Fragment.lit(",\n`contact_phone` = "),
                Fragment.encode(MariaTypes.varchar.opt(), row.contactPhone()),
                Fragment.lit("\nwhere `warehouse_id` = "),
                Fragment.encode(WarehousesId.pgType, warehouseId),
                Fragment.lit(""))
            .update()
            .runUnchecked(c)
        > 0;
  }
  ;

  @Override
  public WarehousesRow upsert(WarehousesRow unsaved, Connection c) {
    return interpolate(
            Fragment.lit(
                "INSERT INTO `warehouses`(`code`, `name`, `address`, `location`, `service_area`,"
                    + " `timezone`, `is_active`, `contact_email`, `contact_phone`)\n"
                    + "VALUES ("),
            Fragment.encode(MariaTypes.char_, unsaved.code()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.varchar, unsaved.name()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.varchar, unsaved.address()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.point, unsaved.location()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.polygon.opt(), unsaved.serviceArea()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.varchar, unsaved.timezone()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.bool, unsaved.isActive()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.varchar.opt(), unsaved.contactEmail()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.varchar.opt(), unsaved.contactPhone()),
            Fragment.lit(
                ")\n"
                    + "ON DUPLICATE KEY UPDATE `code` = VALUES(`code`),\n"
                    + "`name` = VALUES(`name`),\n"
                    + "`address` = VALUES(`address`),\n"
                    + "`location` = VALUES(`location`),\n"
                    + "`service_area` = VALUES(`service_area`),\n"
                    + "`timezone` = VALUES(`timezone`),\n"
                    + "`is_active` = VALUES(`is_active`),\n"
                    + "`contact_email` = VALUES(`contact_email`),\n"
                    + "`contact_phone` = VALUES(`contact_phone`)\n"
                    + "RETURNING `warehouse_id`, `code`, `name`, `address`, `location`,"
                    + " `service_area`, `timezone`, `is_active`, `contact_email`, `contact_phone`"))
        .updateReturning(WarehousesRow._rowParser.exactlyOne())
        .runUnchecked(c);
  }
  ;

  @Override
  public List<WarehousesRow> upsertBatch(Iterator<WarehousesRow> unsaved, Connection c) {
    return interpolate(
            Fragment.lit(
                "INSERT INTO `warehouses`(`warehouse_id`, `code`, `name`, `address`, `location`,"
                    + " `service_area`, `timezone`, `is_active`, `contact_email`,"
                    + " `contact_phone`)\n"
                    + "VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)\n"
                    + "ON DUPLICATE KEY UPDATE `code` = VALUES(`code`),\n"
                    + "`name` = VALUES(`name`),\n"
                    + "`address` = VALUES(`address`),\n"
                    + "`location` = VALUES(`location`),\n"
                    + "`service_area` = VALUES(`service_area`),\n"
                    + "`timezone` = VALUES(`timezone`),\n"
                    + "`is_active` = VALUES(`is_active`),\n"
                    + "`contact_email` = VALUES(`contact_email`),\n"
                    + "`contact_phone` = VALUES(`contact_phone`)\n"
                    + "RETURNING `warehouse_id`, `code`, `name`, `address`, `location`,"
                    + " `service_area`, `timezone`, `is_active`, `contact_email`, `contact_phone`"))
        .updateReturningEach(WarehousesRow._rowParser, unsaved)
        .runUnchecked(c);
  }
  ;
}
