/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.audit_log;

import com.fasterxml.jackson.annotation.JsonProperty;
import java.time.LocalDateTime;
import java.util.Optional;
import testdb.customtypes.Defaulted;
import typo.data.maria.Inet6;
import typo.runtime.MariaTypes;
import typo.runtime.RowParser;
import typo.runtime.RowParsers;

/** Table: audit_log Primary key: log_id */
public record AuditLogRow(
    /** AUTO_INCREMENT */
    @JsonProperty("log_id") AuditLogId logId,
    /** */
    @JsonProperty("table_name") String tableName,
    /** */
    @JsonProperty("record_id") String recordId,
    /** */
    String action,
    /** Default: NULL */
    @JsonProperty("old_values") Optional<String> oldValues,
    /** Default: NULL */
    @JsonProperty("new_values") Optional<String> newValues,
    /** Default: NULL */
    @JsonProperty("changed_by") Optional<String> changedBy,
    /** Default: current_timestamp(6) */
    @JsonProperty("changed_at") LocalDateTime changedAt,
    /** Default: NULL */
    @JsonProperty("client_ip") Optional<Inet6> clientIp,
    /** Default: NULL */
    @JsonProperty("session_id") Optional<byte[]> sessionId) {
  /** AUTO_INCREMENT */
  public AuditLogRow withLogId(AuditLogId logId) {
    return new AuditLogRow(
        logId, tableName, recordId, action, oldValues, newValues, changedBy, changedAt, clientIp,
        sessionId);
  }
  ;

  /** */
  public AuditLogRow withTableName(String tableName) {
    return new AuditLogRow(
        logId, tableName, recordId, action, oldValues, newValues, changedBy, changedAt, clientIp,
        sessionId);
  }
  ;

  /** */
  public AuditLogRow withRecordId(String recordId) {
    return new AuditLogRow(
        logId, tableName, recordId, action, oldValues, newValues, changedBy, changedAt, clientIp,
        sessionId);
  }
  ;

  /** */
  public AuditLogRow withAction(String action) {
    return new AuditLogRow(
        logId, tableName, recordId, action, oldValues, newValues, changedBy, changedAt, clientIp,
        sessionId);
  }
  ;

  /** Default: NULL */
  public AuditLogRow withOldValues(Optional<String> oldValues) {
    return new AuditLogRow(
        logId, tableName, recordId, action, oldValues, newValues, changedBy, changedAt, clientIp,
        sessionId);
  }
  ;

  /** Default: NULL */
  public AuditLogRow withNewValues(Optional<String> newValues) {
    return new AuditLogRow(
        logId, tableName, recordId, action, oldValues, newValues, changedBy, changedAt, clientIp,
        sessionId);
  }
  ;

  /** Default: NULL */
  public AuditLogRow withChangedBy(Optional<String> changedBy) {
    return new AuditLogRow(
        logId, tableName, recordId, action, oldValues, newValues, changedBy, changedAt, clientIp,
        sessionId);
  }
  ;

  /** Default: current_timestamp(6) */
  public AuditLogRow withChangedAt(LocalDateTime changedAt) {
    return new AuditLogRow(
        logId, tableName, recordId, action, oldValues, newValues, changedBy, changedAt, clientIp,
        sessionId);
  }
  ;

  /** Default: NULL */
  public AuditLogRow withClientIp(Optional<Inet6> clientIp) {
    return new AuditLogRow(
        logId, tableName, recordId, action, oldValues, newValues, changedBy, changedAt, clientIp,
        sessionId);
  }
  ;

  /** Default: NULL */
  public AuditLogRow withSessionId(Optional<byte[]> sessionId) {
    return new AuditLogRow(
        logId, tableName, recordId, action, oldValues, newValues, changedBy, changedAt, clientIp,
        sessionId);
  }
  ;

  public static RowParser<AuditLogRow> _rowParser =
      RowParsers.of(
          AuditLogId.pgType,
          MariaTypes.varchar,
          MariaTypes.varchar,
          MariaTypes.text,
          MariaTypes.longtext.opt(),
          MariaTypes.longtext.opt(),
          MariaTypes.varchar.opt(),
          MariaTypes.datetime,
          MariaTypes.inet6.opt(),
          MariaTypes.varbinary.opt(),
          AuditLogRow::new,
          row ->
              new Object[] {
                row.logId(),
                row.tableName(),
                row.recordId(),
                row.action(),
                row.oldValues(),
                row.newValues(),
                row.changedBy(),
                row.changedAt(),
                row.clientIp(),
                row.sessionId()
              });
  ;

  public AuditLogId id() {
    return logId;
  }
  ;

  public AuditLogRowUnsaved toUnsavedRow(
      Defaulted<Optional<String>> oldValues,
      Defaulted<Optional<String>> newValues,
      Defaulted<Optional<String>> changedBy,
      Defaulted<LocalDateTime> changedAt,
      Defaulted<Optional<Inet6>> clientIp,
      Defaulted<Optional<byte[]>> sessionId) {
    return new AuditLogRowUnsaved(
        tableName, recordId, action, oldValues, newValues, changedBy, changedAt, clientIp,
        sessionId);
  }
  ;
}
