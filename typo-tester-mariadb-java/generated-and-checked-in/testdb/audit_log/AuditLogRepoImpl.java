/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.audit_log;

import static typo.runtime.Fragment.interpolate;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import typo.dsl.DeleteBuilder;
import typo.dsl.Dialect;
import typo.dsl.SelectBuilder;
import typo.dsl.UpdateBuilder;
import typo.runtime.Fragment;
import typo.runtime.MariaTypes;

public class AuditLogRepoImpl implements AuditLogRepo {
  @Override
  public DeleteBuilder<AuditLogFields, AuditLogRow> delete() {
    return DeleteBuilder.of("`audit_log`", AuditLogFields.structure(), Dialect.MARIADB);
  }
  ;

  @Override
  public Boolean deleteById(AuditLogId logId, Connection c) {
    return interpolate(
                Fragment.lit("delete from `audit_log` where `log_id` = "),
                Fragment.encode(AuditLogId.pgType, logId),
                Fragment.lit(""))
            .update()
            .runUnchecked(c)
        > 0;
  }
  ;

  @Override
  public Integer deleteByIds(AuditLogId[] logIds, Connection c) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : logIds) {
      fragments.add(Fragment.encode(AuditLogId.pgType, id));
    }
    ;
    return Fragment.interpolate(
            Fragment.lit("delete from `audit_log` where `log_id` in ("),
            Fragment.comma(fragments),
            Fragment.lit(")"))
        .update()
        .runUnchecked(c);
  }
  ;

  @Override
  public AuditLogRow insert(AuditLogRow unsaved, Connection c) {
    return interpolate(
            Fragment.lit(
                "insert into `audit_log`(`table_name`, `record_id`, `action`, `old_values`,"
                    + " `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`)\n"
                    + "values ("),
            Fragment.encode(MariaTypes.varchar, unsaved.tableName()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.varchar, unsaved.recordId()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.text, unsaved.action()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.longtext.opt(), unsaved.oldValues()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.longtext.opt(), unsaved.newValues()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.varchar.opt(), unsaved.changedBy()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.datetime, unsaved.changedAt()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.inet6.opt(), unsaved.clientIp()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.varbinary.opt(), unsaved.sessionId()),
            Fragment.lit(
                ")\n"
                    + "RETURNING `log_id`, `table_name`, `record_id`, `action`, `old_values`,"
                    + " `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`\n"))
        .updateReturning(AuditLogRow._rowParser.exactlyOne())
        .runUnchecked(c);
  }
  ;

  @Override
  public AuditLogRow insert(AuditLogRowUnsaved unsaved, Connection c) {
    ArrayList<Fragment> columns = new ArrayList<>();
    ;
    ArrayList<Fragment> values = new ArrayList<>();
    ;
    columns.add(Fragment.lit("`table_name`"));
    values.add(
        interpolate(Fragment.encode(MariaTypes.varchar, unsaved.tableName()), Fragment.lit("")));
    columns.add(Fragment.lit("`record_id`"));
    values.add(
        interpolate(Fragment.encode(MariaTypes.varchar, unsaved.recordId()), Fragment.lit("")));
    columns.add(Fragment.lit("`action`"));
    values.add(interpolate(Fragment.encode(MariaTypes.text, unsaved.action()), Fragment.lit("")));
    unsaved
        .oldValues()
        .visit(
            () -> {},
            value -> {
              columns.add(Fragment.lit("`old_values`"));
              values.add(
                  interpolate(Fragment.encode(MariaTypes.longtext.opt(), value), Fragment.lit("")));
            });
    ;
    unsaved
        .newValues()
        .visit(
            () -> {},
            value -> {
              columns.add(Fragment.lit("`new_values`"));
              values.add(
                  interpolate(Fragment.encode(MariaTypes.longtext.opt(), value), Fragment.lit("")));
            });
    ;
    unsaved
        .changedBy()
        .visit(
            () -> {},
            value -> {
              columns.add(Fragment.lit("`changed_by`"));
              values.add(
                  interpolate(Fragment.encode(MariaTypes.varchar.opt(), value), Fragment.lit("")));
            });
    ;
    unsaved
        .changedAt()
        .visit(
            () -> {},
            value -> {
              columns.add(Fragment.lit("`changed_at`"));
              values.add(
                  interpolate(Fragment.encode(MariaTypes.datetime, value), Fragment.lit("")));
            });
    ;
    unsaved
        .clientIp()
        .visit(
            () -> {},
            value -> {
              columns.add(Fragment.lit("`client_ip`"));
              values.add(
                  interpolate(Fragment.encode(MariaTypes.inet6.opt(), value), Fragment.lit("")));
            });
    ;
    unsaved
        .sessionId()
        .visit(
            () -> {},
            value -> {
              columns.add(Fragment.lit("`session_id`"));
              values.add(
                  interpolate(
                      Fragment.encode(MariaTypes.varbinary.opt(), value), Fragment.lit("")));
            });
    ;
    Fragment q =
        interpolate(
            Fragment.lit("insert into `audit_log`("),
            Fragment.comma(columns),
            Fragment.lit(")\nvalues ("),
            Fragment.comma(values),
            Fragment.lit(
                ")\n"
                    + "RETURNING `log_id`, `table_name`, `record_id`, `action`, `old_values`,"
                    + " `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`\n"));
    ;
    return q.updateReturning(AuditLogRow._rowParser.exactlyOne()).runUnchecked(c);
  }
  ;

  @Override
  public SelectBuilder<AuditLogFields, AuditLogRow> select() {
    return SelectBuilder.of(
        "`audit_log`", AuditLogFields.structure(), AuditLogRow._rowParser, Dialect.MARIADB);
  }
  ;

  @Override
  public List<AuditLogRow> selectAll(Connection c) {
    return interpolate(
            Fragment.lit(
                "select `log_id`, `table_name`, `record_id`, `action`, `old_values`, `new_values`,"
                    + " `changed_by`, `changed_at`, `client_ip`, `session_id`\n"
                    + "from `audit_log`\n"))
        .query(AuditLogRow._rowParser.all())
        .runUnchecked(c);
  }
  ;

  @Override
  public Optional<AuditLogRow> selectById(AuditLogId logId, Connection c) {
    return interpolate(
            Fragment.lit(
                "select `log_id`, `table_name`, `record_id`, `action`, `old_values`, `new_values`,"
                    + " `changed_by`, `changed_at`, `client_ip`, `session_id`\n"
                    + "from `audit_log`\n"
                    + "where `log_id` = "),
            Fragment.encode(AuditLogId.pgType, logId),
            Fragment.lit(""))
        .query(AuditLogRow._rowParser.first())
        .runUnchecked(c);
  }
  ;

  @Override
  public List<AuditLogRow> selectByIds(AuditLogId[] logIds, Connection c) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : logIds) {
      fragments.add(Fragment.encode(AuditLogId.pgType, id));
    }
    ;
    return Fragment.interpolate(
            Fragment.lit(
                "select `log_id`, `table_name`, `record_id`, `action`, `old_values`, `new_values`,"
                    + " `changed_by`, `changed_at`, `client_ip`, `session_id` from `audit_log`"
                    + " where `log_id` in ("),
            Fragment.comma(fragments),
            Fragment.lit(")"))
        .query(AuditLogRow._rowParser.all())
        .runUnchecked(c);
  }
  ;

  @Override
  public Map<AuditLogId, AuditLogRow> selectByIdsTracked(AuditLogId[] logIds, Connection c) {
    HashMap<AuditLogId, AuditLogRow> ret = new HashMap<AuditLogId, AuditLogRow>();
    selectByIds(logIds, c).forEach(row -> ret.put(row.logId(), row));
    return ret;
  }
  ;

  @Override
  public UpdateBuilder<AuditLogFields, AuditLogRow> update() {
    return UpdateBuilder.of(
        "`audit_log`", AuditLogFields.structure(), AuditLogRow._rowParser, Dialect.MARIADB);
  }
  ;

  @Override
  public Boolean update(AuditLogRow row, Connection c) {
    AuditLogId logId = row.logId();
    ;
    return interpolate(
                Fragment.lit("update `audit_log`\nset `table_name` = "),
                Fragment.encode(MariaTypes.varchar, row.tableName()),
                Fragment.lit(",\n`record_id` = "),
                Fragment.encode(MariaTypes.varchar, row.recordId()),
                Fragment.lit(",\n`action` = "),
                Fragment.encode(MariaTypes.text, row.action()),
                Fragment.lit(",\n`old_values` = "),
                Fragment.encode(MariaTypes.longtext.opt(), row.oldValues()),
                Fragment.lit(",\n`new_values` = "),
                Fragment.encode(MariaTypes.longtext.opt(), row.newValues()),
                Fragment.lit(",\n`changed_by` = "),
                Fragment.encode(MariaTypes.varchar.opt(), row.changedBy()),
                Fragment.lit(",\n`changed_at` = "),
                Fragment.encode(MariaTypes.datetime, row.changedAt()),
                Fragment.lit(",\n`client_ip` = "),
                Fragment.encode(MariaTypes.inet6.opt(), row.clientIp()),
                Fragment.lit(",\n`session_id` = "),
                Fragment.encode(MariaTypes.varbinary.opt(), row.sessionId()),
                Fragment.lit("\nwhere `log_id` = "),
                Fragment.encode(AuditLogId.pgType, logId),
                Fragment.lit(""))
            .update()
            .runUnchecked(c)
        > 0;
  }
  ;

  @Override
  public AuditLogRow upsert(AuditLogRow unsaved, Connection c) {
    return interpolate(
            Fragment.lit(
                "INSERT INTO `audit_log`(`table_name`, `record_id`, `action`, `old_values`,"
                    + " `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`)\n"
                    + "VALUES ("),
            Fragment.encode(MariaTypes.varchar, unsaved.tableName()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.varchar, unsaved.recordId()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.text, unsaved.action()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.longtext.opt(), unsaved.oldValues()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.longtext.opt(), unsaved.newValues()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.varchar.opt(), unsaved.changedBy()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.datetime, unsaved.changedAt()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.inet6.opt(), unsaved.clientIp()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.varbinary.opt(), unsaved.sessionId()),
            Fragment.lit(
                ")\n"
                    + "ON DUPLICATE KEY UPDATE `table_name` = VALUES(`table_name`),\n"
                    + "`record_id` = VALUES(`record_id`),\n"
                    + "`action` = VALUES(`action`),\n"
                    + "`old_values` = VALUES(`old_values`),\n"
                    + "`new_values` = VALUES(`new_values`),\n"
                    + "`changed_by` = VALUES(`changed_by`),\n"
                    + "`changed_at` = VALUES(`changed_at`),\n"
                    + "`client_ip` = VALUES(`client_ip`),\n"
                    + "`session_id` = VALUES(`session_id`)\n"
                    + "RETURNING `log_id`, `table_name`, `record_id`, `action`, `old_values`,"
                    + " `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`"))
        .updateReturning(AuditLogRow._rowParser.exactlyOne())
        .runUnchecked(c);
  }
  ;

  @Override
  public List<AuditLogRow> upsertBatch(Iterator<AuditLogRow> unsaved, Connection c) {
    return interpolate(
            Fragment.lit(
                "INSERT INTO `audit_log`(`log_id`, `table_name`, `record_id`, `action`,"
                    + " `old_values`, `new_values`, `changed_by`, `changed_at`, `client_ip`,"
                    + " `session_id`)\n"
                    + "VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)\n"
                    + "ON DUPLICATE KEY UPDATE `table_name` = VALUES(`table_name`),\n"
                    + "`record_id` = VALUES(`record_id`),\n"
                    + "`action` = VALUES(`action`),\n"
                    + "`old_values` = VALUES(`old_values`),\n"
                    + "`new_values` = VALUES(`new_values`),\n"
                    + "`changed_by` = VALUES(`changed_by`),\n"
                    + "`changed_at` = VALUES(`changed_at`),\n"
                    + "`client_ip` = VALUES(`client_ip`),\n"
                    + "`session_id` = VALUES(`session_id`)\n"
                    + "RETURNING `log_id`, `table_name`, `record_id`, `action`, `old_values`,"
                    + " `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`"))
        .updateReturningEach(AuditLogRow._rowParser, unsaved)
        .runUnchecked(c);
  }
  ;
}
