/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.price_tiers;

import java.lang.RuntimeException;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record PriceTiersRepoMock(
  java.util.function.Function<PriceTiersRowUnsaved, PriceTiersRow> toRow,
  HashMap<PriceTiersId, PriceTiersRow> map
) implements PriceTiersRepo {
  public PriceTiersRepoMock(java.util.function.Function<PriceTiersRowUnsaved, PriceTiersRow> toRow) {
    this(toRow, new HashMap<PriceTiersId, PriceTiersRow>());
  };

  public PriceTiersRepoMock withToRow(java.util.function.Function<PriceTiersRowUnsaved, PriceTiersRow> toRow) {
    return new PriceTiersRepoMock(toRow, map);
  };

  public PriceTiersRepoMock withMap(HashMap<PriceTiersId, PriceTiersRow> map) {
    return new PriceTiersRepoMock(toRow, map);
  };

  @Override
  public DeleteBuilder<PriceTiersFields, PriceTiersRow> delete() {
    return new DeleteBuilderMock<>(PriceTiersFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.tierId(), id -> map.remove(id));
  };

  @Override
  public Boolean deleteById(
    PriceTiersId tierId,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(tierId)).isPresent();
  };

  @Override
  public Integer deleteByIds(
    PriceTiersId[] tierIds,
    Connection c
  ) {
    var count = 0;
    for (var id : tierIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    };
    };
    return count;
  };

  @Override
  public PriceTiersRow insert(
    PriceTiersRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.tierId())) {
      throw new RuntimeException(str("id $unsaved.tierId() already exists"));
    };
    map.put(unsaved.tierId(), unsaved);
    return unsaved;
  };

  @Override
  public PriceTiersRow insert(
    PriceTiersRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  @Override
  public SelectBuilder<PriceTiersFields, PriceTiersRow> select() {
    return new SelectBuilderMock<>(PriceTiersFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  @Override
  public List<PriceTiersRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  @Override
  public Optional<PriceTiersRow> selectById(
    PriceTiersId tierId,
    Connection c
  ) {
    return Optional.ofNullable(map.get(tierId));
  };

  @Override
  public List<PriceTiersRow> selectByIds(
    PriceTiersId[] tierIds,
    Connection c
  ) {
    var result = new ArrayList<PriceTiersRow>();
    for (var id : tierIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    };
    };
    return result;
  };

  @Override
  public Map<PriceTiersId, PriceTiersRow> selectByIdsTracked(
    PriceTiersId[] tierIds,
    Connection c
  ) {
    return selectByIds(tierIds, c).stream().collect(Collectors.toMap((PriceTiersRow row) -> row.tierId(), Function.identity()));
  };

  @Override
  public UpdateBuilder<PriceTiersFields, PriceTiersRow> update() {
    return new UpdateBuilderMock<>(PriceTiersFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  @Override
  public Boolean update(
    PriceTiersRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.tierId())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.tierId(), row);;
    };
    return shouldUpdate;
  };

  @Override
  public PriceTiersRow upsert(
    PriceTiersRow unsaved,
    Connection c
  ) {
    map.put(unsaved.tierId(), unsaved);
    return unsaved;
  };

  @Override
  public List<PriceTiersRow> upsertBatch(
    Iterator<PriceTiersRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<PriceTiersRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.tierId(), row);
      result.add(row);
    };
    return result;
  };
}