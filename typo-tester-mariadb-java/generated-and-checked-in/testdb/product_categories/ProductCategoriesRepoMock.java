/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.product_categories;

import java.lang.RuntimeException;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record ProductCategoriesRepoMock(
  java.util.function.Function<ProductCategoriesRowUnsaved, ProductCategoriesRow> toRow,
  HashMap<ProductCategoriesId, ProductCategoriesRow> map
) implements ProductCategoriesRepo {
  public ProductCategoriesRepoMock(java.util.function.Function<ProductCategoriesRowUnsaved, ProductCategoriesRow> toRow) {
    this(toRow, new HashMap<ProductCategoriesId, ProductCategoriesRow>());
  };

  public ProductCategoriesRepoMock withToRow(java.util.function.Function<ProductCategoriesRowUnsaved, ProductCategoriesRow> toRow) {
    return new ProductCategoriesRepoMock(toRow, map);
  };

  public ProductCategoriesRepoMock withMap(HashMap<ProductCategoriesId, ProductCategoriesRow> map) {
    return new ProductCategoriesRepoMock(toRow, map);
  };

  @Override
  public DeleteBuilder<ProductCategoriesFields, ProductCategoriesRow> delete() {
    return new DeleteBuilderMock<>(ProductCategoriesFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.compositeId(), id -> map.remove(id));
  };

  @Override
  public Boolean deleteById(
    ProductCategoriesId compositeId,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(compositeId)).isPresent();
  };

  @Override
  public Integer deleteByIds(
    ProductCategoriesId[] compositeIds,
    Connection c
  ) {
    var count = 0;
    for (var id : compositeIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    };
    };
    return count;
  };

  @Override
  public ProductCategoriesRow insert(
    ProductCategoriesRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.compositeId())) {
      throw new RuntimeException(str("id $unsaved.compositeId() already exists"));
    };
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  };

  @Override
  public ProductCategoriesRow insert(
    ProductCategoriesRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  @Override
  public SelectBuilder<ProductCategoriesFields, ProductCategoriesRow> select() {
    return new SelectBuilderMock<>(ProductCategoriesFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  @Override
  public List<ProductCategoriesRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  @Override
  public Optional<ProductCategoriesRow> selectById(
    ProductCategoriesId compositeId,
    Connection c
  ) {
    return Optional.ofNullable(map.get(compositeId));
  };

  @Override
  public List<ProductCategoriesRow> selectByIds(
    ProductCategoriesId[] compositeIds,
    Connection c
  ) {
    var result = new ArrayList<ProductCategoriesRow>();
    for (var id : compositeIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    };
    };
    return result;
  };

  @Override
  public Map<ProductCategoriesId, ProductCategoriesRow> selectByIdsTracked(
    ProductCategoriesId[] compositeIds,
    Connection c
  ) {
    return selectByIds(compositeIds, c).stream().collect(Collectors.toMap((ProductCategoriesRow row) -> row.compositeId(), Function.identity()));
  };

  @Override
  public UpdateBuilder<ProductCategoriesFields, ProductCategoriesRow> update() {
    return new UpdateBuilderMock<>(ProductCategoriesFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  @Override
  public Boolean update(
    ProductCategoriesRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.compositeId())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.compositeId(), row);;
    };
    return shouldUpdate;
  };

  @Override
  public ProductCategoriesRow upsert(
    ProductCategoriesRow unsaved,
    Connection c
  ) {
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  };

  @Override
  public List<ProductCategoriesRow> upsertBatch(
    Iterator<ProductCategoriesRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<ProductCategoriesRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      result.add(row);
    };
    return result;
  };
}