/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.shipments;

import java.lang.RuntimeException;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record ShipmentsRepoMock(
  java.util.function.Function<ShipmentsRowUnsaved, ShipmentsRow> toRow,
  HashMap<ShipmentsId, ShipmentsRow> map
) implements ShipmentsRepo {
  public ShipmentsRepoMock(java.util.function.Function<ShipmentsRowUnsaved, ShipmentsRow> toRow) {
    this(toRow, new HashMap<ShipmentsId, ShipmentsRow>());
  };

  public ShipmentsRepoMock withToRow(java.util.function.Function<ShipmentsRowUnsaved, ShipmentsRow> toRow) {
    return new ShipmentsRepoMock(toRow, map);
  };

  public ShipmentsRepoMock withMap(HashMap<ShipmentsId, ShipmentsRow> map) {
    return new ShipmentsRepoMock(toRow, map);
  };

  @Override
  public DeleteBuilder<ShipmentsFields, ShipmentsRow> delete() {
    return new DeleteBuilderMock<>(ShipmentsFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.shipmentId(), id -> map.remove(id));
  };

  @Override
  public Boolean deleteById(
    ShipmentsId shipmentId,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(shipmentId)).isPresent();
  };

  @Override
  public Integer deleteByIds(
    ShipmentsId[] shipmentIds,
    Connection c
  ) {
    var count = 0;
    for (var id : shipmentIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    };
    };
    return count;
  };

  @Override
  public ShipmentsRow insert(
    ShipmentsRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.shipmentId())) {
      throw new RuntimeException(str("id $unsaved.shipmentId() already exists"));
    };
    map.put(unsaved.shipmentId(), unsaved);
    return unsaved;
  };

  @Override
  public ShipmentsRow insert(
    ShipmentsRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  @Override
  public SelectBuilder<ShipmentsFields, ShipmentsRow> select() {
    return new SelectBuilderMock<>(ShipmentsFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  @Override
  public List<ShipmentsRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  @Override
  public Optional<ShipmentsRow> selectById(
    ShipmentsId shipmentId,
    Connection c
  ) {
    return Optional.ofNullable(map.get(shipmentId));
  };

  @Override
  public List<ShipmentsRow> selectByIds(
    ShipmentsId[] shipmentIds,
    Connection c
  ) {
    var result = new ArrayList<ShipmentsRow>();
    for (var id : shipmentIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    };
    };
    return result;
  };

  @Override
  public Map<ShipmentsId, ShipmentsRow> selectByIdsTracked(
    ShipmentsId[] shipmentIds,
    Connection c
  ) {
    return selectByIds(shipmentIds, c).stream().collect(Collectors.toMap((ShipmentsRow row) -> row.shipmentId(), Function.identity()));
  };

  @Override
  public UpdateBuilder<ShipmentsFields, ShipmentsRow> update() {
    return new UpdateBuilderMock<>(ShipmentsFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  @Override
  public Boolean update(
    ShipmentsRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.shipmentId())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.shipmentId(), row);;
    };
    return shouldUpdate;
  };

  @Override
  public ShipmentsRow upsert(
    ShipmentsRow unsaved,
    Connection c
  ) {
    map.put(unsaved.shipmentId(), unsaved);
    return unsaved;
  };

  @Override
  public List<ShipmentsRow> upsertBatch(
    Iterator<ShipmentsRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<ShipmentsRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.shipmentId(), row);
      result.add(row);
    };
    return result;
  };
}