/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.customer_addresses;

import java.lang.RuntimeException;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record CustomerAddressesRepoMock(
  java.util.function.Function<CustomerAddressesRowUnsaved, CustomerAddressesRow> toRow,
  HashMap<CustomerAddressesId, CustomerAddressesRow> map
) implements CustomerAddressesRepo {
  public CustomerAddressesRepoMock(java.util.function.Function<CustomerAddressesRowUnsaved, CustomerAddressesRow> toRow) {
    this(toRow, new HashMap<CustomerAddressesId, CustomerAddressesRow>());
  };

  public CustomerAddressesRepoMock withToRow(java.util.function.Function<CustomerAddressesRowUnsaved, CustomerAddressesRow> toRow) {
    return new CustomerAddressesRepoMock(toRow, map);
  };

  public CustomerAddressesRepoMock withMap(HashMap<CustomerAddressesId, CustomerAddressesRow> map) {
    return new CustomerAddressesRepoMock(toRow, map);
  };

  @Override
  public DeleteBuilder<CustomerAddressesFields, CustomerAddressesRow> delete() {
    return new DeleteBuilderMock<>(CustomerAddressesFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.addressId(), id -> map.remove(id));
  };

  @Override
  public Boolean deleteById(
    CustomerAddressesId addressId,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(addressId)).isPresent();
  };

  @Override
  public Integer deleteByIds(
    CustomerAddressesId[] addressIds,
    Connection c
  ) {
    var count = 0;
    for (var id : addressIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    };
    };
    return count;
  };

  @Override
  public CustomerAddressesRow insert(
    CustomerAddressesRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.addressId())) {
      throw new RuntimeException(str("id $unsaved.addressId() already exists"));
    };
    map.put(unsaved.addressId(), unsaved);
    return unsaved;
  };

  @Override
  public CustomerAddressesRow insert(
    CustomerAddressesRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  @Override
  public SelectBuilder<CustomerAddressesFields, CustomerAddressesRow> select() {
    return new SelectBuilderMock<>(CustomerAddressesFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  @Override
  public List<CustomerAddressesRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  @Override
  public Optional<CustomerAddressesRow> selectById(
    CustomerAddressesId addressId,
    Connection c
  ) {
    return Optional.ofNullable(map.get(addressId));
  };

  @Override
  public List<CustomerAddressesRow> selectByIds(
    CustomerAddressesId[] addressIds,
    Connection c
  ) {
    var result = new ArrayList<CustomerAddressesRow>();
    for (var id : addressIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    };
    };
    return result;
  };

  @Override
  public Map<CustomerAddressesId, CustomerAddressesRow> selectByIdsTracked(
    CustomerAddressesId[] addressIds,
    Connection c
  ) {
    return selectByIds(addressIds, c).stream().collect(Collectors.toMap((CustomerAddressesRow row) -> row.addressId(), Function.identity()));
  };

  @Override
  public UpdateBuilder<CustomerAddressesFields, CustomerAddressesRow> update() {
    return new UpdateBuilderMock<>(CustomerAddressesFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  @Override
  public Boolean update(
    CustomerAddressesRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.addressId())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.addressId(), row);;
    };
    return shouldUpdate;
  };

  @Override
  public CustomerAddressesRow upsert(
    CustomerAddressesRow unsaved,
    Connection c
  ) {
    map.put(unsaved.addressId(), unsaved);
    return unsaved;
  };

  @Override
  public List<CustomerAddressesRow> upsertBatch(
    Iterator<CustomerAddressesRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<CustomerAddressesRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.addressId(), row);
      result.add(row);
    };
    return result;
  };
}