/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.payments;

import java.lang.RuntimeException;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record PaymentsRepoMock(
  java.util.function.Function<PaymentsRowUnsaved, PaymentsRow> toRow,
  HashMap<PaymentsId, PaymentsRow> map
) implements PaymentsRepo {
  public PaymentsRepoMock(java.util.function.Function<PaymentsRowUnsaved, PaymentsRow> toRow) {
    this(toRow, new HashMap<PaymentsId, PaymentsRow>());
  };

  public PaymentsRepoMock withToRow(java.util.function.Function<PaymentsRowUnsaved, PaymentsRow> toRow) {
    return new PaymentsRepoMock(toRow, map);
  };

  public PaymentsRepoMock withMap(HashMap<PaymentsId, PaymentsRow> map) {
    return new PaymentsRepoMock(toRow, map);
  };

  @Override
  public DeleteBuilder<PaymentsFields, PaymentsRow> delete() {
    return new DeleteBuilderMock<>(PaymentsFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.paymentId(), id -> map.remove(id));
  };

  @Override
  public Boolean deleteById(
    PaymentsId paymentId,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(paymentId)).isPresent();
  };

  @Override
  public Integer deleteByIds(
    PaymentsId[] paymentIds,
    Connection c
  ) {
    var count = 0;
    for (var id : paymentIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    };
    };
    return count;
  };

  @Override
  public PaymentsRow insert(
    PaymentsRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.paymentId())) {
      throw new RuntimeException(str("id $unsaved.paymentId() already exists"));
    };
    map.put(unsaved.paymentId(), unsaved);
    return unsaved;
  };

  @Override
  public PaymentsRow insert(
    PaymentsRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  @Override
  public SelectBuilder<PaymentsFields, PaymentsRow> select() {
    return new SelectBuilderMock<>(PaymentsFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  @Override
  public List<PaymentsRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  @Override
  public Optional<PaymentsRow> selectById(
    PaymentsId paymentId,
    Connection c
  ) {
    return Optional.ofNullable(map.get(paymentId));
  };

  @Override
  public List<PaymentsRow> selectByIds(
    PaymentsId[] paymentIds,
    Connection c
  ) {
    var result = new ArrayList<PaymentsRow>();
    for (var id : paymentIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    };
    };
    return result;
  };

  @Override
  public Map<PaymentsId, PaymentsRow> selectByIdsTracked(
    PaymentsId[] paymentIds,
    Connection c
  ) {
    return selectByIds(paymentIds, c).stream().collect(Collectors.toMap((PaymentsRow row) -> row.paymentId(), Function.identity()));
  };

  @Override
  public UpdateBuilder<PaymentsFields, PaymentsRow> update() {
    return new UpdateBuilderMock<>(PaymentsFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  @Override
  public Boolean update(
    PaymentsRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.paymentId())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.paymentId(), row);;
    };
    return shouldUpdate;
  };

  @Override
  public PaymentsRow upsert(
    PaymentsRow unsaved,
    Connection c
  ) {
    map.put(unsaved.paymentId(), unsaved);
    return unsaved;
  };

  @Override
  public List<PaymentsRow> upsertBatch(
    Iterator<PaymentsRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<PaymentsRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.paymentId(), row);
      result.add(row);
    };
    return result;
  };
}