/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.orders;

import com.fasterxml.jackson.annotation.JsonProperty;
import java.math.BigDecimal;
import java.time.LocalDateTime;
import java.util.Optional;
import testdb.customer_addresses.CustomerAddressesId;
import testdb.customers.CustomersId;
import testdb.customtypes.Defaulted;
import testdb.promotions.PromotionsId;
import typo.data.maria.Inet6;
import typo.runtime.MariaText;
import typo.runtime.MariaTypes;
import typo.runtime.RowParser;
import typo.runtime.RowParsers;

/** Table: orders
  * Primary key: order_id
  */
public record OrdersRow(
  /** 
    * AUTO_INCREMENT
    */
  @JsonProperty("order_id") OrdersId orderId,
  /**  */
  @JsonProperty("order_number") String orderNumber,
  /** 
    * Points to {@link testdb.customers.CustomersRow#customerId()}
    */
  @JsonProperty("customer_id") CustomersId customerId,
  /** 
    * Default: 'pending'
    */
  @JsonProperty("order_status") String orderStatus,
  /** 
    * Default: 'pending'
    */
  @JsonProperty("payment_status") String paymentStatus,
  /** 
    * Default: NULL
    * Points to {@link testdb.customer_addresses.CustomerAddressesRow#addressId()}
    */
  @JsonProperty("shipping_address_id") Optional<CustomerAddressesId> shippingAddressId,
  /** 
    * Default: NULL
    * Points to {@link testdb.customer_addresses.CustomerAddressesRow#addressId()}
    */
  @JsonProperty("billing_address_id") Optional<CustomerAddressesId> billingAddressId,
  /**  */
  BigDecimal subtotal,
  /** 
    * Default: 0.0000
    */
  @JsonProperty("shipping_cost") BigDecimal shippingCost,
  /** 
    * Default: 0.0000
    */
  @JsonProperty("tax_amount") BigDecimal taxAmount,
  /** 
    * Default: 0.0000
    */
  @JsonProperty("discount_amount") BigDecimal discountAmount,
  /**  */
  @JsonProperty("total_amount") BigDecimal totalAmount,
  /** 
    * Default: 'USD'
    */
  @JsonProperty("currency_code") String currencyCode,
  /** 
    * Default: NULL
    * Points to {@link testdb.promotions.PromotionsRow#promotionId()}
    */
  @JsonProperty("promotion_id") Optional<PromotionsId> promotionId,
  /** 
    * Default: NULL
    */
  Optional<String> notes,
  /** 
    * Default: NULL
    */
  @JsonProperty("internal_notes") Optional<String> internalNotes,
  /** 
    * Default: NULL
    */
  @JsonProperty("ip_address") Optional<Inet6> ipAddress,
  /** 
    * Default: NULL
    */
  @JsonProperty("user_agent") Optional<String> userAgent,
  /** 
    * Default: current_timestamp(6)
    */
  @JsonProperty("ordered_at") LocalDateTime orderedAt,
  /** 
    * Default: NULL
    */
  @JsonProperty("confirmed_at") Optional<LocalDateTime> confirmedAt,
  /** 
    * Default: NULL
    */
  @JsonProperty("shipped_at") Optional<LocalDateTime> shippedAt,
  /** 
    * Default: NULL
    */
  @JsonProperty("delivered_at") Optional<LocalDateTime> deliveredAt
) {
  /** 
    * AUTO_INCREMENT
    */
  public OrdersRow withOrderId(OrdersId orderId) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  /**  */
  public OrdersRow withOrderNumber(String orderNumber) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  /** 
    * Points to {@link testdb.customers.CustomersRow#customerId()}
    */
  public OrdersRow withCustomerId(CustomersId customerId) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  /** 
    * Default: 'pending'
    */
  public OrdersRow withOrderStatus(String orderStatus) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  /** 
    * Default: 'pending'
    */
  public OrdersRow withPaymentStatus(String paymentStatus) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  /** 
    * Default: NULL
    * Points to {@link testdb.customer_addresses.CustomerAddressesRow#addressId()}
    */
  public OrdersRow withShippingAddressId(Optional<CustomerAddressesId> shippingAddressId) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  /** 
    * Default: NULL
    * Points to {@link testdb.customer_addresses.CustomerAddressesRow#addressId()}
    */
  public OrdersRow withBillingAddressId(Optional<CustomerAddressesId> billingAddressId) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  /**  */
  public OrdersRow withSubtotal(BigDecimal subtotal) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  /** 
    * Default: 0.0000
    */
  public OrdersRow withShippingCost(BigDecimal shippingCost) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  /** 
    * Default: 0.0000
    */
  public OrdersRow withTaxAmount(BigDecimal taxAmount) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  /** 
    * Default: 0.0000
    */
  public OrdersRow withDiscountAmount(BigDecimal discountAmount) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  /**  */
  public OrdersRow withTotalAmount(BigDecimal totalAmount) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  /** 
    * Default: 'USD'
    */
  public OrdersRow withCurrencyCode(String currencyCode) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  /** 
    * Default: NULL
    * Points to {@link testdb.promotions.PromotionsRow#promotionId()}
    */
  public OrdersRow withPromotionId(Optional<PromotionsId> promotionId) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  /** 
    * Default: NULL
    */
  public OrdersRow withNotes(Optional<String> notes) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  /** 
    * Default: NULL
    */
  public OrdersRow withInternalNotes(Optional<String> internalNotes) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  /** 
    * Default: NULL
    */
  public OrdersRow withIpAddress(Optional<Inet6> ipAddress) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  /** 
    * Default: NULL
    */
  public OrdersRow withUserAgent(Optional<String> userAgent) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  /** 
    * Default: current_timestamp(6)
    */
  public OrdersRow withOrderedAt(LocalDateTime orderedAt) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  /** 
    * Default: NULL
    */
  public OrdersRow withConfirmedAt(Optional<LocalDateTime> confirmedAt) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  /** 
    * Default: NULL
    */
  public OrdersRow withShippedAt(Optional<LocalDateTime> shippedAt) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  /** 
    * Default: NULL
    */
  public OrdersRow withDeliveredAt(Optional<LocalDateTime> deliveredAt) {
    return new OrdersRow(orderId, orderNumber, customerId, orderStatus, paymentStatus, shippingAddressId, billingAddressId, subtotal, shippingCost, taxAmount, discountAmount, totalAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };

  static RowParser<OrdersRow> _rowParser = RowParsers.of(OrdersId.pgType, MariaTypes.varchar, CustomersId.pgType, MariaTypes.text, MariaTypes.text, CustomerAddressesId.pgType.opt(), CustomerAddressesId.pgType.opt(), MariaTypes.decimal, MariaTypes.decimal, MariaTypes.decimal, MariaTypes.decimal, MariaTypes.decimal, MariaTypes.char_, PromotionsId.pgType.opt(), MariaTypes.text.opt(), MariaTypes.mediumtext.opt(), MariaTypes.inet6.opt(), MariaTypes.varchar.opt(), MariaTypes.datetime, MariaTypes.datetime.opt(), MariaTypes.datetime.opt(), MariaTypes.datetime.opt(), OrdersRow::new, row -> new Object[]{row.orderId(), row.orderNumber(), row.customerId(), row.orderStatus(), row.paymentStatus(), row.shippingAddressId(), row.billingAddressId(), row.subtotal(), row.shippingCost(), row.taxAmount(), row.discountAmount(), row.totalAmount(), row.currencyCode(), row.promotionId(), row.notes(), row.internalNotes(), row.ipAddress(), row.userAgent(), row.orderedAt(), row.confirmedAt(), row.shippedAt(), row.deliveredAt()});;

  static public MariaText<OrdersRow> mariaText =
    MariaText.from(_rowParser);

  public OrdersId id() {
    return orderId;
  };

  public OrdersRowUnsaved toUnsavedRow(
    Defaulted<String> orderStatus,
    Defaulted<String> paymentStatus,
    Defaulted<Optional<CustomerAddressesId>> shippingAddressId,
    Defaulted<Optional<CustomerAddressesId>> billingAddressId,
    Defaulted<BigDecimal> shippingCost,
    Defaulted<BigDecimal> taxAmount,
    Defaulted<BigDecimal> discountAmount,
    Defaulted<String> currencyCode,
    Defaulted<Optional<PromotionsId>> promotionId,
    Defaulted<Optional<String>> notes,
    Defaulted<Optional<String>> internalNotes,
    Defaulted<Optional<Inet6>> ipAddress,
    Defaulted<Optional<String>> userAgent,
    Defaulted<LocalDateTime> orderedAt,
    Defaulted<Optional<LocalDateTime>> confirmedAt,
    Defaulted<Optional<LocalDateTime>> shippedAt,
    Defaulted<Optional<LocalDateTime>> deliveredAt
  ) {
    return new OrdersRowUnsaved(orderNumber, customerId, subtotal, totalAmount, orderStatus, paymentStatus, shippingAddressId, billingAddressId, shippingCost, taxAmount, discountAmount, currencyCode, promotionId, notes, internalNotes, ipAddress, userAgent, orderedAt, confirmedAt, shippedAt, deliveredAt);
  };
}