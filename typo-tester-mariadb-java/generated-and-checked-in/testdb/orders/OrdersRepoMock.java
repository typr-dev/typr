/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.orders;

import java.lang.RuntimeException;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record OrdersRepoMock(
  java.util.function.Function<OrdersRowUnsaved, OrdersRow> toRow,
  HashMap<OrdersId, OrdersRow> map
) implements OrdersRepo {
  public OrdersRepoMock(java.util.function.Function<OrdersRowUnsaved, OrdersRow> toRow) {
    this(toRow, new HashMap<OrdersId, OrdersRow>());
  };

  public OrdersRepoMock withToRow(java.util.function.Function<OrdersRowUnsaved, OrdersRow> toRow) {
    return new OrdersRepoMock(toRow, map);
  };

  public OrdersRepoMock withMap(HashMap<OrdersId, OrdersRow> map) {
    return new OrdersRepoMock(toRow, map);
  };

  @Override
  public DeleteBuilder<OrdersFields, OrdersRow> delete() {
    return new DeleteBuilderMock<>(OrdersFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.orderId(), id -> map.remove(id));
  };

  @Override
  public Boolean deleteById(
    OrdersId orderId,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(orderId)).isPresent();
  };

  @Override
  public Integer deleteByIds(
    OrdersId[] orderIds,
    Connection c
  ) {
    var count = 0;
    for (var id : orderIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    };
    };
    return count;
  };

  @Override
  public OrdersRow insert(
    OrdersRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.orderId())) {
      throw new RuntimeException(str("id $unsaved.orderId() already exists"));
    };
    map.put(unsaved.orderId(), unsaved);
    return unsaved;
  };

  @Override
  public OrdersRow insert(
    OrdersRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  @Override
  public SelectBuilder<OrdersFields, OrdersRow> select() {
    return new SelectBuilderMock<>(OrdersFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  @Override
  public List<OrdersRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  @Override
  public Optional<OrdersRow> selectById(
    OrdersId orderId,
    Connection c
  ) {
    return Optional.ofNullable(map.get(orderId));
  };

  @Override
  public List<OrdersRow> selectByIds(
    OrdersId[] orderIds,
    Connection c
  ) {
    var result = new ArrayList<OrdersRow>();
    for (var id : orderIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    };
    };
    return result;
  };

  @Override
  public Map<OrdersId, OrdersRow> selectByIdsTracked(
    OrdersId[] orderIds,
    Connection c
  ) {
    return selectByIds(orderIds, c).stream().collect(Collectors.toMap((OrdersRow row) -> row.orderId(), Function.identity()));
  };

  @Override
  public Optional<OrdersRow> selectByUniqueOrderNumber(
    String orderNumber,
    Connection c
  ) {
    return new ArrayList<>(map.values()).stream().filter(v -> orderNumber.equals(v.orderNumber())).findFirst();
  };

  @Override
  public UpdateBuilder<OrdersFields, OrdersRow> update() {
    return new UpdateBuilderMock<>(OrdersFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  @Override
  public Boolean update(
    OrdersRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.orderId())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.orderId(), row);;
    };
    return shouldUpdate;
  };

  @Override
  public OrdersRow upsert(
    OrdersRow unsaved,
    Connection c
  ) {
    map.put(unsaved.orderId(), unsaved);
    return unsaved;
  };

  @Override
  public List<OrdersRow> upsertBatch(
    Iterator<OrdersRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<OrdersRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.orderId(), row);
      result.add(row);
    };
    return result;
  };
}