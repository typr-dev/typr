/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.orders;

import static typr.runtime.Fragment.interpolate;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import testdb.customers.CustomersId;
import typr.dsl.DeleteBuilder;
import typr.dsl.Dialect;
import typr.dsl.SelectBuilder;
import typr.dsl.UpdateBuilder;
import typr.runtime.Fragment;
import typr.runtime.SqlServerTypes;

public class OrdersRepoImpl implements OrdersRepo {
  @Override
  public DeleteBuilder<OrdersFields, OrdersRow> delete() {
    return DeleteBuilder.of("[orders]", OrdersFields.structure(), Dialect.SQLSERVER);
  }
  ;

  @Override
  public Boolean deleteById(OrdersId orderId, Connection c) {
    return interpolate(
                Fragment.lit("delete from [orders] where [order_id] = "),
                Fragment.encode(OrdersId.sqlServerType, orderId),
                Fragment.lit(""))
            .update()
            .runUnchecked(c)
        > 0;
  }
  ;

  @Override
  public Integer deleteByIds(OrdersId[] orderIds, Connection c) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : orderIds) {
      fragments.add(Fragment.encode(OrdersId.sqlServerType, id));
    }
    ;
    return Fragment.interpolate(
            Fragment.lit("delete from [orders] where [order_id] in ("),
            Fragment.comma(fragments),
            Fragment.lit(")"))
        .update()
        .runUnchecked(c);
  }
  ;

  @Override
  public OrdersRow insert(OrdersRow unsaved, Connection c) {
    return interpolate(
            Fragment.lit(
                "insert into [orders]([customer_id], [order_date], [total_amount])\n"
                    + "OUTPUT INSERTED.[order_id], INSERTED.[customer_id], INSERTED.[order_date],"
                    + " INSERTED.[total_amount]\n"
                    + "values ("),
            Fragment.encode(CustomersId.sqlServerType, unsaved.customerId()),
            Fragment.lit(", "),
            Fragment.encode(SqlServerTypes.datetime2.opt(), unsaved.orderDate()),
            Fragment.lit(", "),
            Fragment.encode(SqlServerTypes.money, unsaved.totalAmount()),
            Fragment.lit(")\n"))
        .updateReturning(OrdersRow._rowParser.exactlyOne())
        .runUnchecked(c);
  }
  ;

  @Override
  public OrdersRow insert(OrdersRowUnsaved unsaved, Connection c) {
    ArrayList<Fragment> columns = new ArrayList<>();
    ;
    ArrayList<Fragment> values = new ArrayList<>();
    ;
    columns.add(Fragment.lit("[customer_id]"));
    values.add(
        interpolate(
            Fragment.encode(CustomersId.sqlServerType, unsaved.customerId()), Fragment.lit("")));
    columns.add(Fragment.lit("[total_amount]"));
    values.add(
        interpolate(
            Fragment.encode(SqlServerTypes.money, unsaved.totalAmount()), Fragment.lit("")));
    unsaved
        .orderDate()
        .visit(
            () -> {},
            value -> {
              columns.add(Fragment.lit("[order_date]"));
              values.add(
                  interpolate(
                      Fragment.encode(SqlServerTypes.datetime2.opt(), value), Fragment.lit("")));
            });
    ;
    Fragment q =
        interpolate(
            Fragment.lit("insert into [orders]("),
            Fragment.comma(columns),
            Fragment.lit(
                ")\n"
                    + "OUTPUT INSERTED.[order_id], INSERTED.[customer_id], INSERTED.[order_date],"
                    + " INSERTED.[total_amount]\n"
                    + "values ("),
            Fragment.comma(values),
            Fragment.lit(")\n"));
    ;
    return q.updateReturning(OrdersRow._rowParser.exactlyOne()).runUnchecked(c);
  }
  ;

  @Override
  public SelectBuilder<OrdersFields, OrdersRow> select() {
    return SelectBuilder.of(
        "[orders]", OrdersFields.structure(), OrdersRow._rowParser, Dialect.SQLSERVER);
  }
  ;

  @Override
  public List<OrdersRow> selectAll(Connection c) {
    return interpolate(
            Fragment.lit(
                "select [order_id], [customer_id], [order_date], [total_amount]\nfrom [orders]\n"))
        .query(OrdersRow._rowParser.all())
        .runUnchecked(c);
  }
  ;

  @Override
  public Optional<OrdersRow> selectById(OrdersId orderId, Connection c) {
    return interpolate(
            Fragment.lit(
                "select [order_id], [customer_id], [order_date], [total_amount]\n"
                    + "from [orders]\n"
                    + "where [order_id] = "),
            Fragment.encode(OrdersId.sqlServerType, orderId),
            Fragment.lit(""))
        .query(OrdersRow._rowParser.first())
        .runUnchecked(c);
  }
  ;

  @Override
  public List<OrdersRow> selectByIds(OrdersId[] orderIds, Connection c) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : orderIds) {
      fragments.add(Fragment.encode(OrdersId.sqlServerType, id));
    }
    ;
    return Fragment.interpolate(
            Fragment.lit(
                "select [order_id], [customer_id], [order_date], [total_amount] from [orders] where"
                    + " [order_id] in ("),
            Fragment.comma(fragments),
            Fragment.lit(")"))
        .query(OrdersRow._rowParser.all())
        .runUnchecked(c);
  }
  ;

  @Override
  public Map<OrdersId, OrdersRow> selectByIdsTracked(OrdersId[] orderIds, Connection c) {
    HashMap<OrdersId, OrdersRow> ret = new HashMap<OrdersId, OrdersRow>();
    selectByIds(orderIds, c).forEach(row -> ret.put(row.orderId(), row));
    return ret;
  }
  ;

  @Override
  public UpdateBuilder<OrdersFields, OrdersRow> update() {
    return UpdateBuilder.of(
        "[orders]", OrdersFields.structure(), OrdersRow._rowParser, Dialect.SQLSERVER);
  }
  ;

  @Override
  public Boolean update(OrdersRow row, Connection c) {
    OrdersId orderId = row.orderId();
    ;
    return interpolate(
                Fragment.lit("update [orders]\nset [customer_id] = "),
                Fragment.encode(CustomersId.sqlServerType, row.customerId()),
                Fragment.lit(",\n[order_date] = "),
                Fragment.encode(SqlServerTypes.datetime2.opt(), row.orderDate()),
                Fragment.lit(",\n[total_amount] = "),
                Fragment.encode(SqlServerTypes.money, row.totalAmount()),
                Fragment.lit("\nwhere [order_id] = "),
                Fragment.encode(OrdersId.sqlServerType, orderId),
                Fragment.lit(""))
            .update()
            .runUnchecked(c)
        > 0;
  }
  ;

  @Override
  public OrdersRow upsert(OrdersRow unsaved, Connection c) {
    return interpolate(
            Fragment.lit("MERGE INTO [orders] AS target\nUSING (VALUES ("),
            Fragment.encode(CustomersId.sqlServerType, unsaved.customerId()),
            Fragment.lit(", "),
            Fragment.encode(SqlServerTypes.datetime2.opt(), unsaved.orderDate()),
            Fragment.lit(", "),
            Fragment.encode(SqlServerTypes.money, unsaved.totalAmount()),
            Fragment.lit(
                ")) AS source([customer_id], [order_date], [total_amount])\n"
                    + "ON target.[order_id] = source.[order_id]\n"
                    + "WHEN MATCHED THEN UPDATE SET [customer_id] = source.[customer_id],\n"
                    + "[order_date] = source.[order_date],\n"
                    + "[total_amount] = source.[total_amount]\n"
                    + "WHEN NOT MATCHED THEN INSERT ([customer_id], [order_date], [total_amount])"
                    + " VALUES ("),
            Fragment.encode(CustomersId.sqlServerType, unsaved.customerId()),
            Fragment.lit(", "),
            Fragment.encode(SqlServerTypes.datetime2.opt(), unsaved.orderDate()),
            Fragment.lit(", "),
            Fragment.encode(SqlServerTypes.money, unsaved.totalAmount()),
            Fragment.lit(
                ")\n"
                    + "OUTPUT INSERTED.[order_id], INSERTED.[customer_id], INSERTED.[order_date],"
                    + " INSERTED.[total_amount];"))
        .updateReturning(OrdersRow._rowParser.exactlyOne())
        .runUnchecked(c);
  }
  ;

  @Override
  public List<OrdersRow> upsertBatch(Iterator<OrdersRow> unsaved, Connection c) {
    return interpolate(
            Fragment.lit(
                "MERGE INTO [orders] AS target\n"
                    + "USING (VALUES (?, ?, ?, ?)) AS source([order_id], [customer_id],"
                    + " [order_date], [total_amount])\n"
                    + "ON target.[order_id] = source.[order_id]\n"
                    + "WHEN MATCHED THEN UPDATE SET [customer_id] = source.[customer_id],\n"
                    + "[order_date] = source.[order_date],\n"
                    + "[total_amount] = source.[total_amount]\n"
                    + "WHEN NOT MATCHED THEN INSERT ([order_id], [customer_id], [order_date],"
                    + " [total_amount]) VALUES (?, ?, ?, ?)\n"
                    + "OUTPUT INSERTED.[order_id], INSERTED.[customer_id], INSERTED.[order_date],"
                    + " INSERTED.[total_amount];"))
        .updateReturningEach(OrdersRow._rowParser, unsaved)
        .runUnchecked(c);
  }
  ;
}
