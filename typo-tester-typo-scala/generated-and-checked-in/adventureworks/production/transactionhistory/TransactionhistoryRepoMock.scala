/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.transactionhistory

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.HashMap
import java.util.Optional
import java.util.function.Function
import java.util.stream.Collectors
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

case class TransactionhistoryRepoMock(
  toRow: TransactionhistoryRowUnsaved => TransactionhistoryRow,
  map: HashMap[TransactionhistoryId, TransactionhistoryRow] = new HashMap[TransactionhistoryId, TransactionhistoryRow]()
) extends TransactionhistoryRepo {
  override def delete: DeleteBuilder[TransactionhistoryFields, TransactionhistoryRow] = {
    new DeleteBuilderMock(
      TransactionhistoryFields.structure,
      () => new ArrayList(map.values()),
      DeleteParams.empty(),
      row => row.transactionid,
      id => map.remove(id): @scala.annotation.nowarn
    )
  }

  override def deleteById(transactionid: TransactionhistoryId)(using c: Connection): java.lang.Boolean = Optional.ofNullable(map.remove(transactionid)).isPresent()

  override def deleteByIds(transactionids: Array[TransactionhistoryId])(using c: Connection): Integer = {
    var count = 0
    transactionids.foreach { id => if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: TransactionhistoryRow)(using c: Connection): TransactionhistoryRow = {
    if (map.containsKey(unsaved.transactionid)) {
      throw new RuntimeException(s"id $unsaved.transactionid already exists")
    }
    map.put(unsaved.transactionid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: TransactionhistoryRowUnsaved)(using c: Connection): TransactionhistoryRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: java.util.Iterator[TransactionhistoryRow],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.transactionid, row): @scala.annotation.nowarn
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: java.util.Iterator[TransactionhistoryRowUnsaved],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map.put(row.transactionid, row): @scala.annotation.nowarn
      count = count + 1L
    }
    return count
  }

  override def select: SelectBuilder[TransactionhistoryFields, TransactionhistoryRow] = new SelectBuilderMock(TransactionhistoryFields.structure, () => new ArrayList(map.values()), SelectParams.empty())

  override def selectAll(using c: Connection): java.util.List[TransactionhistoryRow] = new ArrayList(map.values())

  override def selectById(transactionid: TransactionhistoryId)(using c: Connection): Optional[TransactionhistoryRow] = Optional.ofNullable(map.get(transactionid))

  override def selectByIds(transactionids: Array[TransactionhistoryId])(using c: Connection): java.util.List[TransactionhistoryRow] = {
    val result = new ArrayList[TransactionhistoryRow]()
    transactionids.foreach { id => val opt = Optional.ofNullable(map.get(id)); if (opt.isPresent()) {
      result.add(opt.get()): @scala.annotation.nowarn
    } }
    return result
  }

  override def selectByIdsTracked(transactionids: Array[TransactionhistoryId])(using c: Connection): java.util.Map[TransactionhistoryId, TransactionhistoryRow] = selectByIds(transactionids)(using c).stream().collect(Collectors.toMap((row: TransactionhistoryRow) => row.transactionid, Function.identity()))

  override def update: UpdateBuilder[TransactionhistoryFields, TransactionhistoryRow] = {
    new UpdateBuilderMock(
      TransactionhistoryFields.structure,
      () => new ArrayList(map.values()),
      UpdateParams.empty(),
      row => row
    )
  }

  override def update(row: TransactionhistoryRow)(using c: Connection): java.lang.Boolean = {
    val shouldUpdate = Optional.ofNullable(map.get(row.transactionid)).filter(oldRow => (oldRow != row)).isPresent()
    if (shouldUpdate) {
      map.put(row.transactionid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: TransactionhistoryRow)(using c: Connection): TransactionhistoryRow = {
    map.put(unsaved.transactionid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: java.util.Iterator[TransactionhistoryRow])(using c: Connection): java.util.List[TransactionhistoryRow] = {
    val result = new ArrayList[TransactionhistoryRow]()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.transactionid, row): @scala.annotation.nowarn
      result.add(row): @scala.annotation.nowarn
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: java.util.Iterator[TransactionhistoryRow],
    batchSize: Integer = 10000
  )(using c: Connection): Integer = {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.transactionid, row): @scala.annotation.nowarn
      count = count + 1
    }
    return count
  }
}