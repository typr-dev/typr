/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.payments

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class PaymentsRepoMock(
  toRow: PaymentsRowUnsaved => PaymentsRow,
  map: scala.collection.mutable.Map[PaymentsId, PaymentsRow] = scala.collection.mutable.Map.empty[PaymentsId, PaymentsRow]
) extends PaymentsRepo {
  override def delete: DeleteBuilder[PaymentsFields, PaymentsRow] = DeleteBuilderMock(PaymentsFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.paymentId, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(paymentId: PaymentsId)(using c: Connection): Boolean = map.remove(paymentId).isDefined

  override def deleteByIds(paymentIds: Array[PaymentsId])(using c: Connection): Int = {
    var count = 0
    paymentIds.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: PaymentsRow)(using c: Connection): PaymentsRow = {
    if (map.contains(unsaved.paymentId)) {
      throw new RuntimeException(s"id ${unsaved.paymentId} already exists")
    }
    map.put(unsaved.paymentId, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: PaymentsRowUnsaved)(using c: Connection): PaymentsRow = insert(toRow(unsaved))(using c)

  override def select: SelectBuilder[PaymentsFields, PaymentsRow] = SelectBuilderMock(PaymentsFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[PaymentsRow] = map.values.toList

  override def selectById(paymentId: PaymentsId)(using c: Connection): Option[PaymentsRow] = map.get(paymentId)

  override def selectByIds(paymentIds: Array[PaymentsId])(using c: Connection): List[PaymentsRow] = paymentIds.flatMap(map.get(_)).toList

  override def selectByIdsTracked(paymentIds: Array[PaymentsId])(using c: Connection): Map[PaymentsId, PaymentsRow] = selectByIds(paymentIds)(using c).map(x => (((row: PaymentsRow) => row.paymentId).apply(x), x)).toMap

  override def update: UpdateBuilder[PaymentsFields, PaymentsRow] = UpdateBuilderMock(PaymentsFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: PaymentsRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.paymentId).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.paymentId, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: PaymentsRow)(using c: Connection): PaymentsRow = {
    map.put(unsaved.paymentId, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[PaymentsRow])(using c: Connection): List[PaymentsRow] = {
    unsaved.map { row =>
      map.put(row.paymentId, row): @scala.annotation.nowarn
      row
    }.toList
  }
}