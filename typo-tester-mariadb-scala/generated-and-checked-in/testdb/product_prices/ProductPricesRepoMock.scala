/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.product_prices

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.HashMap
import java.util.Optional
import java.util.function.Function
import java.util.stream.Collectors
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

case class ProductPricesRepoMock(
  toRow: ProductPricesRowUnsaved => ProductPricesRow,
  map: HashMap[ProductPricesId, ProductPricesRow] = new HashMap[ProductPricesId, ProductPricesRow]()
) extends ProductPricesRepo {
  override def delete: DeleteBuilder[ProductPricesFields, ProductPricesRow] = {
    new DeleteBuilderMock(
      ProductPricesFields.structure,
      () => new ArrayList(map.values()),
      DeleteParams.empty(),
      row => row.priceId,
      id => map.remove(id): @scala.annotation.nowarn
    )
  }

  override def deleteById(priceId: ProductPricesId)(using c: Connection): java.lang.Boolean = Optional.ofNullable(map.remove(priceId)).isPresent()

  override def deleteByIds(priceIds: Array[ProductPricesId])(using c: Connection): Integer = {
    var count = 0
    priceIds.foreach { id => if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: ProductPricesRow)(using c: Connection): ProductPricesRow = {
    if (map.containsKey(unsaved.priceId)) {
      throw new RuntimeException(s"id $unsaved.priceId already exists")
    }
    map.put(unsaved.priceId, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: ProductPricesRowUnsaved)(using c: Connection): ProductPricesRow = insert(toRow(unsaved))(using c)

  override def select: SelectBuilder[ProductPricesFields, ProductPricesRow] = new SelectBuilderMock(ProductPricesFields.structure, () => new ArrayList(map.values()), SelectParams.empty())

  override def selectAll(using c: Connection): java.util.List[ProductPricesRow] = new ArrayList(map.values())

  override def selectById(priceId: ProductPricesId)(using c: Connection): Optional[ProductPricesRow] = Optional.ofNullable(map.get(priceId))

  override def selectByIds(priceIds: Array[ProductPricesId])(using c: Connection): java.util.List[ProductPricesRow] = {
    val result = new ArrayList[ProductPricesRow]()
    priceIds.foreach { id => val opt = Optional.ofNullable(map.get(id)); if (opt.isPresent()) {
      result.add(opt.get()): @scala.annotation.nowarn
    } }
    return result
  }

  override def selectByIdsTracked(priceIds: Array[ProductPricesId])(using c: Connection): java.util.Map[ProductPricesId, ProductPricesRow] = selectByIds(priceIds)(using c).stream().collect(Collectors.toMap((row: ProductPricesRow) => row.priceId, Function.identity()))

  override def update: UpdateBuilder[ProductPricesFields, ProductPricesRow] = {
    new UpdateBuilderMock(
      ProductPricesFields.structure,
      () => new ArrayList(map.values()),
      UpdateParams.empty(),
      row => row
    )
  }

  override def update(row: ProductPricesRow)(using c: Connection): java.lang.Boolean = {
    val shouldUpdate = Optional.ofNullable(map.get(row.priceId)).filter(oldRow => (oldRow != row)).isPresent()
    if (shouldUpdate) {
      map.put(row.priceId, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: ProductPricesRow)(using c: Connection): ProductPricesRow = {
    map.put(unsaved.priceId, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: java.util.Iterator[ProductPricesRow])(using c: Connection): java.util.List[ProductPricesRow] = {
    val result = new ArrayList[ProductPricesRow]()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.priceId, row): @scala.annotation.nowarn
      result.add(row): @scala.annotation.nowarn
    }
    return result
  }
}