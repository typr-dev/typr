/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.payments

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.HashMap
import java.util.Optional
import java.util.function.Function
import java.util.stream.Collectors
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

case class PaymentsRepoMock(
  toRow: PaymentsRowUnsaved => PaymentsRow,
  map: HashMap[PaymentsId, PaymentsRow] = new HashMap[PaymentsId, PaymentsRow]()
) extends PaymentsRepo {
  override def delete: DeleteBuilder[PaymentsFields, PaymentsRow] = {
    new DeleteBuilderMock(
      PaymentsFields.structure,
      () => new ArrayList(map.values()),
      DeleteParams.empty(),
      row => row.paymentId,
      id => map.remove(id): @scala.annotation.nowarn
    )
  }

  override def deleteById(paymentId: PaymentsId)(using c: Connection): java.lang.Boolean = Optional.ofNullable(map.remove(paymentId)).isPresent()

  override def deleteByIds(paymentIds: Array[PaymentsId])(using c: Connection): Integer = {
    var count = 0
    paymentIds.foreach { id => if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: PaymentsRow)(using c: Connection): PaymentsRow = {
    if (map.containsKey(unsaved.paymentId)) {
      throw new RuntimeException(s"id $unsaved.paymentId already exists")
    }
    map.put(unsaved.paymentId, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: PaymentsRowUnsaved)(using c: Connection): PaymentsRow = insert(toRow(unsaved))(using c)

  override def select: SelectBuilder[PaymentsFields, PaymentsRow] = new SelectBuilderMock(PaymentsFields.structure, () => new ArrayList(map.values()), SelectParams.empty())

  override def selectAll(using c: Connection): java.util.List[PaymentsRow] = new ArrayList(map.values())

  override def selectById(paymentId: PaymentsId)(using c: Connection): Optional[PaymentsRow] = Optional.ofNullable(map.get(paymentId))

  override def selectByIds(paymentIds: Array[PaymentsId])(using c: Connection): java.util.List[PaymentsRow] = {
    val result = new ArrayList[PaymentsRow]()
    paymentIds.foreach { id => val opt = Optional.ofNullable(map.get(id)); if (opt.isPresent()) {
      result.add(opt.get()): @scala.annotation.nowarn
    } }
    return result
  }

  override def selectByIdsTracked(paymentIds: Array[PaymentsId])(using c: Connection): java.util.Map[PaymentsId, PaymentsRow] = selectByIds(paymentIds)(using c).stream().collect(Collectors.toMap((row: PaymentsRow) => row.paymentId, Function.identity()))

  override def update: UpdateBuilder[PaymentsFields, PaymentsRow] = {
    new UpdateBuilderMock(
      PaymentsFields.structure,
      () => new ArrayList(map.values()),
      UpdateParams.empty(),
      row => row
    )
  }

  override def update(row: PaymentsRow)(using c: Connection): java.lang.Boolean = {
    val shouldUpdate = Optional.ofNullable(map.get(row.paymentId)).filter(oldRow => (oldRow != row)).isPresent()
    if (shouldUpdate) {
      map.put(row.paymentId, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: PaymentsRow)(using c: Connection): PaymentsRow = {
    map.put(unsaved.paymentId, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: java.util.Iterator[PaymentsRow])(using c: Connection): java.util.List[PaymentsRow] = {
    val result = new ArrayList[PaymentsRow]()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.paymentId, row): @scala.annotation.nowarn
      result.add(row): @scala.annotation.nowarn
    }
    return result
  }
}