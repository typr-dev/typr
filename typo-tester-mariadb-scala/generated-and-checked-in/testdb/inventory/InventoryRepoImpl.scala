/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.inventory

import java.sql.Connection
import java.util.ArrayList
import java.util.HashMap
import java.util.Optional
import testdb.products.ProductsId
import testdb.warehouses.WarehousesId
import typo.dsl.DeleteBuilder
import typo.dsl.Dialect
import typo.dsl.SelectBuilder
import typo.dsl.UpdateBuilder
import typo.runtime.Fragment
import typo.runtime.Fragment.Literal
import typo.runtime.MariaTypes
import typo.runtime.FragmentInterpolator.interpolate

class InventoryRepoImpl extends InventoryRepo {
  override def delete: DeleteBuilder[InventoryFields, InventoryRow] = DeleteBuilder.of("`inventory`", InventoryFields.structure, Dialect.MARIADB)

  override def deleteById(inventoryId: InventoryId)(using c: Connection): java.lang.Boolean = interpolate"delete from `inventory` where `inventory_id` = ${InventoryId.pgType.encode(inventoryId)}".update().runUnchecked(c) > 0

  override def deleteByIds(inventoryIds: Array[InventoryId])(using c: Connection): Integer = {
    val fragments: ArrayList[Fragment] = new ArrayList[Fragment]()
    inventoryIds.foreach { id => fragments.add(InventoryId.pgType.encode(id)) }
    return Fragment.interpolate(Fragment.lit("delete from `inventory` where `inventory_id` in ("), Fragment.comma(fragments), Fragment.lit(")")).update().runUnchecked(c)
  }

  override def insert(unsaved: InventoryRow)(using c: Connection): InventoryRow = {
  interpolate"""insert into `inventory`(`product_id`, `warehouse_id`, `quantity_on_hand`, `quantity_reserved`, `quantity_on_order`, `reorder_point`, `reorder_quantity`, `bin_location`, `last_counted_at`, `updated_at`)
    values (${ProductsId.pgType.encode(unsaved.productId)}, ${WarehousesId.pgType.encode(unsaved.warehouseId)}, ${MariaTypes.int_.encode(unsaved.quantityOnHand)}, ${MariaTypes.int_.encode(unsaved.quantityReserved)}, ${MariaTypes.int_.encode(unsaved.quantityOnOrder)}, ${MariaTypes.int_.encode(unsaved.reorderPoint)}, ${MariaTypes.int_.encode(unsaved.reorderQuantity)}, ${MariaTypes.text.opt().encode(unsaved.binLocation)}, ${MariaTypes.datetime.opt().encode(unsaved.lastCountedAt)}, ${MariaTypes.datetime.encode(unsaved.updatedAt)})
    returning `inventory_id`, `product_id`, `warehouse_id`, `quantity_on_hand`, `quantity_reserved`, `quantity_on_order`, `reorder_point`, `reorder_quantity`, `bin_location`, `last_counted_at`, `updated_at`
    """
    .updateReturning(InventoryRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: InventoryRowUnsaved)(using c: Connection): InventoryRow = {
    val columns: ArrayList[Literal] = new ArrayList[Literal]()
    val values: ArrayList[Fragment] = new ArrayList[Fragment]()
    columns.add(Fragment.lit("`product_id`")): @scala.annotation.nowarn
    values.add(interpolate"${ProductsId.pgType.encode(unsaved.productId)}"): @scala.annotation.nowarn
    columns.add(Fragment.lit("`warehouse_id`")): @scala.annotation.nowarn
    values.add(interpolate"${WarehousesId.pgType.encode(unsaved.warehouseId)}"): @scala.annotation.nowarn
    unsaved.quantityOnHand.visit(
      {  },
      value => { columns.add(Fragment.lit("`quantity_on_hand`")): @scala.annotation.nowarn; values.add(interpolate"${MariaTypes.int_.encode(value)}"): @scala.annotation.nowarn }
    );
    unsaved.quantityReserved.visit(
      {  },
      value => { columns.add(Fragment.lit("`quantity_reserved`")): @scala.annotation.nowarn; values.add(interpolate"${MariaTypes.int_.encode(value)}"): @scala.annotation.nowarn }
    );
    unsaved.quantityOnOrder.visit(
      {  },
      value => { columns.add(Fragment.lit("`quantity_on_order`")): @scala.annotation.nowarn; values.add(interpolate"${MariaTypes.int_.encode(value)}"): @scala.annotation.nowarn }
    );
    unsaved.reorderPoint.visit(
      {  },
      value => { columns.add(Fragment.lit("`reorder_point`")): @scala.annotation.nowarn; values.add(interpolate"${MariaTypes.int_.encode(value)}"): @scala.annotation.nowarn }
    );
    unsaved.reorderQuantity.visit(
      {  },
      value => { columns.add(Fragment.lit("`reorder_quantity`")): @scala.annotation.nowarn; values.add(interpolate"${MariaTypes.int_.encode(value)}"): @scala.annotation.nowarn }
    );
    unsaved.binLocation.visit(
      {  },
      value => { columns.add(Fragment.lit("`bin_location`")): @scala.annotation.nowarn; values.add(interpolate"${MariaTypes.text.opt().encode(value)}"): @scala.annotation.nowarn }
    );
    unsaved.lastCountedAt.visit(
      {  },
      value => { columns.add(Fragment.lit("`last_counted_at`")): @scala.annotation.nowarn; values.add(interpolate"${MariaTypes.datetime.opt().encode(value)}"): @scala.annotation.nowarn }
    );
    unsaved.updatedAt.visit(
      {  },
      value => { columns.add(Fragment.lit("`updated_at`")): @scala.annotation.nowarn; values.add(interpolate"${MariaTypes.datetime.encode(value)}"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      interpolate"""insert into `inventory`(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      returning `inventory_id`, `product_id`, `warehouse_id`, `quantity_on_hand`, `quantity_reserved`, `quantity_on_order`, `reorder_point`, `reorder_quantity`, `bin_location`, `last_counted_at`, `updated_at`
      """
    }
    return q.updateReturning(InventoryRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def select: SelectBuilder[InventoryFields, InventoryRow] = SelectBuilder.of("`inventory`", InventoryFields.structure, InventoryRow.`_rowParser`, Dialect.MARIADB)

  override def selectAll(using c: Connection): java.util.List[InventoryRow] = {
    interpolate"""select `inventory_id`, `product_id`, `warehouse_id`, `quantity_on_hand`, `quantity_reserved`, `quantity_on_order`, `reorder_point`, `reorder_quantity`, `bin_location`, `last_counted_at`, `updated_at`
    from `inventory`
    """.query(InventoryRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(inventoryId: InventoryId)(using c: Connection): Optional[InventoryRow] = {
    interpolate"""select `inventory_id`, `product_id`, `warehouse_id`, `quantity_on_hand`, `quantity_reserved`, `quantity_on_order`, `reorder_point`, `reorder_quantity`, `bin_location`, `last_counted_at`, `updated_at`
    from `inventory`
    where `inventory_id` = ${InventoryId.pgType.encode(inventoryId)}""".query(InventoryRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(inventoryIds: Array[InventoryId])(using c: Connection): java.util.List[InventoryRow] = {
    val fragments: ArrayList[Fragment] = new ArrayList[Fragment]()
    inventoryIds.foreach { id => fragments.add(InventoryId.pgType.encode(id)) }
    return Fragment.interpolate(Fragment.lit("select `inventory_id`, `product_id`, `warehouse_id`, `quantity_on_hand`, `quantity_reserved`, `quantity_on_order`, `reorder_point`, `reorder_quantity`, `bin_location`, `last_counted_at`, `updated_at` from `inventory` where `inventory_id` in ("), Fragment.comma(fragments), Fragment.lit(")")).query(InventoryRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(inventoryIds: Array[InventoryId])(using c: Connection): java.util.Map[InventoryId, InventoryRow] = {
    val ret: HashMap[InventoryId, InventoryRow] = new HashMap[InventoryId, InventoryRow]()
    selectByIds(inventoryIds)(using c).forEach(row => ret.put(row.inventoryId, row): @scala.annotation.nowarn)
    return ret
  }

  override def selectByUniqueProductIdAndWarehouseId(
    productId: ProductsId,
    warehouseId: WarehousesId
  )(using c: Connection): Optional[InventoryRow] = {
    interpolate"""select `inventory_id`, `product_id`, `warehouse_id`, `quantity_on_hand`, `quantity_reserved`, `quantity_on_order`, `reorder_point`, `reorder_quantity`, `bin_location`, `last_counted_at`, `updated_at`
    from `inventory`
    where `product_id` = ${ProductsId.pgType.encode(productId)} AND `warehouse_id` = ${WarehousesId.pgType.encode(warehouseId)}
    """.query(InventoryRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def update: UpdateBuilder[InventoryFields, InventoryRow] = UpdateBuilder.of("`inventory`", InventoryFields.structure, InventoryRow.`_rowParser`.all(), Dialect.MARIADB)

  override def update(row: InventoryRow)(using c: Connection): java.lang.Boolean = {
    val inventoryId: InventoryId = row.inventoryId
    return interpolate"""update `inventory`
    set `product_id` = ${ProductsId.pgType.encode(row.productId)},
    `warehouse_id` = ${WarehousesId.pgType.encode(row.warehouseId)},
    `quantity_on_hand` = ${MariaTypes.int_.encode(row.quantityOnHand)},
    `quantity_reserved` = ${MariaTypes.int_.encode(row.quantityReserved)},
    `quantity_on_order` = ${MariaTypes.int_.encode(row.quantityOnOrder)},
    `reorder_point` = ${MariaTypes.int_.encode(row.reorderPoint)},
    `reorder_quantity` = ${MariaTypes.int_.encode(row.reorderQuantity)},
    `bin_location` = ${MariaTypes.text.opt().encode(row.binLocation)},
    `last_counted_at` = ${MariaTypes.datetime.opt().encode(row.lastCountedAt)},
    `updated_at` = ${MariaTypes.datetime.encode(row.updatedAt)}
    where `inventory_id` = ${InventoryId.pgType.encode(inventoryId)}""".update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: InventoryRow)(using c: Connection): InventoryRow = {
  interpolate"""INSERT INTO `inventory`(`product_id`, `warehouse_id`, `quantity_on_hand`, `quantity_reserved`, `quantity_on_order`, `reorder_point`, `reorder_quantity`, `bin_location`, `last_counted_at`, `updated_at`)
    VALUES (${ProductsId.pgType.encode(unsaved.productId)}, ${WarehousesId.pgType.encode(unsaved.warehouseId)}, ${MariaTypes.int_.encode(unsaved.quantityOnHand)}, ${MariaTypes.int_.encode(unsaved.quantityReserved)}, ${MariaTypes.int_.encode(unsaved.quantityOnOrder)}, ${MariaTypes.int_.encode(unsaved.reorderPoint)}, ${MariaTypes.int_.encode(unsaved.reorderQuantity)}, ${MariaTypes.text.opt().encode(unsaved.binLocation)}, ${MariaTypes.datetime.opt().encode(unsaved.lastCountedAt)}, ${MariaTypes.datetime.encode(unsaved.updatedAt)})
    ON DUPLICATE KEY UPDATE `product_id` = VALUES(`product_id`),
    `warehouse_id` = VALUES(`warehouse_id`),
    `quantity_on_hand` = VALUES(`quantity_on_hand`),
    `quantity_reserved` = VALUES(`quantity_reserved`),
    `quantity_on_order` = VALUES(`quantity_on_order`),
    `reorder_point` = VALUES(`reorder_point`),
    `reorder_quantity` = VALUES(`reorder_quantity`),
    `bin_location` = VALUES(`bin_location`),
    `last_counted_at` = VALUES(`last_counted_at`),
    `updated_at` = VALUES(`updated_at`)
    RETURNING `inventory_id`, `product_id`, `warehouse_id`, `quantity_on_hand`, `quantity_reserved`, `quantity_on_order`, `reorder_point`, `reorder_quantity`, `bin_location`, `last_counted_at`, `updated_at`"""
    .updateReturning(InventoryRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: java.util.Iterator[InventoryRow])(using c: Connection): java.util.List[InventoryRow] = {
    interpolate"""INSERT INTO `inventory`(`inventory_id`, `product_id`, `warehouse_id`, `quantity_on_hand`, `quantity_reserved`, `quantity_on_order`, `reorder_point`, `reorder_quantity`, `bin_location`, `last_counted_at`, `updated_at`)
    VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
    ON DUPLICATE KEY UPDATE `product_id` = VALUES(`product_id`),
    `warehouse_id` = VALUES(`warehouse_id`),
    `quantity_on_hand` = VALUES(`quantity_on_hand`),
    `quantity_reserved` = VALUES(`quantity_reserved`),
    `quantity_on_order` = VALUES(`quantity_on_order`),
    `reorder_point` = VALUES(`reorder_point`),
    `reorder_quantity` = VALUES(`reorder_quantity`),
    `bin_location` = VALUES(`bin_location`),
    `last_counted_at` = VALUES(`last_counted_at`),
    `updated_at` = VALUES(`updated_at`)
    RETURNING `inventory_id`, `product_id`, `warehouse_id`, `quantity_on_hand`, `quantity_reserved`, `quantity_on_order`, `reorder_point`, `reorder_quantity`, `bin_location`, `last_counted_at`, `updated_at`"""
      .updateReturningEach(InventoryRow.`_rowParser`, unsaved)
      .runUnchecked(c)
  }
}