/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.product_categories

import java.sql.Connection
import java.util.ArrayList
import java.util.HashMap
import java.util.Optional
import testdb.categories.CategoriesId
import testdb.products.ProductsId
import typo.dsl.DeleteBuilder
import typo.dsl.Dialect
import typo.dsl.SelectBuilder
import typo.dsl.UpdateBuilder
import typo.runtime.Fragment
import typo.runtime.Fragment.Literal
import typo.runtime.MariaTypes
import typo.runtime.FragmentInterpolator.interpolate

class ProductCategoriesRepoImpl extends ProductCategoriesRepo {
  override def delete: DeleteBuilder[ProductCategoriesFields, ProductCategoriesRow] = DeleteBuilder.of("`product_categories`", ProductCategoriesFields.structure, Dialect.MARIADB)

  override def deleteById(compositeId: ProductCategoriesId)(using c: Connection): java.lang.Boolean = interpolate"delete from `product_categories` where `product_id` = ${ProductsId.pgType.encode(compositeId.productId)} AND `category_id` = ${CategoriesId.pgType.encode(compositeId.categoryId)}".update().runUnchecked(c) > 0

  override def deleteByIds(compositeIds: Array[ProductCategoriesId])(using c: Connection): Integer = {
    val fragments: ArrayList[Fragment] = new ArrayList[Fragment]()
    compositeIds.foreach { id => fragments.add(Fragment.interpolate(Fragment.lit("("), ProductsId.pgType.encode(id.productId), Fragment.lit(", "), CategoriesId.pgType.encode(id.categoryId), Fragment.lit(")"))) }
    return Fragment.interpolate(Fragment.lit("delete from `product_categories` where (`product_id`, `category_id`) in ("), Fragment.comma(fragments), Fragment.lit(")")).update().runUnchecked(c)
  }

  override def insert(unsaved: ProductCategoriesRow)(using c: Connection): ProductCategoriesRow = {
  interpolate"""insert into `product_categories`(`product_id`, `category_id`, `is_primary`, `sort_order`)
    values (${ProductsId.pgType.encode(unsaved.productId)}, ${CategoriesId.pgType.encode(unsaved.categoryId)}, ${MariaTypes.bool.encode(unsaved.isPrimary)}, ${MariaTypes.smallint.encode(unsaved.sortOrder)})
    returning `product_id`, `category_id`, `is_primary`, `sort_order`
    """
    .updateReturning(ProductCategoriesRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: ProductCategoriesRowUnsaved)(using c: Connection): ProductCategoriesRow = {
    val columns: ArrayList[Literal] = new ArrayList[Literal]()
    val values: ArrayList[Fragment] = new ArrayList[Fragment]()
    columns.add(Fragment.lit("`product_id`")): @scala.annotation.nowarn
    values.add(interpolate"${ProductsId.pgType.encode(unsaved.productId)}"): @scala.annotation.nowarn
    columns.add(Fragment.lit("`category_id`")): @scala.annotation.nowarn
    values.add(interpolate"${CategoriesId.pgType.encode(unsaved.categoryId)}"): @scala.annotation.nowarn
    unsaved.isPrimary.visit(
      {  },
      value => { columns.add(Fragment.lit("`is_primary`")): @scala.annotation.nowarn; values.add(interpolate"${MariaTypes.bool.encode(value)}"): @scala.annotation.nowarn }
    );
    unsaved.sortOrder.visit(
      {  },
      value => { columns.add(Fragment.lit("`sort_order`")): @scala.annotation.nowarn; values.add(interpolate"${MariaTypes.smallint.encode(value)}"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      interpolate"""insert into `product_categories`(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      returning `product_id`, `category_id`, `is_primary`, `sort_order`
      """
    }
    return q.updateReturning(ProductCategoriesRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def select: SelectBuilder[ProductCategoriesFields, ProductCategoriesRow] = SelectBuilder.of("`product_categories`", ProductCategoriesFields.structure, ProductCategoriesRow.`_rowParser`, Dialect.MARIADB)

  override def selectAll(using c: Connection): java.util.List[ProductCategoriesRow] = {
    interpolate"""select `product_id`, `category_id`, `is_primary`, `sort_order`
    from `product_categories`
    """.query(ProductCategoriesRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(compositeId: ProductCategoriesId)(using c: Connection): Optional[ProductCategoriesRow] = {
    interpolate"""select `product_id`, `category_id`, `is_primary`, `sort_order`
    from `product_categories`
    where `product_id` = ${ProductsId.pgType.encode(compositeId.productId)} AND `category_id` = ${CategoriesId.pgType.encode(compositeId.categoryId)}""".query(ProductCategoriesRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(compositeIds: Array[ProductCategoriesId])(using c: Connection): java.util.List[ProductCategoriesRow] = {
    val fragments: ArrayList[Fragment] = new ArrayList[Fragment]()
    compositeIds.foreach { id => fragments.add(Fragment.interpolate(Fragment.lit("("), ProductsId.pgType.encode(id.productId), Fragment.lit(", "), CategoriesId.pgType.encode(id.categoryId), Fragment.lit(")"))) }
    return Fragment.interpolate(Fragment.lit("select `product_id`, `category_id`, `is_primary`, `sort_order` from `product_categories` where (`product_id`, `category_id`) in ("), Fragment.comma(fragments), Fragment.lit(")")).query(ProductCategoriesRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(compositeIds: Array[ProductCategoriesId])(using c: Connection): java.util.Map[ProductCategoriesId, ProductCategoriesRow] = {
    val ret: HashMap[ProductCategoriesId, ProductCategoriesRow] = new HashMap[ProductCategoriesId, ProductCategoriesRow]()
    selectByIds(compositeIds)(using c).forEach(row => ret.put(row.compositeId, row): @scala.annotation.nowarn)
    return ret
  }

  override def update: UpdateBuilder[ProductCategoriesFields, ProductCategoriesRow] = UpdateBuilder.of("`product_categories`", ProductCategoriesFields.structure, ProductCategoriesRow.`_rowParser`.all(), Dialect.MARIADB)

  override def update(row: ProductCategoriesRow)(using c: Connection): java.lang.Boolean = {
    val compositeId: ProductCategoriesId = row.compositeId
    return interpolate"""update `product_categories`
    set `is_primary` = ${MariaTypes.bool.encode(row.isPrimary)},
    `sort_order` = ${MariaTypes.smallint.encode(row.sortOrder)}
    where `product_id` = ${ProductsId.pgType.encode(compositeId.productId)} AND `category_id` = ${CategoriesId.pgType.encode(compositeId.categoryId)}""".update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: ProductCategoriesRow)(using c: Connection): ProductCategoriesRow = {
  interpolate"""INSERT INTO `product_categories`(`product_id`, `category_id`, `is_primary`, `sort_order`)
    VALUES (${ProductsId.pgType.encode(unsaved.productId)}, ${CategoriesId.pgType.encode(unsaved.categoryId)}, ${MariaTypes.bool.encode(unsaved.isPrimary)}, ${MariaTypes.smallint.encode(unsaved.sortOrder)})
    ON DUPLICATE KEY UPDATE `is_primary` = VALUES(`is_primary`),
    `sort_order` = VALUES(`sort_order`)
    RETURNING `product_id`, `category_id`, `is_primary`, `sort_order`"""
    .updateReturning(ProductCategoriesRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: java.util.Iterator[ProductCategoriesRow])(using c: Connection): java.util.List[ProductCategoriesRow] = {
    interpolate"""INSERT INTO `product_categories`(`product_id`, `category_id`, `is_primary`, `sort_order`)
    VALUES (?, ?, ?, ?)
    ON DUPLICATE KEY UPDATE `is_primary` = VALUES(`is_primary`),
    `sort_order` = VALUES(`sort_order`)
    RETURNING `product_id`, `category_id`, `is_primary`, `sort_order`"""
      .updateReturningEach(ProductCategoriesRow.`_rowParser`, unsaved)
      .runUnchecked(c)
  }
}