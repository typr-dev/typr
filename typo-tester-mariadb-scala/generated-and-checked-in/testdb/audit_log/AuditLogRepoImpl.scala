/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.audit_log

import java.sql.Connection
import java.util.ArrayList
import java.util.HashMap
import java.util.Optional
import typo.dsl.DeleteBuilder
import typo.dsl.Dialect
import typo.dsl.SelectBuilder
import typo.dsl.UpdateBuilder
import typo.runtime.Fragment
import typo.runtime.Fragment.Literal
import typo.runtime.MariaTypes
import typo.runtime.FragmentInterpolator.interpolate

class AuditLogRepoImpl extends AuditLogRepo {
  override def delete: DeleteBuilder[AuditLogFields, AuditLogRow] = DeleteBuilder.of("`audit_log`", AuditLogFields.structure, Dialect.MARIADB)

  override def deleteById(logId: AuditLogId)(using c: Connection): java.lang.Boolean = interpolate"delete from `audit_log` where `log_id` = ${AuditLogId.pgType.encode(logId)}".update().runUnchecked(c) > 0

  override def deleteByIds(logIds: Array[AuditLogId])(using c: Connection): Integer = {
    val fragments: ArrayList[Fragment] = new ArrayList[Fragment]()
    logIds.foreach { id => fragments.add(AuditLogId.pgType.encode(id)) }
    return Fragment.interpolate(Fragment.lit("delete from `audit_log` where `log_id` in ("), Fragment.comma(fragments), Fragment.lit(")")).update().runUnchecked(c)
  }

  override def insert(unsaved: AuditLogRow)(using c: Connection): AuditLogRow = {
  interpolate"""insert into `audit_log`(`table_name`, `record_id`, `action`, `old_values`, `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`)
    values (${MariaTypes.text.encode(unsaved.tableName)}, ${MariaTypes.text.encode(unsaved.recordId)}, ${MariaTypes.text.encode(unsaved.action)}, ${MariaTypes.text.opt().encode(unsaved.oldValues)}, ${MariaTypes.text.opt().encode(unsaved.newValues)}, ${MariaTypes.text.opt().encode(unsaved.changedBy)}, ${MariaTypes.datetime.encode(unsaved.changedAt)}, ${MariaTypes.inet6.opt().encode(unsaved.clientIp)}, ${MariaTypes.blob.opt().encode(unsaved.sessionId)})
    returning `log_id`, `table_name`, `record_id`, `action`, `old_values`, `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`
    """
    .updateReturning(AuditLogRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: AuditLogRowUnsaved)(using c: Connection): AuditLogRow = {
    val columns: ArrayList[Literal] = new ArrayList[Literal]()
    val values: ArrayList[Fragment] = new ArrayList[Fragment]()
    columns.add(Fragment.lit("`table_name`")): @scala.annotation.nowarn
    values.add(interpolate"${MariaTypes.text.encode(unsaved.tableName)}"): @scala.annotation.nowarn
    columns.add(Fragment.lit("`record_id`")): @scala.annotation.nowarn
    values.add(interpolate"${MariaTypes.text.encode(unsaved.recordId)}"): @scala.annotation.nowarn
    columns.add(Fragment.lit("`action`")): @scala.annotation.nowarn
    values.add(interpolate"${MariaTypes.text.encode(unsaved.action)}"): @scala.annotation.nowarn
    unsaved.oldValues.visit(
      {  },
      value => { columns.add(Fragment.lit("`old_values`")): @scala.annotation.nowarn; values.add(interpolate"${MariaTypes.text.opt().encode(value)}"): @scala.annotation.nowarn }
    );
    unsaved.newValues.visit(
      {  },
      value => { columns.add(Fragment.lit("`new_values`")): @scala.annotation.nowarn; values.add(interpolate"${MariaTypes.text.opt().encode(value)}"): @scala.annotation.nowarn }
    );
    unsaved.changedBy.visit(
      {  },
      value => { columns.add(Fragment.lit("`changed_by`")): @scala.annotation.nowarn; values.add(interpolate"${MariaTypes.text.opt().encode(value)}"): @scala.annotation.nowarn }
    );
    unsaved.changedAt.visit(
      {  },
      value => { columns.add(Fragment.lit("`changed_at`")): @scala.annotation.nowarn; values.add(interpolate"${MariaTypes.datetime.encode(value)}"): @scala.annotation.nowarn }
    );
    unsaved.clientIp.visit(
      {  },
      value => { columns.add(Fragment.lit("`client_ip`")): @scala.annotation.nowarn; values.add(interpolate"${MariaTypes.inet6.opt().encode(value)}"): @scala.annotation.nowarn }
    );
    unsaved.sessionId.visit(
      {  },
      value => { columns.add(Fragment.lit("`session_id`")): @scala.annotation.nowarn; values.add(interpolate"${MariaTypes.blob.opt().encode(value)}"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      interpolate"""insert into `audit_log`(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      returning `log_id`, `table_name`, `record_id`, `action`, `old_values`, `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`
      """
    }
    return q.updateReturning(AuditLogRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def select: SelectBuilder[AuditLogFields, AuditLogRow] = SelectBuilder.of("`audit_log`", AuditLogFields.structure, AuditLogRow.`_rowParser`, Dialect.MARIADB)

  override def selectAll(using c: Connection): java.util.List[AuditLogRow] = {
    interpolate"""select `log_id`, `table_name`, `record_id`, `action`, `old_values`, `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`
    from `audit_log`
    """.query(AuditLogRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(logId: AuditLogId)(using c: Connection): Optional[AuditLogRow] = {
    interpolate"""select `log_id`, `table_name`, `record_id`, `action`, `old_values`, `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`
    from `audit_log`
    where `log_id` = ${AuditLogId.pgType.encode(logId)}""".query(AuditLogRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(logIds: Array[AuditLogId])(using c: Connection): java.util.List[AuditLogRow] = {
    val fragments: ArrayList[Fragment] = new ArrayList[Fragment]()
    logIds.foreach { id => fragments.add(AuditLogId.pgType.encode(id)) }
    return Fragment.interpolate(Fragment.lit("select `log_id`, `table_name`, `record_id`, `action`, `old_values`, `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id` from `audit_log` where `log_id` in ("), Fragment.comma(fragments), Fragment.lit(")")).query(AuditLogRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(logIds: Array[AuditLogId])(using c: Connection): java.util.Map[AuditLogId, AuditLogRow] = {
    val ret: HashMap[AuditLogId, AuditLogRow] = new HashMap[AuditLogId, AuditLogRow]()
    selectByIds(logIds)(using c).forEach(row => ret.put(row.logId, row): @scala.annotation.nowarn)
    return ret
  }

  override def update: UpdateBuilder[AuditLogFields, AuditLogRow] = UpdateBuilder.of("`audit_log`", AuditLogFields.structure, AuditLogRow.`_rowParser`.all(), Dialect.MARIADB)

  override def update(row: AuditLogRow)(using c: Connection): java.lang.Boolean = {
    val logId: AuditLogId = row.logId
    return interpolate"""update `audit_log`
    set `table_name` = ${MariaTypes.text.encode(row.tableName)},
    `record_id` = ${MariaTypes.text.encode(row.recordId)},
    `action` = ${MariaTypes.text.encode(row.action)},
    `old_values` = ${MariaTypes.text.opt().encode(row.oldValues)},
    `new_values` = ${MariaTypes.text.opt().encode(row.newValues)},
    `changed_by` = ${MariaTypes.text.opt().encode(row.changedBy)},
    `changed_at` = ${MariaTypes.datetime.encode(row.changedAt)},
    `client_ip` = ${MariaTypes.inet6.opt().encode(row.clientIp)},
    `session_id` = ${MariaTypes.blob.opt().encode(row.sessionId)}
    where `log_id` = ${AuditLogId.pgType.encode(logId)}""".update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: AuditLogRow)(using c: Connection): AuditLogRow = {
  interpolate"""INSERT INTO `audit_log`(`table_name`, `record_id`, `action`, `old_values`, `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`)
    VALUES (${MariaTypes.text.encode(unsaved.tableName)}, ${MariaTypes.text.encode(unsaved.recordId)}, ${MariaTypes.text.encode(unsaved.action)}, ${MariaTypes.text.opt().encode(unsaved.oldValues)}, ${MariaTypes.text.opt().encode(unsaved.newValues)}, ${MariaTypes.text.opt().encode(unsaved.changedBy)}, ${MariaTypes.datetime.encode(unsaved.changedAt)}, ${MariaTypes.inet6.opt().encode(unsaved.clientIp)}, ${MariaTypes.blob.opt().encode(unsaved.sessionId)})
    ON DUPLICATE KEY UPDATE `table_name` = VALUES(`table_name`),
    `record_id` = VALUES(`record_id`),
    `action` = VALUES(`action`),
    `old_values` = VALUES(`old_values`),
    `new_values` = VALUES(`new_values`),
    `changed_by` = VALUES(`changed_by`),
    `changed_at` = VALUES(`changed_at`),
    `client_ip` = VALUES(`client_ip`),
    `session_id` = VALUES(`session_id`)
    RETURNING `log_id`, `table_name`, `record_id`, `action`, `old_values`, `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`"""
    .updateReturning(AuditLogRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: java.util.Iterator[AuditLogRow])(using c: Connection): java.util.List[AuditLogRow] = {
    interpolate"""INSERT INTO `audit_log`(`log_id`, `table_name`, `record_id`, `action`, `old_values`, `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`)
    VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
    ON DUPLICATE KEY UPDATE `table_name` = VALUES(`table_name`),
    `record_id` = VALUES(`record_id`),
    `action` = VALUES(`action`),
    `old_values` = VALUES(`old_values`),
    `new_values` = VALUES(`new_values`),
    `changed_by` = VALUES(`changed_by`),
    `changed_at` = VALUES(`changed_at`),
    `client_ip` = VALUES(`client_ip`),
    `session_id` = VALUES(`session_id`)
    RETURNING `log_id`, `table_name`, `record_id`, `action`, `old_values`, `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`"""
      .updateReturningEach(AuditLogRow.`_rowParser`, unsaved)
      .runUnchecked(c)
  }
}