/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.customer

import adventureworks.person.businessentity.BusinessentityId
import adventureworks.sales.salesterritory.SalesterritoryId
import java.sql.Connection
import java.util.ArrayList
import java.util.HashMap
import java.util.Optional
import typo.dsl.DeleteBuilder
import typo.dsl.Dialect
import typo.dsl.SelectBuilder
import typo.dsl.UpdateBuilder
import typo.runtime.Fragment
import typo.runtime.PgTypes
import typo.runtime.streamingInsert
import typo.runtime.Fragment.interpolate

class CustomerRepoImpl extends CustomerRepo {
  override def delete: DeleteBuilder[CustomerFields, CustomerRow] = DeleteBuilder.of(""""sales"."customer"""", CustomerFields.structure, Dialect.POSTGRESQL)

  override def deleteById(customerid: CustomerId)(using c: Connection): java.lang.Boolean = interpolate(Fragment.lit("""delete from "sales"."customer" where "customerid" = """), Fragment.encode(CustomerId.pgType, customerid), Fragment.lit("")).update().runUnchecked(c) > 0

  override def deleteByIds(customerids: Array[CustomerId])(using c: Connection): Integer = {
    interpolate(Fragment.lit("""delete
    from "sales"."customer"
    where "customerid" = ANY("""), Fragment.encode(CustomerId.pgTypeArray, customerids), Fragment.lit(")"))
      .update()
      .runUnchecked(c)
  }

  override def insert(unsaved: CustomerRow)(using c: Connection): CustomerRow = {
  interpolate(Fragment.lit("""insert into "sales"."customer"("customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate")
    values ("""), Fragment.encode(CustomerId.pgType, unsaved.customerid), Fragment.lit("::int4, "), Fragment.encode(BusinessentityId.pgType.opt(), unsaved.personid), Fragment.lit("::int4, "), Fragment.encode(BusinessentityId.pgType.opt(), unsaved.storeid), Fragment.lit("::int4, "), Fragment.encode(SalesterritoryId.pgType.opt(), unsaved.territoryid), Fragment.lit("::int4, "), Fragment.encode(PgTypes.uuid, unsaved.rowguid), Fragment.lit("::uuid, "), Fragment.encode(PgTypes.timestamp, unsaved.modifieddate), Fragment.lit("""::timestamp)
    RETURNING "customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate"
    """))
    .updateReturning(CustomerRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: CustomerRowUnsaved)(using c: Connection): CustomerRow = {
    val columns: ArrayList[Fragment] = new ArrayList()
    val values: ArrayList[Fragment] = new ArrayList()
    columns.add(Fragment.lit(""""personid"""")): @scala.annotation.nowarn
    values.add(interpolate(Fragment.encode(BusinessentityId.pgType.opt(), unsaved.personid), Fragment.lit("::int4"))): @scala.annotation.nowarn
    columns.add(Fragment.lit(""""storeid"""")): @scala.annotation.nowarn
    values.add(interpolate(Fragment.encode(BusinessentityId.pgType.opt(), unsaved.storeid), Fragment.lit("::int4"))): @scala.annotation.nowarn
    columns.add(Fragment.lit(""""territoryid"""")): @scala.annotation.nowarn
    values.add(interpolate(Fragment.encode(SalesterritoryId.pgType.opt(), unsaved.territoryid), Fragment.lit("::int4"))): @scala.annotation.nowarn
    unsaved.customerid.visit(
      {  },
      value => { columns.add(Fragment.lit(""""customerid"""")): @scala.annotation.nowarn; values.add(interpolate(Fragment.encode(CustomerId.pgType, value), Fragment.lit("::int4"))): @scala.annotation.nowarn }
    );
    unsaved.rowguid.visit(
      {  },
      value => { columns.add(Fragment.lit(""""rowguid"""")): @scala.annotation.nowarn; values.add(interpolate(Fragment.encode(PgTypes.uuid, value), Fragment.lit("::uuid"))): @scala.annotation.nowarn }
    );
    unsaved.modifieddate.visit(
      {  },
      value => { columns.add(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.add(interpolate(Fragment.encode(PgTypes.timestamp, value), Fragment.lit("::timestamp"))): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      interpolate(Fragment.lit("""insert into "sales"."customer"("""), Fragment.comma(columns), Fragment.lit(""")
      values ("""), Fragment.comma(values), Fragment.lit(""")
      RETURNING "customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate"
      """))
    }
    return q.updateReturning(CustomerRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: java.util.Iterator[CustomerRow],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = streamingInsert.insertUnchecked(s"""COPY "sales"."customer"("customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate") FROM STDIN""", batchSize, unsaved, c, CustomerRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: java.util.Iterator[CustomerRowUnsaved],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = streamingInsert.insertUnchecked(s"""COPY "sales"."customer"("personid", "storeid", "territoryid", "customerid", "rowguid", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved, c, CustomerRowUnsaved.pgText)

  override def select: SelectBuilder[CustomerFields, CustomerRow] = SelectBuilder.of(""""sales"."customer"""", CustomerFields.structure, CustomerRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): java.util.List[CustomerRow] = {
    interpolate(Fragment.lit("""select "customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate"
    from "sales"."customer"
    """)).query(CustomerRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(customerid: CustomerId)(using c: Connection): Optional[CustomerRow] = {
    interpolate(Fragment.lit("""select "customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate"
    from "sales"."customer"
    where "customerid" = """), Fragment.encode(CustomerId.pgType, customerid), Fragment.lit("")).query(CustomerRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(customerids: Array[CustomerId])(using c: Connection): java.util.List[CustomerRow] = {
    interpolate(Fragment.lit("""select "customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate"
    from "sales"."customer"
    where "customerid" = ANY("""), Fragment.encode(CustomerId.pgTypeArray, customerids), Fragment.lit(")")).query(CustomerRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(customerids: Array[CustomerId])(using c: Connection): java.util.Map[CustomerId, CustomerRow] = {
    val ret: HashMap[CustomerId, CustomerRow] = new HashMap[CustomerId, CustomerRow]()
    selectByIds(customerids)(using c).forEach(row => ret.put(row.customerid, row): @scala.annotation.nowarn)
    return ret
  }

  override def update: UpdateBuilder[CustomerFields, CustomerRow] = UpdateBuilder.of(""""sales"."customer"""", CustomerFields.structure, CustomerRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: CustomerRow)(using c: Connection): java.lang.Boolean = {
    val customerid: CustomerId = row.customerid
    return interpolate(Fragment.lit("""update "sales"."customer"
    set "personid" = """), Fragment.encode(BusinessentityId.pgType.opt(), row.personid), Fragment.lit("""::int4,
    "storeid" = """), Fragment.encode(BusinessentityId.pgType.opt(), row.storeid), Fragment.lit("""::int4,
    "territoryid" = """), Fragment.encode(SalesterritoryId.pgType.opt(), row.territoryid), Fragment.lit("""::int4,
    "rowguid" = """), Fragment.encode(PgTypes.uuid, row.rowguid), Fragment.lit("""::uuid,
    "modifieddate" = """), Fragment.encode(PgTypes.timestamp, row.modifieddate), Fragment.lit("""::timestamp
    where "customerid" = """), Fragment.encode(CustomerId.pgType, customerid), Fragment.lit("")).update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: CustomerRow)(using c: Connection): CustomerRow = {
  interpolate(Fragment.lit("""insert into "sales"."customer"("customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate")
    values ("""), Fragment.encode(CustomerId.pgType, unsaved.customerid), Fragment.lit("::int4, "), Fragment.encode(BusinessentityId.pgType.opt(), unsaved.personid), Fragment.lit("::int4, "), Fragment.encode(BusinessentityId.pgType.opt(), unsaved.storeid), Fragment.lit("::int4, "), Fragment.encode(SalesterritoryId.pgType.opt(), unsaved.territoryid), Fragment.lit("::int4, "), Fragment.encode(PgTypes.uuid, unsaved.rowguid), Fragment.lit("::uuid, "), Fragment.encode(PgTypes.timestamp, unsaved.modifieddate), Fragment.lit("""::timestamp)
    on conflict ("customerid")
    do update set
      "personid" = EXCLUDED."personid",
    "storeid" = EXCLUDED."storeid",
    "territoryid" = EXCLUDED."territoryid",
    "rowguid" = EXCLUDED."rowguid",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate""""))
    .updateReturning(CustomerRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: java.util.Iterator[CustomerRow])(using c: Connection): java.util.List[CustomerRow] = {
    interpolate(Fragment.lit("""insert into "sales"."customer"("customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate")
    values (?::int4, ?::int4, ?::int4, ?::int4, ?::uuid, ?::timestamp)
    on conflict ("customerid")
    do update set
      "personid" = EXCLUDED."personid",
    "storeid" = EXCLUDED."storeid",
    "territoryid" = EXCLUDED."territoryid",
    "rowguid" = EXCLUDED."rowguid",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate""""))
      .updateManyReturning(CustomerRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: java.util.Iterator[CustomerRow],
    batchSize: Integer = 10000
  )(using c: Connection): Integer = {
    interpolate(Fragment.lit("""create temporary table customer_TEMP (like "sales"."customer") on commit drop""")).update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy customer_TEMP("customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate") from stdin""", batchSize, unsaved, c, CustomerRow.pgText): @scala.annotation.nowarn
    return interpolate(Fragment.lit("""insert into "sales"."customer"("customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate")
    select * from customer_TEMP
    on conflict ("customerid")
    do update set
      "personid" = EXCLUDED."personid",
    "storeid" = EXCLUDED."storeid",
    "territoryid" = EXCLUDED."territoryid",
    "rowguid" = EXCLUDED."rowguid",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table customer_TEMP;""")).update().runUnchecked(c)
  }
}