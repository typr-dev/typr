/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productreview

import adventureworks.customtypes.Defaulted
import adventureworks.production.product.ProductId
import adventureworks.public.Name
import java.time.LocalDateTime
import java.util.Optional
import typo.runtime.PgText
import typo.runtime.PgTypes
import typo.runtime.RowParser
import typo.runtime.RowParsers

/** Table: production.productreview
 * Customer reviews of products they have purchased.
 * Primary key: productreviewid
 */
case class ProductreviewRow(
  /** Primary key for ProductReview records.
   * Default: nextval('production.productreview_productreviewid_seq'::regclass)
   */
  productreviewid: ProductreviewId,
  /** Product identification number. Foreign key to Product.ProductID.
   * Points to [[adventureworks.production.product.ProductRow.productid]]
   */
  productid: ProductId,
  /** Name of the reviewer. */
  reviewername: Name,
  /** Date review was submitted.
   * Default: now()
   */
  reviewdate: LocalDateTime,
  /** Reviewer's e-mail address. */
  emailaddress: String,
  /** Product rating given by the reviewer. Scale is 1 to 5 with 5 as the highest rating.
   * Constraint CK_ProductReview_Rating affecting columns rating: (((rating >= 1) AND (rating <= 5)))
   */
  rating: Integer,
  /** Reviewer's comments */
  comments: Optional[/* max 3850 chars */ String],
  /** Default: now() */
  modifieddate: LocalDateTime
) {
  def id: ProductreviewId = productreviewid

  def toUnsavedRow(
    productreviewid: Defaulted[ProductreviewId],
    reviewdate: Defaulted[LocalDateTime] = Defaulted.Provided(this.reviewdate),
    modifieddate: Defaulted[LocalDateTime] = Defaulted.Provided(this.modifieddate)
  ): ProductreviewRowUnsaved = {
    new ProductreviewRowUnsaved(
      productid,
      reviewername,
      emailaddress,
      rating,
      comments,
      productreviewid,
      reviewdate,
      modifieddate
    )
  }
}

object ProductreviewRow {
  val `_rowParser`: RowParser[ProductreviewRow] = RowParsers.of(ProductreviewId.pgType, ProductId.pgType, Name.pgType, PgTypes.timestamp, PgTypes.text, PgTypes.int4, PgTypes.text.opt(), PgTypes.timestamp, ProductreviewRow.apply, row => Array[Any](row.productreviewid, row.productid, row.reviewername, row.reviewdate, row.emailaddress, row.rating, row.comments, row.modifieddate))

  given pgText: PgText[ProductreviewRow] = PgText.from(`_rowParser`)
}