/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.transactionhistory

import adventureworks.customtypes.Defaulted
import adventureworks.production.product.ProductId
import java.time.LocalDateTime
import typo.runtime.PgText
import typo.runtime.PgTypes
import typo.runtime.RowParser
import typo.runtime.RowParsers

/** Table: production.transactionhistory
 * Record of each purchase order, sales order, or work order transaction year to date.
 * Primary key: transactionid
 */
case class TransactionhistoryRow(
  /** Primary key for TransactionHistory records.
   * Default: nextval('production.transactionhistory_transactionid_seq'::regclass)
   */
  transactionid: TransactionhistoryId,
  /** Product identification number. Foreign key to Product.ProductID.
   * Points to [[adventureworks.production.product.ProductRow.productid]]
   */
  productid: ProductId,
  /** Purchase order, sales order, or work order identification number. */
  referenceorderid: Integer,
  /** Line number associated with the purchase order, sales order, or work order.
   * Default: 0
   */
  referenceorderlineid: Integer,
  /** Date and time of the transaction.
   * Default: now()
   */
  transactiondate: LocalDateTime,
  /** W = WorkOrder, S = SalesOrder, P = PurchaseOrder
   * Constraint CK_TransactionHistory_TransactionType affecting columns transactiontype: ((upper((transactiontype)::text) = ANY (ARRAY['W'::text, 'S'::text, 'P'::text])))
   */
  transactiontype: String,
  /** Product quantity. */
  quantity: Integer,
  /** Product cost. */
  actualcost: java.math.BigDecimal,
  /** Default: now() */
  modifieddate: LocalDateTime
) {
  def id: TransactionhistoryId = transactionid

  def toUnsavedRow(
    transactionid: Defaulted[TransactionhistoryId],
    referenceorderlineid: Defaulted[Integer] = Defaulted.Provided(this.referenceorderlineid),
    transactiondate: Defaulted[LocalDateTime] = Defaulted.Provided(this.transactiondate),
    modifieddate: Defaulted[LocalDateTime] = Defaulted.Provided(this.modifieddate)
  ): TransactionhistoryRowUnsaved = {
    new TransactionhistoryRowUnsaved(
      productid,
      referenceorderid,
      transactiontype,
      quantity,
      actualcost,
      transactionid,
      referenceorderlineid,
      transactiondate,
      modifieddate
    )
  }
}

object TransactionhistoryRow {
  val `_rowParser`: RowParser[TransactionhistoryRow] = RowParsers.of(TransactionhistoryId.pgType, ProductId.pgType, PgTypes.int4, PgTypes.int4, PgTypes.timestamp, PgTypes.bpchar, PgTypes.int4, PgTypes.numeric, PgTypes.timestamp, TransactionhistoryRow.apply, row => Array[Any](row.transactionid, row.productid, row.referenceorderid, row.referenceorderlineid, row.transactiondate, row.transactiontype, row.quantity, row.actualcost, row.modifieddate))

  given pgText: PgText[TransactionhistoryRow] = PgText.from(`_rowParser`)
}