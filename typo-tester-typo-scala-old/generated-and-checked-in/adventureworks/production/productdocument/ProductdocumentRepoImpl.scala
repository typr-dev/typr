/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productdocument

import adventureworks.production.document.DocumentId
import adventureworks.production.product.ProductId
import java.sql.Connection
import java.util.ArrayList
import java.util.HashMap
import java.util.Optional
import typo.dsl.DeleteBuilder
import typo.dsl.Dialect
import typo.dsl.SelectBuilder
import typo.dsl.UpdateBuilder
import typo.runtime.Fragment
import typo.runtime.PgTypes
import typo.runtime.streamingInsert
import typo.runtime.Fragment.interpolate

class ProductdocumentRepoImpl extends ProductdocumentRepo {
  override def delete: DeleteBuilder[ProductdocumentFields, ProductdocumentRow] = DeleteBuilder.of(""""production"."productdocument"""", ProductdocumentFields.structure, Dialect.POSTGRESQL)

  override def deleteById(compositeId: ProductdocumentId)(using c: Connection): java.lang.Boolean = interpolate(Fragment.lit("""delete from "production"."productdocument" where "productid" = """), Fragment.encode(ProductId.pgType, compositeId.productid), Fragment.lit(""" AND "documentnode" = """), Fragment.encode(DocumentId.pgType, compositeId.documentnode), Fragment.lit("")).update().runUnchecked(c) > 0

  override def deleteByIds(compositeIds: Array[ProductdocumentId])(using c: Connection): Integer = {
    val productid: Array[ProductId] = compositeIds.map(_.productid)
    val documentnode: Array[DocumentId] = compositeIds.map(_.documentnode)
    return interpolate(Fragment.lit("""delete
    from "production"."productdocument"
    where ("productid", "documentnode")
    in (select * from unnest("""), Fragment.encode(ProductId.pgTypeArray, productid), Fragment.lit(", "), Fragment.encode(DocumentId.pgTypeArray, documentnode), Fragment.lit("""))
    """)).update().runUnchecked(c)
  }

  override def insert(unsaved: ProductdocumentRow)(using c: Connection): ProductdocumentRow = {
  interpolate(Fragment.lit("""insert into "production"."productdocument"("productid", "modifieddate", "documentnode")
    values ("""), Fragment.encode(ProductId.pgType, unsaved.productid), Fragment.lit("::int4, "), Fragment.encode(PgTypes.timestamp, unsaved.modifieddate), Fragment.lit("::timestamp, "), Fragment.encode(DocumentId.pgType, unsaved.documentnode), Fragment.lit(""")
    RETURNING "productid", "modifieddate", "documentnode"
    """))
    .updateReturning(ProductdocumentRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: ProductdocumentRowUnsaved)(using c: Connection): ProductdocumentRow = {
    val columns: ArrayList[Fragment] = new ArrayList()
    val values: ArrayList[Fragment] = new ArrayList()
    columns.add(Fragment.lit(""""productid"""")): @scala.annotation.nowarn
    values.add(interpolate(Fragment.encode(ProductId.pgType, unsaved.productid), Fragment.lit("::int4"))): @scala.annotation.nowarn
    unsaved.modifieddate.visit(
      {  },
      value => { columns.add(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.add(interpolate(Fragment.encode(PgTypes.timestamp, value), Fragment.lit("::timestamp"))): @scala.annotation.nowarn }
    );
    unsaved.documentnode.visit(
      {  },
      value => { columns.add(Fragment.lit(""""documentnode"""")): @scala.annotation.nowarn; values.add(interpolate(Fragment.encode(DocumentId.pgType, value), Fragment.lit(""))): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      interpolate(Fragment.lit("""insert into "production"."productdocument"("""), Fragment.comma(columns), Fragment.lit(""")
      values ("""), Fragment.comma(values), Fragment.lit(""")
      RETURNING "productid", "modifieddate", "documentnode"
      """))
    }
    return q.updateReturning(ProductdocumentRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: java.util.Iterator[ProductdocumentRow],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = streamingInsert.insertUnchecked(s"""COPY "production"."productdocument"("productid", "modifieddate", "documentnode") FROM STDIN""", batchSize, unsaved, c, ProductdocumentRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: java.util.Iterator[ProductdocumentRowUnsaved],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = streamingInsert.insertUnchecked(s"""COPY "production"."productdocument"("productid", "modifieddate", "documentnode") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved, c, ProductdocumentRowUnsaved.pgText)

  override def select: SelectBuilder[ProductdocumentFields, ProductdocumentRow] = SelectBuilder.of(""""production"."productdocument"""", ProductdocumentFields.structure, ProductdocumentRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): java.util.List[ProductdocumentRow] = {
    interpolate(Fragment.lit("""select "productid", "modifieddate", "documentnode"
    from "production"."productdocument"
    """)).query(ProductdocumentRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(compositeId: ProductdocumentId)(using c: Connection): Optional[ProductdocumentRow] = {
    interpolate(Fragment.lit("""select "productid", "modifieddate", "documentnode"
    from "production"."productdocument"
    where "productid" = """), Fragment.encode(ProductId.pgType, compositeId.productid), Fragment.lit(""" AND "documentnode" = """), Fragment.encode(DocumentId.pgType, compositeId.documentnode), Fragment.lit("")).query(ProductdocumentRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(compositeIds: Array[ProductdocumentId])(using c: Connection): java.util.List[ProductdocumentRow] = {
    val productid: Array[ProductId] = compositeIds.map(_.productid)
    val documentnode: Array[DocumentId] = compositeIds.map(_.documentnode)
    return interpolate(Fragment.lit("""select "productid", "modifieddate", "documentnode"
    from "production"."productdocument"
    where ("productid", "documentnode")
    in (select * from unnest("""), Fragment.encode(ProductId.pgTypeArray, productid), Fragment.lit(", "), Fragment.encode(DocumentId.pgTypeArray, documentnode), Fragment.lit("""))
    """)).query(ProductdocumentRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(compositeIds: Array[ProductdocumentId])(using c: Connection): java.util.Map[ProductdocumentId, ProductdocumentRow] = {
    val ret: HashMap[ProductdocumentId, ProductdocumentRow] = new HashMap[ProductdocumentId, ProductdocumentRow]()
    selectByIds(compositeIds)(using c).forEach(row => ret.put(row.compositeId, row): @scala.annotation.nowarn)
    return ret
  }

  override def update: UpdateBuilder[ProductdocumentFields, ProductdocumentRow] = UpdateBuilder.of(""""production"."productdocument"""", ProductdocumentFields.structure, ProductdocumentRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: ProductdocumentRow)(using c: Connection): java.lang.Boolean = {
    val compositeId: ProductdocumentId = row.compositeId
    return interpolate(Fragment.lit("""update "production"."productdocument"
    set "modifieddate" = """), Fragment.encode(PgTypes.timestamp, row.modifieddate), Fragment.lit("""::timestamp
    where "productid" = """), Fragment.encode(ProductId.pgType, compositeId.productid), Fragment.lit(""" AND "documentnode" = """), Fragment.encode(DocumentId.pgType, compositeId.documentnode), Fragment.lit("")).update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: ProductdocumentRow)(using c: Connection): ProductdocumentRow = {
  interpolate(Fragment.lit("""insert into "production"."productdocument"("productid", "modifieddate", "documentnode")
    values ("""), Fragment.encode(ProductId.pgType, unsaved.productid), Fragment.lit("::int4, "), Fragment.encode(PgTypes.timestamp, unsaved.modifieddate), Fragment.lit("::timestamp, "), Fragment.encode(DocumentId.pgType, unsaved.documentnode), Fragment.lit(""")
    on conflict ("productid", "documentnode")
    do update set
      "modifieddate" = EXCLUDED."modifieddate"
    returning "productid", "modifieddate", "documentnode""""))
    .updateReturning(ProductdocumentRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: java.util.Iterator[ProductdocumentRow])(using c: Connection): java.util.List[ProductdocumentRow] = {
    interpolate(Fragment.lit("""insert into "production"."productdocument"("productid", "modifieddate", "documentnode")
    values (?::int4, ?::timestamp, ?)
    on conflict ("productid", "documentnode")
    do update set
      "modifieddate" = EXCLUDED."modifieddate"
    returning "productid", "modifieddate", "documentnode""""))
      .updateManyReturning(ProductdocumentRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: java.util.Iterator[ProductdocumentRow],
    batchSize: Integer = 10000
  )(using c: Connection): Integer = {
    interpolate(Fragment.lit("""create temporary table productdocument_TEMP (like "production"."productdocument") on commit drop""")).update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy productdocument_TEMP("productid", "modifieddate", "documentnode") from stdin""", batchSize, unsaved, c, ProductdocumentRow.pgText): @scala.annotation.nowarn
    return interpolate(Fragment.lit("""insert into "production"."productdocument"("productid", "modifieddate", "documentnode")
    select * from productdocument_TEMP
    on conflict ("productid", "documentnode")
    do update set
      "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table productdocument_TEMP;""")).update().runUnchecked(c)
  }
}