/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.culture

import adventureworks.public.Name
import java.sql.Connection
import java.util.ArrayList
import java.util.HashMap
import java.util.Optional
import typo.dsl.DeleteBuilder
import typo.dsl.Dialect
import typo.dsl.SelectBuilder
import typo.dsl.UpdateBuilder
import typo.runtime.Fragment
import typo.runtime.PgTypes
import typo.runtime.streamingInsert
import typo.runtime.Fragment.interpolate

class CultureRepoImpl extends CultureRepo {
  override def delete: DeleteBuilder[CultureFields, CultureRow] = DeleteBuilder.of(""""production"."culture"""", CultureFields.structure, Dialect.POSTGRESQL)

  override def deleteById(cultureid: CultureId)(using c: Connection): java.lang.Boolean = interpolate(Fragment.lit("""delete from "production"."culture" where "cultureid" = """), Fragment.encode(CultureId.pgType, cultureid), Fragment.lit("")).update().runUnchecked(c) > 0

  override def deleteByIds(cultureids: Array[CultureId])(using c: Connection): Integer = {
    interpolate(Fragment.lit("""delete
    from "production"."culture"
    where "cultureid" = ANY("""), Fragment.encode(CultureId.pgTypeArray, cultureids), Fragment.lit(")"))
      .update()
      .runUnchecked(c)
  }

  override def insert(unsaved: CultureRow)(using c: Connection): CultureRow = {
  interpolate(Fragment.lit("""insert into "production"."culture"("cultureid", "name", "modifieddate")
    values ("""), Fragment.encode(CultureId.pgType, unsaved.cultureid), Fragment.lit("::bpchar, "), Fragment.encode(Name.pgType, unsaved.name), Fragment.lit("::varchar, "), Fragment.encode(PgTypes.timestamp, unsaved.modifieddate), Fragment.lit("""::timestamp)
    RETURNING "cultureid", "name", "modifieddate"
    """))
    .updateReturning(CultureRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: CultureRowUnsaved)(using c: Connection): CultureRow = {
    val columns: ArrayList[Fragment] = new ArrayList()
    val values: ArrayList[Fragment] = new ArrayList()
    columns.add(Fragment.lit(""""cultureid"""")): @scala.annotation.nowarn
    values.add(interpolate(Fragment.encode(CultureId.pgType, unsaved.cultureid), Fragment.lit("::bpchar"))): @scala.annotation.nowarn
    columns.add(Fragment.lit(""""name"""")): @scala.annotation.nowarn
    values.add(interpolate(Fragment.encode(Name.pgType, unsaved.name), Fragment.lit("::varchar"))): @scala.annotation.nowarn
    unsaved.modifieddate.visit(
      {  },
      value => { columns.add(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.add(interpolate(Fragment.encode(PgTypes.timestamp, value), Fragment.lit("::timestamp"))): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      interpolate(Fragment.lit("""insert into "production"."culture"("""), Fragment.comma(columns), Fragment.lit(""")
      values ("""), Fragment.comma(values), Fragment.lit(""")
      RETURNING "cultureid", "name", "modifieddate"
      """))
    }
    return q.updateReturning(CultureRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: java.util.Iterator[CultureRow],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = streamingInsert.insertUnchecked(s"""COPY "production"."culture"("cultureid", "name", "modifieddate") FROM STDIN""", batchSize, unsaved, c, CultureRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: java.util.Iterator[CultureRowUnsaved],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = streamingInsert.insertUnchecked(s"""COPY "production"."culture"("cultureid", "name", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved, c, CultureRowUnsaved.pgText)

  override def select: SelectBuilder[CultureFields, CultureRow] = SelectBuilder.of(""""production"."culture"""", CultureFields.structure, CultureRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): java.util.List[CultureRow] = {
    interpolate(Fragment.lit("""select "cultureid", "name", "modifieddate"
    from "production"."culture"
    """)).query(CultureRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(cultureid: CultureId)(using c: Connection): Optional[CultureRow] = {
    interpolate(Fragment.lit("""select "cultureid", "name", "modifieddate"
    from "production"."culture"
    where "cultureid" = """), Fragment.encode(CultureId.pgType, cultureid), Fragment.lit("")).query(CultureRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(cultureids: Array[CultureId])(using c: Connection): java.util.List[CultureRow] = {
    interpolate(Fragment.lit("""select "cultureid", "name", "modifieddate"
    from "production"."culture"
    where "cultureid" = ANY("""), Fragment.encode(CultureId.pgTypeArray, cultureids), Fragment.lit(")")).query(CultureRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(cultureids: Array[CultureId])(using c: Connection): java.util.Map[CultureId, CultureRow] = {
    val ret: HashMap[CultureId, CultureRow] = new HashMap[CultureId, CultureRow]()
    selectByIds(cultureids)(using c).forEach(row => ret.put(row.cultureid, row): @scala.annotation.nowarn)
    return ret
  }

  override def update: UpdateBuilder[CultureFields, CultureRow] = UpdateBuilder.of(""""production"."culture"""", CultureFields.structure, CultureRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: CultureRow)(using c: Connection): java.lang.Boolean = {
    val cultureid: CultureId = row.cultureid
    return interpolate(Fragment.lit("""update "production"."culture"
    set "name" = """), Fragment.encode(Name.pgType, row.name), Fragment.lit("""::varchar,
    "modifieddate" = """), Fragment.encode(PgTypes.timestamp, row.modifieddate), Fragment.lit("""::timestamp
    where "cultureid" = """), Fragment.encode(CultureId.pgType, cultureid), Fragment.lit("")).update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: CultureRow)(using c: Connection): CultureRow = {
  interpolate(Fragment.lit("""insert into "production"."culture"("cultureid", "name", "modifieddate")
    values ("""), Fragment.encode(CultureId.pgType, unsaved.cultureid), Fragment.lit("::bpchar, "), Fragment.encode(Name.pgType, unsaved.name), Fragment.lit("::varchar, "), Fragment.encode(PgTypes.timestamp, unsaved.modifieddate), Fragment.lit("""::timestamp)
    on conflict ("cultureid")
    do update set
      "name" = EXCLUDED."name",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "cultureid", "name", "modifieddate""""))
    .updateReturning(CultureRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: java.util.Iterator[CultureRow])(using c: Connection): java.util.List[CultureRow] = {
    interpolate(Fragment.lit("""insert into "production"."culture"("cultureid", "name", "modifieddate")
    values (?::bpchar, ?::varchar, ?::timestamp)
    on conflict ("cultureid")
    do update set
      "name" = EXCLUDED."name",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "cultureid", "name", "modifieddate""""))
      .updateManyReturning(CultureRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: java.util.Iterator[CultureRow],
    batchSize: Integer = 10000
  )(using c: Connection): Integer = {
    interpolate(Fragment.lit("""create temporary table culture_TEMP (like "production"."culture") on commit drop""")).update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy culture_TEMP("cultureid", "name", "modifieddate") from stdin""", batchSize, unsaved, c, CultureRow.pgText): @scala.annotation.nowarn
    return interpolate(Fragment.lit("""insert into "production"."culture"("cultureid", "name", "modifieddate")
    select * from culture_TEMP
    on conflict ("cultureid")
    do update set
      "name" = EXCLUDED."name",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table culture_TEMP;""")).update().runUnchecked(c)
  }
}