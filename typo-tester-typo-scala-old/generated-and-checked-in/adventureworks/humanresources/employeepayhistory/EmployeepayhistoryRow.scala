/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.humanresources.employeepayhistory

import adventureworks.customtypes.Defaulted
import adventureworks.person.businessentity.BusinessentityId
import java.time.LocalDateTime
import typo.runtime.PgText
import typo.runtime.PgTypes
import typo.runtime.RowParser
import typo.runtime.RowParsers

/** Table: humanresources.employeepayhistory
 * Employee pay history.
 * Composite primary key: businessentityid, ratechangedate
 */
case class EmployeepayhistoryRow(
  /** Employee identification number. Foreign key to Employee.BusinessEntityID.
   * Points to [[adventureworks.humanresources.employee.EmployeeRow.businessentityid]]
   */
  businessentityid: BusinessentityId,
  /** Date the change in pay is effective */
  ratechangedate: LocalDateTime,
  /** Salary hourly rate.
   * Constraint CK_EmployeePayHistory_Rate affecting columns rate: (((rate >= 6.50) AND (rate <= 200.00)))
   */
  rate: java.math.BigDecimal,
  /** 1 = Salary received monthly, 2 = Salary received biweekly
   * Constraint CK_EmployeePayHistory_PayFrequency affecting columns payfrequency: ((payfrequency = ANY (ARRAY[1, 2])))
   */
  payfrequency: java.lang.Short,
  /** Default: now() */
  modifieddate: LocalDateTime
) {
  def compositeId: EmployeepayhistoryId = new EmployeepayhistoryId(businessentityid, ratechangedate)

  def id: EmployeepayhistoryId = this.compositeId

  def toUnsavedRow(modifieddate: Defaulted[LocalDateTime] = Defaulted.Provided(this.modifieddate)): EmployeepayhistoryRowUnsaved = {
    new EmployeepayhistoryRowUnsaved(
      businessentityid,
      ratechangedate,
      rate,
      payfrequency,
      modifieddate
    )
  }
}

object EmployeepayhistoryRow {
  val `_rowParser`: RowParser[EmployeepayhistoryRow] = RowParsers.of(BusinessentityId.pgType, PgTypes.timestamp, PgTypes.numeric, PgTypes.int2, PgTypes.timestamp, EmployeepayhistoryRow.apply, row => Array[Any](row.businessentityid, row.ratechangedate, row.rate, row.payfrequency, row.modifieddate))

  def apply(
    compositeId: EmployeepayhistoryId,
    rate: java.math.BigDecimal,
    payfrequency: java.lang.Short,
    modifieddate: LocalDateTime
  ): EmployeepayhistoryRow = {
    new EmployeepayhistoryRow(
      compositeId.businessentityid,
      compositeId.ratechangedate,
      rate,
      payfrequency,
      modifieddate
    )
  }

  given pgText: PgText[EmployeepayhistoryRow] = PgText.from(`_rowParser`)
}