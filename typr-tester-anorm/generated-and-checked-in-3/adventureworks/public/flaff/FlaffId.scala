/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.flaff

import adventureworks.public.ShortText
import anorm.Column
import anorm.RowParser
import anorm.Success
import play.api.libs.json.JsObject
import play.api.libs.json.JsResult
import play.api.libs.json.JsValue
import play.api.libs.json.OWrites
import play.api.libs.json.Reads
import play.api.libs.json.Writes
import scala.collection.immutable.ListMap
import scala.util.Try

/** Type for the composite primary key of table `public.flaff` */
case class FlaffId(
  code: ShortText,
  anotherCode: String,
  someNumber: Int,
  specifier: ShortText
)

object FlaffId {
  given reads: Reads[FlaffId] = {
    Reads[FlaffId](json => JsResult.fromTry(
        Try(
          FlaffId(
            code = json.\("code").as(using ShortText.reads),
            anotherCode = json.\("another_code").as(using Reads.StringReads),
            someNumber = json.\("some_number").as(using Reads.IntReads),
            specifier = json.\("specifier").as(using ShortText.reads)
          )
        )
      ),
    )
  }

  def rowParser(idx: Int): RowParser[FlaffId] = {
    RowParser[FlaffId] { row =>
      Success(
        FlaffId(
          code = row(idx + 0)(using ShortText.column),
          anotherCode = row(idx + 1)(using Column.columnToString),
          someNumber = row(idx + 2)(using Column.columnToInt),
          specifier = row(idx + 3)(using ShortText.column)
        )
      )
    }
  }

  given writes: OWrites[FlaffId] = {
    OWrites[FlaffId](o =>
      new JsObject(ListMap[String, JsValue](
        "code" -> ShortText.writes.writes(o.code),
        "another_code" -> Writes.StringWrites.writes(o.anotherCode),
        "some_number" -> Writes.IntWrites.writes(o.someNumber),
        "specifier" -> ShortText.writes.writes(o.specifier)
      ))
    )
  }
}