/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.issue142

import adventureworks.Text
import anorm.Column
import anorm.ParameterMetaData
import anorm.ToStatement
import java.sql.Types
import play.api.libs.json.JsValue
import play.api.libs.json.Reads
import play.api.libs.json.Writes

/** Type for the primary key of table `public.issue142`. It has some known values: 
 *  - aa
 *  - bb
 */

sealed abstract class Issue142Id(val value: String)

object Issue142Id {
  def apply(underlying: String): Issue142Id =
    ByName.getOrElse(underlying, Unknown(underlying))
  given arrayColumn: Column[Array[Issue142Id]] = Column.columnToArray[String](using Column.columnToString, summon).map(_.map(Issue142Id.apply))

  given column: Column[Issue142Id] = Column.columnToString.map(Issue142Id.apply)

  given toStatement: ToStatement[Issue142Id] = ToStatement.stringToStatement.contramap(_.value)

  given arrayToStatement: ToStatement[Array[Issue142Id]] = ToStatement.arrayToParameter(using ParameterMetaData.StringParameterMetaData).contramap(_.map(_.value))

  given parameterMetadata: ParameterMetaData[Issue142Id] = {
    new ParameterMetaData[Issue142Id] {
      override def sqlType: String = "text"
      override def jdbcType: Int = Types.OTHER
    }
  }

  given pgText: Text[Issue142Id] = {
    new Text[Issue142Id] {
      override def unsafeEncode(v: Issue142Id, sb: StringBuilder): Unit = Text.stringInstance.unsafeEncode(v.value, sb)
      override def unsafeArrayEncode(v: Issue142Id, sb: StringBuilder): Unit = Text.stringInstance.unsafeArrayEncode(v.value, sb)
    }
  }

  given reads: Reads[Issue142Id] = Reads[Issue142Id]{(value: JsValue) => value.validate(using Reads.StringReads).map(Issue142Id.apply)}

  given writes: Writes[Issue142Id] = Writes[Issue142Id](value => Writes.StringWrites.writes(value.value))

  case object aa extends Issue142Id("aa")

  case object bb extends Issue142Id("bb")
  case class Unknown(override val value: String) extends Issue142Id(value)
  val All: scala.List[Issue142Id] = scala.List(aa, bb)
  val ByName: scala.collection.immutable.Map[String, Issue142Id] = All.map(x => (x.value, x)).toMap

}