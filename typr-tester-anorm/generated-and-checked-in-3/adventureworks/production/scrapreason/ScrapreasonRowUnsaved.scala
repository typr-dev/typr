/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.scrapreason

import adventureworks.Text
import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.Defaulted.UseDefault
import adventureworks.customtypes.TypoLocalDateTime
import adventureworks.public.Name
import play.api.libs.json.JsObject
import play.api.libs.json.JsResult
import play.api.libs.json.JsValue
import play.api.libs.json.OWrites
import play.api.libs.json.Reads
import scala.collection.immutable.ListMap
import scala.util.Try

/** This class corresponds to a row in table `production.scrapreason` which has not been persisted yet */
case class ScrapreasonRowUnsaved(
  /** Failure description. */
  name: Name,
  /** Default: nextval('production.scrapreason_scrapreasonid_seq'::regclass)
   * Primary key for ScrapReason records.
   */
  scrapreasonid: Defaulted[ScrapreasonId] = new UseDefault(),
  /** Default: now() */
  modifieddate: Defaulted[TypoLocalDateTime] = new UseDefault()
) {
  def toRow(
    scrapreasonidDefault: => ScrapreasonId,
    modifieddateDefault: => TypoLocalDateTime
  ): ScrapreasonRow = new ScrapreasonRow(scrapreasonid = scrapreasonid.getOrElse(scrapreasonidDefault), name = name, modifieddate = modifieddate.getOrElse(modifieddateDefault))
}

object ScrapreasonRowUnsaved {
  given pgText: Text[ScrapreasonRowUnsaved] = {
    Text.instance[ScrapreasonRowUnsaved]{ (row, sb) =>
      Name.pgText.unsafeEncode(row.name, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using ScrapreasonId.pgText).unsafeEncode(row.scrapreasonid, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using TypoLocalDateTime.pgText).unsafeEncode(row.modifieddate, sb)
    }
  }

  given reads: Reads[ScrapreasonRowUnsaved] = {
    Reads[ScrapreasonRowUnsaved](json => JsResult.fromTry(
        Try(
          ScrapreasonRowUnsaved(
            name = json.\("name").as(using Name.reads),
            scrapreasonid = json.\("scrapreasonid").as(using Defaulted.reads(using ScrapreasonId.reads)),
            modifieddate = json.\("modifieddate").as(using Defaulted.reads(using TypoLocalDateTime.reads))
          )
        )
      ),
    )
  }

  given writes: OWrites[ScrapreasonRowUnsaved] = {
    OWrites[ScrapreasonRowUnsaved](o =>
      new JsObject(ListMap[String, JsValue](
        "name" -> Name.writes.writes(o.name),
        "scrapreasonid" -> Defaulted.writes(using ScrapreasonId.writes).writes(o.scrapreasonid),
        "modifieddate" -> Defaulted.writes(using TypoLocalDateTime.writes).writes(o.modifieddate)
      ))
    )
  }
}