/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.transactionhistoryarchive

import java.sql.Connection
import scala.annotation.nowarn
import typr.dsl.DeleteBuilder
import typr.dsl.DeleteBuilderMock
import typr.dsl.DeleteParams
import typr.dsl.SelectBuilder
import typr.dsl.SelectBuilderMock
import typr.dsl.SelectParams
import typr.dsl.UpdateBuilder
import typr.dsl.UpdateBuilderMock
import typr.dsl.UpdateParams

case class TransactionhistoryarchiveRepoMock(
  toRow: TransactionhistoryarchiveRowUnsaved => TransactionhistoryarchiveRow,
  map: scala.collection.mutable.Map[TransactionhistoryarchiveId, TransactionhistoryarchiveRow] = scala.collection.mutable.Map.empty[TransactionhistoryarchiveId, TransactionhistoryarchiveRow]
) extends TransactionhistoryarchiveRepo {
  override def delete: DeleteBuilder[TransactionhistoryarchiveFields, TransactionhistoryarchiveRow] = DeleteBuilderMock(DeleteParams.empty, TransactionhistoryarchiveFields.structure, map)

  override def deleteById(transactionid: TransactionhistoryarchiveId)(using c: Connection): Boolean = map.remove(transactionid).isDefined

  override def deleteByIds(transactionids: Array[TransactionhistoryarchiveId])(using c: Connection): Int = transactionids.map(id => map.remove(id)).count(_.isDefined)

  override def insert(unsaved: TransactionhistoryarchiveRow)(using c: Connection): TransactionhistoryarchiveRow = {
    val _ = if (map.contains(unsaved.transactionid))
      sys.error(s"id ${unsaved.transactionid} already exists")
    else
      map.put(unsaved.transactionid, unsaved)
  
    unsaved
  }

  override def insert(unsaved: TransactionhistoryarchiveRowUnsaved)(using c: Connection): TransactionhistoryarchiveRow = insert(toRow(unsaved))

  override def insertStreaming(
    unsaved: Iterator[TransactionhistoryarchiveRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.transactionid -> row)
    }
    unsaved.size.toLong
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[TransactionhistoryarchiveRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.transactionid -> row)
    }
    unsaved.size.toLong
  }

  override def select: SelectBuilder[TransactionhistoryarchiveFields, TransactionhistoryarchiveRow] = SelectBuilderMock(TransactionhistoryarchiveFields.structure, () => map.values.toList, SelectParams.empty)

  override def selectAll(using c: Connection): List[TransactionhistoryarchiveRow] = map.values.toList

  override def selectById(transactionid: TransactionhistoryarchiveId)(using c: Connection): Option[TransactionhistoryarchiveRow] = map.get(transactionid)

  override def selectByIds(transactionids: Array[TransactionhistoryarchiveId])(using c: Connection): List[TransactionhistoryarchiveRow] = transactionids.flatMap(map.get).toList

  override def selectByIdsTracked(transactionids: Array[TransactionhistoryarchiveId])(using c: Connection): Map[TransactionhistoryarchiveId, TransactionhistoryarchiveRow] = {
    val byId = selectByIds(transactionids).view.map(x => (x.transactionid, x)).toMap
    transactionids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  override def update: UpdateBuilder[TransactionhistoryarchiveFields, TransactionhistoryarchiveRow] = UpdateBuilderMock(UpdateParams.empty, TransactionhistoryarchiveFields.structure, map)

  override def update(row: TransactionhistoryarchiveRow)(using c: Connection): Option[TransactionhistoryarchiveRow] = {
    map.get(row.transactionid).map { _ =>
      map.put(row.transactionid, row): @nowarn
      row
    }
  }

  override def upsert(unsaved: TransactionhistoryarchiveRow)(using c: Connection): TransactionhistoryarchiveRow = {
    map.put(unsaved.transactionid, unsaved): @nowarn
    unsaved
  }

  override def upsertBatch(unsaved: Iterable[TransactionhistoryarchiveRow])(using c: Connection): List[TransactionhistoryarchiveRow] = {
    unsaved.map { row =>
      map += (row.transactionid -> row)
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[TransactionhistoryarchiveRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.transactionid -> row)
    }
    unsaved.size
  }
}