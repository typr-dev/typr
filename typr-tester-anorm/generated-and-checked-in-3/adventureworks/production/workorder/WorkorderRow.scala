/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.workorder

import adventureworks.Text
import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.TypoLocalDateTime
import adventureworks.customtypes.TypoShort
import adventureworks.production.product.ProductId
import adventureworks.production.scrapreason.ScrapreasonId
import anorm.Column
import anorm.RowParser
import anorm.Success
import play.api.libs.json.JsObject
import play.api.libs.json.JsResult
import play.api.libs.json.JsValue
import play.api.libs.json.OWrites
import play.api.libs.json.Reads
import play.api.libs.json.Writes
import scala.collection.immutable.ListMap
import scala.util.Try

/** Table: production.workorder
 * Manufacturing work orders.
 * Primary key: workorderid
 */
case class WorkorderRow(
  /** Primary key for WorkOrder records.
   * Default: nextval('production.workorder_workorderid_seq'::regclass)
   */
  workorderid: WorkorderId,
  /** Product identification number. Foreign key to Product.ProductID.
   * Points to [[adventureworks.production.product.ProductRow.productid]]
   */
  productid: ProductId,
  /** Product quantity to build.
   * Constraint CK_WorkOrder_OrderQty affecting columns orderqty: ((orderqty > 0))
   */
  orderqty: Int,
  /** Quantity that failed inspection.
   * Constraint CK_WorkOrder_ScrappedQty affecting columns scrappedqty: ((scrappedqty >= 0))
   */
  scrappedqty: TypoShort,
  /** Work order start date.
   * Constraint CK_WorkOrder_EndDate affecting columns enddate, startdate: (((enddate >= startdate) OR (enddate IS NULL)))
   */
  startdate: TypoLocalDateTime,
  /** Work order end date.
   * Constraint CK_WorkOrder_EndDate affecting columns enddate, startdate: (((enddate >= startdate) OR (enddate IS NULL)))
   */
  enddate: Option[TypoLocalDateTime],
  /** Work order due date. */
  duedate: TypoLocalDateTime,
  /** Reason for inspection failure.
   * Points to [[adventureworks.production.scrapreason.ScrapreasonRow.scrapreasonid]]
   */
  scrapreasonid: Option[ScrapreasonId],
  /** Default: now() */
  modifieddate: TypoLocalDateTime
) {
  def id: WorkorderId = workorderid

  def toUnsavedRow(
    workorderid: Defaulted[WorkorderId],
    modifieddate: Defaulted[TypoLocalDateTime] = Defaulted.Provided(this.modifieddate)
  ): WorkorderRowUnsaved = {
    new WorkorderRowUnsaved(
      productid,
      orderqty,
      scrappedqty,
      startdate,
      enddate,
      duedate,
      scrapreasonid,
      workorderid,
      modifieddate
    )
  }
}

object WorkorderRow {
  given pgText: Text[WorkorderRow] = {
    Text.instance[WorkorderRow]{ (row, sb) =>
      WorkorderId.pgText.unsafeEncode(row.workorderid, sb)
      sb.append(Text.DELIMETER)
      ProductId.pgText.unsafeEncode(row.productid, sb)
      sb.append(Text.DELIMETER)
      Text.intInstance.unsafeEncode(row.orderqty, sb)
      sb.append(Text.DELIMETER)
      TypoShort.pgText.unsafeEncode(row.scrappedqty, sb)
      sb.append(Text.DELIMETER)
      TypoLocalDateTime.pgText.unsafeEncode(row.startdate, sb)
      sb.append(Text.DELIMETER)
      Text.option(using TypoLocalDateTime.pgText).unsafeEncode(row.enddate, sb)
      sb.append(Text.DELIMETER)
      TypoLocalDateTime.pgText.unsafeEncode(row.duedate, sb)
      sb.append(Text.DELIMETER)
      Text.option(using ScrapreasonId.pgText).unsafeEncode(row.scrapreasonid, sb)
      sb.append(Text.DELIMETER)
      TypoLocalDateTime.pgText.unsafeEncode(row.modifieddate, sb)
    }
  }

  given reads: Reads[WorkorderRow] = {
    Reads[WorkorderRow](json => JsResult.fromTry(
        Try(
          WorkorderRow(
            workorderid = json.\("workorderid").as(using WorkorderId.reads),
            productid = json.\("productid").as(using ProductId.reads),
            orderqty = json.\("orderqty").as(using Reads.IntReads),
            scrappedqty = json.\("scrappedqty").as(using TypoShort.reads),
            startdate = json.\("startdate").as(using TypoLocalDateTime.reads),
            enddate = json.\("enddate").toOption.map(_.as(using TypoLocalDateTime.reads)),
            duedate = json.\("duedate").as(using TypoLocalDateTime.reads),
            scrapreasonid = json.\("scrapreasonid").toOption.map(_.as(using ScrapreasonId.reads)),
            modifieddate = json.\("modifieddate").as(using TypoLocalDateTime.reads)
          )
        )
      ),
    )
  }

  def rowParser(idx: Int): RowParser[WorkorderRow] = {
    RowParser[WorkorderRow] { row =>
      Success(
        WorkorderRow(
          workorderid = row(idx + 0)(using WorkorderId.column),
          productid = row(idx + 1)(using ProductId.column),
          orderqty = row(idx + 2)(using Column.columnToInt),
          scrappedqty = row(idx + 3)(using TypoShort.column),
          startdate = row(idx + 4)(using TypoLocalDateTime.column),
          enddate = row(idx + 5)(using Column.columnToOption(using TypoLocalDateTime.column)),
          duedate = row(idx + 6)(using TypoLocalDateTime.column),
          scrapreasonid = row(idx + 7)(using Column.columnToOption(using ScrapreasonId.column)),
          modifieddate = row(idx + 8)(using TypoLocalDateTime.column)
        )
      )
    }
  }

  given writes: OWrites[WorkorderRow] = {
    OWrites[WorkorderRow](o =>
      new JsObject(ListMap[String, JsValue](
        "workorderid" -> WorkorderId.writes.writes(o.workorderid),
        "productid" -> ProductId.writes.writes(o.productid),
        "orderqty" -> Writes.IntWrites.writes(o.orderqty),
        "scrappedqty" -> TypoShort.writes.writes(o.scrappedqty),
        "startdate" -> TypoLocalDateTime.writes.writes(o.startdate),
        "enddate" -> Writes.OptionWrites(using TypoLocalDateTime.writes).writes(o.enddate),
        "duedate" -> TypoLocalDateTime.writes.writes(o.duedate),
        "scrapreasonid" -> Writes.OptionWrites(using ScrapreasonId.writes).writes(o.scrapreasonid),
        "modifieddate" -> TypoLocalDateTime.writes.writes(o.modifieddate)
      ))
    )
  }
}