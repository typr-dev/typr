/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productproductphoto

import java.sql.Connection
import scala.annotation.nowarn
import typr.dsl.DeleteBuilder
import typr.dsl.DeleteBuilderMock
import typr.dsl.DeleteParams
import typr.dsl.SelectBuilder
import typr.dsl.SelectBuilderMock
import typr.dsl.SelectParams
import typr.dsl.UpdateBuilder
import typr.dsl.UpdateBuilderMock
import typr.dsl.UpdateParams

case class ProductproductphotoRepoMock(
  toRow: ProductproductphotoRowUnsaved => ProductproductphotoRow,
  map: scala.collection.mutable.Map[ProductproductphotoId, ProductproductphotoRow] = scala.collection.mutable.Map.empty[ProductproductphotoId, ProductproductphotoRow]
) extends ProductproductphotoRepo {
  override def delete: DeleteBuilder[ProductproductphotoFields, ProductproductphotoRow] = DeleteBuilderMock(DeleteParams.empty, ProductproductphotoFields.structure, map)

  override def deleteById(compositeId: ProductproductphotoId)(using c: Connection): Boolean = map.remove(compositeId).isDefined

  override def deleteByIds(compositeIds: Array[ProductproductphotoId])(using c: Connection): Int = compositeIds.map(id => map.remove(id)).count(_.isDefined)

  override def insert(unsaved: ProductproductphotoRow)(using c: Connection): ProductproductphotoRow = {
    val _ = if (map.contains(unsaved.compositeId))
      sys.error(s"id ${unsaved.compositeId} already exists")
    else
      map.put(unsaved.compositeId, unsaved)
  
    unsaved
  }

  override def insert(unsaved: ProductproductphotoRowUnsaved)(using c: Connection): ProductproductphotoRow = insert(toRow(unsaved))

  override def insertStreaming(
    unsaved: Iterator[ProductproductphotoRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.compositeId -> row)
    }
    unsaved.size.toLong
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[ProductproductphotoRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.compositeId -> row)
    }
    unsaved.size.toLong
  }

  override def select: SelectBuilder[ProductproductphotoFields, ProductproductphotoRow] = SelectBuilderMock(ProductproductphotoFields.structure, () => map.values.toList, SelectParams.empty)

  override def selectAll(using c: Connection): List[ProductproductphotoRow] = map.values.toList

  override def selectById(compositeId: ProductproductphotoId)(using c: Connection): Option[ProductproductphotoRow] = map.get(compositeId)

  override def selectByIds(compositeIds: Array[ProductproductphotoId])(using c: Connection): List[ProductproductphotoRow] = compositeIds.flatMap(map.get).toList

  override def selectByIdsTracked(compositeIds: Array[ProductproductphotoId])(using c: Connection): Map[ProductproductphotoId, ProductproductphotoRow] = {
    val byId = selectByIds(compositeIds).view.map(x => (x.compositeId, x)).toMap
    compositeIds.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  override def update: UpdateBuilder[ProductproductphotoFields, ProductproductphotoRow] = UpdateBuilderMock(UpdateParams.empty, ProductproductphotoFields.structure, map)

  override def update(row: ProductproductphotoRow)(using c: Connection): Option[ProductproductphotoRow] = {
    map.get(row.compositeId).map { _ =>
      map.put(row.compositeId, row): @nowarn
      row
    }
  }

  override def upsert(unsaved: ProductproductphotoRow)(using c: Connection): ProductproductphotoRow = {
    map.put(unsaved.compositeId, unsaved): @nowarn
    unsaved
  }

  override def upsertBatch(unsaved: Iterable[ProductproductphotoRow])(using c: Connection): List[ProductproductphotoRow] = {
    unsaved.map { row =>
      map += (row.compositeId -> row)
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[ProductproductphotoRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.compositeId -> row)
    }
    unsaved.size
  }
}