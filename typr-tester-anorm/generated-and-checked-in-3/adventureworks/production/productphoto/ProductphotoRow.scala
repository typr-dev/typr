/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productphoto

import adventureworks.Text
import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.TypoBytea
import adventureworks.customtypes.TypoLocalDateTime
import anorm.Column
import anorm.RowParser
import anorm.Success
import play.api.libs.json.JsObject
import play.api.libs.json.JsResult
import play.api.libs.json.JsValue
import play.api.libs.json.OWrites
import play.api.libs.json.Reads
import play.api.libs.json.Writes
import scala.collection.immutable.ListMap
import scala.util.Try

/** Table: production.productphoto
 * Product images.
 * Primary key: productphotoid
 */
case class ProductphotoRow(
  /** Primary key for ProductPhoto records.
   * Default: nextval('production.productphoto_productphotoid_seq'::regclass)
   */
  productphotoid: ProductphotoId,
  /** Small image of the product. */
  thumbnailphoto: Option[TypoBytea],
  /** Small image file name. */
  thumbnailphotofilename: Option[/* max 50 chars */ String],
  /** Large image of the product. */
  largephoto: Option[TypoBytea],
  /** Large image file name. */
  largephotofilename: Option[/* max 50 chars */ String],
  /** Default: now() */
  modifieddate: TypoLocalDateTime
) {
  def id: ProductphotoId = productphotoid

  def toUnsavedRow(
    productphotoid: Defaulted[ProductphotoId],
    modifieddate: Defaulted[TypoLocalDateTime] = Defaulted.Provided(this.modifieddate)
  ): ProductphotoRowUnsaved = {
    new ProductphotoRowUnsaved(
      thumbnailphoto,
      thumbnailphotofilename,
      largephoto,
      largephotofilename,
      productphotoid,
      modifieddate
    )
  }
}

object ProductphotoRow {
  given pgText: Text[ProductphotoRow] = {
    Text.instance[ProductphotoRow]{ (row, sb) =>
      ProductphotoId.pgText.unsafeEncode(row.productphotoid, sb)
      sb.append(Text.DELIMETER)
      Text.option(using TypoBytea.pgText).unsafeEncode(row.thumbnailphoto, sb)
      sb.append(Text.DELIMETER)
      Text.option(using Text.stringInstance).unsafeEncode(row.thumbnailphotofilename, sb)
      sb.append(Text.DELIMETER)
      Text.option(using TypoBytea.pgText).unsafeEncode(row.largephoto, sb)
      sb.append(Text.DELIMETER)
      Text.option(using Text.stringInstance).unsafeEncode(row.largephotofilename, sb)
      sb.append(Text.DELIMETER)
      TypoLocalDateTime.pgText.unsafeEncode(row.modifieddate, sb)
    }
  }

  given reads: Reads[ProductphotoRow] = {
    Reads[ProductphotoRow](json => JsResult.fromTry(
        Try(
          ProductphotoRow(
            productphotoid = json.\("productphotoid").as(using ProductphotoId.reads),
            thumbnailphoto = json.\("thumbnailphoto").toOption.map(_.as(using TypoBytea.reads)),
            thumbnailphotofilename = json.\("thumbnailphotofilename").toOption.map(_.as(using Reads.StringReads)),
            largephoto = json.\("largephoto").toOption.map(_.as(using TypoBytea.reads)),
            largephotofilename = json.\("largephotofilename").toOption.map(_.as(using Reads.StringReads)),
            modifieddate = json.\("modifieddate").as(using TypoLocalDateTime.reads)
          )
        )
      ),
    )
  }

  def rowParser(idx: Int): RowParser[ProductphotoRow] = {
    RowParser[ProductphotoRow] { row =>
      Success(
        ProductphotoRow(
          productphotoid = row(idx + 0)(using ProductphotoId.column),
          thumbnailphoto = row(idx + 1)(using Column.columnToOption(using TypoBytea.column)),
          thumbnailphotofilename = row(idx + 2)(using Column.columnToOption(using Column.columnToString)),
          largephoto = row(idx + 3)(using Column.columnToOption(using TypoBytea.column)),
          largephotofilename = row(idx + 4)(using Column.columnToOption(using Column.columnToString)),
          modifieddate = row(idx + 5)(using TypoLocalDateTime.column)
        )
      )
    }
  }

  given writes: OWrites[ProductphotoRow] = {
    OWrites[ProductphotoRow](o =>
      new JsObject(ListMap[String, JsValue](
        "productphotoid" -> ProductphotoId.writes.writes(o.productphotoid),
        "thumbnailphoto" -> Writes.OptionWrites(using TypoBytea.writes).writes(o.thumbnailphoto),
        "thumbnailphotofilename" -> Writes.OptionWrites(using Writes.StringWrites).writes(o.thumbnailphotofilename),
        "largephoto" -> Writes.OptionWrites(using TypoBytea.writes).writes(o.largephoto),
        "largephotofilename" -> Writes.OptionWrites(using Writes.StringWrites).writes(o.largephotofilename),
        "modifieddate" -> TypoLocalDateTime.writes.writes(o.modifieddate)
      ))
    )
  }
}