/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productinventory

import java.sql.Connection
import scala.annotation.nowarn
import typr.dsl.DeleteBuilder
import typr.dsl.DeleteBuilderMock
import typr.dsl.DeleteParams
import typr.dsl.SelectBuilder
import typr.dsl.SelectBuilderMock
import typr.dsl.SelectParams
import typr.dsl.UpdateBuilder
import typr.dsl.UpdateBuilderMock
import typr.dsl.UpdateParams

case class ProductinventoryRepoMock(
  toRow: ProductinventoryRowUnsaved => ProductinventoryRow,
  map: scala.collection.mutable.Map[ProductinventoryId, ProductinventoryRow] = scala.collection.mutable.Map.empty[ProductinventoryId, ProductinventoryRow]
) extends ProductinventoryRepo {
  override def delete: DeleteBuilder[ProductinventoryFields, ProductinventoryRow] = DeleteBuilderMock(DeleteParams.empty, ProductinventoryFields.structure, map)

  override def deleteById(compositeId: ProductinventoryId)(using c: Connection): Boolean = map.remove(compositeId).isDefined

  override def deleteByIds(compositeIds: Array[ProductinventoryId])(using c: Connection): Int = compositeIds.map(id => map.remove(id)).count(_.isDefined)

  override def insert(unsaved: ProductinventoryRow)(using c: Connection): ProductinventoryRow = {
    val _ = if (map.contains(unsaved.compositeId))
      sys.error(s"id ${unsaved.compositeId} already exists")
    else
      map.put(unsaved.compositeId, unsaved)
  
    unsaved
  }

  override def insert(unsaved: ProductinventoryRowUnsaved)(using c: Connection): ProductinventoryRow = insert(toRow(unsaved))

  override def insertStreaming(
    unsaved: Iterator[ProductinventoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.compositeId -> row)
    }
    unsaved.size.toLong
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[ProductinventoryRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.compositeId -> row)
    }
    unsaved.size.toLong
  }

  override def select: SelectBuilder[ProductinventoryFields, ProductinventoryRow] = SelectBuilderMock(ProductinventoryFields.structure, () => map.values.toList, SelectParams.empty)

  override def selectAll(using c: Connection): List[ProductinventoryRow] = map.values.toList

  override def selectById(compositeId: ProductinventoryId)(using c: Connection): Option[ProductinventoryRow] = map.get(compositeId)

  override def selectByIds(compositeIds: Array[ProductinventoryId])(using c: Connection): List[ProductinventoryRow] = compositeIds.flatMap(map.get).toList

  override def selectByIdsTracked(compositeIds: Array[ProductinventoryId])(using c: Connection): Map[ProductinventoryId, ProductinventoryRow] = {
    val byId = selectByIds(compositeIds).view.map(x => (x.compositeId, x)).toMap
    compositeIds.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  override def update: UpdateBuilder[ProductinventoryFields, ProductinventoryRow] = UpdateBuilderMock(UpdateParams.empty, ProductinventoryFields.structure, map)

  override def update(row: ProductinventoryRow)(using c: Connection): Option[ProductinventoryRow] = {
    map.get(row.compositeId).map { _ =>
      map.put(row.compositeId, row): @nowarn
      row
    }
  }

  override def upsert(unsaved: ProductinventoryRow)(using c: Connection): ProductinventoryRow = {
    map.put(unsaved.compositeId, unsaved): @nowarn
    unsaved
  }

  override def upsertBatch(unsaved: Iterable[ProductinventoryRow])(using c: Connection): List[ProductinventoryRow] = {
    unsaved.map { row =>
      map += (row.compositeId -> row)
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[ProductinventoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.compositeId -> row)
    }
    unsaved.size
  }
}