/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productdescription

import java.sql.Connection
import scala.annotation.nowarn
import typr.dsl.DeleteBuilder
import typr.dsl.DeleteBuilderMock
import typr.dsl.DeleteParams
import typr.dsl.SelectBuilder
import typr.dsl.SelectBuilderMock
import typr.dsl.SelectParams
import typr.dsl.UpdateBuilder
import typr.dsl.UpdateBuilderMock
import typr.dsl.UpdateParams

case class ProductdescriptionRepoMock(
  toRow: ProductdescriptionRowUnsaved => ProductdescriptionRow,
  map: scala.collection.mutable.Map[ProductdescriptionId, ProductdescriptionRow] = scala.collection.mutable.Map.empty[ProductdescriptionId, ProductdescriptionRow]
) extends ProductdescriptionRepo {
  override def delete: DeleteBuilder[ProductdescriptionFields, ProductdescriptionRow] = DeleteBuilderMock(DeleteParams.empty, ProductdescriptionFields.structure, map)

  override def deleteById(productdescriptionid: ProductdescriptionId)(using c: Connection): Boolean = map.remove(productdescriptionid).isDefined

  override def deleteByIds(productdescriptionids: Array[ProductdescriptionId])(using c: Connection): Int = productdescriptionids.map(id => map.remove(id)).count(_.isDefined)

  override def insert(unsaved: ProductdescriptionRow)(using c: Connection): ProductdescriptionRow = {
    val _ = if (map.contains(unsaved.productdescriptionid))
      sys.error(s"id ${unsaved.productdescriptionid} already exists")
    else
      map.put(unsaved.productdescriptionid, unsaved)
  
    unsaved
  }

  override def insert(unsaved: ProductdescriptionRowUnsaved)(using c: Connection): ProductdescriptionRow = insert(toRow(unsaved))

  override def insertStreaming(
    unsaved: Iterator[ProductdescriptionRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.productdescriptionid -> row)
    }
    unsaved.size.toLong
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[ProductdescriptionRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.productdescriptionid -> row)
    }
    unsaved.size.toLong
  }

  override def select: SelectBuilder[ProductdescriptionFields, ProductdescriptionRow] = SelectBuilderMock(ProductdescriptionFields.structure, () => map.values.toList, SelectParams.empty)

  override def selectAll(using c: Connection): List[ProductdescriptionRow] = map.values.toList

  override def selectById(productdescriptionid: ProductdescriptionId)(using c: Connection): Option[ProductdescriptionRow] = map.get(productdescriptionid)

  override def selectByIds(productdescriptionids: Array[ProductdescriptionId])(using c: Connection): List[ProductdescriptionRow] = productdescriptionids.flatMap(map.get).toList

  override def selectByIdsTracked(productdescriptionids: Array[ProductdescriptionId])(using c: Connection): Map[ProductdescriptionId, ProductdescriptionRow] = {
    val byId = selectByIds(productdescriptionids).view.map(x => (x.productdescriptionid, x)).toMap
    productdescriptionids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  override def update: UpdateBuilder[ProductdescriptionFields, ProductdescriptionRow] = UpdateBuilderMock(UpdateParams.empty, ProductdescriptionFields.structure, map)

  override def update(row: ProductdescriptionRow)(using c: Connection): Option[ProductdescriptionRow] = {
    map.get(row.productdescriptionid).map { _ =>
      map.put(row.productdescriptionid, row): @nowarn
      row
    }
  }

  override def upsert(unsaved: ProductdescriptionRow)(using c: Connection): ProductdescriptionRow = {
    map.put(unsaved.productdescriptionid, unsaved): @nowarn
    unsaved
  }

  override def upsertBatch(unsaved: Iterable[ProductdescriptionRow])(using c: Connection): List[ProductdescriptionRow] = {
    unsaved.map { row =>
      map += (row.productdescriptionid -> row)
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[ProductdescriptionRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.productdescriptionid -> row)
    }
    unsaved.size
  }
}