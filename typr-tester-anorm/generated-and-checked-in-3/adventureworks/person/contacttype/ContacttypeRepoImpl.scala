/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.contacttype

import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.TypoLocalDateTime
import adventureworks.public.Name
import adventureworks.streamingInsert
import anorm.BatchSql
import anorm.NamedParameter
import anorm.ParameterValue
import anorm.RowParser
import anorm.SQL
import anorm.SimpleSql
import java.sql.Connection
import scala.annotation.nowarn
import typr.dsl.DeleteBuilder
import typr.dsl.SelectBuilder
import typr.dsl.UpdateBuilder
import anorm.SqlStringInterpolation

class ContacttypeRepoImpl extends ContacttypeRepo {
  override def delete: DeleteBuilder[ContacttypeFields, ContacttypeRow] = DeleteBuilder.of(""""person"."contacttype"""", ContacttypeFields.structure, ContacttypeRow.rowParser(1).*)

  override def deleteById(contacttypeid: ContacttypeId)(using c: Connection): Boolean = SQL"""delete from "person"."contacttype" where "contacttypeid" = ${ParameterValue(contacttypeid, null, ContacttypeId.toStatement)}""".executeUpdate() > 0

  override def deleteByIds(contacttypeids: Array[ContacttypeId])(using c: Connection): Int = {
    SQL"""delete
    from "person"."contacttype"
    where "contacttypeid" = ANY(${ParameterValue(contacttypeids, null, ContacttypeId.arrayToStatement)})
    """.executeUpdate()
  }

  override def insert(unsaved: ContacttypeRow)(using c: Connection): ContacttypeRow = {
  SQL"""insert into "person"."contacttype"("contacttypeid", "name", "modifieddate")
    values (${ParameterValue(unsaved.contacttypeid, null, ContacttypeId.toStatement)}::int4, ${ParameterValue(unsaved.name, null, Name.toStatement)}::varchar, ${ParameterValue(unsaved.modifieddate, null, TypoLocalDateTime.toStatement)}::timestamp)
    returning "contacttypeid", "name", "modifieddate"::text
    """
    .executeInsert(ContacttypeRow.rowParser(1).single)
  }

  override def insert(unsaved: ContacttypeRowUnsaved)(using c: Connection): ContacttypeRow = {
    val namedParameters = List(
      Some((NamedParameter("name", ParameterValue(unsaved.name, null, Name.toStatement)), "::varchar")),
      unsaved.contacttypeid match {
        case Defaulted.UseDefault() => None
        case Defaulted.Provided(value) => Some((NamedParameter("contacttypeid", ParameterValue(value, null, ContacttypeId.toStatement)), "::int4"))
      },
      unsaved.modifieddate match {
        case Defaulted.UseDefault() => None
        case Defaulted.Provided(value) => Some((NamedParameter("modifieddate", ParameterValue(value, null, TypoLocalDateTime.toStatement)), "::timestamp"))
      }
    ).flatten
    val quote = '"'.toString
    if (namedParameters.isEmpty) {
      SQL"""insert into "person"."contacttype" default values
      returning "contacttypeid", "name", "modifieddate"::text
      """
        .executeInsert(ContacttypeRow.rowParser(1).single)
    } else {
      val q = s"""insert into "person"."contacttype"(${namedParameters.map{case (x, _) => quote + x.name + quote}.mkString(", ")})
              values (${namedParameters.map{ case (np, cast) => s"{${np.name}}$cast"}.mkString(", ")})
              returning "contacttypeid", "name", "modifieddate"::text
              """
      SimpleSql(SQL(q), namedParameters.map { case (np, _) => np.tupled }.toMap, RowParser.successful)
        .executeInsert(ContacttypeRow.rowParser(1).single)
    }
  }

  override def insertStreaming(
    unsaved: Iterator[ContacttypeRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert(s"""COPY "person"."contacttype"("contacttypeid", "name", "modifieddate") FROM STDIN""", batchSize, unsaved)(using ContacttypeRow.pgText, c)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[ContacttypeRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert(s"""COPY "person"."contacttype"("name", "contacttypeid", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(using ContacttypeRowUnsaved.pgText, c)

  override def select: SelectBuilder[ContacttypeFields, ContacttypeRow] = SelectBuilder.of(""""person"."contacttype"""", ContacttypeFields.structure, ContacttypeRow.rowParser)

  override def selectAll(using c: Connection): List[ContacttypeRow] = {
    SQL"""select "contacttypeid", "name", "modifieddate"::text
    from "person"."contacttype"
    """.as(ContacttypeRow.rowParser(1).*)
  }

  override def selectById(contacttypeid: ContacttypeId)(using c: Connection): Option[ContacttypeRow] = {
    SQL"""select "contacttypeid", "name", "modifieddate"::text
    from "person"."contacttype"
    where "contacttypeid" = ${ParameterValue(contacttypeid, null, ContacttypeId.toStatement)}
    """.as(ContacttypeRow.rowParser(1).singleOpt)
  }

  override def selectByIds(contacttypeids: Array[ContacttypeId])(using c: Connection): List[ContacttypeRow] = {
    SQL"""select "contacttypeid", "name", "modifieddate"::text
    from "person"."contacttype"
    where "contacttypeid" = ANY(${ParameterValue(contacttypeids, null, ContacttypeId.arrayToStatement)})
    """.as(ContacttypeRow.rowParser(1).*)
  }

  override def selectByIdsTracked(contacttypeids: Array[ContacttypeId])(using c: Connection): Map[ContacttypeId, ContacttypeRow] = {
    val byId = selectByIds(contacttypeids).view.map(x => (x.contacttypeid, x)).toMap
    contacttypeids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  override def update: UpdateBuilder[ContacttypeFields, ContacttypeRow] = UpdateBuilder.of(""""person"."contacttype"""", ContacttypeFields.structure, ContacttypeRow.rowParser(1).*)

  override def update(row: ContacttypeRow)(using c: Connection): Option[ContacttypeRow] = {
    val contacttypeid = row.contacttypeid
    SQL"""update "person"."contacttype"
    set "name" = ${ParameterValue(row.name, null, Name.toStatement)}::varchar,
    "modifieddate" = ${ParameterValue(row.modifieddate, null, TypoLocalDateTime.toStatement)}::timestamp
    where "contacttypeid" = ${ParameterValue(contacttypeid, null, ContacttypeId.toStatement)}
    returning "contacttypeid", "name", "modifieddate"::text
    """.executeInsert(ContacttypeRow.rowParser(1).singleOpt)
  }

  override def upsert(unsaved: ContacttypeRow)(using c: Connection): ContacttypeRow = {
  SQL"""insert into "person"."contacttype"("contacttypeid", "name", "modifieddate")
    values (
      ${ParameterValue(unsaved.contacttypeid, null, ContacttypeId.toStatement)}::int4,
    ${ParameterValue(unsaved.name, null, Name.toStatement)}::varchar,
    ${ParameterValue(unsaved.modifieddate, null, TypoLocalDateTime.toStatement)}::timestamp
    )
    on conflict ("contacttypeid")
    do update set
      "name" = EXCLUDED."name",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "contacttypeid", "name", "modifieddate"::text
    """
    .executeInsert(ContacttypeRow.rowParser(1).single)
  }

  override def upsertBatch(unsaved: Iterable[ContacttypeRow])(using c: Connection): List[ContacttypeRow] = {
    def toNamedParameter(row: ContacttypeRow): List[NamedParameter] = List(
      NamedParameter("contacttypeid", ParameterValue(row.contacttypeid, null, ContacttypeId.toStatement)),
      NamedParameter("name", ParameterValue(row.name, null, Name.toStatement)),
      NamedParameter("modifieddate", ParameterValue(row.modifieddate, null, TypoLocalDateTime.toStatement))
    )
  
    unsaved.toList match {
      case Nil => Nil
      case head :: rest =>
        new anorm.adventureworks.ExecuteReturningSyntax.Ops(
          BatchSql(
            s"""insert into "person"."contacttype"("contacttypeid", "name", "modifieddate")
            values ({contacttypeid}::int4, {name}::varchar, {modifieddate}::timestamp)
            on conflict ("contacttypeid")
            do update set
              "name" = EXCLUDED."name",
            "modifieddate" = EXCLUDED."modifieddate"
            returning "contacttypeid", "name", "modifieddate"::text
            """,
            toNamedParameter(head),
            rest.map(toNamedParameter)*
          )
        ).executeReturning(ContacttypeRow.rowParser(1).*)
    }
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[ContacttypeRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    SQL"""create temporary table contacttype_TEMP (like "person"."contacttype") on commit drop""".execute(): @nowarn
    streamingInsert(s"""copy contacttype_TEMP("contacttypeid", "name", "modifieddate") from stdin""", batchSize, unsaved)(using ContacttypeRow.pgText, c): @nowarn
    SQL"""insert into "person"."contacttype"("contacttypeid", "name", "modifieddate")
    select * from contacttype_TEMP
    on conflict ("contacttypeid")
    do update set
      "name" = EXCLUDED."name",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table contacttype_TEMP;""".executeUpdate()
  }
}