/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.countryregion

import java.sql.Connection
import scala.annotation.nowarn
import typr.dsl.DeleteBuilder
import typr.dsl.DeleteBuilderMock
import typr.dsl.DeleteParams
import typr.dsl.SelectBuilder
import typr.dsl.SelectBuilderMock
import typr.dsl.SelectParams
import typr.dsl.UpdateBuilder
import typr.dsl.UpdateBuilderMock
import typr.dsl.UpdateParams

case class CountryregionRepoMock(
  toRow: CountryregionRowUnsaved => CountryregionRow,
  map: scala.collection.mutable.Map[CountryregionId, CountryregionRow] = scala.collection.mutable.Map.empty[CountryregionId, CountryregionRow]
) extends CountryregionRepo {
  override def delete: DeleteBuilder[CountryregionFields, CountryregionRow] = DeleteBuilderMock(DeleteParams.empty, CountryregionFields.structure, map)

  override def deleteById(countryregioncode: CountryregionId)(using c: Connection): Boolean = map.remove(countryregioncode).isDefined

  override def deleteByIds(countryregioncodes: Array[CountryregionId])(using c: Connection): Int = countryregioncodes.map(id => map.remove(id)).count(_.isDefined)

  override def insert(unsaved: CountryregionRow)(using c: Connection): CountryregionRow = {
    val _ = if (map.contains(unsaved.countryregioncode))
      sys.error(s"id ${unsaved.countryregioncode} already exists")
    else
      map.put(unsaved.countryregioncode, unsaved)
  
    unsaved
  }

  override def insert(unsaved: CountryregionRowUnsaved)(using c: Connection): CountryregionRow = insert(toRow(unsaved))

  override def insertStreaming(
    unsaved: Iterator[CountryregionRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.countryregioncode -> row)
    }
    unsaved.size.toLong
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[CountryregionRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.countryregioncode -> row)
    }
    unsaved.size.toLong
  }

  override def select: SelectBuilder[CountryregionFields, CountryregionRow] = SelectBuilderMock(CountryregionFields.structure, () => map.values.toList, SelectParams.empty)

  override def selectAll(using c: Connection): List[CountryregionRow] = map.values.toList

  override def selectById(countryregioncode: CountryregionId)(using c: Connection): Option[CountryregionRow] = map.get(countryregioncode)

  override def selectByIds(countryregioncodes: Array[CountryregionId])(using c: Connection): List[CountryregionRow] = countryregioncodes.flatMap(map.get).toList

  override def selectByIdsTracked(countryregioncodes: Array[CountryregionId])(using c: Connection): Map[CountryregionId, CountryregionRow] = {
    val byId = selectByIds(countryregioncodes).view.map(x => (x.countryregioncode, x)).toMap
    countryregioncodes.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  override def update: UpdateBuilder[CountryregionFields, CountryregionRow] = UpdateBuilderMock(UpdateParams.empty, CountryregionFields.structure, map)

  override def update(row: CountryregionRow)(using c: Connection): Option[CountryregionRow] = {
    map.get(row.countryregioncode).map { _ =>
      map.put(row.countryregioncode, row): @nowarn
      row
    }
  }

  override def upsert(unsaved: CountryregionRow)(using c: Connection): CountryregionRow = {
    map.put(unsaved.countryregioncode, unsaved): @nowarn
    unsaved
  }

  override def upsertBatch(unsaved: Iterable[CountryregionRow])(using c: Connection): List[CountryregionRow] = {
    unsaved.map { row =>
      map += (row.countryregioncode -> row)
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[CountryregionRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.countryregioncode -> row)
    }
    unsaved.size
  }
}