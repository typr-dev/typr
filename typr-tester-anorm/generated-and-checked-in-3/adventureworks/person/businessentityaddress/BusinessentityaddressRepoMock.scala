/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.businessentityaddress

import java.sql.Connection
import scala.annotation.nowarn
import typr.dsl.DeleteBuilder
import typr.dsl.DeleteBuilderMock
import typr.dsl.DeleteParams
import typr.dsl.SelectBuilder
import typr.dsl.SelectBuilderMock
import typr.dsl.SelectParams
import typr.dsl.UpdateBuilder
import typr.dsl.UpdateBuilderMock
import typr.dsl.UpdateParams

case class BusinessentityaddressRepoMock(
  toRow: BusinessentityaddressRowUnsaved => BusinessentityaddressRow,
  map: scala.collection.mutable.Map[BusinessentityaddressId, BusinessentityaddressRow] = scala.collection.mutable.Map.empty[BusinessentityaddressId, BusinessentityaddressRow]
) extends BusinessentityaddressRepo {
  override def delete: DeleteBuilder[BusinessentityaddressFields, BusinessentityaddressRow] = DeleteBuilderMock(DeleteParams.empty, BusinessentityaddressFields.structure, map)

  override def deleteById(compositeId: BusinessentityaddressId)(using c: Connection): Boolean = map.remove(compositeId).isDefined

  override def deleteByIds(compositeIds: Array[BusinessentityaddressId])(using c: Connection): Int = compositeIds.map(id => map.remove(id)).count(_.isDefined)

  override def insert(unsaved: BusinessentityaddressRow)(using c: Connection): BusinessentityaddressRow = {
    val _ = if (map.contains(unsaved.compositeId))
      sys.error(s"id ${unsaved.compositeId} already exists")
    else
      map.put(unsaved.compositeId, unsaved)
  
    unsaved
  }

  override def insert(unsaved: BusinessentityaddressRowUnsaved)(using c: Connection): BusinessentityaddressRow = insert(toRow(unsaved))

  override def insertStreaming(
    unsaved: Iterator[BusinessentityaddressRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.compositeId -> row)
    }
    unsaved.size.toLong
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[BusinessentityaddressRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.compositeId -> row)
    }
    unsaved.size.toLong
  }

  override def select: SelectBuilder[BusinessentityaddressFields, BusinessentityaddressRow] = SelectBuilderMock(BusinessentityaddressFields.structure, () => map.values.toList, SelectParams.empty)

  override def selectAll(using c: Connection): List[BusinessentityaddressRow] = map.values.toList

  override def selectById(compositeId: BusinessentityaddressId)(using c: Connection): Option[BusinessentityaddressRow] = map.get(compositeId)

  override def selectByIds(compositeIds: Array[BusinessentityaddressId])(using c: Connection): List[BusinessentityaddressRow] = compositeIds.flatMap(map.get).toList

  override def selectByIdsTracked(compositeIds: Array[BusinessentityaddressId])(using c: Connection): Map[BusinessentityaddressId, BusinessentityaddressRow] = {
    val byId = selectByIds(compositeIds).view.map(x => (x.compositeId, x)).toMap
    compositeIds.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }

  override def update: UpdateBuilder[BusinessentityaddressFields, BusinessentityaddressRow] = UpdateBuilderMock(UpdateParams.empty, BusinessentityaddressFields.structure, map)

  override def update(row: BusinessentityaddressRow)(using c: Connection): Option[BusinessentityaddressRow] = {
    map.get(row.compositeId).map { _ =>
      map.put(row.compositeId, row): @nowarn
      row
    }
  }

  override def upsert(unsaved: BusinessentityaddressRow)(using c: Connection): BusinessentityaddressRow = {
    map.put(unsaved.compositeId, unsaved): @nowarn
    unsaved
  }

  override def upsertBatch(unsaved: Iterable[BusinessentityaddressRow])(using c: Connection): List[BusinessentityaddressRow] = {
    unsaved.map { row =>
      map += (row.compositeId -> row)
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[BusinessentityaddressRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.compositeId -> row)
    }
    unsaved.size
  }
}