/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.humanresources.shift

import adventureworks.Text
import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.TypoLocalDateTime
import adventureworks.customtypes.TypoLocalTime
import adventureworks.public.Name
import anorm.RowParser
import anorm.Success
import play.api.libs.json.JsObject
import play.api.libs.json.JsResult
import play.api.libs.json.JsValue
import play.api.libs.json.OWrites
import play.api.libs.json.Reads
import scala.collection.immutable.ListMap
import scala.util.Try

/** Table: humanresources.shift
 * Work shift lookup table.
 * Primary key: shiftid
 */
case class ShiftRow(
  /** Primary key for Shift records.
   * Default: nextval('humanresources.shift_shiftid_seq'::regclass)
   */
  shiftid: ShiftId,
  /** Shift description. */
  name: Name,
  /** Shift start time. */
  starttime: TypoLocalTime,
  /** Shift end time. */
  endtime: TypoLocalTime,
  /** Default: now() */
  modifieddate: TypoLocalDateTime
) {
  def id: ShiftId = shiftid

  def toUnsavedRow(
    shiftid: Defaulted[ShiftId],
    modifieddate: Defaulted[TypoLocalDateTime] = Defaulted.Provided(this.modifieddate)
  ): ShiftRowUnsaved = {
    new ShiftRowUnsaved(
      name,
      starttime,
      endtime,
      shiftid,
      modifieddate
    )
  }
}

object ShiftRow {
  given pgText: Text[ShiftRow] = {
    Text.instance[ShiftRow]{ (row, sb) =>
      ShiftId.pgText.unsafeEncode(row.shiftid, sb)
      sb.append(Text.DELIMETER)
      Name.pgText.unsafeEncode(row.name, sb)
      sb.append(Text.DELIMETER)
      TypoLocalTime.pgText.unsafeEncode(row.starttime, sb)
      sb.append(Text.DELIMETER)
      TypoLocalTime.pgText.unsafeEncode(row.endtime, sb)
      sb.append(Text.DELIMETER)
      TypoLocalDateTime.pgText.unsafeEncode(row.modifieddate, sb)
    }
  }

  given reads: Reads[ShiftRow] = {
    Reads[ShiftRow](json => JsResult.fromTry(
        Try(
          ShiftRow(
            shiftid = json.\("shiftid").as(using ShiftId.reads),
            name = json.\("name").as(using Name.reads),
            starttime = json.\("starttime").as(using TypoLocalTime.reads),
            endtime = json.\("endtime").as(using TypoLocalTime.reads),
            modifieddate = json.\("modifieddate").as(using TypoLocalDateTime.reads)
          )
        )
      ),
    )
  }

  def rowParser(idx: Int): RowParser[ShiftRow] = {
    RowParser[ShiftRow] { row =>
      Success(
        ShiftRow(
          shiftid = row(idx + 0)(using ShiftId.column),
          name = row(idx + 1)(using Name.column),
          starttime = row(idx + 2)(using TypoLocalTime.column),
          endtime = row(idx + 3)(using TypoLocalTime.column),
          modifieddate = row(idx + 4)(using TypoLocalDateTime.column)
        )
      )
    }
  }

  given writes: OWrites[ShiftRow] = {
    OWrites[ShiftRow](o =>
      new JsObject(ListMap[String, JsValue](
        "shiftid" -> ShiftId.writes.writes(o.shiftid),
        "name" -> Name.writes.writes(o.name),
        "starttime" -> TypoLocalTime.writes.writes(o.starttime),
        "endtime" -> TypoLocalTime.writes.writes(o.endtime),
        "modifieddate" -> TypoLocalDateTime.writes.writes(o.modifieddate)
      ))
    )
  }
}