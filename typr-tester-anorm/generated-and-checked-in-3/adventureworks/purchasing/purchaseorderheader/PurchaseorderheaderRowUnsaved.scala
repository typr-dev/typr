/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.purchasing.purchaseorderheader

import adventureworks.Text
import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.Defaulted.UseDefault
import adventureworks.customtypes.TypoLocalDateTime
import adventureworks.customtypes.TypoShort
import adventureworks.person.businessentity.BusinessentityId
import adventureworks.purchasing.shipmethod.ShipmethodId
import play.api.libs.json.JsObject
import play.api.libs.json.JsResult
import play.api.libs.json.JsValue
import play.api.libs.json.OWrites
import play.api.libs.json.Reads
import play.api.libs.json.Writes
import scala.collection.immutable.ListMap
import scala.util.Try

/** This class corresponds to a row in table `purchasing.purchaseorderheader` which has not been persisted yet */
case class PurchaseorderheaderRowUnsaved(
  /** Employee who created the purchase order. Foreign key to Employee.BusinessEntityID.
   * Points to [[adventureworks.humanresources.employee.EmployeeRow.businessentityid]]
   */
  employeeid: BusinessentityId,
  /** Vendor with whom the purchase order is placed. Foreign key to Vendor.BusinessEntityID.
   * Points to [[adventureworks.purchasing.vendor.VendorRow.businessentityid]]
   */
  vendorid: BusinessentityId,
  /** Shipping method. Foreign key to ShipMethod.ShipMethodID.
   * Points to [[adventureworks.purchasing.shipmethod.ShipmethodRow.shipmethodid]]
   */
  shipmethodid: ShipmethodId,
  /** Estimated shipment date from the vendor.
   * Constraint CK_PurchaseOrderHeader_ShipDate affecting columns orderdate, shipdate:  (((shipdate >= orderdate) OR (shipdate IS NULL)))
   */
  shipdate: Option[TypoLocalDateTime] = None,
  /** Default: nextval('purchasing.purchaseorderheader_purchaseorderid_seq'::regclass)
   * Primary key.
   */
  purchaseorderid: Defaulted[PurchaseorderheaderId] = new UseDefault(),
  /** Default: 0
   * Incremental number to track changes to the purchase order over time.
   */
  revisionnumber: Defaulted[TypoShort] = new UseDefault(),
  /** Default: 1
   * Order current status. 1 = Pending; 2 = Approved; 3 = Rejected; 4 = Complete
   * Constraint CK_PurchaseOrderHeader_Status affecting columns status:  (((status >= 1) AND (status <= 4)))
   */
  status: Defaulted[TypoShort] = new UseDefault(),
  /** Default: now()
   * Purchase order creation date.
   * Constraint CK_PurchaseOrderHeader_ShipDate affecting columns orderdate, shipdate:  (((shipdate >= orderdate) OR (shipdate IS NULL)))
   */
  orderdate: Defaulted[TypoLocalDateTime] = new UseDefault(),
  /** Default: 0.00
   * Purchase order subtotal. Computed as SUM(PurchaseOrderDetail.LineTotal)for the appropriate PurchaseOrderID.
   * Constraint CK_PurchaseOrderHeader_SubTotal affecting columns subtotal:  ((subtotal >= 0.00))
   */
  subtotal: Defaulted[BigDecimal] = new UseDefault(),
  /** Default: 0.00
   * Tax amount.
   * Constraint CK_PurchaseOrderHeader_TaxAmt affecting columns taxamt:  ((taxamt >= 0.00))
   */
  taxamt: Defaulted[BigDecimal] = new UseDefault(),
  /** Default: 0.00
   * Shipping cost.
   * Constraint CK_PurchaseOrderHeader_Freight affecting columns freight:  ((freight >= 0.00))
   */
  freight: Defaulted[BigDecimal] = new UseDefault(),
  /** Default: now() */
  modifieddate: Defaulted[TypoLocalDateTime] = new UseDefault()
) {
  def toRow(
    purchaseorderidDefault: => PurchaseorderheaderId,
    revisionnumberDefault: => TypoShort,
    statusDefault: => TypoShort,
    orderdateDefault: => TypoLocalDateTime,
    subtotalDefault: => BigDecimal,
    taxamtDefault: => BigDecimal,
    freightDefault: => BigDecimal,
    modifieddateDefault: => TypoLocalDateTime
  ): PurchaseorderheaderRow = {
    new PurchaseorderheaderRow(
      purchaseorderid = purchaseorderid.getOrElse(purchaseorderidDefault),
      revisionnumber = revisionnumber.getOrElse(revisionnumberDefault),
      status = status.getOrElse(statusDefault),
      employeeid = employeeid,
      vendorid = vendorid,
      shipmethodid = shipmethodid,
      orderdate = orderdate.getOrElse(orderdateDefault),
      shipdate = shipdate,
      subtotal = subtotal.getOrElse(subtotalDefault),
      taxamt = taxamt.getOrElse(taxamtDefault),
      freight = freight.getOrElse(freightDefault),
      modifieddate = modifieddate.getOrElse(modifieddateDefault)
    )
  }
}

object PurchaseorderheaderRowUnsaved {
  given pgText: Text[PurchaseorderheaderRowUnsaved] = {
    Text.instance[PurchaseorderheaderRowUnsaved]{ (row, sb) =>
      BusinessentityId.pgText.unsafeEncode(row.employeeid, sb)
      sb.append(Text.DELIMETER)
      BusinessentityId.pgText.unsafeEncode(row.vendorid, sb)
      sb.append(Text.DELIMETER)
      ShipmethodId.pgText.unsafeEncode(row.shipmethodid, sb)
      sb.append(Text.DELIMETER)
      Text.option(using TypoLocalDateTime.pgText).unsafeEncode(row.shipdate, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using PurchaseorderheaderId.pgText).unsafeEncode(row.purchaseorderid, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using TypoShort.pgText).unsafeEncode(row.revisionnumber, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using TypoShort.pgText).unsafeEncode(row.status, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using TypoLocalDateTime.pgText).unsafeEncode(row.orderdate, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using Text.bigDecimalInstance).unsafeEncode(row.subtotal, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using Text.bigDecimalInstance).unsafeEncode(row.taxamt, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using Text.bigDecimalInstance).unsafeEncode(row.freight, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using TypoLocalDateTime.pgText).unsafeEncode(row.modifieddate, sb)
    }
  }

  given reads: Reads[PurchaseorderheaderRowUnsaved] = {
    Reads[PurchaseorderheaderRowUnsaved](json => JsResult.fromTry(
        Try(
          PurchaseorderheaderRowUnsaved(
            employeeid = json.\("employeeid").as(using BusinessentityId.reads),
            vendorid = json.\("vendorid").as(using BusinessentityId.reads),
            shipmethodid = json.\("shipmethodid").as(using ShipmethodId.reads),
            shipdate = json.\("shipdate").toOption.map(_.as(using TypoLocalDateTime.reads)),
            purchaseorderid = json.\("purchaseorderid").as(using Defaulted.reads(using PurchaseorderheaderId.reads)),
            revisionnumber = json.\("revisionnumber").as(using Defaulted.reads(using TypoShort.reads)),
            status = json.\("status").as(using Defaulted.reads(using TypoShort.reads)),
            orderdate = json.\("orderdate").as(using Defaulted.reads(using TypoLocalDateTime.reads)),
            subtotal = json.\("subtotal").as(using Defaulted.reads(using Reads.bigDecReads)),
            taxamt = json.\("taxamt").as(using Defaulted.reads(using Reads.bigDecReads)),
            freight = json.\("freight").as(using Defaulted.reads(using Reads.bigDecReads)),
            modifieddate = json.\("modifieddate").as(using Defaulted.reads(using TypoLocalDateTime.reads))
          )
        )
      ),
    )
  }

  given writes: OWrites[PurchaseorderheaderRowUnsaved] = {
    OWrites[PurchaseorderheaderRowUnsaved](o =>
      new JsObject(ListMap[String, JsValue](
        "employeeid" -> BusinessentityId.writes.writes(o.employeeid),
        "vendorid" -> BusinessentityId.writes.writes(o.vendorid),
        "shipmethodid" -> ShipmethodId.writes.writes(o.shipmethodid),
        "shipdate" -> Writes.OptionWrites(using TypoLocalDateTime.writes).writes(o.shipdate),
        "purchaseorderid" -> Defaulted.writes(using PurchaseorderheaderId.writes).writes(o.purchaseorderid),
        "revisionnumber" -> Defaulted.writes(using TypoShort.writes).writes(o.revisionnumber),
        "status" -> Defaulted.writes(using TypoShort.writes).writes(o.status),
        "orderdate" -> Defaulted.writes(using TypoLocalDateTime.writes).writes(o.orderdate),
        "subtotal" -> Defaulted.writes(using Writes.BigDecimalWrites).writes(o.subtotal),
        "taxamt" -> Defaulted.writes(using Writes.BigDecimalWrites).writes(o.taxamt),
        "freight" -> Defaulted.writes(using Writes.BigDecimalWrites).writes(o.freight),
        "modifieddate" -> Defaulted.writes(using TypoLocalDateTime.writes).writes(o.modifieddate)
      ))
    )
  }
}