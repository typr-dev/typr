/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.businessentityaddress

import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.TypoLocalDateTime
import adventureworks.customtypes.TypoUUID
import adventureworks.person.address.AddressId
import adventureworks.person.addresstype.AddresstypeId
import adventureworks.person.businessentity.BusinessentityId
import cats.instances.list.catsStdInstancesForList
import doobie.free.connection.ConnectionIO
import doobie.postgres.syntax.FragmentOps
import doobie.syntax.SqlInterpolator.SingleFragment.fromWrite
import doobie.util.Write
import doobie.util.fragment.Fragment
import doobie.util.update.Update
import fs2.Stream
import typr.dsl.DeleteBuilder
import typr.dsl.SelectBuilder
import typr.dsl.UpdateBuilder
import doobie.syntax.string.toSqlInterpolator

class BusinessentityaddressRepoImpl extends BusinessentityaddressRepo {
  override def delete: DeleteBuilder[BusinessentityaddressFields, BusinessentityaddressRow] = DeleteBuilder.of(""""person"."businessentityaddress"""", BusinessentityaddressFields.structure, BusinessentityaddressRow.read)

  override def deleteById(compositeId: BusinessentityaddressId): ConnectionIO[Boolean] = sql"""delete from "person"."businessentityaddress" where "businessentityid" = ${fromWrite(compositeId.businessentityid)(new Write.Single(BusinessentityId.put))} AND "addressid" = ${fromWrite(compositeId.addressid)(new Write.Single(AddressId.put))} AND "addresstypeid" = ${fromWrite(compositeId.addresstypeid)(new Write.Single(AddresstypeId.put))}""".update.run.map(_ > 0)

  override def deleteByIds(compositeIds: Array[BusinessentityaddressId]): ConnectionIO[Int] = {
    val businessentityid = compositeIds.map(_.businessentityid)
    val addressid = compositeIds.map(_.addressid)
    val addresstypeid = compositeIds.map(_.addresstypeid)
    sql"""delete
    from "person"."businessentityaddress"
    where ("businessentityid", "addressid", "addresstypeid")
    in (select unnest(${fromWrite(businessentityid)(new Write.Single(BusinessentityId.arrayPut))}), unnest(${fromWrite(addressid)(new Write.Single(AddressId.arrayPut))}), unnest(${fromWrite(addresstypeid)(new Write.Single(AddresstypeId.arrayPut))}))
    """.update.run
  }

  override def insert(unsaved: BusinessentityaddressRow): ConnectionIO[BusinessentityaddressRow] = {
    sql"""insert into "person"."businessentityaddress"("businessentityid", "addressid", "addresstypeid", "rowguid", "modifieddate")
    values (${fromWrite(unsaved.businessentityid)(new Write.Single(BusinessentityId.put))}::int4, ${fromWrite(unsaved.addressid)(new Write.Single(AddressId.put))}::int4, ${fromWrite(unsaved.addresstypeid)(new Write.Single(AddresstypeId.put))}::int4, ${fromWrite(unsaved.rowguid)(new Write.Single(TypoUUID.put))}::uuid, ${fromWrite(unsaved.modifieddate)(new Write.Single(TypoLocalDateTime.put))}::timestamp)
    returning "businessentityid", "addressid", "addresstypeid", "rowguid", "modifieddate"::text
    """.query(BusinessentityaddressRow.read).unique
  }

  override def insert(unsaved: BusinessentityaddressRowUnsaved): ConnectionIO[BusinessentityaddressRow] = {
    val fs = List(
      Some((Fragment.const0(s""""businessentityid""""), fr"${fromWrite(unsaved.businessentityid)(new Write.Single(BusinessentityId.put))}::int4")),
      Some((Fragment.const0(s""""addressid""""), fr"${fromWrite(unsaved.addressid)(new Write.Single(AddressId.put))}::int4")),
      Some((Fragment.const0(s""""addresstypeid""""), fr"${fromWrite(unsaved.addresstypeid)(new Write.Single(AddresstypeId.put))}::int4")),
      unsaved.rowguid match {
        case Defaulted.UseDefault() => None
        case Defaulted.Provided(value) => Some((Fragment.const0(s""""rowguid""""), fr"${fromWrite(value: TypoUUID)(new Write.Single(TypoUUID.put))}::uuid"))
      },
      unsaved.modifieddate match {
        case Defaulted.UseDefault() => None
        case Defaulted.Provided(value) => Some((Fragment.const0(s""""modifieddate""""), fr"${fromWrite(value: TypoLocalDateTime)(new Write.Single(TypoLocalDateTime.put))}::timestamp"))
      }
    ).flatten
    val q = if (fs.isEmpty) {
      sql"""insert into "person"."businessentityaddress" default values
      returning "businessentityid", "addressid", "addresstypeid", "rowguid", "modifieddate"::text
      """
    } else {
      val CommaSeparate = Fragment.FragmentMonoid.intercalate(fr", ")
      sql"""insert into "person"."businessentityaddress"(${CommaSeparate.combineAllOption(fs.map { case (n, _) => n }).get})
      values (${CommaSeparate.combineAllOption(fs.map { case (_, f) => f }).get})
      returning "businessentityid", "addressid", "addresstypeid", "rowguid", "modifieddate"::text
      """
    }
    q.query(BusinessentityaddressRow.read).unique
  }

  override def insertStreaming(
    unsaved: Stream[ConnectionIO, BusinessentityaddressRow],
    batchSize: Int = 10000
  ): ConnectionIO[Long] = new FragmentOps(sql"""COPY "person"."businessentityaddress"("businessentityid", "addressid", "addresstypeid", "rowguid", "modifieddate") FROM STDIN""").copyIn(unsaved, batchSize)(BusinessentityaddressRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Stream[ConnectionIO, BusinessentityaddressRowUnsaved],
    batchSize: Int = 10000
  ): ConnectionIO[Long] = new FragmentOps(sql"""COPY "person"."businessentityaddress"("businessentityid", "addressid", "addresstypeid", "rowguid", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""").copyIn(unsaved, batchSize)(BusinessentityaddressRowUnsaved.pgText)

  override def select: SelectBuilder[BusinessentityaddressFields, BusinessentityaddressRow] = SelectBuilder.of(""""person"."businessentityaddress"""", BusinessentityaddressFields.structure, BusinessentityaddressRow.read)

  override def selectAll: Stream[ConnectionIO, BusinessentityaddressRow] = sql"""select "businessentityid", "addressid", "addresstypeid", "rowguid", "modifieddate"::text from "person"."businessentityaddress"""".query(BusinessentityaddressRow.read).stream

  override def selectById(compositeId: BusinessentityaddressId): ConnectionIO[Option[BusinessentityaddressRow]] = sql"""select "businessentityid", "addressid", "addresstypeid", "rowguid", "modifieddate"::text from "person"."businessentityaddress" where "businessentityid" = ${fromWrite(compositeId.businessentityid)(new Write.Single(BusinessentityId.put))} AND "addressid" = ${fromWrite(compositeId.addressid)(new Write.Single(AddressId.put))} AND "addresstypeid" = ${fromWrite(compositeId.addresstypeid)(new Write.Single(AddresstypeId.put))}""".query(BusinessentityaddressRow.read).option

  override def selectByIds(compositeIds: Array[BusinessentityaddressId]): Stream[ConnectionIO, BusinessentityaddressRow] = {
    val businessentityid = compositeIds.map(_.businessentityid)
    val addressid = compositeIds.map(_.addressid)
    val addresstypeid = compositeIds.map(_.addresstypeid)
    sql"""select "businessentityid", "addressid", "addresstypeid", "rowguid", "modifieddate"::text
    from "person"."businessentityaddress"
    where ("businessentityid", "addressid", "addresstypeid")
    in (select unnest(${fromWrite(businessentityid)(new Write.Single(BusinessentityId.arrayPut))}), unnest(${fromWrite(addressid)(new Write.Single(AddressId.arrayPut))}), unnest(${fromWrite(addresstypeid)(new Write.Single(AddresstypeId.arrayPut))}))
    """.query(BusinessentityaddressRow.read).stream
  }

  override def selectByIdsTracked(compositeIds: Array[BusinessentityaddressId]): ConnectionIO[Map[BusinessentityaddressId, BusinessentityaddressRow]] = {
    selectByIds(compositeIds).compile.toList.map { rows =>
      val byId = rows.view.map(x => (x.compositeId, x)).toMap
      compositeIds.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
    }
  }

  override def update: UpdateBuilder[BusinessentityaddressFields, BusinessentityaddressRow] = UpdateBuilder.of(""""person"."businessentityaddress"""", BusinessentityaddressFields.structure, BusinessentityaddressRow.read)

  override def update(row: BusinessentityaddressRow): ConnectionIO[Option[BusinessentityaddressRow]] = {
    val compositeId = row.compositeId
    sql"""update "person"."businessentityaddress"
    set "rowguid" = ${fromWrite(row.rowguid)(new Write.Single(TypoUUID.put))}::uuid,
    "modifieddate" = ${fromWrite(row.modifieddate)(new Write.Single(TypoLocalDateTime.put))}::timestamp
    where "businessentityid" = ${fromWrite(compositeId.businessentityid)(new Write.Single(BusinessentityId.put))} AND "addressid" = ${fromWrite(compositeId.addressid)(new Write.Single(AddressId.put))} AND "addresstypeid" = ${fromWrite(compositeId.addresstypeid)(new Write.Single(AddresstypeId.put))}
    returning "businessentityid", "addressid", "addresstypeid", "rowguid", "modifieddate"::text""".query(BusinessentityaddressRow.read).option
  }

  override def upsert(unsaved: BusinessentityaddressRow): ConnectionIO[BusinessentityaddressRow] = {
    sql"""insert into "person"."businessentityaddress"("businessentityid", "addressid", "addresstypeid", "rowguid", "modifieddate")
    values (
      ${fromWrite(unsaved.businessentityid)(new Write.Single(BusinessentityId.put))}::int4,
    ${fromWrite(unsaved.addressid)(new Write.Single(AddressId.put))}::int4,
    ${fromWrite(unsaved.addresstypeid)(new Write.Single(AddresstypeId.put))}::int4,
    ${fromWrite(unsaved.rowguid)(new Write.Single(TypoUUID.put))}::uuid,
    ${fromWrite(unsaved.modifieddate)(new Write.Single(TypoLocalDateTime.put))}::timestamp
    )
    on conflict ("businessentityid", "addressid", "addresstypeid")
    do update set
      "rowguid" = EXCLUDED."rowguid",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "businessentityid", "addressid", "addresstypeid", "rowguid", "modifieddate"::text
    """.query(BusinessentityaddressRow.read).unique
  }

  override def upsertBatch(unsaved: List[BusinessentityaddressRow]): Stream[ConnectionIO, BusinessentityaddressRow] = {
    Update[BusinessentityaddressRow](
      s"""insert into "person"."businessentityaddress"("businessentityid", "addressid", "addresstypeid", "rowguid", "modifieddate")
      values (?::int4,?::int4,?::int4,?::uuid,?::timestamp)
      on conflict ("businessentityid", "addressid", "addresstypeid")
      do update set
        "rowguid" = EXCLUDED."rowguid",
      "modifieddate" = EXCLUDED."modifieddate"
      returning "businessentityid", "addressid", "addresstypeid", "rowguid", "modifieddate"::text"""
    )(BusinessentityaddressRow.write)
    .updateManyWithGeneratedKeys[BusinessentityaddressRow]("businessentityid", "addressid", "addresstypeid", "rowguid", "modifieddate")(unsaved)(catsStdInstancesForList, BusinessentityaddressRow.read)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Stream[ConnectionIO, BusinessentityaddressRow],
    batchSize: Int = 10000
  ): ConnectionIO[Int] = {
    for {
      _ <- sql"""create temporary table businessentityaddress_TEMP (like "person"."businessentityaddress") on commit drop""".update.run
      _ <- new FragmentOps(sql"""copy businessentityaddress_TEMP("businessentityid", "addressid", "addresstypeid", "rowguid", "modifieddate") from stdin""").copyIn(unsaved, batchSize)(BusinessentityaddressRow.pgText)
      res <- sql"""insert into "person"."businessentityaddress"("businessentityid", "addressid", "addresstypeid", "rowguid", "modifieddate")
             select * from businessentityaddress_TEMP
             on conflict ("businessentityid", "addressid", "addresstypeid")
             do update set
               "rowguid" = EXCLUDED."rowguid",
             "modifieddate" = EXCLUDED."modifieddate"
             ;
             drop table businessentityaddress_TEMP;""".update.run
    } yield res
  }
}