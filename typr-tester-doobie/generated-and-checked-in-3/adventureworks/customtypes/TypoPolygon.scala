/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.customtypes

import cats.data.NonEmptyList
import doobie.postgres.Text
import doobie.util.Get
import doobie.util.Put
import io.circe.Decoder
import io.circe.Encoder
import org.postgresql.geometric.PGpoint
import org.postgresql.geometric.PGpolygon
import typr.dsl.Bijection

/** Polygon datatype in PostgreSQL */
case class TypoPolygon(points: List[TypoPoint])

object TypoPolygon {
  given arrayGet: Get[Array[TypoPolygon]] = {
    Get.Advanced.array[AnyRef](NonEmptyList.one("polygon[]"))
      .map(_.map(v => new TypoPolygon(v.asInstanceOf[PGpolygon].points.map(p => new TypoPoint(p.x, p.y)).toList)))
  }

  given arrayPut: Put[Array[TypoPolygon]] = {
    Put.Advanced.array[AnyRef](NonEmptyList.one("polygon[]"), "polygon")
      .contramap(_.map(v => new PGpolygon(v.points.map(p => new PGpoint(p.x, p.y)).toArray)))
  }

  given bijection: Bijection[TypoPolygon, List[TypoPoint]] = Bijection.apply[TypoPolygon, List[TypoPoint]](_.points)(TypoPolygon.apply)

  given decoder: Decoder[TypoPolygon] = Decoder[List[TypoPoint]].map(TypoPolygon.apply)

  given encoder: Encoder[TypoPolygon] = Encoder[List[TypoPoint]].contramap(_.points)

  given get: Get[TypoPolygon] = {
    Get.Advanced.other[PGpolygon](NonEmptyList.one("polygon"))
      .map(v => new TypoPolygon(v.points.map(p => new TypoPoint(p.x, p.y)).toList))
  }

  given pgText: Text[TypoPolygon] = {
    new Text[TypoPolygon] {
      override def unsafeEncode(v: TypoPolygon, sb: StringBuilder): Unit = Text.stringInstance.unsafeEncode("(" + v.points.map(p => s"${p.x}, ${p.y}").mkString(",") + ")", sb)
      override def unsafeArrayEncode(v: TypoPolygon, sb: StringBuilder): Unit = Text.stringInstance.unsafeArrayEncode("(" + v.points.map(p => s"${p.x}, ${p.y}").mkString(",") + ")", sb)
    }
  }

  given put: Put[TypoPolygon] = Put.Advanced.other[PGpolygon](NonEmptyList.one("polygon")).contramap(v => new PGpolygon(v.points.map(p => new PGpoint(p.x, p.y)).toArray))
}