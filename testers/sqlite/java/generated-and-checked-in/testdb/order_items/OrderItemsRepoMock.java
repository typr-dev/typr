/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.order_items;

import dev.typr.dsl.DeleteBuilder;
import dev.typr.dsl.DeleteBuilderMock;
import dev.typr.dsl.DeleteParams;
import dev.typr.dsl.SelectBuilder;
import dev.typr.dsl.SelectBuilderMock;
import dev.typr.dsl.SelectParams;
import dev.typr.dsl.UpdateBuilder;
import dev.typr.dsl.UpdateBuilderMock;
import dev.typr.dsl.UpdateParams;
import dev.typr.foundations.Connection;
import dev.typr.foundations.ConnectionRead;
import java.lang.RuntimeException;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;

public record OrderItemsRepoMock(
  java.util.function.Function<OrderItemsRowUnsaved, OrderItemsRow> toRow,
  HashMap<OrderItemsId, OrderItemsRow> map
) implements OrderItemsRepo {
  public OrderItemsRepoMock(java.util.function.Function<OrderItemsRowUnsaved, OrderItemsRow> toRow) {
    this(toRow, new HashMap<OrderItemsId, OrderItemsRow>());
  }

  public OrderItemsRepoMock withToRow(java.util.function.Function<OrderItemsRowUnsaved, OrderItemsRow> toRow) {
    return new OrderItemsRepoMock(toRow, map);
  }

  public OrderItemsRepoMock withMap(HashMap<OrderItemsId, OrderItemsRow> map) {
    return new OrderItemsRepoMock(toRow, map);
  }

  @Override
  public DeleteBuilder<OrderItemsFields, OrderItemsRow> delete() {
    return new DeleteBuilderMock<>(OrderItemsFields.structure, () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.compositeId(), id -> map.remove(id));
  }

  @Override
  public Boolean deleteById(
    OrderItemsId compositeId,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(compositeId)).isPresent();
  }

  @Override
  public Integer deleteByIds(
    List<OrderItemsId> compositeIds,
    Connection c
  ) {
    var count = 0;
    for (var id : compositeIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    }
    };
    return count;
  }

  @Override
  public OrderItemsRow insert(
    OrderItemsRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.compositeId())) {
      throw new RuntimeException("id " + unsaved.compositeId() + " already exists");
    }
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  }

  @Override
  public OrderItemsRow insert(
    OrderItemsRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  }

  @Override
  public SelectBuilder<OrderItemsFields, OrderItemsRow> select() {
    return new SelectBuilderMock<>(OrderItemsFields.structure, () -> new ArrayList<>(map.values()), SelectParams.empty());
  }

  @Override
  public List<OrderItemsRow> selectAll(ConnectionRead c) {
    return new ArrayList<>(map.values());
  }

  @Override
  public Optional<OrderItemsRow> selectById(
    OrderItemsId compositeId,
    ConnectionRead c
  ) {
    return Optional.ofNullable(map.get(compositeId));
  }

  @Override
  public List<OrderItemsRow> selectByIds(
    List<OrderItemsId> compositeIds,
    ConnectionRead c
  ) {
    var result = new ArrayList<OrderItemsRow>();
    for (var id : compositeIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    }
    };
    return result;
  }

  @Override
  public Map<OrderItemsId, OrderItemsRow> selectByIdsTracked(
    List<OrderItemsId> compositeIds,
    ConnectionRead c
  ) {
    return selectByIds(compositeIds, c).stream().collect(Collectors.toMap((OrderItemsRow row) -> row.compositeId(), Function.identity()));
  }

  @Override
  public UpdateBuilder<OrderItemsFields, OrderItemsRow> update() {
    return new UpdateBuilderMock<>(OrderItemsFields.structure, () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  }

  @Override
  public Boolean update(
    OrderItemsRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.compositeId())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.compositeId(), row);;
    }
    return shouldUpdate;
  }

  @Override
  public OrderItemsRow upsert(
    OrderItemsRow unsaved,
    Connection c
  ) {
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  }

  @Override
  public List<OrderItemsRow> upsertBatch(
    Iterator<OrderItemsRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<OrderItemsRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      result.add(row);
    };
    return result;
  }
}