/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.users;

import dev.typr.dsl.DeleteBuilder;
import dev.typr.dsl.DeleteBuilderMock;
import dev.typr.dsl.DeleteParams;
import dev.typr.dsl.SelectBuilder;
import dev.typr.dsl.SelectBuilderMock;
import dev.typr.dsl.SelectParams;
import dev.typr.dsl.UpdateBuilder;
import dev.typr.dsl.UpdateBuilderMock;
import dev.typr.dsl.UpdateParams;
import dev.typr.foundations.Connection;
import dev.typr.foundations.ConnectionRead;
import java.lang.RuntimeException;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import testdb.userdefined.Email;

public record UsersRepoMock(HashMap<UsersId, UsersRow> map) implements UsersRepo {
  public UsersRepoMock() {
    this(new HashMap<UsersId, UsersRow>());
  }

  public UsersRepoMock withMap(HashMap<UsersId, UsersRow> map) {
    return new UsersRepoMock(map);
  }

  @Override
  public DeleteBuilder<UsersFields, UsersRow> delete() {
    return new DeleteBuilderMock<>(UsersFields.structure, () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.userId(), id -> map.remove(id));
  }

  @Override
  public Boolean deleteById(
    UsersId userId,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(userId)).isPresent();
  }

  @Override
  public Integer deleteByIds(
    List<UsersId> userIds,
    Connection c
  ) {
    var count = 0;
    for (var id : userIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    }
    };
    return count;
  }

  @Override
  public UsersRow insert(
    UsersRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.userId())) {
      throw new RuntimeException("id " + unsaved.userId() + " already exists");
    }
    map.put(unsaved.userId(), unsaved);
    return unsaved;
  }

  @Override
  public SelectBuilder<UsersFields, UsersRow> select() {
    return new SelectBuilderMock<>(UsersFields.structure, () -> new ArrayList<>(map.values()), SelectParams.empty());
  }

  @Override
  public List<UsersRow> selectAll(ConnectionRead c) {
    return new ArrayList<>(map.values());
  }

  @Override
  public Optional<UsersRow> selectById(
    UsersId userId,
    ConnectionRead c
  ) {
    return Optional.ofNullable(map.get(userId));
  }

  @Override
  public List<UsersRow> selectByIds(
    List<UsersId> userIds,
    ConnectionRead c
  ) {
    var result = new ArrayList<UsersRow>();
    for (var id : userIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    }
    };
    return result;
  }

  @Override
  public Map<UsersId, UsersRow> selectByIdsTracked(
    List<UsersId> userIds,
    ConnectionRead c
  ) {
    return selectByIds(userIds, c).stream().collect(Collectors.toMap((UsersRow row) -> row.userId(), Function.identity()));
  }

  @Override
  public Optional<UsersRow> selectByUniqueEmail(
    /* user-picked */ Email email,
    ConnectionRead c
  ) {
    return new ArrayList<>(map.values()).stream().filter(v -> email.equals(v.email())).findFirst();
  }

  @Override
  public Optional<UsersRow> selectByUniqueUsername(
    String username,
    ConnectionRead c
  ) {
    return new ArrayList<>(map.values()).stream().filter(v -> username.equals(v.username())).findFirst();
  }

  @Override
  public UpdateBuilder<UsersFields, UsersRow> update() {
    return new UpdateBuilderMock<>(UsersFields.structure, () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  }

  @Override
  public Boolean update(
    UsersRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.userId())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.userId(), row);;
    }
    return shouldUpdate;
  }

  @Override
  public UsersRow upsert(
    UsersRow unsaved,
    Connection c
  ) {
    map.put(unsaved.userId(), unsaved);
    return unsaved;
  }

  @Override
  public List<UsersRow> upsertBatch(
    Iterator<UsersRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<UsersRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.userId(), row);
      result.add(row);
    };
    return result;
  }
}