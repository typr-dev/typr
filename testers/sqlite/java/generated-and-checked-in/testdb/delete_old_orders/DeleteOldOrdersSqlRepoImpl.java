/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.delete_old_orders;

import dev.typr.foundations.Connection;
import dev.typr.foundations.Fragment;
import dev.typr.foundations.SqliteTypes;
import java.time.LocalDate;
import static dev.typr.foundations.Fragment.concat;

public class DeleteOldOrdersSqlRepoImpl implements DeleteOldOrdersSqlRepo {
  @Override
  public Integer apply(
    LocalDate cutoffDate,
    Connection c
  ) {
    return concat(Fragment.of("-- Delete completed orders older than a cutoff date.\n-- Tests: DELETE statement with required date param, no result columns.\n--\n-- SQLite stores DATE as ISO-8601 TEXT, so sqlglot can't infer that\n-- order_date < :cutoff_date wants a LocalDate. We pin the type\n-- explicitly with the `:name:Type!` annotation — same opt-in pattern\n-- as the other dialects use for parameters not otherwise resolvable\n-- from schema lineage.\nDELETE FROM orders\nWHERE status = 'completed' AND order_date < "), Fragment.encode(SqliteTypes.date, cutoffDate), Fragment.of("")).update().run(c);
  }
}