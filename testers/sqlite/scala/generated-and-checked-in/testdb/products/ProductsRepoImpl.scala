/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.products

import dev.typr.dslsc.DeleteBuilder
import dev.typr.dslsc.Dialect
import dev.typr.dslsc.SelectBuilder
import dev.typr.dslsc.UpdateBuilder
import dev.typr.foundationssc.Connection
import dev.typr.foundationssc.ConnectionRead
import dev.typr.foundationssc.Fragment
import dev.typr.foundationssc.SqliteTypes
import scala.collection.mutable.ListBuffer
import dev.typr.foundationssc.Fragment.sql

class ProductsRepoImpl extends ProductsRepo {
  override def delete: DeleteBuilder[ProductsFields, ProductsRow] = DeleteBuilder.of(""""products"""", ProductsFields.structure, Dialect.SQLITE)

  override def deleteById(productId: ProductsId)(using c: Connection): Boolean = sql"""delete from "products" where "product_id" = ${Fragment.encode(ProductsId.sqliteType, productId)}""".update().run(using c) > 0

  override def deleteByIds(productIds: List[ProductsId])(using c: Connection): Int = {
    val fragments: ListBuffer[Fragment] = ListBuffer()
    productIds.foreach { id => fragments.addOne(Fragment.encode(ProductsId.sqliteType, id)): @scala.annotation.nowarn }
    return Fragment.concat(Fragment.of("""delete from "products" where "product_id" in ("""), Fragment.comma(fragments), Fragment.of(")")).update().run(using c)
  }

  override def insert(unsaved: ProductsRow)(using c: Connection): ProductsRow = {
  sql"""insert into "products"("product_id", "sku", "name", "price", "metadata")
    values (${Fragment.encode(ProductsId.sqliteType, unsaved.productId)}, ${Fragment.encode(SqliteTypes.varchar, unsaved.sku)}, ${Fragment.encode(SqliteTypes.text, unsaved.name)}, ${Fragment.encode(SqliteTypes.numeric, unsaved.price)}, ${Fragment.encode(SqliteTypes.json.opt, unsaved.metadata)})
    RETURNING "product_id", "sku", "name", "price", "metadata"
    """
    .updateReturning(ProductsRow.rowCodec.exactlyOne()).run(using c)
  }

  override def select: SelectBuilder[ProductsFields, ProductsRow] = SelectBuilder.of(""""products"""", ProductsFields.structure, ProductsRow.rowCodec, Dialect.SQLITE)

  override def selectAll(using c: ConnectionRead): List[ProductsRow] = {
    sql"""select "product_id", "sku", "name", "price", "metadata"
    from "products"
    """.query(ProductsRow.rowCodec.all()).run(using c)
  }

  override def selectById(productId: ProductsId)(using c: ConnectionRead): Option[ProductsRow] = {
    sql"""select "product_id", "sku", "name", "price", "metadata"
    from "products"
    where "product_id" = ${Fragment.encode(ProductsId.sqliteType, productId)}""".query(ProductsRow.rowCodec.first()).run(using c)
  }

  override def selectByIds(productIds: List[ProductsId])(using c: ConnectionRead): List[ProductsRow] = {
    val fragments: ListBuffer[Fragment] = ListBuffer()
    productIds.foreach { id => fragments.addOne(Fragment.encode(ProductsId.sqliteType, id)): @scala.annotation.nowarn }
    return Fragment.concat(Fragment.of("""select "product_id", "sku", "name", "price", "metadata" from "products" where "product_id" in ("""), Fragment.comma(fragments), Fragment.of(")")).query(ProductsRow.rowCodec.all()).run(using c)
  }

  override def selectByIdsTracked(productIds: List[ProductsId])(using c: ConnectionRead): Map[ProductsId, ProductsRow] = {
    val ret: scala.collection.mutable.Map[ProductsId, ProductsRow] = scala.collection.mutable.Map.empty[ProductsId, ProductsRow]
    selectByIds(productIds)(using c).foreach(row => ret.put(row.productId, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def selectByUniqueSku(sku: String)(using c: ConnectionRead): Option[ProductsRow] = {
    sql"""select "product_id", "sku", "name", "price", "metadata"
    from "products"
    where "sku" = ${Fragment.encode(SqliteTypes.varchar, sku)}
    """.query(ProductsRow.rowCodec.first()).run(using c)
  }

  override def update: UpdateBuilder[ProductsFields, ProductsRow] = UpdateBuilder.of(""""products"""", ProductsFields.structure, ProductsRow.rowCodec, Dialect.SQLITE)

  override def update(row: ProductsRow)(using c: Connection): Boolean = {
    val productId: ProductsId = row.productId
    return sql"""update "products"
    set "sku" = ${Fragment.encode(SqliteTypes.varchar, row.sku)},
    "name" = ${Fragment.encode(SqliteTypes.text, row.name)},
    "price" = ${Fragment.encode(SqliteTypes.numeric, row.price)},
    "metadata" = ${Fragment.encode(SqliteTypes.json.opt, row.metadata)}
    where "product_id" = ${Fragment.encode(ProductsId.sqliteType, productId)}""".update().run(using c) > 0
  }

  override def upsert(unsaved: ProductsRow)(using c: Connection): ProductsRow = {
  sql"""INSERT INTO "products"("product_id", "sku", "name", "price", "metadata")
    VALUES (${Fragment.encode(ProductsId.sqliteType, unsaved.productId)}, ${Fragment.encode(SqliteTypes.varchar, unsaved.sku)}, ${Fragment.encode(SqliteTypes.text, unsaved.name)}, ${Fragment.encode(SqliteTypes.numeric, unsaved.price)}, ${Fragment.encode(SqliteTypes.json.opt, unsaved.metadata)})
    ON CONFLICT ("product_id")
    DO UPDATE SET
      "sku" = EXCLUDED."sku",
    "name" = EXCLUDED."name",
    "price" = EXCLUDED."price",
    "metadata" = EXCLUDED."metadata"
    RETURNING "product_id", "sku", "name", "price", "metadata""""
    .updateReturning(ProductsRow.rowCodec.exactlyOne())
    .run(using c)
  }

  override def upsertBatch(unsaved: Iterator[ProductsRow])(using c: Connection): List[ProductsRow] = {
    sql"""INSERT INTO "products"("product_id", "sku", "name", "price", "metadata")
    VALUES (?, ?, ?, ?, ?)
    ON CONFLICT ("product_id")
    DO UPDATE SET
      "sku" = EXCLUDED."sku",
    "name" = EXCLUDED."name",
    "price" = EXCLUDED."price",
    "metadata" = EXCLUDED."metadata"
    RETURNING "product_id", "sku", "name", "price", "metadata""""
      .updateReturningEach(ProductsRow.rowCodec, unsaved)
    .run(using c)
  }
}