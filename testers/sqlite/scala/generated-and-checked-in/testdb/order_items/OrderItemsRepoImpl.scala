/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.order_items

import dev.typr.dslsc.DeleteBuilder
import dev.typr.dslsc.Dialect
import dev.typr.dslsc.SelectBuilder
import dev.typr.dslsc.UpdateBuilder
import dev.typr.foundationssc.Connection
import dev.typr.foundationssc.ConnectionRead
import dev.typr.foundationssc.Fragment
import dev.typr.foundationssc.SqliteTypes
import scala.collection.mutable.ListBuffer
import testdb.orders.OrdersId
import testdb.products.ProductsId
import dev.typr.foundationssc.Fragment.sql

class OrderItemsRepoImpl extends OrderItemsRepo {
  override def delete: DeleteBuilder[OrderItemsFields, OrderItemsRow] = DeleteBuilder.of(""""order_items"""", OrderItemsFields.structure, Dialect.SQLITE)

  override def deleteById(compositeId: OrderItemsId)(using c: Connection): Boolean = sql"""delete from "order_items" where "order_id" = ${Fragment.encode(OrdersId.sqliteType, compositeId.orderId)} AND "product_id" = ${Fragment.encode(ProductsId.sqliteType, compositeId.productId)}""".update().run(using c) > 0

  override def deleteByIds(compositeIds: List[OrderItemsId])(using c: Connection): Int = {
    val fragments: ListBuffer[Fragment] = ListBuffer()
    compositeIds.foreach { id => fragments.addOne(Fragment.concat(Fragment.of("("), Fragment.encode(OrdersId.sqliteType, id.orderId), Fragment.of(", "), Fragment.encode(ProductsId.sqliteType, id.productId), Fragment.of(")"))): @scala.annotation.nowarn }
    return Fragment.concat(Fragment.of("""delete from "order_items" where ("order_id", "product_id") in ("""), Fragment.comma(fragments), Fragment.of(")")).update().run(using c)
  }

  override def insert(unsaved: OrderItemsRow)(using c: Connection): OrderItemsRow = {
  sql"""insert into "order_items"("order_id", "product_id", "quantity", "unit_price")
    values (${Fragment.encode(OrdersId.sqliteType, unsaved.orderId)}, ${Fragment.encode(ProductsId.sqliteType, unsaved.productId)}, ${Fragment.encode(SqliteTypes.integer, unsaved.quantity)}, ${Fragment.encode(SqliteTypes.numeric, unsaved.unitPrice)})
    RETURNING "order_id", "product_id", "quantity", "unit_price"
    """
    .updateReturning(OrderItemsRow.rowCodec.exactlyOne()).run(using c)
  }

  override def insert(unsaved: OrderItemsRowUnsaved)(using c: Connection): OrderItemsRow = {
    val columns: ListBuffer[Fragment] = ListBuffer()
    val values: ListBuffer[Fragment] = ListBuffer()
    columns.addOne(Fragment.of(""""order_id"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(OrdersId.sqliteType, unsaved.orderId)}"): @scala.annotation.nowarn
    columns.addOne(Fragment.of(""""product_id"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(ProductsId.sqliteType, unsaved.productId)}"): @scala.annotation.nowarn
    columns.addOne(Fragment.of(""""unit_price"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(SqliteTypes.numeric, unsaved.unitPrice)}"): @scala.annotation.nowarn
    unsaved.quantity.visit(
      {  },
      value => { columns.addOne(Fragment.of(""""quantity"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(SqliteTypes.integer, value)}"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      sql"""insert into "order_items"(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      RETURNING "order_id", "product_id", "quantity", "unit_price"
      """
    }
    return q.updateReturning(OrderItemsRow.rowCodec.exactlyOne()).run(using c)
  }

  override def select: SelectBuilder[OrderItemsFields, OrderItemsRow] = SelectBuilder.of(""""order_items"""", OrderItemsFields.structure, OrderItemsRow.rowCodec, Dialect.SQLITE)

  override def selectAll(using c: ConnectionRead): List[OrderItemsRow] = {
    sql"""select "order_id", "product_id", "quantity", "unit_price"
    from "order_items"
    """.query(OrderItemsRow.rowCodec.all()).run(using c)
  }

  override def selectById(compositeId: OrderItemsId)(using c: ConnectionRead): Option[OrderItemsRow] = {
    sql"""select "order_id", "product_id", "quantity", "unit_price"
    from "order_items"
    where "order_id" = ${Fragment.encode(OrdersId.sqliteType, compositeId.orderId)} AND "product_id" = ${Fragment.encode(ProductsId.sqliteType, compositeId.productId)}""".query(OrderItemsRow.rowCodec.first()).run(using c)
  }

  override def selectByIds(compositeIds: List[OrderItemsId])(using c: ConnectionRead): List[OrderItemsRow] = {
    val fragments: ListBuffer[Fragment] = ListBuffer()
    compositeIds.foreach { id => fragments.addOne(Fragment.concat(Fragment.of("("), Fragment.encode(OrdersId.sqliteType, id.orderId), Fragment.of(", "), Fragment.encode(ProductsId.sqliteType, id.productId), Fragment.of(")"))): @scala.annotation.nowarn }
    return Fragment.concat(Fragment.of("""select "order_id", "product_id", "quantity", "unit_price" from "order_items" where ("order_id", "product_id") in ("""), Fragment.comma(fragments), Fragment.of(")")).query(OrderItemsRow.rowCodec.all()).run(using c)
  }

  override def selectByIdsTracked(compositeIds: List[OrderItemsId])(using c: ConnectionRead): Map[OrderItemsId, OrderItemsRow] = {
    val ret: scala.collection.mutable.Map[OrderItemsId, OrderItemsRow] = scala.collection.mutable.Map.empty[OrderItemsId, OrderItemsRow]
    selectByIds(compositeIds)(using c).foreach(row => ret.put(row.compositeId, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[OrderItemsFields, OrderItemsRow] = UpdateBuilder.of(""""order_items"""", OrderItemsFields.structure, OrderItemsRow.rowCodec, Dialect.SQLITE)

  override def update(row: OrderItemsRow)(using c: Connection): Boolean = {
    val compositeId: OrderItemsId = row.compositeId
    return sql"""update "order_items"
    set "quantity" = ${Fragment.encode(SqliteTypes.integer, row.quantity)},
    "unit_price" = ${Fragment.encode(SqliteTypes.numeric, row.unitPrice)}
    where "order_id" = ${Fragment.encode(OrdersId.sqliteType, compositeId.orderId)} AND "product_id" = ${Fragment.encode(ProductsId.sqliteType, compositeId.productId)}""".update().run(using c) > 0
  }

  override def upsert(unsaved: OrderItemsRow)(using c: Connection): OrderItemsRow = {
  sql"""INSERT INTO "order_items"("order_id", "product_id", "quantity", "unit_price")
    VALUES (${Fragment.encode(OrdersId.sqliteType, unsaved.orderId)}, ${Fragment.encode(ProductsId.sqliteType, unsaved.productId)}, ${Fragment.encode(SqliteTypes.integer, unsaved.quantity)}, ${Fragment.encode(SqliteTypes.numeric, unsaved.unitPrice)})
    ON CONFLICT ("order_id", "product_id")
    DO UPDATE SET
      "quantity" = EXCLUDED."quantity",
    "unit_price" = EXCLUDED."unit_price"
    RETURNING "order_id", "product_id", "quantity", "unit_price""""
    .updateReturning(OrderItemsRow.rowCodec.exactlyOne())
    .run(using c)
  }

  override def upsertBatch(unsaved: Iterator[OrderItemsRow])(using c: Connection): List[OrderItemsRow] = {
    sql"""INSERT INTO "order_items"("order_id", "product_id", "quantity", "unit_price")
    VALUES (?, ?, ?, ?)
    ON CONFLICT ("order_id", "product_id")
    DO UPDATE SET
      "quantity" = EXCLUDED."quantity",
    "unit_price" = EXCLUDED."unit_price"
    RETURNING "order_id", "product_id", "quantity", "unit_price""""
      .updateReturningEach(OrderItemsRow.rowCodec, unsaved)
    .run(using c)
  }
}