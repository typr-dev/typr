/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.order_items

import com.fasterxml.jackson.annotation.JsonProperty
import dev.typr.dslsc.RowCodecs
import dev.typr.foundations.Tuple.Tuple4
import dev.typr.foundationssc.RowCodec
import dev.typr.foundationssc.SqliteTypes
import testdb.customtypes.Defaulted
import testdb.orders.OrdersId
import testdb.products.ProductsId

/** Table: order_items
 * Composite primary key: order_id, product_id
 */
case class OrderItemsRow(
  /** Points to [[testdb.orders.OrdersRow.orderId]] */
  @JsonProperty("order_id") orderId: OrdersId,
  /** Points to [[testdb.products.ProductsRow.productId]] */
  @JsonProperty("product_id") productId: ProductsId,
  /** Default: 1 */
  quantity: Long,
  @JsonProperty("unit_price") unitPrice: BigDecimal
) extends Tuple4[OrdersId, ProductsId, Long, BigDecimal] {
  def compositeId: OrderItemsId = new OrderItemsId(orderId, productId)

  def id: OrderItemsId = this.compositeId

  def toUnsavedRow(quantity: Defaulted[Long] = Defaulted.Provided(this.quantity)): OrderItemsRowUnsaved = {
    new OrderItemsRowUnsaved(
      orderId,
      productId,
      unitPrice,
      quantity
    )
  }

  override def `_1`: OrdersId = orderId

  override def `_2`: ProductsId = productId

  override def `_3`: Long = quantity

  override def `_4`: BigDecimal = unitPrice
}

object OrderItemsRow {
  def apply(
    compositeId: OrderItemsId,
    quantity: Long,
    unitPrice: BigDecimal
  ): OrderItemsRow = {
    new OrderItemsRow(
      compositeId.orderId,
      compositeId.productId,
      quantity,
      unitPrice
    )
  }

  val rowCodec: RowCodec[OrderItemsRow] = RowCodecs.of(OrdersId.sqliteType, ProductsId.sqliteType, SqliteTypes.integer, SqliteTypes.numeric)(OrderItemsRow.apply)(row => Array[Any](row.orderId, row.productId, row.quantity, row.unitPrice))
}