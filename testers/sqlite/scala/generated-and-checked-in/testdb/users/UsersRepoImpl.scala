/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.users

import dev.typr.dslsc.DeleteBuilder
import dev.typr.dslsc.Dialect
import dev.typr.dslsc.SelectBuilder
import dev.typr.dslsc.UpdateBuilder
import dev.typr.foundationssc.Connection
import dev.typr.foundationssc.ConnectionRead
import dev.typr.foundationssc.Fragment
import dev.typr.foundationssc.SqliteTypes
import scala.collection.mutable.ListBuffer
import testdb.userdefined.Email
import dev.typr.foundationssc.Fragment.sql

class UsersRepoImpl extends UsersRepo {
  override def delete: DeleteBuilder[UsersFields, UsersRow] = DeleteBuilder.of(""""users"""", UsersFields.structure, Dialect.SQLITE)

  override def deleteById(userId: UsersId)(using c: Connection): Boolean = sql"""delete from "users" where "user_id" = ${Fragment.encode(UsersId.sqliteType, userId)}""".update().run(using c) > 0

  override def deleteByIds(userIds: List[UsersId])(using c: Connection): Int = {
    val fragments: ListBuffer[Fragment] = ListBuffer()
    userIds.foreach { id => fragments.addOne(Fragment.encode(UsersId.sqliteType, id)): @scala.annotation.nowarn }
    return Fragment.concat(Fragment.of("""delete from "users" where "user_id" in ("""), Fragment.comma(fragments), Fragment.of(")")).update().run(using c)
  }

  override def insert(unsaved: UsersRow)(using c: Connection): UsersRow = {
  sql"""insert into "users"("user_id", "username", "email")
    values (${Fragment.encode(UsersId.sqliteType, unsaved.userId)}, ${Fragment.encode(SqliteTypes.varchar, unsaved.username)}, ${Fragment.encode(Email.sqliteType, unsaved.email)})
    RETURNING "user_id", "username", "email"
    """
    .updateReturning(UsersRow.rowCodec.exactlyOne()).run(using c)
  }

  override def select: SelectBuilder[UsersFields, UsersRow] = SelectBuilder.of(""""users"""", UsersFields.structure, UsersRow.rowCodec, Dialect.SQLITE)

  override def selectAll(using c: ConnectionRead): List[UsersRow] = {
    sql"""select "user_id", "username", "email"
    from "users"
    """.query(UsersRow.rowCodec.all()).run(using c)
  }

  override def selectById(userId: UsersId)(using c: ConnectionRead): Option[UsersRow] = {
    sql"""select "user_id", "username", "email"
    from "users"
    where "user_id" = ${Fragment.encode(UsersId.sqliteType, userId)}""".query(UsersRow.rowCodec.first()).run(using c)
  }

  override def selectByIds(userIds: List[UsersId])(using c: ConnectionRead): List[UsersRow] = {
    val fragments: ListBuffer[Fragment] = ListBuffer()
    userIds.foreach { id => fragments.addOne(Fragment.encode(UsersId.sqliteType, id)): @scala.annotation.nowarn }
    return Fragment.concat(Fragment.of("""select "user_id", "username", "email" from "users" where "user_id" in ("""), Fragment.comma(fragments), Fragment.of(")")).query(UsersRow.rowCodec.all()).run(using c)
  }

  override def selectByIdsTracked(userIds: List[UsersId])(using c: ConnectionRead): Map[UsersId, UsersRow] = {
    val ret: scala.collection.mutable.Map[UsersId, UsersRow] = scala.collection.mutable.Map.empty[UsersId, UsersRow]
    selectByIds(userIds)(using c).foreach(row => ret.put(row.userId, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def selectByUniqueEmail(email: /* user-picked */ Email)(using c: ConnectionRead): Option[UsersRow] = {
    sql"""select "user_id", "username", "email"
    from "users"
    where "email" = ${Fragment.encode(Email.sqliteType, email)}
    """.query(UsersRow.rowCodec.first()).run(using c)
  }

  override def selectByUniqueUsername(username: String)(using c: ConnectionRead): Option[UsersRow] = {
    sql"""select "user_id", "username", "email"
    from "users"
    where "username" = ${Fragment.encode(SqliteTypes.varchar, username)}
    """.query(UsersRow.rowCodec.first()).run(using c)
  }

  override def update: UpdateBuilder[UsersFields, UsersRow] = UpdateBuilder.of(""""users"""", UsersFields.structure, UsersRow.rowCodec, Dialect.SQLITE)

  override def update(row: UsersRow)(using c: Connection): Boolean = {
    val userId: UsersId = row.userId
    return sql"""update "users"
    set "username" = ${Fragment.encode(SqliteTypes.varchar, row.username)},
    "email" = ${Fragment.encode(Email.sqliteType, row.email)}
    where "user_id" = ${Fragment.encode(UsersId.sqliteType, userId)}""".update().run(using c) > 0
  }

  override def upsert(unsaved: UsersRow)(using c: Connection): UsersRow = {
  sql"""INSERT INTO "users"("user_id", "username", "email")
    VALUES (${Fragment.encode(UsersId.sqliteType, unsaved.userId)}, ${Fragment.encode(SqliteTypes.varchar, unsaved.username)}, ${Fragment.encode(Email.sqliteType, unsaved.email)})
    ON CONFLICT ("user_id")
    DO UPDATE SET
      "username" = EXCLUDED."username",
    "email" = EXCLUDED."email"
    RETURNING "user_id", "username", "email""""
    .updateReturning(UsersRow.rowCodec.exactlyOne())
    .run(using c)
  }

  override def upsertBatch(unsaved: Iterator[UsersRow])(using c: Connection): List[UsersRow] = {
    sql"""INSERT INTO "users"("user_id", "username", "email")
    VALUES (?, ?, ?)
    ON CONFLICT ("user_id")
    DO UPDATE SET
      "username" = EXCLUDED."username",
    "email" = EXCLUDED."email"
    RETURNING "user_id", "username", "email""""
      .updateReturningEach(UsersRow.rowCodec, unsaved)
    .run(using c)
  }
}