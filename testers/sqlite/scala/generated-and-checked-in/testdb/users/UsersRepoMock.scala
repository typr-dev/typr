/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.users

import dev.typr.dslsc.DeleteBuilder
import dev.typr.dslsc.DeleteBuilderMock
import dev.typr.dslsc.DeleteParams
import dev.typr.dslsc.SelectBuilder
import dev.typr.dslsc.SelectBuilderMock
import dev.typr.dslsc.SelectParams
import dev.typr.dslsc.UpdateBuilder
import dev.typr.dslsc.UpdateBuilderMock
import dev.typr.dslsc.UpdateParams
import dev.typr.foundationssc.Connection
import dev.typr.foundationssc.ConnectionRead
import java.lang.RuntimeException
import testdb.userdefined.Email

case class UsersRepoMock(map: scala.collection.mutable.Map[UsersId, UsersRow] = scala.collection.mutable.Map.empty[UsersId, UsersRow]) extends UsersRepo {
  override def delete: DeleteBuilder[UsersFields, UsersRow] = DeleteBuilderMock(UsersFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.userId, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(userId: UsersId)(using c: Connection): Boolean = map.remove(userId).isDefined

  override def deleteByIds(userIds: List[UsersId])(using c: Connection): Int = {
    var count = 0
    userIds.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: UsersRow)(using c: Connection): UsersRow = {
    if (map.contains(unsaved.userId)) {
      throw new RuntimeException(s"id ${unsaved.userId} already exists")
    }
    map.put(unsaved.userId, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def select: SelectBuilder[UsersFields, UsersRow] = SelectBuilderMock(UsersFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: ConnectionRead): List[UsersRow] = map.values.toList

  override def selectById(userId: UsersId)(using c: ConnectionRead): Option[UsersRow] = map.get(userId)

  override def selectByIds(userIds: List[UsersId])(using c: ConnectionRead): List[UsersRow] = userIds.flatMap(map.get(_)).toList

  override def selectByIdsTracked(userIds: List[UsersId])(using c: ConnectionRead): Map[UsersId, UsersRow] = selectByIds(userIds)(using c).map(x => (((row: UsersRow) => row.userId).apply(x), x)).toMap

  override def selectByUniqueEmail(email: /* user-picked */ Email)(using c: ConnectionRead): Option[UsersRow] = map.values.toList.find(v => (email == v.email))

  override def selectByUniqueUsername(username: String)(using c: ConnectionRead): Option[UsersRow] = map.values.toList.find(v => (username == v.username))

  override def update: UpdateBuilder[UsersFields, UsersRow] = UpdateBuilderMock(UsersFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: UsersRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.userId).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.userId, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: UsersRow)(using c: Connection): UsersRow = {
    map.put(unsaved.userId, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[UsersRow])(using c: Connection): List[UsersRow] = {
    unsaved.map { row =>
      map.put(row.userId, row): @scala.annotation.nowarn
      row
    }.toList
  }
}