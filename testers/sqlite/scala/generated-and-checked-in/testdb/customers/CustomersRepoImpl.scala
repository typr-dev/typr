/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.customers

import dev.typr.dslsc.DeleteBuilder
import dev.typr.dslsc.Dialect
import dev.typr.dslsc.SelectBuilder
import dev.typr.dslsc.UpdateBuilder
import dev.typr.foundationssc.Connection
import dev.typr.foundationssc.ConnectionRead
import dev.typr.foundationssc.Fragment
import dev.typr.foundationssc.SqliteTypes
import scala.collection.mutable.ListBuffer
import testdb.userdefined.Email
import dev.typr.foundationssc.Fragment.sql

class CustomersRepoImpl extends CustomersRepo {
  override def delete: DeleteBuilder[CustomersFields, CustomersRow] = DeleteBuilder.of(""""customers"""", CustomersFields.structure, Dialect.SQLITE)

  override def deleteById(customerId: CustomersId)(using c: Connection): Boolean = sql"""delete from "customers" where "customer_id" = ${Fragment.encode(CustomersId.sqliteType, customerId)}""".update().run(using c) > 0

  override def deleteByIds(customerIds: List[CustomersId])(using c: Connection): Int = {
    val fragments: ListBuffer[Fragment] = ListBuffer()
    customerIds.foreach { id => fragments.addOne(Fragment.encode(CustomersId.sqliteType, id)): @scala.annotation.nowarn }
    return Fragment.concat(Fragment.of("""delete from "customers" where "customer_id" in ("""), Fragment.comma(fragments), Fragment.of(")")).update().run(using c)
  }

  override def insert(unsaved: CustomersRow)(using c: Connection): CustomersRow = {
  sql"""insert into "customers"("customer_id", "name", "email", "created_at")
    values (${Fragment.encode(CustomersId.sqliteType, unsaved.customerId)}, ${Fragment.encode(SqliteTypes.text, unsaved.name)}, ${Fragment.encode(Email.sqliteType.opt, unsaved.email)}, ${Fragment.encode(SqliteTypes.timestamp, unsaved.createdAt)})
    RETURNING "customer_id", "name", "email", "created_at"
    """
    .updateReturning(CustomersRow.rowCodec.exactlyOne()).run(using c)
  }

  override def insert(unsaved: CustomersRowUnsaved)(using c: Connection): CustomersRow = {
    val columns: ListBuffer[Fragment] = ListBuffer()
    val values: ListBuffer[Fragment] = ListBuffer()
    columns.addOne(Fragment.of(""""customer_id"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(CustomersId.sqliteType, unsaved.customerId)}"): @scala.annotation.nowarn
    columns.addOne(Fragment.of(""""name"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(SqliteTypes.text, unsaved.name)}"): @scala.annotation.nowarn
    columns.addOne(Fragment.of(""""email"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(Email.sqliteType.opt, unsaved.email)}"): @scala.annotation.nowarn
    unsaved.createdAt.visit(
      {  },
      value => { columns.addOne(Fragment.of(""""created_at"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(SqliteTypes.timestamp, value)}"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      sql"""insert into "customers"(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      RETURNING "customer_id", "name", "email", "created_at"
      """
    }
    return q.updateReturning(CustomersRow.rowCodec.exactlyOne()).run(using c)
  }

  override def select: SelectBuilder[CustomersFields, CustomersRow] = SelectBuilder.of(""""customers"""", CustomersFields.structure, CustomersRow.rowCodec, Dialect.SQLITE)

  override def selectAll(using c: ConnectionRead): List[CustomersRow] = {
    sql"""select "customer_id", "name", "email", "created_at"
    from "customers"
    """.query(CustomersRow.rowCodec.all()).run(using c)
  }

  override def selectById(customerId: CustomersId)(using c: ConnectionRead): Option[CustomersRow] = {
    sql"""select "customer_id", "name", "email", "created_at"
    from "customers"
    where "customer_id" = ${Fragment.encode(CustomersId.sqliteType, customerId)}""".query(CustomersRow.rowCodec.first()).run(using c)
  }

  override def selectByIds(customerIds: List[CustomersId])(using c: ConnectionRead): List[CustomersRow] = {
    val fragments: ListBuffer[Fragment] = ListBuffer()
    customerIds.foreach { id => fragments.addOne(Fragment.encode(CustomersId.sqliteType, id)): @scala.annotation.nowarn }
    return Fragment.concat(Fragment.of("""select "customer_id", "name", "email", "created_at" from "customers" where "customer_id" in ("""), Fragment.comma(fragments), Fragment.of(")")).query(CustomersRow.rowCodec.all()).run(using c)
  }

  override def selectByIdsTracked(customerIds: List[CustomersId])(using c: ConnectionRead): Map[CustomersId, CustomersRow] = {
    val ret: scala.collection.mutable.Map[CustomersId, CustomersRow] = scala.collection.mutable.Map.empty[CustomersId, CustomersRow]
    selectByIds(customerIds)(using c).foreach(row => ret.put(row.customerId, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[CustomersFields, CustomersRow] = UpdateBuilder.of(""""customers"""", CustomersFields.structure, CustomersRow.rowCodec, Dialect.SQLITE)

  override def update(row: CustomersRow)(using c: Connection): Boolean = {
    val customerId: CustomersId = row.customerId
    return sql"""update "customers"
    set "name" = ${Fragment.encode(SqliteTypes.text, row.name)},
    "email" = ${Fragment.encode(Email.sqliteType.opt, row.email)},
    "created_at" = ${Fragment.encode(SqliteTypes.timestamp, row.createdAt)}
    where "customer_id" = ${Fragment.encode(CustomersId.sqliteType, customerId)}""".update().run(using c) > 0
  }

  override def upsert(unsaved: CustomersRow)(using c: Connection): CustomersRow = {
  sql"""INSERT INTO "customers"("customer_id", "name", "email", "created_at")
    VALUES (${Fragment.encode(CustomersId.sqliteType, unsaved.customerId)}, ${Fragment.encode(SqliteTypes.text, unsaved.name)}, ${Fragment.encode(Email.sqliteType.opt, unsaved.email)}, ${Fragment.encode(SqliteTypes.timestamp, unsaved.createdAt)})
    ON CONFLICT ("customer_id")
    DO UPDATE SET
      "name" = EXCLUDED."name",
    "email" = EXCLUDED."email",
    "created_at" = EXCLUDED."created_at"
    RETURNING "customer_id", "name", "email", "created_at""""
    .updateReturning(CustomersRow.rowCodec.exactlyOne())
    .run(using c)
  }

  override def upsertBatch(unsaved: Iterator[CustomersRow])(using c: Connection): List[CustomersRow] = {
    sql"""INSERT INTO "customers"("customer_id", "name", "email", "created_at")
    VALUES (?, ?, ?, ?)
    ON CONFLICT ("customer_id")
    DO UPDATE SET
      "name" = EXCLUDED."name",
    "email" = EXCLUDED."email",
    "created_at" = EXCLUDED."created_at"
    RETURNING "customer_id", "name", "email", "created_at""""
      .updateReturningEach(CustomersRow.rowCodec, unsaved)
    .run(using c)
  }
}