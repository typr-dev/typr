/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.customers

import com.fasterxml.jackson.annotation.JsonProperty
import dev.typr.dslsc.RowCodecs
import dev.typr.foundations.Tuple.Tuple4
import dev.typr.foundationssc.RowCodec
import dev.typr.foundationssc.SqliteTypes
import java.time.LocalDateTime
import testdb.customtypes.Defaulted
import testdb.userdefined.Email

/** Table: customers
 * Primary key: customer_id
 */
case class CustomersRow(
  @JsonProperty("customer_id") customerId: CustomersId,
  name: String,
  email: Option[/* user-picked */ Email],
  /** Default: CURRENT_TIMESTAMP */
  @JsonProperty("created_at") createdAt: LocalDateTime
) extends Tuple4[CustomersId, String, Option[/* user-picked */ Email], LocalDateTime] {
  def id: CustomersId = customerId

  def toUnsavedRow(createdAt: Defaulted[LocalDateTime] = Defaulted.Provided(this.createdAt)): CustomersRowUnsaved = {
    new CustomersRowUnsaved(
      customerId,
      name,
      email,
      createdAt
    )
  }

  override def `_1`: CustomersId = customerId

  override def `_2`: String = name

  override def `_3`: Option[/* user-picked */ Email] = email

  override def `_4`: LocalDateTime = createdAt
}

object CustomersRow {
  val rowCodec: RowCodec[CustomersRow] = RowCodecs.of(CustomersId.sqliteType, SqliteTypes.text, Email.sqliteType.opt, SqliteTypes.timestamp)(CustomersRow.apply)(row => Array[Any](row.customerId, row.name, row.email, row.createdAt))
}