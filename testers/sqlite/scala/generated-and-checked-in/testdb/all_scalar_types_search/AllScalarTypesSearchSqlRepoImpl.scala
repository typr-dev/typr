/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.all_scalar_types_search

import dev.typr.foundationssc.ConnectionRead
import dev.typr.foundationssc.Fragment
import dev.typr.foundationssc.SqliteTypes
import java.time.LocalDate
import dev.typr.foundationssc.Fragment.sql

class AllScalarTypesSearchSqlRepoImpl extends AllScalarTypesSearchSqlRepo {
  override def apply(
    minId: Option[Long],
    textPattern: Option[String],
    afterDate: Option[LocalDate]
  )(using c: ConnectionRead): List[AllScalarTypesSearchSqlRow] = {
    sql"""-- Filter the all_scalar_types table by a handful of columns.
    -- Tests: a parameter for each affinity (INTEGER, REAL, TEXT, DATE).
  
    SELECT
        id,
        col_tinyint,
        col_smallint,
        col_integer,
        col_bigint,
        col_real,
        col_double,
        col_decimal,
        col_text,
        col_date,
        col_uuid,
        col_json
    FROM all_scalar_types
    WHERE
        (${Fragment.encode(SqliteTypes.integer.opt, minId)} IS NULL OR id >= ${Fragment.encode(SqliteTypes.integer.opt, minId)})
        AND (${Fragment.encode(SqliteTypes.text.opt, textPattern)} IS NULL OR col_text LIKE ${Fragment.encode(SqliteTypes.text.opt, textPattern)})
        AND (${Fragment.encode(SqliteTypes.date.opt, afterDate)} IS NULL OR col_date >= ${Fragment.encode(SqliteTypes.date.opt, afterDate)})
    ORDER BY id""".query(AllScalarTypesSearchSqlRow.rowCodec.all()).run(using c)
  }
}