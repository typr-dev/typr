/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package oracledb.contacts

import dev.typr.dslkt.DeleteBuilder
import dev.typr.dslkt.DeleteBuilderMock
import dev.typr.dslkt.DeleteParams
import dev.typr.dslkt.SelectBuilder
import dev.typr.dslkt.SelectBuilderMock
import dev.typr.dslkt.SelectParams
import dev.typr.dslkt.UpdateBuilder
import dev.typr.dslkt.UpdateBuilderMock
import dev.typr.dslkt.UpdateParams
import dev.typr.foundationskt.Connection
import dev.typr.foundationskt.ConnectionRead
import java.lang.RuntimeException
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap

data class ContactsRepoMock(
  val toRow: (ContactsRowUnsaved) -> ContactsRow,
  val map: MutableMap<ContactsId, ContactsRow> = mutableMapOf<ContactsId, ContactsRow>()
) : ContactsRepo {
  override fun delete(): DeleteBuilder<ContactsFields, ContactsRow> = DeleteBuilderMock(ContactsFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.contactId }, { id -> map.remove(id) })

  override fun deleteById(
    contactId: ContactsId,
    c: Connection
  ): kotlin.Boolean = map.remove(contactId) != null

  override fun deleteByIds(
    contactIds: List<ContactsId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in contactIds) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: ContactsRow,
    c: Connection
  ): ContactsId {
    if (map.containsKey(unsaved.contactId)) {
      throw RuntimeException("id " + unsaved.contactId + " already exists")
    }
    map[unsaved.contactId] = unsaved
    return unsaved.contactId
  }

  override fun insert(
    unsaved: ContactsRowUnsaved,
    c: Connection
  ): ContactsId = insert(toRow(unsaved), c)

  override fun select(): SelectBuilder<ContactsFields, ContactsRow> = SelectBuilderMock(ContactsFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: ConnectionRead): List<ContactsRow> = map.values.toList()

  override fun selectById(
    contactId: ContactsId,
    c: ConnectionRead
  ): ContactsRow? = map[contactId]

  override fun selectByIds(
    contactIds: List<ContactsId>,
    c: ConnectionRead
  ): List<ContactsRow> {
    val result = ArrayList<ContactsRow>()
    for (id in contactIds) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    contactIds: List<ContactsId>,
    c: ConnectionRead
  ): Map<ContactsId, ContactsRow> = selectByIds(contactIds, c).associateBy({ row: ContactsRow -> row.contactId })

  override fun update(): UpdateBuilder<ContactsFields, ContactsRow> = UpdateBuilderMock(ContactsFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: ContactsRow,
    c: Connection
  ): kotlin.Boolean {
    val shouldUpdate = map[row.contactId]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.contactId] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: ContactsRow,
    c: Connection
  ) {
    map[unsaved.contactId] = unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<ContactsRow>,
    c: Connection
  ) {
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.contactId] = row
    }
  }
}