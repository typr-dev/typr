/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package oracledb.precisetypes

import com.fasterxml.jackson.annotation.JsonValue
import dev.typr.foundations.OracleType
import dev.typr.foundations.OracleTypes
import dev.typr.foundations.data.precise.DecimalN
import dev.typr.foundations.dsl.Bijection
import java.lang.IllegalArgumentException
import java.math.BigInteger
import java.util.Optional

case class Int10 private(@JsonValue value: BigInteger) extends DecimalN {
  override def decimalValue: java.math.BigDecimal = new java.math.BigDecimal(value)

  override def precision: scala.Int = 10

  override def scale: scala.Int = 0

  override def semanticEquals(other: DecimalN): scala.Boolean = (if (other == null) false else decimalValue().compareTo(other.decimalValue()) == 0)

  override def semanticHashCode: scala.Int = decimalValue().stripTrailingZeros().hashCode()

  override def equals(that: Any): scala.Boolean = (this eq that.asInstanceOf[AnyRef]) || (that match { case other: DecimalN => decimalValue().compareTo(other.decimalValue()) == 0; case _ => false })

  override def hashCode: scala.Int = decimalValue().stripTrailingZeros().hashCode()
}

object Int10 {
  given Zero: Int10 = new Int10(BigInteger.ZERO)

  given bijection: Bijection[Int10, BigInteger] = Bijection.apply[Int10, BigInteger](_.value)(Int10.apply)

  def of(value: BigInteger): Optional[Int10] = (if (value.bitLength <= 40) Optional.of(new Int10(value)) else Optional.empty())

  def of(value: Integer): Int10 = new Int10(BigInteger.valueOf(value.toLong))

  def of(value: java.lang.Long): Optional[Int10] = Int10.of(BigInteger.valueOf(value))

  given oracleType: OracleType[Int10] = OracleTypes.number.bimap(bd => new Int10(bd.toBigIntegerExact()), v => new java.math.BigDecimal(v.value))

  def unsafeForce(value: BigInteger): Int10 = {
    if (value.bitLength > 40) {
      throw new IllegalArgumentException("Value exceeds precision(10, 0)")
    }
    return new Int10(value)
  }
}