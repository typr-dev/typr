/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package oracledb.contacts;

import dev.typr.dsl.DeleteBuilder;
import dev.typr.dsl.Dialect;
import dev.typr.dsl.SelectBuilder;
import dev.typr.dsl.UpdateBuilder;
import dev.typr.foundations.Connection;
import dev.typr.foundations.ConnectionRead;
import dev.typr.foundations.Fragment;
import dev.typr.foundations.OracleTypes;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import oracledb.EmailTableT;
import oracledb.TagVarrayT;
import static dev.typr.foundations.Fragment.concat;

public class ContactsRepoImpl implements ContactsRepo {
  @Override
  public DeleteBuilder<ContactsFields, ContactsRow> delete() {
    return DeleteBuilder.of("\"CONTACTS\"", ContactsFields.structure, Dialect.ORACLE);
  }

  @Override
  public Boolean deleteById(
    ContactsId contactId,
    Connection c
  ) {
    return concat(Fragment.of("delete from \"CONTACTS\" where \"CONTACT_ID\" = "), Fragment.encode(ContactsId.oracleType, contactId), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public Integer deleteByIds(
    List<ContactsId> contactIds,
    Connection c
  ) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : contactIds) { fragments.add(Fragment.encode(ContactsId.oracleType, id)); };
    return Fragment.concat(Fragment.of("delete from \"CONTACTS\" where \"CONTACT_ID\" in ("), Fragment.comma(fragments), Fragment.of(")")).update().run(c);
  }

  @Override
  public ContactsId insert(
    ContactsRow unsaved,
    Connection c
  ) {
    return concat(Fragment.of("insert into \"CONTACTS\"(\"CONTACT_ID\", \"NAME\", \"EMAILS\", \"TAGS\")\nvalues ("), Fragment.encode(ContactsId.oracleType, unsaved.contactId()), Fragment.of(", "), Fragment.encode(OracleTypes.varchar2, unsaved.name()), Fragment.of(", "), Fragment.encode(EmailTableT.oracleType.opt(), unsaved.emails()), Fragment.of(", "), Fragment.encode(TagVarrayT.oracleType.opt(), unsaved.tags()), Fragment.of(")\n"))
      .updateReturningGeneratedKeys(new String[]{"CONTACT_ID"}, ContactsId.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public ContactsId insert(
    ContactsRowUnsaved unsaved,
    Connection c
  ) {
    ArrayList<Fragment> columns = new ArrayList<>();;
    ArrayList<Fragment> values = new ArrayList<>();;
    columns.add(Fragment.of("\"NAME\""));
    values.add(concat(Fragment.encode(OracleTypes.varchar2, unsaved.name()), Fragment.of("")));
    columns.add(Fragment.of("\"EMAILS\""));
    values.add(concat(Fragment.encode(EmailTableT.oracleType.opt(), unsaved.emails()), Fragment.of("")));
    columns.add(Fragment.of("\"TAGS\""));
    values.add(concat(Fragment.encode(TagVarrayT.oracleType.opt(), unsaved.tags()), Fragment.of("")));
    unsaved.contactId().visit(
      () -> {
  
      },
      value -> {
        columns.add(Fragment.of("\"CONTACT_ID\""));
        values.add(concat(Fragment.encode(ContactsId.oracleType, value), Fragment.of("")));
      }
    );;
    Fragment q = concat(Fragment.of("insert into \"CONTACTS\"("), Fragment.comma(columns), Fragment.of(")\nvalues ("), Fragment.comma(values), Fragment.of(")\n"));;
    return q.updateReturningGeneratedKeys(new String[]{"CONTACT_ID"}, ContactsId.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public SelectBuilder<ContactsFields, ContactsRow> select() {
    return SelectBuilder.of("\"CONTACTS\"", ContactsFields.structure, ContactsRow.rowCodec, Dialect.ORACLE);
  }

  @Override
  public List<ContactsRow> selectAll(ConnectionRead c) {
    return concat(Fragment.of("select \"CONTACT_ID\", \"NAME\", \"EMAILS\", \"TAGS\"\nfrom \"CONTACTS\"\n")).query(ContactsRow.rowCodec.all()).run(c);
  }

  @Override
  public Optional<ContactsRow> selectById(
    ContactsId contactId,
    ConnectionRead c
  ) {
    return concat(Fragment.of("select \"CONTACT_ID\", \"NAME\", \"EMAILS\", \"TAGS\"\nfrom \"CONTACTS\"\nwhere \"CONTACT_ID\" = "), Fragment.encode(ContactsId.oracleType, contactId), Fragment.of("")).query(ContactsRow.rowCodec.first()).run(c);
  }

  @Override
  public List<ContactsRow> selectByIds(
    List<ContactsId> contactIds,
    ConnectionRead c
  ) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : contactIds) { fragments.add(Fragment.encode(ContactsId.oracleType, id)); };
    return Fragment.concat(Fragment.of("select \"CONTACT_ID\", \"NAME\", \"EMAILS\", \"TAGS\" from \"CONTACTS\" where \"CONTACT_ID\" in ("), Fragment.comma(fragments), Fragment.of(")")).query(ContactsRow.rowCodec.all()).run(c);
  }

  @Override
  public Map<ContactsId, ContactsRow> selectByIdsTracked(
    List<ContactsId> contactIds,
    ConnectionRead c
  ) {
    HashMap<ContactsId, ContactsRow> ret = new HashMap<ContactsId, ContactsRow>();
    selectByIds(contactIds, c).forEach(row -> ret.put(row.contactId(), row));
    return ret;
  }

  @Override
  public UpdateBuilder<ContactsFields, ContactsRow> update() {
    return UpdateBuilder.of("\"CONTACTS\"", ContactsFields.structure, ContactsRow.rowCodec, Dialect.ORACLE);
  }

  @Override
  public Boolean update(
    ContactsRow row,
    Connection c
  ) {
    ContactsId contactId = row.contactId();;
    return concat(Fragment.of("update \"CONTACTS\"\nset \"NAME\" = "), Fragment.encode(OracleTypes.varchar2, row.name()), Fragment.of(",\n\"EMAILS\" = "), Fragment.encode(EmailTableT.oracleType.opt(), row.emails()), Fragment.of(",\n\"TAGS\" = "), Fragment.encode(TagVarrayT.oracleType.opt(), row.tags()), Fragment.of("\nwhere \"CONTACT_ID\" = "), Fragment.encode(ContactsId.oracleType, contactId), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public void upsert(
    ContactsRow unsaved,
    Connection c
  ) {
    concat(Fragment.of("MERGE INTO \"CONTACTS\" t\nUSING (SELECT "), Fragment.encode(ContactsId.oracleType, unsaved.contactId()), Fragment.of(", "), Fragment.encode(OracleTypes.varchar2, unsaved.name()), Fragment.of(", "), Fragment.encode(EmailTableT.oracleType.opt(), unsaved.emails()), Fragment.of(", "), Fragment.encode(TagVarrayT.oracleType.opt(), unsaved.tags()), Fragment.of(" FROM DUAL) s\nON (t.\"CONTACT_ID\" = s.\"CONTACT_ID\")\nWHEN MATCHED THEN UPDATE SET t.\"NAME\" = s.\"NAME\",\nt.\"EMAILS\" = s.\"EMAILS\",\nt.\"TAGS\" = s.\"TAGS\"\nWHEN NOT MATCHED THEN INSERT (\"CONTACT_ID\", \"NAME\", \"EMAILS\", \"TAGS\") VALUES ("), Fragment.encode(ContactsId.oracleType, unsaved.contactId()), Fragment.of(", "), Fragment.encode(OracleTypes.varchar2, unsaved.name()), Fragment.of(", "), Fragment.encode(EmailTableT.oracleType.opt(), unsaved.emails()), Fragment.of(", "), Fragment.encode(TagVarrayT.oracleType.opt(), unsaved.tags()), Fragment.of(")"))
      .update()
      .run(c);
  }

  @Override
  public void upsertBatch(
    Iterator<ContactsRow> unsaved,
    Connection c
  ) {
    concat(Fragment.of("MERGE INTO \"CONTACTS\" t\nUSING (SELECT ?, ?, ?, ? FROM DUAL) s\nON (t.\"CONTACT_ID\" = s.\"CONTACT_ID\")\nWHEN MATCHED THEN UPDATE SET t.\"NAME\" = s.\"NAME\",\nt.\"EMAILS\" = s.\"EMAILS\",\nt.\"TAGS\" = s.\"TAGS\"\nWHEN NOT MATCHED THEN INSERT (\"CONTACT_ID\", \"NAME\", \"EMAILS\", \"TAGS\") VALUES (?, ?, ?, ?)"))
      .updateMany(ContactsRow.rowCodec, unsaved)
      .run(c);
  }
}