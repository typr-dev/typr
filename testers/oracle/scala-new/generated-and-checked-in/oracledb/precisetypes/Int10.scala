/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package oracledb.precisetypes

import com.fasterxml.jackson.annotation.JsonValue
import dev.typr.dslsc.Bijection
import dev.typr.foundations.data.precise.DecimalN
import dev.typr.foundationssc.OracleType
import dev.typr.foundationssc.OracleTypes
import java.lang.IllegalArgumentException
import scala.math.BigInt

case class Int10 private(@JsonValue value: BigInt) extends DecimalN {
  override def decimalValue: java.math.BigDecimal = new java.math.BigDecimal(value.bigInteger)

  override def precision: Int = 10

  override def scale: Int = 0

  override def semanticEquals(other: DecimalN): Boolean = (if (other == null) false else decimalValue().compareTo(other.decimalValue()) == 0)

  override def semanticHashCode: Int = decimalValue().stripTrailingZeros().hashCode()

  override def equals(that: Any): Boolean = (this eq that.asInstanceOf[AnyRef]) || (that match { case other: DecimalN => decimalValue().compareTo(other.decimalValue()) == 0; case _ => false })

  override def hashCode: Int = decimalValue().stripTrailingZeros().hashCode()
}

object Int10 {
  given Zero: Int10 = new Int10(BigInt(0))

  given bijection: Bijection[Int10, BigInt] = Bijection.of[Int10, BigInt](_.value, Int10.apply)

  def of(value: BigInt): Option[Int10] = (if (value.bitLength <= 40) Some(new Int10(value)) else None)

  def of(value: Int): Int10 = new Int10(BigInt(value.toLong))

  def of(value: Long): Option[Int10] = Int10.of(BigInt(value))

  given oracleType: OracleType[Int10] = OracleTypes.number.to(Bijection.of(bd => new Int10(BigInt(bd.bigDecimal.toBigIntegerExact())), v => BigDecimal(new java.math.BigDecimal(v.value.bigInteger))))

  def unsafeForce(value: BigInt): Int10 = {
    if (value.bitLength > 40) {
      throw new IllegalArgumentException("Value exceeds precision(10, 0)")
    }
    return new Int10(value)
  }
}