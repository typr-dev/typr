/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.precisetypes

import com.fasterxml.jackson.annotation.JsonValue
import dev.typr.foundations.DuckDbType
import dev.typr.foundations.DuckDbTypes
import dev.typr.foundations.data.precise.DecimalN
import dev.typr.foundations.scala.Bijection
import java.lang.IllegalArgumentException
import scala.math.BigInt

case class Int5 private(@JsonValue value: BigInt) extends DecimalN {
  override def decimalValue: java.math.BigDecimal = new java.math.BigDecimal(value.bigInteger)

  override def precision: Int = 5

  override def scale: Int = 0

  override def semanticEquals(other: DecimalN): Boolean = (if (other == null) false else decimalValue().compareTo(other.decimalValue()) == 0)

  override def semanticHashCode: Int = decimalValue().stripTrailingZeros().hashCode()

  override def equals(that: Any): Boolean = (this eq that.asInstanceOf[AnyRef]) || (that match { case other: DecimalN => decimalValue().compareTo(other.decimalValue()) == 0; case _ => false })

  override def hashCode: Int = decimalValue().stripTrailingZeros().hashCode()
}

object Int5 {
  given Zero: Int5 = new Int5(BigInt(0))

  given bijection: Bijection[Int5, BigInt] = Bijection.apply[Int5, BigInt](_.value)(Int5.apply)

  given dbTypeArray: DuckDbType[Array[Int5]] = DuckDbTypes.decimalArray.bimap(xs => xs.map(bd => new Int5(BigInt(bd.toBigIntegerExact()))), xs => xs.map(v => new java.math.BigDecimal(v.value.bigInteger)))

  given duckDbType: DuckDbType[Int5] = DuckDbTypes.numeric.bimap(bd => new Int5(BigInt(bd.toBigIntegerExact())), v => new java.math.BigDecimal(v.value.bigInteger))

  def of(value: BigInt): Option[Int5] = (if (value.bitLength <= 20) Some(new Int5(value)) else None)

  def of(value: Int): Int5 = new Int5(BigInt(value))

  def of(value: Long): Option[Int5] = Int5.of(BigInt(value))

  def unsafeForce(value: BigInt): Int5 = {
    if (value.bitLength > 20) {
      throw new IllegalArgumentException("Value exceeds precision(5, 0)")
    }
    return new Int5(value)
  }
}