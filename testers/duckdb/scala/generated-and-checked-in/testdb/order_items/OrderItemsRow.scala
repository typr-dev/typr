/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.order_items

import com.fasterxml.jackson.annotation.JsonProperty
import dev.typr.dslsc.RowCodecs
import dev.typr.foundations.Tuple.Tuple4
import dev.typr.foundationssc.DuckDbTypes
import dev.typr.foundationssc.RowCodec
import testdb.customtypes.Defaulted

/** Table: order_items
 * Composite primary key: order_id, product_id
 */
case class OrderItemsRow(
  @JsonProperty("order_id") orderId: Int,
  @JsonProperty("product_id") productId: Int,
  /** Default: 1 */
  quantity: Int,
  @JsonProperty("unit_price") unitPrice: BigDecimal
) extends Tuple4[Int, Int, Int, BigDecimal] {
  def compositeId: OrderItemsId = new OrderItemsId(orderId, productId)

  def id: OrderItemsId = this.compositeId

  def toUnsavedRow(quantity: Defaulted[Int] = Defaulted.Provided(this.quantity)): OrderItemsRowUnsaved = {
    new OrderItemsRowUnsaved(
      orderId,
      productId,
      unitPrice,
      quantity
    )
  }

  override def `_1`: Int = orderId

  override def `_2`: Int = productId

  override def `_3`: Int = quantity

  override def `_4`: BigDecimal = unitPrice
}

object OrderItemsRow {
  def apply(
    compositeId: OrderItemsId,
    quantity: Int,
    unitPrice: BigDecimal
  ): OrderItemsRow = {
    new OrderItemsRow(
      compositeId.orderId,
      compositeId.productId,
      quantity,
      unitPrice
    )
  }

  val rowCodec: RowCodec[OrderItemsRow] = RowCodecs.of(DuckDbTypes.integer, DuckDbTypes.integer, DuckDbTypes.integer, DuckDbTypes.numeric)(OrderItemsRow.apply)(row => Array[Any](row.orderId, row.productId, row.quantity, row.unitPrice))
}