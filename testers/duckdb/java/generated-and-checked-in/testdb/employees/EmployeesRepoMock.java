/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.employees;

import dev.typr.dsl.DeleteBuilder;
import dev.typr.dsl.DeleteBuilderMock;
import dev.typr.dsl.DeleteParams;
import dev.typr.dsl.SelectBuilder;
import dev.typr.dsl.SelectBuilderMock;
import dev.typr.dsl.SelectParams;
import dev.typr.dsl.UpdateBuilder;
import dev.typr.dsl.UpdateBuilderMock;
import dev.typr.dsl.UpdateParams;
import dev.typr.foundations.Connection;
import dev.typr.foundations.ConnectionRead;
import java.lang.RuntimeException;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;

public record EmployeesRepoMock(
  java.util.function.Function<EmployeesRowUnsaved, EmployeesRow> toRow,
  HashMap<EmployeesId, EmployeesRow> map
) implements EmployeesRepo {
  public EmployeesRepoMock(java.util.function.Function<EmployeesRowUnsaved, EmployeesRow> toRow) {
    this(toRow, new HashMap<EmployeesId, EmployeesRow>());
  }

  public EmployeesRepoMock withToRow(java.util.function.Function<EmployeesRowUnsaved, EmployeesRow> toRow) {
    return new EmployeesRepoMock(toRow, map);
  }

  public EmployeesRepoMock withMap(HashMap<EmployeesId, EmployeesRow> map) {
    return new EmployeesRepoMock(toRow, map);
  }

  @Override
  public DeleteBuilder<EmployeesFields, EmployeesRow> delete() {
    return new DeleteBuilderMock<>(EmployeesFields.structure, () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.compositeId(), id -> map.remove(id));
  }

  @Override
  public Boolean deleteById(
    EmployeesId compositeId,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(compositeId)).isPresent();
  }

  @Override
  public Integer deleteByIds(
    List<EmployeesId> compositeIds,
    Connection c
  ) {
    var count = 0;
    for (var id : compositeIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    }
    };
    return count;
  }

  @Override
  public EmployeesRow insert(
    EmployeesRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.compositeId())) {
      throw new RuntimeException("id " + unsaved.compositeId() + " already exists");
    }
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  }

  @Override
  public EmployeesRow insert(
    EmployeesRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  }

  @Override
  public SelectBuilder<EmployeesFields, EmployeesRow> select() {
    return new SelectBuilderMock<>(EmployeesFields.structure, () -> new ArrayList<>(map.values()), SelectParams.empty());
  }

  @Override
  public List<EmployeesRow> selectAll(ConnectionRead c) {
    return new ArrayList<>(map.values());
  }

  @Override
  public Optional<EmployeesRow> selectById(
    EmployeesId compositeId,
    ConnectionRead c
  ) {
    return Optional.ofNullable(map.get(compositeId));
  }

  @Override
  public List<EmployeesRow> selectByIds(
    List<EmployeesId> compositeIds,
    ConnectionRead c
  ) {
    var result = new ArrayList<EmployeesRow>();
    for (var id : compositeIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    }
    };
    return result;
  }

  @Override
  public Map<EmployeesId, EmployeesRow> selectByIdsTracked(
    List<EmployeesId> compositeIds,
    ConnectionRead c
  ) {
    return selectByIds(compositeIds, c).stream().collect(Collectors.toMap((EmployeesRow row) -> row.compositeId(), Function.identity()));
  }

  @Override
  public UpdateBuilder<EmployeesFields, EmployeesRow> update() {
    return new UpdateBuilderMock<>(EmployeesFields.structure, () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  }

  @Override
  public Boolean update(
    EmployeesRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.compositeId())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.compositeId(), row);;
    }
    return shouldUpdate;
  }

  @Override
  public EmployeesRow upsert(
    EmployeesRow unsaved,
    Connection c
  ) {
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  }

  @Override
  public List<EmployeesRow> upsertBatch(
    Iterator<EmployeesRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<EmployeesRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      result.add(row);
    };
    return result;
  }
}