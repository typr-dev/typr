/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.brands

import dev.typr.dslkt.DeleteBuilder
import dev.typr.dslkt.DeleteBuilderMock
import dev.typr.dslkt.DeleteParams
import dev.typr.dslkt.SelectBuilder
import dev.typr.dslkt.SelectBuilderMock
import dev.typr.dslkt.SelectParams
import dev.typr.dslkt.UpdateBuilder
import dev.typr.dslkt.UpdateBuilderMock
import dev.typr.dslkt.UpdateParams
import dev.typr.foundationskt.Connection
import dev.typr.foundationskt.ConnectionRead
import java.lang.RuntimeException
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap

data class BrandsRepoMock(
  val toRow: (BrandsRowUnsaved) -> BrandsRow,
  val map: MutableMap<BrandsId, BrandsRow> = mutableMapOf<BrandsId, BrandsRow>()
) : BrandsRepo {
  override fun delete(): DeleteBuilder<BrandsFields, BrandsRow> = DeleteBuilderMock(BrandsFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.brandId }, { id -> map.remove(id) })

  override fun deleteById(
    brandId: BrandsId,
    c: Connection
  ): kotlin.Boolean = map.remove(brandId) != null

  override fun deleteByIds(
    brandIds: List<BrandsId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in brandIds) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: BrandsRow,
    c: Connection
  ): BrandsRow {
    if (map.containsKey(unsaved.brandId)) {
      throw RuntimeException("id " + unsaved.brandId + " already exists")
    }
    map[unsaved.brandId] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: BrandsRowUnsaved,
    c: Connection
  ): BrandsRow = insert(toRow(unsaved), c)

  override fun select(): SelectBuilder<BrandsFields, BrandsRow> = SelectBuilderMock(BrandsFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: ConnectionRead): List<BrandsRow> = map.values.toList()

  override fun selectById(
    brandId: BrandsId,
    c: ConnectionRead
  ): BrandsRow? = map[brandId]

  override fun selectByIds(
    brandIds: List<BrandsId>,
    c: ConnectionRead
  ): List<BrandsRow> {
    val result = ArrayList<BrandsRow>()
    for (id in brandIds) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    brandIds: List<BrandsId>,
    c: ConnectionRead
  ): Map<BrandsId, BrandsRow> = selectByIds(brandIds, c).associateBy({ row: BrandsRow -> row.brandId })

  override fun selectByUniqueSlug(
    slug: kotlin.String,
    c: ConnectionRead
  ): BrandsRow? = map.values.toList().find({ v -> (slug == v.slug) })

  override fun update(): UpdateBuilder<BrandsFields, BrandsRow> = UpdateBuilderMock(BrandsFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: BrandsRow,
    c: Connection
  ): kotlin.Boolean {
    val shouldUpdate = map[row.brandId]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.brandId] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: BrandsRow,
    c: Connection
  ): BrandsRow {
    map[unsaved.brandId] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<BrandsRow>,
    c: Connection
  ): List<BrandsRow> {
    val result = ArrayList<BrandsRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.brandId] = row
      result.add(row)
    }
    return result
  }
}