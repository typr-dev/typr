/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.price_tiers

import dev.typr.foundations.kotlin.DeleteBuilder
import dev.typr.foundations.kotlin.DeleteBuilderMock
import dev.typr.foundations.kotlin.DeleteParams
import dev.typr.foundations.kotlin.SelectBuilder
import dev.typr.foundations.kotlin.SelectBuilderMock
import dev.typr.foundations.kotlin.SelectParams
import dev.typr.foundations.kotlin.UpdateBuilder
import dev.typr.foundations.kotlin.UpdateBuilderMock
import dev.typr.foundations.kotlin.UpdateParams
import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap

data class PriceTiersRepoMock(
  val toRow: (PriceTiersRowUnsaved) -> PriceTiersRow,
  val map: MutableMap<PriceTiersId, PriceTiersRow> = mutableMapOf<PriceTiersId, PriceTiersRow>()
) : PriceTiersRepo {
  override fun delete(): DeleteBuilder<PriceTiersFields, PriceTiersRow> = DeleteBuilderMock(PriceTiersFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.tierId }, { id -> map.remove(id) })

  override fun deleteById(
    tierId: PriceTiersId,
    c: Connection
  ): Boolean = map.remove(tierId) != null

  override fun deleteByIds(
    tierIds: Array<PriceTiersId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in tierIds) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: PriceTiersRow,
    c: Connection
  ): PriceTiersRow {
    if (map.containsKey(unsaved.tierId)) {
      throw RuntimeException("id " + unsaved.tierId + " already exists")
    }
    map[unsaved.tierId] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: PriceTiersRowUnsaved,
    c: Connection
  ): PriceTiersRow = insert(toRow(unsaved), c)

  override fun select(): SelectBuilder<PriceTiersFields, PriceTiersRow> = SelectBuilderMock(PriceTiersFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<PriceTiersRow> = map.values.toList()

  override fun selectById(
    tierId: PriceTiersId,
    c: Connection
  ): PriceTiersRow? = map[tierId]

  override fun selectByIds(
    tierIds: Array<PriceTiersId>,
    c: Connection
  ): List<PriceTiersRow> {
    val result = ArrayList<PriceTiersRow>()
    for (id in tierIds) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    tierIds: Array<PriceTiersId>,
    c: Connection
  ): Map<PriceTiersId, PriceTiersRow> = selectByIds(tierIds, c).associateBy({ row: PriceTiersRow -> row.tierId })

  override fun update(): UpdateBuilder<PriceTiersFields, PriceTiersRow> = UpdateBuilderMock(PriceTiersFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: PriceTiersRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.tierId]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.tierId] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: PriceTiersRow,
    c: Connection
  ): PriceTiersRow {
    map[unsaved.tierId] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<PriceTiersRow>,
    c: Connection
  ): List<PriceTiersRow> {
    val result = ArrayList<PriceTiersRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.tierId] = row
      result.add(row)
    }
    return result
  }
}