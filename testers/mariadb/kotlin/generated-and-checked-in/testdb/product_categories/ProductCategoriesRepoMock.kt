/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.product_categories

import dev.typr.foundations.kotlin.DeleteBuilder
import dev.typr.foundations.kotlin.DeleteBuilderMock
import dev.typr.foundations.kotlin.DeleteParams
import dev.typr.foundations.kotlin.SelectBuilder
import dev.typr.foundations.kotlin.SelectBuilderMock
import dev.typr.foundations.kotlin.SelectParams
import dev.typr.foundations.kotlin.UpdateBuilder
import dev.typr.foundations.kotlin.UpdateBuilderMock
import dev.typr.foundations.kotlin.UpdateParams
import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap

data class ProductCategoriesRepoMock(
  val toRow: (ProductCategoriesRowUnsaved) -> ProductCategoriesRow,
  val map: MutableMap<ProductCategoriesId, ProductCategoriesRow> = mutableMapOf<ProductCategoriesId, ProductCategoriesRow>()
) : ProductCategoriesRepo {
  override fun delete(): DeleteBuilder<ProductCategoriesFields, ProductCategoriesRow> = DeleteBuilderMock(ProductCategoriesFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.compositeId() }, { id -> map.remove(id) })

  override fun deleteById(
    compositeId: ProductCategoriesId,
    c: Connection
  ): Boolean = map.remove(compositeId) != null

  override fun deleteByIds(
    compositeIds: Array<ProductCategoriesId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in compositeIds) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: ProductCategoriesRow,
    c: Connection
  ): ProductCategoriesRow {
    if (map.containsKey(unsaved.compositeId())) {
      throw RuntimeException("id " + unsaved.compositeId() + " already exists")
    }
    map[unsaved.compositeId()] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: ProductCategoriesRowUnsaved,
    c: Connection
  ): ProductCategoriesRow = insert(toRow(unsaved), c)

  override fun select(): SelectBuilder<ProductCategoriesFields, ProductCategoriesRow> = SelectBuilderMock(ProductCategoriesFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<ProductCategoriesRow> = map.values.toList()

  override fun selectById(
    compositeId: ProductCategoriesId,
    c: Connection
  ): ProductCategoriesRow? = map[compositeId]

  override fun selectByIds(
    compositeIds: Array<ProductCategoriesId>,
    c: Connection
  ): List<ProductCategoriesRow> {
    val result = ArrayList<ProductCategoriesRow>()
    for (id in compositeIds) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    compositeIds: Array<ProductCategoriesId>,
    c: Connection
  ): Map<ProductCategoriesId, ProductCategoriesRow> = selectByIds(compositeIds, c).associateBy({ row: ProductCategoriesRow -> row.compositeId() })

  override fun update(): UpdateBuilder<ProductCategoriesFields, ProductCategoriesRow> = UpdateBuilderMock(ProductCategoriesFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: ProductCategoriesRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.compositeId()]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.compositeId()] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: ProductCategoriesRow,
    c: Connection
  ): ProductCategoriesRow {
    map[unsaved.compositeId()] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<ProductCategoriesRow>,
    c: Connection
  ): List<ProductCategoriesRow> {
    val result = ArrayList<ProductCategoriesRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      result.add(row)
    }
    return result
  }
}