/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.mariatest

import dev.typr.foundations.kotlin.DeleteBuilder
import dev.typr.foundations.kotlin.DeleteBuilderMock
import dev.typr.foundations.kotlin.DeleteParams
import dev.typr.foundations.kotlin.SelectBuilder
import dev.typr.foundations.kotlin.SelectBuilderMock
import dev.typr.foundations.kotlin.SelectParams
import dev.typr.foundations.kotlin.UpdateBuilder
import dev.typr.foundations.kotlin.UpdateBuilderMock
import dev.typr.foundations.kotlin.UpdateParams
import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap

data class MariatestRepoMock(
  val toRow: (MariatestRowUnsaved) -> MariatestRow,
  val map: MutableMap<MariatestId, MariatestRow> = mutableMapOf<MariatestId, MariatestRow>()
) : MariatestRepo {
  override fun delete(): DeleteBuilder<MariatestFields, MariatestRow> = DeleteBuilderMock(MariatestFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.intCol }, { id -> map.remove(id) })

  override fun deleteById(
    intCol: MariatestId,
    c: Connection
  ): Boolean = map.remove(intCol) != null

  override fun deleteByIds(
    intCols: Array<MariatestId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in intCols) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: MariatestRow,
    c: Connection
  ): MariatestRow {
    if (map.containsKey(unsaved.intCol)) {
      throw RuntimeException("id " + unsaved.intCol + " already exists")
    }
    map[unsaved.intCol] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: MariatestRowUnsaved,
    c: Connection
  ): MariatestRow = insert(toRow(unsaved), c)

  override fun select(): SelectBuilder<MariatestFields, MariatestRow> = SelectBuilderMock(MariatestFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<MariatestRow> = map.values.toList()

  override fun selectById(
    intCol: MariatestId,
    c: Connection
  ): MariatestRow? = map[intCol]

  override fun selectByIds(
    intCols: Array<MariatestId>,
    c: Connection
  ): List<MariatestRow> {
    val result = ArrayList<MariatestRow>()
    for (id in intCols) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    intCols: Array<MariatestId>,
    c: Connection
  ): Map<MariatestId, MariatestRow> = selectByIds(intCols, c).associateBy({ row: MariatestRow -> row.intCol })

  override fun update(): UpdateBuilder<MariatestFields, MariatestRow> = UpdateBuilderMock(MariatestFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: MariatestRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.intCol]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.intCol] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: MariatestRow,
    c: Connection
  ): MariatestRow {
    map[unsaved.intCol] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<MariatestRow>,
    c: Connection
  ): List<MariatestRow> {
    val result = ArrayList<MariatestRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.intCol] = row
      result.add(row)
    }
    return result
  }
}