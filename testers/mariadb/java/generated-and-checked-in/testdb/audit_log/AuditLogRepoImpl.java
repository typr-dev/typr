/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.audit_log;

import dev.typr.dsl.DeleteBuilder;
import dev.typr.dsl.Dialect;
import dev.typr.dsl.SelectBuilder;
import dev.typr.dsl.UpdateBuilder;
import dev.typr.foundations.Connection;
import dev.typr.foundations.ConnectionRead;
import dev.typr.foundations.Fragment;
import dev.typr.foundations.MariaTypes;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import static dev.typr.foundations.Fragment.concat;

public class AuditLogRepoImpl implements AuditLogRepo {
  @Override
  public DeleteBuilder<AuditLogFields, AuditLogRow> delete() {
    return DeleteBuilder.of("`audit_log`", AuditLogFields.structure, Dialect.MARIADB);
  }

  @Override
  public Boolean deleteById(
    AuditLogId logId,
    Connection c
  ) {
    return concat(Fragment.of("delete from `audit_log` where `log_id` = "), Fragment.encode(AuditLogId.mariaType, logId), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public Integer deleteByIds(
    List<AuditLogId> logIds,
    Connection c
  ) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : logIds) { fragments.add(Fragment.encode(AuditLogId.mariaType, id)); };
    return Fragment.concat(Fragment.of("delete from `audit_log` where `log_id` in ("), Fragment.comma(fragments), Fragment.of(")")).update().run(c);
  }

  @Override
  public AuditLogRow insert(
    AuditLogRow unsaved,
    Connection c
  ) {
    return concat(Fragment.of("insert into `audit_log`(`table_name`, `record_id`, `action`, `old_values`, `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`)\nvalues ("), Fragment.encode(MariaTypes.varchar, unsaved.tableName()), Fragment.of(", "), Fragment.encode(MariaTypes.varchar, unsaved.recordId()), Fragment.of(", "), Fragment.encode(MariaTypes.text, unsaved.action()), Fragment.of(", "), Fragment.encode(MariaTypes.json.opt(), unsaved.oldValues()), Fragment.of(", "), Fragment.encode(MariaTypes.json.opt(), unsaved.newValues()), Fragment.of(", "), Fragment.encode(MariaTypes.varchar.opt(), unsaved.changedBy()), Fragment.of(", "), Fragment.encode(MariaTypes.datetime, unsaved.changedAt()), Fragment.of(", "), Fragment.encode(MariaTypes.inet6.opt(), unsaved.clientIp()), Fragment.of(", "), Fragment.encode(MariaTypes.varbinary.opt(), unsaved.sessionId()), Fragment.of(")\nRETURNING `log_id`, `table_name`, `record_id`, `action`, `old_values`, `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`\n"))
      .updateReturning(AuditLogRow.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public AuditLogRow insert(
    AuditLogRowUnsaved unsaved,
    Connection c
  ) {
    ArrayList<Fragment> columns = new ArrayList<>();;
    ArrayList<Fragment> values = new ArrayList<>();;
    columns.add(Fragment.of("`table_name`"));
    values.add(concat(Fragment.encode(MariaTypes.varchar, unsaved.tableName()), Fragment.of("")));
    columns.add(Fragment.of("`record_id`"));
    values.add(concat(Fragment.encode(MariaTypes.varchar, unsaved.recordId()), Fragment.of("")));
    columns.add(Fragment.of("`action`"));
    values.add(concat(Fragment.encode(MariaTypes.text, unsaved.action()), Fragment.of("")));
    unsaved.oldValues().visit(
      () -> {
  
      },
      value -> {
        columns.add(Fragment.of("`old_values`"));
        values.add(concat(Fragment.encode(MariaTypes.json.opt(), value), Fragment.of("")));
      }
    );;
    unsaved.newValues().visit(
      () -> {
  
      },
      value -> {
        columns.add(Fragment.of("`new_values`"));
        values.add(concat(Fragment.encode(MariaTypes.json.opt(), value), Fragment.of("")));
      }
    );;
    unsaved.changedBy().visit(
      () -> {
  
      },
      value -> {
        columns.add(Fragment.of("`changed_by`"));
        values.add(concat(Fragment.encode(MariaTypes.varchar.opt(), value), Fragment.of("")));
      }
    );;
    unsaved.changedAt().visit(
      () -> {
  
      },
      value -> {
        columns.add(Fragment.of("`changed_at`"));
        values.add(concat(Fragment.encode(MariaTypes.datetime, value), Fragment.of("")));
      }
    );;
    unsaved.clientIp().visit(
      () -> {
  
      },
      value -> {
        columns.add(Fragment.of("`client_ip`"));
        values.add(concat(Fragment.encode(MariaTypes.inet6.opt(), value), Fragment.of("")));
      }
    );;
    unsaved.sessionId().visit(
      () -> {
  
      },
      value -> {
        columns.add(Fragment.of("`session_id`"));
        values.add(concat(Fragment.encode(MariaTypes.varbinary.opt(), value), Fragment.of("")));
      }
    );;
    Fragment q = concat(Fragment.of("insert into `audit_log`("), Fragment.comma(columns), Fragment.of(")\nvalues ("), Fragment.comma(values), Fragment.of(")\nRETURNING `log_id`, `table_name`, `record_id`, `action`, `old_values`, `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`\n"));;
    return q.updateReturning(AuditLogRow.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public SelectBuilder<AuditLogFields, AuditLogRow> select() {
    return SelectBuilder.of("`audit_log`", AuditLogFields.structure, AuditLogRow.rowCodec, Dialect.MARIADB);
  }

  @Override
  public List<AuditLogRow> selectAll(ConnectionRead c) {
    return concat(Fragment.of("select `log_id`, `table_name`, `record_id`, `action`, `old_values`, `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`\nfrom `audit_log`\n")).query(AuditLogRow.rowCodec.all()).run(c);
  }

  @Override
  public Optional<AuditLogRow> selectById(
    AuditLogId logId,
    ConnectionRead c
  ) {
    return concat(Fragment.of("select `log_id`, `table_name`, `record_id`, `action`, `old_values`, `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`\nfrom `audit_log`\nwhere `log_id` = "), Fragment.encode(AuditLogId.mariaType, logId), Fragment.of("")).query(AuditLogRow.rowCodec.first()).run(c);
  }

  @Override
  public List<AuditLogRow> selectByIds(
    List<AuditLogId> logIds,
    ConnectionRead c
  ) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : logIds) { fragments.add(Fragment.encode(AuditLogId.mariaType, id)); };
    return Fragment.concat(Fragment.of("select `log_id`, `table_name`, `record_id`, `action`, `old_values`, `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id` from `audit_log` where `log_id` in ("), Fragment.comma(fragments), Fragment.of(")")).query(AuditLogRow.rowCodec.all()).run(c);
  }

  @Override
  public Map<AuditLogId, AuditLogRow> selectByIdsTracked(
    List<AuditLogId> logIds,
    ConnectionRead c
  ) {
    HashMap<AuditLogId, AuditLogRow> ret = new HashMap<AuditLogId, AuditLogRow>();
    selectByIds(logIds, c).forEach(row -> ret.put(row.logId(), row));
    return ret;
  }

  @Override
  public UpdateBuilder<AuditLogFields, AuditLogRow> update() {
    return UpdateBuilder.of("`audit_log`", AuditLogFields.structure, AuditLogRow.rowCodec, Dialect.MARIADB);
  }

  @Override
  public Boolean update(
    AuditLogRow row,
    Connection c
  ) {
    AuditLogId logId = row.logId();;
    return concat(Fragment.of("update `audit_log`\nset `table_name` = "), Fragment.encode(MariaTypes.varchar, row.tableName()), Fragment.of(",\n`record_id` = "), Fragment.encode(MariaTypes.varchar, row.recordId()), Fragment.of(",\n`action` = "), Fragment.encode(MariaTypes.text, row.action()), Fragment.of(",\n`old_values` = "), Fragment.encode(MariaTypes.json.opt(), row.oldValues()), Fragment.of(",\n`new_values` = "), Fragment.encode(MariaTypes.json.opt(), row.newValues()), Fragment.of(",\n`changed_by` = "), Fragment.encode(MariaTypes.varchar.opt(), row.changedBy()), Fragment.of(",\n`changed_at` = "), Fragment.encode(MariaTypes.datetime, row.changedAt()), Fragment.of(",\n`client_ip` = "), Fragment.encode(MariaTypes.inet6.opt(), row.clientIp()), Fragment.of(",\n`session_id` = "), Fragment.encode(MariaTypes.varbinary.opt(), row.sessionId()), Fragment.of("\nwhere `log_id` = "), Fragment.encode(AuditLogId.mariaType, logId), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public AuditLogRow upsert(
    AuditLogRow unsaved,
    Connection c
  ) {
    return concat(Fragment.of("INSERT INTO `audit_log`(`log_id`, `table_name`, `record_id`, `action`, `old_values`, `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`)\nVALUES ("), Fragment.encode(AuditLogId.mariaType, unsaved.logId()), Fragment.of(", "), Fragment.encode(MariaTypes.varchar, unsaved.tableName()), Fragment.of(", "), Fragment.encode(MariaTypes.varchar, unsaved.recordId()), Fragment.of(", "), Fragment.encode(MariaTypes.text, unsaved.action()), Fragment.of(", "), Fragment.encode(MariaTypes.json.opt(), unsaved.oldValues()), Fragment.of(", "), Fragment.encode(MariaTypes.json.opt(), unsaved.newValues()), Fragment.of(", "), Fragment.encode(MariaTypes.varchar.opt(), unsaved.changedBy()), Fragment.of(", "), Fragment.encode(MariaTypes.datetime, unsaved.changedAt()), Fragment.of(", "), Fragment.encode(MariaTypes.inet6.opt(), unsaved.clientIp()), Fragment.of(", "), Fragment.encode(MariaTypes.varbinary.opt(), unsaved.sessionId()), Fragment.of(")\nON DUPLICATE KEY UPDATE `table_name` = VALUES(`table_name`),\n`record_id` = VALUES(`record_id`),\n`action` = VALUES(`action`),\n`old_values` = VALUES(`old_values`),\n`new_values` = VALUES(`new_values`),\n`changed_by` = VALUES(`changed_by`),\n`changed_at` = VALUES(`changed_at`),\n`client_ip` = VALUES(`client_ip`),\n`session_id` = VALUES(`session_id`)\nRETURNING `log_id`, `table_name`, `record_id`, `action`, `old_values`, `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`"))
      .updateReturning(AuditLogRow.rowCodec.exactlyOne())
      .run(c);
  }

  @Override
  public List<AuditLogRow> upsertBatch(
    Iterator<AuditLogRow> unsaved,
    Connection c
  ) {
    return concat(Fragment.of("INSERT INTO `audit_log`(`log_id`, `table_name`, `record_id`, `action`, `old_values`, `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`)\nVALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)\nON DUPLICATE KEY UPDATE `table_name` = VALUES(`table_name`),\n`record_id` = VALUES(`record_id`),\n`action` = VALUES(`action`),\n`old_values` = VALUES(`old_values`),\n`new_values` = VALUES(`new_values`),\n`changed_by` = VALUES(`changed_by`),\n`changed_at` = VALUES(`changed_at`),\n`client_ip` = VALUES(`client_ip`),\n`session_id` = VALUES(`session_id`)\nRETURNING `log_id`, `table_name`, `record_id`, `action`, `old_values`, `new_values`, `changed_by`, `changed_at`, `client_ip`, `session_id`"))
      .updateReturningEach(AuditLogRow.rowCodec, unsaved)
    .run(c);
  }
}