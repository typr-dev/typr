/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.product_images;

import dev.typr.dsl.DeleteBuilder;
import dev.typr.dsl.Dialect;
import dev.typr.dsl.SelectBuilder;
import dev.typr.dsl.UpdateBuilder;
import dev.typr.foundations.Connection;
import dev.typr.foundations.ConnectionRead;
import dev.typr.foundations.Fragment;
import dev.typr.foundations.MariaTypes;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import testdb.products.ProductsId;
import testdb.userdefined.IsPrimary;
import static dev.typr.foundations.Fragment.concat;

public class ProductImagesRepoImpl implements ProductImagesRepo {
  @Override
  public DeleteBuilder<ProductImagesFields, ProductImagesRow> delete() {
    return DeleteBuilder.of("`product_images`", ProductImagesFields.structure, Dialect.MARIADB);
  }

  @Override
  public Boolean deleteById(
    ProductImagesId imageId,
    Connection c
  ) {
    return concat(Fragment.of("delete from `product_images` where `image_id` = "), Fragment.encode(ProductImagesId.mariaType, imageId), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public Integer deleteByIds(
    List<ProductImagesId> imageIds,
    Connection c
  ) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : imageIds) { fragments.add(Fragment.encode(ProductImagesId.mariaType, id)); };
    return Fragment.concat(Fragment.of("delete from `product_images` where `image_id` in ("), Fragment.comma(fragments), Fragment.of(")")).update().run(c);
  }

  @Override
  public ProductImagesRow insert(
    ProductImagesRow unsaved,
    Connection c
  ) {
    return concat(Fragment.of("insert into `product_images`(`product_id`, `image_url`, `thumbnail_url`, `alt_text`, `sort_order`, `is_primary`, `image_data`)\nvalues ("), Fragment.encode(ProductsId.mariaType, unsaved.productId()), Fragment.of(", "), Fragment.encode(MariaTypes.varchar, unsaved.imageUrl()), Fragment.of(", "), Fragment.encode(MariaTypes.varchar.opt(), unsaved.thumbnailUrl()), Fragment.of(", "), Fragment.encode(MariaTypes.varchar.opt(), unsaved.altText()), Fragment.of(", "), Fragment.encode(MariaTypes.tinyintUnsigned, unsaved.sortOrder()), Fragment.of(", "), Fragment.encode(IsPrimary.mariaType, unsaved.isPrimary()), Fragment.of(", "), Fragment.encode(MariaTypes.longblob.opt(), unsaved.imageData()), Fragment.of(")\nRETURNING `image_id`, `product_id`, `image_url`, `thumbnail_url`, `alt_text`, `sort_order`, `is_primary`, `image_data`\n"))
      .updateReturning(ProductImagesRow.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public ProductImagesRow insert(
    ProductImagesRowUnsaved unsaved,
    Connection c
  ) {
    ArrayList<Fragment> columns = new ArrayList<>();;
    ArrayList<Fragment> values = new ArrayList<>();;
    columns.add(Fragment.of("`product_id`"));
    values.add(concat(Fragment.encode(ProductsId.mariaType, unsaved.productId()), Fragment.of("")));
    columns.add(Fragment.of("`image_url`"));
    values.add(concat(Fragment.encode(MariaTypes.varchar, unsaved.imageUrl()), Fragment.of("")));
    unsaved.thumbnailUrl().visit(
      () -> {
  
      },
      value -> {
        columns.add(Fragment.of("`thumbnail_url`"));
        values.add(concat(Fragment.encode(MariaTypes.varchar.opt(), value), Fragment.of("")));
      }
    );;
    unsaved.altText().visit(
      () -> {
  
      },
      value -> {
        columns.add(Fragment.of("`alt_text`"));
        values.add(concat(Fragment.encode(MariaTypes.varchar.opt(), value), Fragment.of("")));
      }
    );;
    unsaved.sortOrder().visit(
      () -> {
  
      },
      value -> {
        columns.add(Fragment.of("`sort_order`"));
        values.add(concat(Fragment.encode(MariaTypes.tinyintUnsigned, value), Fragment.of("")));
      }
    );;
    unsaved.isPrimary().visit(
      () -> {
  
      },
      value -> {
        columns.add(Fragment.of("`is_primary`"));
        values.add(concat(Fragment.encode(IsPrimary.mariaType, value), Fragment.of("")));
      }
    );;
    unsaved.imageData().visit(
      () -> {
  
      },
      value -> {
        columns.add(Fragment.of("`image_data`"));
        values.add(concat(Fragment.encode(MariaTypes.longblob.opt(), value), Fragment.of("")));
      }
    );;
    Fragment q = concat(Fragment.of("insert into `product_images`("), Fragment.comma(columns), Fragment.of(")\nvalues ("), Fragment.comma(values), Fragment.of(")\nRETURNING `image_id`, `product_id`, `image_url`, `thumbnail_url`, `alt_text`, `sort_order`, `is_primary`, `image_data`\n"));;
    return q.updateReturning(ProductImagesRow.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public SelectBuilder<ProductImagesFields, ProductImagesRow> select() {
    return SelectBuilder.of("`product_images`", ProductImagesFields.structure, ProductImagesRow.rowCodec, Dialect.MARIADB);
  }

  @Override
  public List<ProductImagesRow> selectAll(ConnectionRead c) {
    return concat(Fragment.of("select `image_id`, `product_id`, `image_url`, `thumbnail_url`, `alt_text`, `sort_order`, `is_primary`, `image_data`\nfrom `product_images`\n")).query(ProductImagesRow.rowCodec.all()).run(c);
  }

  @Override
  public Optional<ProductImagesRow> selectById(
    ProductImagesId imageId,
    ConnectionRead c
  ) {
    return concat(Fragment.of("select `image_id`, `product_id`, `image_url`, `thumbnail_url`, `alt_text`, `sort_order`, `is_primary`, `image_data`\nfrom `product_images`\nwhere `image_id` = "), Fragment.encode(ProductImagesId.mariaType, imageId), Fragment.of("")).query(ProductImagesRow.rowCodec.first()).run(c);
  }

  @Override
  public List<ProductImagesRow> selectByIds(
    List<ProductImagesId> imageIds,
    ConnectionRead c
  ) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : imageIds) { fragments.add(Fragment.encode(ProductImagesId.mariaType, id)); };
    return Fragment.concat(Fragment.of("select `image_id`, `product_id`, `image_url`, `thumbnail_url`, `alt_text`, `sort_order`, `is_primary`, `image_data` from `product_images` where `image_id` in ("), Fragment.comma(fragments), Fragment.of(")")).query(ProductImagesRow.rowCodec.all()).run(c);
  }

  @Override
  public Map<ProductImagesId, ProductImagesRow> selectByIdsTracked(
    List<ProductImagesId> imageIds,
    ConnectionRead c
  ) {
    HashMap<ProductImagesId, ProductImagesRow> ret = new HashMap<ProductImagesId, ProductImagesRow>();
    selectByIds(imageIds, c).forEach(row -> ret.put(row.imageId(), row));
    return ret;
  }

  @Override
  public UpdateBuilder<ProductImagesFields, ProductImagesRow> update() {
    return UpdateBuilder.of("`product_images`", ProductImagesFields.structure, ProductImagesRow.rowCodec, Dialect.MARIADB);
  }

  @Override
  public Boolean update(
    ProductImagesRow row,
    Connection c
  ) {
    ProductImagesId imageId = row.imageId();;
    return concat(Fragment.of("update `product_images`\nset `product_id` = "), Fragment.encode(ProductsId.mariaType, row.productId()), Fragment.of(",\n`image_url` = "), Fragment.encode(MariaTypes.varchar, row.imageUrl()), Fragment.of(",\n`thumbnail_url` = "), Fragment.encode(MariaTypes.varchar.opt(), row.thumbnailUrl()), Fragment.of(",\n`alt_text` = "), Fragment.encode(MariaTypes.varchar.opt(), row.altText()), Fragment.of(",\n`sort_order` = "), Fragment.encode(MariaTypes.tinyintUnsigned, row.sortOrder()), Fragment.of(",\n`is_primary` = "), Fragment.encode(IsPrimary.mariaType, row.isPrimary()), Fragment.of(",\n`image_data` = "), Fragment.encode(MariaTypes.longblob.opt(), row.imageData()), Fragment.of("\nwhere `image_id` = "), Fragment.encode(ProductImagesId.mariaType, imageId), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public ProductImagesRow upsert(
    ProductImagesRow unsaved,
    Connection c
  ) {
    return concat(Fragment.of("INSERT INTO `product_images`(`image_id`, `product_id`, `image_url`, `thumbnail_url`, `alt_text`, `sort_order`, `is_primary`, `image_data`)\nVALUES ("), Fragment.encode(ProductImagesId.mariaType, unsaved.imageId()), Fragment.of(", "), Fragment.encode(ProductsId.mariaType, unsaved.productId()), Fragment.of(", "), Fragment.encode(MariaTypes.varchar, unsaved.imageUrl()), Fragment.of(", "), Fragment.encode(MariaTypes.varchar.opt(), unsaved.thumbnailUrl()), Fragment.of(", "), Fragment.encode(MariaTypes.varchar.opt(), unsaved.altText()), Fragment.of(", "), Fragment.encode(MariaTypes.tinyintUnsigned, unsaved.sortOrder()), Fragment.of(", "), Fragment.encode(IsPrimary.mariaType, unsaved.isPrimary()), Fragment.of(", "), Fragment.encode(MariaTypes.longblob.opt(), unsaved.imageData()), Fragment.of(")\nON DUPLICATE KEY UPDATE `product_id` = VALUES(`product_id`),\n`image_url` = VALUES(`image_url`),\n`thumbnail_url` = VALUES(`thumbnail_url`),\n`alt_text` = VALUES(`alt_text`),\n`sort_order` = VALUES(`sort_order`),\n`is_primary` = VALUES(`is_primary`),\n`image_data` = VALUES(`image_data`)\nRETURNING `image_id`, `product_id`, `image_url`, `thumbnail_url`, `alt_text`, `sort_order`, `is_primary`, `image_data`"))
      .updateReturning(ProductImagesRow.rowCodec.exactlyOne())
      .run(c);
  }

  @Override
  public List<ProductImagesRow> upsertBatch(
    Iterator<ProductImagesRow> unsaved,
    Connection c
  ) {
    return concat(Fragment.of("INSERT INTO `product_images`(`image_id`, `product_id`, `image_url`, `thumbnail_url`, `alt_text`, `sort_order`, `is_primary`, `image_data`)\nVALUES (?, ?, ?, ?, ?, ?, ?, ?)\nON DUPLICATE KEY UPDATE `product_id` = VALUES(`product_id`),\n`image_url` = VALUES(`image_url`),\n`thumbnail_url` = VALUES(`thumbnail_url`),\n`alt_text` = VALUES(`alt_text`),\n`sort_order` = VALUES(`sort_order`),\n`is_primary` = VALUES(`is_primary`),\n`image_data` = VALUES(`image_data`)\nRETURNING `image_id`, `product_id`, `image_url`, `thumbnail_url`, `alt_text`, `sort_order`, `is_primary`, `image_data`"))
      .updateReturningEach(ProductImagesRow.rowCodec, unsaved)
    .run(c);
  }
}