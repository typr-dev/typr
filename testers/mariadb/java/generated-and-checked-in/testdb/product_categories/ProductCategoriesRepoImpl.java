/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.product_categories;

import dev.typr.dsl.DeleteBuilder;
import dev.typr.dsl.Dialect;
import dev.typr.dsl.SelectBuilder;
import dev.typr.dsl.UpdateBuilder;
import dev.typr.foundations.Connection;
import dev.typr.foundations.ConnectionRead;
import dev.typr.foundations.Fragment;
import dev.typr.foundations.MariaTypes;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import testdb.categories.CategoriesId;
import testdb.products.ProductsId;
import testdb.userdefined.IsPrimary;
import static dev.typr.foundations.Fragment.concat;

public class ProductCategoriesRepoImpl implements ProductCategoriesRepo {
  @Override
  public DeleteBuilder<ProductCategoriesFields, ProductCategoriesRow> delete() {
    return DeleteBuilder.of("`product_categories`", ProductCategoriesFields.structure, Dialect.MARIADB);
  }

  @Override
  public Boolean deleteById(
    ProductCategoriesId compositeId,
    Connection c
  ) {
    return concat(Fragment.of("delete from `product_categories` where `product_id` = "), Fragment.encode(ProductsId.mariaType, compositeId.productId()), Fragment.of(" AND `category_id` = "), Fragment.encode(CategoriesId.mariaType, compositeId.categoryId()), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public Integer deleteByIds(
    List<ProductCategoriesId> compositeIds,
    Connection c
  ) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : compositeIds) { fragments.add(Fragment.concat(Fragment.of("("), Fragment.encode(ProductsId.mariaType, id.productId()), Fragment.of(", "), Fragment.encode(CategoriesId.mariaType, id.categoryId()), Fragment.of(")"))); };
    return Fragment.concat(Fragment.of("delete from `product_categories` where (`product_id`, `category_id`) in ("), Fragment.comma(fragments), Fragment.of(")")).update().run(c);
  }

  @Override
  public ProductCategoriesRow insert(
    ProductCategoriesRow unsaved,
    Connection c
  ) {
    return concat(Fragment.of("insert into `product_categories`(`product_id`, `category_id`, `is_primary`, `sort_order`)\nvalues ("), Fragment.encode(ProductsId.mariaType, unsaved.productId()), Fragment.of(", "), Fragment.encode(CategoriesId.mariaType, unsaved.categoryId()), Fragment.of(", "), Fragment.encode(IsPrimary.mariaType, unsaved.isPrimary()), Fragment.of(", "), Fragment.encode(MariaTypes.smallint, unsaved.sortOrder()), Fragment.of(")\nRETURNING `product_id`, `category_id`, `is_primary`, `sort_order`\n"))
      .updateReturning(ProductCategoriesRow.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public ProductCategoriesRow insert(
    ProductCategoriesRowUnsaved unsaved,
    Connection c
  ) {
    ArrayList<Fragment> columns = new ArrayList<>();;
    ArrayList<Fragment> values = new ArrayList<>();;
    columns.add(Fragment.of("`product_id`"));
    values.add(concat(Fragment.encode(ProductsId.mariaType, unsaved.productId()), Fragment.of("")));
    columns.add(Fragment.of("`category_id`"));
    values.add(concat(Fragment.encode(CategoriesId.mariaType, unsaved.categoryId()), Fragment.of("")));
    unsaved.isPrimary().visit(
      () -> {
  
      },
      value -> {
        columns.add(Fragment.of("`is_primary`"));
        values.add(concat(Fragment.encode(IsPrimary.mariaType, value), Fragment.of("")));
      }
    );;
    unsaved.sortOrder().visit(
      () -> {
  
      },
      value -> {
        columns.add(Fragment.of("`sort_order`"));
        values.add(concat(Fragment.encode(MariaTypes.smallint, value), Fragment.of("")));
      }
    );;
    Fragment q = concat(Fragment.of("insert into `product_categories`("), Fragment.comma(columns), Fragment.of(")\nvalues ("), Fragment.comma(values), Fragment.of(")\nRETURNING `product_id`, `category_id`, `is_primary`, `sort_order`\n"));;
    return q.updateReturning(ProductCategoriesRow.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public SelectBuilder<ProductCategoriesFields, ProductCategoriesRow> select() {
    return SelectBuilder.of("`product_categories`", ProductCategoriesFields.structure, ProductCategoriesRow.rowCodec, Dialect.MARIADB);
  }

  @Override
  public List<ProductCategoriesRow> selectAll(ConnectionRead c) {
    return concat(Fragment.of("select `product_id`, `category_id`, `is_primary`, `sort_order`\nfrom `product_categories`\n")).query(ProductCategoriesRow.rowCodec.all()).run(c);
  }

  @Override
  public Optional<ProductCategoriesRow> selectById(
    ProductCategoriesId compositeId,
    ConnectionRead c
  ) {
    return concat(Fragment.of("select `product_id`, `category_id`, `is_primary`, `sort_order`\nfrom `product_categories`\nwhere `product_id` = "), Fragment.encode(ProductsId.mariaType, compositeId.productId()), Fragment.of(" AND `category_id` = "), Fragment.encode(CategoriesId.mariaType, compositeId.categoryId()), Fragment.of("")).query(ProductCategoriesRow.rowCodec.first()).run(c);
  }

  @Override
  public List<ProductCategoriesRow> selectByIds(
    List<ProductCategoriesId> compositeIds,
    ConnectionRead c
  ) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : compositeIds) { fragments.add(Fragment.concat(Fragment.of("("), Fragment.encode(ProductsId.mariaType, id.productId()), Fragment.of(", "), Fragment.encode(CategoriesId.mariaType, id.categoryId()), Fragment.of(")"))); };
    return Fragment.concat(Fragment.of("select `product_id`, `category_id`, `is_primary`, `sort_order` from `product_categories` where (`product_id`, `category_id`) in ("), Fragment.comma(fragments), Fragment.of(")")).query(ProductCategoriesRow.rowCodec.all()).run(c);
  }

  @Override
  public Map<ProductCategoriesId, ProductCategoriesRow> selectByIdsTracked(
    List<ProductCategoriesId> compositeIds,
    ConnectionRead c
  ) {
    HashMap<ProductCategoriesId, ProductCategoriesRow> ret = new HashMap<ProductCategoriesId, ProductCategoriesRow>();
    selectByIds(compositeIds, c).forEach(row -> ret.put(row.compositeId(), row));
    return ret;
  }

  @Override
  public UpdateBuilder<ProductCategoriesFields, ProductCategoriesRow> update() {
    return UpdateBuilder.of("`product_categories`", ProductCategoriesFields.structure, ProductCategoriesRow.rowCodec, Dialect.MARIADB);
  }

  @Override
  public Boolean update(
    ProductCategoriesRow row,
    Connection c
  ) {
    ProductCategoriesId compositeId = row.compositeId();;
    return concat(Fragment.of("update `product_categories`\nset `is_primary` = "), Fragment.encode(IsPrimary.mariaType, row.isPrimary()), Fragment.of(",\n`sort_order` = "), Fragment.encode(MariaTypes.smallint, row.sortOrder()), Fragment.of("\nwhere `product_id` = "), Fragment.encode(ProductsId.mariaType, compositeId.productId()), Fragment.of(" AND `category_id` = "), Fragment.encode(CategoriesId.mariaType, compositeId.categoryId()), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public ProductCategoriesRow upsert(
    ProductCategoriesRow unsaved,
    Connection c
  ) {
    return concat(Fragment.of("INSERT INTO `product_categories`(`product_id`, `category_id`, `is_primary`, `sort_order`)\nVALUES ("), Fragment.encode(ProductsId.mariaType, unsaved.productId()), Fragment.of(", "), Fragment.encode(CategoriesId.mariaType, unsaved.categoryId()), Fragment.of(", "), Fragment.encode(IsPrimary.mariaType, unsaved.isPrimary()), Fragment.of(", "), Fragment.encode(MariaTypes.smallint, unsaved.sortOrder()), Fragment.of(")\nON DUPLICATE KEY UPDATE `is_primary` = VALUES(`is_primary`),\n`sort_order` = VALUES(`sort_order`)\nRETURNING `product_id`, `category_id`, `is_primary`, `sort_order`"))
      .updateReturning(ProductCategoriesRow.rowCodec.exactlyOne())
      .run(c);
  }

  @Override
  public List<ProductCategoriesRow> upsertBatch(
    Iterator<ProductCategoriesRow> unsaved,
    Connection c
  ) {
    return concat(Fragment.of("INSERT INTO `product_categories`(`product_id`, `category_id`, `is_primary`, `sort_order`)\nVALUES (?, ?, ?, ?)\nON DUPLICATE KEY UPDATE `is_primary` = VALUES(`is_primary`),\n`sort_order` = VALUES(`sort_order`)\nRETURNING `product_id`, `category_id`, `is_primary`, `sort_order`"))
      .updateReturningEach(ProductCategoriesRow.rowCodec, unsaved)
    .run(c);
  }
}