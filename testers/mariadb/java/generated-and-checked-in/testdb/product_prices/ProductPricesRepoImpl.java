/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.product_prices;

import dev.typr.dsl.DeleteBuilder;
import dev.typr.dsl.Dialect;
import dev.typr.dsl.SelectBuilder;
import dev.typr.dsl.UpdateBuilder;
import dev.typr.foundations.Connection;
import dev.typr.foundations.ConnectionRead;
import dev.typr.foundations.Fragment;
import dev.typr.foundations.MariaTypes;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import testdb.price_tiers.PriceTiersId;
import testdb.products.ProductsId;
import static dev.typr.foundations.Fragment.concat;

public class ProductPricesRepoImpl implements ProductPricesRepo {
  @Override
  public DeleteBuilder<ProductPricesFields, ProductPricesRow> delete() {
    return DeleteBuilder.of("`product_prices`", ProductPricesFields.structure, Dialect.MARIADB);
  }

  @Override
  public Boolean deleteById(
    ProductPricesId priceId,
    Connection c
  ) {
    return concat(Fragment.of("delete from `product_prices` where `price_id` = "), Fragment.encode(ProductPricesId.mariaType, priceId), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public Integer deleteByIds(
    List<ProductPricesId> priceIds,
    Connection c
  ) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : priceIds) { fragments.add(Fragment.encode(ProductPricesId.mariaType, id)); };
    return Fragment.concat(Fragment.of("delete from `product_prices` where `price_id` in ("), Fragment.comma(fragments), Fragment.of(")")).update().run(c);
  }

  @Override
  public ProductPricesRow insert(
    ProductPricesRow unsaved,
    Connection c
  ) {
    return concat(Fragment.of("insert into `product_prices`(`product_id`, `tier_id`, `price`, `currency_code`, `valid_from`, `valid_to`)\nvalues ("), Fragment.encode(ProductsId.mariaType, unsaved.productId()), Fragment.of(", "), Fragment.encode(PriceTiersId.mariaType.opt(), unsaved.tierId()), Fragment.of(", "), Fragment.encode(MariaTypes.numeric, unsaved.price()), Fragment.of(", "), Fragment.encode(MariaTypes.char_, unsaved.currencyCode()), Fragment.of(", "), Fragment.encode(MariaTypes.date, unsaved.validFrom()), Fragment.of(", "), Fragment.encode(MariaTypes.date.opt(), unsaved.validTo()), Fragment.of(")\nRETURNING `price_id`, `product_id`, `tier_id`, `price`, `currency_code`, `valid_from`, `valid_to`\n"))
      .updateReturning(ProductPricesRow.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public ProductPricesRow insert(
    ProductPricesRowUnsaved unsaved,
    Connection c
  ) {
    ArrayList<Fragment> columns = new ArrayList<>();;
    ArrayList<Fragment> values = new ArrayList<>();;
    columns.add(Fragment.of("`product_id`"));
    values.add(concat(Fragment.encode(ProductsId.mariaType, unsaved.productId()), Fragment.of("")));
    columns.add(Fragment.of("`price`"));
    values.add(concat(Fragment.encode(MariaTypes.numeric, unsaved.price()), Fragment.of("")));
    columns.add(Fragment.of("`valid_from`"));
    values.add(concat(Fragment.encode(MariaTypes.date, unsaved.validFrom()), Fragment.of("")));
    unsaved.tierId().visit(
      () -> {
  
      },
      value -> {
        columns.add(Fragment.of("`tier_id`"));
        values.add(concat(Fragment.encode(PriceTiersId.mariaType.opt(), value), Fragment.of("")));
      }
    );;
    unsaved.currencyCode().visit(
      () -> {
  
      },
      value -> {
        columns.add(Fragment.of("`currency_code`"));
        values.add(concat(Fragment.encode(MariaTypes.char_, value), Fragment.of("")));
      }
    );;
    unsaved.validTo().visit(
      () -> {
  
      },
      value -> {
        columns.add(Fragment.of("`valid_to`"));
        values.add(concat(Fragment.encode(MariaTypes.date.opt(), value), Fragment.of("")));
      }
    );;
    Fragment q = concat(Fragment.of("insert into `product_prices`("), Fragment.comma(columns), Fragment.of(")\nvalues ("), Fragment.comma(values), Fragment.of(")\nRETURNING `price_id`, `product_id`, `tier_id`, `price`, `currency_code`, `valid_from`, `valid_to`\n"));;
    return q.updateReturning(ProductPricesRow.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public SelectBuilder<ProductPricesFields, ProductPricesRow> select() {
    return SelectBuilder.of("`product_prices`", ProductPricesFields.structure, ProductPricesRow.rowCodec, Dialect.MARIADB);
  }

  @Override
  public List<ProductPricesRow> selectAll(ConnectionRead c) {
    return concat(Fragment.of("select `price_id`, `product_id`, `tier_id`, `price`, `currency_code`, `valid_from`, `valid_to`\nfrom `product_prices`\n")).query(ProductPricesRow.rowCodec.all()).run(c);
  }

  @Override
  public Optional<ProductPricesRow> selectById(
    ProductPricesId priceId,
    ConnectionRead c
  ) {
    return concat(Fragment.of("select `price_id`, `product_id`, `tier_id`, `price`, `currency_code`, `valid_from`, `valid_to`\nfrom `product_prices`\nwhere `price_id` = "), Fragment.encode(ProductPricesId.mariaType, priceId), Fragment.of("")).query(ProductPricesRow.rowCodec.first()).run(c);
  }

  @Override
  public List<ProductPricesRow> selectByIds(
    List<ProductPricesId> priceIds,
    ConnectionRead c
  ) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : priceIds) { fragments.add(Fragment.encode(ProductPricesId.mariaType, id)); };
    return Fragment.concat(Fragment.of("select `price_id`, `product_id`, `tier_id`, `price`, `currency_code`, `valid_from`, `valid_to` from `product_prices` where `price_id` in ("), Fragment.comma(fragments), Fragment.of(")")).query(ProductPricesRow.rowCodec.all()).run(c);
  }

  @Override
  public Map<ProductPricesId, ProductPricesRow> selectByIdsTracked(
    List<ProductPricesId> priceIds,
    ConnectionRead c
  ) {
    HashMap<ProductPricesId, ProductPricesRow> ret = new HashMap<ProductPricesId, ProductPricesRow>();
    selectByIds(priceIds, c).forEach(row -> ret.put(row.priceId(), row));
    return ret;
  }

  @Override
  public UpdateBuilder<ProductPricesFields, ProductPricesRow> update() {
    return UpdateBuilder.of("`product_prices`", ProductPricesFields.structure, ProductPricesRow.rowCodec, Dialect.MARIADB);
  }

  @Override
  public Boolean update(
    ProductPricesRow row,
    Connection c
  ) {
    ProductPricesId priceId = row.priceId();;
    return concat(Fragment.of("update `product_prices`\nset `product_id` = "), Fragment.encode(ProductsId.mariaType, row.productId()), Fragment.of(",\n`tier_id` = "), Fragment.encode(PriceTiersId.mariaType.opt(), row.tierId()), Fragment.of(",\n`price` = "), Fragment.encode(MariaTypes.numeric, row.price()), Fragment.of(",\n`currency_code` = "), Fragment.encode(MariaTypes.char_, row.currencyCode()), Fragment.of(",\n`valid_from` = "), Fragment.encode(MariaTypes.date, row.validFrom()), Fragment.of(",\n`valid_to` = "), Fragment.encode(MariaTypes.date.opt(), row.validTo()), Fragment.of("\nwhere `price_id` = "), Fragment.encode(ProductPricesId.mariaType, priceId), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public ProductPricesRow upsert(
    ProductPricesRow unsaved,
    Connection c
  ) {
    return concat(Fragment.of("INSERT INTO `product_prices`(`price_id`, `product_id`, `tier_id`, `price`, `currency_code`, `valid_from`, `valid_to`)\nVALUES ("), Fragment.encode(ProductPricesId.mariaType, unsaved.priceId()), Fragment.of(", "), Fragment.encode(ProductsId.mariaType, unsaved.productId()), Fragment.of(", "), Fragment.encode(PriceTiersId.mariaType.opt(), unsaved.tierId()), Fragment.of(", "), Fragment.encode(MariaTypes.numeric, unsaved.price()), Fragment.of(", "), Fragment.encode(MariaTypes.char_, unsaved.currencyCode()), Fragment.of(", "), Fragment.encode(MariaTypes.date, unsaved.validFrom()), Fragment.of(", "), Fragment.encode(MariaTypes.date.opt(), unsaved.validTo()), Fragment.of(")\nON DUPLICATE KEY UPDATE `product_id` = VALUES(`product_id`),\n`tier_id` = VALUES(`tier_id`),\n`price` = VALUES(`price`),\n`currency_code` = VALUES(`currency_code`),\n`valid_from` = VALUES(`valid_from`),\n`valid_to` = VALUES(`valid_to`)\nRETURNING `price_id`, `product_id`, `tier_id`, `price`, `currency_code`, `valid_from`, `valid_to`"))
      .updateReturning(ProductPricesRow.rowCodec.exactlyOne())
      .run(c);
  }

  @Override
  public List<ProductPricesRow> upsertBatch(
    Iterator<ProductPricesRow> unsaved,
    Connection c
  ) {
    return concat(Fragment.of("INSERT INTO `product_prices`(`price_id`, `product_id`, `tier_id`, `price`, `currency_code`, `valid_from`, `valid_to`)\nVALUES (?, ?, ?, ?, ?, ?, ?)\nON DUPLICATE KEY UPDATE `product_id` = VALUES(`product_id`),\n`tier_id` = VALUES(`tier_id`),\n`price` = VALUES(`price`),\n`currency_code` = VALUES(`currency_code`),\n`valid_from` = VALUES(`valid_from`),\n`valid_to` = VALUES(`valid_to`)\nRETURNING `price_id`, `product_id`, `tier_id`, `price`, `currency_code`, `valid_from`, `valid_to`"))
      .updateReturningEach(ProductPricesRow.rowCodec, unsaved)
    .run(c);
  }
}