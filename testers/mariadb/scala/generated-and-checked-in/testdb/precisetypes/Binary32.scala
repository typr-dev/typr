/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.precisetypes

import com.fasterxml.jackson.annotation.JsonValue
import dev.typr.foundations.MariaType
import dev.typr.foundations.MariaTypes
import dev.typr.foundations.data.precise.BinaryN
import dev.typr.foundations.scala.Bijection
import java.lang.IllegalArgumentException
import java.util.Arrays

case class Binary32 private(@JsonValue value: Array[Byte]) extends BinaryN {
  override def rawValue: Array[Byte] = value

  override def maxLength: Int = 32

  override def semanticEquals(other: BinaryN): Boolean = (if (other == null) false else Arrays.equals(value, other.rawValue()))

  override def semanticHashCode: Int = Arrays.hashCode(value)

  override def equals(that: Any): Boolean = (this eq that.asInstanceOf[AnyRef]) || (that match { case other: BinaryN => Arrays.equals(value, other.rawValue()); case _ => false })

  override def hashCode: Int = Arrays.hashCode(value)
}

object Binary32 {
  given bijection: Bijection[Binary32, Array[Byte]] = Bijection.apply[Binary32, Array[Byte]](_.value)(Binary32.apply)

  given dbType: MariaType[Binary32] = MariaTypes.binary.bimap(Binary32.apply, _.value)

  def of(value: Array[Byte]): Option[Binary32] = (if (value.length <= 32) Some(new Binary32(value)) else None)

  def unsafeForce(value: Array[Byte]): Binary32 = {
    if (value.length > 32) {
      throw new IllegalArgumentException(s"Value length ${value.length} exceeds maximum 32")
    }
    return new Binary32(value)
  }
}