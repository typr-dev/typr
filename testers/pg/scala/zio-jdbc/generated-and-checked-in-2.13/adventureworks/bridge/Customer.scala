/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.bridge

import zio.json.JsonDecoder
import zio.json.JsonEncoder
import zio.json.ast.Json
import zio.json.internal.Write

/** Bridge composite type: Customer */
case class Customer(
  created_at: Option[String],
  customer_id: String,
  email: Option[String]
)

object Customer {
  implicit lazy val jsonDecoder: JsonDecoder[Customer] = {
    JsonDecoder[Json.Obj].mapOrFail { jsonObj =>
      val created_at = jsonObj.get("created_at").fold[Either[String, Option[String]]](Right(None))(_.as(JsonDecoder.option(JsonDecoder.string)))
      val customer_id = jsonObj.get("customer_id").toRight("Missing field 'customer_id'").flatMap(_.as(JsonDecoder.string))
      val email = jsonObj.get("email").fold[Either[String, Option[String]]](Right(None))(_.as(JsonDecoder.option(JsonDecoder.string)))
      if (created_at.isRight && customer_id.isRight && email.isRight)
        Right(Customer(created_at = created_at.toOption.get, customer_id = customer_id.toOption.get, email = email.toOption.get))
      else Left(List[Either[String, Any]](created_at, customer_id, email).flatMap(_.left.toOption).mkString(", "))
    }
  }

  implicit lazy val jsonEncoder: JsonEncoder[Customer] = {
    new JsonEncoder[Customer] {
      override def unsafeEncode(a: Customer, indent: Option[Int], out: Write): Unit = {
        out.write("{")
        out.write(""""created_at":""")
        JsonEncoder.option(JsonEncoder.string).unsafeEncode(a.created_at, indent, out)
        out.write(",")
        out.write(""""customer_id":""")
        JsonEncoder.string.unsafeEncode(a.customer_id, indent, out)
        out.write(",")
        out.write(""""email":""")
        JsonEncoder.option(JsonEncoder.string).unsafeEncode(a.email, indent, out)
        out.write("}")
      }
    }
  }
}