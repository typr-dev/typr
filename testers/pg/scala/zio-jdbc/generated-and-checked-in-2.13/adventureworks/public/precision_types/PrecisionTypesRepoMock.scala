/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.precision_types

import scala.annotation.nowarn
import typr.dsl.DeleteBuilder
import typr.dsl.DeleteBuilderMock
import typr.dsl.DeleteParams
import typr.dsl.SelectBuilder
import typr.dsl.SelectBuilderMock
import typr.dsl.SelectParams
import typr.dsl.UpdateBuilder
import typr.dsl.UpdateBuilderMock
import typr.dsl.UpdateParams
import zio.Chunk
import zio.ZIO
import zio.jdbc.UpdateResult
import zio.jdbc.ZConnection
import zio.stream.ZStream

case class PrecisionTypesRepoMock(
  toRow: PrecisionTypesRowUnsaved => PrecisionTypesRow,
  map: scala.collection.mutable.Map[PrecisionTypesId, PrecisionTypesRow] = scala.collection.mutable.Map.empty[PrecisionTypesId, PrecisionTypesRow]
) extends PrecisionTypesRepo {
  override def delete: DeleteBuilder[PrecisionTypesFields, PrecisionTypesRow] = DeleteBuilderMock(DeleteParams.empty, PrecisionTypesFields.structure, map)

  override def deleteById(id: PrecisionTypesId): ZIO[ZConnection, Throwable, Boolean] = ZIO.succeed(map.remove(id).isDefined)

  override def deleteByIds(ids: Array[PrecisionTypesId]): ZIO[ZConnection, Throwable, Long] = ZIO.succeed(ids.map(id => map.remove(id)).count(_.isDefined).toLong)

  override def insert(unsaved: PrecisionTypesRow): ZIO[ZConnection, Throwable, PrecisionTypesRow] = {
  ZIO.succeed {
    val _ =
      if (map.contains(unsaved.id))
        sys.error(s"id ${unsaved.id} already exists")
      else
        map.put(unsaved.id, unsaved)

    unsaved
  }
  }

  override def insert(unsaved: PrecisionTypesRowUnsaved): ZIO[ZConnection, Throwable, PrecisionTypesRow] = insert(toRow(unsaved))

  override def insertStreaming(
    unsaved: ZStream[ZConnection, Throwable, PrecisionTypesRow],
    batchSize: Int = 10000
  ): ZIO[ZConnection, Throwable, Long] = {
    unsaved.scanZIO(0L) { case (acc, row) =>
      ZIO.succeed {
        map += (row.id -> row)
        acc + 1
      }
    }.runLast.map(_.getOrElse(0L))
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: ZStream[ZConnection, Throwable, PrecisionTypesRowUnsaved],
    batchSize: Int = 10000
  ): ZIO[ZConnection, Throwable, Long] = {
    unsaved.scanZIO(0L) { case (acc, unsavedRow) =>
      ZIO.succeed {
        val row = toRow(unsavedRow)
        map += (row.id -> row)
        acc + 1
      }
    }.runLast.map(_.getOrElse(0L))
  }

  override def select: SelectBuilder[PrecisionTypesFields, PrecisionTypesRow] = SelectBuilderMock(PrecisionTypesFields.structure, ZIO.succeed(Chunk.fromIterable(map.values)), SelectParams.empty)

  override def selectAll: ZStream[ZConnection, Throwable, PrecisionTypesRow] = ZStream.fromIterable(map.values)

  override def selectById(id: PrecisionTypesId): ZIO[ZConnection, Throwable, Option[PrecisionTypesRow]] = ZIO.succeed(map.get(id))

  override def selectByIds(ids: Array[PrecisionTypesId]): ZStream[ZConnection, Throwable, PrecisionTypesRow] = ZStream.fromIterable(ids.flatMap(map.get))

  override def selectByIdsTracked(ids: Array[PrecisionTypesId]): ZIO[ZConnection, Throwable, Map[PrecisionTypesId, PrecisionTypesRow]] = {
    selectByIds(ids).runCollect.map { rows =>
      val byId = rows.view.map(x => (x.id, x)).toMap
      ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
    }
  }

  override def update: UpdateBuilder[PrecisionTypesFields, PrecisionTypesRow] = UpdateBuilderMock(UpdateParams.empty, PrecisionTypesFields.structure, map)

  override def update(row: PrecisionTypesRow): ZIO[ZConnection, Throwable, Option[PrecisionTypesRow]] = {
    ZIO.succeed {
      map.get(row.id).map { _ =>
        map.put(row.id, row): @nowarn
        row
      }
    }
  }

  override def upsert(unsaved: PrecisionTypesRow): ZIO[ZConnection, Throwable, UpdateResult[PrecisionTypesRow]] = {
    ZIO.succeed {
      map.put(unsaved.id, unsaved): @nowarn
      UpdateResult(1, Chunk.single(unsaved))
    }
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: ZStream[ZConnection, Throwable, PrecisionTypesRow],
    batchSize: Int = 10000
  ): ZIO[ZConnection, Throwable, Long] = {
    unsaved.scanZIO(0L) { case (acc, row) =>
      ZIO.succeed {
        map += (row.id -> row)
        acc + 1
      }
    }.runLast.map(_.getOrElse(0L))
  }
}