/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public

import adventureworks.Text
import java.sql.ResultSet
import java.sql.Types
import typr.dsl.PGType
import zio.jdbc.JdbcDecoder
import zio.jdbc.JdbcDecoderError
import zio.jdbc.JdbcEncoder
import zio.jdbc.SqlFragment.Setter
import zio.json.JsonDecoder
import zio.json.JsonEncoder

/** Enum `public.myenum`
 *  - a
 *  - b
 *  - c
 */

enum Myenum {
  case a, b, c
  
}

object Myenum {
  given arraySetter: Setter[Array[Myenum]] = {
    Setter.forSqlType[Array[Myenum]](
        (ps, i, v) => ps.setArray(i, ps.getConnection.createArrayOf("public.myenum", v.map(x => x.value))),
        java.sql.Types.ARRAY
      )
  }

  given arrayJdbcDecoder: JdbcDecoder[Array[Myenum]] = adventureworks.StringArrayDecoder.map(a => if (a == null) null else a.map(force))

  given arrayJdbcEncoder: JdbcEncoder[Array[Myenum]] = JdbcEncoder.singleParamEncoder(using arraySetter)

  given jdbcEncoder: JdbcEncoder[Myenum] = JdbcEncoder.stringEncoder.contramap(_.value)

  given jdbcDecoder: JdbcDecoder[Myenum] = {
    JdbcDecoder.stringDecoder.flatMap { s =>
      new JdbcDecoder[Myenum] {
        override def unsafeDecode(columIndex: Int, rs: ResultSet): (Int, Myenum) = {
          def error(msg: String): JdbcDecoderError =
            JdbcDecoderError(
              message = s"Error decoding Myenum from ResultSet",
              cause = new RuntimeException(msg),
              metadata = rs.getMetaData,
              row = rs.getRow
            )
  
          Myenum.apply(s).fold(e => throw error(e), (columIndex, _))
        }
      }
    }
  }

  given setter: Setter[Myenum] = Setter.stringSetter.contramap(_.value)

  given pgType: PGType[Myenum] = PGType.instance[Myenum]("public.myenum", Types.OTHER)

  given pgText: Text[Myenum] = {
    new Text[Myenum] {
      override def unsafeEncode(v: Myenum, sb: StringBuilder): Unit = Text.stringInstance.unsafeEncode(v.value, sb)
      override def unsafeArrayEncode(v: Myenum, sb: StringBuilder): Unit = Text.stringInstance.unsafeArrayEncode(v.value, sb)
    }
  }

  given jsonDecoder: JsonDecoder[Myenum] = JsonDecoder.string.mapOrFail(Myenum.apply)

  given jsonEncoder: JsonEncoder[Myenum] = JsonEncoder.string.contramap(_.value)
  extension (e: Myenum) def value: java.lang.String = e.toString
  def apply(str: java.lang.String): scala.Either[java.lang.String, Myenum] =
    scala.util.Try(Myenum.valueOf(str)).toEither.left.map(_ => s"'$str' does not match any of the following legal values: $Names")
  def force(str: java.lang.String): Myenum = Myenum.valueOf(str)
  val All: scala.List[Myenum] = values.toList
  val Names: java.lang.String = All.map(_.toString).mkString(", ")
  val ByName: scala.collection.immutable.Map[java.lang.String, Myenum] = All.map(x => (x.toString, x)).toMap
}