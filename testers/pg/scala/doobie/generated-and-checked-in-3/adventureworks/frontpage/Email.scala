/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.frontpage

import doobie.postgres.Text
import doobie.util.Get
import doobie.util.Put
import doobie.util.meta.Meta
import io.circe.Decoder
import io.circe.Encoder
import typr.dsl.Bijection

/** Domain `frontpage.email`
 * Constraint: CHECK ((VALUE ~ '^[^@]+@[^@]+\.[^@]+$'::text))
 */
case class Email(value: String)

object Email {
  given arrayGet: Get[Array[Email]] = adventureworks.StringArrayMeta.get.map(_.map(Email.apply))

  given arrayPut: Put[Array[Email]] = adventureworks.StringArrayMeta.put.contramap(_.map(_.value))

  given bijection: Bijection[Email, String] = Bijection.apply[Email, String](_.value)(Email.apply)

  given decoder: Decoder[Email] = Decoder.decodeString.map(Email.apply)

  given encoder: Encoder[Email] = Encoder.encodeString.contramap(_.value)

  given get: Get[Email] = Meta.StringMeta.get.map(Email.apply)

  given pgText: Text[Email] = {
    new Text[Email] {
      override def unsafeEncode(v: Email, sb: StringBuilder): Unit = Text.stringInstance.unsafeEncode(v.value, sb)
      override def unsafeArrayEncode(v: Email, sb: StringBuilder): Unit = Text.stringInstance.unsafeArrayEncode(v.value, sb)
    }
  }

  given put: Put[Email] = Meta.StringMeta.put.contramap(_.value)
}