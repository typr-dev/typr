/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.userdefined

import adventureworks.public.Flag
import doobie.postgres.Text
import doobie.util.Get
import doobie.util.Put
import io.circe.Decoder
import io.circe.Encoder
import typr.dsl.Bijection

/** Shared type `CurrentFlag`
 * Generated from TypeDefinitions matching
 */
case class CurrentFlag(value: Flag) extends scala.AnyVal

object CurrentFlag {
  given arrayGet: Get[Array[CurrentFlag]] = Flag.arrayGet.map(_.map(CurrentFlag.apply))

  given arrayPut: Put[Array[CurrentFlag]] = Flag.arrayPut.contramap(_.map(_.value))

  given bijection: Bijection[CurrentFlag, Flag] = Bijection.apply[CurrentFlag, Flag](_.value)(CurrentFlag.apply)

  given decoder: Decoder[CurrentFlag] = Flag.decoder.map(CurrentFlag.apply)

  given encoder: Encoder[CurrentFlag] = Flag.encoder.contramap(_.value)

  given get: Get[CurrentFlag] = Flag.get.map(CurrentFlag.apply)

  given pgText: Text[CurrentFlag] = {
    new Text[CurrentFlag] {
      override def unsafeEncode(v: CurrentFlag, sb: StringBuilder): Unit = Flag.pgText.unsafeEncode(v.value, sb)
      override def unsafeArrayEncode(v: CurrentFlag, sb: StringBuilder): Unit = Flag.pgText.unsafeArrayEncode(v.value, sb)
    }
  }

  given put: Put[CurrentFlag] = Flag.put.contramap(_.value)
}