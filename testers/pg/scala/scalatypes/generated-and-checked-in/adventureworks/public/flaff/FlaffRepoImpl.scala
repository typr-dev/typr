/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.flaff

import adventureworks.public.ShortText
import dev.typr.dslsc.DeleteBuilder
import dev.typr.dslsc.Dialect
import dev.typr.dslsc.SelectBuilder
import dev.typr.dslsc.UpdateBuilder
import dev.typr.foundationssc.Connection
import dev.typr.foundationssc.ConnectionRead
import dev.typr.foundationssc.Fragment
import dev.typr.foundationssc.PgTypes
import dev.typr.foundationssc.StreamingInsert
import dev.typr.foundationssc.Fragment.sql

class FlaffRepoImpl extends FlaffRepo {
  override def delete: DeleteBuilder[FlaffFields, FlaffRow] = DeleteBuilder.of(""""public"."flaff"""", FlaffFields.structure, Dialect.POSTGRESQL)

  override def deleteById(compositeId: FlaffId)(using c: Connection): Boolean = sql"""delete from "public"."flaff" where "code" = ${Fragment.encode(ShortText.pgType, compositeId.code)} AND "another_code" = ${Fragment.encode(PgTypes.text, compositeId.anotherCode)} AND "some_number" = ${Fragment.encode(PgTypes.int4, compositeId.someNumber)} AND "specifier" = ${Fragment.encode(ShortText.pgType, compositeId.specifier)}""".update().run(using c) > 0

  override def deleteByIds(compositeIds: List[FlaffId])(using c: Connection): Int = {
    val code: List[ShortText] = compositeIds.map(_.code).toList
    val anotherCode: List[String] = compositeIds.map(_.anotherCode).toList
    val someNumber: List[Int] = compositeIds.map(_.someNumber).toList
    val specifier: List[ShortText] = compositeIds.map(_.specifier).toList
    return sql"""delete
    from "public"."flaff"
    where ("code", "another_code", "some_number", "specifier")
    in (select * from unnest(${Fragment.encode(ShortText.pgType.array, code)}, ${Fragment.encode(PgTypes.text.array, anotherCode)}, ${Fragment.encode(PgTypes.int4.array, someNumber)}, ${Fragment.encode(ShortText.pgType.array, specifier)}))
    """.update().run(using c)
  }

  override def insert(unsaved: FlaffRow)(using c: Connection): FlaffRow = {
  sql"""insert into "public"."flaff"("code", "another_code", "some_number", "specifier", "parentspecifier")
    values (${Fragment.encode(ShortText.pgType, unsaved.code)}::text, ${Fragment.encode(PgTypes.text, unsaved.anotherCode)}, ${Fragment.encode(PgTypes.int4, unsaved.someNumber)}::int4, ${Fragment.encode(ShortText.pgType, unsaved.specifier)}::text, ${Fragment.encode(ShortText.pgType.opt, unsaved.parentspecifier)}::text)
    RETURNING "code", "another_code", "some_number", "specifier", "parentspecifier"
    """
    .updateReturning(FlaffRow.rowCodec.exactlyOne()).run(using c)
  }

  override def insertStreaming(
    unsaved: Iterator[FlaffRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = StreamingInsert.of(s"""COPY "public"."flaff"("code", "another_code", "some_number", "specifier", "parentspecifier") FROM STDIN""", batchSize, unsaved, FlaffRow.pgText).run(using c)

  override def select: SelectBuilder[FlaffFields, FlaffRow] = SelectBuilder.of(""""public"."flaff"""", FlaffFields.structure, FlaffRow.rowCodec, Dialect.POSTGRESQL)

  override def selectAll(using c: ConnectionRead): List[FlaffRow] = {
    sql"""select "code", "another_code", "some_number", "specifier", "parentspecifier"
    from "public"."flaff"
    """.query(FlaffRow.rowCodec.all()).run(using c)
  }

  override def selectById(compositeId: FlaffId)(using c: ConnectionRead): Option[FlaffRow] = {
    sql"""select "code", "another_code", "some_number", "specifier", "parentspecifier"
    from "public"."flaff"
    where "code" = ${Fragment.encode(ShortText.pgType, compositeId.code)} AND "another_code" = ${Fragment.encode(PgTypes.text, compositeId.anotherCode)} AND "some_number" = ${Fragment.encode(PgTypes.int4, compositeId.someNumber)} AND "specifier" = ${Fragment.encode(ShortText.pgType, compositeId.specifier)}""".query(FlaffRow.rowCodec.first()).run(using c)
  }

  override def selectByIds(compositeIds: List[FlaffId])(using c: ConnectionRead): List[FlaffRow] = {
    val code: List[ShortText] = compositeIds.map(_.code).toList
    val anotherCode: List[String] = compositeIds.map(_.anotherCode).toList
    val someNumber: List[Int] = compositeIds.map(_.someNumber).toList
    val specifier: List[ShortText] = compositeIds.map(_.specifier).toList
    return sql"""select "code", "another_code", "some_number", "specifier", "parentspecifier"
    from "public"."flaff"
    where ("code", "another_code", "some_number", "specifier")
    in (select * from unnest(${Fragment.encode(ShortText.pgType.array, code)}, ${Fragment.encode(PgTypes.text.array, anotherCode)}, ${Fragment.encode(PgTypes.int4.array, someNumber)}, ${Fragment.encode(ShortText.pgType.array, specifier)}))
    """.query(FlaffRow.rowCodec.all()).run(using c)
  }

  override def selectByIdsTracked(compositeIds: List[FlaffId])(using c: ConnectionRead): Map[FlaffId, FlaffRow] = {
    val ret: scala.collection.mutable.Map[FlaffId, FlaffRow] = scala.collection.mutable.Map.empty[FlaffId, FlaffRow]
    selectByIds(compositeIds)(using c).foreach(row => ret.put(row.compositeId, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[FlaffFields, FlaffRow] = UpdateBuilder.of(""""public"."flaff"""", FlaffFields.structure, FlaffRow.rowCodec, Dialect.POSTGRESQL)

  override def update(row: FlaffRow)(using c: Connection): Boolean = {
    val compositeId: FlaffId = row.compositeId
    return sql"""update "public"."flaff"
    set "parentspecifier" = ${Fragment.encode(ShortText.pgType.opt, row.parentspecifier)}::text
    where "code" = ${Fragment.encode(ShortText.pgType, compositeId.code)} AND "another_code" = ${Fragment.encode(PgTypes.text, compositeId.anotherCode)} AND "some_number" = ${Fragment.encode(PgTypes.int4, compositeId.someNumber)} AND "specifier" = ${Fragment.encode(ShortText.pgType, compositeId.specifier)}""".update().run(using c) > 0
  }

  override def upsert(unsaved: FlaffRow)(using c: Connection): FlaffRow = {
  sql"""insert into "public"."flaff"("code", "another_code", "some_number", "specifier", "parentspecifier")
    values (${Fragment.encode(ShortText.pgType, unsaved.code)}::text, ${Fragment.encode(PgTypes.text, unsaved.anotherCode)}, ${Fragment.encode(PgTypes.int4, unsaved.someNumber)}::int4, ${Fragment.encode(ShortText.pgType, unsaved.specifier)}::text, ${Fragment.encode(ShortText.pgType.opt, unsaved.parentspecifier)}::text)
    on conflict ("code", "another_code", "some_number", "specifier")
    do update set
      "parentspecifier" = EXCLUDED."parentspecifier"
    returning "code", "another_code", "some_number", "specifier", "parentspecifier""""
    .updateReturning(FlaffRow.rowCodec.exactlyOne())
    .run(using c)
  }

  override def upsertBatch(unsaved: Iterator[FlaffRow])(using c: Connection): List[FlaffRow] = {
    sql"""insert into "public"."flaff"("code", "another_code", "some_number", "specifier", "parentspecifier")
    values (?::text, ?, ?::int4, ?::text, ?::text)
    on conflict ("code", "another_code", "some_number", "specifier")
    do update set
      "parentspecifier" = EXCLUDED."parentspecifier"
    returning "code", "another_code", "some_number", "specifier", "parentspecifier""""
      .updateManyReturning(FlaffRow.rowCodec, unsaved)
    .run(using c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[FlaffRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    sql"""create temporary table flaff_TEMP (like "public"."flaff") on commit drop""".update().run(using c): @scala.annotation.nowarn
    StreamingInsert.of(s"""copy flaff_TEMP("code", "another_code", "some_number", "specifier", "parentspecifier") from stdin""", batchSize, unsaved, FlaffRow.pgText).run(using c): @scala.annotation.nowarn
    return sql"""insert into "public"."flaff"("code", "another_code", "some_number", "specifier", "parentspecifier")
    select * from flaff_TEMP
    on conflict ("code", "another_code", "some_number", "specifier")
    do update set
      "parentspecifier" = EXCLUDED."parentspecifier"
    ;
    drop table flaff_TEMP;""".update().run(using c)
  }
}