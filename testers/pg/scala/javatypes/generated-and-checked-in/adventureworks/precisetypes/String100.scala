/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.precisetypes

import com.fasterxml.jackson.annotation.JsonValue
import dev.typr.foundations.PgType
import dev.typr.foundations.PgTypes
import dev.typr.foundations.data.precise.StringN
import dev.typr.foundations.dsl.Bijection
import java.lang.IllegalArgumentException
import java.util.Optional

case class String100 private(@JsonValue value: String) extends StringN {
  override def rawValue: String = value

  override def maxLength: scala.Int = 100

  override def semanticEquals(other: StringN): scala.Boolean = (if (other == null) false else value == other.rawValue())

  override def semanticHashCode: scala.Int = value.hashCode()

  override def equals(that: Any): scala.Boolean = (this eq that.asInstanceOf[AnyRef]) || (that match { case other: StringN => value == other.rawValue(); case _ => false })

  override def hashCode: scala.Int = value.hashCode()
}

object String100 {
  given bijection: Bijection[String100, String] = Bijection.apply[String100, String](_.value)(String100.apply)

  given dbType: PgType[String100] = PgTypes.text.bimap(String100.apply, _.value)

  given dbTypeArray: PgType[Array[String100]] = PgTypes.textArray.bimap(xs => xs.map(String100.apply), xs => xs.map(_.value))

  def of(value: String): Optional[String100] = (if (value.length <= 100) Optional.of(new String100(value)) else Optional.empty())

  def truncate(value: String): String100 = new String100((if (value.length <= 100) value else value.substring(0, 100)))

  def unsafeForce(value: String): String100 = {
    if (value.length > 100) {
      throw new IllegalArgumentException(s"Value length ${value.length} exceeds maximum 100")
    }
    return new String100(value)
  }
}