/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.precisetypes

import com.fasterxml.jackson.annotation.JsonValue
import dev.typr.foundations.PgType
import dev.typr.foundations.PgTypes
import dev.typr.foundations.data.precise.PaddedStringN
import dev.typr.foundations.dsl.Bijection
import java.lang.IllegalArgumentException
import java.util.Optional

case class PaddedString10 private(@JsonValue value: String) extends PaddedStringN {
  override def rawValue: String = value

  override def length: scala.Int = 10

  override def trimmed: String = value.stripTrailing()

  override def semanticEquals(other: PaddedStringN): scala.Boolean = (if (other == null) false else trimmed() == other.trimmed())

  override def semanticHashCode: scala.Int = trimmed().hashCode()

  override def equals(that: Any): scala.Boolean = (this eq that.asInstanceOf[AnyRef]) || (that match { case other: PaddedStringN => trimmed() == other.trimmed(); case _ => false })

  override def hashCode: scala.Int = trimmed().hashCode()
}

object PaddedString10 {
  given bijection: Bijection[PaddedString10, String] = Bijection.apply[PaddedString10, String](_.value)(PaddedString10.apply)

  given dbType: PgType[PaddedString10] = PgTypes.bpchar.bimap(PaddedString10.apply, _.value)

  given dbTypeArray: PgType[Array[PaddedString10]] = PgTypes.bpcharArray.bimap(xs => xs.map(PaddedString10.apply), xs => xs.map(_.value))

  def of(value: String): Optional[PaddedString10] = (if (value.length <= 10) Optional.of(new PaddedString10(String.format("%-10s", value))) else Optional.empty())

  def unsafeForce(value: String): PaddedString10 = {
    if (value.length > 10) {
      throw new IllegalArgumentException(s"Value length ${value.length} exceeds fixed length 10")
    }
    return new PaddedString10(String.format("%-10s", value))
  }
}