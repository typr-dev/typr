/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.title

import dev.typr.dsl.DeleteBuilder
import dev.typr.dsl.DeleteBuilderMock
import dev.typr.dsl.DeleteParams
import dev.typr.dsl.SelectBuilder
import dev.typr.dsl.SelectBuilderMock
import dev.typr.dsl.SelectParams
import dev.typr.dsl.UpdateBuilder
import dev.typr.dsl.UpdateBuilderMock
import dev.typr.dsl.UpdateParams
import dev.typr.foundations.Connection
import dev.typr.foundations.ConnectionRead
import java.lang.RuntimeException
import java.util.ArrayList
import java.util.HashMap
import java.util.Optional
import java.util.function.Function
import java.util.stream.Collectors

case class TitleRepoMock(map: HashMap[TitleId, TitleRow] = new HashMap[TitleId, TitleRow]()) extends TitleRepo {
  override def delete: DeleteBuilder[TitleFields, TitleRow] = DeleteBuilderMock(TitleFields.structure, () => new ArrayList(map.values()), DeleteParams.empty(), row => row.code, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(code: TitleId)(using c: Connection): java.lang.Boolean = Optional.ofNullable(map.remove(code)).isPresent()

  override def deleteByIds(codes: java.util.List[TitleId])(using c: Connection): Integer = {
    var count = 0
    codes.forEach { id => if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: TitleRow)(using c: Connection): TitleRow = {
    if (map.containsKey(unsaved.code)) {
      throw new RuntimeException(s"id ${unsaved.code} already exists")
    }
    map.put(unsaved.code, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insertStreaming(
    unsaved: java.util.Iterator[TitleRow],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.code, row): @scala.annotation.nowarn
      count = count + 1L
    }
    return count
  }

  override def select: SelectBuilder[TitleFields, TitleRow] = SelectBuilderMock(TitleFields.structure, () => new ArrayList(map.values()), SelectParams.empty())

  override def selectAll(using c: ConnectionRead): java.util.List[TitleRow] = new ArrayList(map.values())

  override def selectById(code: TitleId)(using c: ConnectionRead): Optional[TitleRow] = Optional.ofNullable(map.get(code))

  override def selectByIds(codes: java.util.List[TitleId])(using c: ConnectionRead): java.util.List[TitleRow] = {
    val result = new ArrayList[TitleRow]()
    codes.forEach { id => val opt = Optional.ofNullable(map.get(id)); if (opt.isPresent()) {
      result.add(opt.get()): @scala.annotation.nowarn
    } }
    return result
  }

  override def selectByIdsTracked(codes: java.util.List[TitleId])(using c: ConnectionRead): java.util.Map[TitleId, TitleRow] = selectByIds(codes)(using c).stream().collect(Collectors.toMap((row: TitleRow) => row.code, Function.identity()))

  override def update: UpdateBuilder[TitleFields, TitleRow] = UpdateBuilderMock(TitleFields.structure, () => new ArrayList(map.values()), UpdateParams.empty(), row => row)

  override def upsert(unsaved: TitleRow)(using c: Connection): TitleRow = {
    map.put(unsaved.code, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: java.util.Iterator[TitleRow])(using c: Connection): java.util.List[TitleRow] = {
    val result = new ArrayList[TitleRow]()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.code, row): @scala.annotation.nowarn
      result.add(row): @scala.annotation.nowarn
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: java.util.Iterator[TitleRow],
    batchSize: Integer = 10000
  )(using c: Connection): Integer = {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.code, row): @scala.annotation.nowarn
      count = count + 1
    }
    return count
  }
}