/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.issue142_2

import adventureworks.public.issue142.Issue142Id
import dev.typr.dsl.DeleteBuilder
import dev.typr.dsl.DeleteBuilderMock
import dev.typr.dsl.DeleteParams
import dev.typr.dsl.SelectBuilder
import dev.typr.dsl.SelectBuilderMock
import dev.typr.dsl.SelectParams
import dev.typr.dsl.UpdateBuilder
import dev.typr.dsl.UpdateBuilderMock
import dev.typr.dsl.UpdateParams
import dev.typr.foundations.Connection
import dev.typr.foundations.ConnectionRead
import java.lang.RuntimeException
import java.util.ArrayList
import java.util.HashMap
import java.util.Optional
import java.util.function.Function
import java.util.stream.Collectors

case class Issue1422RepoMock(map: HashMap[Issue142Id, Issue1422Row] = new HashMap[Issue142Id, Issue1422Row]()) extends Issue1422Repo {
  override def delete: DeleteBuilder[Issue1422Fields, Issue1422Row] = DeleteBuilderMock(Issue1422Fields.structure, () => new ArrayList(map.values()), DeleteParams.empty(), row => row.tabellkode, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(tabellkode: Issue142Id)(using c: Connection): java.lang.Boolean = Optional.ofNullable(map.remove(tabellkode)).isPresent()

  override def deleteByIds(tabellkodes: java.util.List[Issue142Id])(using c: Connection): Integer = {
    var count = 0
    tabellkodes.forEach { id => if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: Issue1422Row)(using c: Connection): Issue1422Row = {
    if (map.containsKey(unsaved.tabellkode)) {
      throw new RuntimeException(s"id ${unsaved.tabellkode} already exists")
    }
    map.put(unsaved.tabellkode, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insertStreaming(
    unsaved: java.util.Iterator[Issue1422Row],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.tabellkode, row): @scala.annotation.nowarn
      count = count + 1L
    }
    return count
  }

  override def select: SelectBuilder[Issue1422Fields, Issue1422Row] = SelectBuilderMock(Issue1422Fields.structure, () => new ArrayList(map.values()), SelectParams.empty())

  override def selectAll(using c: ConnectionRead): java.util.List[Issue1422Row] = new ArrayList(map.values())

  override def selectById(tabellkode: Issue142Id)(using c: ConnectionRead): Optional[Issue1422Row] = Optional.ofNullable(map.get(tabellkode))

  override def selectByIds(tabellkodes: java.util.List[Issue142Id])(using c: ConnectionRead): java.util.List[Issue1422Row] = {
    val result = new ArrayList[Issue1422Row]()
    tabellkodes.forEach { id => val opt = Optional.ofNullable(map.get(id)); if (opt.isPresent()) {
      result.add(opt.get()): @scala.annotation.nowarn
    } }
    return result
  }

  override def selectByIdsTracked(tabellkodes: java.util.List[Issue142Id])(using c: ConnectionRead): java.util.Map[Issue142Id, Issue1422Row] = selectByIds(tabellkodes)(using c).stream().collect(Collectors.toMap((row: Issue1422Row) => row.tabellkode, Function.identity()))

  override def update: UpdateBuilder[Issue1422Fields, Issue1422Row] = UpdateBuilderMock(Issue1422Fields.structure, () => new ArrayList(map.values()), UpdateParams.empty(), row => row)

  override def upsert(unsaved: Issue1422Row)(using c: Connection): Issue1422Row = {
    map.put(unsaved.tabellkode, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: java.util.Iterator[Issue1422Row])(using c: Connection): java.util.List[Issue1422Row] = {
    val result = new ArrayList[Issue1422Row]()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.tabellkode, row): @scala.annotation.nowarn
      result.add(row): @scala.annotation.nowarn
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: java.util.Iterator[Issue1422Row],
    batchSize: Integer = 10000
  )(using c: Connection): Integer = {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.tabellkode, row): @scala.annotation.nowarn
      count = count + 1
    }
    return count
  }
}