/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.product

import dev.typr.foundations.kotlin.DeleteBuilder
import dev.typr.foundations.kotlin.DeleteBuilderMock
import dev.typr.foundations.kotlin.DeleteParams
import dev.typr.foundations.kotlin.SelectBuilder
import dev.typr.foundations.kotlin.SelectBuilderMock
import dev.typr.foundations.kotlin.SelectParams
import dev.typr.foundations.kotlin.UpdateBuilder
import dev.typr.foundations.kotlin.UpdateBuilderMock
import dev.typr.foundations.kotlin.UpdateParams
import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap

data class ProductRepoMock(
  val toRow: (ProductRowUnsaved) -> ProductRow,
  val map: MutableMap<ProductId, ProductRow> = mutableMapOf<ProductId, ProductRow>()
) : ProductRepo {
  override fun delete(): DeleteBuilder<ProductFields, ProductRow> = DeleteBuilderMock(ProductFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.productid }, { id -> map.remove(id) })

  override fun deleteById(
    productid: ProductId,
    c: Connection
  ): Boolean = map.remove(productid) != null

  override fun deleteByIds(
    productids: Array<ProductId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in productids) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: ProductRow,
    c: Connection
  ): ProductRow {
    if (map.containsKey(unsaved.productid)) {
      throw RuntimeException("id " + unsaved.productid + " already exists")
    }
    map[unsaved.productid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: ProductRowUnsaved,
    c: Connection
  ): ProductRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<ProductRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.productid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<ProductRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.productid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<ProductFields, ProductRow> = SelectBuilderMock(ProductFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<ProductRow> = map.values.toList()

  override fun selectById(
    productid: ProductId,
    c: Connection
  ): ProductRow? = map[productid]

  override fun selectByIds(
    productids: Array<ProductId>,
    c: Connection
  ): List<ProductRow> {
    val result = ArrayList<ProductRow>()
    for (id in productids) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    productids: Array<ProductId>,
    c: Connection
  ): Map<ProductId, ProductRow> = selectByIds(productids, c).associateBy({ row: ProductRow -> row.productid })

  override fun update(): UpdateBuilder<ProductFields, ProductRow> = UpdateBuilderMock(ProductFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: ProductRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.productid]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.productid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: ProductRow,
    c: Connection
  ): ProductRow {
    map[unsaved.productid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<ProductRow>,
    c: Connection
  ): List<ProductRow> {
    val result = ArrayList<ProductRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.productid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<ProductRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.productid] = row
      count = count + 1
    }
    return count
  }
}