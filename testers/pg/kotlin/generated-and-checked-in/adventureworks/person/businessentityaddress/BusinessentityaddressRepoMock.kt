/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.businessentityaddress

import dev.typr.foundations.kotlin.DeleteBuilder
import dev.typr.foundations.kotlin.DeleteBuilderMock
import dev.typr.foundations.kotlin.DeleteParams
import dev.typr.foundations.kotlin.SelectBuilder
import dev.typr.foundations.kotlin.SelectBuilderMock
import dev.typr.foundations.kotlin.SelectParams
import dev.typr.foundations.kotlin.UpdateBuilder
import dev.typr.foundations.kotlin.UpdateBuilderMock
import dev.typr.foundations.kotlin.UpdateParams
import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap

data class BusinessentityaddressRepoMock(
  val toRow: (BusinessentityaddressRowUnsaved) -> BusinessentityaddressRow,
  val map: MutableMap<BusinessentityaddressId, BusinessentityaddressRow> = mutableMapOf<BusinessentityaddressId, BusinessentityaddressRow>()
) : BusinessentityaddressRepo {
  override fun delete(): DeleteBuilder<BusinessentityaddressFields, BusinessentityaddressRow> = DeleteBuilderMock(BusinessentityaddressFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.compositeId() }, { id -> map.remove(id) })

  override fun deleteById(
    compositeId: BusinessentityaddressId,
    c: Connection
  ): Boolean = map.remove(compositeId) != null

  override fun deleteByIds(
    compositeIds: Array<BusinessentityaddressId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in compositeIds) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: BusinessentityaddressRow,
    c: Connection
  ): BusinessentityaddressRow {
    if (map.containsKey(unsaved.compositeId())) {
      throw RuntimeException("id " + unsaved.compositeId() + " already exists")
    }
    map[unsaved.compositeId()] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: BusinessentityaddressRowUnsaved,
    c: Connection
  ): BusinessentityaddressRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<BusinessentityaddressRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<BusinessentityaddressRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.compositeId()] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<BusinessentityaddressFields, BusinessentityaddressRow> = SelectBuilderMock(BusinessentityaddressFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<BusinessentityaddressRow> = map.values.toList()

  override fun selectById(
    compositeId: BusinessentityaddressId,
    c: Connection
  ): BusinessentityaddressRow? = map[compositeId]

  override fun selectByIds(
    compositeIds: Array<BusinessentityaddressId>,
    c: Connection
  ): List<BusinessentityaddressRow> {
    val result = ArrayList<BusinessentityaddressRow>()
    for (id in compositeIds) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    compositeIds: Array<BusinessentityaddressId>,
    c: Connection
  ): Map<BusinessentityaddressId, BusinessentityaddressRow> = selectByIds(compositeIds, c).associateBy({ row: BusinessentityaddressRow -> row.compositeId() })

  override fun update(): UpdateBuilder<BusinessentityaddressFields, BusinessentityaddressRow> = UpdateBuilderMock(BusinessentityaddressFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: BusinessentityaddressRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.compositeId()]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.compositeId()] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: BusinessentityaddressRow,
    c: Connection
  ): BusinessentityaddressRow {
    map[unsaved.compositeId()] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<BusinessentityaddressRow>,
    c: Connection
  ): List<BusinessentityaddressRow> {
    val result = ArrayList<BusinessentityaddressRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<BusinessentityaddressRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      count = count + 1
    }
    return count
  }
}