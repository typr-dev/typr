/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.businessentity;

import java.lang.RuntimeException;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typr.dsl.DeleteBuilder;
import typr.dsl.DeleteBuilderMock;
import typr.dsl.DeleteParams;
import typr.dsl.SelectBuilder;
import typr.dsl.SelectBuilderMock;
import typr.dsl.SelectParams;
import typr.dsl.UpdateBuilder;
import typr.dsl.UpdateBuilderMock;
import typr.dsl.UpdateParams;

public record BusinessentityRepoMock(
  java.util.function.Function<BusinessentityRowUnsaved, BusinessentityRow> toRow,
  HashMap<BusinessentityId, BusinessentityRow> map
) implements BusinessentityRepo {
  public BusinessentityRepoMock(java.util.function.Function<BusinessentityRowUnsaved, BusinessentityRow> toRow) {
    this(toRow, new HashMap<BusinessentityId, BusinessentityRow>());
  };

  public BusinessentityRepoMock withToRow(java.util.function.Function<BusinessentityRowUnsaved, BusinessentityRow> toRow) {
    return new BusinessentityRepoMock(toRow, map);
  };

  public BusinessentityRepoMock withMap(HashMap<BusinessentityId, BusinessentityRow> map) {
    return new BusinessentityRepoMock(toRow, map);
  };

  @Override
  public DeleteBuilder<BusinessentityFields, BusinessentityRow> delete() {
    return new DeleteBuilderMock<>(BusinessentityFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.businessentityid(), id -> map.remove(id));
  };

  @Override
  public Boolean deleteById(
    BusinessentityId businessentityid,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(businessentityid)).isPresent();
  };

  @Override
  public Integer deleteByIds(
    BusinessentityId[] businessentityids,
    Connection c
  ) {
    var count = 0;
    for (var id : businessentityids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    };
    };
    return count;
  };

  @Override
  public BusinessentityRow insert(
    BusinessentityRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.businessentityid())) {
      throw new RuntimeException("id " + unsaved.businessentityid() + " already exists");
    };
    map.put(unsaved.businessentityid(), unsaved);
    return unsaved;
  };

  @Override
  public BusinessentityRow insert(
    BusinessentityRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  @Override
  public Long insertStreaming(
    Iterator<BusinessentityRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.businessentityid(), row);
      count = count + 1L;
    };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  @Override
  public Long insertUnsavedStreaming(
    Iterator<BusinessentityRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var unsavedRow = unsaved.next();
      var row = toRow.apply(unsavedRow);
      map.put(row.businessentityid(), row);
      count = count + 1L;
    };
    return count;
  };

  @Override
  public SelectBuilder<BusinessentityFields, BusinessentityRow> select() {
    return new SelectBuilderMock<>(BusinessentityFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  @Override
  public List<BusinessentityRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  @Override
  public Optional<BusinessentityRow> selectById(
    BusinessentityId businessentityid,
    Connection c
  ) {
    return Optional.ofNullable(map.get(businessentityid));
  };

  @Override
  public List<BusinessentityRow> selectByIds(
    BusinessentityId[] businessentityids,
    Connection c
  ) {
    var result = new ArrayList<BusinessentityRow>();
    for (var id : businessentityids) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    };
    };
    return result;
  };

  @Override
  public Map<BusinessentityId, BusinessentityRow> selectByIdsTracked(
    BusinessentityId[] businessentityids,
    Connection c
  ) {
    return selectByIds(businessentityids, c).stream().collect(Collectors.toMap((BusinessentityRow row) -> row.businessentityid(), Function.identity()));
  };

  @Override
  public UpdateBuilder<BusinessentityFields, BusinessentityRow> update() {
    return new UpdateBuilderMock<>(BusinessentityFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  @Override
  public Boolean update(
    BusinessentityRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.businessentityid())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.businessentityid(), row);;
    };
    return shouldUpdate;
  };

  @Override
  public BusinessentityRow upsert(
    BusinessentityRow unsaved,
    Connection c
  ) {
    map.put(unsaved.businessentityid(), unsaved);
    return unsaved;
  };

  @Override
  public List<BusinessentityRow> upsertBatch(
    Iterator<BusinessentityRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<BusinessentityRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.businessentityid(), row);
      result.add(row);
    };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
    Iterator<BusinessentityRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.businessentityid(), row);
      count = count + 1;
    };
    return count;
  };
}