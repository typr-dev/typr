/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.businessentityaddress;

import dev.typr.foundations.dsl.DeleteBuilder;
import dev.typr.foundations.dsl.DeleteBuilderMock;
import dev.typr.foundations.dsl.DeleteParams;
import dev.typr.foundations.dsl.SelectBuilder;
import dev.typr.foundations.dsl.SelectBuilderMock;
import dev.typr.foundations.dsl.SelectParams;
import dev.typr.foundations.dsl.UpdateBuilder;
import dev.typr.foundations.dsl.UpdateBuilderMock;
import dev.typr.foundations.dsl.UpdateParams;
import java.lang.RuntimeException;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;

public record BusinessentityaddressRepoMock(
  java.util.function.Function<BusinessentityaddressRowUnsaved, BusinessentityaddressRow> toRow,
  HashMap<BusinessentityaddressId, BusinessentityaddressRow> map
) implements BusinessentityaddressRepo {
  public BusinessentityaddressRepoMock(java.util.function.Function<BusinessentityaddressRowUnsaved, BusinessentityaddressRow> toRow) {
    this(toRow, new HashMap<BusinessentityaddressId, BusinessentityaddressRow>());
  };

  public BusinessentityaddressRepoMock withToRow(java.util.function.Function<BusinessentityaddressRowUnsaved, BusinessentityaddressRow> toRow) {
    return new BusinessentityaddressRepoMock(toRow, map);
  };

  public BusinessentityaddressRepoMock withMap(HashMap<BusinessentityaddressId, BusinessentityaddressRow> map) {
    return new BusinessentityaddressRepoMock(toRow, map);
  };

  @Override
  public DeleteBuilder<BusinessentityaddressFields, BusinessentityaddressRow> delete() {
    return new DeleteBuilderMock<>(BusinessentityaddressFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.compositeId(), id -> map.remove(id));
  };

  @Override
  public Boolean deleteById(
    BusinessentityaddressId compositeId,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(compositeId)).isPresent();
  };

  @Override
  public Integer deleteByIds(
    BusinessentityaddressId[] compositeIds,
    Connection c
  ) {
    var count = 0;
    for (var id : compositeIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    };
    };
    return count;
  };

  @Override
  public BusinessentityaddressRow insert(
    BusinessentityaddressRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.compositeId())) {
      throw new RuntimeException("id " + unsaved.compositeId() + " already exists");
    };
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  };

  @Override
  public BusinessentityaddressRow insert(
    BusinessentityaddressRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  @Override
  public Long insertStreaming(
    Iterator<BusinessentityaddressRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      count = count + 1L;
    };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  @Override
  public Long insertUnsavedStreaming(
    Iterator<BusinessentityaddressRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var unsavedRow = unsaved.next();
      var row = toRow.apply(unsavedRow);
      map.put(row.compositeId(), row);
      count = count + 1L;
    };
    return count;
  };

  @Override
  public SelectBuilder<BusinessentityaddressFields, BusinessentityaddressRow> select() {
    return new SelectBuilderMock<>(BusinessentityaddressFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  @Override
  public List<BusinessentityaddressRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  @Override
  public Optional<BusinessentityaddressRow> selectById(
    BusinessentityaddressId compositeId,
    Connection c
  ) {
    return Optional.ofNullable(map.get(compositeId));
  };

  @Override
  public List<BusinessentityaddressRow> selectByIds(
    BusinessentityaddressId[] compositeIds,
    Connection c
  ) {
    var result = new ArrayList<BusinessentityaddressRow>();
    for (var id : compositeIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    };
    };
    return result;
  };

  @Override
  public Map<BusinessentityaddressId, BusinessentityaddressRow> selectByIdsTracked(
    BusinessentityaddressId[] compositeIds,
    Connection c
  ) {
    return selectByIds(compositeIds, c).stream().collect(Collectors.toMap((BusinessentityaddressRow row) -> row.compositeId(), Function.identity()));
  };

  @Override
  public UpdateBuilder<BusinessentityaddressFields, BusinessentityaddressRow> update() {
    return new UpdateBuilderMock<>(BusinessentityaddressFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  @Override
  public Boolean update(
    BusinessentityaddressRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.compositeId())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.compositeId(), row);;
    };
    return shouldUpdate;
  };

  @Override
  public BusinessentityaddressRow upsert(
    BusinessentityaddressRow unsaved,
    Connection c
  ) {
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  };

  @Override
  public List<BusinessentityaddressRow> upsertBatch(
    Iterator<BusinessentityaddressRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<BusinessentityaddressRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      result.add(row);
    };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
    Iterator<BusinessentityaddressRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      count = count + 1;
    };
    return count;
  };
}