/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.countryregion;

import dev.typr.foundations.dsl.DeleteBuilder;
import dev.typr.foundations.dsl.DeleteBuilderMock;
import dev.typr.foundations.dsl.DeleteParams;
import dev.typr.foundations.dsl.SelectBuilder;
import dev.typr.foundations.dsl.SelectBuilderMock;
import dev.typr.foundations.dsl.SelectParams;
import dev.typr.foundations.dsl.UpdateBuilder;
import dev.typr.foundations.dsl.UpdateBuilderMock;
import dev.typr.foundations.dsl.UpdateParams;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;

public record CountryregionRepoMock(
    java.util.function.Function<CountryregionRowUnsaved, CountryregionRow> toRow,
    HashMap<CountryregionId, CountryregionRow> map)
    implements CountryregionRepo {
  public CountryregionRepoMock(
      java.util.function.Function<CountryregionRowUnsaved, CountryregionRow> toRow) {
    this(toRow, new HashMap<CountryregionId, CountryregionRow>());
  }
  ;

  public CountryregionRepoMock withToRow(
      java.util.function.Function<CountryregionRowUnsaved, CountryregionRow> toRow) {
    return new CountryregionRepoMock(toRow, map);
  }
  ;

  public CountryregionRepoMock withMap(HashMap<CountryregionId, CountryregionRow> map) {
    return new CountryregionRepoMock(toRow, map);
  }
  ;

  @Override
  public DeleteBuilder<CountryregionFields, CountryregionRow> delete() {
    return new DeleteBuilderMock<>(
        CountryregionFields.structure(),
        () -> new ArrayList<>(map.values()),
        DeleteParams.empty(),
        row -> row.countryregioncode(),
        id -> map.remove(id));
  }
  ;

  @Override
  public Boolean deleteById(CountryregionId countryregioncode, Connection c) {
    return Optional.ofNullable(map.remove(countryregioncode)).isPresent();
  }
  ;

  @Override
  public Integer deleteByIds(CountryregionId[] countryregioncodes, Connection c) {
    var count = 0;
    for (var id : countryregioncodes) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
        ;
      }
      ;
    }
    ;
    return count;
  }
  ;

  @Override
  public CountryregionRow insert(CountryregionRow unsaved, Connection c) {
    if (map.containsKey(unsaved.countryregioncode())) {
      throw new RuntimeException("id " + unsaved.countryregioncode() + " already exists");
    }
    ;
    map.put(unsaved.countryregioncode(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public CountryregionRow insert(CountryregionRowUnsaved unsaved, Connection c) {
    return insert(toRow.apply(unsaved), c);
  }
  ;

  @Override
  public Long insertStreaming(Iterator<CountryregionRow> unsaved, Integer batchSize, Connection c) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.countryregioncode(), row);
      count = count + 1L;
    }
    ;
    return count;
  }
  ;

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  @Override
  public Long insertUnsavedStreaming(
      Iterator<CountryregionRowUnsaved> unsaved, Integer batchSize, Connection c) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var unsavedRow = unsaved.next();
      var row = toRow.apply(unsavedRow);
      map.put(row.countryregioncode(), row);
      count = count + 1L;
    }
    ;
    return count;
  }
  ;

  @Override
  public SelectBuilder<CountryregionFields, CountryregionRow> select() {
    return new SelectBuilderMock<>(
        CountryregionFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  }
  ;

  @Override
  public List<CountryregionRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  }
  ;

  @Override
  public Optional<CountryregionRow> selectById(CountryregionId countryregioncode, Connection c) {
    return Optional.ofNullable(map.get(countryregioncode));
  }
  ;

  @Override
  public List<CountryregionRow> selectByIds(CountryregionId[] countryregioncodes, Connection c) {
    var result = new ArrayList<CountryregionRow>();
    for (var id : countryregioncodes) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
        result.add(opt.get());
      }
      ;
    }
    ;
    return result;
  }
  ;

  @Override
  public Map<CountryregionId, CountryregionRow> selectByIdsTracked(
      CountryregionId[] countryregioncodes, Connection c) {
    return selectByIds(countryregioncodes, c).stream()
        .collect(
            Collectors.toMap(
                (CountryregionRow row) -> row.countryregioncode(), Function.identity()));
  }
  ;

  @Override
  public UpdateBuilder<CountryregionFields, CountryregionRow> update() {
    return new UpdateBuilderMock<>(
        CountryregionFields.structure(),
        () -> new ArrayList<>(map.values()),
        UpdateParams.empty(),
        row -> row);
  }
  ;

  @Override
  public Boolean update(CountryregionRow row, Connection c) {
    var shouldUpdate =
        Optional.ofNullable(map.get(row.countryregioncode()))
            .filter(oldRow -> !oldRow.equals(row))
            .isPresent();
    if (shouldUpdate) {
      map.put(row.countryregioncode(), row);
      ;
    }
    ;
    return shouldUpdate;
  }
  ;

  @Override
  public CountryregionRow upsert(CountryregionRow unsaved, Connection c) {
    map.put(unsaved.countryregioncode(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public List<CountryregionRow> upsertBatch(Iterator<CountryregionRow> unsaved, Connection c) {
    var result = new ArrayList<CountryregionRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.countryregioncode(), row);
      result.add(row);
    }
    ;
    return result;
  }
  ;

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
      Iterator<CountryregionRow> unsaved, Integer batchSize, Connection c) {
    var count = 0;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.countryregioncode(), row);
      count = count + 1;
    }
    ;
    return count;
  }
  ;
}
