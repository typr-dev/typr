/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.emailaddress;

import static dev.typr.foundations.Fragment.interpolate;

import adventureworks.person.businessentity.BusinessentityId;
import dev.typr.foundations.Fragment;
import dev.typr.foundations.PgTypes;
import dev.typr.foundations.dsl.DeleteBuilder;
import dev.typr.foundations.dsl.Dialect;
import dev.typr.foundations.dsl.SelectBuilder;
import dev.typr.foundations.dsl.UpdateBuilder;
import dev.typr.foundations.internal.arrayMap;
import dev.typr.foundations.streamingInsert;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;

public class EmailaddressRepoImpl implements EmailaddressRepo {
  @Override
  public DeleteBuilder<EmailaddressFields, EmailaddressRow> delete() {
    return DeleteBuilder.of(
        "\"person\".\"emailaddress\"", EmailaddressFields.structure(), Dialect.POSTGRESQL);
  }
  ;

  @Override
  public Boolean deleteById(EmailaddressId compositeId, Connection c) {
    return interpolate(
                Fragment.lit(
                    "delete from \"person\".\"emailaddress\" where \"businessentityid\" = "),
                Fragment.encode(BusinessentityId.pgType, compositeId.businessentityid()),
                Fragment.lit(" AND \"emailaddressid\" = "),
                Fragment.encode(PgTypes.int4, compositeId.emailaddressid()),
                Fragment.lit(""))
            .update()
            .runUnchecked(c)
        > 0;
  }
  ;

  @Override
  public Integer deleteByIds(EmailaddressId[] compositeIds, Connection c) {
    BusinessentityId[] businessentityid =
        arrayMap.map(compositeIds, EmailaddressId::businessentityid, BusinessentityId.class);
    ;
    Integer[] emailaddressid =
        arrayMap.map(compositeIds, EmailaddressId::emailaddressid, Integer.class);
    ;
    return interpolate(
            Fragment.lit(
                "delete\n"
                    + "from \"person\".\"emailaddress\"\n"
                    + "where (\"businessentityid\", \"emailaddressid\")\n"
                    + "in (select * from unnest("),
            Fragment.encode(BusinessentityId.pgTypeArray, businessentityid),
            Fragment.lit(", "),
            Fragment.encode(PgTypes.int4Array, emailaddressid),
            Fragment.lit("))\n"))
        .update()
        .runUnchecked(c);
  }
  ;

  @Override
  public EmailaddressRow insert(EmailaddressRow unsaved, Connection c) {
    return interpolate(
            Fragment.lit(
                "insert into \"person\".\"emailaddress\"(\"businessentityid\", \"emailaddressid\","
                    + " \"emailaddress\", \"rowguid\", \"modifieddate\")\n"
                    + "values ("),
            Fragment.encode(BusinessentityId.pgType, unsaved.businessentityid()),
            Fragment.lit("::int4, "),
            Fragment.encode(PgTypes.int4, unsaved.emailaddressid()),
            Fragment.lit("::int4, "),
            Fragment.encode(PgTypes.text.opt(), unsaved.emailaddress()),
            Fragment.lit(", "),
            Fragment.encode(PgTypes.uuid, unsaved.rowguid()),
            Fragment.lit("::uuid, "),
            Fragment.encode(PgTypes.timestamp, unsaved.modifieddate()),
            Fragment.lit(
                "::timestamp)\n"
                    + "RETURNING \"businessentityid\", \"emailaddressid\", \"emailaddress\","
                    + " \"rowguid\", \"modifieddate\"\n"))
        .updateReturning(EmailaddressRow._rowParser.exactlyOne())
        .runUnchecked(c);
  }
  ;

  @Override
  public EmailaddressRow insert(EmailaddressRowUnsaved unsaved, Connection c) {
    ArrayList<Fragment> columns = new ArrayList<>();
    ;
    ArrayList<Fragment> values = new ArrayList<>();
    ;
    columns.add(Fragment.lit("\"businessentityid\""));
    values.add(
        interpolate(
            Fragment.encode(BusinessentityId.pgType, unsaved.businessentityid()),
            Fragment.lit("::int4")));
    columns.add(Fragment.lit("\"emailaddress\""));
    values.add(
        interpolate(Fragment.encode(PgTypes.text.opt(), unsaved.emailaddress()), Fragment.lit("")));
    unsaved
        .emailaddressid()
        .visit(
            () -> {},
            value -> {
              columns.add(Fragment.lit("\"emailaddressid\""));
              values.add(interpolate(Fragment.encode(PgTypes.int4, value), Fragment.lit("::int4")));
            });
    ;
    unsaved
        .rowguid()
        .visit(
            () -> {},
            value -> {
              columns.add(Fragment.lit("\"rowguid\""));
              values.add(interpolate(Fragment.encode(PgTypes.uuid, value), Fragment.lit("::uuid")));
            });
    ;
    unsaved
        .modifieddate()
        .visit(
            () -> {},
            value -> {
              columns.add(Fragment.lit("\"modifieddate\""));
              values.add(
                  interpolate(
                      Fragment.encode(PgTypes.timestamp, value), Fragment.lit("::timestamp")));
            });
    ;
    Fragment q =
        interpolate(
            Fragment.lit("insert into \"person\".\"emailaddress\"("),
            Fragment.comma(columns),
            Fragment.lit(")\nvalues ("),
            Fragment.comma(values),
            Fragment.lit(
                ")\n"
                    + "RETURNING \"businessentityid\", \"emailaddressid\", \"emailaddress\","
                    + " \"rowguid\", \"modifieddate\"\n"));
    ;
    return q.updateReturning(EmailaddressRow._rowParser.exactlyOne()).runUnchecked(c);
  }
  ;

  @Override
  public Long insertStreaming(Iterator<EmailaddressRow> unsaved, Integer batchSize, Connection c) {
    return streamingInsert.insertUnchecked(
        "COPY \"person\".\"emailaddress\"(\"businessentityid\", \"emailaddressid\","
            + " \"emailaddress\", \"rowguid\", \"modifieddate\") FROM STDIN",
        batchSize,
        unsaved,
        c,
        EmailaddressRow.pgText);
  }
  ;

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  @Override
  public Long insertUnsavedStreaming(
      Iterator<EmailaddressRowUnsaved> unsaved, Integer batchSize, Connection c) {
    return streamingInsert.insertUnchecked(
        "COPY \"person\".\"emailaddress\"(\"businessentityid\", \"emailaddress\","
            + " \"emailaddressid\", \"rowguid\", \"modifieddate\") FROM STDIN (DEFAULT"
            + " '__DEFAULT_VALUE__')",
        batchSize,
        unsaved,
        c,
        EmailaddressRowUnsaved.pgText);
  }
  ;

  @Override
  public SelectBuilder<EmailaddressFields, EmailaddressRow> select() {
    return SelectBuilder.of(
        "\"person\".\"emailaddress\"",
        EmailaddressFields.structure(),
        EmailaddressRow._rowParser,
        Dialect.POSTGRESQL);
  }
  ;

  @Override
  public List<EmailaddressRow> selectAll(Connection c) {
    return interpolate(
            Fragment.lit(
                "select \"businessentityid\", \"emailaddressid\", \"emailaddress\", \"rowguid\","
                    + " \"modifieddate\"\n"
                    + "from \"person\".\"emailaddress\"\n"))
        .query(EmailaddressRow._rowParser.all())
        .runUnchecked(c);
  }
  ;

  @Override
  public Optional<EmailaddressRow> selectById(EmailaddressId compositeId, Connection c) {
    return interpolate(
            Fragment.lit(
                "select \"businessentityid\", \"emailaddressid\", \"emailaddress\", \"rowguid\","
                    + " \"modifieddate\"\n"
                    + "from \"person\".\"emailaddress\"\n"
                    + "where \"businessentityid\" = "),
            Fragment.encode(BusinessentityId.pgType, compositeId.businessentityid()),
            Fragment.lit(" AND \"emailaddressid\" = "),
            Fragment.encode(PgTypes.int4, compositeId.emailaddressid()),
            Fragment.lit(""))
        .query(EmailaddressRow._rowParser.first())
        .runUnchecked(c);
  }
  ;

  @Override
  public List<EmailaddressRow> selectByIds(EmailaddressId[] compositeIds, Connection c) {
    BusinessentityId[] businessentityid =
        arrayMap.map(compositeIds, EmailaddressId::businessentityid, BusinessentityId.class);
    ;
    Integer[] emailaddressid =
        arrayMap.map(compositeIds, EmailaddressId::emailaddressid, Integer.class);
    ;
    return interpolate(
            Fragment.lit(
                "select \"businessentityid\", \"emailaddressid\", \"emailaddress\", \"rowguid\","
                    + " \"modifieddate\"\n"
                    + "from \"person\".\"emailaddress\"\n"
                    + "where (\"businessentityid\", \"emailaddressid\")\n"
                    + "in (select * from unnest("),
            Fragment.encode(BusinessentityId.pgTypeArray, businessentityid),
            Fragment.lit(", "),
            Fragment.encode(PgTypes.int4Array, emailaddressid),
            Fragment.lit("))\n"))
        .query(EmailaddressRow._rowParser.all())
        .runUnchecked(c);
  }
  ;

  @Override
  public Map<EmailaddressId, EmailaddressRow> selectByIdsTracked(
      EmailaddressId[] compositeIds, Connection c) {
    HashMap<EmailaddressId, EmailaddressRow> ret = new HashMap<EmailaddressId, EmailaddressRow>();
    selectByIds(compositeIds, c).forEach(row -> ret.put(row.compositeId(), row));
    return ret;
  }
  ;

  @Override
  public UpdateBuilder<EmailaddressFields, EmailaddressRow> update() {
    return UpdateBuilder.of(
        "\"person\".\"emailaddress\"",
        EmailaddressFields.structure(),
        EmailaddressRow._rowParser,
        Dialect.POSTGRESQL);
  }
  ;

  @Override
  public Boolean update(EmailaddressRow row, Connection c) {
    EmailaddressId compositeId = row.compositeId();
    ;
    return interpolate(
                Fragment.lit("update \"person\".\"emailaddress\"\nset \"emailaddress\" = "),
                Fragment.encode(PgTypes.text.opt(), row.emailaddress()),
                Fragment.lit(",\n\"rowguid\" = "),
                Fragment.encode(PgTypes.uuid, row.rowguid()),
                Fragment.lit("::uuid,\n\"modifieddate\" = "),
                Fragment.encode(PgTypes.timestamp, row.modifieddate()),
                Fragment.lit("::timestamp\nwhere \"businessentityid\" = "),
                Fragment.encode(BusinessentityId.pgType, compositeId.businessentityid()),
                Fragment.lit(" AND \"emailaddressid\" = "),
                Fragment.encode(PgTypes.int4, compositeId.emailaddressid()),
                Fragment.lit(""))
            .update()
            .runUnchecked(c)
        > 0;
  }
  ;

  @Override
  public EmailaddressRow upsert(EmailaddressRow unsaved, Connection c) {
    return interpolate(
            Fragment.lit(
                "insert into \"person\".\"emailaddress\"(\"businessentityid\", \"emailaddressid\","
                    + " \"emailaddress\", \"rowguid\", \"modifieddate\")\n"
                    + "values ("),
            Fragment.encode(BusinessentityId.pgType, unsaved.businessentityid()),
            Fragment.lit("::int4, "),
            Fragment.encode(PgTypes.int4, unsaved.emailaddressid()),
            Fragment.lit("::int4, "),
            Fragment.encode(PgTypes.text.opt(), unsaved.emailaddress()),
            Fragment.lit(", "),
            Fragment.encode(PgTypes.uuid, unsaved.rowguid()),
            Fragment.lit("::uuid, "),
            Fragment.encode(PgTypes.timestamp, unsaved.modifieddate()),
            Fragment.lit(
                "::timestamp)\n"
                    + "on conflict (\"businessentityid\", \"emailaddressid\")\n"
                    + "do update set\n"
                    + "  \"emailaddress\" = EXCLUDED.\"emailaddress\",\n"
                    + "\"rowguid\" = EXCLUDED.\"rowguid\",\n"
                    + "\"modifieddate\" = EXCLUDED.\"modifieddate\"\n"
                    + "returning \"businessentityid\", \"emailaddressid\", \"emailaddress\","
                    + " \"rowguid\", \"modifieddate\""))
        .updateReturning(EmailaddressRow._rowParser.exactlyOne())
        .runUnchecked(c);
  }
  ;

  @Override
  public List<EmailaddressRow> upsertBatch(Iterator<EmailaddressRow> unsaved, Connection c) {
    return interpolate(
            Fragment.lit(
                "insert into \"person\".\"emailaddress\"(\"businessentityid\", \"emailaddressid\","
                    + " \"emailaddress\", \"rowguid\", \"modifieddate\")\n"
                    + "values (?::int4, ?::int4, ?, ?::uuid, ?::timestamp)\n"
                    + "on conflict (\"businessentityid\", \"emailaddressid\")\n"
                    + "do update set\n"
                    + "  \"emailaddress\" = EXCLUDED.\"emailaddress\",\n"
                    + "\"rowguid\" = EXCLUDED.\"rowguid\",\n"
                    + "\"modifieddate\" = EXCLUDED.\"modifieddate\"\n"
                    + "returning \"businessentityid\", \"emailaddressid\", \"emailaddress\","
                    + " \"rowguid\", \"modifieddate\""))
        .updateManyReturning(EmailaddressRow._rowParser, unsaved)
        .runUnchecked(c);
  }
  ;

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
      Iterator<EmailaddressRow> unsaved, Integer batchSize, Connection c) {
    interpolate(
            Fragment.lit(
                "create temporary table emailaddress_TEMP (like \"person\".\"emailaddress\") on"
                    + " commit drop"))
        .update()
        .runUnchecked(c);
    streamingInsert.insertUnchecked(
        "copy emailaddress_TEMP(\"businessentityid\", \"emailaddressid\", \"emailaddress\","
            + " \"rowguid\", \"modifieddate\") from stdin",
        batchSize,
        unsaved,
        c,
        EmailaddressRow.pgText);
    return interpolate(
            Fragment.lit(
                "insert into \"person\".\"emailaddress\"(\"businessentityid\", \"emailaddressid\","
                    + " \"emailaddress\", \"rowguid\", \"modifieddate\")\n"
                    + "select * from emailaddress_TEMP\n"
                    + "on conflict (\"businessentityid\", \"emailaddressid\")\n"
                    + "do update set\n"
                    + "  \"emailaddress\" = EXCLUDED.\"emailaddress\",\n"
                    + "\"rowguid\" = EXCLUDED.\"rowguid\",\n"
                    + "\"modifieddate\" = EXCLUDED.\"modifieddate\"\n"
                    + ";\n"
                    + "drop table emailaddress_TEMP;"))
        .update()
        .runUnchecked(c);
  }
  ;
}
