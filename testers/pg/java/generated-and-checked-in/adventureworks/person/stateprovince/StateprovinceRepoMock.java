/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.stateprovince;

import dev.typr.foundations.dsl.DeleteBuilder;
import dev.typr.foundations.dsl.DeleteBuilderMock;
import dev.typr.foundations.dsl.DeleteParams;
import dev.typr.foundations.dsl.SelectBuilder;
import dev.typr.foundations.dsl.SelectBuilderMock;
import dev.typr.foundations.dsl.SelectParams;
import dev.typr.foundations.dsl.UpdateBuilder;
import dev.typr.foundations.dsl.UpdateBuilderMock;
import dev.typr.foundations.dsl.UpdateParams;
import java.lang.RuntimeException;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;

public record StateprovinceRepoMock(
  java.util.function.Function<StateprovinceRowUnsaved, StateprovinceRow> toRow,
  HashMap<StateprovinceId, StateprovinceRow> map
) implements StateprovinceRepo {
  public StateprovinceRepoMock(java.util.function.Function<StateprovinceRowUnsaved, StateprovinceRow> toRow) {
    this(toRow, new HashMap<StateprovinceId, StateprovinceRow>());
  };

  public StateprovinceRepoMock withToRow(java.util.function.Function<StateprovinceRowUnsaved, StateprovinceRow> toRow) {
    return new StateprovinceRepoMock(toRow, map);
  };

  public StateprovinceRepoMock withMap(HashMap<StateprovinceId, StateprovinceRow> map) {
    return new StateprovinceRepoMock(toRow, map);
  };

  @Override
  public DeleteBuilder<StateprovinceFields, StateprovinceRow> delete() {
    return new DeleteBuilderMock<>(StateprovinceFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.stateprovinceid(), id -> map.remove(id));
  };

  @Override
  public Boolean deleteById(
    StateprovinceId stateprovinceid,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(stateprovinceid)).isPresent();
  };

  @Override
  public Integer deleteByIds(
    StateprovinceId[] stateprovinceids,
    Connection c
  ) {
    var count = 0;
    for (var id : stateprovinceids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    };
    };
    return count;
  };

  @Override
  public StateprovinceRow insert(
    StateprovinceRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.stateprovinceid())) {
      throw new RuntimeException("id " + unsaved.stateprovinceid() + " already exists");
    };
    map.put(unsaved.stateprovinceid(), unsaved);
    return unsaved;
  };

  @Override
  public StateprovinceRow insert(
    StateprovinceRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  @Override
  public Long insertStreaming(
    Iterator<StateprovinceRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.stateprovinceid(), row);
      count = count + 1L;
    };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  @Override
  public Long insertUnsavedStreaming(
    Iterator<StateprovinceRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var unsavedRow = unsaved.next();
      var row = toRow.apply(unsavedRow);
      map.put(row.stateprovinceid(), row);
      count = count + 1L;
    };
    return count;
  };

  @Override
  public SelectBuilder<StateprovinceFields, StateprovinceRow> select() {
    return new SelectBuilderMock<>(StateprovinceFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  @Override
  public List<StateprovinceRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  @Override
  public Optional<StateprovinceRow> selectById(
    StateprovinceId stateprovinceid,
    Connection c
  ) {
    return Optional.ofNullable(map.get(stateprovinceid));
  };

  @Override
  public List<StateprovinceRow> selectByIds(
    StateprovinceId[] stateprovinceids,
    Connection c
  ) {
    var result = new ArrayList<StateprovinceRow>();
    for (var id : stateprovinceids) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    };
    };
    return result;
  };

  @Override
  public Map<StateprovinceId, StateprovinceRow> selectByIdsTracked(
    StateprovinceId[] stateprovinceids,
    Connection c
  ) {
    return selectByIds(stateprovinceids, c).stream().collect(Collectors.toMap((StateprovinceRow row) -> row.stateprovinceid(), Function.identity()));
  };

  @Override
  public UpdateBuilder<StateprovinceFields, StateprovinceRow> update() {
    return new UpdateBuilderMock<>(StateprovinceFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  @Override
  public Boolean update(
    StateprovinceRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.stateprovinceid())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.stateprovinceid(), row);;
    };
    return shouldUpdate;
  };

  @Override
  public StateprovinceRow upsert(
    StateprovinceRow unsaved,
    Connection c
  ) {
    map.put(unsaved.stateprovinceid(), unsaved);
    return unsaved;
  };

  @Override
  public List<StateprovinceRow> upsertBatch(
    Iterator<StateprovinceRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<StateprovinceRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.stateprovinceid(), row);
      result.add(row);
    };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
    Iterator<StateprovinceRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.stateprovinceid(), row);
      count = count + 1;
    };
    return count;
  };
}