/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.salesperson;

import adventureworks.person.businessentity.BusinessentityId;
import dev.typr.foundations.dsl.DeleteBuilder;
import dev.typr.foundations.dsl.DeleteBuilderMock;
import dev.typr.foundations.dsl.DeleteParams;
import dev.typr.foundations.dsl.SelectBuilder;
import dev.typr.foundations.dsl.SelectBuilderMock;
import dev.typr.foundations.dsl.SelectParams;
import dev.typr.foundations.dsl.UpdateBuilder;
import dev.typr.foundations.dsl.UpdateBuilderMock;
import dev.typr.foundations.dsl.UpdateParams;
import java.lang.RuntimeException;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;

public record SalespersonRepoMock(
  java.util.function.Function<SalespersonRowUnsaved, SalespersonRow> toRow,
  HashMap<BusinessentityId, SalespersonRow> map
) implements SalespersonRepo {
  public SalespersonRepoMock(java.util.function.Function<SalespersonRowUnsaved, SalespersonRow> toRow) {
    this(toRow, new HashMap<BusinessentityId, SalespersonRow>());
  };

  public SalespersonRepoMock withToRow(java.util.function.Function<SalespersonRowUnsaved, SalespersonRow> toRow) {
    return new SalespersonRepoMock(toRow, map);
  };

  public SalespersonRepoMock withMap(HashMap<BusinessentityId, SalespersonRow> map) {
    return new SalespersonRepoMock(toRow, map);
  };

  @Override
  public DeleteBuilder<SalespersonFields, SalespersonRow> delete() {
    return new DeleteBuilderMock<>(SalespersonFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.businessentityid(), id -> map.remove(id));
  };

  @Override
  public Boolean deleteById(
    BusinessentityId businessentityid,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(businessentityid)).isPresent();
  };

  @Override
  public Integer deleteByIds(
    BusinessentityId[] businessentityids,
    Connection c
  ) {
    var count = 0;
    for (var id : businessentityids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    };
    };
    return count;
  };

  @Override
  public SalespersonRow insert(
    SalespersonRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.businessentityid())) {
      throw new RuntimeException("id " + unsaved.businessentityid() + " already exists");
    };
    map.put(unsaved.businessentityid(), unsaved);
    return unsaved;
  };

  @Override
  public SalespersonRow insert(
    SalespersonRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  @Override
  public Long insertStreaming(
    Iterator<SalespersonRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.businessentityid(), row);
      count = count + 1L;
    };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  @Override
  public Long insertUnsavedStreaming(
    Iterator<SalespersonRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var unsavedRow = unsaved.next();
      var row = toRow.apply(unsavedRow);
      map.put(row.businessentityid(), row);
      count = count + 1L;
    };
    return count;
  };

  @Override
  public SelectBuilder<SalespersonFields, SalespersonRow> select() {
    return new SelectBuilderMock<>(SalespersonFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  @Override
  public List<SalespersonRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  @Override
  public Optional<SalespersonRow> selectById(
    BusinessentityId businessentityid,
    Connection c
  ) {
    return Optional.ofNullable(map.get(businessentityid));
  };

  @Override
  public List<SalespersonRow> selectByIds(
    BusinessentityId[] businessentityids,
    Connection c
  ) {
    var result = new ArrayList<SalespersonRow>();
    for (var id : businessentityids) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    };
    };
    return result;
  };

  @Override
  public Map<BusinessentityId, SalespersonRow> selectByIdsTracked(
    BusinessentityId[] businessentityids,
    Connection c
  ) {
    return selectByIds(businessentityids, c).stream().collect(Collectors.toMap((SalespersonRow row) -> row.businessentityid(), Function.identity()));
  };

  @Override
  public UpdateBuilder<SalespersonFields, SalespersonRow> update() {
    return new UpdateBuilderMock<>(SalespersonFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  @Override
  public Boolean update(
    SalespersonRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.businessentityid())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.businessentityid(), row);;
    };
    return shouldUpdate;
  };

  @Override
  public SalespersonRow upsert(
    SalespersonRow unsaved,
    Connection c
  ) {
    map.put(unsaved.businessentityid(), unsaved);
    return unsaved;
  };

  @Override
  public List<SalespersonRow> upsertBatch(
    Iterator<SalespersonRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<SalespersonRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.businessentityid(), row);
      result.add(row);
    };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
    Iterator<SalespersonRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.businessentityid(), row);
      count = count + 1;
    };
    return count;
  };
}