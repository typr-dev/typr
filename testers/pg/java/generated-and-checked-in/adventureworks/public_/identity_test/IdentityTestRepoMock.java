/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public_.identity_test;

import dev.typr.foundations.dsl.DeleteBuilder;
import dev.typr.foundations.dsl.DeleteBuilderMock;
import dev.typr.foundations.dsl.DeleteParams;
import dev.typr.foundations.dsl.SelectBuilder;
import dev.typr.foundations.dsl.SelectBuilderMock;
import dev.typr.foundations.dsl.SelectParams;
import dev.typr.foundations.dsl.UpdateBuilder;
import dev.typr.foundations.dsl.UpdateBuilderMock;
import dev.typr.foundations.dsl.UpdateParams;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;

public record IdentityTestRepoMock(
    java.util.function.Function<IdentityTestRowUnsaved, IdentityTestRow> toRow,
    HashMap<IdentityTestId, IdentityTestRow> map)
    implements IdentityTestRepo {
  public IdentityTestRepoMock(
      java.util.function.Function<IdentityTestRowUnsaved, IdentityTestRow> toRow) {
    this(toRow, new HashMap<IdentityTestId, IdentityTestRow>());
  }
  ;

  public IdentityTestRepoMock withToRow(
      java.util.function.Function<IdentityTestRowUnsaved, IdentityTestRow> toRow) {
    return new IdentityTestRepoMock(toRow, map);
  }
  ;

  public IdentityTestRepoMock withMap(HashMap<IdentityTestId, IdentityTestRow> map) {
    return new IdentityTestRepoMock(toRow, map);
  }
  ;

  @Override
  public DeleteBuilder<IdentityTestFields, IdentityTestRow> delete() {
    return new DeleteBuilderMock<>(
        IdentityTestFields.structure(),
        () -> new ArrayList<>(map.values()),
        DeleteParams.empty(),
        row -> row.name(),
        id -> map.remove(id));
  }
  ;

  @Override
  public Boolean deleteById(IdentityTestId name, Connection c) {
    return Optional.ofNullable(map.remove(name)).isPresent();
  }
  ;

  @Override
  public Integer deleteByIds(IdentityTestId[] names, Connection c) {
    var count = 0;
    for (var id : names) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
        ;
      }
      ;
    }
    ;
    return count;
  }
  ;

  @Override
  public IdentityTestRow insert(IdentityTestRow unsaved, Connection c) {
    if (map.containsKey(unsaved.name())) {
      throw new RuntimeException("id " + unsaved.name() + " already exists");
    }
    ;
    map.put(unsaved.name(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public IdentityTestRow insert(IdentityTestRowUnsaved unsaved, Connection c) {
    return insert(toRow.apply(unsaved), c);
  }
  ;

  @Override
  public Long insertStreaming(Iterator<IdentityTestRow> unsaved, Integer batchSize, Connection c) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.name(), row);
      count = count + 1L;
    }
    ;
    return count;
  }
  ;

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  @Override
  public Long insertUnsavedStreaming(
      Iterator<IdentityTestRowUnsaved> unsaved, Integer batchSize, Connection c) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var unsavedRow = unsaved.next();
      var row = toRow.apply(unsavedRow);
      map.put(row.name(), row);
      count = count + 1L;
    }
    ;
    return count;
  }
  ;

  @Override
  public SelectBuilder<IdentityTestFields, IdentityTestRow> select() {
    return new SelectBuilderMock<>(
        IdentityTestFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  }
  ;

  @Override
  public List<IdentityTestRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  }
  ;

  @Override
  public Optional<IdentityTestRow> selectById(IdentityTestId name, Connection c) {
    return Optional.ofNullable(map.get(name));
  }
  ;

  @Override
  public List<IdentityTestRow> selectByIds(IdentityTestId[] names, Connection c) {
    var result = new ArrayList<IdentityTestRow>();
    for (var id : names) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
        result.add(opt.get());
      }
      ;
    }
    ;
    return result;
  }
  ;

  @Override
  public Map<IdentityTestId, IdentityTestRow> selectByIdsTracked(
      IdentityTestId[] names, Connection c) {
    return selectByIds(names, c).stream()
        .collect(Collectors.toMap((IdentityTestRow row) -> row.name(), Function.identity()));
  }
  ;

  @Override
  public UpdateBuilder<IdentityTestFields, IdentityTestRow> update() {
    return new UpdateBuilderMock<>(
        IdentityTestFields.structure(),
        () -> new ArrayList<>(map.values()),
        UpdateParams.empty(),
        row -> row);
  }
  ;

  @Override
  public Boolean update(IdentityTestRow row, Connection c) {
    var shouldUpdate =
        Optional.ofNullable(map.get(row.name())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.name(), row);
      ;
    }
    ;
    return shouldUpdate;
  }
  ;

  @Override
  public IdentityTestRow upsert(IdentityTestRow unsaved, Connection c) {
    map.put(unsaved.name(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public List<IdentityTestRow> upsertBatch(Iterator<IdentityTestRow> unsaved, Connection c) {
    var result = new ArrayList<IdentityTestRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.name(), row);
      result.add(row);
    }
    ;
    return result;
  }
  ;

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
      Iterator<IdentityTestRow> unsaved, Integer batchSize, Connection c) {
    var count = 0;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.name(), row);
      count = count + 1;
    }
    ;
    return count;
  }
  ;
}
