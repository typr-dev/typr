/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public_.title;

import dev.typr.dsl.DeleteBuilder;
import dev.typr.dsl.Dialect;
import dev.typr.dsl.SelectBuilder;
import dev.typr.dsl.UpdateBuilder;
import dev.typr.foundations.Connection;
import dev.typr.foundations.ConnectionRead;
import dev.typr.foundations.Fragment;
import dev.typr.foundations.StreamingInsert;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import static dev.typr.foundations.Fragment.concat;

public class TitleRepoImpl implements TitleRepo {
  @Override
  public DeleteBuilder<TitleFields, TitleRow> delete() {
    return DeleteBuilder.of("\"public\".\"title\"", TitleFields.structure, Dialect.POSTGRESQL);
  }

  @Override
  public Boolean deleteById(
    TitleId code,
    Connection c
  ) {
    return concat(Fragment.of("delete from \"public\".\"title\" where \"code\" = "), Fragment.encode(TitleId.pgType, code), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public Integer deleteByIds(
    List<TitleId> codes,
    Connection c
  ) {
    return concat(Fragment.of("delete\nfrom \"public\".\"title\"\nwhere \"code\" = ANY("), Fragment.encode(TitleId.pgType.array(), codes), Fragment.of(")"))
      .update()
      .run(c);
  }

  @Override
  public TitleRow insert(
    TitleRow unsaved,
    Connection c
  ) {
    return concat(Fragment.of("insert into \"public\".\"title\"(\"code\")\nvalues ("), Fragment.encode(TitleId.pgType, unsaved.code()), Fragment.of(")\nRETURNING \"code\"\n"))
      .updateReturning(TitleRow.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public Long insertStreaming(
    Iterator<TitleRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    return StreamingInsert.of("COPY \"public\".\"title\"(\"code\") FROM STDIN", batchSize, unsaved, TitleRow.pgText).run(c);
  }

  @Override
  public SelectBuilder<TitleFields, TitleRow> select() {
    return SelectBuilder.of("\"public\".\"title\"", TitleFields.structure, TitleRow.rowCodec, Dialect.POSTGRESQL);
  }

  @Override
  public List<TitleRow> selectAll(ConnectionRead c) {
    return concat(Fragment.of("select \"code\"\nfrom \"public\".\"title\"\n")).query(TitleRow.rowCodec.all()).run(c);
  }

  @Override
  public Optional<TitleRow> selectById(
    TitleId code,
    ConnectionRead c
  ) {
    return concat(Fragment.of("select \"code\"\nfrom \"public\".\"title\"\nwhere \"code\" = "), Fragment.encode(TitleId.pgType, code), Fragment.of("")).query(TitleRow.rowCodec.first()).run(c);
  }

  @Override
  public List<TitleRow> selectByIds(
    List<TitleId> codes,
    ConnectionRead c
  ) {
    return concat(Fragment.of("select \"code\"\nfrom \"public\".\"title\"\nwhere \"code\" = ANY("), Fragment.encode(TitleId.pgType.array(), codes), Fragment.of(")")).query(TitleRow.rowCodec.all()).run(c);
  }

  @Override
  public Map<TitleId, TitleRow> selectByIdsTracked(
    List<TitleId> codes,
    ConnectionRead c
  ) {
    HashMap<TitleId, TitleRow> ret = new HashMap<TitleId, TitleRow>();
    selectByIds(codes, c).forEach(row -> ret.put(row.code(), row));
    return ret;
  }

  @Override
  public UpdateBuilder<TitleFields, TitleRow> update() {
    return UpdateBuilder.of("\"public\".\"title\"", TitleFields.structure, TitleRow.rowCodec, Dialect.POSTGRESQL);
  }

  @Override
  public TitleRow upsert(
    TitleRow unsaved,
    Connection c
  ) {
    return concat(Fragment.of("insert into \"public\".\"title\"(\"code\")\nvalues ("), Fragment.encode(TitleId.pgType, unsaved.code()), Fragment.of(")\non conflict (\"code\")\ndo update set \"code\" = EXCLUDED.\"code\"\nreturning \"code\""))
      .updateReturning(TitleRow.rowCodec.exactlyOne())
      .run(c);
  }

  @Override
  public List<TitleRow> upsertBatch(
    Iterator<TitleRow> unsaved,
    Connection c
  ) {
    return concat(Fragment.of("insert into \"public\".\"title\"(\"code\")\nvalues (?)\non conflict (\"code\")\ndo update set \"code\" = EXCLUDED.\"code\"\nreturning \"code\""))
      .updateManyReturning(TitleRow.rowCodec, unsaved)
    .run(c);
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
    Iterator<TitleRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    concat(Fragment.of("create temporary table title_TEMP (like \"public\".\"title\") on commit drop")).update().run(c);
    StreamingInsert.of("copy title_TEMP(\"code\") from stdin", batchSize, unsaved, TitleRow.pgText).run(c);
    return concat(Fragment.of("insert into \"public\".\"title\"(\"code\")\nselect * from title_TEMP\non conflict (\"code\")\ndo nothing\n;\ndrop table title_TEMP;")).update().run(c);
  }
}