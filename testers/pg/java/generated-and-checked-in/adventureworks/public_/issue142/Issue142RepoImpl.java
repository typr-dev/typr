/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public_.issue142;

import dev.typr.dsl.DeleteBuilder;
import dev.typr.dsl.Dialect;
import dev.typr.dsl.SelectBuilder;
import dev.typr.dsl.UpdateBuilder;
import dev.typr.foundations.Connection;
import dev.typr.foundations.ConnectionRead;
import dev.typr.foundations.Fragment;
import dev.typr.foundations.StreamingInsert;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import static dev.typr.foundations.Fragment.concat;

public class Issue142RepoImpl implements Issue142Repo {
  @Override
  public DeleteBuilder<Issue142Fields, Issue142Row> delete() {
    return DeleteBuilder.of("\"public\".\"issue142\"", Issue142Fields.structure, Dialect.POSTGRESQL);
  }

  @Override
  public Boolean deleteById(
    Issue142Id tabellkode,
    Connection c
  ) {
    return concat(Fragment.of("delete from \"public\".\"issue142\" where \"tabellkode\" = "), Fragment.encode(Issue142Id.pgType, tabellkode), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public Integer deleteByIds(
    List<Issue142Id> tabellkodes,
    Connection c
  ) {
    return concat(Fragment.of("delete\nfrom \"public\".\"issue142\"\nwhere \"tabellkode\" = ANY("), Fragment.encode(Issue142Id.pgType.array(), tabellkodes), Fragment.of(")"))
      .update()
      .run(c);
  }

  @Override
  public Issue142Row insert(
    Issue142Row unsaved,
    Connection c
  ) {
    return concat(Fragment.of("insert into \"public\".\"issue142\"(\"tabellkode\")\nvalues ("), Fragment.encode(Issue142Id.pgType, unsaved.tabellkode()), Fragment.of(")\nRETURNING \"tabellkode\"\n"))
      .updateReturning(Issue142Row.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public Long insertStreaming(
    Iterator<Issue142Row> unsaved,
    Integer batchSize,
    Connection c
  ) {
    return StreamingInsert.of("COPY \"public\".\"issue142\"(\"tabellkode\") FROM STDIN", batchSize, unsaved, Issue142Row.pgText).run(c);
  }

  @Override
  public SelectBuilder<Issue142Fields, Issue142Row> select() {
    return SelectBuilder.of("\"public\".\"issue142\"", Issue142Fields.structure, Issue142Row.rowCodec, Dialect.POSTGRESQL);
  }

  @Override
  public List<Issue142Row> selectAll(ConnectionRead c) {
    return concat(Fragment.of("select \"tabellkode\"\nfrom \"public\".\"issue142\"\n")).query(Issue142Row.rowCodec.all()).run(c);
  }

  @Override
  public Optional<Issue142Row> selectById(
    Issue142Id tabellkode,
    ConnectionRead c
  ) {
    return concat(Fragment.of("select \"tabellkode\"\nfrom \"public\".\"issue142\"\nwhere \"tabellkode\" = "), Fragment.encode(Issue142Id.pgType, tabellkode), Fragment.of("")).query(Issue142Row.rowCodec.first()).run(c);
  }

  @Override
  public List<Issue142Row> selectByIds(
    List<Issue142Id> tabellkodes,
    ConnectionRead c
  ) {
    return concat(Fragment.of("select \"tabellkode\"\nfrom \"public\".\"issue142\"\nwhere \"tabellkode\" = ANY("), Fragment.encode(Issue142Id.pgType.array(), tabellkodes), Fragment.of(")")).query(Issue142Row.rowCodec.all()).run(c);
  }

  @Override
  public Map<Issue142Id, Issue142Row> selectByIdsTracked(
    List<Issue142Id> tabellkodes,
    ConnectionRead c
  ) {
    HashMap<Issue142Id, Issue142Row> ret = new HashMap<Issue142Id, Issue142Row>();
    selectByIds(tabellkodes, c).forEach(row -> ret.put(row.tabellkode(), row));
    return ret;
  }

  @Override
  public UpdateBuilder<Issue142Fields, Issue142Row> update() {
    return UpdateBuilder.of("\"public\".\"issue142\"", Issue142Fields.structure, Issue142Row.rowCodec, Dialect.POSTGRESQL);
  }

  @Override
  public Issue142Row upsert(
    Issue142Row unsaved,
    Connection c
  ) {
    return concat(Fragment.of("insert into \"public\".\"issue142\"(\"tabellkode\")\nvalues ("), Fragment.encode(Issue142Id.pgType, unsaved.tabellkode()), Fragment.of(")\non conflict (\"tabellkode\")\ndo update set \"tabellkode\" = EXCLUDED.\"tabellkode\"\nreturning \"tabellkode\""))
      .updateReturning(Issue142Row.rowCodec.exactlyOne())
      .run(c);
  }

  @Override
  public List<Issue142Row> upsertBatch(
    Iterator<Issue142Row> unsaved,
    Connection c
  ) {
    return concat(Fragment.of("insert into \"public\".\"issue142\"(\"tabellkode\")\nvalues (?)\non conflict (\"tabellkode\")\ndo update set \"tabellkode\" = EXCLUDED.\"tabellkode\"\nreturning \"tabellkode\""))
      .updateManyReturning(Issue142Row.rowCodec, unsaved)
    .run(c);
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
    Iterator<Issue142Row> unsaved,
    Integer batchSize,
    Connection c
  ) {
    concat(Fragment.of("create temporary table issue142_TEMP (like \"public\".\"issue142\") on commit drop")).update().run(c);
    StreamingInsert.of("copy issue142_TEMP(\"tabellkode\") from stdin", batchSize, unsaved, Issue142Row.pgText).run(c);
    return concat(Fragment.of("insert into \"public\".\"issue142\"(\"tabellkode\")\nselect * from issue142_TEMP\non conflict (\"tabellkode\")\ndo nothing\n;\ndrop table issue142_TEMP;")).update().run(c);
  }
}