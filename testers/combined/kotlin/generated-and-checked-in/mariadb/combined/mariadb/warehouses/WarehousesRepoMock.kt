/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package combined.mariadb.warehouses

import dev.typr.dslkt.DeleteBuilder
import dev.typr.dslkt.DeleteBuilderMock
import dev.typr.dslkt.DeleteParams
import dev.typr.dslkt.SelectBuilder
import dev.typr.dslkt.SelectBuilderMock
import dev.typr.dslkt.SelectParams
import dev.typr.dslkt.UpdateBuilder
import dev.typr.dslkt.UpdateBuilderMock
import dev.typr.dslkt.UpdateParams
import dev.typr.foundationskt.Connection
import dev.typr.foundationskt.ConnectionRead
import java.lang.RuntimeException
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap

data class WarehousesRepoMock(
  val toRow: (WarehousesRowUnsaved) -> WarehousesRow,
  val map: MutableMap<WarehousesId, WarehousesRow> = mutableMapOf<WarehousesId, WarehousesRow>()
) : WarehousesRepo {
  override fun delete(): DeleteBuilder<WarehousesFields, WarehousesRow> = DeleteBuilderMock(WarehousesFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.warehouseId }, { id -> map.remove(id) })

  override fun deleteById(
    warehouseId: WarehousesId,
    c: Connection
  ): kotlin.Boolean = map.remove(warehouseId) != null

  override fun deleteByIds(
    warehouseIds: List<WarehousesId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in warehouseIds) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: WarehousesRow,
    c: Connection
  ): WarehousesRow {
    if (map.containsKey(unsaved.warehouseId)) {
      throw RuntimeException("id " + unsaved.warehouseId + " already exists")
    }
    map[unsaved.warehouseId] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: WarehousesRowUnsaved,
    c: Connection
  ): WarehousesRow = insert(toRow(unsaved), c)

  override fun select(): SelectBuilder<WarehousesFields, WarehousesRow> = SelectBuilderMock(WarehousesFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: ConnectionRead): List<WarehousesRow> = map.values.toList()

  override fun selectById(
    warehouseId: WarehousesId,
    c: ConnectionRead
  ): WarehousesRow? = map[warehouseId]

  override fun selectByIds(
    warehouseIds: List<WarehousesId>,
    c: ConnectionRead
  ): List<WarehousesRow> {
    val result = ArrayList<WarehousesRow>()
    for (id in warehouseIds) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    warehouseIds: List<WarehousesId>,
    c: ConnectionRead
  ): Map<WarehousesId, WarehousesRow> = selectByIds(warehouseIds, c).associateBy({ row: WarehousesRow -> row.warehouseId })

  override fun selectByUniqueCode(
    code: kotlin.String,
    c: ConnectionRead
  ): WarehousesRow? = map.values.toList().find({ v -> (code == v.code) })

  override fun update(): UpdateBuilder<WarehousesFields, WarehousesRow> = UpdateBuilderMock(WarehousesFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: WarehousesRow,
    c: Connection
  ): kotlin.Boolean {
    val shouldUpdate = map[row.warehouseId]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.warehouseId] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: WarehousesRow,
    c: Connection
  ): WarehousesRow {
    map[unsaved.warehouseId] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<WarehousesRow>,
    c: Connection
  ): List<WarehousesRow> {
    val result = ArrayList<WarehousesRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.warehouseId] = row
      result.add(row)
    }
    return result
  }
}