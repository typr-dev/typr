/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package combined.mariadb.products

import dev.typr.dslkt.DeleteBuilder
import dev.typr.dslkt.DeleteBuilderMock
import dev.typr.dslkt.DeleteParams
import dev.typr.dslkt.SelectBuilder
import dev.typr.dslkt.SelectBuilderMock
import dev.typr.dslkt.SelectParams
import dev.typr.dslkt.UpdateBuilder
import dev.typr.dslkt.UpdateBuilderMock
import dev.typr.dslkt.UpdateParams
import dev.typr.foundationskt.Connection
import dev.typr.foundationskt.ConnectionRead
import java.lang.RuntimeException
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap

data class ProductsRepoMock(
  val toRow: (ProductsRowUnsaved) -> ProductsRow,
  val map: MutableMap<ProductsId, ProductsRow> = mutableMapOf<ProductsId, ProductsRow>()
) : ProductsRepo {
  override fun delete(): DeleteBuilder<ProductsFields, ProductsRow> = DeleteBuilderMock(ProductsFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.productId }, { id -> map.remove(id) })

  override fun deleteById(
    productId: ProductsId,
    c: Connection
  ): kotlin.Boolean = map.remove(productId) != null

  override fun deleteByIds(
    productIds: List<ProductsId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in productIds) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: ProductsRow,
    c: Connection
  ): ProductsRow {
    if (map.containsKey(unsaved.productId)) {
      throw RuntimeException("id " + unsaved.productId + " already exists")
    }
    map[unsaved.productId] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: ProductsRowUnsaved,
    c: Connection
  ): ProductsRow = insert(toRow(unsaved), c)

  override fun select(): SelectBuilder<ProductsFields, ProductsRow> = SelectBuilderMock(ProductsFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: ConnectionRead): List<ProductsRow> = map.values.toList()

  override fun selectById(
    productId: ProductsId,
    c: ConnectionRead
  ): ProductsRow? = map[productId]

  override fun selectByIds(
    productIds: List<ProductsId>,
    c: ConnectionRead
  ): List<ProductsRow> {
    val result = ArrayList<ProductsRow>()
    for (id in productIds) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    productIds: List<ProductsId>,
    c: ConnectionRead
  ): Map<ProductsId, ProductsRow> = selectByIds(productIds, c).associateBy({ row: ProductsRow -> row.productId })

  override fun selectByUniqueSku(
    sku: kotlin.String,
    c: ConnectionRead
  ): ProductsRow? = map.values.toList().find({ v -> (sku == v.sku) })

  override fun update(): UpdateBuilder<ProductsFields, ProductsRow> = UpdateBuilderMock(ProductsFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: ProductsRow,
    c: Connection
  ): kotlin.Boolean {
    val shouldUpdate = map[row.productId]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.productId] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: ProductsRow,
    c: Connection
  ): ProductsRow {
    map[unsaved.productId] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<ProductsRow>,
    c: Connection
  ): List<ProductsRow> {
    val result = ArrayList<ProductsRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.productId] = row
      result.add(row)
    }
    return result
  }
}