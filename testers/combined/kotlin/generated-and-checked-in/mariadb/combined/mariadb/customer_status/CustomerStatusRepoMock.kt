/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package combined.mariadb.customer_status

import dev.typr.dslkt.DeleteBuilder
import dev.typr.dslkt.DeleteBuilderMock
import dev.typr.dslkt.DeleteParams
import dev.typr.dslkt.SelectBuilder
import dev.typr.dslkt.SelectBuilderMock
import dev.typr.dslkt.SelectParams
import dev.typr.dslkt.UpdateBuilder
import dev.typr.dslkt.UpdateBuilderMock
import dev.typr.dslkt.UpdateParams
import dev.typr.foundationskt.Connection
import dev.typr.foundationskt.ConnectionRead
import java.lang.RuntimeException
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap

data class CustomerStatusRepoMock(
  val toRow: (CustomerStatusRowUnsaved) -> CustomerStatusRow,
  val map: MutableMap<CustomerStatusId, CustomerStatusRow> = mutableMapOf<CustomerStatusId, CustomerStatusRow>()
) : CustomerStatusRepo {
  override fun delete(): DeleteBuilder<CustomerStatusFields, CustomerStatusRow> = DeleteBuilderMock(CustomerStatusFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.statusCode }, { id -> map.remove(id) })

  override fun deleteById(
    statusCode: CustomerStatusId,
    c: Connection
  ): kotlin.Boolean = map.remove(statusCode) != null

  override fun deleteByIds(
    statusCodes: List<CustomerStatusId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in statusCodes) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: CustomerStatusRow,
    c: Connection
  ): CustomerStatusRow {
    if (map.containsKey(unsaved.statusCode)) {
      throw RuntimeException("id " + unsaved.statusCode + " already exists")
    }
    map[unsaved.statusCode] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: CustomerStatusRowUnsaved,
    c: Connection
  ): CustomerStatusRow = insert(toRow(unsaved), c)

  override fun select(): SelectBuilder<CustomerStatusFields, CustomerStatusRow> = SelectBuilderMock(CustomerStatusFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: ConnectionRead): List<CustomerStatusRow> = map.values.toList()

  override fun selectById(
    statusCode: CustomerStatusId,
    c: ConnectionRead
  ): CustomerStatusRow? = map[statusCode]

  override fun selectByIds(
    statusCodes: List<CustomerStatusId>,
    c: ConnectionRead
  ): List<CustomerStatusRow> {
    val result = ArrayList<CustomerStatusRow>()
    for (id in statusCodes) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    statusCodes: List<CustomerStatusId>,
    c: ConnectionRead
  ): Map<CustomerStatusId, CustomerStatusRow> = selectByIds(statusCodes, c).associateBy({ row: CustomerStatusRow -> row.statusCode })

  override fun update(): UpdateBuilder<CustomerStatusFields, CustomerStatusRow> = UpdateBuilderMock(CustomerStatusFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: CustomerStatusRow,
    c: Connection
  ): kotlin.Boolean {
    val shouldUpdate = map[row.statusCode]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.statusCode] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: CustomerStatusRow,
    c: Connection
  ): CustomerStatusRow {
    map[unsaved.statusCode] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<CustomerStatusRow>,
    c: Connection
  ): List<CustomerStatusRow> {
    val result = ArrayList<CustomerStatusRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.statusCode] = row
      result.add(row)
    }
    return result
  }
}