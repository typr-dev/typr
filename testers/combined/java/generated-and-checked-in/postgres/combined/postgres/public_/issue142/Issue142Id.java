/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package combined.postgres.public_.issue142;

import dev.typr.foundations.Bijection;
import dev.typr.foundations.PgType;
import dev.typr.foundations.PgTypes;
import java.util.List;

public sealed interface Issue142Id permits Issue142Id.Unknown, Issue142Id.Known {
    String value();

    record Unknown(String value) implements Issue142Id {
    }

    enum Known implements Issue142Id {
        aa("aa"),
        bb("bb");
        final String value;

        @Override
        public String value() {
            return value;
        }

        Known(String value) {
            this.value = value;
        }
    }

    java.util.Map<String, Known> ByName = java.util.Arrays.stream(Known.values()).collect(java.util.stream.Collectors.toMap(n -> n.value, n -> n));

    static Issue142Id apply(String str) {
        if (Known.ByName.containsKey(str)) {
            return Known.ByName.get(str);
        } else {
            return new Unknown(str);
        }
    }
    static public PgType<List<Issue142Id>> pgTypeArray =
      PgTypes.text.array()
        .to(Bijection.of(xs -> xs.stream().map(Issue142Id::apply).toList(), xs -> xs.stream().map(Issue142Id::value).toList()));;
    static public PgType<Issue142Id> pgType =
      PgTypes.text.to(Bijection.of(Issue142Id::apply, Issue142Id::value));;
}