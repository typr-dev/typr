/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package combined.postgres.production.productcategory;

import dev.typr.dsl.DeleteBuilder;
import dev.typr.dsl.DeleteBuilderMock;
import dev.typr.dsl.DeleteParams;
import dev.typr.dsl.SelectBuilder;
import dev.typr.dsl.SelectBuilderMock;
import dev.typr.dsl.SelectParams;
import dev.typr.dsl.UpdateBuilder;
import dev.typr.dsl.UpdateBuilderMock;
import dev.typr.dsl.UpdateParams;
import dev.typr.foundations.Connection;
import dev.typr.foundations.ConnectionRead;
import java.lang.RuntimeException;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;

public record ProductcategoryRepoMock(
  java.util.function.Function<ProductcategoryRowUnsaved, ProductcategoryRow> toRow,
  HashMap<ProductcategoryId, ProductcategoryRow> map
) implements ProductcategoryRepo {
  public ProductcategoryRepoMock(java.util.function.Function<ProductcategoryRowUnsaved, ProductcategoryRow> toRow) {
    this(toRow, new HashMap<ProductcategoryId, ProductcategoryRow>());
  }

  public ProductcategoryRepoMock withToRow(java.util.function.Function<ProductcategoryRowUnsaved, ProductcategoryRow> toRow) {
    return new ProductcategoryRepoMock(toRow, map);
  }

  public ProductcategoryRepoMock withMap(HashMap<ProductcategoryId, ProductcategoryRow> map) {
    return new ProductcategoryRepoMock(toRow, map);
  }

  @Override
  public DeleteBuilder<ProductcategoryFields, ProductcategoryRow> delete() {
    return new DeleteBuilderMock<>(ProductcategoryFields.structure, () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.productcategoryid(), id -> map.remove(id));
  }

  @Override
  public Boolean deleteById(
    ProductcategoryId productcategoryid,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(productcategoryid)).isPresent();
  }

  @Override
  public Integer deleteByIds(
    List<ProductcategoryId> productcategoryids,
    Connection c
  ) {
    var count = 0;
    for (var id : productcategoryids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    }
    };
    return count;
  }

  @Override
  public ProductcategoryRow insert(
    ProductcategoryRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.productcategoryid())) {
      throw new RuntimeException("id " + unsaved.productcategoryid() + " already exists");
    }
    map.put(unsaved.productcategoryid(), unsaved);
    return unsaved;
  }

  @Override
  public ProductcategoryRow insert(
    ProductcategoryRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  }

  @Override
  public Long insertStreaming(
    Iterator<ProductcategoryRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.productcategoryid(), row);
      count = count + 1L;
    };
    return count;
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  @Override
  public Long insertUnsavedStreaming(
    Iterator<ProductcategoryRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var unsavedRow = unsaved.next();
      var row = toRow.apply(unsavedRow);
      map.put(row.productcategoryid(), row);
      count = count + 1L;
    };
    return count;
  }

  @Override
  public SelectBuilder<ProductcategoryFields, ProductcategoryRow> select() {
    return new SelectBuilderMock<>(ProductcategoryFields.structure, () -> new ArrayList<>(map.values()), SelectParams.empty());
  }

  @Override
  public List<ProductcategoryRow> selectAll(ConnectionRead c) {
    return new ArrayList<>(map.values());
  }

  @Override
  public Optional<ProductcategoryRow> selectById(
    ProductcategoryId productcategoryid,
    ConnectionRead c
  ) {
    return Optional.ofNullable(map.get(productcategoryid));
  }

  @Override
  public List<ProductcategoryRow> selectByIds(
    List<ProductcategoryId> productcategoryids,
    ConnectionRead c
  ) {
    var result = new ArrayList<ProductcategoryRow>();
    for (var id : productcategoryids) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    }
    };
    return result;
  }

  @Override
  public Map<ProductcategoryId, ProductcategoryRow> selectByIdsTracked(
    List<ProductcategoryId> productcategoryids,
    ConnectionRead c
  ) {
    return selectByIds(productcategoryids, c).stream().collect(Collectors.toMap((ProductcategoryRow row) -> row.productcategoryid(), Function.identity()));
  }

  @Override
  public UpdateBuilder<ProductcategoryFields, ProductcategoryRow> update() {
    return new UpdateBuilderMock<>(ProductcategoryFields.structure, () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  }

  @Override
  public Boolean update(
    ProductcategoryRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.productcategoryid())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.productcategoryid(), row);;
    }
    return shouldUpdate;
  }

  @Override
  public ProductcategoryRow upsert(
    ProductcategoryRow unsaved,
    Connection c
  ) {
    map.put(unsaved.productcategoryid(), unsaved);
    return unsaved;
  }

  @Override
  public List<ProductcategoryRow> upsertBatch(
    Iterator<ProductcategoryRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<ProductcategoryRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.productcategoryid(), row);
      result.add(row);
    };
    return result;
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
    Iterator<ProductcategoryRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.productcategoryid(), row);
      count = count + 1;
    };
    return count;
  }
}