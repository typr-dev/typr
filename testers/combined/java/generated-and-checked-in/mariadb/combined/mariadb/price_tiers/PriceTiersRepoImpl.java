/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package combined.mariadb.price_tiers;

import dev.typr.dsl.DeleteBuilder;
import dev.typr.dsl.Dialect;
import dev.typr.dsl.SelectBuilder;
import dev.typr.dsl.UpdateBuilder;
import dev.typr.foundations.Connection;
import dev.typr.foundations.ConnectionRead;
import dev.typr.foundations.Fragment;
import dev.typr.foundations.MariaTypes;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import static dev.typr.foundations.Fragment.concat;

public class PriceTiersRepoImpl implements PriceTiersRepo {
  @Override
  public DeleteBuilder<PriceTiersFields, PriceTiersRow> delete() {
    return DeleteBuilder.of("`price_tiers`", PriceTiersFields.structure, Dialect.MARIADB);
  }

  @Override
  public Boolean deleteById(
    PriceTiersId tierId,
    Connection c
  ) {
    return concat(Fragment.of("delete from `price_tiers` where `tier_id` = "), Fragment.encode(PriceTiersId.mariaType, tierId), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public Integer deleteByIds(
    List<PriceTiersId> tierIds,
    Connection c
  ) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : tierIds) { fragments.add(Fragment.encode(PriceTiersId.mariaType, id)); };
    return Fragment.concat(Fragment.of("delete from `price_tiers` where `tier_id` in ("), Fragment.comma(fragments), Fragment.of(")")).update().run(c);
  }

  @Override
  public PriceTiersRow insert(
    PriceTiersRow unsaved,
    Connection c
  ) {
    return concat(Fragment.of("insert into `price_tiers`(`name`, `min_quantity`, `discount_type`, `discount_value`)\nvalues ("), Fragment.encode(MariaTypes.varchar, unsaved.name()), Fragment.of(", "), Fragment.encode(MariaTypes.intUnsigned, unsaved.minQuantity()), Fragment.of(", "), Fragment.encode(MariaTypes.text, unsaved.discountType()), Fragment.of(", "), Fragment.encode(MariaTypes.numeric, unsaved.discountValue()), Fragment.of(")\nRETURNING `tier_id`, `name`, `min_quantity`, `discount_type`, `discount_value`\n"))
      .updateReturning(PriceTiersRow.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public PriceTiersRow insert(
    PriceTiersRowUnsaved unsaved,
    Connection c
  ) {
    ArrayList<Fragment> columns = new ArrayList<>();;
    ArrayList<Fragment> values = new ArrayList<>();;
    columns.add(Fragment.of("`name`"));
    values.add(concat(Fragment.encode(MariaTypes.varchar, unsaved.name()), Fragment.of("")));
    columns.add(Fragment.of("`discount_type`"));
    values.add(concat(Fragment.encode(MariaTypes.text, unsaved.discountType()), Fragment.of("")));
    columns.add(Fragment.of("`discount_value`"));
    values.add(concat(Fragment.encode(MariaTypes.numeric, unsaved.discountValue()), Fragment.of("")));
    unsaved.minQuantity().visit(
      () -> {
  
      },
      value -> {
        columns.add(Fragment.of("`min_quantity`"));
        values.add(concat(Fragment.encode(MariaTypes.intUnsigned, value), Fragment.of("")));
      }
    );;
    Fragment q = concat(Fragment.of("insert into `price_tiers`("), Fragment.comma(columns), Fragment.of(")\nvalues ("), Fragment.comma(values), Fragment.of(")\nRETURNING `tier_id`, `name`, `min_quantity`, `discount_type`, `discount_value`\n"));;
    return q.updateReturning(PriceTiersRow.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public SelectBuilder<PriceTiersFields, PriceTiersRow> select() {
    return SelectBuilder.of("`price_tiers`", PriceTiersFields.structure, PriceTiersRow.rowCodec, Dialect.MARIADB);
  }

  @Override
  public List<PriceTiersRow> selectAll(ConnectionRead c) {
    return concat(Fragment.of("select `tier_id`, `name`, `min_quantity`, `discount_type`, `discount_value`\nfrom `price_tiers`\n")).query(PriceTiersRow.rowCodec.all()).run(c);
  }

  @Override
  public Optional<PriceTiersRow> selectById(
    PriceTiersId tierId,
    ConnectionRead c
  ) {
    return concat(Fragment.of("select `tier_id`, `name`, `min_quantity`, `discount_type`, `discount_value`\nfrom `price_tiers`\nwhere `tier_id` = "), Fragment.encode(PriceTiersId.mariaType, tierId), Fragment.of("")).query(PriceTiersRow.rowCodec.first()).run(c);
  }

  @Override
  public List<PriceTiersRow> selectByIds(
    List<PriceTiersId> tierIds,
    ConnectionRead c
  ) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : tierIds) { fragments.add(Fragment.encode(PriceTiersId.mariaType, id)); };
    return Fragment.concat(Fragment.of("select `tier_id`, `name`, `min_quantity`, `discount_type`, `discount_value` from `price_tiers` where `tier_id` in ("), Fragment.comma(fragments), Fragment.of(")")).query(PriceTiersRow.rowCodec.all()).run(c);
  }

  @Override
  public Map<PriceTiersId, PriceTiersRow> selectByIdsTracked(
    List<PriceTiersId> tierIds,
    ConnectionRead c
  ) {
    HashMap<PriceTiersId, PriceTiersRow> ret = new HashMap<PriceTiersId, PriceTiersRow>();
    selectByIds(tierIds, c).forEach(row -> ret.put(row.tierId(), row));
    return ret;
  }

  @Override
  public UpdateBuilder<PriceTiersFields, PriceTiersRow> update() {
    return UpdateBuilder.of("`price_tiers`", PriceTiersFields.structure, PriceTiersRow.rowCodec, Dialect.MARIADB);
  }

  @Override
  public Boolean update(
    PriceTiersRow row,
    Connection c
  ) {
    PriceTiersId tierId = row.tierId();;
    return concat(Fragment.of("update `price_tiers`\nset `name` = "), Fragment.encode(MariaTypes.varchar, row.name()), Fragment.of(",\n`min_quantity` = "), Fragment.encode(MariaTypes.intUnsigned, row.minQuantity()), Fragment.of(",\n`discount_type` = "), Fragment.encode(MariaTypes.text, row.discountType()), Fragment.of(",\n`discount_value` = "), Fragment.encode(MariaTypes.numeric, row.discountValue()), Fragment.of("\nwhere `tier_id` = "), Fragment.encode(PriceTiersId.mariaType, tierId), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public PriceTiersRow upsert(
    PriceTiersRow unsaved,
    Connection c
  ) {
    return concat(Fragment.of("INSERT INTO `price_tiers`(`tier_id`, `name`, `min_quantity`, `discount_type`, `discount_value`)\nVALUES ("), Fragment.encode(PriceTiersId.mariaType, unsaved.tierId()), Fragment.of(", "), Fragment.encode(MariaTypes.varchar, unsaved.name()), Fragment.of(", "), Fragment.encode(MariaTypes.intUnsigned, unsaved.minQuantity()), Fragment.of(", "), Fragment.encode(MariaTypes.text, unsaved.discountType()), Fragment.of(", "), Fragment.encode(MariaTypes.numeric, unsaved.discountValue()), Fragment.of(")\nON DUPLICATE KEY UPDATE `name` = VALUES(`name`),\n`min_quantity` = VALUES(`min_quantity`),\n`discount_type` = VALUES(`discount_type`),\n`discount_value` = VALUES(`discount_value`)\nRETURNING `tier_id`, `name`, `min_quantity`, `discount_type`, `discount_value`"))
      .updateReturning(PriceTiersRow.rowCodec.exactlyOne())
      .run(c);
  }

  @Override
  public List<PriceTiersRow> upsertBatch(
    Iterator<PriceTiersRow> unsaved,
    Connection c
  ) {
    return concat(Fragment.of("INSERT INTO `price_tiers`(`tier_id`, `name`, `min_quantity`, `discount_type`, `discount_value`)\nVALUES (?, ?, ?, ?, ?)\nON DUPLICATE KEY UPDATE `name` = VALUES(`name`),\n`min_quantity` = VALUES(`min_quantity`),\n`discount_type` = VALUES(`discount_type`),\n`discount_value` = VALUES(`discount_value`)\nRETURNING `tier_id`, `name`, `min_quantity`, `discount_type`, `discount_value`"))
      .updateReturningEach(PriceTiersRow.rowCodec, unsaved)
    .run(c);
  }
}