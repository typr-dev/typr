/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.order_items

import dev.typr.dslsc.DeleteBuilder
import dev.typr.dslsc.Dialect
import dev.typr.dslsc.SelectBuilder
import dev.typr.dslsc.UpdateBuilder
import dev.typr.foundationssc.Connection
import dev.typr.foundationssc.ConnectionRead
import dev.typr.foundationssc.Db2Types
import dev.typr.foundationssc.Fragment
import scala.collection.mutable.ListBuffer
import testdb.orders.OrdersId
import dev.typr.foundationssc.Fragment.sql

class OrderItemsRepoImpl extends OrderItemsRepo {
  override def delete: DeleteBuilder[OrderItemsFields, OrderItemsRow] = DeleteBuilder.of(""""ORDER_ITEMS"""", OrderItemsFields.structure, Dialect.DB2)

  override def deleteById(compositeId: OrderItemsId)(using c: Connection): Boolean = sql"""delete from "ORDER_ITEMS" where "ORDER_ID" = ${Fragment.encode(OrdersId.db2Type, compositeId.orderId)} AND "ITEM_NUMBER" = ${Fragment.encode(Db2Types.integer, compositeId.itemNumber)}""".update().run(using c) > 0

  override def deleteByIds(compositeIds: List[OrderItemsId])(using c: Connection): Int = {
    val fragments: ListBuffer[Fragment] = ListBuffer()
    compositeIds.foreach { id => fragments.addOne(Fragment.concat(Fragment.of("("), Fragment.encode(OrdersId.db2Type, id.orderId), Fragment.of(", "), Fragment.encode(Db2Types.integer, id.itemNumber), Fragment.of(")"))): @scala.annotation.nowarn }
    return Fragment.concat(Fragment.of("""delete from "ORDER_ITEMS" where ("ORDER_ID", "ITEM_NUMBER") in ("""), Fragment.comma(fragments), Fragment.of(")")).update().run(using c)
  }

  override def insert(unsaved: OrderItemsRow)(using c: Connection): OrderItemsRow = {
  sql"""SELECT "ORDER_ID", "ITEM_NUMBER", "PRODUCT_NAME", "QUANTITY", "UNIT_PRICE" FROM FINAL TABLE (INSERT INTO "ORDER_ITEMS"("ORDER_ID", "ITEM_NUMBER", "PRODUCT_NAME", "QUANTITY", "UNIT_PRICE")
    VALUES (${Fragment.encode(OrdersId.db2Type, unsaved.orderId)}, ${Fragment.encode(Db2Types.integer, unsaved.itemNumber)}, ${Fragment.encode(Db2Types.varchar, unsaved.productName)}, ${Fragment.encode(Db2Types.integer, unsaved.quantity)}, ${Fragment.encode(Db2Types.decimal, unsaved.unitPrice)}))
    """
    .updateReturning(OrderItemsRow.rowCodec.exactlyOne()).run(using c)
  }

  override def select: SelectBuilder[OrderItemsFields, OrderItemsRow] = SelectBuilder.of(""""ORDER_ITEMS"""", OrderItemsFields.structure, OrderItemsRow.rowCodec, Dialect.DB2)

  override def selectAll(using c: ConnectionRead): List[OrderItemsRow] = {
    sql"""select "ORDER_ID", "ITEM_NUMBER", "PRODUCT_NAME", "QUANTITY", "UNIT_PRICE"
    from "ORDER_ITEMS"
    """.query(OrderItemsRow.rowCodec.all()).run(using c)
  }

  override def selectById(compositeId: OrderItemsId)(using c: ConnectionRead): Option[OrderItemsRow] = {
    sql"""select "ORDER_ID", "ITEM_NUMBER", "PRODUCT_NAME", "QUANTITY", "UNIT_PRICE"
    from "ORDER_ITEMS"
    where "ORDER_ID" = ${Fragment.encode(OrdersId.db2Type, compositeId.orderId)} AND "ITEM_NUMBER" = ${Fragment.encode(Db2Types.integer, compositeId.itemNumber)}""".query(OrderItemsRow.rowCodec.first()).run(using c)
  }

  override def selectByIds(compositeIds: List[OrderItemsId])(using c: ConnectionRead): List[OrderItemsRow] = {
    val fragments: ListBuffer[Fragment] = ListBuffer()
    compositeIds.foreach { id => fragments.addOne(Fragment.concat(Fragment.of("("), Fragment.encode(OrdersId.db2Type, id.orderId), Fragment.of(", "), Fragment.encode(Db2Types.integer, id.itemNumber), Fragment.of(")"))): @scala.annotation.nowarn }
    return Fragment.concat(Fragment.of("""select "ORDER_ID", "ITEM_NUMBER", "PRODUCT_NAME", "QUANTITY", "UNIT_PRICE" from "ORDER_ITEMS" where ("ORDER_ID", "ITEM_NUMBER") in ("""), Fragment.comma(fragments), Fragment.of(")")).query(OrderItemsRow.rowCodec.all()).run(using c)
  }

  override def selectByIdsTracked(compositeIds: List[OrderItemsId])(using c: ConnectionRead): Map[OrderItemsId, OrderItemsRow] = {
    val ret: scala.collection.mutable.Map[OrderItemsId, OrderItemsRow] = scala.collection.mutable.Map.empty[OrderItemsId, OrderItemsRow]
    selectByIds(compositeIds)(using c).foreach(row => ret.put(row.compositeId, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[OrderItemsFields, OrderItemsRow] = UpdateBuilder.of(""""ORDER_ITEMS"""", OrderItemsFields.structure, OrderItemsRow.rowCodec, Dialect.DB2)

  override def update(row: OrderItemsRow)(using c: Connection): Boolean = {
    val compositeId: OrderItemsId = row.compositeId
    return sql"""update "ORDER_ITEMS"
    set "PRODUCT_NAME" = ${Fragment.encode(Db2Types.varchar, row.productName)},
    "QUANTITY" = ${Fragment.encode(Db2Types.integer, row.quantity)},
    "UNIT_PRICE" = ${Fragment.encode(Db2Types.decimal, row.unitPrice)}
    where "ORDER_ID" = ${Fragment.encode(OrdersId.db2Type, compositeId.orderId)} AND "ITEM_NUMBER" = ${Fragment.encode(Db2Types.integer, compositeId.itemNumber)}""".update().run(using c) > 0
  }

  override def upsert(unsaved: OrderItemsRow)(using c: Connection): Unit = {
    sql"""MERGE INTO "ORDER_ITEMS" AS t
    USING (VALUES (${Fragment.encode(OrdersId.db2Type, unsaved.orderId)}, ${Fragment.encode(Db2Types.integer, unsaved.itemNumber)}, ${Fragment.encode(Db2Types.varchar, unsaved.productName)}, ${Fragment.encode(Db2Types.integer, unsaved.quantity)}, ${Fragment.encode(Db2Types.decimal, unsaved.unitPrice)})) AS s("ORDER_ID", "ITEM_NUMBER", "PRODUCT_NAME", "QUANTITY", "UNIT_PRICE")
    ON t."ORDER_ID" = s."ORDER_ID" AND t."ITEM_NUMBER" = s."ITEM_NUMBER"
    WHEN MATCHED THEN UPDATE SET "PRODUCT_NAME" = s."PRODUCT_NAME",
    "QUANTITY" = s."QUANTITY",
    "UNIT_PRICE" = s."UNIT_PRICE"
    WHEN NOT MATCHED THEN INSERT ("ORDER_ID", "ITEM_NUMBER", "PRODUCT_NAME", "QUANTITY", "UNIT_PRICE") VALUES (${Fragment.encode(OrdersId.db2Type, unsaved.orderId)}, ${Fragment.encode(Db2Types.integer, unsaved.itemNumber)}, ${Fragment.encode(Db2Types.varchar, unsaved.productName)}, ${Fragment.encode(Db2Types.integer, unsaved.quantity)}, ${Fragment.encode(Db2Types.decimal, unsaved.unitPrice)})"""
      .update()
      .run(using c): @scala.annotation.nowarn
  }

  override def upsertBatch(unsaved: Iterator[OrderItemsRow])(using c: Connection): Unit = {
    sql"""MERGE INTO "ORDER_ITEMS" AS t
    USING (VALUES (?, ?, ?, ?, ?)) AS s("ORDER_ID", "ITEM_NUMBER", "PRODUCT_NAME", "QUANTITY", "UNIT_PRICE")
    ON t."ORDER_ID" = s."ORDER_ID" AND t."ITEM_NUMBER" = s."ITEM_NUMBER"
    WHEN MATCHED THEN UPDATE SET "PRODUCT_NAME" = s."PRODUCT_NAME",
    "QUANTITY" = s."QUANTITY",
    "UNIT_PRICE" = s."UNIT_PRICE"
    WHEN NOT MATCHED THEN INSERT ("ORDER_ID", "ITEM_NUMBER", "PRODUCT_NAME", "QUANTITY", "UNIT_PRICE") VALUES (?, ?, ?, ?, ?)"""
      .updateMany(OrderItemsRow.rowCodec, unsaved)
      .run(using c): @scala.annotation.nowarn
  }
}