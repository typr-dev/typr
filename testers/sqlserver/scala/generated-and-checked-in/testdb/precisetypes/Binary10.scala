/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.precisetypes

import com.fasterxml.jackson.annotation.JsonValue
import dev.typr.foundations.SqlServerType
import dev.typr.foundations.SqlServerTypes
import dev.typr.foundations.data.precise.BinaryN
import dev.typr.foundations.scala.Bijection
import java.lang.IllegalArgumentException
import java.util.Arrays

case class Binary10 private(@JsonValue value: Array[Byte]) extends BinaryN {
  override def rawValue: Array[Byte] = value

  override def maxLength: Int = 10

  override def semanticEquals(other: BinaryN): Boolean = (if (other == null) false else Arrays.equals(value, other.rawValue()))

  override def semanticHashCode: Int = Arrays.hashCode(value)

  override def equals(that: Any): Boolean = (this eq that.asInstanceOf[AnyRef]) || (that match { case other: BinaryN => Arrays.equals(value, other.rawValue()); case _ => false })

  override def hashCode: Int = Arrays.hashCode(value)
}

object Binary10 {
  given bijection: Bijection[Binary10, Array[Byte]] = Bijection.apply[Binary10, Array[Byte]](_.value)(Binary10.apply)

  def of(value: Array[Byte]): Option[Binary10] = (if (value.length <= 10) Some(new Binary10(value)) else None)

  given sqlServerType: SqlServerType[Binary10] = SqlServerTypes.binary.bimap(Binary10.apply, _.value)

  def unsafeForce(value: Array[Byte]): Binary10 = {
    if (value.length > 10) {
      throw new IllegalArgumentException(s"Value length ${value.length} exceeds maximum 10")
    }
    return new Binary10(value)
  }
}