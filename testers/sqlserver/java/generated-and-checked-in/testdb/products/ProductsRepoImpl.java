/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.products;

import dev.typr.dsl.DeleteBuilder;
import dev.typr.dsl.Dialect;
import dev.typr.dsl.SelectBuilder;
import dev.typr.dsl.UpdateBuilder;
import dev.typr.foundations.Connection;
import dev.typr.foundations.ConnectionRead;
import dev.typr.foundations.Fragment;
import dev.typr.foundations.SqlServerTypes;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import static dev.typr.foundations.Fragment.concat;

public class ProductsRepoImpl implements ProductsRepo {
  @Override
  public DeleteBuilder<ProductsFields, ProductsRow> delete() {
    return DeleteBuilder.of("[products]", ProductsFields.structure, Dialect.SQLSERVER);
  }

  @Override
  public Boolean deleteById(
    ProductsId productId,
    Connection c
  ) {
    return concat(Fragment.of("delete from [products] where [product_id] = "), Fragment.encode(ProductsId.sqlServerType, productId), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public Integer deleteByIds(
    List<ProductsId> productIds,
    Connection c
  ) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : productIds) { fragments.add(Fragment.encode(ProductsId.sqlServerType, id)); };
    return Fragment.concat(Fragment.of("delete from [products] where [product_id] in ("), Fragment.comma(fragments), Fragment.of(")")).update().run(c);
  }

  @Override
  public ProductsRow insert(
    ProductsRow unsaved,
    Connection c
  ) {
    return concat(Fragment.of("insert into [products]([name], [price], [description])\nOUTPUT INSERTED.[product_id], INSERTED.[name], INSERTED.[price], INSERTED.[description]\nvalues ("), Fragment.encode(SqlServerTypes.nvarchar, unsaved.name()), Fragment.of(", "), Fragment.encode(SqlServerTypes.money, unsaved.price()), Fragment.of(", "), Fragment.encode(SqlServerTypes.nvarchar.opt(), unsaved.description()), Fragment.of(")\n"))
      .updateReturning(ProductsRow.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public ProductsRow insert(
    ProductsRowUnsaved unsaved,
    Connection c
  ) {
    ArrayList<Fragment> columns = new ArrayList<>();;
    ArrayList<Fragment> values = new ArrayList<>();;
    columns.add(Fragment.of("[name]"));
    values.add(concat(Fragment.encode(SqlServerTypes.nvarchar, unsaved.name()), Fragment.of("")));
    columns.add(Fragment.of("[price]"));
    values.add(concat(Fragment.encode(SqlServerTypes.money, unsaved.price()), Fragment.of("")));
    columns.add(Fragment.of("[description]"));
    values.add(concat(Fragment.encode(SqlServerTypes.nvarchar.opt(), unsaved.description()), Fragment.of("")));
    Fragment q = concat(Fragment.of("insert into [products]("), Fragment.comma(columns), Fragment.of(")\nOUTPUT INSERTED.[product_id], INSERTED.[name], INSERTED.[price], INSERTED.[description]\nvalues ("), Fragment.comma(values), Fragment.of(")\n"));;
    return q.updateReturning(ProductsRow.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public SelectBuilder<ProductsFields, ProductsRow> select() {
    return SelectBuilder.of("[products]", ProductsFields.structure, ProductsRow.rowCodec, Dialect.SQLSERVER);
  }

  @Override
  public List<ProductsRow> selectAll(ConnectionRead c) {
    return concat(Fragment.of("select [product_id], [name], [price], [description]\nfrom [products]\n")).query(ProductsRow.rowCodec.all()).run(c);
  }

  @Override
  public Optional<ProductsRow> selectById(
    ProductsId productId,
    ConnectionRead c
  ) {
    return concat(Fragment.of("select [product_id], [name], [price], [description]\nfrom [products]\nwhere [product_id] = "), Fragment.encode(ProductsId.sqlServerType, productId), Fragment.of("")).query(ProductsRow.rowCodec.first()).run(c);
  }

  @Override
  public List<ProductsRow> selectByIds(
    List<ProductsId> productIds,
    ConnectionRead c
  ) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : productIds) { fragments.add(Fragment.encode(ProductsId.sqlServerType, id)); };
    return Fragment.concat(Fragment.of("select [product_id], [name], [price], [description] from [products] where [product_id] in ("), Fragment.comma(fragments), Fragment.of(")")).query(ProductsRow.rowCodec.all()).run(c);
  }

  @Override
  public Map<ProductsId, ProductsRow> selectByIdsTracked(
    List<ProductsId> productIds,
    ConnectionRead c
  ) {
    HashMap<ProductsId, ProductsRow> ret = new HashMap<ProductsId, ProductsRow>();
    selectByIds(productIds, c).forEach(row -> ret.put(row.productId(), row));
    return ret;
  }

  @Override
  public UpdateBuilder<ProductsFields, ProductsRow> update() {
    return UpdateBuilder.of("[products]", ProductsFields.structure, ProductsRow.rowCodec, Dialect.SQLSERVER);
  }

  @Override
  public Boolean update(
    ProductsRow row,
    Connection c
  ) {
    ProductsId productId = row.productId();;
    return concat(Fragment.of("update [products]\nset [name] = "), Fragment.encode(SqlServerTypes.nvarchar, row.name()), Fragment.of(",\n[price] = "), Fragment.encode(SqlServerTypes.money, row.price()), Fragment.of(",\n[description] = "), Fragment.encode(SqlServerTypes.nvarchar.opt(), row.description()), Fragment.of("\nwhere [product_id] = "), Fragment.encode(ProductsId.sqlServerType, productId), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public ProductsRow upsert(
    ProductsRow unsaved,
    Connection c
  ) {
    return concat(Fragment.of("MERGE INTO [products] AS target\nUSING (VALUES ("), Fragment.encode(ProductsId.sqlServerType, unsaved.productId()), Fragment.of(", "), Fragment.encode(SqlServerTypes.nvarchar, unsaved.name()), Fragment.of(", "), Fragment.encode(SqlServerTypes.money, unsaved.price()), Fragment.of(", "), Fragment.encode(SqlServerTypes.nvarchar.opt(), unsaved.description()), Fragment.of(")) AS source([product_id], [name], [price], [description])\nON target.[product_id] = source.[product_id]\nWHEN MATCHED THEN UPDATE SET [name] = source.[name],\n[price] = source.[price],\n[description] = source.[description]\nWHEN NOT MATCHED THEN INSERT ([product_id], [name], [price], [description]) VALUES ("), Fragment.encode(ProductsId.sqlServerType, unsaved.productId()), Fragment.of(", "), Fragment.encode(SqlServerTypes.nvarchar, unsaved.name()), Fragment.of(", "), Fragment.encode(SqlServerTypes.money, unsaved.price()), Fragment.of(", "), Fragment.encode(SqlServerTypes.nvarchar.opt(), unsaved.description()), Fragment.of(")\nOUTPUT INSERTED.[product_id], INSERTED.[name], INSERTED.[price], INSERTED.[description];"))
      .updateReturning(ProductsRow.rowCodec.exactlyOne())
      .run(c);
  }

  @Override
  public List<ProductsRow> upsertBatch(
    Iterator<ProductsRow> unsaved,
    Connection c
  ) {
    return concat(Fragment.of("MERGE INTO [products] AS target\nUSING (VALUES (?, ?, ?, ?)) AS source([product_id], [name], [price], [description])\nON target.[product_id] = source.[product_id]\nWHEN MATCHED THEN UPDATE SET [name] = source.[name],\n[price] = source.[price],\n[description] = source.[description]\nWHEN NOT MATCHED THEN INSERT ([product_id], [name], [price], [description]) VALUES (?, ?, ?, ?)\nOUTPUT INSERTED.[product_id], INSERTED.[name], INSERTED.[price], INSERTED.[description];"))
      .updateReturningEach(ProductsRow.rowCodec, unsaved)
    .run(c);
  }
}