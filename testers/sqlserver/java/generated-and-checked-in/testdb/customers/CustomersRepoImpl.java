/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.customers;

import dev.typr.dsl.DeleteBuilder;
import dev.typr.dsl.Dialect;
import dev.typr.dsl.SelectBuilder;
import dev.typr.dsl.UpdateBuilder;
import dev.typr.foundations.Connection;
import dev.typr.foundations.ConnectionRead;
import dev.typr.foundations.Fragment;
import dev.typr.foundations.SqlServerTypes;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import testdb.userdefined.Email;
import static dev.typr.foundations.Fragment.concat;

public class CustomersRepoImpl implements CustomersRepo {
  @Override
  public DeleteBuilder<CustomersFields, CustomersRow> delete() {
    return DeleteBuilder.of("[customers]", CustomersFields.structure, Dialect.SQLSERVER);
  }

  @Override
  public Boolean deleteById(
    CustomersId customerId,
    Connection c
  ) {
    return concat(Fragment.of("delete from [customers] where [customer_id] = "), Fragment.encode(CustomersId.sqlServerType, customerId), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public Integer deleteByIds(
    List<CustomersId> customerIds,
    Connection c
  ) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : customerIds) { fragments.add(Fragment.encode(CustomersId.sqlServerType, id)); };
    return Fragment.concat(Fragment.of("delete from [customers] where [customer_id] in ("), Fragment.comma(fragments), Fragment.of(")")).update().run(c);
  }

  @Override
  public CustomersRow insert(
    CustomersRow unsaved,
    Connection c
  ) {
    return concat(Fragment.of("insert into [customers]([name], [email], [created_at])\nOUTPUT INSERTED.[customer_id], INSERTED.[name], INSERTED.[email], INSERTED.[created_at]\nvalues ("), Fragment.encode(SqlServerTypes.nvarchar, unsaved.name()), Fragment.of(", "), Fragment.encode(Email.sqlServerType, unsaved.email()), Fragment.of(", "), Fragment.encode(SqlServerTypes.datetime2.opt(), unsaved.createdAt()), Fragment.of(")\n"))
      .updateReturning(CustomersRow.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public CustomersRow insert(
    CustomersRowUnsaved unsaved,
    Connection c
  ) {
    ArrayList<Fragment> columns = new ArrayList<>();;
    ArrayList<Fragment> values = new ArrayList<>();;
    columns.add(Fragment.of("[name]"));
    values.add(concat(Fragment.encode(SqlServerTypes.nvarchar, unsaved.name()), Fragment.of("")));
    columns.add(Fragment.of("[email]"));
    values.add(concat(Fragment.encode(Email.sqlServerType, unsaved.email()), Fragment.of("")));
    unsaved.createdAt().visit(
      () -> {
  
      },
      value -> {
        columns.add(Fragment.of("[created_at]"));
        values.add(concat(Fragment.encode(SqlServerTypes.datetime2.opt(), value), Fragment.of("")));
      }
    );;
    Fragment q = concat(Fragment.of("insert into [customers]("), Fragment.comma(columns), Fragment.of(")\nOUTPUT INSERTED.[customer_id], INSERTED.[name], INSERTED.[email], INSERTED.[created_at]\nvalues ("), Fragment.comma(values), Fragment.of(")\n"));;
    return q.updateReturning(CustomersRow.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public SelectBuilder<CustomersFields, CustomersRow> select() {
    return SelectBuilder.of("[customers]", CustomersFields.structure, CustomersRow.rowCodec, Dialect.SQLSERVER);
  }

  @Override
  public List<CustomersRow> selectAll(ConnectionRead c) {
    return concat(Fragment.of("select [customer_id], [name], [email], [created_at]\nfrom [customers]\n")).query(CustomersRow.rowCodec.all()).run(c);
  }

  @Override
  public Optional<CustomersRow> selectById(
    CustomersId customerId,
    ConnectionRead c
  ) {
    return concat(Fragment.of("select [customer_id], [name], [email], [created_at]\nfrom [customers]\nwhere [customer_id] = "), Fragment.encode(CustomersId.sqlServerType, customerId), Fragment.of("")).query(CustomersRow.rowCodec.first()).run(c);
  }

  @Override
  public List<CustomersRow> selectByIds(
    List<CustomersId> customerIds,
    ConnectionRead c
  ) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : customerIds) { fragments.add(Fragment.encode(CustomersId.sqlServerType, id)); };
    return Fragment.concat(Fragment.of("select [customer_id], [name], [email], [created_at] from [customers] where [customer_id] in ("), Fragment.comma(fragments), Fragment.of(")")).query(CustomersRow.rowCodec.all()).run(c);
  }

  @Override
  public Map<CustomersId, CustomersRow> selectByIdsTracked(
    List<CustomersId> customerIds,
    ConnectionRead c
  ) {
    HashMap<CustomersId, CustomersRow> ret = new HashMap<CustomersId, CustomersRow>();
    selectByIds(customerIds, c).forEach(row -> ret.put(row.customerId(), row));
    return ret;
  }

  @Override
  public Optional<CustomersRow> selectByUniqueEmail(
    /* user-picked */ Email email,
    ConnectionRead c
  ) {
    return concat(Fragment.of("select [customer_id], [name], [email], [created_at]\nfrom [customers]\nwhere [email] = "), Fragment.encode(Email.sqlServerType, email), Fragment.of("\n")).query(CustomersRow.rowCodec.first()).run(c);
  }

  @Override
  public UpdateBuilder<CustomersFields, CustomersRow> update() {
    return UpdateBuilder.of("[customers]", CustomersFields.structure, CustomersRow.rowCodec, Dialect.SQLSERVER);
  }

  @Override
  public Boolean update(
    CustomersRow row,
    Connection c
  ) {
    CustomersId customerId = row.customerId();;
    return concat(Fragment.of("update [customers]\nset [name] = "), Fragment.encode(SqlServerTypes.nvarchar, row.name()), Fragment.of(",\n[email] = "), Fragment.encode(Email.sqlServerType, row.email()), Fragment.of(",\n[created_at] = "), Fragment.encode(SqlServerTypes.datetime2.opt(), row.createdAt()), Fragment.of("\nwhere [customer_id] = "), Fragment.encode(CustomersId.sqlServerType, customerId), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public CustomersRow upsert(
    CustomersRow unsaved,
    Connection c
  ) {
    return concat(Fragment.of("MERGE INTO [customers] AS target\nUSING (VALUES ("), Fragment.encode(CustomersId.sqlServerType, unsaved.customerId()), Fragment.of(", "), Fragment.encode(SqlServerTypes.nvarchar, unsaved.name()), Fragment.of(", "), Fragment.encode(Email.sqlServerType, unsaved.email()), Fragment.of(", "), Fragment.encode(SqlServerTypes.datetime2.opt(), unsaved.createdAt()), Fragment.of(")) AS source([customer_id], [name], [email], [created_at])\nON target.[customer_id] = source.[customer_id]\nWHEN MATCHED THEN UPDATE SET [name] = source.[name],\n[email] = source.[email],\n[created_at] = source.[created_at]\nWHEN NOT MATCHED THEN INSERT ([customer_id], [name], [email], [created_at]) VALUES ("), Fragment.encode(CustomersId.sqlServerType, unsaved.customerId()), Fragment.of(", "), Fragment.encode(SqlServerTypes.nvarchar, unsaved.name()), Fragment.of(", "), Fragment.encode(Email.sqlServerType, unsaved.email()), Fragment.of(", "), Fragment.encode(SqlServerTypes.datetime2.opt(), unsaved.createdAt()), Fragment.of(")\nOUTPUT INSERTED.[customer_id], INSERTED.[name], INSERTED.[email], INSERTED.[created_at];"))
      .updateReturning(CustomersRow.rowCodec.exactlyOne())
      .run(c);
  }

  @Override
  public List<CustomersRow> upsertBatch(
    Iterator<CustomersRow> unsaved,
    Connection c
  ) {
    return concat(Fragment.of("MERGE INTO [customers] AS target\nUSING (VALUES (?, ?, ?, ?)) AS source([customer_id], [name], [email], [created_at])\nON target.[customer_id] = source.[customer_id]\nWHEN MATCHED THEN UPDATE SET [name] = source.[name],\n[email] = source.[email],\n[created_at] = source.[created_at]\nWHEN NOT MATCHED THEN INSERT ([customer_id], [name], [email], [created_at]) VALUES (?, ?, ?, ?)\nOUTPUT INSERTED.[customer_id], INSERTED.[name], INSERTED.[email], INSERTED.[created_at];"))
      .updateReturningEach(CustomersRow.rowCodec, unsaved)
    .run(c);
  }
}