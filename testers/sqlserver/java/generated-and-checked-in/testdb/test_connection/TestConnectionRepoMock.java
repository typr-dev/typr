/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.test_connection;

import dev.typr.dsl.DeleteBuilder;
import dev.typr.dsl.DeleteBuilderMock;
import dev.typr.dsl.DeleteParams;
import dev.typr.dsl.SelectBuilder;
import dev.typr.dsl.SelectBuilderMock;
import dev.typr.dsl.SelectParams;
import dev.typr.dsl.UpdateBuilder;
import dev.typr.dsl.UpdateBuilderMock;
import dev.typr.dsl.UpdateParams;
import dev.typr.foundations.Connection;
import dev.typr.foundations.ConnectionRead;
import java.lang.RuntimeException;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;

public record TestConnectionRepoMock(
  java.util.function.Function<TestConnectionRowUnsaved, TestConnectionRow> toRow,
  HashMap<TestConnectionId, TestConnectionRow> map
) implements TestConnectionRepo {
  public TestConnectionRepoMock(java.util.function.Function<TestConnectionRowUnsaved, TestConnectionRow> toRow) {
    this(toRow, new HashMap<TestConnectionId, TestConnectionRow>());
  }

  public TestConnectionRepoMock withToRow(java.util.function.Function<TestConnectionRowUnsaved, TestConnectionRow> toRow) {
    return new TestConnectionRepoMock(toRow, map);
  }

  public TestConnectionRepoMock withMap(HashMap<TestConnectionId, TestConnectionRow> map) {
    return new TestConnectionRepoMock(toRow, map);
  }

  @Override
  public DeleteBuilder<TestConnectionFields, TestConnectionRow> delete() {
    return new DeleteBuilderMock<>(TestConnectionFields.structure, () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.id(), id -> map.remove(id));
  }

  @Override
  public Boolean deleteById(
    TestConnectionId id,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(id)).isPresent();
  }

  @Override
  public Integer deleteByIds(
    List<TestConnectionId> ids,
    Connection c
  ) {
    var count = 0;
    for (var id : ids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    }
    };
    return count;
  }

  @Override
  public TestConnectionRow insert(
    TestConnectionRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.id())) {
      throw new RuntimeException("id " + unsaved.id() + " already exists");
    }
    map.put(unsaved.id(), unsaved);
    return unsaved;
  }

  @Override
  public TestConnectionRow insert(
    TestConnectionRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  }

  @Override
  public SelectBuilder<TestConnectionFields, TestConnectionRow> select() {
    return new SelectBuilderMock<>(TestConnectionFields.structure, () -> new ArrayList<>(map.values()), SelectParams.empty());
  }

  @Override
  public List<TestConnectionRow> selectAll(ConnectionRead c) {
    return new ArrayList<>(map.values());
  }

  @Override
  public Optional<TestConnectionRow> selectById(
    TestConnectionId id,
    ConnectionRead c
  ) {
    return Optional.ofNullable(map.get(id));
  }

  @Override
  public List<TestConnectionRow> selectByIds(
    List<TestConnectionId> ids,
    ConnectionRead c
  ) {
    var result = new ArrayList<TestConnectionRow>();
    for (var id : ids) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    }
    };
    return result;
  }

  @Override
  public Map<TestConnectionId, TestConnectionRow> selectByIdsTracked(
    List<TestConnectionId> ids,
    ConnectionRead c
  ) {
    return selectByIds(ids, c).stream().collect(Collectors.toMap((TestConnectionRow row) -> row.id(), Function.identity()));
  }

  @Override
  public UpdateBuilder<TestConnectionFields, TestConnectionRow> update() {
    return new UpdateBuilderMock<>(TestConnectionFields.structure, () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  }

  @Override
  public Boolean update(
    TestConnectionRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.id())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.id(), row);;
    }
    return shouldUpdate;
  }

  @Override
  public TestConnectionRow upsert(
    TestConnectionRow unsaved,
    Connection c
  ) {
    map.put(unsaved.id(), unsaved);
    return unsaved;
  }

  @Override
  public List<TestConnectionRow> upsertBatch(
    Iterator<TestConnectionRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<TestConnectionRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.id(), row);
      result.add(row);
    };
    return result;
  }
}