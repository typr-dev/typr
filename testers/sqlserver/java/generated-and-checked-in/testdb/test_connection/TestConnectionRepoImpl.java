/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.test_connection;

import dev.typr.dsl.DeleteBuilder;
import dev.typr.dsl.Dialect;
import dev.typr.dsl.SelectBuilder;
import dev.typr.dsl.UpdateBuilder;
import dev.typr.foundations.Connection;
import dev.typr.foundations.ConnectionRead;
import dev.typr.foundations.Fragment;
import dev.typr.foundations.SqlServerTypes;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import static dev.typr.foundations.Fragment.concat;

public class TestConnectionRepoImpl implements TestConnectionRepo {
  @Override
  public DeleteBuilder<TestConnectionFields, TestConnectionRow> delete() {
    return DeleteBuilder.of("[test_connection]", TestConnectionFields.structure, Dialect.SQLSERVER);
  }

  @Override
  public Boolean deleteById(
    TestConnectionId id,
    Connection c
  ) {
    return concat(Fragment.of("delete from [test_connection] where [id] = "), Fragment.encode(TestConnectionId.sqlServerType, id), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public Integer deleteByIds(
    List<TestConnectionId> ids,
    Connection c
  ) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : ids) { fragments.add(Fragment.encode(TestConnectionId.sqlServerType, id)); };
    return Fragment.concat(Fragment.of("delete from [test_connection] where [id] in ("), Fragment.comma(fragments), Fragment.of(")")).update().run(c);
  }

  @Override
  public TestConnectionRow insert(
    TestConnectionRow unsaved,
    Connection c
  ) {
    return concat(Fragment.of("insert into [test_connection]([message], [created_at])\nOUTPUT INSERTED.[id], INSERTED.[message], INSERTED.[created_at]\nvalues ("), Fragment.encode(SqlServerTypes.nvarchar, unsaved.message()), Fragment.of(", "), Fragment.encode(SqlServerTypes.datetime2.opt(), unsaved.createdAt()), Fragment.of(")\n"))
      .updateReturning(TestConnectionRow.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public TestConnectionRow insert(
    TestConnectionRowUnsaved unsaved,
    Connection c
  ) {
    ArrayList<Fragment> columns = new ArrayList<>();;
    ArrayList<Fragment> values = new ArrayList<>();;
    columns.add(Fragment.of("[message]"));
    values.add(concat(Fragment.encode(SqlServerTypes.nvarchar, unsaved.message()), Fragment.of("")));
    unsaved.createdAt().visit(
      () -> {
  
      },
      value -> {
        columns.add(Fragment.of("[created_at]"));
        values.add(concat(Fragment.encode(SqlServerTypes.datetime2.opt(), value), Fragment.of("")));
      }
    );;
    Fragment q = concat(Fragment.of("insert into [test_connection]("), Fragment.comma(columns), Fragment.of(")\nOUTPUT INSERTED.[id], INSERTED.[message], INSERTED.[created_at]\nvalues ("), Fragment.comma(values), Fragment.of(")\n"));;
    return q.updateReturning(TestConnectionRow.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public SelectBuilder<TestConnectionFields, TestConnectionRow> select() {
    return SelectBuilder.of("[test_connection]", TestConnectionFields.structure, TestConnectionRow.rowCodec, Dialect.SQLSERVER);
  }

  @Override
  public List<TestConnectionRow> selectAll(ConnectionRead c) {
    return concat(Fragment.of("select [id], [message], [created_at]\nfrom [test_connection]\n")).query(TestConnectionRow.rowCodec.all()).run(c);
  }

  @Override
  public Optional<TestConnectionRow> selectById(
    TestConnectionId id,
    ConnectionRead c
  ) {
    return concat(Fragment.of("select [id], [message], [created_at]\nfrom [test_connection]\nwhere [id] = "), Fragment.encode(TestConnectionId.sqlServerType, id), Fragment.of("")).query(TestConnectionRow.rowCodec.first()).run(c);
  }

  @Override
  public List<TestConnectionRow> selectByIds(
    List<TestConnectionId> ids,
    ConnectionRead c
  ) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : ids) { fragments.add(Fragment.encode(TestConnectionId.sqlServerType, id)); };
    return Fragment.concat(Fragment.of("select [id], [message], [created_at] from [test_connection] where [id] in ("), Fragment.comma(fragments), Fragment.of(")")).query(TestConnectionRow.rowCodec.all()).run(c);
  }

  @Override
  public Map<TestConnectionId, TestConnectionRow> selectByIdsTracked(
    List<TestConnectionId> ids,
    ConnectionRead c
  ) {
    HashMap<TestConnectionId, TestConnectionRow> ret = new HashMap<TestConnectionId, TestConnectionRow>();
    selectByIds(ids, c).forEach(row -> ret.put(row.id(), row));
    return ret;
  }

  @Override
  public UpdateBuilder<TestConnectionFields, TestConnectionRow> update() {
    return UpdateBuilder.of("[test_connection]", TestConnectionFields.structure, TestConnectionRow.rowCodec, Dialect.SQLSERVER);
  }

  @Override
  public Boolean update(
    TestConnectionRow row,
    Connection c
  ) {
    TestConnectionId id = row.id();;
    return concat(Fragment.of("update [test_connection]\nset [message] = "), Fragment.encode(SqlServerTypes.nvarchar, row.message()), Fragment.of(",\n[created_at] = "), Fragment.encode(SqlServerTypes.datetime2.opt(), row.createdAt()), Fragment.of("\nwhere [id] = "), Fragment.encode(TestConnectionId.sqlServerType, id), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public TestConnectionRow upsert(
    TestConnectionRow unsaved,
    Connection c
  ) {
    return concat(Fragment.of("MERGE INTO [test_connection] AS target\nUSING (VALUES ("), Fragment.encode(TestConnectionId.sqlServerType, unsaved.id()), Fragment.of(", "), Fragment.encode(SqlServerTypes.nvarchar, unsaved.message()), Fragment.of(", "), Fragment.encode(SqlServerTypes.datetime2.opt(), unsaved.createdAt()), Fragment.of(")) AS source([id], [message], [created_at])\nON target.[id] = source.[id]\nWHEN MATCHED THEN UPDATE SET [message] = source.[message],\n[created_at] = source.[created_at]\nWHEN NOT MATCHED THEN INSERT ([id], [message], [created_at]) VALUES ("), Fragment.encode(TestConnectionId.sqlServerType, unsaved.id()), Fragment.of(", "), Fragment.encode(SqlServerTypes.nvarchar, unsaved.message()), Fragment.of(", "), Fragment.encode(SqlServerTypes.datetime2.opt(), unsaved.createdAt()), Fragment.of(")\nOUTPUT INSERTED.[id], INSERTED.[message], INSERTED.[created_at];"))
      .updateReturning(TestConnectionRow.rowCodec.exactlyOne())
      .run(c);
  }

  @Override
  public List<TestConnectionRow> upsertBatch(
    Iterator<TestConnectionRow> unsaved,
    Connection c
  ) {
    return concat(Fragment.of("MERGE INTO [test_connection] AS target\nUSING (VALUES (?, ?, ?)) AS source([id], [message], [created_at])\nON target.[id] = source.[id]\nWHEN MATCHED THEN UPDATE SET [message] = source.[message],\n[created_at] = source.[created_at]\nWHEN NOT MATCHED THEN INSERT ([id], [message], [created_at]) VALUES (?, ?, ?)\nOUTPUT INSERTED.[id], INSERTED.[message], INSERTED.[created_at];"))
      .updateReturningEach(TestConnectionRow.rowCodec, unsaved)
    .run(c);
  }
}