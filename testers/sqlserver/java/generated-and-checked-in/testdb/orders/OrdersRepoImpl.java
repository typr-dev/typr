/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.orders;

import dev.typr.dsl.DeleteBuilder;
import dev.typr.dsl.Dialect;
import dev.typr.dsl.SelectBuilder;
import dev.typr.dsl.UpdateBuilder;
import dev.typr.foundations.Connection;
import dev.typr.foundations.ConnectionRead;
import dev.typr.foundations.Fragment;
import dev.typr.foundations.SqlServerTypes;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import testdb.customers.CustomersId;
import static dev.typr.foundations.Fragment.concat;

public class OrdersRepoImpl implements OrdersRepo {
  @Override
  public DeleteBuilder<OrdersFields, OrdersRow> delete() {
    return DeleteBuilder.of("[orders]", OrdersFields.structure, Dialect.SQLSERVER);
  }

  @Override
  public Boolean deleteById(
    OrdersId orderId,
    Connection c
  ) {
    return concat(Fragment.of("delete from [orders] where [order_id] = "), Fragment.encode(OrdersId.sqlServerType, orderId), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public Integer deleteByIds(
    List<OrdersId> orderIds,
    Connection c
  ) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : orderIds) { fragments.add(Fragment.encode(OrdersId.sqlServerType, id)); };
    return Fragment.concat(Fragment.of("delete from [orders] where [order_id] in ("), Fragment.comma(fragments), Fragment.of(")")).update().run(c);
  }

  @Override
  public OrdersRow insert(
    OrdersRow unsaved,
    Connection c
  ) {
    return concat(Fragment.of("insert into [orders]([customer_id], [order_date], [total_amount])\nOUTPUT INSERTED.[order_id], INSERTED.[customer_id], INSERTED.[order_date], INSERTED.[total_amount]\nvalues ("), Fragment.encode(CustomersId.sqlServerType, unsaved.customerId()), Fragment.of(", "), Fragment.encode(SqlServerTypes.datetime2.opt(), unsaved.orderDate()), Fragment.of(", "), Fragment.encode(SqlServerTypes.money, unsaved.totalAmount()), Fragment.of(")\n"))
      .updateReturning(OrdersRow.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public OrdersRow insert(
    OrdersRowUnsaved unsaved,
    Connection c
  ) {
    ArrayList<Fragment> columns = new ArrayList<>();;
    ArrayList<Fragment> values = new ArrayList<>();;
    columns.add(Fragment.of("[customer_id]"));
    values.add(concat(Fragment.encode(CustomersId.sqlServerType, unsaved.customerId()), Fragment.of("")));
    columns.add(Fragment.of("[total_amount]"));
    values.add(concat(Fragment.encode(SqlServerTypes.money, unsaved.totalAmount()), Fragment.of("")));
    unsaved.orderDate().visit(
      () -> {
  
      },
      value -> {
        columns.add(Fragment.of("[order_date]"));
        values.add(concat(Fragment.encode(SqlServerTypes.datetime2.opt(), value), Fragment.of("")));
      }
    );;
    Fragment q = concat(Fragment.of("insert into [orders]("), Fragment.comma(columns), Fragment.of(")\nOUTPUT INSERTED.[order_id], INSERTED.[customer_id], INSERTED.[order_date], INSERTED.[total_amount]\nvalues ("), Fragment.comma(values), Fragment.of(")\n"));;
    return q.updateReturning(OrdersRow.rowCodec.exactlyOne()).run(c);
  }

  @Override
  public SelectBuilder<OrdersFields, OrdersRow> select() {
    return SelectBuilder.of("[orders]", OrdersFields.structure, OrdersRow.rowCodec, Dialect.SQLSERVER);
  }

  @Override
  public List<OrdersRow> selectAll(ConnectionRead c) {
    return concat(Fragment.of("select [order_id], [customer_id], [order_date], [total_amount]\nfrom [orders]\n")).query(OrdersRow.rowCodec.all()).run(c);
  }

  @Override
  public Optional<OrdersRow> selectById(
    OrdersId orderId,
    ConnectionRead c
  ) {
    return concat(Fragment.of("select [order_id], [customer_id], [order_date], [total_amount]\nfrom [orders]\nwhere [order_id] = "), Fragment.encode(OrdersId.sqlServerType, orderId), Fragment.of("")).query(OrdersRow.rowCodec.first()).run(c);
  }

  @Override
  public List<OrdersRow> selectByIds(
    List<OrdersId> orderIds,
    ConnectionRead c
  ) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : orderIds) { fragments.add(Fragment.encode(OrdersId.sqlServerType, id)); };
    return Fragment.concat(Fragment.of("select [order_id], [customer_id], [order_date], [total_amount] from [orders] where [order_id] in ("), Fragment.comma(fragments), Fragment.of(")")).query(OrdersRow.rowCodec.all()).run(c);
  }

  @Override
  public Map<OrdersId, OrdersRow> selectByIdsTracked(
    List<OrdersId> orderIds,
    ConnectionRead c
  ) {
    HashMap<OrdersId, OrdersRow> ret = new HashMap<OrdersId, OrdersRow>();
    selectByIds(orderIds, c).forEach(row -> ret.put(row.orderId(), row));
    return ret;
  }

  @Override
  public UpdateBuilder<OrdersFields, OrdersRow> update() {
    return UpdateBuilder.of("[orders]", OrdersFields.structure, OrdersRow.rowCodec, Dialect.SQLSERVER);
  }

  @Override
  public Boolean update(
    OrdersRow row,
    Connection c
  ) {
    OrdersId orderId = row.orderId();;
    return concat(Fragment.of("update [orders]\nset [customer_id] = "), Fragment.encode(CustomersId.sqlServerType, row.customerId()), Fragment.of(",\n[order_date] = "), Fragment.encode(SqlServerTypes.datetime2.opt(), row.orderDate()), Fragment.of(",\n[total_amount] = "), Fragment.encode(SqlServerTypes.money, row.totalAmount()), Fragment.of("\nwhere [order_id] = "), Fragment.encode(OrdersId.sqlServerType, orderId), Fragment.of("")).update().run(c) > 0;
  }

  @Override
  public OrdersRow upsert(
    OrdersRow unsaved,
    Connection c
  ) {
    return concat(Fragment.of("MERGE INTO [orders] AS target\nUSING (VALUES ("), Fragment.encode(OrdersId.sqlServerType, unsaved.orderId()), Fragment.of(", "), Fragment.encode(CustomersId.sqlServerType, unsaved.customerId()), Fragment.of(", "), Fragment.encode(SqlServerTypes.datetime2.opt(), unsaved.orderDate()), Fragment.of(", "), Fragment.encode(SqlServerTypes.money, unsaved.totalAmount()), Fragment.of(")) AS source([order_id], [customer_id], [order_date], [total_amount])\nON target.[order_id] = source.[order_id]\nWHEN MATCHED THEN UPDATE SET [customer_id] = source.[customer_id],\n[order_date] = source.[order_date],\n[total_amount] = source.[total_amount]\nWHEN NOT MATCHED THEN INSERT ([order_id], [customer_id], [order_date], [total_amount]) VALUES ("), Fragment.encode(OrdersId.sqlServerType, unsaved.orderId()), Fragment.of(", "), Fragment.encode(CustomersId.sqlServerType, unsaved.customerId()), Fragment.of(", "), Fragment.encode(SqlServerTypes.datetime2.opt(), unsaved.orderDate()), Fragment.of(", "), Fragment.encode(SqlServerTypes.money, unsaved.totalAmount()), Fragment.of(")\nOUTPUT INSERTED.[order_id], INSERTED.[customer_id], INSERTED.[order_date], INSERTED.[total_amount];"))
      .updateReturning(OrdersRow.rowCodec.exactlyOne())
      .run(c);
  }

  @Override
  public List<OrdersRow> upsertBatch(
    Iterator<OrdersRow> unsaved,
    Connection c
  ) {
    return concat(Fragment.of("MERGE INTO [orders] AS target\nUSING (VALUES (?, ?, ?, ?)) AS source([order_id], [customer_id], [order_date], [total_amount])\nON target.[order_id] = source.[order_id]\nWHEN MATCHED THEN UPDATE SET [customer_id] = source.[customer_id],\n[order_date] = source.[order_date],\n[total_amount] = source.[total_amount]\nWHEN NOT MATCHED THEN INSERT ([order_id], [customer_id], [order_date], [total_amount]) VALUES (?, ?, ?, ?)\nOUTPUT INSERTED.[order_id], INSERTED.[customer_id], INSERTED.[order_date], INSERTED.[total_amount];"))
      .updateReturningEach(OrdersRow.rowCodec, unsaved)
    .run(c);
  }
}