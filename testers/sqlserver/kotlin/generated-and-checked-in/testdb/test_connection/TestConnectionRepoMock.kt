/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.test_connection

import dev.typr.dslkt.DeleteBuilder
import dev.typr.dslkt.DeleteBuilderMock
import dev.typr.dslkt.DeleteParams
import dev.typr.dslkt.SelectBuilder
import dev.typr.dslkt.SelectBuilderMock
import dev.typr.dslkt.SelectParams
import dev.typr.dslkt.UpdateBuilder
import dev.typr.dslkt.UpdateBuilderMock
import dev.typr.dslkt.UpdateParams
import dev.typr.foundationskt.Connection
import dev.typr.foundationskt.ConnectionRead
import java.lang.RuntimeException
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap

data class TestConnectionRepoMock(
  val toRow: (TestConnectionRowUnsaved) -> TestConnectionRow,
  val map: MutableMap<TestConnectionId, TestConnectionRow> = mutableMapOf<TestConnectionId, TestConnectionRow>()
) : TestConnectionRepo {
  override fun delete(): DeleteBuilder<TestConnectionFields, TestConnectionRow> = DeleteBuilderMock(TestConnectionFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.id }, { id -> map.remove(id) })

  override fun deleteById(
    id: TestConnectionId,
    c: Connection
  ): kotlin.Boolean = map.remove(id) != null

  override fun deleteByIds(
    ids: List<TestConnectionId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in ids) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: TestConnectionRow,
    c: Connection
  ): TestConnectionRow {
    if (map.containsKey(unsaved.id)) {
      throw RuntimeException("id " + unsaved.id + " already exists")
    }
    map[unsaved.id] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: TestConnectionRowUnsaved,
    c: Connection
  ): TestConnectionRow = insert(toRow(unsaved), c)

  override fun select(): SelectBuilder<TestConnectionFields, TestConnectionRow> = SelectBuilderMock(TestConnectionFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: ConnectionRead): List<TestConnectionRow> = map.values.toList()

  override fun selectById(
    id: TestConnectionId,
    c: ConnectionRead
  ): TestConnectionRow? = map[id]

  override fun selectByIds(
    ids: List<TestConnectionId>,
    c: ConnectionRead
  ): List<TestConnectionRow> {
    val result = ArrayList<TestConnectionRow>()
    for (id in ids) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    ids: List<TestConnectionId>,
    c: ConnectionRead
  ): Map<TestConnectionId, TestConnectionRow> = selectByIds(ids, c).associateBy({ row: TestConnectionRow -> row.id })

  override fun update(): UpdateBuilder<TestConnectionFields, TestConnectionRow> = UpdateBuilderMock(TestConnectionFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: TestConnectionRow,
    c: Connection
  ): kotlin.Boolean {
    val shouldUpdate = map[row.id]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.id] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: TestConnectionRow,
    c: Connection
  ): TestConnectionRow {
    map[unsaved.id] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<TestConnectionRow>,
    c: Connection
  ): List<TestConnectionRow> {
    val result = ArrayList<TestConnectionRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.id] = row
      result.add(row)
    }
    return result
  }
}