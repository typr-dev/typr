/**
 * File automatically generated by `typo` for its own test suite.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN
 */
package testdb
package hardcoded
package compositepk
package person

import testdb.hardcoded.customtypes.Defaulted
import zio.json.JsonDecoder
import zio.json.JsonEncoder
import zio.json.ast.Json
import zio.json.internal.Write

/** This class corresponds to a row in table `compositepk.person` which has not been persisted yet */
case class PersonRowUnsaved(
  name: Option[String],
  /** Default: auto-increment */
  one: Defaulted[Long] = Defaulted.UseDefault,
  /** Default: auto-increment */
  two: Defaulted[Option[String]] = Defaulted.UseDefault
) {
  def toRow(oneDefault: => Long, twoDefault: => Option[String]): PersonRow =
    PersonRow(
      name = name,
      one = one match {
              case Defaulted.UseDefault => oneDefault
              case Defaulted.Provided(value) => value
            },
      two = two match {
              case Defaulted.UseDefault => twoDefault
              case Defaulted.Provided(value) => value
            }
    )
}
object PersonRowUnsaved {
  given jsonDecoder: JsonDecoder[PersonRowUnsaved] = JsonDecoder[Json.Obj].mapOrFail { jsonObj =>
    val name = jsonObj.get("name").fold[Either[String, Option[String]]](Right(None))(_.as(using JsonDecoder.option(using JsonDecoder.string)))
    val one = jsonObj.get("one").toRight("Missing field 'one'").flatMap(_.as(using Defaulted.jsonDecoder(using JsonDecoder.long)))
    val two = jsonObj.get("two").toRight("Missing field 'two'").flatMap(_.as(using Defaulted.jsonDecoder(using JsonDecoder.option(using JsonDecoder.string))))
    if (name.isRight && one.isRight && two.isRight)
      Right(PersonRowUnsaved(name = name.toOption.get, one = one.toOption.get, two = two.toOption.get))
    else Left(List[Either[String, Any]](name, one, two).flatMap(_.left.toOption).mkString(", "))
  }
  given jsonEncoder: JsonEncoder[PersonRowUnsaved] = new JsonEncoder[PersonRowUnsaved] {
    override def unsafeEncode(a: PersonRowUnsaved, indent: Option[Int], out: Write): Unit = {
      out.write("{")
      out.write(""""name":""")
      JsonEncoder.option(using JsonEncoder.string).unsafeEncode(a.name, indent, out)
      out.write(",")
      out.write(""""one":""")
      Defaulted.jsonEncoder(using JsonEncoder.long).unsafeEncode(a.one, indent, out)
      out.write(",")
      out.write(""""two":""")
      Defaulted.jsonEncoder(using JsonEncoder.option(using JsonEncoder.string)).unsafeEncode(a.two, indent, out)
      out.write("}")
    }
  }
  given text: Text[PersonRowUnsaved] = Text.instance[PersonRowUnsaved]{ (row, sb) =>
    Text.option(using Text.stringInstance).unsafeEncode(row.name, sb)
    sb.append(Text.DELIMETER)
    Defaulted.text(using Text.longInstance).unsafeEncode(row.one, sb)
    sb.append(Text.DELIMETER)
    Defaulted.text(using Text.option(using Text.stringInstance)).unsafeEncode(row.two, sb)
  }
}
