/**
 * File automatically generated by `typo` for its own test suite.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN
 */
package testdb.hardcoded.myschema;

import typo.runtime.PgType;
import typo.runtime.PgTypes;
import typo.runtime.internal.arrayMap;

public enum Sector {
    _public("PUBLIC"),
    _private("PRIVATE"),
    _other("OTHER");
    final java.lang.String value;

    public java.lang.String value() {
        return value;
    }

    Sector(java.lang.String value) {
        this.value = value;
    }

    public static final java.lang.String Names = java.util.Arrays.stream(Sector.values()).map(x -> x.value).collect(java.util.stream.Collectors.joining(", "));
    public static final java.util.Map<java.lang.String, Sector> ByName = java.util.Arrays.stream(Sector.values()).collect(java.util.stream.Collectors.toMap(n -> n.value, n -> n));

    static public PgType<Sector[]> pgTypeArray =
      PgTypes.textArray
          .bimap(xs -> arrayMap.map(xs, Sector::force, Sector.class), xs -> arrayMap.map(xs, Sector::value, String.class))
        .renamedDropPrecision("myschema.sector");
    static public PgType<Sector> pgType =
      PgTypes.text.bimap(Sector::force, Sector::value)
        .renamedDropPrecision("myschema.sector");

    public static Sector force(java.lang.String str) {
        if (ByName.containsKey(str)) {
            return ByName.get(str);
        } else {
            throw new RuntimeException("'" + str + "' does not match any of the following legal values: " + Names);
        }
    }
}
