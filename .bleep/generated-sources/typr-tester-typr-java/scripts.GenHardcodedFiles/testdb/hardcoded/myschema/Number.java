/**
 * File automatically generated by `typo` for its own test suite.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN
 */
package testdb.hardcoded.myschema;

import typr.runtime.PgType;
import typr.runtime.PgTypes;
import typr.runtime.internal.arrayMap;

public enum Number {
    _one("one"),
    _two("two"),
    _three("three");
    final java.lang.String value;

    public java.lang.String value() {
        return value;
    }

    Number(java.lang.String value) {
        this.value = value;
    }

    public static final java.lang.String Names = java.util.Arrays.stream(Number.values()).map(x -> x.value).collect(java.util.stream.Collectors.joining(", "));
    public static final java.util.Map<java.lang.String, Number> ByName = java.util.Arrays.stream(Number.values()).collect(java.util.stream.Collectors.toMap(n -> n.value, n -> n));

    static public PgType<Number[]> pgTypeArray =
      PgTypes.textArray
          .bimap(xs -> arrayMap.map(xs, Number::force, Number.class), xs -> arrayMap.map(xs, Number::value, String.class))
        .renamedDropPrecision("myschema.number");
    static public PgType<Number> pgType =
      PgTypes.text.bimap(Number::force, Number::value)
        .renamedDropPrecision("myschema.number");

    public static Number force(java.lang.String str) {
        if (ByName.containsKey(str)) {
            return ByName.get(str);
        } else {
            throw new RuntimeException("'" + str + "' does not match any of the following legal values: " + Names);
        }
    }
}
