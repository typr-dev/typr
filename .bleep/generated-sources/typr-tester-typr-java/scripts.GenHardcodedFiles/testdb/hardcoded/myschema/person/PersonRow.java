/**
 * File automatically generated by `typo` for its own test suite.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN
 */
package testdb.hardcoded.myschema.person;

import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.Optional;
import testdb.hardcoded.customtypes.Defaulted;
import testdb.hardcoded.myschema.Number;
import testdb.hardcoded.myschema.Sector;
import testdb.hardcoded.myschema.football_club.FootballClubId;
import testdb.hardcoded.myschema.marital_status.MaritalStatusId;
import typr.runtime.PgText;
import typr.runtime.PgTypes;
import typr.runtime.RowParser;
import typr.runtime.RowParsers;

/** Table: myschema.person
  * person table
  * Primary key: id
  */
public record PersonRow(
  /** Default: auto-increment */
  PersonId id,
  /** Points to {@link testdb.hardcoded.myschema.football_club.FootballClubRow#id()} */
  @JsonProperty("favourite_football_club_id") FootballClubId favouriteFootballClubId,
  String name,
  @JsonProperty("nick_name") Optional</* max 30 chars */ String> nickName,
  @JsonProperty("blog_url") Optional</* max 100 chars */ String> blogUrl,
  String email,
  String phone,
  @JsonProperty("likes_pizza") Boolean likesPizza,
  /** Default: some-value
    * Points to {@link testdb.hardcoded.myschema.marital_status.MaritalStatusRow#id()}
    */
  @JsonProperty("marital_status_id") MaritalStatusId maritalStatusId,
  @JsonProperty("work_email") Optional</* max 254 chars */ String> workEmail,
  /** Default: PUBLIC
    * Identity ALWAYS
    */
  Sector sector,
  /** Default: one */
  @JsonProperty("favorite_number") Number favoriteNumber
) {
  /** Default: auto-increment */
  public PersonRow withId(PersonId id) {
    return new PersonRow(id, favouriteFootballClubId, name, nickName, blogUrl, email, phone, likesPizza, maritalStatusId, workEmail, sector, favoriteNumber);
  };

  /** Points to {@link testdb.hardcoded.myschema.football_club.FootballClubRow#id()} */
  public PersonRow withFavouriteFootballClubId(FootballClubId favouriteFootballClubId) {
    return new PersonRow(id, favouriteFootballClubId, name, nickName, blogUrl, email, phone, likesPizza, maritalStatusId, workEmail, sector, favoriteNumber);
  };

  public PersonRow withName(String name) {
    return new PersonRow(id, favouriteFootballClubId, name, nickName, blogUrl, email, phone, likesPizza, maritalStatusId, workEmail, sector, favoriteNumber);
  };

  public PersonRow withNickName(Optional</* max 30 chars */ String> nickName) {
    return new PersonRow(id, favouriteFootballClubId, name, nickName, blogUrl, email, phone, likesPizza, maritalStatusId, workEmail, sector, favoriteNumber);
  };

  public PersonRow withBlogUrl(Optional</* max 100 chars */ String> blogUrl) {
    return new PersonRow(id, favouriteFootballClubId, name, nickName, blogUrl, email, phone, likesPizza, maritalStatusId, workEmail, sector, favoriteNumber);
  };

  public PersonRow withEmail(String email) {
    return new PersonRow(id, favouriteFootballClubId, name, nickName, blogUrl, email, phone, likesPizza, maritalStatusId, workEmail, sector, favoriteNumber);
  };

  public PersonRow withPhone(String phone) {
    return new PersonRow(id, favouriteFootballClubId, name, nickName, blogUrl, email, phone, likesPizza, maritalStatusId, workEmail, sector, favoriteNumber);
  };

  public PersonRow withLikesPizza(Boolean likesPizza) {
    return new PersonRow(id, favouriteFootballClubId, name, nickName, blogUrl, email, phone, likesPizza, maritalStatusId, workEmail, sector, favoriteNumber);
  };

  /** Default: some-value
    * Points to {@link testdb.hardcoded.myschema.marital_status.MaritalStatusRow#id()}
    */
  public PersonRow withMaritalStatusId(MaritalStatusId maritalStatusId) {
    return new PersonRow(id, favouriteFootballClubId, name, nickName, blogUrl, email, phone, likesPizza, maritalStatusId, workEmail, sector, favoriteNumber);
  };

  public PersonRow withWorkEmail(Optional</* max 254 chars */ String> workEmail) {
    return new PersonRow(id, favouriteFootballClubId, name, nickName, blogUrl, email, phone, likesPizza, maritalStatusId, workEmail, sector, favoriteNumber);
  };

  /** Default: PUBLIC
    * Identity ALWAYS
    */
  public PersonRow withSector(Sector sector) {
    return new PersonRow(id, favouriteFootballClubId, name, nickName, blogUrl, email, phone, likesPizza, maritalStatusId, workEmail, sector, favoriteNumber);
  };

  /** Default: one */
  public PersonRow withFavoriteNumber(Number favoriteNumber) {
    return new PersonRow(id, favouriteFootballClubId, name, nickName, blogUrl, email, phone, likesPizza, maritalStatusId, workEmail, sector, favoriteNumber);
  };

  public static RowParser<PersonRow> _rowParser = RowParsers.of(PersonId.pgType, FootballClubId.pgType, PgTypes.text, PgTypes.text.opt(), PgTypes.text.opt(), PgTypes.text, PgTypes.text, PgTypes.bool, MaritalStatusId.pgType, PgTypes.text.opt(), Sector.pgType, Number.pgType, PersonRow::new, row -> new Object[]{row.id(), row.favouriteFootballClubId(), row.name(), row.nickName(), row.blogUrl(), row.email(), row.phone(), row.likesPizza(), row.maritalStatusId(), row.workEmail(), row.sector(), row.favoriteNumber()});;

  static public PgText<PersonRow> pgText =
    PgText.from(_rowParser);

  public PersonRowUnsaved toUnsavedRow(
    Defaulted<PersonId> id,
    Defaulted<MaritalStatusId> maritalStatusId,
    Defaulted<Number> favoriteNumber
  ) {
    return new PersonRowUnsaved(favouriteFootballClubId, name, nickName, blogUrl, email, phone, likesPizza, workEmail, id, maritalStatusId, favoriteNumber);
  };
}