/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.product_categories

import java.sql.Connection
import java.util.ArrayList
import java.util.Optional
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableIterator
import kotlin.collections.MutableMap
import testdb.categories.CategoriesId
import testdb.products.ProductsId
import typo.dsl.DeleteBuilder
import typo.dsl.Dialect
import typo.dsl.SelectBuilder
import typo.dsl.UpdateBuilder
import typo.runtime.Fragment
import typo.runtime.Fragment.Literal
import typo.runtime.MariaTypes
import typo.runtime.Fragment.interpolate

class ProductCategoriesRepoImpl() : ProductCategoriesRepo {
  override fun delete(): DeleteBuilder<ProductCategoriesFields, ProductCategoriesRow> = DeleteBuilder.of("`product_categories`", ProductCategoriesFields.structure, Dialect.MARIADB)

  override fun deleteById(
    compositeId: ProductCategoriesId,
    c: Connection
  ): Boolean = interpolate(
    typo.runtime.Fragment.lit("delete from `product_categories` where `product_id` = "),
    ProductsId.pgType.encode(compositeId.productId),
    typo.runtime.Fragment.lit(" AND `category_id` = "),
    CategoriesId.pgType.encode(compositeId.categoryId),
    typo.runtime.Fragment.lit("")
  ).update().runUnchecked(c) > 0

  override fun deleteByIds(
    compositeIds: Array<ProductCategoriesId>,
    c: Connection
  ): Int {
    val fragments: ArrayList<Fragment> = ArrayList<Fragment>()
    for (id in compositeIds) { fragments.add(Fragment.interpolate(Fragment.lit("("), ProductsId.pgType.encode(id.productId), Fragment.lit(", "), CategoriesId.pgType.encode(id.categoryId), Fragment.lit(")"))) }
    return Fragment.interpolate(Fragment.lit("delete from `product_categories` where (`product_id`, `category_id`) in ("), Fragment.comma(fragments), Fragment.lit(")")).update().runUnchecked(c)
  }

  override fun insert(
    unsaved: ProductCategoriesRow,
    c: Connection
  ): ProductCategoriesRow = interpolate(
    typo.runtime.Fragment.lit("""
      insert into `product_categories`(`product_id`, `category_id`, `is_primary`, `sort_order`)
      values (""".trimMargin()),
    ProductsId.pgType.encode(unsaved.productId),
    typo.runtime.Fragment.lit(", "),
    CategoriesId.pgType.encode(unsaved.categoryId),
    typo.runtime.Fragment.lit(", "),
    MariaTypes.bool.encode(unsaved.isPrimary),
    typo.runtime.Fragment.lit(", "),
    MariaTypes.smallint.encode(unsaved.sortOrder),
    typo.runtime.Fragment.lit("""
      )
      returning `product_id`, `category_id`, `is_primary`, `sort_order`
    """.trimMargin())
  )
    .updateReturning(ProductCategoriesRow._rowParser.exactlyOne()).runUnchecked(c)

  override fun insert(
    unsaved: ProductCategoriesRowUnsaved,
    c: Connection
  ): ProductCategoriesRow {
    val columns: ArrayList<Literal> = ArrayList<Literal>()
    val values: ArrayList<Fragment> = ArrayList<Fragment>()
    columns.add(Fragment.lit("`product_id`"))
    values.add(interpolate(
      ProductsId.pgType.encode(unsaved.productId),
      typo.runtime.Fragment.lit("""
      """.trimMargin())
    ))
    columns.add(Fragment.lit("`category_id`"))
    values.add(interpolate(
      CategoriesId.pgType.encode(unsaved.categoryId),
      typo.runtime.Fragment.lit("""
      """.trimMargin())
    ))
    unsaved.isPrimary.visit(
      {  },
      { value -> columns.add(Fragment.lit("`is_primary`"))
      values.add(interpolate(
        MariaTypes.bool.encode(value),
        typo.runtime.Fragment.lit("""
        """.trimMargin())
      )) }
    );
    unsaved.sortOrder.visit(
      {  },
      { value -> columns.add(Fragment.lit("`sort_order`"))
      values.add(interpolate(
        MariaTypes.smallint.encode(value),
        typo.runtime.Fragment.lit("""
        """.trimMargin())
      )) }
    );
    val q: Fragment = interpolate(
      typo.runtime.Fragment.lit("insert into `product_categories`("),
      Fragment.comma(columns),
      typo.runtime.Fragment.lit("""
        )
        values (""".trimMargin()),
      Fragment.comma(values),
      typo.runtime.Fragment.lit("""
        )
        returning `product_id`, `category_id`, `is_primary`, `sort_order`
      """.trimMargin())
    )
    return q.updateReturning(ProductCategoriesRow._rowParser.exactlyOne()).runUnchecked(c)
  }

  override fun select(): SelectBuilder<ProductCategoriesFields, ProductCategoriesRow> = SelectBuilder.of("`product_categories`", ProductCategoriesFields.structure, ProductCategoriesRow._rowParser, Dialect.MARIADB)

  override fun selectAll(c: Connection): List<ProductCategoriesRow> = interpolate(typo.runtime.Fragment.lit("""
    select `product_id`, `category_id`, `is_primary`, `sort_order`
    from `product_categories`
  """.trimMargin())).query(ProductCategoriesRow._rowParser.all()).runUnchecked(c)

  override fun selectById(
    compositeId: ProductCategoriesId,
    c: Connection
  ): Optional<ProductCategoriesRow> = interpolate(
    typo.runtime.Fragment.lit("""
      select `product_id`, `category_id`, `is_primary`, `sort_order`
      from `product_categories`
      where `product_id` = """.trimMargin()),
    ProductsId.pgType.encode(compositeId.productId),
    typo.runtime.Fragment.lit(" AND `category_id` = "),
    CategoriesId.pgType.encode(compositeId.categoryId),
    typo.runtime.Fragment.lit("")
  ).query(ProductCategoriesRow._rowParser.first()).runUnchecked(c)

  override fun selectByIds(
    compositeIds: Array<ProductCategoriesId>,
    c: Connection
  ): List<ProductCategoriesRow> {
    val fragments: ArrayList<Fragment> = ArrayList<Fragment>()
    for (id in compositeIds) { fragments.add(Fragment.interpolate(Fragment.lit("("), ProductsId.pgType.encode(id.productId), Fragment.lit(", "), CategoriesId.pgType.encode(id.categoryId), Fragment.lit(")"))) }
    return Fragment.interpolate(Fragment.lit("select `product_id`, `category_id`, `is_primary`, `sort_order` from `product_categories` where (`product_id`, `category_id`) in ("), Fragment.comma(fragments), Fragment.lit(")")).query(ProductCategoriesRow._rowParser.all()).runUnchecked(c)
  }

  override fun selectByIdsTracked(
    compositeIds: Array<ProductCategoriesId>,
    c: Connection
  ): Map<ProductCategoriesId, ProductCategoriesRow> {
    val ret: MutableMap<ProductCategoriesId, ProductCategoriesRow> = mutableMapOf<ProductCategoriesId, ProductCategoriesRow>()
    selectByIds(compositeIds, c).forEach({ row -> ret.put(row.compositeId(), row) })
    return ret
  }

  override fun update(): UpdateBuilder<ProductCategoriesFields, ProductCategoriesRow> = UpdateBuilder.of("`product_categories`", ProductCategoriesFields.structure, ProductCategoriesRow._rowParser.all(), Dialect.MARIADB)

  override fun update(
    row: ProductCategoriesRow,
    c: Connection
  ): Boolean {
    val compositeId: ProductCategoriesId = row.compositeId()
    return interpolate(
      typo.runtime.Fragment.lit("""
        update `product_categories`
        set `is_primary` = """.trimMargin()),
      MariaTypes.bool.encode(row.isPrimary),
      typo.runtime.Fragment.lit("""
        ,
        `sort_order` = """.trimMargin()),
      MariaTypes.smallint.encode(row.sortOrder),
      typo.runtime.Fragment.lit("""
  
        where `product_id` = """.trimMargin()),
      ProductsId.pgType.encode(compositeId.productId),
      typo.runtime.Fragment.lit(" AND `category_id` = "),
      CategoriesId.pgType.encode(compositeId.categoryId),
      typo.runtime.Fragment.lit("")
    ).update().runUnchecked(c) > 0
  }

  override fun upsert(
    unsaved: ProductCategoriesRow,
    c: Connection
  ): ProductCategoriesRow = interpolate(
    typo.runtime.Fragment.lit("""
      INSERT INTO `product_categories`(`product_id`, `category_id`, `is_primary`, `sort_order`)
      VALUES (""".trimMargin()),
    ProductsId.pgType.encode(unsaved.productId),
    typo.runtime.Fragment.lit(", "),
    CategoriesId.pgType.encode(unsaved.categoryId),
    typo.runtime.Fragment.lit(", "),
    MariaTypes.bool.encode(unsaved.isPrimary),
    typo.runtime.Fragment.lit(", "),
    MariaTypes.smallint.encode(unsaved.sortOrder),
    typo.runtime.Fragment.lit("""
      )
      ON DUPLICATE KEY UPDATE `is_primary` = VALUES(`is_primary`),
      `sort_order` = VALUES(`sort_order`)
      RETURNING `product_id`, `category_id`, `is_primary`, `sort_order`""".trimMargin())
  )
    .updateReturning(ProductCategoriesRow._rowParser.exactlyOne())
    .runUnchecked(c)

  override fun upsertBatch(
    unsaved: MutableIterator<ProductCategoriesRow>,
    c: Connection
  ): List<ProductCategoriesRow> = interpolate(typo.runtime.Fragment.lit("""
                                    INSERT INTO `product_categories`(`product_id`, `category_id`, `is_primary`, `sort_order`)
                                    VALUES (?, ?, ?, ?)
                                    ON DUPLICATE KEY UPDATE `is_primary` = VALUES(`is_primary`),
                                    `sort_order` = VALUES(`sort_order`)
                                    RETURNING `product_id`, `category_id`, `is_primary`, `sort_order`""".trimMargin()))
    .updateReturningEach(ProductCategoriesRow._rowParser, unsaved)
    .runUnchecked(c)
}