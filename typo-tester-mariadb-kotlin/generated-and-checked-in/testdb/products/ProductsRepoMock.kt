/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.products

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.Optional
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableIterator
import kotlin.collections.MutableMap
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import typo.runtime.internal.stringInterpolator.str

data class ProductsRepoMock(
  val toRow: (ProductsRowUnsaved) -> ProductsRow,
  val map: MutableMap<ProductsId, ProductsRow> = mutableMapOf<ProductsId, ProductsRow>()
) : ProductsRepo {
  override fun delete(): DeleteBuilder<ProductsFields, ProductsRow> = DeleteBuilderMock(ProductsFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.productId }, { id -> map.remove(id) })

  override fun deleteById(
    productId: ProductsId,
    c: Connection
  ): Boolean = Optional.ofNullable(map.remove(productId)).isPresent()

  override fun deleteByIds(
    productIds: Array<ProductsId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in productIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: ProductsRow,
    c: Connection
  ): ProductsRow {
    if (map.containsKey(unsaved.productId)) {
      throw RuntimeException(str("id $unsaved.productId already exists"))
    }
    map[unsaved.productId] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: ProductsRowUnsaved,
    c: Connection
  ): ProductsRow = insert(toRow(unsaved), c)

  override fun select(): SelectBuilder<ProductsFields, ProductsRow> = SelectBuilderMock(ProductsFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<ProductsRow> = map.values.toList()

  override fun selectById(
    productId: ProductsId,
    c: Connection
  ): Optional<ProductsRow> = Optional.ofNullable(map[productId])

  override fun selectByIds(
    productIds: Array<ProductsId>,
    c: Connection
  ): List<ProductsRow> {
    val result = ArrayList<ProductsRow>()
    for (id in productIds) {
      val opt = Optional.ofNullable(map[id])
      if (opt.isPresent()) {
      result.add(opt.get())
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    productIds: Array<ProductsId>,
    c: Connection
  ): Map<ProductsId, ProductsRow> = selectByIds(productIds, c).associateBy({ row: ProductsRow -> row.productId })

  override fun selectByUniqueSku(
    sku: String,
    c: Connection
  ): Optional<ProductsRow> = Optional.ofNullable(map.values.toList().find({ v -> (sku == v.sku) }))

  override fun update(): UpdateBuilder<ProductsFields, ProductsRow> = UpdateBuilderMock(ProductsFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: ProductsRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = Optional.ofNullable(map[row.productId]).filter({ oldRow -> (oldRow != row) }).isPresent()
    if (shouldUpdate) {
      map[row.productId] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: ProductsRow,
    c: Connection
  ): ProductsRow {
    map[unsaved.productId] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: MutableIterator<ProductsRow>,
    c: Connection
  ): List<ProductsRow> {
    val result = ArrayList<ProductsRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.productId] = row
      result.add(row)
    }
    return result
  }
}