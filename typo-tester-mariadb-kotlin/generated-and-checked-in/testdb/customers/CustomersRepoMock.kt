/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.customers

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.Optional
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableIterator
import kotlin.collections.MutableMap
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import typo.runtime.internal.stringInterpolator.str

data class CustomersRepoMock(
  val toRow: (CustomersRowUnsaved) -> CustomersRow,
  val map: MutableMap<CustomersId, CustomersRow> = mutableMapOf<CustomersId, CustomersRow>()
) : CustomersRepo {
  override fun delete(): DeleteBuilder<CustomersFields, CustomersRow> = DeleteBuilderMock(CustomersFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.customerId }, { id -> map.remove(id) })

  override fun deleteById(
    customerId: CustomersId,
    c: Connection
  ): Boolean = Optional.ofNullable(map.remove(customerId)).isPresent()

  override fun deleteByIds(
    customerIds: Array<CustomersId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in customerIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: CustomersRow,
    c: Connection
  ): CustomersRow {
    if (map.containsKey(unsaved.customerId)) {
      throw RuntimeException(str("id $unsaved.customerId already exists"))
    }
    map[unsaved.customerId] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: CustomersRowUnsaved,
    c: Connection
  ): CustomersRow = insert(toRow(unsaved), c)

  override fun select(): SelectBuilder<CustomersFields, CustomersRow> = SelectBuilderMock(CustomersFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<CustomersRow> = map.values.toList()

  override fun selectById(
    customerId: CustomersId,
    c: Connection
  ): Optional<CustomersRow> = Optional.ofNullable(map[customerId])

  override fun selectByIds(
    customerIds: Array<CustomersId>,
    c: Connection
  ): List<CustomersRow> {
    val result = ArrayList<CustomersRow>()
    for (id in customerIds) {
      val opt = Optional.ofNullable(map[id])
      if (opt.isPresent()) {
      result.add(opt.get())
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    customerIds: Array<CustomersId>,
    c: Connection
  ): Map<CustomersId, CustomersRow> = selectByIds(customerIds, c).associateBy({ row: CustomersRow -> row.customerId })

  override fun selectByUniqueEmail(
    email: String,
    c: Connection
  ): Optional<CustomersRow> = Optional.ofNullable(map.values.toList().find({ v -> (email == v.email) }))

  override fun update(): UpdateBuilder<CustomersFields, CustomersRow> = UpdateBuilderMock(CustomersFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: CustomersRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = Optional.ofNullable(map[row.customerId]).filter({ oldRow -> (oldRow != row) }).isPresent()
    if (shouldUpdate) {
      map[row.customerId] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: CustomersRow,
    c: Connection
  ): CustomersRow {
    map[unsaved.customerId] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: MutableIterator<CustomersRow>,
    c: Connection
  ): List<CustomersRow> {
    val result = ArrayList<CustomersRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.customerId] = row
      result.add(row)
    }
    return result
  }
}