/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productphoto;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typr.dsl.DeleteBuilder;
import typr.dsl.DeleteBuilderMock;
import typr.dsl.DeleteParams;
import typr.dsl.SelectBuilder;
import typr.dsl.SelectBuilderMock;
import typr.dsl.SelectParams;
import typr.dsl.UpdateBuilder;
import typr.dsl.UpdateBuilderMock;
import typr.dsl.UpdateParams;

public record ProductphotoRepoMock(
    java.util.function.Function<ProductphotoRowUnsaved, ProductphotoRow> toRow,
    HashMap<ProductphotoId, ProductphotoRow> map)
    implements ProductphotoRepo {
  public ProductphotoRepoMock(
      java.util.function.Function<ProductphotoRowUnsaved, ProductphotoRow> toRow) {
    this(toRow, new HashMap<ProductphotoId, ProductphotoRow>());
  }
  ;

  public ProductphotoRepoMock withToRow(
      java.util.function.Function<ProductphotoRowUnsaved, ProductphotoRow> toRow) {
    return new ProductphotoRepoMock(toRow, map);
  }
  ;

  public ProductphotoRepoMock withMap(HashMap<ProductphotoId, ProductphotoRow> map) {
    return new ProductphotoRepoMock(toRow, map);
  }
  ;

  @Override
  public DeleteBuilder<ProductphotoFields, ProductphotoRow> delete() {
    return new DeleteBuilderMock<>(
        ProductphotoFields.structure(),
        () -> new ArrayList<>(map.values()),
        DeleteParams.empty(),
        row -> row.productphotoid(),
        id -> map.remove(id));
  }
  ;

  @Override
  public Boolean deleteById(ProductphotoId productphotoid, Connection c) {
    return Optional.ofNullable(map.remove(productphotoid)).isPresent();
  }
  ;

  @Override
  public Integer deleteByIds(ProductphotoId[] productphotoids, Connection c) {
    var count = 0;
    for (var id : productphotoids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
        ;
      }
      ;
    }
    ;
    return count;
  }
  ;

  @Override
  public ProductphotoRow insert(ProductphotoRow unsaved, Connection c) {
    if (map.containsKey(unsaved.productphotoid())) {
      throw new RuntimeException("id " + unsaved.productphotoid() + " already exists");
    }
    ;
    map.put(unsaved.productphotoid(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public ProductphotoRow insert(ProductphotoRowUnsaved unsaved, Connection c) {
    return insert(toRow.apply(unsaved), c);
  }
  ;

  @Override
  public Long insertStreaming(Iterator<ProductphotoRow> unsaved, Integer batchSize, Connection c) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.productphotoid(), row);
      count = count + 1L;
    }
    ;
    return count;
  }
  ;

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  @Override
  public Long insertUnsavedStreaming(
      Iterator<ProductphotoRowUnsaved> unsaved, Integer batchSize, Connection c) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var unsavedRow = unsaved.next();
      var row = toRow.apply(unsavedRow);
      map.put(row.productphotoid(), row);
      count = count + 1L;
    }
    ;
    return count;
  }
  ;

  @Override
  public SelectBuilder<ProductphotoFields, ProductphotoRow> select() {
    return new SelectBuilderMock<>(
        ProductphotoFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  }
  ;

  @Override
  public List<ProductphotoRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  }
  ;

  @Override
  public Optional<ProductphotoRow> selectById(ProductphotoId productphotoid, Connection c) {
    return Optional.ofNullable(map.get(productphotoid));
  }
  ;

  @Override
  public List<ProductphotoRow> selectByIds(ProductphotoId[] productphotoids, Connection c) {
    var result = new ArrayList<ProductphotoRow>();
    for (var id : productphotoids) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
        result.add(opt.get());
      }
      ;
    }
    ;
    return result;
  }
  ;

  @Override
  public Map<ProductphotoId, ProductphotoRow> selectByIdsTracked(
      ProductphotoId[] productphotoids, Connection c) {
    return selectByIds(productphotoids, c).stream()
        .collect(
            Collectors.toMap((ProductphotoRow row) -> row.productphotoid(), Function.identity()));
  }
  ;

  @Override
  public UpdateBuilder<ProductphotoFields, ProductphotoRow> update() {
    return new UpdateBuilderMock<>(
        ProductphotoFields.structure(),
        () -> new ArrayList<>(map.values()),
        UpdateParams.empty(),
        row -> row);
  }
  ;

  @Override
  public Boolean update(ProductphotoRow row, Connection c) {
    var shouldUpdate =
        Optional.ofNullable(map.get(row.productphotoid()))
            .filter(oldRow -> !oldRow.equals(row))
            .isPresent();
    if (shouldUpdate) {
      map.put(row.productphotoid(), row);
      ;
    }
    ;
    return shouldUpdate;
  }
  ;

  @Override
  public ProductphotoRow upsert(ProductphotoRow unsaved, Connection c) {
    map.put(unsaved.productphotoid(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public List<ProductphotoRow> upsertBatch(Iterator<ProductphotoRow> unsaved, Connection c) {
    var result = new ArrayList<ProductphotoRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.productphotoid(), row);
      result.add(row);
    }
    ;
    return result;
  }
  ;

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
      Iterator<ProductphotoRow> unsaved, Integer batchSize, Connection c) {
    var count = 0;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.productphotoid(), row);
      count = count + 1;
    }
    ;
    return count;
  }
  ;
}
