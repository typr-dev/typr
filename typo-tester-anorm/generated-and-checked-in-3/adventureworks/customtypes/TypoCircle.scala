/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package customtypes

import anorm.Column
import anorm.ParameterMetaData
import anorm.ToStatement
import anorm.TypeDoesNotMatch
import java.sql.Types
import org.postgresql.geometric.PGcircle
import org.postgresql.jdbc.PgArray
import play.api.libs.json.JsObject
import play.api.libs.json.JsResult
import play.api.libs.json.JsValue
import play.api.libs.json.OWrites
import play.api.libs.json.Reads
import play.api.libs.json.Writes
import scala.collection.immutable.ListMap
import scala.util.Try

/** This represents circle datatype in PostgreSQL, consisting of a point and a radius */
case class TypoCircle(center: TypoPoint, radius: Double)

object TypoCircle {
  given arrayColumn: Column[Array[TypoCircle]] = Column.nonNull[Array[TypoCircle]]((v1: Any, _) =>
    v1 match {
        case v: PgArray =>
         v.getArray match {
           case v: Array[?] =>
             Right(v.map(v => TypoCircle(TypoPoint(v.asInstanceOf[PGcircle].center.x, v.asInstanceOf[PGcircle].center.y), v.asInstanceOf[PGcircle].radius)))
           case other => Left(TypeDoesNotMatch(s"Expected one-dimensional array from JDBC to produce an array of TypoCircle, got ${other.getClass.getName}"))
         }
      case other => Left(TypeDoesNotMatch(s"Expected instance of org.postgresql.jdbc.PgArray, got ${other.getClass.getName}"))
    }
  )
  given arrayToStatement: ToStatement[Array[TypoCircle]] = ToStatement[Array[TypoCircle]]((s, index, v) => s.setArray(index, s.getConnection.createArrayOf("circle", v.map(v => new PGcircle(v.center.x, v.center.y, v.radius)))))
  given column: Column[TypoCircle] = Column.nonNull[TypoCircle]((v1: Any, _) =>
    v1 match {
      case v: PGcircle => Right(TypoCircle(TypoPoint(v.center.x, v.center.y), v.radius))
      case other => Left(TypeDoesNotMatch(s"Expected instance of org.postgresql.geometric.PGcircle, got ${other.getClass.getName}"))
    }
  )
  given ordering(using O0: Ordering[TypoPoint]): Ordering[TypoCircle] = Ordering.by(x => (x.center, x.radius))
  given parameterMetadata: ParameterMetaData[TypoCircle] = new ParameterMetaData[TypoCircle] {
    override def sqlType: String = "circle"
    override def jdbcType: Int = Types.OTHER
  }
  given reads: Reads[TypoCircle] = Reads[TypoCircle](json => JsResult.fromTry(
      Try(
        TypoCircle(
          center = json.\("center").as(summon[Reads[TypoPoint]]),
          radius = json.\("radius").as(Reads.DoubleReads)
        )
      )
    ),
  )
  given text: Text[TypoCircle] = new Text[TypoCircle] {
    override def unsafeEncode(v: TypoCircle, sb: StringBuilder): Unit = Text.stringInstance.unsafeEncode(s"<(${v.center.x},${v.center.y}),${v.radius}>", sb)
    override def unsafeArrayEncode(v: TypoCircle, sb: StringBuilder): Unit = Text.stringInstance.unsafeArrayEncode(s"<(${v.center.x},${v.center.y}),${v.radius}>", sb)
  }
  given toStatement: ToStatement[TypoCircle] = ToStatement[TypoCircle]((s, index, v) => s.setObject(index, new PGcircle(v.center.x, v.center.y, v.radius)))
  given writes: OWrites[TypoCircle] = OWrites[TypoCircle](o =>
    new JsObject(ListMap[String, JsValue](
      "center" -> summon[Writes[TypoPoint]].writes(o.center),
      "radius" -> Writes.DoubleWrites.writes(o.radius)
    ))
  )
}
