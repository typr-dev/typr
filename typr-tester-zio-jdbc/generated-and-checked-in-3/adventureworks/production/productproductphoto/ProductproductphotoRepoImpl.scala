/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productproductphoto

import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.TypoLocalDateTime
import adventureworks.production.product.ProductId
import adventureworks.production.productphoto.ProductphotoId
import adventureworks.public.Flag
import adventureworks.streamingInsert
import typr.dsl.DeleteBuilder
import typr.dsl.SelectBuilder
import typr.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment
import zio.jdbc.SqlFragment.Segment
import zio.jdbc.UpdateResult
import zio.jdbc.ZConnection
import zio.stream.ZStream
import zio.jdbc.sqlInterpolator

class ProductproductphotoRepoImpl extends ProductproductphotoRepo {
  override def delete: DeleteBuilder[ProductproductphotoFields, ProductproductphotoRow] = DeleteBuilder.of(""""production"."productproductphoto"""", ProductproductphotoFields.structure, ProductproductphotoRow.jdbcDecoder)

  override def deleteById(compositeId: ProductproductphotoId): ZIO[ZConnection, Throwable, Boolean] = sql"""delete from "production"."productproductphoto" where "productid" = ${Segment.paramSegment(compositeId.productid)(using ProductId.setter)} AND "productphotoid" = ${Segment.paramSegment(compositeId.productphotoid)(using ProductphotoId.setter)}""".delete.map(_ > 0)

  override def deleteByIds(compositeIds: Array[ProductproductphotoId]): ZIO[ZConnection, Throwable, Long] = {
    val productid = compositeIds.map(_.productid)
    val productphotoid = compositeIds.map(_.productphotoid)
    sql"""delete
    from "production"."productproductphoto"
    where ("productid", "productphotoid")
    in (select unnest(${Segment.paramSegment(productid)(using ProductId.arraySetter)}), unnest(${Segment.paramSegment(productphotoid)(using ProductphotoId.arraySetter)}))
    """.delete
  }

  override def insert(unsaved: ProductproductphotoRow): ZIO[ZConnection, Throwable, ProductproductphotoRow] = {
    sql"""insert into "production"."productproductphoto"("productid", "productphotoid", "primary", "modifieddate")
    values (${Segment.paramSegment(unsaved.productid)(using ProductId.setter)}::int4, ${Segment.paramSegment(unsaved.productphotoid)(using ProductphotoId.setter)}::int4, ${Segment.paramSegment(unsaved.primary)(using Flag.setter)}::bool, ${Segment.paramSegment(unsaved.modifieddate)(using TypoLocalDateTime.setter)}::timestamp)
    returning "productid", "productphotoid", "primary", "modifieddate"::text
    """.insertReturning(using ProductproductphotoRow.jdbcDecoder).map(_.updatedKeys.head)
  }

  override def insert(unsaved: ProductproductphotoRowUnsaved): ZIO[ZConnection, Throwable, ProductproductphotoRow] = {
    val fs = List(
      Some((sql""""productid"""", sql"${Segment.paramSegment(unsaved.productid)(using ProductId.setter)}::int4")),
      Some((sql""""productphotoid"""", sql"${Segment.paramSegment(unsaved.productphotoid)(using ProductphotoId.setter)}::int4")),
      unsaved.primary match {
        case Defaulted.UseDefault() => None
        case Defaulted.Provided(value) => Some((sql""""primary"""", sql"${Segment.paramSegment(value: Flag)(using Flag.setter)}::bool"))
      },
      unsaved.modifieddate match {
        case Defaulted.UseDefault() => None
        case Defaulted.Provided(value) => Some((sql""""modifieddate"""", sql"${Segment.paramSegment(value: TypoLocalDateTime)(using TypoLocalDateTime.setter)}::timestamp"))
      }
    ).flatten
    val q = if (fs.isEmpty) {
      sql"""insert into "production"."productproductphoto" default values
      returning "productid", "productphotoid", "primary", "modifieddate"::text
      """
    } else {
      val names  = fs.map { case (n, _) => n }.mkFragment(SqlFragment(", "))
      val values = fs.map { case (_, f) => f }.mkFragment(SqlFragment(", "))
      sql"""insert into "production"."productproductphoto"($names) values ($values) returning "productid", "productphotoid", "primary", "modifieddate"::text"""
    }
    q.insertReturning(using ProductproductphotoRow.jdbcDecoder).map(_.updatedKeys.head)
  }

  override def insertStreaming(
    unsaved: ZStream[ZConnection, Throwable, ProductproductphotoRow],
    batchSize: Int = 10000
  ): ZIO[ZConnection, Throwable, Long] = streamingInsert(s"""COPY "production"."productproductphoto"("productid", "productphotoid", "primary", "modifieddate") FROM STDIN""", batchSize, unsaved)(using ProductproductphotoRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: ZStream[ZConnection, Throwable, ProductproductphotoRowUnsaved],
    batchSize: Int = 10000
  ): ZIO[ZConnection, Throwable, Long] = streamingInsert(s"""COPY "production"."productproductphoto"("productid", "productphotoid", "primary", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(using ProductproductphotoRowUnsaved.pgText)

  override def select: SelectBuilder[ProductproductphotoFields, ProductproductphotoRow] = SelectBuilder.of(""""production"."productproductphoto"""", ProductproductphotoFields.structure, ProductproductphotoRow.jdbcDecoder)

  override def selectAll: ZStream[ZConnection, Throwable, ProductproductphotoRow] = sql"""select "productid", "productphotoid", "primary", "modifieddate"::text from "production"."productproductphoto"""".query(using ProductproductphotoRow.jdbcDecoder).selectStream()

  override def selectById(compositeId: ProductproductphotoId): ZIO[ZConnection, Throwable, Option[ProductproductphotoRow]] = sql"""select "productid", "productphotoid", "primary", "modifieddate"::text from "production"."productproductphoto" where "productid" = ${Segment.paramSegment(compositeId.productid)(using ProductId.setter)} AND "productphotoid" = ${Segment.paramSegment(compositeId.productphotoid)(using ProductphotoId.setter)}""".query(using ProductproductphotoRow.jdbcDecoder).selectOne

  override def selectByIds(compositeIds: Array[ProductproductphotoId]): ZStream[ZConnection, Throwable, ProductproductphotoRow] = {
    val productid = compositeIds.map(_.productid)
    val productphotoid = compositeIds.map(_.productphotoid)
    sql"""select "productid", "productphotoid", "primary", "modifieddate"::text
    from "production"."productproductphoto"
    where ("productid", "productphotoid")
    in (select unnest(${Segment.paramSegment(productid)(using ProductId.arraySetter)}), unnest(${Segment.paramSegment(productphotoid)(using ProductphotoId.arraySetter)}))
    """.query(using ProductproductphotoRow.jdbcDecoder).selectStream()
  }

  override def selectByIdsTracked(compositeIds: Array[ProductproductphotoId]): ZIO[ZConnection, Throwable, Map[ProductproductphotoId, ProductproductphotoRow]] = {
    selectByIds(compositeIds).runCollect.map { rows =>
      val byId = rows.view.map(x => (x.compositeId, x)).toMap
      compositeIds.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
    }
  }

  override def update: UpdateBuilder[ProductproductphotoFields, ProductproductphotoRow] = UpdateBuilder.of(""""production"."productproductphoto"""", ProductproductphotoFields.structure, ProductproductphotoRow.jdbcDecoder)

  override def update(row: ProductproductphotoRow): ZIO[ZConnection, Throwable, Option[ProductproductphotoRow]] = {
    val compositeId = row.compositeId
    sql"""update "production"."productproductphoto"
    set "primary" = ${Segment.paramSegment(row.primary)(using Flag.setter)}::bool,
    "modifieddate" = ${Segment.paramSegment(row.modifieddate)(using TypoLocalDateTime.setter)}::timestamp
    where "productid" = ${Segment.paramSegment(compositeId.productid)(using ProductId.setter)} AND "productphotoid" = ${Segment.paramSegment(compositeId.productphotoid)(using ProductphotoId.setter)}
    returning "productid", "productphotoid", "primary", "modifieddate"::text"""
      .query(using ProductproductphotoRow.jdbcDecoder)
      .selectOne
  }

  override def upsert(unsaved: ProductproductphotoRow): ZIO[ZConnection, Throwable, UpdateResult[ProductproductphotoRow]] = {
    sql"""insert into "production"."productproductphoto"("productid", "productphotoid", "primary", "modifieddate")
    values (
      ${Segment.paramSegment(unsaved.productid)(using ProductId.setter)}::int4,
    ${Segment.paramSegment(unsaved.productphotoid)(using ProductphotoId.setter)}::int4,
    ${Segment.paramSegment(unsaved.primary)(using Flag.setter)}::bool,
    ${Segment.paramSegment(unsaved.modifieddate)(using TypoLocalDateTime.setter)}::timestamp
    )
    on conflict ("productid", "productphotoid")
    do update set
      "primary" = EXCLUDED."primary",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "productid", "productphotoid", "primary", "modifieddate"::text""".insertReturning(using ProductproductphotoRow.jdbcDecoder)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: ZStream[ZConnection, Throwable, ProductproductphotoRow],
    batchSize: Int = 10000
  ): ZIO[ZConnection, Throwable, Long] = {
    val created = sql"""create temporary table productproductphoto_TEMP (like "production"."productproductphoto") on commit drop""".execute
    val copied = streamingInsert(s"""copy productproductphoto_TEMP("productid", "productphotoid", "primary", "modifieddate") from stdin""", batchSize, unsaved)(using ProductproductphotoRow.pgText)
    val merged = sql"""insert into "production"."productproductphoto"("productid", "productphotoid", "primary", "modifieddate")
    select * from productproductphoto_TEMP
    on conflict ("productid", "productphotoid")
    do update set
      "primary" = EXCLUDED."primary",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table productproductphoto_TEMP;""".update
    created *> copied *> merged
  }
}