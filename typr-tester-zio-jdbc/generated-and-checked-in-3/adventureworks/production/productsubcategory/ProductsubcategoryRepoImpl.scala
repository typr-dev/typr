/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productsubcategory

import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.TypoLocalDateTime
import adventureworks.customtypes.TypoUUID
import adventureworks.production.productcategory.ProductcategoryId
import adventureworks.public.Name
import adventureworks.streamingInsert
import typr.dsl.DeleteBuilder
import typr.dsl.SelectBuilder
import typr.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment
import zio.jdbc.SqlFragment.Segment
import zio.jdbc.UpdateResult
import zio.jdbc.ZConnection
import zio.stream.ZStream
import zio.jdbc.sqlInterpolator

class ProductsubcategoryRepoImpl extends ProductsubcategoryRepo {
  override def delete: DeleteBuilder[ProductsubcategoryFields, ProductsubcategoryRow] = DeleteBuilder.of(""""production"."productsubcategory"""", ProductsubcategoryFields.structure, ProductsubcategoryRow.jdbcDecoder)

  override def deleteById(productsubcategoryid: ProductsubcategoryId): ZIO[ZConnection, Throwable, Boolean] = sql"""delete from "production"."productsubcategory" where "productsubcategoryid" = ${Segment.paramSegment(productsubcategoryid)(using ProductsubcategoryId.setter)}""".delete.map(_ > 0)

  override def deleteByIds(productsubcategoryids: Array[ProductsubcategoryId]): ZIO[ZConnection, Throwable, Long] = sql"""delete from "production"."productsubcategory" where "productsubcategoryid" = ANY(${Segment.paramSegment(productsubcategoryids)(using ProductsubcategoryId.arraySetter)})""".delete

  override def insert(unsaved: ProductsubcategoryRow): ZIO[ZConnection, Throwable, ProductsubcategoryRow] = {
    sql"""insert into "production"."productsubcategory"("productsubcategoryid", "productcategoryid", "name", "rowguid", "modifieddate")
    values (${Segment.paramSegment(unsaved.productsubcategoryid)(using ProductsubcategoryId.setter)}::int4, ${Segment.paramSegment(unsaved.productcategoryid)(using ProductcategoryId.setter)}::int4, ${Segment.paramSegment(unsaved.name)(using Name.setter)}::varchar, ${Segment.paramSegment(unsaved.rowguid)(using TypoUUID.setter)}::uuid, ${Segment.paramSegment(unsaved.modifieddate)(using TypoLocalDateTime.setter)}::timestamp)
    returning "productsubcategoryid", "productcategoryid", "name", "rowguid", "modifieddate"::text
    """.insertReturning(using ProductsubcategoryRow.jdbcDecoder).map(_.updatedKeys.head)
  }

  override def insert(unsaved: ProductsubcategoryRowUnsaved): ZIO[ZConnection, Throwable, ProductsubcategoryRow] = {
    val fs = List(
      Some((sql""""productcategoryid"""", sql"${Segment.paramSegment(unsaved.productcategoryid)(using ProductcategoryId.setter)}::int4")),
      Some((sql""""name"""", sql"${Segment.paramSegment(unsaved.name)(using Name.setter)}::varchar")),
      unsaved.productsubcategoryid match {
        case Defaulted.UseDefault() => None
        case Defaulted.Provided(value) => Some((sql""""productsubcategoryid"""", sql"${Segment.paramSegment(value: ProductsubcategoryId)(using ProductsubcategoryId.setter)}::int4"))
      },
      unsaved.rowguid match {
        case Defaulted.UseDefault() => None
        case Defaulted.Provided(value) => Some((sql""""rowguid"""", sql"${Segment.paramSegment(value: TypoUUID)(using TypoUUID.setter)}::uuid"))
      },
      unsaved.modifieddate match {
        case Defaulted.UseDefault() => None
        case Defaulted.Provided(value) => Some((sql""""modifieddate"""", sql"${Segment.paramSegment(value: TypoLocalDateTime)(using TypoLocalDateTime.setter)}::timestamp"))
      }
    ).flatten
    val q = if (fs.isEmpty) {
      sql"""insert into "production"."productsubcategory" default values
      returning "productsubcategoryid", "productcategoryid", "name", "rowguid", "modifieddate"::text
      """
    } else {
      val names  = fs.map { case (n, _) => n }.mkFragment(SqlFragment(", "))
      val values = fs.map { case (_, f) => f }.mkFragment(SqlFragment(", "))
      sql"""insert into "production"."productsubcategory"($names) values ($values) returning "productsubcategoryid", "productcategoryid", "name", "rowguid", "modifieddate"::text"""
    }
    q.insertReturning(using ProductsubcategoryRow.jdbcDecoder).map(_.updatedKeys.head)
  }

  override def insertStreaming(
    unsaved: ZStream[ZConnection, Throwable, ProductsubcategoryRow],
    batchSize: Int = 10000
  ): ZIO[ZConnection, Throwable, Long] = streamingInsert(s"""COPY "production"."productsubcategory"("productsubcategoryid", "productcategoryid", "name", "rowguid", "modifieddate") FROM STDIN""", batchSize, unsaved)(using ProductsubcategoryRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: ZStream[ZConnection, Throwable, ProductsubcategoryRowUnsaved],
    batchSize: Int = 10000
  ): ZIO[ZConnection, Throwable, Long] = streamingInsert(s"""COPY "production"."productsubcategory"("productcategoryid", "name", "productsubcategoryid", "rowguid", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(using ProductsubcategoryRowUnsaved.pgText)

  override def select: SelectBuilder[ProductsubcategoryFields, ProductsubcategoryRow] = SelectBuilder.of(""""production"."productsubcategory"""", ProductsubcategoryFields.structure, ProductsubcategoryRow.jdbcDecoder)

  override def selectAll: ZStream[ZConnection, Throwable, ProductsubcategoryRow] = sql"""select "productsubcategoryid", "productcategoryid", "name", "rowguid", "modifieddate"::text from "production"."productsubcategory"""".query(using ProductsubcategoryRow.jdbcDecoder).selectStream()

  override def selectById(productsubcategoryid: ProductsubcategoryId): ZIO[ZConnection, Throwable, Option[ProductsubcategoryRow]] = sql"""select "productsubcategoryid", "productcategoryid", "name", "rowguid", "modifieddate"::text from "production"."productsubcategory" where "productsubcategoryid" = ${Segment.paramSegment(productsubcategoryid)(using ProductsubcategoryId.setter)}""".query(using ProductsubcategoryRow.jdbcDecoder).selectOne

  override def selectByIds(productsubcategoryids: Array[ProductsubcategoryId]): ZStream[ZConnection, Throwable, ProductsubcategoryRow] = sql"""select "productsubcategoryid", "productcategoryid", "name", "rowguid", "modifieddate"::text from "production"."productsubcategory" where "productsubcategoryid" = ANY(${Segment.paramSegment(productsubcategoryids)(using ProductsubcategoryId.arraySetter)})""".query(using ProductsubcategoryRow.jdbcDecoder).selectStream()

  override def selectByIdsTracked(productsubcategoryids: Array[ProductsubcategoryId]): ZIO[ZConnection, Throwable, Map[ProductsubcategoryId, ProductsubcategoryRow]] = {
    selectByIds(productsubcategoryids).runCollect.map { rows =>
      val byId = rows.view.map(x => (x.productsubcategoryid, x)).toMap
      productsubcategoryids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
    }
  }

  override def update: UpdateBuilder[ProductsubcategoryFields, ProductsubcategoryRow] = UpdateBuilder.of(""""production"."productsubcategory"""", ProductsubcategoryFields.structure, ProductsubcategoryRow.jdbcDecoder)

  override def update(row: ProductsubcategoryRow): ZIO[ZConnection, Throwable, Option[ProductsubcategoryRow]] = {
    val productsubcategoryid = row.productsubcategoryid
    sql"""update "production"."productsubcategory"
    set "productcategoryid" = ${Segment.paramSegment(row.productcategoryid)(using ProductcategoryId.setter)}::int4,
    "name" = ${Segment.paramSegment(row.name)(using Name.setter)}::varchar,
    "rowguid" = ${Segment.paramSegment(row.rowguid)(using TypoUUID.setter)}::uuid,
    "modifieddate" = ${Segment.paramSegment(row.modifieddate)(using TypoLocalDateTime.setter)}::timestamp
    where "productsubcategoryid" = ${Segment.paramSegment(productsubcategoryid)(using ProductsubcategoryId.setter)}
    returning "productsubcategoryid", "productcategoryid", "name", "rowguid", "modifieddate"::text"""
      .query(using ProductsubcategoryRow.jdbcDecoder)
      .selectOne
  }

  override def upsert(unsaved: ProductsubcategoryRow): ZIO[ZConnection, Throwable, UpdateResult[ProductsubcategoryRow]] = {
    sql"""insert into "production"."productsubcategory"("productsubcategoryid", "productcategoryid", "name", "rowguid", "modifieddate")
    values (
      ${Segment.paramSegment(unsaved.productsubcategoryid)(using ProductsubcategoryId.setter)}::int4,
    ${Segment.paramSegment(unsaved.productcategoryid)(using ProductcategoryId.setter)}::int4,
    ${Segment.paramSegment(unsaved.name)(using Name.setter)}::varchar,
    ${Segment.paramSegment(unsaved.rowguid)(using TypoUUID.setter)}::uuid,
    ${Segment.paramSegment(unsaved.modifieddate)(using TypoLocalDateTime.setter)}::timestamp
    )
    on conflict ("productsubcategoryid")
    do update set
      "productcategoryid" = EXCLUDED."productcategoryid",
    "name" = EXCLUDED."name",
    "rowguid" = EXCLUDED."rowguid",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "productsubcategoryid", "productcategoryid", "name", "rowguid", "modifieddate"::text""".insertReturning(using ProductsubcategoryRow.jdbcDecoder)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: ZStream[ZConnection, Throwable, ProductsubcategoryRow],
    batchSize: Int = 10000
  ): ZIO[ZConnection, Throwable, Long] = {
    val created = sql"""create temporary table productsubcategory_TEMP (like "production"."productsubcategory") on commit drop""".execute
    val copied = streamingInsert(s"""copy productsubcategory_TEMP("productsubcategoryid", "productcategoryid", "name", "rowguid", "modifieddate") from stdin""", batchSize, unsaved)(using ProductsubcategoryRow.pgText)
    val merged = sql"""insert into "production"."productsubcategory"("productsubcategoryid", "productcategoryid", "name", "rowguid", "modifieddate")
    select * from productsubcategory_TEMP
    on conflict ("productsubcategoryid")
    do update set
      "productcategoryid" = EXCLUDED."productcategoryid",
    "name" = EXCLUDED."name",
    "rowguid" = EXCLUDED."rowguid",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table productsubcategory_TEMP;""".update
    created *> copied *> merged
  }
}