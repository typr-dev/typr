/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.illustration

import adventureworks.Text
import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.Defaulted.UseDefault
import adventureworks.customtypes.TypoLocalDateTime
import adventureworks.customtypes.TypoXml
import zio.json.JsonDecoder
import zio.json.JsonEncoder
import zio.json.ast.Json
import zio.json.internal.Write

/** This class corresponds to a row in table `production.illustration` which has not been persisted yet */
case class IllustrationRowUnsaved(
  /** Illustrations used in manufacturing instructions. Stored as XML. */
  diagram: Option[TypoXml] = None,
  /** Default: nextval('production.illustration_illustrationid_seq'::regclass)
   * Primary key for Illustration records.
   */
  illustrationid: Defaulted[IllustrationId] = new UseDefault(),
  /** Default: now() */
  modifieddate: Defaulted[TypoLocalDateTime] = new UseDefault()
) {
  def toRow(
    illustrationidDefault: => IllustrationId,
    modifieddateDefault: => TypoLocalDateTime
  ): IllustrationRow = new IllustrationRow(illustrationid = illustrationid.getOrElse(illustrationidDefault), diagram = diagram, modifieddate = modifieddate.getOrElse(modifieddateDefault))
}

object IllustrationRowUnsaved {
  given jsonDecoder: JsonDecoder[IllustrationRowUnsaved] = {
    JsonDecoder[Json.Obj].mapOrFail { jsonObj =>
      val diagram = jsonObj.get("diagram").fold[Either[String, Option[TypoXml]]](Right(None))(_.as(using JsonDecoder.option(using TypoXml.jsonDecoder)))
      val illustrationid = jsonObj.get("illustrationid").toRight("Missing field 'illustrationid'").flatMap(_.as(using Defaulted.jsonDecoder(using IllustrationId.jsonDecoder)))
      val modifieddate = jsonObj.get("modifieddate").toRight("Missing field 'modifieddate'").flatMap(_.as(using Defaulted.jsonDecoder(using TypoLocalDateTime.jsonDecoder)))
      if (diagram.isRight && illustrationid.isRight && modifieddate.isRight)
        Right(IllustrationRowUnsaved(diagram = diagram.toOption.get, illustrationid = illustrationid.toOption.get, modifieddate = modifieddate.toOption.get))
      else Left(List[Either[String, Any]](diagram, illustrationid, modifieddate).flatMap(_.left.toOption).mkString(", "))
    }
  }

  given jsonEncoder: JsonEncoder[IllustrationRowUnsaved] = {
    new JsonEncoder[IllustrationRowUnsaved] {
      override def unsafeEncode(a: IllustrationRowUnsaved, indent: Option[Int], out: Write): Unit = {
        out.write("{")
        out.write(""""diagram":""")
        JsonEncoder.option(using TypoXml.jsonEncoder).unsafeEncode(a.diagram, indent, out)
        out.write(",")
        out.write(""""illustrationid":""")
        Defaulted.jsonEncoder(using IllustrationId.jsonEncoder).unsafeEncode(a.illustrationid, indent, out)
        out.write(",")
        out.write(""""modifieddate":""")
        Defaulted.jsonEncoder(using TypoLocalDateTime.jsonEncoder).unsafeEncode(a.modifieddate, indent, out)
        out.write("}")
      }
    }
  }

  given pgText: Text[IllustrationRowUnsaved] = {
    Text.instance[IllustrationRowUnsaved]{ (row, sb) =>
      Text.option(using TypoXml.pgText).unsafeEncode(row.diagram, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using IllustrationId.pgText).unsafeEncode(row.illustrationid, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using TypoLocalDateTime.pgText).unsafeEncode(row.modifieddate, sb)
    }
  }
}