/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.creditcard

import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.TypoLocalDateTime
import adventureworks.customtypes.TypoShort
import adventureworks.streamingInsert
import adventureworks.userdefined.CustomCreditcardId
import typr.dsl.DeleteBuilder
import typr.dsl.SelectBuilder
import typr.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment
import zio.jdbc.SqlFragment.Segment
import zio.jdbc.SqlFragment.Setter
import zio.jdbc.UpdateResult
import zio.jdbc.ZConnection
import zio.stream.ZStream
import zio.jdbc.sqlInterpolator

class CreditcardRepoImpl extends CreditcardRepo {
  override def delete: DeleteBuilder[CreditcardFields, CreditcardRow] = DeleteBuilder.of(""""sales"."creditcard"""", CreditcardFields.structure, CreditcardRow.jdbcDecoder)

  override def deleteById(creditcardid: /* user-picked */ CustomCreditcardId): ZIO[ZConnection, Throwable, Boolean] = sql"""delete from "sales"."creditcard" where "creditcardid" = ${Segment.paramSegment(creditcardid)(using /* user-picked */ CustomCreditcardId.setter)}""".delete.map(_ > 0)

  override def deleteByIds(creditcardids: Array[/* user-picked */ CustomCreditcardId]): ZIO[ZConnection, Throwable, Long] = sql"""delete from "sales"."creditcard" where "creditcardid" = ANY(${Segment.paramSegment(creditcardids)(using CustomCreditcardId.arraySetter)})""".delete

  override def insert(unsaved: CreditcardRow): ZIO[ZConnection, Throwable, CreditcardRow] = {
    sql"""insert into "sales"."creditcard"("creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate")
    values (${Segment.paramSegment(unsaved.creditcardid)(using /* user-picked */ CustomCreditcardId.setter)}::int4, ${Segment.paramSegment(unsaved.cardtype)(using Setter.stringSetter)}, ${Segment.paramSegment(unsaved.cardnumber)(using Setter.stringSetter)}, ${Segment.paramSegment(unsaved.expmonth)(using TypoShort.setter)}::int2, ${Segment.paramSegment(unsaved.expyear)(using TypoShort.setter)}::int2, ${Segment.paramSegment(unsaved.modifieddate)(using TypoLocalDateTime.setter)}::timestamp)
    returning "creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate"::text
    """.insertReturning(using CreditcardRow.jdbcDecoder).map(_.updatedKeys.head)
  }

  override def insert(unsaved: CreditcardRowUnsaved): ZIO[ZConnection, Throwable, CreditcardRow] = {
    val fs = List(
      Some((sql""""cardtype"""", sql"${Segment.paramSegment(unsaved.cardtype)(using Setter.stringSetter)}")),
      Some((sql""""cardnumber"""", sql"${Segment.paramSegment(unsaved.cardnumber)(using Setter.stringSetter)}")),
      Some((sql""""expmonth"""", sql"${Segment.paramSegment(unsaved.expmonth)(using TypoShort.setter)}::int2")),
      Some((sql""""expyear"""", sql"${Segment.paramSegment(unsaved.expyear)(using TypoShort.setter)}::int2")),
      unsaved.creditcardid match {
        case Defaulted.UseDefault() => None
        case Defaulted.Provided(value) => Some((sql""""creditcardid"""", sql"${Segment.paramSegment(value: /* user-picked */ CustomCreditcardId)(using /* user-picked */ CustomCreditcardId.setter)}::int4"))
      },
      unsaved.modifieddate match {
        case Defaulted.UseDefault() => None
        case Defaulted.Provided(value) => Some((sql""""modifieddate"""", sql"${Segment.paramSegment(value: TypoLocalDateTime)(using TypoLocalDateTime.setter)}::timestamp"))
      }
    ).flatten
    val q = if (fs.isEmpty) {
      sql"""insert into "sales"."creditcard" default values
      returning "creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate"::text
      """
    } else {
      val names  = fs.map { case (n, _) => n }.mkFragment(SqlFragment(", "))
      val values = fs.map { case (_, f) => f }.mkFragment(SqlFragment(", "))
      sql"""insert into "sales"."creditcard"($names) values ($values) returning "creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate"::text"""
    }
    q.insertReturning(using CreditcardRow.jdbcDecoder).map(_.updatedKeys.head)
  }

  override def insertStreaming(
    unsaved: ZStream[ZConnection, Throwable, CreditcardRow],
    batchSize: Int = 10000
  ): ZIO[ZConnection, Throwable, Long] = streamingInsert(s"""COPY "sales"."creditcard"("creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate") FROM STDIN""", batchSize, unsaved)(using CreditcardRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: ZStream[ZConnection, Throwable, CreditcardRowUnsaved],
    batchSize: Int = 10000
  ): ZIO[ZConnection, Throwable, Long] = streamingInsert(s"""COPY "sales"."creditcard"("cardtype", "cardnumber", "expmonth", "expyear", "creditcardid", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(using CreditcardRowUnsaved.pgText)

  override def select: SelectBuilder[CreditcardFields, CreditcardRow] = SelectBuilder.of(""""sales"."creditcard"""", CreditcardFields.structure, CreditcardRow.jdbcDecoder)

  override def selectAll: ZStream[ZConnection, Throwable, CreditcardRow] = sql"""select "creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate"::text from "sales"."creditcard"""".query(using CreditcardRow.jdbcDecoder).selectStream()

  override def selectById(creditcardid: /* user-picked */ CustomCreditcardId): ZIO[ZConnection, Throwable, Option[CreditcardRow]] = sql"""select "creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate"::text from "sales"."creditcard" where "creditcardid" = ${Segment.paramSegment(creditcardid)(using /* user-picked */ CustomCreditcardId.setter)}""".query(using CreditcardRow.jdbcDecoder).selectOne

  override def selectByIds(creditcardids: Array[/* user-picked */ CustomCreditcardId]): ZStream[ZConnection, Throwable, CreditcardRow] = sql"""select "creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate"::text from "sales"."creditcard" where "creditcardid" = ANY(${Segment.paramSegment(creditcardids)(using CustomCreditcardId.arraySetter)})""".query(using CreditcardRow.jdbcDecoder).selectStream()

  override def selectByIdsTracked(creditcardids: Array[/* user-picked */ CustomCreditcardId]): ZIO[ZConnection, Throwable, Map[/* user-picked */ CustomCreditcardId, CreditcardRow]] = {
    selectByIds(creditcardids).runCollect.map { rows =>
      val byId = rows.view.map(x => (x.creditcardid, x)).toMap
      creditcardids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
    }
  }

  override def update: UpdateBuilder[CreditcardFields, CreditcardRow] = UpdateBuilder.of(""""sales"."creditcard"""", CreditcardFields.structure, CreditcardRow.jdbcDecoder)

  override def update(row: CreditcardRow): ZIO[ZConnection, Throwable, Option[CreditcardRow]] = {
    val creditcardid = row.creditcardid
    sql"""update "sales"."creditcard"
    set "cardtype" = ${Segment.paramSegment(row.cardtype)(using Setter.stringSetter)},
    "cardnumber" = ${Segment.paramSegment(row.cardnumber)(using Setter.stringSetter)},
    "expmonth" = ${Segment.paramSegment(row.expmonth)(using TypoShort.setter)}::int2,
    "expyear" = ${Segment.paramSegment(row.expyear)(using TypoShort.setter)}::int2,
    "modifieddate" = ${Segment.paramSegment(row.modifieddate)(using TypoLocalDateTime.setter)}::timestamp
    where "creditcardid" = ${Segment.paramSegment(creditcardid)(using /* user-picked */ CustomCreditcardId.setter)}
    returning "creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate"::text"""
      .query(using CreditcardRow.jdbcDecoder)
      .selectOne
  }

  override def upsert(unsaved: CreditcardRow): ZIO[ZConnection, Throwable, UpdateResult[CreditcardRow]] = {
    sql"""insert into "sales"."creditcard"("creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate")
    values (
      ${Segment.paramSegment(unsaved.creditcardid)(using /* user-picked */ CustomCreditcardId.setter)}::int4,
    ${Segment.paramSegment(unsaved.cardtype)(using Setter.stringSetter)},
    ${Segment.paramSegment(unsaved.cardnumber)(using Setter.stringSetter)},
    ${Segment.paramSegment(unsaved.expmonth)(using TypoShort.setter)}::int2,
    ${Segment.paramSegment(unsaved.expyear)(using TypoShort.setter)}::int2,
    ${Segment.paramSegment(unsaved.modifieddate)(using TypoLocalDateTime.setter)}::timestamp
    )
    on conflict ("creditcardid")
    do update set
      "cardtype" = EXCLUDED."cardtype",
    "cardnumber" = EXCLUDED."cardnumber",
    "expmonth" = EXCLUDED."expmonth",
    "expyear" = EXCLUDED."expyear",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate"::text""".insertReturning(using CreditcardRow.jdbcDecoder)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: ZStream[ZConnection, Throwable, CreditcardRow],
    batchSize: Int = 10000
  ): ZIO[ZConnection, Throwable, Long] = {
    val created = sql"""create temporary table creditcard_TEMP (like "sales"."creditcard") on commit drop""".execute
    val copied = streamingInsert(s"""copy creditcard_TEMP("creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate") from stdin""", batchSize, unsaved)(using CreditcardRow.pgText)
    val merged = sql"""insert into "sales"."creditcard"("creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate")
    select * from creditcard_TEMP
    on conflict ("creditcardid")
    do update set
      "cardtype" = EXCLUDED."cardtype",
    "cardnumber" = EXCLUDED."cardnumber",
    "expmonth" = EXCLUDED."expmonth",
    "expyear" = EXCLUDED."expyear",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table creditcard_TEMP;""".update
    created *> copied *> merged
  }
}