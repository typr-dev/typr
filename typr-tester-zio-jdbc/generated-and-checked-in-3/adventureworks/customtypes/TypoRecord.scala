/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.customtypes

import adventureworks.Text
import java.sql.ResultSet
import java.sql.Types
import org.postgresql.util.PGobject
import typr.dsl.Bijection
import typr.dsl.PGType
import zio.jdbc.JdbcDecoder
import zio.jdbc.JdbcEncoder
import zio.jdbc.SqlFragment.Setter
import zio.json.JsonDecoder
import zio.json.JsonEncoder

/** record (via PGObject) */
case class TypoRecord(value: String)

object TypoRecord {
  given arrayJdbcDecoder: JdbcDecoder[Array[TypoRecord]] = {
    JdbcDecoder[Array[TypoRecord]]((rs: ResultSet) => (i: Int) =>
      rs.getArray(i) match {
        case null => null
        case arr => arr.getArray.asInstanceOf[Array[AnyRef]].map(x => new TypoRecord(x.asInstanceOf[PGobject].getValue))
      },
      "Array[org.postgresql.util.PGobject]"
    )
  }

  given arrayJdbcEncoder: JdbcEncoder[Array[TypoRecord]] = JdbcEncoder.singleParamEncoder(using arraySetter)

  given arraySetter: Setter[Array[TypoRecord]] = {
    Setter.forSqlType((ps, i, v) =>
      ps.setArray(
        i,
        ps.getConnection.createArrayOf(
          "record",
          v.map { vv =>
            {
              val obj = new PGobject()
              obj.setType("record")
              obj.setValue(vv.value)
              obj
            }
          }
        )
      ),
      Types.ARRAY
    )
  }

  given bijection: Bijection[TypoRecord, String] = Bijection.apply[TypoRecord, String](_.value)(TypoRecord.apply)

  given jdbcDecoder: JdbcDecoder[TypoRecord] = {
    JdbcDecoder[TypoRecord](
      (rs: ResultSet) => (i: Int) => {
        val v = rs.getObject(i)
        if (v eq null) null else new TypoRecord(v.asInstanceOf[PGobject].getValue)
      },
      "org.postgresql.util.PGobject"
    )
  }

  given jdbcEncoder: JdbcEncoder[TypoRecord] = JdbcEncoder.singleParamEncoder(using setter)

  given jsonDecoder: JsonDecoder[TypoRecord] = JsonDecoder.string.map(TypoRecord.apply)

  given jsonEncoder: JsonEncoder[TypoRecord] = JsonEncoder.string.contramap(_.value)

  given pgText: Text[TypoRecord] = {
    new Text[TypoRecord] {
      override def unsafeEncode(v: TypoRecord, sb: StringBuilder): Unit = Text.stringInstance.unsafeEncode(v.value, sb)
      override def unsafeArrayEncode(v: TypoRecord, sb: StringBuilder): Unit = Text.stringInstance.unsafeArrayEncode(v.value, sb)
    }
  }

  given pgType: PGType[TypoRecord] = PGType.instance[TypoRecord]("record", Types.OTHER)

  given setter: Setter[TypoRecord] = {
    Setter.other(
      (ps, i, v) => {
        ps.setObject(
          i,
          {
            val obj = new PGobject()
            obj.setType("record")
            obj.setValue(v.value)
            obj
          }
        )
      },
      "record"
    )
  }
}