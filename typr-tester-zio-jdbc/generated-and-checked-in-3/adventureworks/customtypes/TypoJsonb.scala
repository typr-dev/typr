/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.customtypes

import adventureworks.Text
import java.sql.ResultSet
import java.sql.Types
import org.postgresql.util.PGobject
import typr.dsl.Bijection
import typr.dsl.PGType
import zio.jdbc.JdbcDecoder
import zio.jdbc.JdbcEncoder
import zio.jdbc.SqlFragment.Setter
import zio.json.JsonDecoder
import zio.json.JsonEncoder

/** jsonb (via PGObject) */
case class TypoJsonb(value: String)

object TypoJsonb {
  given arrayJdbcDecoder: JdbcDecoder[Array[TypoJsonb]] = {
    JdbcDecoder[Array[TypoJsonb]]((rs: ResultSet) => (i: Int) =>
      rs.getArray(i) match {
        case null => null
        case arr => arr.getArray.asInstanceOf[Array[AnyRef]].map(x => new TypoJsonb(x.asInstanceOf[String]))
      },
      "Array[org.postgresql.util.PGobject]"
    )
  }

  given arrayJdbcEncoder: JdbcEncoder[Array[TypoJsonb]] = JdbcEncoder.singleParamEncoder(using arraySetter)

  given arraySetter: Setter[Array[TypoJsonb]] = {
    Setter.forSqlType((ps, i, v) =>
      ps.setArray(
        i,
        ps.getConnection.createArrayOf(
          "jsonb",
          v.map { vv =>
            {
              val obj = new PGobject()
              obj.setType("jsonb")
              obj.setValue(vv.value)
              obj
            }
          }
        )
      ),
      Types.ARRAY
    )
  }

  given bijection: Bijection[TypoJsonb, String] = Bijection.apply[TypoJsonb, String](_.value)(TypoJsonb.apply)

  given jdbcDecoder: JdbcDecoder[TypoJsonb] = {
    JdbcDecoder[TypoJsonb](
      (rs: ResultSet) => (i: Int) => {
        val v = rs.getObject(i)
        if (v eq null) null else new TypoJsonb(v.asInstanceOf[PGobject].getValue)
      },
      "org.postgresql.util.PGobject"
    )
  }

  given jdbcEncoder: JdbcEncoder[TypoJsonb] = JdbcEncoder.singleParamEncoder(using setter)

  given jsonDecoder: JsonDecoder[TypoJsonb] = JsonDecoder.string.map(TypoJsonb.apply)

  given jsonEncoder: JsonEncoder[TypoJsonb] = JsonEncoder.string.contramap(_.value)

  given pgText: Text[TypoJsonb] = {
    new Text[TypoJsonb] {
      override def unsafeEncode(v: TypoJsonb, sb: StringBuilder): Unit = Text.stringInstance.unsafeEncode(v.value, sb)
      override def unsafeArrayEncode(v: TypoJsonb, sb: StringBuilder): Unit = Text.stringInstance.unsafeArrayEncode(v.value, sb)
    }
  }

  given pgType: PGType[TypoJsonb] = PGType.instance[TypoJsonb]("jsonb", Types.OTHER)

  given setter: Setter[TypoJsonb] = {
    Setter.other(
      (ps, i, v) => {
        ps.setObject(
          i,
          {
            val obj = new PGobject()
            obj.setType("jsonb")
            obj.setValue(v.value)
            obj
          }
        )
      },
      "jsonb"
    )
  }
}