/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.customtypes

import adventureworks.Text
import java.sql.ResultSet
import java.sql.Types
import typr.dsl.Bijection
import typr.dsl.PGType
import zio.jdbc.JdbcDecoder
import zio.jdbc.JdbcEncoder
import zio.jdbc.SqlFragment.Setter
import zio.json.JsonDecoder
import zio.json.JsonEncoder

/** Money and cash types in PostgreSQL */
case class TypoMoney(value: BigDecimal)

object TypoMoney {
  given arrayJdbcDecoder: JdbcDecoder[Array[TypoMoney]] = {
    JdbcDecoder[Array[TypoMoney]]((rs: ResultSet) => (i: Int) =>
      rs.getArray(i) match {
        case null => null
        case arr => arr.getArray.asInstanceOf[Array[AnyRef]].map(x => new TypoMoney(BigDecimal(x.asInstanceOf[java.math.BigDecimal])))
      },
      "Array[java.math.BigDecimal]"
    )
  }

  given arrayJdbcEncoder: JdbcEncoder[Array[TypoMoney]] = JdbcEncoder.singleParamEncoder(using arraySetter)

  given arraySetter: Setter[Array[TypoMoney]] = {
    Setter.forSqlType((ps, i, v) =>
      ps.setArray(
        i,
        ps.getConnection.createArrayOf(
          "money",
          v.map { vv =>
            vv.value.bigDecimal
          }
        )
      ),
      Types.ARRAY
    )
  }

  given bijection: Bijection[TypoMoney, BigDecimal] = Bijection.apply[TypoMoney, BigDecimal](_.value)(TypoMoney.apply)

  given jdbcDecoder: JdbcDecoder[TypoMoney] = {
    JdbcDecoder[TypoMoney](
      (rs: ResultSet) => (i: Int) => {
        val v = rs.getObject(i)
        if (v eq null) null else new TypoMoney(BigDecimal(v.asInstanceOf[java.math.BigDecimal]))
      },
      "java.math.BigDecimal"
    )
  }

  given jdbcEncoder: JdbcEncoder[TypoMoney] = JdbcEncoder.singleParamEncoder(using setter)

  given jsonDecoder: JsonDecoder[TypoMoney] = JsonDecoder.scalaBigDecimal.map(TypoMoney.apply)

  given jsonEncoder: JsonEncoder[TypoMoney] = JsonEncoder.scalaBigDecimal.contramap(_.value)

  given pgText: Text[TypoMoney] = {
    new Text[TypoMoney] {
      override def unsafeEncode(v: TypoMoney, sb: StringBuilder): Unit = Text.bigDecimalInstance.unsafeEncode(v.value, sb)
      override def unsafeArrayEncode(v: TypoMoney, sb: StringBuilder): Unit = Text.bigDecimalInstance.unsafeArrayEncode(v.value, sb)
    }
  }

  given pgType: PGType[TypoMoney] = PGType.instance[TypoMoney]("money", Types.OTHER)

  given setter: Setter[TypoMoney] = {
    Setter.other(
      (ps, i, v) => {
        ps.setObject(
          i,
          v.value.bigDecimal
        )
      },
      "money"
    )
  }
}