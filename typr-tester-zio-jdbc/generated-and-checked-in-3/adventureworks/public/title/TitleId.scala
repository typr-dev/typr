/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.title

import adventureworks.Text
import java.sql.Types
import typr.dsl.PGType
import zio.jdbc.JdbcDecoder
import zio.jdbc.JdbcEncoder
import zio.jdbc.SqlFragment.Setter
import zio.json.JsonDecoder
import zio.json.JsonEncoder

/** Type for the primary key of table `public.title`. It has some known values: 
 *  - dr
 *  - mr
 *  - ms
 *  - phd
 */

sealed abstract class TitleId(val value: String)

object TitleId {
  def apply(underlying: String): TitleId =
    ByName.getOrElse(underlying, Unknown(underlying))
  given arraySetter: Setter[Array[TitleId]] = adventureworks.StringArraySetter.contramap(_.map(_.value))

  given arrayJdbcDecoder: JdbcDecoder[Array[TitleId]] = adventureworks.StringArrayDecoder.map(a => if (a == null) null else a.map(apply))

  given arrayJdbcEncoder: JdbcEncoder[Array[TitleId]] = JdbcEncoder.singleParamEncoder(using arraySetter)

  given jdbcEncoder: JdbcEncoder[TitleId] = JdbcEncoder.stringEncoder.contramap(_.value)

  given jdbcDecoder: JdbcDecoder[TitleId] = JdbcDecoder.stringDecoder.map(TitleId.apply)

  given setter: Setter[TitleId] = Setter.stringSetter.contramap(_.value)

  given pgType: PGType[TitleId] = PGType.instance[TitleId]("text", Types.OTHER)

  given pgText: Text[TitleId] = {
    new Text[TitleId] {
      override def unsafeEncode(v: TitleId, sb: StringBuilder): Unit = Text.stringInstance.unsafeEncode(v.value, sb)
      override def unsafeArrayEncode(v: TitleId, sb: StringBuilder): Unit = Text.stringInstance.unsafeArrayEncode(v.value, sb)
    }
  }

  given jsonDecoder: JsonDecoder[TitleId] = JsonDecoder.string.map(TitleId.apply)

  given jsonEncoder: JsonEncoder[TitleId] = JsonEncoder.string.contramap(_.value)

  case object dr extends TitleId("dr")

  case object mr extends TitleId("mr")

  case object ms extends TitleId("ms")

  case object phd extends TitleId("phd")
  case class Unknown(override val value: String) extends TitleId(value)
  val All: scala.List[TitleId] = scala.List(dr, mr, ms, phd)
  val ByName: scala.collection.immutable.Map[String, TitleId] = All.map(x => (x.value, x)).toMap

}