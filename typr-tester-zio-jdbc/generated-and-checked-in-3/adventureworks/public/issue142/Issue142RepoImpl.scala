/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.issue142

import adventureworks.streamingInsert
import typr.dsl.DeleteBuilder
import typr.dsl.SelectBuilder
import typr.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment.Segment
import zio.jdbc.UpdateResult
import zio.jdbc.ZConnection
import zio.stream.ZStream
import zio.jdbc.sqlInterpolator

class Issue142RepoImpl extends Issue142Repo {
  override def delete: DeleteBuilder[Issue142Fields, Issue142Row] = DeleteBuilder.of(""""public"."issue142"""", Issue142Fields.structure, Issue142Row.jdbcDecoder)

  override def deleteById(tabellkode: Issue142Id): ZIO[ZConnection, Throwable, Boolean] = sql"""delete from "public"."issue142" where "tabellkode" = ${Segment.paramSegment(tabellkode)(using Issue142Id.setter)}""".delete.map(_ > 0)

  override def deleteByIds(tabellkodes: Array[Issue142Id]): ZIO[ZConnection, Throwable, Long] = sql"""delete from "public"."issue142" where "tabellkode" = ANY(${Segment.paramSegment(tabellkodes)(using Issue142Id.arraySetter)})""".delete

  override def insert(unsaved: Issue142Row): ZIO[ZConnection, Throwable, Issue142Row] = {
    sql"""insert into "public"."issue142"("tabellkode")
    values (${Segment.paramSegment(unsaved.tabellkode)(using Issue142Id.setter)})
    returning "tabellkode"
    """.insertReturning(using Issue142Row.jdbcDecoder).map(_.updatedKeys.head)
  }

  override def insertStreaming(
    unsaved: ZStream[ZConnection, Throwable, Issue142Row],
    batchSize: Int = 10000
  ): ZIO[ZConnection, Throwable, Long] = streamingInsert(s"""COPY "public"."issue142"("tabellkode") FROM STDIN""", batchSize, unsaved)(using Issue142Row.pgText)

  override def select: SelectBuilder[Issue142Fields, Issue142Row] = SelectBuilder.of(""""public"."issue142"""", Issue142Fields.structure, Issue142Row.jdbcDecoder)

  override def selectAll: ZStream[ZConnection, Throwable, Issue142Row] = sql"""select "tabellkode" from "public"."issue142"""".query(using Issue142Row.jdbcDecoder).selectStream()

  override def selectById(tabellkode: Issue142Id): ZIO[ZConnection, Throwable, Option[Issue142Row]] = sql"""select "tabellkode" from "public"."issue142" where "tabellkode" = ${Segment.paramSegment(tabellkode)(using Issue142Id.setter)}""".query(using Issue142Row.jdbcDecoder).selectOne

  override def selectByIds(tabellkodes: Array[Issue142Id]): ZStream[ZConnection, Throwable, Issue142Row] = sql"""select "tabellkode" from "public"."issue142" where "tabellkode" = ANY(${Segment.paramSegment(tabellkodes)(using Issue142Id.arraySetter)})""".query(using Issue142Row.jdbcDecoder).selectStream()

  override def selectByIdsTracked(tabellkodes: Array[Issue142Id]): ZIO[ZConnection, Throwable, Map[Issue142Id, Issue142Row]] = {
    selectByIds(tabellkodes).runCollect.map { rows =>
      val byId = rows.view.map(x => (x.tabellkode, x)).toMap
      tabellkodes.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
    }
  }

  override def update: UpdateBuilder[Issue142Fields, Issue142Row] = UpdateBuilder.of(""""public"."issue142"""", Issue142Fields.structure, Issue142Row.jdbcDecoder)

  override def upsert(unsaved: Issue142Row): ZIO[ZConnection, Throwable, UpdateResult[Issue142Row]] = {
    sql"""insert into "public"."issue142"("tabellkode")
    values (
      ${Segment.paramSegment(unsaved.tabellkode)(using Issue142Id.setter)}
    )
    on conflict ("tabellkode")
    do update set "tabellkode" = EXCLUDED."tabellkode"
    returning "tabellkode"""".insertReturning(using Issue142Row.jdbcDecoder)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: ZStream[ZConnection, Throwable, Issue142Row],
    batchSize: Int = 10000
  ): ZIO[ZConnection, Throwable, Long] = {
    val created = sql"""create temporary table issue142_TEMP (like "public"."issue142") on commit drop""".execute
    val copied = streamingInsert(s"""copy issue142_TEMP("tabellkode") from stdin""", batchSize, unsaved)(using Issue142Row.pgText)
    val merged = sql"""insert into "public"."issue142"("tabellkode")
    select * from issue142_TEMP
    on conflict ("tabellkode")
    do nothing
    ;
    drop table issue142_TEMP;""".update
    created *> copied *> merged
  }
}