/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.purchasing.shipmethod

import adventureworks.Text
import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.Defaulted.UseDefault
import adventureworks.customtypes.TypoLocalDateTime
import adventureworks.customtypes.TypoUUID
import adventureworks.public.Name
import zio.json.JsonDecoder
import zio.json.JsonEncoder
import zio.json.ast.Json
import zio.json.internal.Write

/** This class corresponds to a row in table `purchasing.shipmethod` which has not been persisted yet */
case class ShipmethodRowUnsaved(
  /** Shipping company name. */
  name: Name,
  /** Default: nextval('purchasing.shipmethod_shipmethodid_seq'::regclass)
   * Primary key for ShipMethod records.
   */
  shipmethodid: Defaulted[ShipmethodId] = new UseDefault(),
  /** Default: 0.00
   * Minimum shipping charge.
   * Constraint CK_ShipMethod_ShipBase affecting columns shipbase:  ((shipbase > 0.00))
   */
  shipbase: Defaulted[BigDecimal] = new UseDefault(),
  /** Default: 0.00
   * Shipping charge per pound.
   * Constraint CK_ShipMethod_ShipRate affecting columns shiprate:  ((shiprate > 0.00))
   */
  shiprate: Defaulted[BigDecimal] = new UseDefault(),
  /** Default: uuid_generate_v1() */
  rowguid: Defaulted[TypoUUID] = new UseDefault(),
  /** Default: now() */
  modifieddate: Defaulted[TypoLocalDateTime] = new UseDefault()
) {
  def toRow(
    shipmethodidDefault: => ShipmethodId,
    shipbaseDefault: => BigDecimal,
    shiprateDefault: => BigDecimal,
    rowguidDefault: => TypoUUID,
    modifieddateDefault: => TypoLocalDateTime
  ): ShipmethodRow = {
    new ShipmethodRow(
      shipmethodid = shipmethodid.getOrElse(shipmethodidDefault),
      name = name,
      shipbase = shipbase.getOrElse(shipbaseDefault),
      shiprate = shiprate.getOrElse(shiprateDefault),
      rowguid = rowguid.getOrElse(rowguidDefault),
      modifieddate = modifieddate.getOrElse(modifieddateDefault)
    )
  }
}

object ShipmethodRowUnsaved {
  given jsonDecoder: JsonDecoder[ShipmethodRowUnsaved] = {
    JsonDecoder[Json.Obj].mapOrFail { jsonObj =>
      val name = jsonObj.get("name").toRight("Missing field 'name'").flatMap(_.as(using Name.jsonDecoder))
      val shipmethodid = jsonObj.get("shipmethodid").toRight("Missing field 'shipmethodid'").flatMap(_.as(using Defaulted.jsonDecoder(using ShipmethodId.jsonDecoder)))
      val shipbase = jsonObj.get("shipbase").toRight("Missing field 'shipbase'").flatMap(_.as(using Defaulted.jsonDecoder(using JsonDecoder.scalaBigDecimal)))
      val shiprate = jsonObj.get("shiprate").toRight("Missing field 'shiprate'").flatMap(_.as(using Defaulted.jsonDecoder(using JsonDecoder.scalaBigDecimal)))
      val rowguid = jsonObj.get("rowguid").toRight("Missing field 'rowguid'").flatMap(_.as(using Defaulted.jsonDecoder(using TypoUUID.jsonDecoder)))
      val modifieddate = jsonObj.get("modifieddate").toRight("Missing field 'modifieddate'").flatMap(_.as(using Defaulted.jsonDecoder(using TypoLocalDateTime.jsonDecoder)))
      if (name.isRight && shipmethodid.isRight && shipbase.isRight && shiprate.isRight && rowguid.isRight && modifieddate.isRight)
        Right(ShipmethodRowUnsaved(name = name.toOption.get, shipmethodid = shipmethodid.toOption.get, shipbase = shipbase.toOption.get, shiprate = shiprate.toOption.get, rowguid = rowguid.toOption.get, modifieddate = modifieddate.toOption.get))
      else Left(List[Either[String, Any]](name, shipmethodid, shipbase, shiprate, rowguid, modifieddate).flatMap(_.left.toOption).mkString(", "))
    }
  }

  given jsonEncoder: JsonEncoder[ShipmethodRowUnsaved] = {
    new JsonEncoder[ShipmethodRowUnsaved] {
      override def unsafeEncode(a: ShipmethodRowUnsaved, indent: Option[Int], out: Write): Unit = {
        out.write("{")
        out.write(""""name":""")
        Name.jsonEncoder.unsafeEncode(a.name, indent, out)
        out.write(",")
        out.write(""""shipmethodid":""")
        Defaulted.jsonEncoder(using ShipmethodId.jsonEncoder).unsafeEncode(a.shipmethodid, indent, out)
        out.write(",")
        out.write(""""shipbase":""")
        Defaulted.jsonEncoder(using JsonEncoder.scalaBigDecimal).unsafeEncode(a.shipbase, indent, out)
        out.write(",")
        out.write(""""shiprate":""")
        Defaulted.jsonEncoder(using JsonEncoder.scalaBigDecimal).unsafeEncode(a.shiprate, indent, out)
        out.write(",")
        out.write(""""rowguid":""")
        Defaulted.jsonEncoder(using TypoUUID.jsonEncoder).unsafeEncode(a.rowguid, indent, out)
        out.write(",")
        out.write(""""modifieddate":""")
        Defaulted.jsonEncoder(using TypoLocalDateTime.jsonEncoder).unsafeEncode(a.modifieddate, indent, out)
        out.write("}")
      }
    }
  }

  given pgText: Text[ShipmethodRowUnsaved] = {
    Text.instance[ShipmethodRowUnsaved]{ (row, sb) =>
      Name.pgText.unsafeEncode(row.name, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using ShipmethodId.pgText).unsafeEncode(row.shipmethodid, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using Text.bigDecimalInstance).unsafeEncode(row.shipbase, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using Text.bigDecimalInstance).unsafeEncode(row.shiprate, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using TypoUUID.pgText).unsafeEncode(row.rowguid, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using TypoLocalDateTime.pgText).unsafeEncode(row.modifieddate, sb)
    }
  }
}