/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.transactionhistory

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class TransactionhistoryRepoMock(
  val toRow: (TransactionhistoryRowUnsaved) -> TransactionhistoryRow,
  val map: MutableMap<TransactionhistoryId, TransactionhistoryRow>
) : TransactionhistoryRepo {
  constructor(toRow: (TransactionhistoryRowUnsaved) -> TransactionhistoryRow) : this(toRow, mutableMapOf<TransactionhistoryId, TransactionhistoryRow>())

  fun deleteById(
    transactionid: TransactionhistoryId,
    c: Connection
  ): Boolean = (map.remove(transactionid) != null)

  fun deleteByIds(
    transactionids: Array<TransactionhistoryId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : transactionids) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: TransactionhistoryRow,
    c: Connection
  ): TransactionhistoryRow {
    if (map.containsKey(unsaved.transactionid)) {
        throw new RuntimeException(str("id $unsaved.transactionid already exists"))
      }
      map[unsaved.transactionid] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: TransactionhistoryRowUnsaved,
    c: Connection
  ): TransactionhistoryRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<TransactionhistoryRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.transactionid] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<TransactionhistoryRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.transactionid] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<TransactionhistoryRow> = map.values.toList()

  fun selectById(
    transactionid: TransactionhistoryId,
    c: Connection
  ): TransactionhistoryRow? = map[transactionid]

  fun selectByIds(
    transactionids: Array<TransactionhistoryId>,
    c: Connection
  ): List<TransactionhistoryRow> = transactionids.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    transactionids: Array<TransactionhistoryId>,
    c: Connection
  ): Map<TransactionhistoryId, TransactionhistoryRow> = selectByIds(transactionids, c).associateBy { { row -> row.transactionid }(it) }

  fun update(
    row: TransactionhistoryRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.transactionid]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.transactionid] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: TransactionhistoryRow,
    c: Connection
  ): TransactionhistoryRow {
    map[unsaved.transactionid] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<TransactionhistoryRow>,
    c: Connection
  ): List<TransactionhistoryRow> {
    var result = ArrayList<TransactionhistoryRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.transactionid] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<TransactionhistoryRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.transactionid] = row
        count = count + 1
      }
    return count
  }
}