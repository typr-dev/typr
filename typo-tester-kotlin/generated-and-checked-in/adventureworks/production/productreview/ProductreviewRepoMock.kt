/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productreview

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class ProductreviewRepoMock(
  val toRow: (ProductreviewRowUnsaved) -> ProductreviewRow,
  val map: MutableMap<ProductreviewId, ProductreviewRow>
) : ProductreviewRepo {
  constructor(toRow: (ProductreviewRowUnsaved) -> ProductreviewRow) : this(toRow, mutableMapOf<ProductreviewId, ProductreviewRow>())

  fun deleteById(
    productreviewid: ProductreviewId,
    c: Connection
  ): Boolean = (map.remove(productreviewid) != null)

  fun deleteByIds(
    productreviewids: Array<ProductreviewId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : productreviewids) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: ProductreviewRow,
    c: Connection
  ): ProductreviewRow {
    if (map.containsKey(unsaved.productreviewid)) {
        throw new RuntimeException(str("id $unsaved.productreviewid already exists"))
      }
      map[unsaved.productreviewid] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: ProductreviewRowUnsaved,
    c: Connection
  ): ProductreviewRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<ProductreviewRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.productreviewid] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<ProductreviewRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.productreviewid] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<ProductreviewRow> = map.values.toList()

  fun selectById(
    productreviewid: ProductreviewId,
    c: Connection
  ): ProductreviewRow? = map[productreviewid]

  fun selectByIds(
    productreviewids: Array<ProductreviewId>,
    c: Connection
  ): List<ProductreviewRow> = productreviewids.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    productreviewids: Array<ProductreviewId>,
    c: Connection
  ): Map<ProductreviewId, ProductreviewRow> = selectByIds(productreviewids, c).associateBy { { row -> row.productreviewid }(it) }

  fun update(
    row: ProductreviewRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.productreviewid]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.productreviewid] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: ProductreviewRow,
    c: Connection
  ): ProductreviewRow {
    map[unsaved.productreviewid] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<ProductreviewRow>,
    c: Connection
  ): List<ProductreviewRow> {
    var result = ArrayList<ProductreviewRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.productreviewid] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<ProductreviewRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.productreviewid] = row
        count = count + 1
      }
    return count
  }
}