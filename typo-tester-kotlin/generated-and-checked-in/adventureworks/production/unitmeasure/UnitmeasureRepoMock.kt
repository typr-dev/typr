/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.unitmeasure

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class UnitmeasureRepoMock(
  val toRow: (UnitmeasureRowUnsaved) -> UnitmeasureRow,
  val map: MutableMap<UnitmeasureId, UnitmeasureRow>
) : UnitmeasureRepo {
  constructor(toRow: (UnitmeasureRowUnsaved) -> UnitmeasureRow) : this(toRow, mutableMapOf<UnitmeasureId, UnitmeasureRow>())

  fun deleteById(
    unitmeasurecode: UnitmeasureId,
    c: Connection
  ): Boolean = (map.remove(unitmeasurecode) != null)

  fun deleteByIds(
    unitmeasurecodes: Array<UnitmeasureId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : unitmeasurecodes) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: UnitmeasureRow,
    c: Connection
  ): UnitmeasureRow {
    if (map.containsKey(unsaved.unitmeasurecode)) {
        throw new RuntimeException(str("id $unsaved.unitmeasurecode already exists"))
      }
      map[unsaved.unitmeasurecode] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: UnitmeasureRowUnsaved,
    c: Connection
  ): UnitmeasureRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<UnitmeasureRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.unitmeasurecode] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<UnitmeasureRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.unitmeasurecode] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<UnitmeasureRow> = map.values.toList()

  fun selectById(
    unitmeasurecode: UnitmeasureId,
    c: Connection
  ): UnitmeasureRow? = map[unitmeasurecode]

  fun selectByIds(
    unitmeasurecodes: Array<UnitmeasureId>,
    c: Connection
  ): List<UnitmeasureRow> = unitmeasurecodes.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    unitmeasurecodes: Array<UnitmeasureId>,
    c: Connection
  ): Map<UnitmeasureId, UnitmeasureRow> = selectByIds(unitmeasurecodes, c).associateBy { { row -> row.unitmeasurecode }(it) }

  fun update(
    row: UnitmeasureRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.unitmeasurecode]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.unitmeasurecode] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: UnitmeasureRow,
    c: Connection
  ): UnitmeasureRow {
    map[unsaved.unitmeasurecode] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<UnitmeasureRow>,
    c: Connection
  ): List<UnitmeasureRow> {
    var result = ArrayList<UnitmeasureRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.unitmeasurecode] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<UnitmeasureRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.unitmeasurecode] = row
        count = count + 1
      }
    return count
  }
}