/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productmodelillustration

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class ProductmodelillustrationRepoMock(
  val toRow: (ProductmodelillustrationRowUnsaved) -> ProductmodelillustrationRow,
  val map: MutableMap<ProductmodelillustrationId, ProductmodelillustrationRow>
) : ProductmodelillustrationRepo {
  constructor(toRow: (ProductmodelillustrationRowUnsaved) -> ProductmodelillustrationRow) : this(toRow, mutableMapOf<ProductmodelillustrationId, ProductmodelillustrationRow>())

  fun deleteById(
    compositeId: ProductmodelillustrationId,
    c: Connection
  ): Boolean = (map.remove(compositeId) != null)

  fun deleteByIds(
    compositeIds: Array<ProductmodelillustrationId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : compositeIds) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: ProductmodelillustrationRow,
    c: Connection
  ): ProductmodelillustrationRow {
    if (map.containsKey(unsaved.compositeId)) {
        throw new RuntimeException(str("id $unsaved.compositeId already exists"))
      }
      map[unsaved.compositeId] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: ProductmodelillustrationRowUnsaved,
    c: Connection
  ): ProductmodelillustrationRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<ProductmodelillustrationRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<ProductmodelillustrationRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.compositeId] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<ProductmodelillustrationRow> = map.values.toList()

  fun selectById(
    compositeId: ProductmodelillustrationId,
    c: Connection
  ): ProductmodelillustrationRow? = map[compositeId]

  fun selectByIds(
    compositeIds: Array<ProductmodelillustrationId>,
    c: Connection
  ): List<ProductmodelillustrationRow> = compositeIds.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    compositeIds: Array<ProductmodelillustrationId>,
    c: Connection
  ): Map<ProductmodelillustrationId, ProductmodelillustrationRow> = selectByIds(compositeIds, c).associateBy { { row -> row.compositeId }(it) }

  fun update(
    row: ProductmodelillustrationRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.compositeId]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.compositeId] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: ProductmodelillustrationRow,
    c: Connection
  ): ProductmodelillustrationRow {
    map[unsaved.compositeId] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<ProductmodelillustrationRow>,
    c: Connection
  ): List<ProductmodelillustrationRow> {
    var result = ArrayList<ProductmodelillustrationRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<ProductmodelillustrationRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        count = count + 1
      }
    return count
  }
}