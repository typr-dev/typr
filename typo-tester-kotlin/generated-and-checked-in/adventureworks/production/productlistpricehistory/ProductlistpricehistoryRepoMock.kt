/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productlistpricehistory

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class ProductlistpricehistoryRepoMock(
  val toRow: (ProductlistpricehistoryRowUnsaved) -> ProductlistpricehistoryRow,
  val map: MutableMap<ProductlistpricehistoryId, ProductlistpricehistoryRow>
) : ProductlistpricehistoryRepo {
  constructor(toRow: (ProductlistpricehistoryRowUnsaved) -> ProductlistpricehistoryRow) : this(toRow, mutableMapOf<ProductlistpricehistoryId, ProductlistpricehistoryRow>())

  fun deleteById(
    compositeId: ProductlistpricehistoryId,
    c: Connection
  ): Boolean = (map.remove(compositeId) != null)

  fun deleteByIds(
    compositeIds: Array<ProductlistpricehistoryId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : compositeIds) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: ProductlistpricehistoryRow,
    c: Connection
  ): ProductlistpricehistoryRow {
    if (map.containsKey(unsaved.compositeId)) {
        throw new RuntimeException(str("id $unsaved.compositeId already exists"))
      }
      map[unsaved.compositeId] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: ProductlistpricehistoryRowUnsaved,
    c: Connection
  ): ProductlistpricehistoryRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<ProductlistpricehistoryRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<ProductlistpricehistoryRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.compositeId] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<ProductlistpricehistoryRow> = map.values.toList()

  fun selectById(
    compositeId: ProductlistpricehistoryId,
    c: Connection
  ): ProductlistpricehistoryRow? = map[compositeId]

  fun selectByIds(
    compositeIds: Array<ProductlistpricehistoryId>,
    c: Connection
  ): List<ProductlistpricehistoryRow> = compositeIds.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    compositeIds: Array<ProductlistpricehistoryId>,
    c: Connection
  ): Map<ProductlistpricehistoryId, ProductlistpricehistoryRow> = selectByIds(compositeIds, c).associateBy { { row -> row.compositeId }(it) }

  fun update(
    row: ProductlistpricehistoryRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.compositeId]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.compositeId] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: ProductlistpricehistoryRow,
    c: Connection
  ): ProductlistpricehistoryRow {
    map[unsaved.compositeId] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<ProductlistpricehistoryRow>,
    c: Connection
  ): List<ProductlistpricehistoryRow> {
    var result = ArrayList<ProductlistpricehistoryRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<ProductlistpricehistoryRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        count = count + 1
      }
    return count
  }
}