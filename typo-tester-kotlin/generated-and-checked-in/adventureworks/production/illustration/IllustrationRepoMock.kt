/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.illustration

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class IllustrationRepoMock(
  val toRow: (IllustrationRowUnsaved) -> IllustrationRow,
  val map: MutableMap<IllustrationId, IllustrationRow>
) : IllustrationRepo {
  constructor(toRow: (IllustrationRowUnsaved) -> IllustrationRow) : this(toRow, mutableMapOf<IllustrationId, IllustrationRow>())

  fun deleteById(
    illustrationid: IllustrationId,
    c: Connection
  ): Boolean = (map.remove(illustrationid) != null)

  fun deleteByIds(
    illustrationids: Array<IllustrationId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : illustrationids) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: IllustrationRow,
    c: Connection
  ): IllustrationRow {
    if (map.containsKey(unsaved.illustrationid)) {
        throw new RuntimeException(str("id $unsaved.illustrationid already exists"))
      }
      map[unsaved.illustrationid] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: IllustrationRowUnsaved,
    c: Connection
  ): IllustrationRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<IllustrationRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.illustrationid] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<IllustrationRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.illustrationid] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<IllustrationRow> = map.values.toList()

  fun selectById(
    illustrationid: IllustrationId,
    c: Connection
  ): IllustrationRow? = map[illustrationid]

  fun selectByIds(
    illustrationids: Array<IllustrationId>,
    c: Connection
  ): List<IllustrationRow> = illustrationids.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    illustrationids: Array<IllustrationId>,
    c: Connection
  ): Map<IllustrationId, IllustrationRow> = selectByIds(illustrationids, c).associateBy { { row -> row.illustrationid }(it) }

  fun update(
    row: IllustrationRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.illustrationid]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.illustrationid] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: IllustrationRow,
    c: Connection
  ): IllustrationRow {
    map[unsaved.illustrationid] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<IllustrationRow>,
    c: Connection
  ): List<IllustrationRow> {
    var result = ArrayList<IllustrationRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.illustrationid] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<IllustrationRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.illustrationid] = row
        count = count + 1
      }
    return count
  }
}