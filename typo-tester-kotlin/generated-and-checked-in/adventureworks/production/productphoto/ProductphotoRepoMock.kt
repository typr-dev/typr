/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productphoto

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class ProductphotoRepoMock(
  val toRow: (ProductphotoRowUnsaved) -> ProductphotoRow,
  val map: MutableMap<ProductphotoId, ProductphotoRow>
) : ProductphotoRepo {
  constructor(toRow: (ProductphotoRowUnsaved) -> ProductphotoRow) : this(toRow, mutableMapOf<ProductphotoId, ProductphotoRow>())

  fun deleteById(
    productphotoid: ProductphotoId,
    c: Connection
  ): Boolean = (map.remove(productphotoid) != null)

  fun deleteByIds(
    productphotoids: Array<ProductphotoId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : productphotoids) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: ProductphotoRow,
    c: Connection
  ): ProductphotoRow {
    if (map.containsKey(unsaved.productphotoid)) {
        throw new RuntimeException(str("id $unsaved.productphotoid already exists"))
      }
      map[unsaved.productphotoid] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: ProductphotoRowUnsaved,
    c: Connection
  ): ProductphotoRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<ProductphotoRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.productphotoid] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<ProductphotoRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.productphotoid] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<ProductphotoRow> = map.values.toList()

  fun selectById(
    productphotoid: ProductphotoId,
    c: Connection
  ): ProductphotoRow? = map[productphotoid]

  fun selectByIds(
    productphotoids: Array<ProductphotoId>,
    c: Connection
  ): List<ProductphotoRow> = productphotoids.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    productphotoids: Array<ProductphotoId>,
    c: Connection
  ): Map<ProductphotoId, ProductphotoRow> = selectByIds(productphotoids, c).associateBy { { row -> row.productphotoid }(it) }

  fun update(
    row: ProductphotoRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.productphotoid]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.productphotoid] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: ProductphotoRow,
    c: Connection
  ): ProductphotoRow {
    map[unsaved.productphotoid] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<ProductphotoRow>,
    c: Connection
  ): List<ProductphotoRow> {
    var result = ArrayList<ProductphotoRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.productphotoid] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<ProductphotoRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.productphotoid] = row
        count = count + 1
      }
    return count
  }
}