/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productdescription

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class ProductdescriptionRepoMock(
  val toRow: (ProductdescriptionRowUnsaved) -> ProductdescriptionRow,
  val map: MutableMap<ProductdescriptionId, ProductdescriptionRow>
) : ProductdescriptionRepo {
  constructor(toRow: (ProductdescriptionRowUnsaved) -> ProductdescriptionRow) : this(toRow, mutableMapOf<ProductdescriptionId, ProductdescriptionRow>())

  fun deleteById(
    productdescriptionid: ProductdescriptionId,
    c: Connection
  ): Boolean = (map.remove(productdescriptionid) != null)

  fun deleteByIds(
    productdescriptionids: Array<ProductdescriptionId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : productdescriptionids) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: ProductdescriptionRow,
    c: Connection
  ): ProductdescriptionRow {
    if (map.containsKey(unsaved.productdescriptionid)) {
        throw new RuntimeException(str("id $unsaved.productdescriptionid already exists"))
      }
      map[unsaved.productdescriptionid] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: ProductdescriptionRowUnsaved,
    c: Connection
  ): ProductdescriptionRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<ProductdescriptionRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.productdescriptionid] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<ProductdescriptionRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.productdescriptionid] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<ProductdescriptionRow> = map.values.toList()

  fun selectById(
    productdescriptionid: ProductdescriptionId,
    c: Connection
  ): ProductdescriptionRow? = map[productdescriptionid]

  fun selectByIds(
    productdescriptionids: Array<ProductdescriptionId>,
    c: Connection
  ): List<ProductdescriptionRow> = productdescriptionids.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    productdescriptionids: Array<ProductdescriptionId>,
    c: Connection
  ): Map<ProductdescriptionId, ProductdescriptionRow> = selectByIds(productdescriptionids, c).associateBy { { row -> row.productdescriptionid }(it) }

  fun update(
    row: ProductdescriptionRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.productdescriptionid]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.productdescriptionid] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: ProductdescriptionRow,
    c: Connection
  ): ProductdescriptionRow {
    map[unsaved.productdescriptionid] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<ProductdescriptionRow>,
    c: Connection
  ): List<ProductdescriptionRow> {
    var result = ArrayList<ProductdescriptionRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.productdescriptionid] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<ProductdescriptionRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.productdescriptionid] = row
        count = count + 1
      }
    return count
  }
}