/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.transactionhistoryarchive

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class TransactionhistoryarchiveRepoMock(
  val toRow: (TransactionhistoryarchiveRowUnsaved) -> TransactionhistoryarchiveRow,
  val map: MutableMap<TransactionhistoryarchiveId, TransactionhistoryarchiveRow>
) : TransactionhistoryarchiveRepo {
  constructor(toRow: (TransactionhistoryarchiveRowUnsaved) -> TransactionhistoryarchiveRow) : this(toRow, mutableMapOf<TransactionhistoryarchiveId, TransactionhistoryarchiveRow>())

  fun deleteById(
    transactionid: TransactionhistoryarchiveId,
    c: Connection
  ): Boolean = (map.remove(transactionid) != null)

  fun deleteByIds(
    transactionids: Array<TransactionhistoryarchiveId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : transactionids) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: TransactionhistoryarchiveRow,
    c: Connection
  ): TransactionhistoryarchiveRow {
    if (map.containsKey(unsaved.transactionid)) {
        throw new RuntimeException(str("id $unsaved.transactionid already exists"))
      }
      map[unsaved.transactionid] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: TransactionhistoryarchiveRowUnsaved,
    c: Connection
  ): TransactionhistoryarchiveRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<TransactionhistoryarchiveRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.transactionid] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<TransactionhistoryarchiveRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.transactionid] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<TransactionhistoryarchiveRow> = map.values.toList()

  fun selectById(
    transactionid: TransactionhistoryarchiveId,
    c: Connection
  ): TransactionhistoryarchiveRow? = map[transactionid]

  fun selectByIds(
    transactionids: Array<TransactionhistoryarchiveId>,
    c: Connection
  ): List<TransactionhistoryarchiveRow> = transactionids.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    transactionids: Array<TransactionhistoryarchiveId>,
    c: Connection
  ): Map<TransactionhistoryarchiveId, TransactionhistoryarchiveRow> = selectByIds(transactionids, c).associateBy { { row -> row.transactionid }(it) }

  fun update(
    row: TransactionhistoryarchiveRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.transactionid]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.transactionid] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: TransactionhistoryarchiveRow,
    c: Connection
  ): TransactionhistoryarchiveRow {
    map[unsaved.transactionid] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<TransactionhistoryarchiveRow>,
    c: Connection
  ): List<TransactionhistoryarchiveRow> {
    var result = ArrayList<TransactionhistoryarchiveRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.transactionid] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<TransactionhistoryarchiveRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.transactionid] = row
        count = count + 1
      }
    return count
  }
}