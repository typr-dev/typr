/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productmodelproductdescriptionculture

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class ProductmodelproductdescriptioncultureRepoMock(
  val toRow: (ProductmodelproductdescriptioncultureRowUnsaved) -> ProductmodelproductdescriptioncultureRow,
  val map: MutableMap<ProductmodelproductdescriptioncultureId, ProductmodelproductdescriptioncultureRow>
) : ProductmodelproductdescriptioncultureRepo {
  constructor(toRow: (ProductmodelproductdescriptioncultureRowUnsaved) -> ProductmodelproductdescriptioncultureRow) : this(toRow, mutableMapOf<ProductmodelproductdescriptioncultureId, ProductmodelproductdescriptioncultureRow>())

  fun deleteById(
    compositeId: ProductmodelproductdescriptioncultureId,
    c: Connection
  ): Boolean = (map.remove(compositeId) != null)

  fun deleteByIds(
    compositeIds: Array<ProductmodelproductdescriptioncultureId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : compositeIds) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: ProductmodelproductdescriptioncultureRow,
    c: Connection
  ): ProductmodelproductdescriptioncultureRow {
    if (map.containsKey(unsaved.compositeId)) {
        throw new RuntimeException(str("id $unsaved.compositeId already exists"))
      }
      map[unsaved.compositeId] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: ProductmodelproductdescriptioncultureRowUnsaved,
    c: Connection
  ): ProductmodelproductdescriptioncultureRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<ProductmodelproductdescriptioncultureRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<ProductmodelproductdescriptioncultureRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.compositeId] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<ProductmodelproductdescriptioncultureRow> = map.values.toList()

  fun selectById(
    compositeId: ProductmodelproductdescriptioncultureId,
    c: Connection
  ): ProductmodelproductdescriptioncultureRow? = map[compositeId]

  fun selectByIds(
    compositeIds: Array<ProductmodelproductdescriptioncultureId>,
    c: Connection
  ): List<ProductmodelproductdescriptioncultureRow> = compositeIds.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    compositeIds: Array<ProductmodelproductdescriptioncultureId>,
    c: Connection
  ): Map<ProductmodelproductdescriptioncultureId, ProductmodelproductdescriptioncultureRow> = selectByIds(compositeIds, c).associateBy { { row -> row.compositeId }(it) }

  fun update(
    row: ProductmodelproductdescriptioncultureRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.compositeId]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.compositeId] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: ProductmodelproductdescriptioncultureRow,
    c: Connection
  ): ProductmodelproductdescriptioncultureRow {
    map[unsaved.compositeId] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<ProductmodelproductdescriptioncultureRow>,
    c: Connection
  ): List<ProductmodelproductdescriptioncultureRow> {
    var result = ArrayList<ProductmodelproductdescriptioncultureRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<ProductmodelproductdescriptioncultureRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        count = count + 1
      }
    return count
  }
}