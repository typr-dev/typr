/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productproductphoto

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class ProductproductphotoRepoMock(
  val toRow: (ProductproductphotoRowUnsaved) -> ProductproductphotoRow,
  val map: MutableMap<ProductproductphotoId, ProductproductphotoRow>
) : ProductproductphotoRepo {
  constructor(toRow: (ProductproductphotoRowUnsaved) -> ProductproductphotoRow) : this(toRow, mutableMapOf<ProductproductphotoId, ProductproductphotoRow>())

  fun deleteById(
    compositeId: ProductproductphotoId,
    c: Connection
  ): Boolean = (map.remove(compositeId) != null)

  fun deleteByIds(
    compositeIds: Array<ProductproductphotoId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : compositeIds) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: ProductproductphotoRow,
    c: Connection
  ): ProductproductphotoRow {
    if (map.containsKey(unsaved.compositeId)) {
        throw new RuntimeException(str("id $unsaved.compositeId already exists"))
      }
      map[unsaved.compositeId] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: ProductproductphotoRowUnsaved,
    c: Connection
  ): ProductproductphotoRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<ProductproductphotoRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<ProductproductphotoRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.compositeId] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<ProductproductphotoRow> = map.values.toList()

  fun selectById(
    compositeId: ProductproductphotoId,
    c: Connection
  ): ProductproductphotoRow? = map[compositeId]

  fun selectByIds(
    compositeIds: Array<ProductproductphotoId>,
    c: Connection
  ): List<ProductproductphotoRow> = compositeIds.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    compositeIds: Array<ProductproductphotoId>,
    c: Connection
  ): Map<ProductproductphotoId, ProductproductphotoRow> = selectByIds(compositeIds, c).associateBy { { row -> row.compositeId }(it) }

  fun update(
    row: ProductproductphotoRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.compositeId]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.compositeId] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: ProductproductphotoRow,
    c: Connection
  ): ProductproductphotoRow {
    map[unsaved.compositeId] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<ProductproductphotoRow>,
    c: Connection
  ): List<ProductproductphotoRow> {
    var result = ArrayList<ProductproductphotoRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<ProductproductphotoRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        count = count + 1
      }
    return count
  }
}