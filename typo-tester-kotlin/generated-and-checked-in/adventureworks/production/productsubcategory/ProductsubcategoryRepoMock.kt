/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productsubcategory

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class ProductsubcategoryRepoMock(
  val toRow: (ProductsubcategoryRowUnsaved) -> ProductsubcategoryRow,
  val map: MutableMap<ProductsubcategoryId, ProductsubcategoryRow>
) : ProductsubcategoryRepo {
  constructor(toRow: (ProductsubcategoryRowUnsaved) -> ProductsubcategoryRow) : this(toRow, mutableMapOf<ProductsubcategoryId, ProductsubcategoryRow>())

  fun deleteById(
    productsubcategoryid: ProductsubcategoryId,
    c: Connection
  ): Boolean = (map.remove(productsubcategoryid) != null)

  fun deleteByIds(
    productsubcategoryids: Array<ProductsubcategoryId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : productsubcategoryids) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: ProductsubcategoryRow,
    c: Connection
  ): ProductsubcategoryRow {
    if (map.containsKey(unsaved.productsubcategoryid)) {
        throw new RuntimeException(str("id $unsaved.productsubcategoryid already exists"))
      }
      map[unsaved.productsubcategoryid] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: ProductsubcategoryRowUnsaved,
    c: Connection
  ): ProductsubcategoryRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<ProductsubcategoryRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.productsubcategoryid] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<ProductsubcategoryRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.productsubcategoryid] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<ProductsubcategoryRow> = map.values.toList()

  fun selectById(
    productsubcategoryid: ProductsubcategoryId,
    c: Connection
  ): ProductsubcategoryRow? = map[productsubcategoryid]

  fun selectByIds(
    productsubcategoryids: Array<ProductsubcategoryId>,
    c: Connection
  ): List<ProductsubcategoryRow> = productsubcategoryids.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    productsubcategoryids: Array<ProductsubcategoryId>,
    c: Connection
  ): Map<ProductsubcategoryId, ProductsubcategoryRow> = selectByIds(productsubcategoryids, c).associateBy { { row -> row.productsubcategoryid }(it) }

  fun update(
    row: ProductsubcategoryRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.productsubcategoryid]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.productsubcategoryid] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: ProductsubcategoryRow,
    c: Connection
  ): ProductsubcategoryRow {
    map[unsaved.productsubcategoryid] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<ProductsubcategoryRow>,
    c: Connection
  ): List<ProductsubcategoryRow> {
    var result = ArrayList<ProductsubcategoryRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.productsubcategoryid] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<ProductsubcategoryRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.productsubcategoryid] = row
        count = count + 1
      }
    return count
  }
}