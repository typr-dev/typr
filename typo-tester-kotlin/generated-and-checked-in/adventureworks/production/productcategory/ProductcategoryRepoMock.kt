/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productcategory

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class ProductcategoryRepoMock(
  val toRow: (ProductcategoryRowUnsaved) -> ProductcategoryRow,
  val map: MutableMap<ProductcategoryId, ProductcategoryRow>
) : ProductcategoryRepo {
  constructor(toRow: (ProductcategoryRowUnsaved) -> ProductcategoryRow) : this(toRow, mutableMapOf<ProductcategoryId, ProductcategoryRow>())

  fun deleteById(
    productcategoryid: ProductcategoryId,
    c: Connection
  ): Boolean = (map.remove(productcategoryid) != null)

  fun deleteByIds(
    productcategoryids: Array<ProductcategoryId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : productcategoryids) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: ProductcategoryRow,
    c: Connection
  ): ProductcategoryRow {
    if (map.containsKey(unsaved.productcategoryid)) {
        throw new RuntimeException(str("id $unsaved.productcategoryid already exists"))
      }
      map[unsaved.productcategoryid] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: ProductcategoryRowUnsaved,
    c: Connection
  ): ProductcategoryRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<ProductcategoryRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.productcategoryid] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<ProductcategoryRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.productcategoryid] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<ProductcategoryRow> = map.values.toList()

  fun selectById(
    productcategoryid: ProductcategoryId,
    c: Connection
  ): ProductcategoryRow? = map[productcategoryid]

  fun selectByIds(
    productcategoryids: Array<ProductcategoryId>,
    c: Connection
  ): List<ProductcategoryRow> = productcategoryids.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    productcategoryids: Array<ProductcategoryId>,
    c: Connection
  ): Map<ProductcategoryId, ProductcategoryRow> = selectByIds(productcategoryids, c).associateBy { { row -> row.productcategoryid }(it) }

  fun update(
    row: ProductcategoryRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.productcategoryid]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.productcategoryid] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: ProductcategoryRow,
    c: Connection
  ): ProductcategoryRow {
    map[unsaved.productcategoryid] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<ProductcategoryRow>,
    c: Connection
  ): List<ProductcategoryRow> {
    var result = ArrayList<ProductcategoryRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.productcategoryid] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<ProductcategoryRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.productcategoryid] = row
        count = count + 1
      }
    return count
  }
}