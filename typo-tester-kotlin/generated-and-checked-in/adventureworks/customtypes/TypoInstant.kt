/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.customtypes

import java.time.Instant
import java.time.OffsetDateTime
import java.time.format.DateTimeFormatter
import java.time.format.DateTimeFormatterBuilder
import java.time.temporal.ChronoField
import java.time.temporal.ChronoUnit
import typo.runtime.PgRead
import typo.runtime.PgText
import typo.runtime.PgType
import typo.runtime.PgTypes
import typo.runtime.PgWrite
import typo.runtime.internal.arrayMap

/** This is `java.time.TypoInstant`, but with microsecond precision and transferred to and from postgres as strings. The reason is that postgres driver and db libs are broken */
data class TypoInstant(val value: Instant) {
  companion object {
    fun apply(value: Instant): TypoInstant = new TypoInstant(value.truncatedTo(ChronoUnit.MICROS))

    fun apply(str: java.lang.String): TypoInstant = new TypoInstant(OffsetDateTime.parse(str, parser).toInstant())

    fun now(): TypoInstant = new TypoInstant(Instant.now())

    val parser: DateTimeFormatter = new DateTimeFormatterBuilder().appendPattern("yyyy-MM-dd HH:mm:ss").appendFraction(ChronoField.MICRO_OF_SECOND, 0, 6, true).appendPattern("X").toFormatter()

    val pgText: PgText<TypoInstant> =
      PgText.textString.contramap({ v -> v.value.toString })

    val pgType: PgType<TypoInstant> =
      PgTypes.text.bimap({ v -> apply(v) }, { v -> v.value.toString }).renamed("timestamptz")

    val pgTypeArray: PgType<Array<TypoInstant>> =
      TypoInstant.pgType.array(PgRead.massageJdbcArrayTo(Array<java.lang.String>::class.java).map({ xs -> arrayMap.map(xs, { v -> apply(v) }, TypoInstant::class.java) }), PgWrite.<java.lang.String>passObjectToJdbc().array(TypoInstant.pgType.typename().<java.lang.String>as()).contramap({ xs -> arrayMap.map(xs, (TypoInstant v) -> v.value.toString, java.lang.String::class.java) }))
  }
}