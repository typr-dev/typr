/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.customtypes

import org.postgresql.geometric.PGbox
import typo.runtime.PgRead
import typo.runtime.PgText
import typo.runtime.PgType
import typo.runtime.PgWrite
import typo.runtime.internal.arrayMap
import typo.runtime.internal.stringInterpolator.str

/** This represents the box datatype in PostgreSQL */
data class TypoBox(
  val x1: Double,
  val y1: Double,
  val x2: Double,
  val y2: Double
) {
  companion object {
    val pgText: PgText<TypoBox> =
      PgText.textString.contramap({ v -> str("((", v.x1, ",", v.y1, "),(", v.x2, ",", v.y2, "))") })

    val pgType: PgType<TypoBox> =
      PgType.of(
        "box",
        PgRead.castJdbcObjectTo(PGbox::class.java).map({ v -> TypoBox(v.point[0].x, v.point[0].y, v.point[1].x, v.point[1].y) }),
        PgWrite.passObjectToJdbc().contramap({ v -> PGbox(v.x1, v.y1, v.x2, v.y2) }),
        TypoBox.pgText
      )

    val pgTypeArray: PgType<Array<TypoBox>> =
      TypoBox.pgType.array(PgRead.castJdbcArrayTo(PGbox::class.java).map({ xs -> arrayMap.map(xs, { v -> TypoBox(v.point[0].x, v.point[0].y, v.point[1].x, v.point[1].y) }, TypoBox::class.java) }), PgWrite.<PGbox>passObjectToJdbc().array(TypoBox.pgType.typename().<PGbox>as()).contramap({ xs -> arrayMap.map(xs, (TypoBox v) -> PGbox(v.x1, v.y1, v.x2, v.y2), PGbox::class.java) }))
  }
}