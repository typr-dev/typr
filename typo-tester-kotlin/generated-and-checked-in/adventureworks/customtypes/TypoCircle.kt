/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.customtypes

import org.postgresql.geometric.PGcircle
import typo.runtime.PgRead
import typo.runtime.PgText
import typo.runtime.PgType
import typo.runtime.PgWrite
import typo.runtime.internal.arrayMap
import typo.runtime.internal.stringInterpolator.str

/** This represents circle datatype in PostgreSQL, consisting of a point and a radius */
data class TypoCircle(
  val center: TypoPoint,
  val radius: Double
) {
  companion object {
    val pgText: PgText<TypoCircle> =
      PgText.textString.contramap({ v -> str("<(", v.center.x, ",", v.center.y, "),", v.radius, ">") })

    val pgType: PgType<TypoCircle> =
      PgType.of(
        "circle",
        PgRead.castJdbcObjectTo(PGcircle::class.java).map({ v -> TypoCircle(TypoPoint(v.center.x, v.center.y), v.radius) }),
        PgWrite.passObjectToJdbc().contramap({ v -> PGcircle(v.center.x, v.center.y, v.radius) }),
        TypoCircle.pgText
      )

    val pgTypeArray: PgType<Array<TypoCircle>> =
      TypoCircle.pgType.array(PgRead.castJdbcArrayTo(PGcircle::class.java).map({ xs -> arrayMap.map(xs, { v -> TypoCircle(TypoPoint(v.center.x, v.center.y), v.radius) }, TypoCircle::class.java) }), PgWrite.<PGcircle>passObjectToJdbc().array(TypoCircle.pgType.typename().<PGcircle>as()).contramap({ xs -> arrayMap.map(xs, (TypoCircle v) -> PGcircle(v.center.x, v.center.y, v.radius), PGcircle::class.java) }))
  }
}