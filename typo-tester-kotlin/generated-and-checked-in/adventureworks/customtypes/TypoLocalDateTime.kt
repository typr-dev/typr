/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.customtypes

import java.time.LocalDateTime
import java.time.format.DateTimeFormatter
import java.time.format.DateTimeFormatterBuilder
import java.time.temporal.ChronoField
import java.time.temporal.ChronoUnit
import typo.runtime.PgRead
import typo.runtime.PgText
import typo.runtime.PgType
import typo.runtime.PgTypes
import typo.runtime.PgWrite
import typo.runtime.internal.arrayMap

/** This is `java.time.LocalDateTime`, but with microsecond precision and transferred to and from postgres as strings. The reason is that postgres driver and db libs are broken */
data class TypoLocalDateTime(val value: LocalDateTime) {
  companion object {
    fun apply(value: LocalDateTime): TypoLocalDateTime = new TypoLocalDateTime(value.truncatedTo(ChronoUnit.MICROS))

    fun apply(str: java.lang.String): TypoLocalDateTime = apply(LocalDateTime.parse(str, parser))

    fun now(): TypoLocalDateTime = new TypoLocalDateTime(LocalDateTime.now())

    val parser: DateTimeFormatter = new DateTimeFormatterBuilder().appendPattern("yyyy-MM-dd HH:mm:ss").appendFraction(ChronoField.MICRO_OF_SECOND, 0, 6, true).toFormatter()

    val pgText: PgText<TypoLocalDateTime> =
      PgText.textString.contramap({ v -> v.value.toString })

    val pgType: PgType<TypoLocalDateTime> =
      PgTypes.text.bimap({ v -> TypoLocalDateTime.apply(LocalDateTime.parse(v, parser)) }, { v -> v.value.toString }).renamed("timestamp")

    val pgTypeArray: PgType<Array<TypoLocalDateTime>> =
      TypoLocalDateTime.pgType.array(PgRead.massageJdbcArrayTo(Array<java.lang.String>::class.java).map({ xs -> arrayMap.map(xs, { v -> TypoLocalDateTime.apply(LocalDateTime.parse(v, parser)) }, TypoLocalDateTime::class.java) }), PgWrite.<java.lang.String>passObjectToJdbc().array(TypoLocalDateTime.pgType.typename().<java.lang.String>as()).contramap({ xs -> arrayMap.map(xs, (TypoLocalDateTime v) -> v.value.toString, java.lang.String::class.java) }))
  }
}