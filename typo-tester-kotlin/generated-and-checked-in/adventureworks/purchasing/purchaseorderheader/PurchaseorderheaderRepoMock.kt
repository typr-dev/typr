/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.purchasing.purchaseorderheader

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class PurchaseorderheaderRepoMock(
  val toRow: (PurchaseorderheaderRowUnsaved) -> PurchaseorderheaderRow,
  val map: MutableMap<PurchaseorderheaderId, PurchaseorderheaderRow>
) : PurchaseorderheaderRepo {
  constructor(toRow: (PurchaseorderheaderRowUnsaved) -> PurchaseorderheaderRow) : this(toRow, mutableMapOf<PurchaseorderheaderId, PurchaseorderheaderRow>())

  fun deleteById(
    purchaseorderid: PurchaseorderheaderId,
    c: Connection
  ): Boolean = (map.remove(purchaseorderid) != null)

  fun deleteByIds(
    purchaseorderids: Array<PurchaseorderheaderId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : purchaseorderids) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: PurchaseorderheaderRow,
    c: Connection
  ): PurchaseorderheaderRow {
    if (map.containsKey(unsaved.purchaseorderid)) {
        throw new RuntimeException(str("id $unsaved.purchaseorderid already exists"))
      }
      map[unsaved.purchaseorderid] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: PurchaseorderheaderRowUnsaved,
    c: Connection
  ): PurchaseorderheaderRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<PurchaseorderheaderRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.purchaseorderid] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<PurchaseorderheaderRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.purchaseorderid] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<PurchaseorderheaderRow> = map.values.toList()

  fun selectById(
    purchaseorderid: PurchaseorderheaderId,
    c: Connection
  ): PurchaseorderheaderRow? = map[purchaseorderid]

  fun selectByIds(
    purchaseorderids: Array<PurchaseorderheaderId>,
    c: Connection
  ): List<PurchaseorderheaderRow> = purchaseorderids.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    purchaseorderids: Array<PurchaseorderheaderId>,
    c: Connection
  ): Map<PurchaseorderheaderId, PurchaseorderheaderRow> = selectByIds(purchaseorderids, c).associateBy { { row -> row.purchaseorderid }(it) }

  fun update(
    row: PurchaseorderheaderRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.purchaseorderid]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.purchaseorderid] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: PurchaseorderheaderRow,
    c: Connection
  ): PurchaseorderheaderRow {
    map[unsaved.purchaseorderid] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<PurchaseorderheaderRow>,
    c: Connection
  ): List<PurchaseorderheaderRow> {
    var result = ArrayList<PurchaseorderheaderRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.purchaseorderid] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<PurchaseorderheaderRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.purchaseorderid] = row
        count = count + 1
      }
    return count
  }
}