/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.humanresources.jobcandidate

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class JobcandidateRepoMock(
  val toRow: (JobcandidateRowUnsaved) -> JobcandidateRow,
  val map: MutableMap<JobcandidateId, JobcandidateRow>
) : JobcandidateRepo {
  constructor(toRow: (JobcandidateRowUnsaved) -> JobcandidateRow) : this(toRow, mutableMapOf<JobcandidateId, JobcandidateRow>())

  fun deleteById(
    jobcandidateid: JobcandidateId,
    c: Connection
  ): Boolean = (map.remove(jobcandidateid) != null)

  fun deleteByIds(
    jobcandidateids: Array<JobcandidateId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : jobcandidateids) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: JobcandidateRow,
    c: Connection
  ): JobcandidateRow {
    if (map.containsKey(unsaved.jobcandidateid)) {
        throw new RuntimeException(str("id $unsaved.jobcandidateid already exists"))
      }
      map[unsaved.jobcandidateid] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: JobcandidateRowUnsaved,
    c: Connection
  ): JobcandidateRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<JobcandidateRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.jobcandidateid] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<JobcandidateRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.jobcandidateid] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<JobcandidateRow> = map.values.toList()

  fun selectById(
    jobcandidateid: JobcandidateId,
    c: Connection
  ): JobcandidateRow? = map[jobcandidateid]

  fun selectByIds(
    jobcandidateids: Array<JobcandidateId>,
    c: Connection
  ): List<JobcandidateRow> = jobcandidateids.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    jobcandidateids: Array<JobcandidateId>,
    c: Connection
  ): Map<JobcandidateId, JobcandidateRow> = selectByIds(jobcandidateids, c).associateBy { { row -> row.jobcandidateid }(it) }

  fun update(
    row: JobcandidateRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.jobcandidateid]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.jobcandidateid] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: JobcandidateRow,
    c: Connection
  ): JobcandidateRow {
    map[unsaved.jobcandidateid] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<JobcandidateRow>,
    c: Connection
  ): List<JobcandidateRow> {
    var result = ArrayList<JobcandidateRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.jobcandidateid] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<JobcandidateRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.jobcandidateid] = row
        count = count + 1
      }
    return count
  }
}