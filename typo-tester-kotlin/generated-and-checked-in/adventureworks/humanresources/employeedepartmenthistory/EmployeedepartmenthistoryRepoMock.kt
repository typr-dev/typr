/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.humanresources.employeedepartmenthistory

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class EmployeedepartmenthistoryRepoMock(
  val toRow: (EmployeedepartmenthistoryRowUnsaved) -> EmployeedepartmenthistoryRow,
  val map: MutableMap<EmployeedepartmenthistoryId, EmployeedepartmenthistoryRow>
) : EmployeedepartmenthistoryRepo {
  constructor(toRow: (EmployeedepartmenthistoryRowUnsaved) -> EmployeedepartmenthistoryRow) : this(toRow, mutableMapOf<EmployeedepartmenthistoryId, EmployeedepartmenthistoryRow>())

  fun deleteById(
    compositeId: EmployeedepartmenthistoryId,
    c: Connection
  ): Boolean = (map.remove(compositeId) != null)

  fun deleteByIds(
    compositeIds: Array<EmployeedepartmenthistoryId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : compositeIds) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: EmployeedepartmenthistoryRow,
    c: Connection
  ): EmployeedepartmenthistoryRow {
    if (map.containsKey(unsaved.compositeId)) {
        throw new RuntimeException(str("id $unsaved.compositeId already exists"))
      }
      map[unsaved.compositeId] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: EmployeedepartmenthistoryRowUnsaved,
    c: Connection
  ): EmployeedepartmenthistoryRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<EmployeedepartmenthistoryRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<EmployeedepartmenthistoryRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.compositeId] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<EmployeedepartmenthistoryRow> = map.values.toList()

  fun selectById(
    compositeId: EmployeedepartmenthistoryId,
    c: Connection
  ): EmployeedepartmenthistoryRow? = map[compositeId]

  fun selectByIds(
    compositeIds: Array<EmployeedepartmenthistoryId>,
    c: Connection
  ): List<EmployeedepartmenthistoryRow> = compositeIds.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    compositeIds: Array<EmployeedepartmenthistoryId>,
    c: Connection
  ): Map<EmployeedepartmenthistoryId, EmployeedepartmenthistoryRow> = selectByIds(compositeIds, c).associateBy { { row -> row.compositeId }(it) }

  fun update(
    row: EmployeedepartmenthistoryRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.compositeId]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.compositeId] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: EmployeedepartmenthistoryRow,
    c: Connection
  ): EmployeedepartmenthistoryRow {
    map[unsaved.compositeId] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<EmployeedepartmenthistoryRow>,
    c: Connection
  ): List<EmployeedepartmenthistoryRow> {
    var result = ArrayList<EmployeedepartmenthistoryRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<EmployeedepartmenthistoryRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        count = count + 1
      }
    return count
  }
}