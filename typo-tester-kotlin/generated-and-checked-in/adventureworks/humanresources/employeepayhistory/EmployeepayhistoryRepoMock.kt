/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.humanresources.employeepayhistory

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class EmployeepayhistoryRepoMock(
  val toRow: (EmployeepayhistoryRowUnsaved) -> EmployeepayhistoryRow,
  val map: MutableMap<EmployeepayhistoryId, EmployeepayhistoryRow>
) : EmployeepayhistoryRepo {
  constructor(toRow: (EmployeepayhistoryRowUnsaved) -> EmployeepayhistoryRow) : this(toRow, mutableMapOf<EmployeepayhistoryId, EmployeepayhistoryRow>())

  fun deleteById(
    compositeId: EmployeepayhistoryId,
    c: Connection
  ): Boolean = (map.remove(compositeId) != null)

  fun deleteByIds(
    compositeIds: Array<EmployeepayhistoryId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : compositeIds) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: EmployeepayhistoryRow,
    c: Connection
  ): EmployeepayhistoryRow {
    if (map.containsKey(unsaved.compositeId)) {
        throw new RuntimeException(str("id $unsaved.compositeId already exists"))
      }
      map[unsaved.compositeId] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: EmployeepayhistoryRowUnsaved,
    c: Connection
  ): EmployeepayhistoryRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<EmployeepayhistoryRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<EmployeepayhistoryRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.compositeId] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<EmployeepayhistoryRow> = map.values.toList()

  fun selectById(
    compositeId: EmployeepayhistoryId,
    c: Connection
  ): EmployeepayhistoryRow? = map[compositeId]

  fun selectByIds(
    compositeIds: Array<EmployeepayhistoryId>,
    c: Connection
  ): List<EmployeepayhistoryRow> = compositeIds.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    compositeIds: Array<EmployeepayhistoryId>,
    c: Connection
  ): Map<EmployeepayhistoryId, EmployeepayhistoryRow> = selectByIds(compositeIds, c).associateBy { { row -> row.compositeId }(it) }

  fun update(
    row: EmployeepayhistoryRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.compositeId]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.compositeId] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: EmployeepayhistoryRow,
    c: Connection
  ): EmployeepayhistoryRow {
    map[unsaved.compositeId] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<EmployeepayhistoryRow>,
    c: Connection
  ): List<EmployeepayhistoryRow> {
    var result = ArrayList<EmployeepayhistoryRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<EmployeepayhistoryRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        count = count + 1
      }
    return count
  }
}