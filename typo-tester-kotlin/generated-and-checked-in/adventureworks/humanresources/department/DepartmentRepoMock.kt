/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.humanresources.department

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class DepartmentRepoMock(
  val toRow: (DepartmentRowUnsaved) -> DepartmentRow,
  val map: MutableMap<DepartmentId, DepartmentRow>
) : DepartmentRepo {
  constructor(toRow: (DepartmentRowUnsaved) -> DepartmentRow) : this(toRow, mutableMapOf<DepartmentId, DepartmentRow>())

  fun deleteById(
    departmentid: DepartmentId,
    c: Connection
  ): Boolean = (map.remove(departmentid) != null)

  fun deleteByIds(
    departmentids: Array<DepartmentId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : departmentids) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: DepartmentRow,
    c: Connection
  ): DepartmentRow {
    if (map.containsKey(unsaved.departmentid)) {
        throw new RuntimeException(str("id $unsaved.departmentid already exists"))
      }
      map[unsaved.departmentid] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: DepartmentRowUnsaved,
    c: Connection
  ): DepartmentRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<DepartmentRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.departmentid] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<DepartmentRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.departmentid] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<DepartmentRow> = map.values.toList()

  fun selectById(
    departmentid: DepartmentId,
    c: Connection
  ): DepartmentRow? = map[departmentid]

  fun selectByIds(
    departmentids: Array<DepartmentId>,
    c: Connection
  ): List<DepartmentRow> = departmentids.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    departmentids: Array<DepartmentId>,
    c: Connection
  ): Map<DepartmentId, DepartmentRow> = selectByIds(departmentids, c).associateBy { { row -> row.departmentid }(it) }

  fun update(
    row: DepartmentRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.departmentid]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.departmentid] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: DepartmentRow,
    c: Connection
  ): DepartmentRow {
    map[unsaved.departmentid] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<DepartmentRow>,
    c: Connection
  ): List<DepartmentRow> {
    var result = ArrayList<DepartmentRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.departmentid] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<DepartmentRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.departmentid] = row
        count = count + 1
      }
    return count
  }
}