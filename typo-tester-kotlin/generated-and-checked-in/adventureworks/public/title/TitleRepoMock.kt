/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.title

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class TitleRepoMock(val map: MutableMap<TitleId, TitleRow>) : TitleRepo {
  fun deleteById(
    code: TitleId,
    c: Connection
  ): Boolean = (map.remove(code) != null)

  fun deleteByIds(
    codes: Array<TitleId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : codes) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: TitleRow,
    c: Connection
  ): TitleRow {
    if (map.containsKey(unsaved.code)) {
        throw new RuntimeException(str("id $unsaved.code already exists"))
      }
      map[unsaved.code] = unsaved
    return unsaved
  }

  fun insertStreaming(
    unsaved: Iterator<TitleRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.code] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<TitleRow> = map.values.toList()

  fun selectById(
    code: TitleId,
    c: Connection
  ): TitleRow? = map[code]

  fun selectByIds(
    codes: Array<TitleId>,
    c: Connection
  ): List<TitleRow> = codes.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    codes: Array<TitleId>,
    c: Connection
  ): Map<TitleId, TitleRow> = selectByIds(codes, c).associateBy { { row -> row.code }(it) }

  fun upsert(
    unsaved: TitleRow,
    c: Connection
  ): TitleRow {
    map[unsaved.code] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<TitleRow>,
    c: Connection
  ): List<TitleRow> {
    var result = ArrayList<TitleRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.code] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<TitleRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.code] = row
        count = count + 1
      }
    return count
  }
}