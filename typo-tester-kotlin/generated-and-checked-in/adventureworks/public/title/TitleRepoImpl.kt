/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.title

import java.sql.Connection
import java.util.HashMap
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import typo.runtime.streamingInsert
import typo.runtime.Fragment.interpolate
import typo.runtime.internal.stringInterpolator.str

data class TitleRepoImpl() : TitleRepo {
  fun deleteById(
    code: TitleId,
    c: Connection
  ): Boolean = interpolate(
    typo.runtime.Fragment.lit("""
    delete from "public"."title" where "code" = 
    """.trimMargin()),
    TitleId.pgType.encode(code),
    typo.runtime.Fragment.lit("")
  ).update().runUnchecked(c) > 0

  fun deleteByIds(
    codes: Array<TitleId>,
    c: Connection
  ): Int = interpolate(
             typo.runtime.Fragment.lit("""
               delete
               from "public"."title"
               where "code" = ANY(""".trimMargin()),
             TitleId.pgTypeArray.encode(codes),
             typo.runtime.Fragment.lit(")")
           )
    .update()
    .runUnchecked(c)

  fun insert(
    unsaved: TitleRow,
    c: Connection
  ): TitleRow = interpolate(
    typo.runtime.Fragment.lit("""
      insert into "public"."title"("code")
      values (""".trimMargin()),
    TitleId.pgType.encode(unsaved.code),
    typo.runtime.Fragment.lit("""
      )
      returning "code"
    """.trimMargin())
  )
    .updateReturning(TitleRow._rowParser.exactlyOne()).runUnchecked(c)

  fun insertStreaming(
    unsaved: Iterator<TitleRow>,
    batchSize: Int,
    c: Connection
  ): Long = streamingInsert.insertUnchecked(str("""
  COPY "public"."title"("code") FROM STDIN
  """.trimMargin()), batchSize, unsaved, c, TitleRow.pgText)

  fun selectAll(c: Connection): List<TitleRow> = interpolate(typo.runtime.Fragment.lit("""
    select "code"
    from "public"."title"
  """.trimMargin())).as(TitleRow._rowParser.all()).runUnchecked(c)

  fun selectById(
    code: TitleId,
    c: Connection
  ): TitleRow? = interpolate(
    typo.runtime.Fragment.lit("""
      select "code"
      from "public"."title"
      where "code" = """.trimMargin()),
    TitleId.pgType.encode(code),
    typo.runtime.Fragment.lit("")
  ).as(TitleRow._rowParser.first()).runUnchecked(c)

  fun selectByIds(
    codes: Array<TitleId>,
    c: Connection
  ): List<TitleRow> = interpolate(
    typo.runtime.Fragment.lit("""
      select "code"
      from "public"."title"
      where "code" = ANY(""".trimMargin()),
    TitleId.pgTypeArray.encode(codes),
    typo.runtime.Fragment.lit(")")
  ).as(TitleRow._rowParser.all()).runUnchecked(c)

  fun selectByIdsTracked(
    codes: Array<TitleId>,
    c: Connection
  ): Map<TitleId, TitleRow> {
    val ret: java.util.Map<TitleId, TitleRow> = HashMap()
      for (var row : selectByIds(codes, c)) {
          ret.put(row.id(), row);
      }
    return ret
  }

  fun upsert(
    unsaved: TitleRow,
    c: Connection
  ): TitleRow = interpolate(
    typo.runtime.Fragment.lit("""
      insert into "public"."title"("code")
      values (""".trimMargin()),
    TitleId.pgType.encode(unsaved.code),
    typo.runtime.Fragment.lit("""
      )
      on conflict ("code")
      do update set "code" = EXCLUDED."code"
      returning "code"
    """.trimMargin())
  )
    .updateReturning(TitleRow._rowParser.exactlyOne())
    .runUnchecked(c)

  fun upsertBatch(
    unsaved: Iterator<TitleRow>,
    c: Connection
  ): List<TitleRow> = interpolate(typo.runtime.Fragment.lit("""
                        insert into "public"."title"("code")
                        values (?)
                        on conflict ("code")
                        do nothing
                        returning "code"
                      """.trimMargin()))
    .updateManyReturning(TitleRow._rowParser, unsaved)
    .runUnchecked(c)

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<TitleRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    interpolate(typo.runtime.Fragment.lit("""
      create temporary table title_TEMP (like "public"."title") on commit drop
      """.trimMargin())).update().runUnchecked(c)
      streamingInsert.insertUnchecked(str("""
      copy title_TEMP("code") from stdin
      """.trimMargin()), batchSize, unsaved, c, TitleRow.pgText)
    return interpolate(typo.runtime.Fragment.lit("""
             insert into "public"."title"("code")
             select * from title_TEMP
             on conflict ("code")
             do nothing
             ;
             drop table title_TEMP;""".trimMargin())).update().runUnchecked(c)
  }
}