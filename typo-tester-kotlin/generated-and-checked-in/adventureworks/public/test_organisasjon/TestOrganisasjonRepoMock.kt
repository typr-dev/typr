/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.test_organisasjon

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class TestOrganisasjonRepoMock(val map: MutableMap<TestOrganisasjonId, TestOrganisasjonRow>) : TestOrganisasjonRepo {
  fun deleteById(
    organisasjonskode: TestOrganisasjonId,
    c: Connection
  ): Boolean = (map.remove(organisasjonskode) != null)

  fun deleteByIds(
    organisasjonskodes: Array<TestOrganisasjonId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : organisasjonskodes) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: TestOrganisasjonRow,
    c: Connection
  ): TestOrganisasjonRow {
    if (map.containsKey(unsaved.organisasjonskode)) {
        throw new RuntimeException(str("id $unsaved.organisasjonskode already exists"))
      }
      map[unsaved.organisasjonskode] = unsaved
    return unsaved
  }

  fun insertStreaming(
    unsaved: Iterator<TestOrganisasjonRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.organisasjonskode] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<TestOrganisasjonRow> = map.values.toList()

  fun selectById(
    organisasjonskode: TestOrganisasjonId,
    c: Connection
  ): TestOrganisasjonRow? = map[organisasjonskode]

  fun selectByIds(
    organisasjonskodes: Array<TestOrganisasjonId>,
    c: Connection
  ): List<TestOrganisasjonRow> = organisasjonskodes.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    organisasjonskodes: Array<TestOrganisasjonId>,
    c: Connection
  ): Map<TestOrganisasjonId, TestOrganisasjonRow> = selectByIds(organisasjonskodes, c).associateBy { { row -> row.organisasjonskode }(it) }

  fun upsert(
    unsaved: TestOrganisasjonRow,
    c: Connection
  ): TestOrganisasjonRow {
    map[unsaved.organisasjonskode] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<TestOrganisasjonRow>,
    c: Connection
  ): List<TestOrganisasjonRow> {
    var result = ArrayList<TestOrganisasjonRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.organisasjonskode] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<TestOrganisasjonRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.organisasjonskode] = row
        count = count + 1
      }
    return count
  }
}