/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.title_domain

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class TitleDomainRepoMock(val map: MutableMap<TitleDomainId, TitleDomainRow>) : TitleDomainRepo {
  fun deleteById(
    code: TitleDomainId,
    c: Connection
  ): Boolean = (map.remove(code) != null)

  fun deleteByIds(
    codes: Array<TitleDomainId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : codes) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: TitleDomainRow,
    c: Connection
  ): TitleDomainRow {
    if (map.containsKey(unsaved.code)) {
        throw new RuntimeException(str("id $unsaved.code already exists"))
      }
      map[unsaved.code] = unsaved
    return unsaved
  }

  fun insertStreaming(
    unsaved: Iterator<TitleDomainRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.code] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<TitleDomainRow> = map.values.toList()

  fun selectById(
    code: TitleDomainId,
    c: Connection
  ): TitleDomainRow? = map[code]

  fun selectByIds(
    codes: Array<TitleDomainId>,
    c: Connection
  ): List<TitleDomainRow> = codes.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    codes: Array<TitleDomainId>,
    c: Connection
  ): Map<TitleDomainId, TitleDomainRow> = selectByIds(codes, c).associateBy { { row -> row.code }(it) }

  fun upsert(
    unsaved: TitleDomainRow,
    c: Connection
  ): TitleDomainRow {
    map[unsaved.code] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<TitleDomainRow>,
    c: Connection
  ): List<TitleDomainRow> {
    var result = ArrayList<TitleDomainRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.code] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<TitleDomainRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.code] = row
        count = count + 1
      }
    return count
  }
}