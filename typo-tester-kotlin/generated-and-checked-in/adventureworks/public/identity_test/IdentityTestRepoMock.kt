/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.identity_test

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class IdentityTestRepoMock(
  val toRow: (IdentityTestRowUnsaved) -> IdentityTestRow,
  val map: MutableMap<IdentityTestId, IdentityTestRow>
) : IdentityTestRepo {
  constructor(toRow: (IdentityTestRowUnsaved) -> IdentityTestRow) : this(toRow, mutableMapOf<IdentityTestId, IdentityTestRow>())

  fun deleteById(
    name: IdentityTestId,
    c: Connection
  ): Boolean = (map.remove(name) != null)

  fun deleteByIds(
    names: Array<IdentityTestId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : names) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: IdentityTestRow,
    c: Connection
  ): IdentityTestRow {
    if (map.containsKey(unsaved.name)) {
        throw new RuntimeException(str("id $unsaved.name already exists"))
      }
      map[unsaved.name] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: IdentityTestRowUnsaved,
    c: Connection
  ): IdentityTestRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<IdentityTestRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.name] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<IdentityTestRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.name] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<IdentityTestRow> = map.values.toList()

  fun selectById(
    name: IdentityTestId,
    c: Connection
  ): IdentityTestRow? = map[name]

  fun selectByIds(
    names: Array<IdentityTestId>,
    c: Connection
  ): List<IdentityTestRow> = names.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    names: Array<IdentityTestId>,
    c: Connection
  ): Map<IdentityTestId, IdentityTestRow> = selectByIds(names, c).associateBy { { row -> row.name }(it) }

  fun update(
    row: IdentityTestRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.name]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.name] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: IdentityTestRow,
    c: Connection
  ): IdentityTestRow {
    map[unsaved.name] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<IdentityTestRow>,
    c: Connection
  ): List<IdentityTestRow> {
    var result = ArrayList<IdentityTestRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.name] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<IdentityTestRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.name] = row
        count = count + 1
      }
    return count
  }
}