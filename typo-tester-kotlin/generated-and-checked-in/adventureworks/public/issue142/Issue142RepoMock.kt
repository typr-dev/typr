/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.issue142

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class Issue142RepoMock(val map: MutableMap<Issue142Id, Issue142Row>) : Issue142Repo {
  fun deleteById(
    tabellkode: Issue142Id,
    c: Connection
  ): Boolean = (map.remove(tabellkode) != null)

  fun deleteByIds(
    tabellkodes: Array<Issue142Id>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : tabellkodes) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: Issue142Row,
    c: Connection
  ): Issue142Row {
    if (map.containsKey(unsaved.tabellkode)) {
        throw new RuntimeException(str("id $unsaved.tabellkode already exists"))
      }
      map[unsaved.tabellkode] = unsaved
    return unsaved
  }

  fun insertStreaming(
    unsaved: Iterator<Issue142Row>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.tabellkode] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<Issue142Row> = map.values.toList()

  fun selectById(
    tabellkode: Issue142Id,
    c: Connection
  ): Issue142Row? = map[tabellkode]

  fun selectByIds(
    tabellkodes: Array<Issue142Id>,
    c: Connection
  ): List<Issue142Row> = tabellkodes.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    tabellkodes: Array<Issue142Id>,
    c: Connection
  ): Map<Issue142Id, Issue142Row> = selectByIds(tabellkodes, c).associateBy { { row -> row.tabellkode }(it) }

  fun upsert(
    unsaved: Issue142Row,
    c: Connection
  ): Issue142Row {
    map[unsaved.tabellkode] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<Issue142Row>,
    c: Connection
  ): List<Issue142Row> {
    var result = ArrayList<Issue142Row>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.tabellkode] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<Issue142Row>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.tabellkode] = row
        count = count + 1
      }
    return count
  }
}