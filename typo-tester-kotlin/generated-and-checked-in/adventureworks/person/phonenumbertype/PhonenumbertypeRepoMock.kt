/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.phonenumbertype

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class PhonenumbertypeRepoMock(
  val toRow: (PhonenumbertypeRowUnsaved) -> PhonenumbertypeRow,
  val map: MutableMap<PhonenumbertypeId, PhonenumbertypeRow>
) : PhonenumbertypeRepo {
  constructor(toRow: (PhonenumbertypeRowUnsaved) -> PhonenumbertypeRow) : this(toRow, mutableMapOf<PhonenumbertypeId, PhonenumbertypeRow>())

  fun deleteById(
    phonenumbertypeid: PhonenumbertypeId,
    c: Connection
  ): Boolean = (map.remove(phonenumbertypeid) != null)

  fun deleteByIds(
    phonenumbertypeids: Array<PhonenumbertypeId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : phonenumbertypeids) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: PhonenumbertypeRow,
    c: Connection
  ): PhonenumbertypeRow {
    if (map.containsKey(unsaved.phonenumbertypeid)) {
        throw new RuntimeException(str("id $unsaved.phonenumbertypeid already exists"))
      }
      map[unsaved.phonenumbertypeid] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: PhonenumbertypeRowUnsaved,
    c: Connection
  ): PhonenumbertypeRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<PhonenumbertypeRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.phonenumbertypeid] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<PhonenumbertypeRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.phonenumbertypeid] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<PhonenumbertypeRow> = map.values.toList()

  fun selectById(
    phonenumbertypeid: PhonenumbertypeId,
    c: Connection
  ): PhonenumbertypeRow? = map[phonenumbertypeid]

  fun selectByIds(
    phonenumbertypeids: Array<PhonenumbertypeId>,
    c: Connection
  ): List<PhonenumbertypeRow> = phonenumbertypeids.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    phonenumbertypeids: Array<PhonenumbertypeId>,
    c: Connection
  ): Map<PhonenumbertypeId, PhonenumbertypeRow> = selectByIds(phonenumbertypeids, c).associateBy { { row -> row.phonenumbertypeid }(it) }

  fun update(
    row: PhonenumbertypeRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.phonenumbertypeid]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.phonenumbertypeid] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: PhonenumbertypeRow,
    c: Connection
  ): PhonenumbertypeRow {
    map[unsaved.phonenumbertypeid] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<PhonenumbertypeRow>,
    c: Connection
  ): List<PhonenumbertypeRow> {
    var result = ArrayList<PhonenumbertypeRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.phonenumbertypeid] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<PhonenumbertypeRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.phonenumbertypeid] = row
        count = count + 1
      }
    return count
  }
}