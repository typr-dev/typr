/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.addresstype

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class AddresstypeRepoMock(
  val toRow: (AddresstypeRowUnsaved) -> AddresstypeRow,
  val map: MutableMap<AddresstypeId, AddresstypeRow>
) : AddresstypeRepo {
  constructor(toRow: (AddresstypeRowUnsaved) -> AddresstypeRow) : this(toRow, mutableMapOf<AddresstypeId, AddresstypeRow>())

  fun deleteById(
    addresstypeid: AddresstypeId,
    c: Connection
  ): Boolean = (map.remove(addresstypeid) != null)

  fun deleteByIds(
    addresstypeids: Array<AddresstypeId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : addresstypeids) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: AddresstypeRow,
    c: Connection
  ): AddresstypeRow {
    if (map.containsKey(unsaved.addresstypeid)) {
        throw new RuntimeException(str("id $unsaved.addresstypeid already exists"))
      }
      map[unsaved.addresstypeid] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: AddresstypeRowUnsaved,
    c: Connection
  ): AddresstypeRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<AddresstypeRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.addresstypeid] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<AddresstypeRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.addresstypeid] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<AddresstypeRow> = map.values.toList()

  fun selectById(
    addresstypeid: AddresstypeId,
    c: Connection
  ): AddresstypeRow? = map[addresstypeid]

  fun selectByIds(
    addresstypeids: Array<AddresstypeId>,
    c: Connection
  ): List<AddresstypeRow> = addresstypeids.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    addresstypeids: Array<AddresstypeId>,
    c: Connection
  ): Map<AddresstypeId, AddresstypeRow> = selectByIds(addresstypeids, c).associateBy { { row -> row.addresstypeid }(it) }

  fun update(
    row: AddresstypeRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.addresstypeid]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.addresstypeid] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: AddresstypeRow,
    c: Connection
  ): AddresstypeRow {
    map[unsaved.addresstypeid] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<AddresstypeRow>,
    c: Connection
  ): List<AddresstypeRow> {
    var result = ArrayList<AddresstypeRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.addresstypeid] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<AddresstypeRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.addresstypeid] = row
        count = count + 1
      }
    return count
  }
}