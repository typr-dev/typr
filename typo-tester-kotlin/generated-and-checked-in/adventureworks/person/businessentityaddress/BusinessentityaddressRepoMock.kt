/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.businessentityaddress

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class BusinessentityaddressRepoMock(
  val toRow: (BusinessentityaddressRowUnsaved) -> BusinessentityaddressRow,
  val map: MutableMap<BusinessentityaddressId, BusinessentityaddressRow>
) : BusinessentityaddressRepo {
  constructor(toRow: (BusinessentityaddressRowUnsaved) -> BusinessentityaddressRow) : this(toRow, mutableMapOf<BusinessentityaddressId, BusinessentityaddressRow>())

  fun deleteById(
    compositeId: BusinessentityaddressId,
    c: Connection
  ): Boolean = (map.remove(compositeId) != null)

  fun deleteByIds(
    compositeIds: Array<BusinessentityaddressId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : compositeIds) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: BusinessentityaddressRow,
    c: Connection
  ): BusinessentityaddressRow {
    if (map.containsKey(unsaved.compositeId)) {
        throw new RuntimeException(str("id $unsaved.compositeId already exists"))
      }
      map[unsaved.compositeId] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: BusinessentityaddressRowUnsaved,
    c: Connection
  ): BusinessentityaddressRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<BusinessentityaddressRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<BusinessentityaddressRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.compositeId] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<BusinessentityaddressRow> = map.values.toList()

  fun selectById(
    compositeId: BusinessentityaddressId,
    c: Connection
  ): BusinessentityaddressRow? = map[compositeId]

  fun selectByIds(
    compositeIds: Array<BusinessentityaddressId>,
    c: Connection
  ): List<BusinessentityaddressRow> = compositeIds.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    compositeIds: Array<BusinessentityaddressId>,
    c: Connection
  ): Map<BusinessentityaddressId, BusinessentityaddressRow> = selectByIds(compositeIds, c).associateBy { { row -> row.compositeId }(it) }

  fun update(
    row: BusinessentityaddressRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.compositeId]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.compositeId] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: BusinessentityaddressRow,
    c: Connection
  ): BusinessentityaddressRow {
    map[unsaved.compositeId] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<BusinessentityaddressRow>,
    c: Connection
  ): List<BusinessentityaddressRow> {
    var result = ArrayList<BusinessentityaddressRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<BusinessentityaddressRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        count = count + 1
      }
    return count
  }
}