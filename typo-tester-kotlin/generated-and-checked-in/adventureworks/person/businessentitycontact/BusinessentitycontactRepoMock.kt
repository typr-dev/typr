/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.businessentitycontact

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class BusinessentitycontactRepoMock(
  val toRow: (BusinessentitycontactRowUnsaved) -> BusinessentitycontactRow,
  val map: MutableMap<BusinessentitycontactId, BusinessentitycontactRow>
) : BusinessentitycontactRepo {
  constructor(toRow: (BusinessentitycontactRowUnsaved) -> BusinessentitycontactRow) : this(toRow, mutableMapOf<BusinessentitycontactId, BusinessentitycontactRow>())

  fun deleteById(
    compositeId: BusinessentitycontactId,
    c: Connection
  ): Boolean = (map.remove(compositeId) != null)

  fun deleteByIds(
    compositeIds: Array<BusinessentitycontactId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : compositeIds) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: BusinessentitycontactRow,
    c: Connection
  ): BusinessentitycontactRow {
    if (map.containsKey(unsaved.compositeId)) {
        throw new RuntimeException(str("id $unsaved.compositeId already exists"))
      }
      map[unsaved.compositeId] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: BusinessentitycontactRowUnsaved,
    c: Connection
  ): BusinessentitycontactRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<BusinessentitycontactRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<BusinessentitycontactRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.compositeId] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<BusinessentitycontactRow> = map.values.toList()

  fun selectById(
    compositeId: BusinessentitycontactId,
    c: Connection
  ): BusinessentitycontactRow? = map[compositeId]

  fun selectByIds(
    compositeIds: Array<BusinessentitycontactId>,
    c: Connection
  ): List<BusinessentitycontactRow> = compositeIds.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    compositeIds: Array<BusinessentitycontactId>,
    c: Connection
  ): Map<BusinessentitycontactId, BusinessentitycontactRow> = selectByIds(compositeIds, c).associateBy { { row -> row.compositeId }(it) }

  fun update(
    row: BusinessentitycontactRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.compositeId]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.compositeId] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: BusinessentitycontactRow,
    c: Connection
  ): BusinessentitycontactRow {
    map[unsaved.compositeId] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<BusinessentitycontactRow>,
    c: Connection
  ): List<BusinessentitycontactRow> {
    var result = ArrayList<BusinessentitycontactRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<BusinessentitycontactRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        count = count + 1
      }
    return count
  }
}