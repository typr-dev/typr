/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.contacttype

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class ContacttypeRepoMock(
  val toRow: (ContacttypeRowUnsaved) -> ContacttypeRow,
  val map: MutableMap<ContacttypeId, ContacttypeRow>
) : ContacttypeRepo {
  constructor(toRow: (ContacttypeRowUnsaved) -> ContacttypeRow) : this(toRow, mutableMapOf<ContacttypeId, ContacttypeRow>())

  fun deleteById(
    contacttypeid: ContacttypeId,
    c: Connection
  ): Boolean = (map.remove(contacttypeid) != null)

  fun deleteByIds(
    contacttypeids: Array<ContacttypeId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : contacttypeids) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: ContacttypeRow,
    c: Connection
  ): ContacttypeRow {
    if (map.containsKey(unsaved.contacttypeid)) {
        throw new RuntimeException(str("id $unsaved.contacttypeid already exists"))
      }
      map[unsaved.contacttypeid] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: ContacttypeRowUnsaved,
    c: Connection
  ): ContacttypeRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<ContacttypeRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.contacttypeid] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<ContacttypeRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.contacttypeid] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<ContacttypeRow> = map.values.toList()

  fun selectById(
    contacttypeid: ContacttypeId,
    c: Connection
  ): ContacttypeRow? = map[contacttypeid]

  fun selectByIds(
    contacttypeids: Array<ContacttypeId>,
    c: Connection
  ): List<ContacttypeRow> = contacttypeids.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    contacttypeids: Array<ContacttypeId>,
    c: Connection
  ): Map<ContacttypeId, ContacttypeRow> = selectByIds(contacttypeids, c).associateBy { { row -> row.contacttypeid }(it) }

  fun update(
    row: ContacttypeRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.contacttypeid]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.contacttypeid] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: ContacttypeRow,
    c: Connection
  ): ContacttypeRow {
    map[unsaved.contacttypeid] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<ContacttypeRow>,
    c: Connection
  ): List<ContacttypeRow> {
    var result = ArrayList<ContacttypeRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.contacttypeid] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<ContacttypeRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.contacttypeid] = row
        count = count + 1
      }
    return count
  }
}