/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.shoppingcartitem

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class ShoppingcartitemRepoMock(
  val toRow: (ShoppingcartitemRowUnsaved) -> ShoppingcartitemRow,
  val map: MutableMap<ShoppingcartitemId, ShoppingcartitemRow>
) : ShoppingcartitemRepo {
  constructor(toRow: (ShoppingcartitemRowUnsaved) -> ShoppingcartitemRow) : this(toRow, mutableMapOf<ShoppingcartitemId, ShoppingcartitemRow>())

  fun deleteById(
    shoppingcartitemid: ShoppingcartitemId,
    c: Connection
  ): Boolean = (map.remove(shoppingcartitemid) != null)

  fun deleteByIds(
    shoppingcartitemids: Array<ShoppingcartitemId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : shoppingcartitemids) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: ShoppingcartitemRow,
    c: Connection
  ): ShoppingcartitemRow {
    if (map.containsKey(unsaved.shoppingcartitemid)) {
        throw new RuntimeException(str("id $unsaved.shoppingcartitemid already exists"))
      }
      map[unsaved.shoppingcartitemid] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: ShoppingcartitemRowUnsaved,
    c: Connection
  ): ShoppingcartitemRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<ShoppingcartitemRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.shoppingcartitemid] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<ShoppingcartitemRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.shoppingcartitemid] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<ShoppingcartitemRow> = map.values.toList()

  fun selectById(
    shoppingcartitemid: ShoppingcartitemId,
    c: Connection
  ): ShoppingcartitemRow? = map[shoppingcartitemid]

  fun selectByIds(
    shoppingcartitemids: Array<ShoppingcartitemId>,
    c: Connection
  ): List<ShoppingcartitemRow> = shoppingcartitemids.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    shoppingcartitemids: Array<ShoppingcartitemId>,
    c: Connection
  ): Map<ShoppingcartitemId, ShoppingcartitemRow> = selectByIds(shoppingcartitemids, c).associateBy { { row -> row.shoppingcartitemid }(it) }

  fun update(
    row: ShoppingcartitemRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.shoppingcartitemid]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.shoppingcartitemid] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: ShoppingcartitemRow,
    c: Connection
  ): ShoppingcartitemRow {
    map[unsaved.shoppingcartitemid] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<ShoppingcartitemRow>,
    c: Connection
  ): List<ShoppingcartitemRow> {
    var result = ArrayList<ShoppingcartitemRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.shoppingcartitemid] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<ShoppingcartitemRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.shoppingcartitemid] = row
        count = count + 1
      }
    return count
  }
}