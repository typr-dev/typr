/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.salestaxrate

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class SalestaxrateRepoMock(
  val toRow: (SalestaxrateRowUnsaved) -> SalestaxrateRow,
  val map: MutableMap<SalestaxrateId, SalestaxrateRow>
) : SalestaxrateRepo {
  constructor(toRow: (SalestaxrateRowUnsaved) -> SalestaxrateRow) : this(toRow, mutableMapOf<SalestaxrateId, SalestaxrateRow>())

  fun deleteById(
    salestaxrateid: SalestaxrateId,
    c: Connection
  ): Boolean = (map.remove(salestaxrateid) != null)

  fun deleteByIds(
    salestaxrateids: Array<SalestaxrateId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : salestaxrateids) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: SalestaxrateRow,
    c: Connection
  ): SalestaxrateRow {
    if (map.containsKey(unsaved.salestaxrateid)) {
        throw new RuntimeException(str("id $unsaved.salestaxrateid already exists"))
      }
      map[unsaved.salestaxrateid] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: SalestaxrateRowUnsaved,
    c: Connection
  ): SalestaxrateRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<SalestaxrateRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.salestaxrateid] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<SalestaxrateRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.salestaxrateid] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<SalestaxrateRow> = map.values.toList()

  fun selectById(
    salestaxrateid: SalestaxrateId,
    c: Connection
  ): SalestaxrateRow? = map[salestaxrateid]

  fun selectByIds(
    salestaxrateids: Array<SalestaxrateId>,
    c: Connection
  ): List<SalestaxrateRow> = salestaxrateids.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    salestaxrateids: Array<SalestaxrateId>,
    c: Connection
  ): Map<SalestaxrateId, SalestaxrateRow> = selectByIds(salestaxrateids, c).associateBy { { row -> row.salestaxrateid }(it) }

  fun update(
    row: SalestaxrateRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.salestaxrateid]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.salestaxrateid] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: SalestaxrateRow,
    c: Connection
  ): SalestaxrateRow {
    map[unsaved.salestaxrateid] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<SalestaxrateRow>,
    c: Connection
  ): List<SalestaxrateRow> {
    var result = ArrayList<SalestaxrateRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.salestaxrateid] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<SalestaxrateRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.salestaxrateid] = row
        count = count + 1
      }
    return count
  }
}