/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.salesterritory

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class SalesterritoryRepoMock(
  val toRow: (SalesterritoryRowUnsaved) -> SalesterritoryRow,
  val map: MutableMap<SalesterritoryId, SalesterritoryRow>
) : SalesterritoryRepo {
  constructor(toRow: (SalesterritoryRowUnsaved) -> SalesterritoryRow) : this(toRow, mutableMapOf<SalesterritoryId, SalesterritoryRow>())

  fun deleteById(
    territoryid: SalesterritoryId,
    c: Connection
  ): Boolean = (map.remove(territoryid) != null)

  fun deleteByIds(
    territoryids: Array<SalesterritoryId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : territoryids) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: SalesterritoryRow,
    c: Connection
  ): SalesterritoryRow {
    if (map.containsKey(unsaved.territoryid)) {
        throw new RuntimeException(str("id $unsaved.territoryid already exists"))
      }
      map[unsaved.territoryid] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: SalesterritoryRowUnsaved,
    c: Connection
  ): SalesterritoryRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<SalesterritoryRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.territoryid] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<SalesterritoryRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.territoryid] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<SalesterritoryRow> = map.values.toList()

  fun selectById(
    territoryid: SalesterritoryId,
    c: Connection
  ): SalesterritoryRow? = map[territoryid]

  fun selectByIds(
    territoryids: Array<SalesterritoryId>,
    c: Connection
  ): List<SalesterritoryRow> = territoryids.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    territoryids: Array<SalesterritoryId>,
    c: Connection
  ): Map<SalesterritoryId, SalesterritoryRow> = selectByIds(territoryids, c).associateBy { { row -> row.territoryid }(it) }

  fun update(
    row: SalesterritoryRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.territoryid]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.territoryid] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: SalesterritoryRow,
    c: Connection
  ): SalesterritoryRow {
    map[unsaved.territoryid] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<SalesterritoryRow>,
    c: Connection
  ): List<SalesterritoryRow> {
    var result = ArrayList<SalesterritoryRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.territoryid] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<SalesterritoryRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.territoryid] = row
        count = count + 1
      }
    return count
  }
}