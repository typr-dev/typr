/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.countryregioncurrency

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class CountryregioncurrencyRepoMock(
  val toRow: (CountryregioncurrencyRowUnsaved) -> CountryregioncurrencyRow,
  val map: MutableMap<CountryregioncurrencyId, CountryregioncurrencyRow>
) : CountryregioncurrencyRepo {
  constructor(toRow: (CountryregioncurrencyRowUnsaved) -> CountryregioncurrencyRow) : this(toRow, mutableMapOf<CountryregioncurrencyId, CountryregioncurrencyRow>())

  fun deleteById(
    compositeId: CountryregioncurrencyId,
    c: Connection
  ): Boolean = (map.remove(compositeId) != null)

  fun deleteByIds(
    compositeIds: Array<CountryregioncurrencyId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : compositeIds) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: CountryregioncurrencyRow,
    c: Connection
  ): CountryregioncurrencyRow {
    if (map.containsKey(unsaved.compositeId)) {
        throw new RuntimeException(str("id $unsaved.compositeId already exists"))
      }
      map[unsaved.compositeId] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: CountryregioncurrencyRowUnsaved,
    c: Connection
  ): CountryregioncurrencyRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<CountryregioncurrencyRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<CountryregioncurrencyRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.compositeId] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<CountryregioncurrencyRow> = map.values.toList()

  fun selectById(
    compositeId: CountryregioncurrencyId,
    c: Connection
  ): CountryregioncurrencyRow? = map[compositeId]

  fun selectByIds(
    compositeIds: Array<CountryregioncurrencyId>,
    c: Connection
  ): List<CountryregioncurrencyRow> = compositeIds.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    compositeIds: Array<CountryregioncurrencyId>,
    c: Connection
  ): Map<CountryregioncurrencyId, CountryregioncurrencyRow> = selectByIds(compositeIds, c).associateBy { { row -> row.compositeId }(it) }

  fun update(
    row: CountryregioncurrencyRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.compositeId]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.compositeId] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: CountryregioncurrencyRow,
    c: Connection
  ): CountryregioncurrencyRow {
    map[unsaved.compositeId] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<CountryregioncurrencyRow>,
    c: Connection
  ): List<CountryregioncurrencyRow> {
    var result = ArrayList<CountryregioncurrencyRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<CountryregioncurrencyRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.compositeId] = row
        count = count + 1
      }
    return count
  }
}