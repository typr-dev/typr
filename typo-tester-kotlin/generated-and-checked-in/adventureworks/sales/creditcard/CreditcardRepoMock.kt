/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.creditcard

import adventureworks.userdefined.CustomCreditcardId
import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class CreditcardRepoMock(
  val toRow: (CreditcardRowUnsaved) -> CreditcardRow,
  val map: MutableMap</* user-picked */ CustomCreditcardId, CreditcardRow>
) : CreditcardRepo {
  constructor(toRow: (CreditcardRowUnsaved) -> CreditcardRow) : this(toRow, mutableMapOf</* user-picked */ CustomCreditcardId, CreditcardRow>())

  fun deleteById(
    creditcardid: /* user-picked */ CustomCreditcardId,
    c: Connection
  ): Boolean = (map.remove(creditcardid) != null)

  fun deleteByIds(
    creditcardids: Array</* user-picked */ CustomCreditcardId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : creditcardids) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: CreditcardRow,
    c: Connection
  ): CreditcardRow {
    if (map.containsKey(unsaved.creditcardid)) {
        throw new RuntimeException(str("id $unsaved.creditcardid already exists"))
      }
      map[unsaved.creditcardid] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: CreditcardRowUnsaved,
    c: Connection
  ): CreditcardRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<CreditcardRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.creditcardid] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<CreditcardRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.creditcardid] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<CreditcardRow> = map.values.toList()

  fun selectById(
    creditcardid: /* user-picked */ CustomCreditcardId,
    c: Connection
  ): CreditcardRow? = map[creditcardid]

  fun selectByIds(
    creditcardids: Array</* user-picked */ CustomCreditcardId>,
    c: Connection
  ): List<CreditcardRow> = creditcardids.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    creditcardids: Array</* user-picked */ CustomCreditcardId>,
    c: Connection
  ): Map</* user-picked */ CustomCreditcardId, CreditcardRow> = selectByIds(creditcardids, c).associateBy { { row -> row.creditcardid }(it) }

  fun update(
    row: CreditcardRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.creditcardid]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.creditcardid] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: CreditcardRow,
    c: Connection
  ): CreditcardRow {
    map[unsaved.creditcardid] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<CreditcardRow>,
    c: Connection
  ): List<CreditcardRow> {
    var result = ArrayList<CreditcardRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.creditcardid] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<CreditcardRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.creditcardid] = row
        count = count + 1
      }
    return count
  }
}