/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.specialoffer

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class SpecialofferRepoMock(
  val toRow: (SpecialofferRowUnsaved) -> SpecialofferRow,
  val map: MutableMap<SpecialofferId, SpecialofferRow>
) : SpecialofferRepo {
  constructor(toRow: (SpecialofferRowUnsaved) -> SpecialofferRow) : this(toRow, mutableMapOf<SpecialofferId, SpecialofferRow>())

  fun deleteById(
    specialofferid: SpecialofferId,
    c: Connection
  ): Boolean = (map.remove(specialofferid) != null)

  fun deleteByIds(
    specialofferids: Array<SpecialofferId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : specialofferids) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: SpecialofferRow,
    c: Connection
  ): SpecialofferRow {
    if (map.containsKey(unsaved.specialofferid)) {
        throw new RuntimeException(str("id $unsaved.specialofferid already exists"))
      }
      map[unsaved.specialofferid] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: SpecialofferRowUnsaved,
    c: Connection
  ): SpecialofferRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<SpecialofferRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.specialofferid] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<SpecialofferRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.specialofferid] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<SpecialofferRow> = map.values.toList()

  fun selectById(
    specialofferid: SpecialofferId,
    c: Connection
  ): SpecialofferRow? = map[specialofferid]

  fun selectByIds(
    specialofferids: Array<SpecialofferId>,
    c: Connection
  ): List<SpecialofferRow> = specialofferids.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    specialofferids: Array<SpecialofferId>,
    c: Connection
  ): Map<SpecialofferId, SpecialofferRow> = selectByIds(specialofferids, c).associateBy { { row -> row.specialofferid }(it) }

  fun update(
    row: SpecialofferRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.specialofferid]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.specialofferid] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: SpecialofferRow,
    c: Connection
  ): SpecialofferRow {
    map[unsaved.specialofferid] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<SpecialofferRow>,
    c: Connection
  ): List<SpecialofferRow> {
    var result = ArrayList<SpecialofferRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.specialofferid] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<SpecialofferRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.specialofferid] = row
        count = count + 1
      }
    return count
  }
}