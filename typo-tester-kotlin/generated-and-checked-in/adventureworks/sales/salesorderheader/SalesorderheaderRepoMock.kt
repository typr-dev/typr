/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.salesorderheader

import java.sql.Connection
import java.util.ArrayList
import java.util.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.runtime.internal.stringInterpolator.str

data class SalesorderheaderRepoMock(
  val toRow: (SalesorderheaderRowUnsaved) -> SalesorderheaderRow,
  val map: MutableMap<SalesorderheaderId, SalesorderheaderRow>
) : SalesorderheaderRepo {
  constructor(toRow: (SalesorderheaderRowUnsaved) -> SalesorderheaderRow) : this(toRow, mutableMapOf<SalesorderheaderId, SalesorderheaderRow>())

  fun deleteById(
    salesorderid: SalesorderheaderId,
    c: Connection
  ): Boolean = (map.remove(salesorderid) != null)

  fun deleteByIds(
    salesorderids: Array<SalesorderheaderId>,
    c: Connection
  ): Int {
    var count = 0
      for (var id : salesorderids) {
        if ((map.remove(id) != null)) {
          count = count + 1
        }
      }
    return count
  }

  fun insert(
    unsaved: SalesorderheaderRow,
    c: Connection
  ): SalesorderheaderRow {
    if (map.containsKey(unsaved.salesorderid)) {
        throw new RuntimeException(str("id $unsaved.salesorderid already exists"))
      }
      map[unsaved.salesorderid] = unsaved
    return unsaved
  }

  fun insert(
    unsaved: SalesorderheaderRowUnsaved,
    c: Connection
  ): SalesorderheaderRow = insert(toRow(unsaved), c)

  fun insertStreaming(
    unsaved: Iterator<SalesorderheaderRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.salesorderid] = row
        count = count + 1L
      }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  fun insertUnsavedStreaming(
    unsaved: Iterator<SalesorderheaderRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
      while (unsaved.hasNext()) {
        var unsavedRow = unsaved.next()
        var row = toRow(unsavedRow)
        map[row.salesorderid] = row
        count = count + 1L
      }
    return count
  }

  fun selectAll(c: Connection): List<SalesorderheaderRow> = map.values.toList()

  fun selectById(
    salesorderid: SalesorderheaderId,
    c: Connection
  ): SalesorderheaderRow? = map[salesorderid]

  fun selectByIds(
    salesorderids: Array<SalesorderheaderId>,
    c: Connection
  ): List<SalesorderheaderRow> = salesorderids.mapNotNull { { id -> map[id] }(it) }

  fun selectByIdsTracked(
    salesorderids: Array<SalesorderheaderId>,
    c: Connection
  ): Map<SalesorderheaderId, SalesorderheaderRow> = selectByIds(salesorderids, c).associateBy { { row -> row.salesorderid }(it) }

  fun update(
    row: SalesorderheaderRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = (map[row.salesorderid]?.takeIf { { oldRow -> !oldRow.equals(row) }(it) } != null)
      if (shouldUpdate) {
        map[row.salesorderid] = row
      }
    return shouldUpdate
  }

  fun upsert(
    unsaved: SalesorderheaderRow,
    c: Connection
  ): SalesorderheaderRow {
    map[unsaved.salesorderid] = unsaved
    return unsaved
  }

  fun upsertBatch(
    unsaved: Iterator<SalesorderheaderRow>,
    c: Connection
  ): List<SalesorderheaderRow> {
    var result = ArrayList<SalesorderheaderRow>()
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.salesorderid] = row
        result.add(row)
      }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  fun upsertStreaming(
    unsaved: Iterator<SalesorderheaderRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
      while (unsaved.hasNext()) {
        var row = unsaved.next()
        map[row.salesorderid] = row
        count = count + 1
      }
    return count
  }
}