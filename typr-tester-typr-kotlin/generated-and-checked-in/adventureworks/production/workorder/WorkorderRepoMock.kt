/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.workorder

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typr.kotlindsl.DeleteBuilder
import typr.kotlindsl.DeleteBuilderMock
import typr.kotlindsl.DeleteParams
import typr.kotlindsl.SelectBuilder
import typr.kotlindsl.SelectBuilderMock
import typr.kotlindsl.SelectParams
import typr.kotlindsl.UpdateBuilder
import typr.kotlindsl.UpdateBuilderMock
import typr.kotlindsl.UpdateParams

data class WorkorderRepoMock(
  val toRow: (WorkorderRowUnsaved) -> WorkorderRow,
  val map: MutableMap<WorkorderId, WorkorderRow> = mutableMapOf<WorkorderId, WorkorderRow>()
) : WorkorderRepo {
  override fun delete(): DeleteBuilder<WorkorderFields, WorkorderRow> = DeleteBuilderMock(WorkorderFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.workorderid }, { id -> map.remove(id) })

  override fun deleteById(
    workorderid: WorkorderId,
    c: Connection
  ): Boolean = map.remove(workorderid) != null

  override fun deleteByIds(
    workorderids: Array<WorkorderId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in workorderids) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: WorkorderRow,
    c: Connection
  ): WorkorderRow {
    if (map.containsKey(unsaved.workorderid)) {
      throw RuntimeException("id " + unsaved.workorderid + " already exists")
    }
    map[unsaved.workorderid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: WorkorderRowUnsaved,
    c: Connection
  ): WorkorderRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<WorkorderRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.workorderid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<WorkorderRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.workorderid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<WorkorderFields, WorkorderRow> = SelectBuilderMock(WorkorderFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<WorkorderRow> = map.values.toList()

  override fun selectById(
    workorderid: WorkorderId,
    c: Connection
  ): WorkorderRow? = map[workorderid]

  override fun selectByIds(
    workorderids: Array<WorkorderId>,
    c: Connection
  ): List<WorkorderRow> {
    val result = ArrayList<WorkorderRow>()
    for (id in workorderids) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    workorderids: Array<WorkorderId>,
    c: Connection
  ): Map<WorkorderId, WorkorderRow> = selectByIds(workorderids, c).associateBy({ row: WorkorderRow -> row.workorderid })

  override fun update(): UpdateBuilder<WorkorderFields, WorkorderRow> = UpdateBuilderMock(WorkorderFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: WorkorderRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.workorderid]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.workorderid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: WorkorderRow,
    c: Connection
  ): WorkorderRow {
    map[unsaved.workorderid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<WorkorderRow>,
    c: Connection
  ): List<WorkorderRow> {
    val result = ArrayList<WorkorderRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.workorderid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<WorkorderRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.workorderid] = row
      count = count + 1
    }
    return count
  }
}