/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.transactionhistory

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typr.kotlindsl.DeleteBuilder
import typr.kotlindsl.DeleteBuilderMock
import typr.kotlindsl.DeleteParams
import typr.kotlindsl.SelectBuilder
import typr.kotlindsl.SelectBuilderMock
import typr.kotlindsl.SelectParams
import typr.kotlindsl.UpdateBuilder
import typr.kotlindsl.UpdateBuilderMock
import typr.kotlindsl.UpdateParams

data class TransactionhistoryRepoMock(
  val toRow: (TransactionhistoryRowUnsaved) -> TransactionhistoryRow,
  val map: MutableMap<TransactionhistoryId, TransactionhistoryRow> = mutableMapOf<TransactionhistoryId, TransactionhistoryRow>()
) : TransactionhistoryRepo {
  override fun delete(): DeleteBuilder<TransactionhistoryFields, TransactionhistoryRow> = DeleteBuilderMock(TransactionhistoryFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.transactionid }, { id -> map.remove(id) })

  override fun deleteById(
    transactionid: TransactionhistoryId,
    c: Connection
  ): Boolean = map.remove(transactionid) != null

  override fun deleteByIds(
    transactionids: Array<TransactionhistoryId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in transactionids) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: TransactionhistoryRow,
    c: Connection
  ): TransactionhistoryRow {
    if (map.containsKey(unsaved.transactionid)) {
      throw RuntimeException("id " + unsaved.transactionid + " already exists")
    }
    map[unsaved.transactionid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: TransactionhistoryRowUnsaved,
    c: Connection
  ): TransactionhistoryRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<TransactionhistoryRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.transactionid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<TransactionhistoryRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.transactionid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<TransactionhistoryFields, TransactionhistoryRow> = SelectBuilderMock(TransactionhistoryFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<TransactionhistoryRow> = map.values.toList()

  override fun selectById(
    transactionid: TransactionhistoryId,
    c: Connection
  ): TransactionhistoryRow? = map[transactionid]

  override fun selectByIds(
    transactionids: Array<TransactionhistoryId>,
    c: Connection
  ): List<TransactionhistoryRow> {
    val result = ArrayList<TransactionhistoryRow>()
    for (id in transactionids) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    transactionids: Array<TransactionhistoryId>,
    c: Connection
  ): Map<TransactionhistoryId, TransactionhistoryRow> = selectByIds(transactionids, c).associateBy({ row: TransactionhistoryRow -> row.transactionid })

  override fun update(): UpdateBuilder<TransactionhistoryFields, TransactionhistoryRow> = UpdateBuilderMock(TransactionhistoryFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: TransactionhistoryRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.transactionid]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.transactionid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: TransactionhistoryRow,
    c: Connection
  ): TransactionhistoryRow {
    map[unsaved.transactionid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<TransactionhistoryRow>,
    c: Connection
  ): List<TransactionhistoryRow> {
    val result = ArrayList<TransactionhistoryRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.transactionid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<TransactionhistoryRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.transactionid] = row
      count = count + 1
    }
    return count
  }
}