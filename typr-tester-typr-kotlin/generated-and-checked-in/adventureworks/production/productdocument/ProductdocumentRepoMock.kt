/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productdocument

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typr.kotlindsl.DeleteBuilder
import typr.kotlindsl.DeleteBuilderMock
import typr.kotlindsl.DeleteParams
import typr.kotlindsl.SelectBuilder
import typr.kotlindsl.SelectBuilderMock
import typr.kotlindsl.SelectParams
import typr.kotlindsl.UpdateBuilder
import typr.kotlindsl.UpdateBuilderMock
import typr.kotlindsl.UpdateParams

data class ProductdocumentRepoMock(
  val toRow: (ProductdocumentRowUnsaved) -> ProductdocumentRow,
  val map: MutableMap<ProductdocumentId, ProductdocumentRow> = mutableMapOf<ProductdocumentId, ProductdocumentRow>()
) : ProductdocumentRepo {
  override fun delete(): DeleteBuilder<ProductdocumentFields, ProductdocumentRow> = DeleteBuilderMock(ProductdocumentFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.compositeId() }, { id -> map.remove(id) })

  override fun deleteById(
    compositeId: ProductdocumentId,
    c: Connection
  ): Boolean = map.remove(compositeId) != null

  override fun deleteByIds(
    compositeIds: Array<ProductdocumentId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in compositeIds) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: ProductdocumentRow,
    c: Connection
  ): ProductdocumentRow {
    if (map.containsKey(unsaved.compositeId())) {
      throw RuntimeException("id " + unsaved.compositeId() + " already exists")
    }
    map[unsaved.compositeId()] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: ProductdocumentRowUnsaved,
    c: Connection
  ): ProductdocumentRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<ProductdocumentRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<ProductdocumentRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.compositeId()] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<ProductdocumentFields, ProductdocumentRow> = SelectBuilderMock(ProductdocumentFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<ProductdocumentRow> = map.values.toList()

  override fun selectById(
    compositeId: ProductdocumentId,
    c: Connection
  ): ProductdocumentRow? = map[compositeId]

  override fun selectByIds(
    compositeIds: Array<ProductdocumentId>,
    c: Connection
  ): List<ProductdocumentRow> {
    val result = ArrayList<ProductdocumentRow>()
    for (id in compositeIds) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    compositeIds: Array<ProductdocumentId>,
    c: Connection
  ): Map<ProductdocumentId, ProductdocumentRow> = selectByIds(compositeIds, c).associateBy({ row: ProductdocumentRow -> row.compositeId() })

  override fun update(): UpdateBuilder<ProductdocumentFields, ProductdocumentRow> = UpdateBuilderMock(ProductdocumentFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: ProductdocumentRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.compositeId()]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.compositeId()] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: ProductdocumentRow,
    c: Connection
  ): ProductdocumentRow {
    map[unsaved.compositeId()] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<ProductdocumentRow>,
    c: Connection
  ): List<ProductdocumentRow> {
    val result = ArrayList<ProductdocumentRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<ProductdocumentRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      count = count + 1
    }
    return count
  }
}