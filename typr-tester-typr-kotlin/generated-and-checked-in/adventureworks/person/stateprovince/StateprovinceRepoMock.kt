/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.stateprovince

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typr.kotlindsl.DeleteBuilder
import typr.kotlindsl.DeleteBuilderMock
import typr.kotlindsl.DeleteParams
import typr.kotlindsl.SelectBuilder
import typr.kotlindsl.SelectBuilderMock
import typr.kotlindsl.SelectParams
import typr.kotlindsl.UpdateBuilder
import typr.kotlindsl.UpdateBuilderMock
import typr.kotlindsl.UpdateParams

data class StateprovinceRepoMock(
  val toRow: (StateprovinceRowUnsaved) -> StateprovinceRow,
  val map: MutableMap<StateprovinceId, StateprovinceRow> = mutableMapOf<StateprovinceId, StateprovinceRow>()
) : StateprovinceRepo {
  override fun delete(): DeleteBuilder<StateprovinceFields, StateprovinceRow> = DeleteBuilderMock(StateprovinceFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.stateprovinceid }, { id -> map.remove(id) })

  override fun deleteById(
    stateprovinceid: StateprovinceId,
    c: Connection
  ): Boolean = map.remove(stateprovinceid) != null

  override fun deleteByIds(
    stateprovinceids: Array<StateprovinceId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in stateprovinceids) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: StateprovinceRow,
    c: Connection
  ): StateprovinceRow {
    if (map.containsKey(unsaved.stateprovinceid)) {
      throw RuntimeException("id " + unsaved.stateprovinceid + " already exists")
    }
    map[unsaved.stateprovinceid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: StateprovinceRowUnsaved,
    c: Connection
  ): StateprovinceRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<StateprovinceRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.stateprovinceid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<StateprovinceRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.stateprovinceid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<StateprovinceFields, StateprovinceRow> = SelectBuilderMock(StateprovinceFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<StateprovinceRow> = map.values.toList()

  override fun selectById(
    stateprovinceid: StateprovinceId,
    c: Connection
  ): StateprovinceRow? = map[stateprovinceid]

  override fun selectByIds(
    stateprovinceids: Array<StateprovinceId>,
    c: Connection
  ): List<StateprovinceRow> {
    val result = ArrayList<StateprovinceRow>()
    for (id in stateprovinceids) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    stateprovinceids: Array<StateprovinceId>,
    c: Connection
  ): Map<StateprovinceId, StateprovinceRow> = selectByIds(stateprovinceids, c).associateBy({ row: StateprovinceRow -> row.stateprovinceid })

  override fun update(): UpdateBuilder<StateprovinceFields, StateprovinceRow> = UpdateBuilderMock(StateprovinceFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: StateprovinceRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.stateprovinceid]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.stateprovinceid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: StateprovinceRow,
    c: Connection
  ): StateprovinceRow {
    map[unsaved.stateprovinceid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<StateprovinceRow>,
    c: Connection
  ): List<StateprovinceRow> {
    val result = ArrayList<StateprovinceRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.stateprovinceid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<StateprovinceRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.stateprovinceid] = row
      count = count + 1
    }
    return count
  }
}