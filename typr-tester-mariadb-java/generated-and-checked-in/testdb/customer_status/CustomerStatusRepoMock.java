/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.customer_status;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typr.dsl.DeleteBuilder;
import typr.dsl.DeleteBuilderMock;
import typr.dsl.DeleteParams;
import typr.dsl.SelectBuilder;
import typr.dsl.SelectBuilderMock;
import typr.dsl.SelectParams;
import typr.dsl.UpdateBuilder;
import typr.dsl.UpdateBuilderMock;
import typr.dsl.UpdateParams;

public record CustomerStatusRepoMock(
    java.util.function.Function<CustomerStatusRowUnsaved, CustomerStatusRow> toRow,
    HashMap<CustomerStatusId, CustomerStatusRow> map)
    implements CustomerStatusRepo {
  public CustomerStatusRepoMock(
      java.util.function.Function<CustomerStatusRowUnsaved, CustomerStatusRow> toRow) {
    this(toRow, new HashMap<CustomerStatusId, CustomerStatusRow>());
  }
  ;

  public CustomerStatusRepoMock withToRow(
      java.util.function.Function<CustomerStatusRowUnsaved, CustomerStatusRow> toRow) {
    return new CustomerStatusRepoMock(toRow, map);
  }
  ;

  public CustomerStatusRepoMock withMap(HashMap<CustomerStatusId, CustomerStatusRow> map) {
    return new CustomerStatusRepoMock(toRow, map);
  }
  ;

  @Override
  public DeleteBuilder<CustomerStatusFields, CustomerStatusRow> delete() {
    return new DeleteBuilderMock<>(
        CustomerStatusFields.structure(),
        () -> new ArrayList<>(map.values()),
        DeleteParams.empty(),
        row -> row.statusCode(),
        id -> map.remove(id));
  }
  ;

  @Override
  public Boolean deleteById(CustomerStatusId statusCode, Connection c) {
    return Optional.ofNullable(map.remove(statusCode)).isPresent();
  }
  ;

  @Override
  public Integer deleteByIds(CustomerStatusId[] statusCodes, Connection c) {
    var count = 0;
    for (var id : statusCodes) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
        ;
      }
      ;
    }
    ;
    return count;
  }
  ;

  @Override
  public CustomerStatusRow insert(CustomerStatusRow unsaved, Connection c) {
    if (map.containsKey(unsaved.statusCode())) {
      throw new RuntimeException("id " + unsaved.statusCode() + " already exists");
    }
    ;
    map.put(unsaved.statusCode(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public CustomerStatusRow insert(CustomerStatusRowUnsaved unsaved, Connection c) {
    return insert(toRow.apply(unsaved), c);
  }
  ;

  @Override
  public SelectBuilder<CustomerStatusFields, CustomerStatusRow> select() {
    return new SelectBuilderMock<>(
        CustomerStatusFields.structure(),
        () -> new ArrayList<>(map.values()),
        SelectParams.empty());
  }
  ;

  @Override
  public List<CustomerStatusRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  }
  ;

  @Override
  public Optional<CustomerStatusRow> selectById(CustomerStatusId statusCode, Connection c) {
    return Optional.ofNullable(map.get(statusCode));
  }
  ;

  @Override
  public List<CustomerStatusRow> selectByIds(CustomerStatusId[] statusCodes, Connection c) {
    var result = new ArrayList<CustomerStatusRow>();
    for (var id : statusCodes) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
        result.add(opt.get());
      }
      ;
    }
    ;
    return result;
  }
  ;

  @Override
  public Map<CustomerStatusId, CustomerStatusRow> selectByIdsTracked(
      CustomerStatusId[] statusCodes, Connection c) {
    return selectByIds(statusCodes, c).stream()
        .collect(
            Collectors.toMap((CustomerStatusRow row) -> row.statusCode(), Function.identity()));
  }
  ;

  @Override
  public UpdateBuilder<CustomerStatusFields, CustomerStatusRow> update() {
    return new UpdateBuilderMock<>(
        CustomerStatusFields.structure(),
        () -> new ArrayList<>(map.values()),
        UpdateParams.empty(),
        row -> row);
  }
  ;

  @Override
  public Boolean update(CustomerStatusRow row, Connection c) {
    var shouldUpdate =
        Optional.ofNullable(map.get(row.statusCode()))
            .filter(oldRow -> !oldRow.equals(row))
            .isPresent();
    if (shouldUpdate) {
      map.put(row.statusCode(), row);
      ;
    }
    ;
    return shouldUpdate;
  }
  ;

  @Override
  public CustomerStatusRow upsert(CustomerStatusRow unsaved, Connection c) {
    map.put(unsaved.statusCode(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public List<CustomerStatusRow> upsertBatch(Iterator<CustomerStatusRow> unsaved, Connection c) {
    var result = new ArrayList<CustomerStatusRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.statusCode(), row);
      result.add(row);
    }
    ;
    return result;
  }
  ;
}
