/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.categories;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typr.dsl.DeleteBuilder;
import typr.dsl.DeleteBuilderMock;
import typr.dsl.DeleteParams;
import typr.dsl.SelectBuilder;
import typr.dsl.SelectBuilderMock;
import typr.dsl.SelectParams;
import typr.dsl.UpdateBuilder;
import typr.dsl.UpdateBuilderMock;
import typr.dsl.UpdateParams;

public record CategoriesRepoMock(
    java.util.function.Function<CategoriesRowUnsaved, CategoriesRow> toRow,
    HashMap<CategoriesId, CategoriesRow> map)
    implements CategoriesRepo {
  public CategoriesRepoMock(
      java.util.function.Function<CategoriesRowUnsaved, CategoriesRow> toRow) {
    this(toRow, new HashMap<CategoriesId, CategoriesRow>());
  }
  ;

  public CategoriesRepoMock withToRow(
      java.util.function.Function<CategoriesRowUnsaved, CategoriesRow> toRow) {
    return new CategoriesRepoMock(toRow, map);
  }
  ;

  public CategoriesRepoMock withMap(HashMap<CategoriesId, CategoriesRow> map) {
    return new CategoriesRepoMock(toRow, map);
  }
  ;

  @Override
  public DeleteBuilder<CategoriesFields, CategoriesRow> delete() {
    return new DeleteBuilderMock<>(
        CategoriesFields.structure(),
        () -> new ArrayList<>(map.values()),
        DeleteParams.empty(),
        row -> row.categoryId(),
        id -> map.remove(id));
  }
  ;

  @Override
  public Boolean deleteById(CategoriesId categoryId, Connection c) {
    return Optional.ofNullable(map.remove(categoryId)).isPresent();
  }
  ;

  @Override
  public Integer deleteByIds(CategoriesId[] categoryIds, Connection c) {
    var count = 0;
    for (var id : categoryIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
        ;
      }
      ;
    }
    ;
    return count;
  }
  ;

  @Override
  public CategoriesRow insert(CategoriesRow unsaved, Connection c) {
    if (map.containsKey(unsaved.categoryId())) {
      throw new RuntimeException("id " + unsaved.categoryId() + " already exists");
    }
    ;
    map.put(unsaved.categoryId(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public CategoriesRow insert(CategoriesRowUnsaved unsaved, Connection c) {
    return insert(toRow.apply(unsaved), c);
  }
  ;

  @Override
  public SelectBuilder<CategoriesFields, CategoriesRow> select() {
    return new SelectBuilderMock<>(
        CategoriesFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  }
  ;

  @Override
  public List<CategoriesRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  }
  ;

  @Override
  public Optional<CategoriesRow> selectById(CategoriesId categoryId, Connection c) {
    return Optional.ofNullable(map.get(categoryId));
  }
  ;

  @Override
  public List<CategoriesRow> selectByIds(CategoriesId[] categoryIds, Connection c) {
    var result = new ArrayList<CategoriesRow>();
    for (var id : categoryIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
        result.add(opt.get());
      }
      ;
    }
    ;
    return result;
  }
  ;

  @Override
  public Map<CategoriesId, CategoriesRow> selectByIdsTracked(
      CategoriesId[] categoryIds, Connection c) {
    return selectByIds(categoryIds, c).stream()
        .collect(Collectors.toMap((CategoriesRow row) -> row.categoryId(), Function.identity()));
  }
  ;

  @Override
  public Optional<CategoriesRow> selectByUniqueSlug(String slug, Connection c) {
    return new ArrayList<>(map.values()).stream().filter(v -> slug.equals(v.slug())).findFirst();
  }
  ;

  @Override
  public UpdateBuilder<CategoriesFields, CategoriesRow> update() {
    return new UpdateBuilderMock<>(
        CategoriesFields.structure(),
        () -> new ArrayList<>(map.values()),
        UpdateParams.empty(),
        row -> row);
  }
  ;

  @Override
  public Boolean update(CategoriesRow row, Connection c) {
    var shouldUpdate =
        Optional.ofNullable(map.get(row.categoryId()))
            .filter(oldRow -> !oldRow.equals(row))
            .isPresent();
    if (shouldUpdate) {
      map.put(row.categoryId(), row);
      ;
    }
    ;
    return shouldUpdate;
  }
  ;

  @Override
  public CategoriesRow upsert(CategoriesRow unsaved, Connection c) {
    map.put(unsaved.categoryId(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public List<CategoriesRow> upsertBatch(Iterator<CategoriesRow> unsaved, Connection c) {
    var result = new ArrayList<CategoriesRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.categoryId(), row);
      result.add(row);
    }
    ;
    return result;
  }
  ;
}
