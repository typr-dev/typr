/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.warehouses;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typr.dsl.DeleteBuilder;
import typr.dsl.DeleteBuilderMock;
import typr.dsl.DeleteParams;
import typr.dsl.SelectBuilder;
import typr.dsl.SelectBuilderMock;
import typr.dsl.SelectParams;
import typr.dsl.UpdateBuilder;
import typr.dsl.UpdateBuilderMock;
import typr.dsl.UpdateParams;

public record WarehousesRepoMock(
    java.util.function.Function<WarehousesRowUnsaved, WarehousesRow> toRow,
    HashMap<WarehousesId, WarehousesRow> map)
    implements WarehousesRepo {
  public WarehousesRepoMock(
      java.util.function.Function<WarehousesRowUnsaved, WarehousesRow> toRow) {
    this(toRow, new HashMap<WarehousesId, WarehousesRow>());
  }
  ;

  public WarehousesRepoMock withToRow(
      java.util.function.Function<WarehousesRowUnsaved, WarehousesRow> toRow) {
    return new WarehousesRepoMock(toRow, map);
  }
  ;

  public WarehousesRepoMock withMap(HashMap<WarehousesId, WarehousesRow> map) {
    return new WarehousesRepoMock(toRow, map);
  }
  ;

  @Override
  public DeleteBuilder<WarehousesFields, WarehousesRow> delete() {
    return new DeleteBuilderMock<>(
        WarehousesFields.structure(),
        () -> new ArrayList<>(map.values()),
        DeleteParams.empty(),
        row -> row.warehouseId(),
        id -> map.remove(id));
  }
  ;

  @Override
  public Boolean deleteById(WarehousesId warehouseId, Connection c) {
    return Optional.ofNullable(map.remove(warehouseId)).isPresent();
  }
  ;

  @Override
  public Integer deleteByIds(WarehousesId[] warehouseIds, Connection c) {
    var count = 0;
    for (var id : warehouseIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
        ;
      }
      ;
    }
    ;
    return count;
  }
  ;

  @Override
  public WarehousesRow insert(WarehousesRow unsaved, Connection c) {
    if (map.containsKey(unsaved.warehouseId())) {
      throw new RuntimeException("id " + unsaved.warehouseId() + " already exists");
    }
    ;
    map.put(unsaved.warehouseId(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public WarehousesRow insert(WarehousesRowUnsaved unsaved, Connection c) {
    return insert(toRow.apply(unsaved), c);
  }
  ;

  @Override
  public SelectBuilder<WarehousesFields, WarehousesRow> select() {
    return new SelectBuilderMock<>(
        WarehousesFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  }
  ;

  @Override
  public List<WarehousesRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  }
  ;

  @Override
  public Optional<WarehousesRow> selectById(WarehousesId warehouseId, Connection c) {
    return Optional.ofNullable(map.get(warehouseId));
  }
  ;

  @Override
  public List<WarehousesRow> selectByIds(WarehousesId[] warehouseIds, Connection c) {
    var result = new ArrayList<WarehousesRow>();
    for (var id : warehouseIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
        result.add(opt.get());
      }
      ;
    }
    ;
    return result;
  }
  ;

  @Override
  public Map<WarehousesId, WarehousesRow> selectByIdsTracked(
      WarehousesId[] warehouseIds, Connection c) {
    return selectByIds(warehouseIds, c).stream()
        .collect(Collectors.toMap((WarehousesRow row) -> row.warehouseId(), Function.identity()));
  }
  ;

  @Override
  public Optional<WarehousesRow> selectByUniqueCode(String code, Connection c) {
    return new ArrayList<>(map.values()).stream().filter(v -> code.equals(v.code())).findFirst();
  }
  ;

  @Override
  public UpdateBuilder<WarehousesFields, WarehousesRow> update() {
    return new UpdateBuilderMock<>(
        WarehousesFields.structure(),
        () -> new ArrayList<>(map.values()),
        UpdateParams.empty(),
        row -> row);
  }
  ;

  @Override
  public Boolean update(WarehousesRow row, Connection c) {
    var shouldUpdate =
        Optional.ofNullable(map.get(row.warehouseId()))
            .filter(oldRow -> !oldRow.equals(row))
            .isPresent();
    if (shouldUpdate) {
      map.put(row.warehouseId(), row);
      ;
    }
    ;
    return shouldUpdate;
  }
  ;

  @Override
  public WarehousesRow upsert(WarehousesRow unsaved, Connection c) {
    map.put(unsaved.warehouseId(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public List<WarehousesRow> upsertBatch(Iterator<WarehousesRow> unsaved, Connection c) {
    var result = new ArrayList<WarehousesRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.warehouseId(), row);
      result.add(row);
    }
    ;
    return result;
  }
  ;
}
