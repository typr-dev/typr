/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.brands;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typr.dsl.DeleteBuilder;
import typr.dsl.DeleteBuilderMock;
import typr.dsl.DeleteParams;
import typr.dsl.SelectBuilder;
import typr.dsl.SelectBuilderMock;
import typr.dsl.SelectParams;
import typr.dsl.UpdateBuilder;
import typr.dsl.UpdateBuilderMock;
import typr.dsl.UpdateParams;

public record BrandsRepoMock(
    java.util.function.Function<BrandsRowUnsaved, BrandsRow> toRow,
    HashMap<BrandsId, BrandsRow> map)
    implements BrandsRepo {
  public BrandsRepoMock(java.util.function.Function<BrandsRowUnsaved, BrandsRow> toRow) {
    this(toRow, new HashMap<BrandsId, BrandsRow>());
  }
  ;

  public BrandsRepoMock withToRow(java.util.function.Function<BrandsRowUnsaved, BrandsRow> toRow) {
    return new BrandsRepoMock(toRow, map);
  }
  ;

  public BrandsRepoMock withMap(HashMap<BrandsId, BrandsRow> map) {
    return new BrandsRepoMock(toRow, map);
  }
  ;

  @Override
  public DeleteBuilder<BrandsFields, BrandsRow> delete() {
    return new DeleteBuilderMock<>(
        BrandsFields.structure(),
        () -> new ArrayList<>(map.values()),
        DeleteParams.empty(),
        row -> row.brandId(),
        id -> map.remove(id));
  }
  ;

  @Override
  public Boolean deleteById(BrandsId brandId, Connection c) {
    return Optional.ofNullable(map.remove(brandId)).isPresent();
  }
  ;

  @Override
  public Integer deleteByIds(BrandsId[] brandIds, Connection c) {
    var count = 0;
    for (var id : brandIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
        ;
      }
      ;
    }
    ;
    return count;
  }
  ;

  @Override
  public BrandsRow insert(BrandsRow unsaved, Connection c) {
    if (map.containsKey(unsaved.brandId())) {
      throw new RuntimeException("id " + unsaved.brandId() + " already exists");
    }
    ;
    map.put(unsaved.brandId(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public BrandsRow insert(BrandsRowUnsaved unsaved, Connection c) {
    return insert(toRow.apply(unsaved), c);
  }
  ;

  @Override
  public SelectBuilder<BrandsFields, BrandsRow> select() {
    return new SelectBuilderMock<>(
        BrandsFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  }
  ;

  @Override
  public List<BrandsRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  }
  ;

  @Override
  public Optional<BrandsRow> selectById(BrandsId brandId, Connection c) {
    return Optional.ofNullable(map.get(brandId));
  }
  ;

  @Override
  public List<BrandsRow> selectByIds(BrandsId[] brandIds, Connection c) {
    var result = new ArrayList<BrandsRow>();
    for (var id : brandIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
        result.add(opt.get());
      }
      ;
    }
    ;
    return result;
  }
  ;

  @Override
  public Map<BrandsId, BrandsRow> selectByIdsTracked(BrandsId[] brandIds, Connection c) {
    return selectByIds(brandIds, c).stream()
        .collect(Collectors.toMap((BrandsRow row) -> row.brandId(), Function.identity()));
  }
  ;

  @Override
  public Optional<BrandsRow> selectByUniqueSlug(String slug, Connection c) {
    return new ArrayList<>(map.values()).stream().filter(v -> slug.equals(v.slug())).findFirst();
  }
  ;

  @Override
  public UpdateBuilder<BrandsFields, BrandsRow> update() {
    return new UpdateBuilderMock<>(
        BrandsFields.structure(),
        () -> new ArrayList<>(map.values()),
        UpdateParams.empty(),
        row -> row);
  }
  ;

  @Override
  public Boolean update(BrandsRow row, Connection c) {
    var shouldUpdate =
        Optional.ofNullable(map.get(row.brandId()))
            .filter(oldRow -> !oldRow.equals(row))
            .isPresent();
    if (shouldUpdate) {
      map.put(row.brandId(), row);
      ;
    }
    ;
    return shouldUpdate;
  }
  ;

  @Override
  public BrandsRow upsert(BrandsRow unsaved, Connection c) {
    map.put(unsaved.brandId(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public List<BrandsRow> upsertBatch(Iterator<BrandsRow> unsaved, Connection c) {
    var result = new ArrayList<BrandsRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.brandId(), row);
      result.add(row);
    }
    ;
    return result;
  }
  ;
}
