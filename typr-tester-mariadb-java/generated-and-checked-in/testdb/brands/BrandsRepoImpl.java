/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.brands;

import static typr.runtime.Fragment.interpolate;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import typr.dsl.DeleteBuilder;
import typr.dsl.Dialect;
import typr.dsl.SelectBuilder;
import typr.dsl.UpdateBuilder;
import typr.runtime.Fragment;
import typr.runtime.MariaTypes;

public class BrandsRepoImpl implements BrandsRepo {
  @Override
  public DeleteBuilder<BrandsFields, BrandsRow> delete() {
    return DeleteBuilder.of("`brands`", BrandsFields.structure(), Dialect.MARIADB);
  }
  ;

  @Override
  public Boolean deleteById(BrandsId brandId, Connection c) {
    return interpolate(
                Fragment.lit("delete from `brands` where `brand_id` = "),
                Fragment.encode(BrandsId.pgType, brandId),
                Fragment.lit(""))
            .update()
            .runUnchecked(c)
        > 0;
  }
  ;

  @Override
  public Integer deleteByIds(BrandsId[] brandIds, Connection c) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : brandIds) {
      fragments.add(Fragment.encode(BrandsId.pgType, id));
    }
    ;
    return Fragment.interpolate(
            Fragment.lit("delete from `brands` where `brand_id` in ("),
            Fragment.comma(fragments),
            Fragment.lit(")"))
        .update()
        .runUnchecked(c);
  }
  ;

  @Override
  public BrandsRow insert(BrandsRow unsaved, Connection c) {
    return interpolate(
            Fragment.lit(
                "insert into `brands`(`name`, `slug`, `logo_blob`, `website_url`,"
                    + " `country_of_origin`, `is_active`)\n"
                    + "values ("),
            Fragment.encode(MariaTypes.varchar, unsaved.name()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.varchar, unsaved.slug()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.mediumblob.opt(), unsaved.logoBlob()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.varchar.opt(), unsaved.websiteUrl()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.char_.opt(), unsaved.countryOfOrigin()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.bool, unsaved.isActive()),
            Fragment.lit(
                ")\n"
                    + "RETURNING `brand_id`, `name`, `slug`, `logo_blob`, `website_url`,"
                    + " `country_of_origin`, `is_active`\n"))
        .updateReturning(BrandsRow._rowParser.exactlyOne())
        .runUnchecked(c);
  }
  ;

  @Override
  public BrandsRow insert(BrandsRowUnsaved unsaved, Connection c) {
    ArrayList<Fragment> columns = new ArrayList<>();
    ;
    ArrayList<Fragment> values = new ArrayList<>();
    ;
    columns.add(Fragment.lit("`name`"));
    values.add(interpolate(Fragment.encode(MariaTypes.varchar, unsaved.name()), Fragment.lit("")));
    columns.add(Fragment.lit("`slug`"));
    values.add(interpolate(Fragment.encode(MariaTypes.varchar, unsaved.slug()), Fragment.lit("")));
    unsaved
        .logoBlob()
        .visit(
            () -> {},
            value -> {
              columns.add(Fragment.lit("`logo_blob`"));
              values.add(
                  interpolate(
                      Fragment.encode(MariaTypes.mediumblob.opt(), value), Fragment.lit("")));
            });
    ;
    unsaved
        .websiteUrl()
        .visit(
            () -> {},
            value -> {
              columns.add(Fragment.lit("`website_url`"));
              values.add(
                  interpolate(Fragment.encode(MariaTypes.varchar.opt(), value), Fragment.lit("")));
            });
    ;
    unsaved
        .countryOfOrigin()
        .visit(
            () -> {},
            value -> {
              columns.add(Fragment.lit("`country_of_origin`"));
              values.add(
                  interpolate(Fragment.encode(MariaTypes.char_.opt(), value), Fragment.lit("")));
            });
    ;
    unsaved
        .isActive()
        .visit(
            () -> {},
            value -> {
              columns.add(Fragment.lit("`is_active`"));
              values.add(interpolate(Fragment.encode(MariaTypes.bool, value), Fragment.lit("")));
            });
    ;
    Fragment q =
        interpolate(
            Fragment.lit("insert into `brands`("),
            Fragment.comma(columns),
            Fragment.lit(")\nvalues ("),
            Fragment.comma(values),
            Fragment.lit(
                ")\n"
                    + "RETURNING `brand_id`, `name`, `slug`, `logo_blob`, `website_url`,"
                    + " `country_of_origin`, `is_active`\n"));
    ;
    return q.updateReturning(BrandsRow._rowParser.exactlyOne()).runUnchecked(c);
  }
  ;

  @Override
  public SelectBuilder<BrandsFields, BrandsRow> select() {
    return SelectBuilder.of(
        "`brands`", BrandsFields.structure(), BrandsRow._rowParser, Dialect.MARIADB);
  }
  ;

  @Override
  public List<BrandsRow> selectAll(Connection c) {
    return interpolate(
            Fragment.lit(
                "select `brand_id`, `name`, `slug`, `logo_blob`, `website_url`,"
                    + " `country_of_origin`, `is_active`\n"
                    + "from `brands`\n"))
        .query(BrandsRow._rowParser.all())
        .runUnchecked(c);
  }
  ;

  @Override
  public Optional<BrandsRow> selectById(BrandsId brandId, Connection c) {
    return interpolate(
            Fragment.lit(
                "select `brand_id`, `name`, `slug`, `logo_blob`, `website_url`,"
                    + " `country_of_origin`, `is_active`\n"
                    + "from `brands`\n"
                    + "where `brand_id` = "),
            Fragment.encode(BrandsId.pgType, brandId),
            Fragment.lit(""))
        .query(BrandsRow._rowParser.first())
        .runUnchecked(c);
  }
  ;

  @Override
  public List<BrandsRow> selectByIds(BrandsId[] brandIds, Connection c) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : brandIds) {
      fragments.add(Fragment.encode(BrandsId.pgType, id));
    }
    ;
    return Fragment.interpolate(
            Fragment.lit(
                "select `brand_id`, `name`, `slug`, `logo_blob`, `website_url`,"
                    + " `country_of_origin`, `is_active` from `brands` where `brand_id` in ("),
            Fragment.comma(fragments),
            Fragment.lit(")"))
        .query(BrandsRow._rowParser.all())
        .runUnchecked(c);
  }
  ;

  @Override
  public Map<BrandsId, BrandsRow> selectByIdsTracked(BrandsId[] brandIds, Connection c) {
    HashMap<BrandsId, BrandsRow> ret = new HashMap<BrandsId, BrandsRow>();
    selectByIds(brandIds, c).forEach(row -> ret.put(row.brandId(), row));
    return ret;
  }
  ;

  @Override
  public Optional<BrandsRow> selectByUniqueSlug(String slug, Connection c) {
    return interpolate(
            Fragment.lit(
                "select `brand_id`, `name`, `slug`, `logo_blob`, `website_url`,"
                    + " `country_of_origin`, `is_active`\n"
                    + "from `brands`\n"
                    + "where `slug` = "),
            Fragment.encode(MariaTypes.varchar, slug),
            Fragment.lit("\n"))
        .query(BrandsRow._rowParser.first())
        .runUnchecked(c);
  }
  ;

  @Override
  public UpdateBuilder<BrandsFields, BrandsRow> update() {
    return UpdateBuilder.of(
        "`brands`", BrandsFields.structure(), BrandsRow._rowParser, Dialect.MARIADB);
  }
  ;

  @Override
  public Boolean update(BrandsRow row, Connection c) {
    BrandsId brandId = row.brandId();
    ;
    return interpolate(
                Fragment.lit("update `brands`\nset `name` = "),
                Fragment.encode(MariaTypes.varchar, row.name()),
                Fragment.lit(",\n`slug` = "),
                Fragment.encode(MariaTypes.varchar, row.slug()),
                Fragment.lit(",\n`logo_blob` = "),
                Fragment.encode(MariaTypes.mediumblob.opt(), row.logoBlob()),
                Fragment.lit(",\n`website_url` = "),
                Fragment.encode(MariaTypes.varchar.opt(), row.websiteUrl()),
                Fragment.lit(",\n`country_of_origin` = "),
                Fragment.encode(MariaTypes.char_.opt(), row.countryOfOrigin()),
                Fragment.lit(",\n`is_active` = "),
                Fragment.encode(MariaTypes.bool, row.isActive()),
                Fragment.lit("\nwhere `brand_id` = "),
                Fragment.encode(BrandsId.pgType, brandId),
                Fragment.lit(""))
            .update()
            .runUnchecked(c)
        > 0;
  }
  ;

  @Override
  public BrandsRow upsert(BrandsRow unsaved, Connection c) {
    return interpolate(
            Fragment.lit(
                "INSERT INTO `brands`(`name`, `slug`, `logo_blob`, `website_url`,"
                    + " `country_of_origin`, `is_active`)\n"
                    + "VALUES ("),
            Fragment.encode(MariaTypes.varchar, unsaved.name()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.varchar, unsaved.slug()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.mediumblob.opt(), unsaved.logoBlob()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.varchar.opt(), unsaved.websiteUrl()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.char_.opt(), unsaved.countryOfOrigin()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.bool, unsaved.isActive()),
            Fragment.lit(
                ")\n"
                    + "ON DUPLICATE KEY UPDATE `name` = VALUES(`name`),\n"
                    + "`slug` = VALUES(`slug`),\n"
                    + "`logo_blob` = VALUES(`logo_blob`),\n"
                    + "`website_url` = VALUES(`website_url`),\n"
                    + "`country_of_origin` = VALUES(`country_of_origin`),\n"
                    + "`is_active` = VALUES(`is_active`)\n"
                    + "RETURNING `brand_id`, `name`, `slug`, `logo_blob`, `website_url`,"
                    + " `country_of_origin`, `is_active`"))
        .updateReturning(BrandsRow._rowParser.exactlyOne())
        .runUnchecked(c);
  }
  ;

  @Override
  public List<BrandsRow> upsertBatch(Iterator<BrandsRow> unsaved, Connection c) {
    return interpolate(
            Fragment.lit(
                "INSERT INTO `brands`(`brand_id`, `name`, `slug`, `logo_blob`, `website_url`,"
                    + " `country_of_origin`, `is_active`)\n"
                    + "VALUES (?, ?, ?, ?, ?, ?, ?)\n"
                    + "ON DUPLICATE KEY UPDATE `name` = VALUES(`name`),\n"
                    + "`slug` = VALUES(`slug`),\n"
                    + "`logo_blob` = VALUES(`logo_blob`),\n"
                    + "`website_url` = VALUES(`website_url`),\n"
                    + "`country_of_origin` = VALUES(`country_of_origin`),\n"
                    + "`is_active` = VALUES(`is_active`)\n"
                    + "RETURNING `brand_id`, `name`, `slug`, `logo_blob`, `website_url`,"
                    + " `country_of_origin`, `is_active`"))
        .updateReturningEach(BrandsRow._rowParser, unsaved)
        .runUnchecked(c);
  }
  ;
}
