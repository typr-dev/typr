/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.product_images;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typr.dsl.DeleteBuilder;
import typr.dsl.DeleteBuilderMock;
import typr.dsl.DeleteParams;
import typr.dsl.SelectBuilder;
import typr.dsl.SelectBuilderMock;
import typr.dsl.SelectParams;
import typr.dsl.UpdateBuilder;
import typr.dsl.UpdateBuilderMock;
import typr.dsl.UpdateParams;

public record ProductImagesRepoMock(
    java.util.function.Function<ProductImagesRowUnsaved, ProductImagesRow> toRow,
    HashMap<ProductImagesId, ProductImagesRow> map)
    implements ProductImagesRepo {
  public ProductImagesRepoMock(
      java.util.function.Function<ProductImagesRowUnsaved, ProductImagesRow> toRow) {
    this(toRow, new HashMap<ProductImagesId, ProductImagesRow>());
  }
  ;

  public ProductImagesRepoMock withToRow(
      java.util.function.Function<ProductImagesRowUnsaved, ProductImagesRow> toRow) {
    return new ProductImagesRepoMock(toRow, map);
  }
  ;

  public ProductImagesRepoMock withMap(HashMap<ProductImagesId, ProductImagesRow> map) {
    return new ProductImagesRepoMock(toRow, map);
  }
  ;

  @Override
  public DeleteBuilder<ProductImagesFields, ProductImagesRow> delete() {
    return new DeleteBuilderMock<>(
        ProductImagesFields.structure(),
        () -> new ArrayList<>(map.values()),
        DeleteParams.empty(),
        row -> row.imageId(),
        id -> map.remove(id));
  }
  ;

  @Override
  public Boolean deleteById(ProductImagesId imageId, Connection c) {
    return Optional.ofNullable(map.remove(imageId)).isPresent();
  }
  ;

  @Override
  public Integer deleteByIds(ProductImagesId[] imageIds, Connection c) {
    var count = 0;
    for (var id : imageIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
        ;
      }
      ;
    }
    ;
    return count;
  }
  ;

  @Override
  public ProductImagesRow insert(ProductImagesRow unsaved, Connection c) {
    if (map.containsKey(unsaved.imageId())) {
      throw new RuntimeException("id " + unsaved.imageId() + " already exists");
    }
    ;
    map.put(unsaved.imageId(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public ProductImagesRow insert(ProductImagesRowUnsaved unsaved, Connection c) {
    return insert(toRow.apply(unsaved), c);
  }
  ;

  @Override
  public SelectBuilder<ProductImagesFields, ProductImagesRow> select() {
    return new SelectBuilderMock<>(
        ProductImagesFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  }
  ;

  @Override
  public List<ProductImagesRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  }
  ;

  @Override
  public Optional<ProductImagesRow> selectById(ProductImagesId imageId, Connection c) {
    return Optional.ofNullable(map.get(imageId));
  }
  ;

  @Override
  public List<ProductImagesRow> selectByIds(ProductImagesId[] imageIds, Connection c) {
    var result = new ArrayList<ProductImagesRow>();
    for (var id : imageIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
        result.add(opt.get());
      }
      ;
    }
    ;
    return result;
  }
  ;

  @Override
  public Map<ProductImagesId, ProductImagesRow> selectByIdsTracked(
      ProductImagesId[] imageIds, Connection c) {
    return selectByIds(imageIds, c).stream()
        .collect(Collectors.toMap((ProductImagesRow row) -> row.imageId(), Function.identity()));
  }
  ;

  @Override
  public UpdateBuilder<ProductImagesFields, ProductImagesRow> update() {
    return new UpdateBuilderMock<>(
        ProductImagesFields.structure(),
        () -> new ArrayList<>(map.values()),
        UpdateParams.empty(),
        row -> row);
  }
  ;

  @Override
  public Boolean update(ProductImagesRow row, Connection c) {
    var shouldUpdate =
        Optional.ofNullable(map.get(row.imageId()))
            .filter(oldRow -> !oldRow.equals(row))
            .isPresent();
    if (shouldUpdate) {
      map.put(row.imageId(), row);
      ;
    }
    ;
    return shouldUpdate;
  }
  ;

  @Override
  public ProductImagesRow upsert(ProductImagesRow unsaved, Connection c) {
    map.put(unsaved.imageId(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public List<ProductImagesRow> upsertBatch(Iterator<ProductImagesRow> unsaved, Connection c) {
    var result = new ArrayList<ProductImagesRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.imageId(), row);
      result.add(row);
    }
    ;
    return result;
  }
  ;
}
