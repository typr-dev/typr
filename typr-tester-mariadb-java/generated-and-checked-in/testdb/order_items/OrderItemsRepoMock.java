/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.order_items;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typr.dsl.DeleteBuilder;
import typr.dsl.DeleteBuilderMock;
import typr.dsl.DeleteParams;
import typr.dsl.SelectBuilder;
import typr.dsl.SelectBuilderMock;
import typr.dsl.SelectParams;
import typr.dsl.UpdateBuilder;
import typr.dsl.UpdateBuilderMock;
import typr.dsl.UpdateParams;

public record OrderItemsRepoMock(
    java.util.function.Function<OrderItemsRowUnsaved, OrderItemsRow> toRow,
    HashMap<OrderItemsId, OrderItemsRow> map)
    implements OrderItemsRepo {
  public OrderItemsRepoMock(
      java.util.function.Function<OrderItemsRowUnsaved, OrderItemsRow> toRow) {
    this(toRow, new HashMap<OrderItemsId, OrderItemsRow>());
  }
  ;

  public OrderItemsRepoMock withToRow(
      java.util.function.Function<OrderItemsRowUnsaved, OrderItemsRow> toRow) {
    return new OrderItemsRepoMock(toRow, map);
  }
  ;

  public OrderItemsRepoMock withMap(HashMap<OrderItemsId, OrderItemsRow> map) {
    return new OrderItemsRepoMock(toRow, map);
  }
  ;

  @Override
  public DeleteBuilder<OrderItemsFields, OrderItemsRow> delete() {
    return new DeleteBuilderMock<>(
        OrderItemsFields.structure(),
        () -> new ArrayList<>(map.values()),
        DeleteParams.empty(),
        row -> row.itemId(),
        id -> map.remove(id));
  }
  ;

  @Override
  public Boolean deleteById(OrderItemsId itemId, Connection c) {
    return Optional.ofNullable(map.remove(itemId)).isPresent();
  }
  ;

  @Override
  public Integer deleteByIds(OrderItemsId[] itemIds, Connection c) {
    var count = 0;
    for (var id : itemIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
        ;
      }
      ;
    }
    ;
    return count;
  }
  ;

  @Override
  public OrderItemsRow insert(OrderItemsRow unsaved, Connection c) {
    if (map.containsKey(unsaved.itemId())) {
      throw new RuntimeException("id " + unsaved.itemId() + " already exists");
    }
    ;
    map.put(unsaved.itemId(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public OrderItemsRow insert(OrderItemsRowUnsaved unsaved, Connection c) {
    return insert(toRow.apply(unsaved), c);
  }
  ;

  @Override
  public SelectBuilder<OrderItemsFields, OrderItemsRow> select() {
    return new SelectBuilderMock<>(
        OrderItemsFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  }
  ;

  @Override
  public List<OrderItemsRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  }
  ;

  @Override
  public Optional<OrderItemsRow> selectById(OrderItemsId itemId, Connection c) {
    return Optional.ofNullable(map.get(itemId));
  }
  ;

  @Override
  public List<OrderItemsRow> selectByIds(OrderItemsId[] itemIds, Connection c) {
    var result = new ArrayList<OrderItemsRow>();
    for (var id : itemIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
        result.add(opt.get());
      }
      ;
    }
    ;
    return result;
  }
  ;

  @Override
  public Map<OrderItemsId, OrderItemsRow> selectByIdsTracked(OrderItemsId[] itemIds, Connection c) {
    return selectByIds(itemIds, c).stream()
        .collect(Collectors.toMap((OrderItemsRow row) -> row.itemId(), Function.identity()));
  }
  ;

  @Override
  public UpdateBuilder<OrderItemsFields, OrderItemsRow> update() {
    return new UpdateBuilderMock<>(
        OrderItemsFields.structure(),
        () -> new ArrayList<>(map.values()),
        UpdateParams.empty(),
        row -> row);
  }
  ;

  @Override
  public Boolean update(OrderItemsRow row, Connection c) {
    var shouldUpdate =
        Optional.ofNullable(map.get(row.itemId()))
            .filter(oldRow -> !oldRow.equals(row))
            .isPresent();
    if (shouldUpdate) {
      map.put(row.itemId(), row);
      ;
    }
    ;
    return shouldUpdate;
  }
  ;

  @Override
  public OrderItemsRow upsert(OrderItemsRow unsaved, Connection c) {
    map.put(unsaved.itemId(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public List<OrderItemsRow> upsertBatch(Iterator<OrderItemsRow> unsaved, Connection c) {
    var result = new ArrayList<OrderItemsRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.itemId(), row);
      result.add(row);
    }
    ;
    return result;
  }
  ;
}
