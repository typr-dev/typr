/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.audit_log;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typr.dsl.DeleteBuilder;
import typr.dsl.DeleteBuilderMock;
import typr.dsl.DeleteParams;
import typr.dsl.SelectBuilder;
import typr.dsl.SelectBuilderMock;
import typr.dsl.SelectParams;
import typr.dsl.UpdateBuilder;
import typr.dsl.UpdateBuilderMock;
import typr.dsl.UpdateParams;

public record AuditLogRepoMock(
    java.util.function.Function<AuditLogRowUnsaved, AuditLogRow> toRow,
    HashMap<AuditLogId, AuditLogRow> map)
    implements AuditLogRepo {
  public AuditLogRepoMock(java.util.function.Function<AuditLogRowUnsaved, AuditLogRow> toRow) {
    this(toRow, new HashMap<AuditLogId, AuditLogRow>());
  }
  ;

  public AuditLogRepoMock withToRow(
      java.util.function.Function<AuditLogRowUnsaved, AuditLogRow> toRow) {
    return new AuditLogRepoMock(toRow, map);
  }
  ;

  public AuditLogRepoMock withMap(HashMap<AuditLogId, AuditLogRow> map) {
    return new AuditLogRepoMock(toRow, map);
  }
  ;

  @Override
  public DeleteBuilder<AuditLogFields, AuditLogRow> delete() {
    return new DeleteBuilderMock<>(
        AuditLogFields.structure(),
        () -> new ArrayList<>(map.values()),
        DeleteParams.empty(),
        row -> row.logId(),
        id -> map.remove(id));
  }
  ;

  @Override
  public Boolean deleteById(AuditLogId logId, Connection c) {
    return Optional.ofNullable(map.remove(logId)).isPresent();
  }
  ;

  @Override
  public Integer deleteByIds(AuditLogId[] logIds, Connection c) {
    var count = 0;
    for (var id : logIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
        ;
      }
      ;
    }
    ;
    return count;
  }
  ;

  @Override
  public AuditLogRow insert(AuditLogRow unsaved, Connection c) {
    if (map.containsKey(unsaved.logId())) {
      throw new RuntimeException("id " + unsaved.logId() + " already exists");
    }
    ;
    map.put(unsaved.logId(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public AuditLogRow insert(AuditLogRowUnsaved unsaved, Connection c) {
    return insert(toRow.apply(unsaved), c);
  }
  ;

  @Override
  public SelectBuilder<AuditLogFields, AuditLogRow> select() {
    return new SelectBuilderMock<>(
        AuditLogFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  }
  ;

  @Override
  public List<AuditLogRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  }
  ;

  @Override
  public Optional<AuditLogRow> selectById(AuditLogId logId, Connection c) {
    return Optional.ofNullable(map.get(logId));
  }
  ;

  @Override
  public List<AuditLogRow> selectByIds(AuditLogId[] logIds, Connection c) {
    var result = new ArrayList<AuditLogRow>();
    for (var id : logIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
        result.add(opt.get());
      }
      ;
    }
    ;
    return result;
  }
  ;

  @Override
  public Map<AuditLogId, AuditLogRow> selectByIdsTracked(AuditLogId[] logIds, Connection c) {
    return selectByIds(logIds, c).stream()
        .collect(Collectors.toMap((AuditLogRow row) -> row.logId(), Function.identity()));
  }
  ;

  @Override
  public UpdateBuilder<AuditLogFields, AuditLogRow> update() {
    return new UpdateBuilderMock<>(
        AuditLogFields.structure(),
        () -> new ArrayList<>(map.values()),
        UpdateParams.empty(),
        row -> row);
  }
  ;

  @Override
  public Boolean update(AuditLogRow row, Connection c) {
    var shouldUpdate =
        Optional.ofNullable(map.get(row.logId())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.logId(), row);
      ;
    }
    ;
    return shouldUpdate;
  }
  ;

  @Override
  public AuditLogRow upsert(AuditLogRow unsaved, Connection c) {
    map.put(unsaved.logId(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public List<AuditLogRow> upsertBatch(Iterator<AuditLogRow> unsaved, Connection c) {
    var result = new ArrayList<AuditLogRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.logId(), row);
      result.add(row);
    }
    ;
    return result;
  }
  ;
}
