/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.price_tiers;

import static typr.runtime.Fragment.interpolate;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import typr.dsl.DeleteBuilder;
import typr.dsl.Dialect;
import typr.dsl.SelectBuilder;
import typr.dsl.UpdateBuilder;
import typr.runtime.Fragment;
import typr.runtime.MariaTypes;

public class PriceTiersRepoImpl implements PriceTiersRepo {
  @Override
  public DeleteBuilder<PriceTiersFields, PriceTiersRow> delete() {
    return DeleteBuilder.of("`price_tiers`", PriceTiersFields.structure(), Dialect.MARIADB);
  }
  ;

  @Override
  public Boolean deleteById(PriceTiersId tierId, Connection c) {
    return interpolate(
                Fragment.lit("delete from `price_tiers` where `tier_id` = "),
                Fragment.encode(PriceTiersId.pgType, tierId),
                Fragment.lit(""))
            .update()
            .runUnchecked(c)
        > 0;
  }
  ;

  @Override
  public Integer deleteByIds(PriceTiersId[] tierIds, Connection c) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : tierIds) {
      fragments.add(Fragment.encode(PriceTiersId.pgType, id));
    }
    ;
    return Fragment.interpolate(
            Fragment.lit("delete from `price_tiers` where `tier_id` in ("),
            Fragment.comma(fragments),
            Fragment.lit(")"))
        .update()
        .runUnchecked(c);
  }
  ;

  @Override
  public PriceTiersRow insert(PriceTiersRow unsaved, Connection c) {
    return interpolate(
            Fragment.lit(
                "insert into `price_tiers`(`name`, `min_quantity`, `discount_type`,"
                    + " `discount_value`)\n"
                    + "values ("),
            Fragment.encode(MariaTypes.varchar, unsaved.name()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.intUnsigned, unsaved.minQuantity()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.text, unsaved.discountType()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.numeric, unsaved.discountValue()),
            Fragment.lit(
                ")\n"
                    + "RETURNING `tier_id`, `name`, `min_quantity`, `discount_type`,"
                    + " `discount_value`\n"))
        .updateReturning(PriceTiersRow._rowParser.exactlyOne())
        .runUnchecked(c);
  }
  ;

  @Override
  public PriceTiersRow insert(PriceTiersRowUnsaved unsaved, Connection c) {
    ArrayList<Fragment> columns = new ArrayList<>();
    ;
    ArrayList<Fragment> values = new ArrayList<>();
    ;
    columns.add(Fragment.lit("`name`"));
    values.add(interpolate(Fragment.encode(MariaTypes.varchar, unsaved.name()), Fragment.lit("")));
    columns.add(Fragment.lit("`discount_type`"));
    values.add(
        interpolate(Fragment.encode(MariaTypes.text, unsaved.discountType()), Fragment.lit("")));
    columns.add(Fragment.lit("`discount_value`"));
    values.add(
        interpolate(
            Fragment.encode(MariaTypes.numeric, unsaved.discountValue()), Fragment.lit("")));
    unsaved
        .minQuantity()
        .visit(
            () -> {},
            value -> {
              columns.add(Fragment.lit("`min_quantity`"));
              values.add(
                  interpolate(Fragment.encode(MariaTypes.intUnsigned, value), Fragment.lit("")));
            });
    ;
    Fragment q =
        interpolate(
            Fragment.lit("insert into `price_tiers`("),
            Fragment.comma(columns),
            Fragment.lit(")\nvalues ("),
            Fragment.comma(values),
            Fragment.lit(
                ")\n"
                    + "RETURNING `tier_id`, `name`, `min_quantity`, `discount_type`,"
                    + " `discount_value`\n"));
    ;
    return q.updateReturning(PriceTiersRow._rowParser.exactlyOne()).runUnchecked(c);
  }
  ;

  @Override
  public SelectBuilder<PriceTiersFields, PriceTiersRow> select() {
    return SelectBuilder.of(
        "`price_tiers`", PriceTiersFields.structure(), PriceTiersRow._rowParser, Dialect.MARIADB);
  }
  ;

  @Override
  public List<PriceTiersRow> selectAll(Connection c) {
    return interpolate(
            Fragment.lit(
                "select `tier_id`, `name`, `min_quantity`, `discount_type`, `discount_value`\n"
                    + "from `price_tiers`\n"))
        .query(PriceTiersRow._rowParser.all())
        .runUnchecked(c);
  }
  ;

  @Override
  public Optional<PriceTiersRow> selectById(PriceTiersId tierId, Connection c) {
    return interpolate(
            Fragment.lit(
                "select `tier_id`, `name`, `min_quantity`, `discount_type`, `discount_value`\n"
                    + "from `price_tiers`\n"
                    + "where `tier_id` = "),
            Fragment.encode(PriceTiersId.pgType, tierId),
            Fragment.lit(""))
        .query(PriceTiersRow._rowParser.first())
        .runUnchecked(c);
  }
  ;

  @Override
  public List<PriceTiersRow> selectByIds(PriceTiersId[] tierIds, Connection c) {
    ArrayList<Fragment> fragments = new ArrayList<>();
    for (var id : tierIds) {
      fragments.add(Fragment.encode(PriceTiersId.pgType, id));
    }
    ;
    return Fragment.interpolate(
            Fragment.lit(
                "select `tier_id`, `name`, `min_quantity`, `discount_type`, `discount_value` from"
                    + " `price_tiers` where `tier_id` in ("),
            Fragment.comma(fragments),
            Fragment.lit(")"))
        .query(PriceTiersRow._rowParser.all())
        .runUnchecked(c);
  }
  ;

  @Override
  public Map<PriceTiersId, PriceTiersRow> selectByIdsTracked(PriceTiersId[] tierIds, Connection c) {
    HashMap<PriceTiersId, PriceTiersRow> ret = new HashMap<PriceTiersId, PriceTiersRow>();
    selectByIds(tierIds, c).forEach(row -> ret.put(row.tierId(), row));
    return ret;
  }
  ;

  @Override
  public UpdateBuilder<PriceTiersFields, PriceTiersRow> update() {
    return UpdateBuilder.of(
        "`price_tiers`", PriceTiersFields.structure(), PriceTiersRow._rowParser, Dialect.MARIADB);
  }
  ;

  @Override
  public Boolean update(PriceTiersRow row, Connection c) {
    PriceTiersId tierId = row.tierId();
    ;
    return interpolate(
                Fragment.lit("update `price_tiers`\nset `name` = "),
                Fragment.encode(MariaTypes.varchar, row.name()),
                Fragment.lit(",\n`min_quantity` = "),
                Fragment.encode(MariaTypes.intUnsigned, row.minQuantity()),
                Fragment.lit(",\n`discount_type` = "),
                Fragment.encode(MariaTypes.text, row.discountType()),
                Fragment.lit(",\n`discount_value` = "),
                Fragment.encode(MariaTypes.numeric, row.discountValue()),
                Fragment.lit("\nwhere `tier_id` = "),
                Fragment.encode(PriceTiersId.pgType, tierId),
                Fragment.lit(""))
            .update()
            .runUnchecked(c)
        > 0;
  }
  ;

  @Override
  public PriceTiersRow upsert(PriceTiersRow unsaved, Connection c) {
    return interpolate(
            Fragment.lit(
                "INSERT INTO `price_tiers`(`name`, `min_quantity`, `discount_type`,"
                    + " `discount_value`)\n"
                    + "VALUES ("),
            Fragment.encode(MariaTypes.varchar, unsaved.name()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.intUnsigned, unsaved.minQuantity()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.text, unsaved.discountType()),
            Fragment.lit(", "),
            Fragment.encode(MariaTypes.numeric, unsaved.discountValue()),
            Fragment.lit(
                ")\n"
                    + "ON DUPLICATE KEY UPDATE `name` = VALUES(`name`),\n"
                    + "`min_quantity` = VALUES(`min_quantity`),\n"
                    + "`discount_type` = VALUES(`discount_type`),\n"
                    + "`discount_value` = VALUES(`discount_value`)\n"
                    + "RETURNING `tier_id`, `name`, `min_quantity`, `discount_type`,"
                    + " `discount_value`"))
        .updateReturning(PriceTiersRow._rowParser.exactlyOne())
        .runUnchecked(c);
  }
  ;

  @Override
  public List<PriceTiersRow> upsertBatch(Iterator<PriceTiersRow> unsaved, Connection c) {
    return interpolate(
            Fragment.lit(
                "INSERT INTO `price_tiers`(`tier_id`, `name`, `min_quantity`, `discount_type`,"
                    + " `discount_value`)\n"
                    + "VALUES (?, ?, ?, ?, ?)\n"
                    + "ON DUPLICATE KEY UPDATE `name` = VALUES(`name`),\n"
                    + "`min_quantity` = VALUES(`min_quantity`),\n"
                    + "`discount_type` = VALUES(`discount_type`),\n"
                    + "`discount_value` = VALUES(`discount_value`)\n"
                    + "RETURNING `tier_id`, `name`, `min_quantity`, `discount_type`,"
                    + " `discount_value`"))
        .updateReturningEach(PriceTiersRow._rowParser, unsaved)
        .runUnchecked(c);
  }
  ;
}
