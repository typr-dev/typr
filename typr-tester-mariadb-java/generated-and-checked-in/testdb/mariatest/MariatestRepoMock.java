/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.mariatest;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typr.dsl.DeleteBuilder;
import typr.dsl.DeleteBuilderMock;
import typr.dsl.DeleteParams;
import typr.dsl.SelectBuilder;
import typr.dsl.SelectBuilderMock;
import typr.dsl.SelectParams;
import typr.dsl.UpdateBuilder;
import typr.dsl.UpdateBuilderMock;
import typr.dsl.UpdateParams;

public record MariatestRepoMock(
    java.util.function.Function<MariatestRowUnsaved, MariatestRow> toRow,
    HashMap<MariatestId, MariatestRow> map)
    implements MariatestRepo {
  public MariatestRepoMock(java.util.function.Function<MariatestRowUnsaved, MariatestRow> toRow) {
    this(toRow, new HashMap<MariatestId, MariatestRow>());
  }
  ;

  public MariatestRepoMock withToRow(
      java.util.function.Function<MariatestRowUnsaved, MariatestRow> toRow) {
    return new MariatestRepoMock(toRow, map);
  }
  ;

  public MariatestRepoMock withMap(HashMap<MariatestId, MariatestRow> map) {
    return new MariatestRepoMock(toRow, map);
  }
  ;

  @Override
  public DeleteBuilder<MariatestFields, MariatestRow> delete() {
    return new DeleteBuilderMock<>(
        MariatestFields.structure(),
        () -> new ArrayList<>(map.values()),
        DeleteParams.empty(),
        row -> row.intCol(),
        id -> map.remove(id));
  }
  ;

  @Override
  public Boolean deleteById(MariatestId intCol, Connection c) {
    return Optional.ofNullable(map.remove(intCol)).isPresent();
  }
  ;

  @Override
  public Integer deleteByIds(MariatestId[] intCols, Connection c) {
    var count = 0;
    for (var id : intCols) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
        ;
      }
      ;
    }
    ;
    return count;
  }
  ;

  @Override
  public MariatestRow insert(MariatestRow unsaved, Connection c) {
    if (map.containsKey(unsaved.intCol())) {
      throw new RuntimeException("id " + unsaved.intCol() + " already exists");
    }
    ;
    map.put(unsaved.intCol(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public MariatestRow insert(MariatestRowUnsaved unsaved, Connection c) {
    return insert(toRow.apply(unsaved), c);
  }
  ;

  @Override
  public SelectBuilder<MariatestFields, MariatestRow> select() {
    return new SelectBuilderMock<>(
        MariatestFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  }
  ;

  @Override
  public List<MariatestRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  }
  ;

  @Override
  public Optional<MariatestRow> selectById(MariatestId intCol, Connection c) {
    return Optional.ofNullable(map.get(intCol));
  }
  ;

  @Override
  public List<MariatestRow> selectByIds(MariatestId[] intCols, Connection c) {
    var result = new ArrayList<MariatestRow>();
    for (var id : intCols) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
        result.add(opt.get());
      }
      ;
    }
    ;
    return result;
  }
  ;

  @Override
  public Map<MariatestId, MariatestRow> selectByIdsTracked(MariatestId[] intCols, Connection c) {
    return selectByIds(intCols, c).stream()
        .collect(Collectors.toMap((MariatestRow row) -> row.intCol(), Function.identity()));
  }
  ;

  @Override
  public UpdateBuilder<MariatestFields, MariatestRow> update() {
    return new UpdateBuilderMock<>(
        MariatestFields.structure(),
        () -> new ArrayList<>(map.values()),
        UpdateParams.empty(),
        row -> row);
  }
  ;

  @Override
  public Boolean update(MariatestRow row, Connection c) {
    var shouldUpdate =
        Optional.ofNullable(map.get(row.intCol()))
            .filter(oldRow -> !oldRow.equals(row))
            .isPresent();
    if (shouldUpdate) {
      map.put(row.intCol(), row);
      ;
    }
    ;
    return shouldUpdate;
  }
  ;

  @Override
  public MariatestRow upsert(MariatestRow unsaved, Connection c) {
    map.put(unsaved.intCol(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public List<MariatestRow> upsertBatch(Iterator<MariatestRow> unsaved, Connection c) {
    var result = new ArrayList<MariatestRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.intCol(), row);
      result.add(row);
    }
    ;
    return result;
  }
  ;
}
