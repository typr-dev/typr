/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.inventory;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import testdb.products.ProductsId;
import testdb.warehouses.WarehousesId;
import typr.dsl.DeleteBuilder;
import typr.dsl.DeleteBuilderMock;
import typr.dsl.DeleteParams;
import typr.dsl.SelectBuilder;
import typr.dsl.SelectBuilderMock;
import typr.dsl.SelectParams;
import typr.dsl.UpdateBuilder;
import typr.dsl.UpdateBuilderMock;
import typr.dsl.UpdateParams;

public record InventoryRepoMock(
    java.util.function.Function<InventoryRowUnsaved, InventoryRow> toRow,
    HashMap<InventoryId, InventoryRow> map)
    implements InventoryRepo {
  public InventoryRepoMock(java.util.function.Function<InventoryRowUnsaved, InventoryRow> toRow) {
    this(toRow, new HashMap<InventoryId, InventoryRow>());
  }
  ;

  public InventoryRepoMock withToRow(
      java.util.function.Function<InventoryRowUnsaved, InventoryRow> toRow) {
    return new InventoryRepoMock(toRow, map);
  }
  ;

  public InventoryRepoMock withMap(HashMap<InventoryId, InventoryRow> map) {
    return new InventoryRepoMock(toRow, map);
  }
  ;

  @Override
  public DeleteBuilder<InventoryFields, InventoryRow> delete() {
    return new DeleteBuilderMock<>(
        InventoryFields.structure(),
        () -> new ArrayList<>(map.values()),
        DeleteParams.empty(),
        row -> row.inventoryId(),
        id -> map.remove(id));
  }
  ;

  @Override
  public Boolean deleteById(InventoryId inventoryId, Connection c) {
    return Optional.ofNullable(map.remove(inventoryId)).isPresent();
  }
  ;

  @Override
  public Integer deleteByIds(InventoryId[] inventoryIds, Connection c) {
    var count = 0;
    for (var id : inventoryIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
        ;
      }
      ;
    }
    ;
    return count;
  }
  ;

  @Override
  public InventoryRow insert(InventoryRow unsaved, Connection c) {
    if (map.containsKey(unsaved.inventoryId())) {
      throw new RuntimeException("id " + unsaved.inventoryId() + " already exists");
    }
    ;
    map.put(unsaved.inventoryId(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public InventoryRow insert(InventoryRowUnsaved unsaved, Connection c) {
    return insert(toRow.apply(unsaved), c);
  }
  ;

  @Override
  public SelectBuilder<InventoryFields, InventoryRow> select() {
    return new SelectBuilderMock<>(
        InventoryFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  }
  ;

  @Override
  public List<InventoryRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  }
  ;

  @Override
  public Optional<InventoryRow> selectById(InventoryId inventoryId, Connection c) {
    return Optional.ofNullable(map.get(inventoryId));
  }
  ;

  @Override
  public List<InventoryRow> selectByIds(InventoryId[] inventoryIds, Connection c) {
    var result = new ArrayList<InventoryRow>();
    for (var id : inventoryIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
        result.add(opt.get());
      }
      ;
    }
    ;
    return result;
  }
  ;

  @Override
  public Map<InventoryId, InventoryRow> selectByIdsTracked(
      InventoryId[] inventoryIds, Connection c) {
    return selectByIds(inventoryIds, c).stream()
        .collect(Collectors.toMap((InventoryRow row) -> row.inventoryId(), Function.identity()));
  }
  ;

  @Override
  public Optional<InventoryRow> selectByUniqueProductIdAndWarehouseId(
      ProductsId productId, WarehousesId warehouseId, Connection c) {
    return new ArrayList<>(map.values())
        .stream()
            .filter(v -> productId.equals(v.productId()) && warehouseId.equals(v.warehouseId()))
            .findFirst();
  }
  ;

  @Override
  public UpdateBuilder<InventoryFields, InventoryRow> update() {
    return new UpdateBuilderMock<>(
        InventoryFields.structure(),
        () -> new ArrayList<>(map.values()),
        UpdateParams.empty(),
        row -> row);
  }
  ;

  @Override
  public Boolean update(InventoryRow row, Connection c) {
    var shouldUpdate =
        Optional.ofNullable(map.get(row.inventoryId()))
            .filter(oldRow -> !oldRow.equals(row))
            .isPresent();
    if (shouldUpdate) {
      map.put(row.inventoryId(), row);
      ;
    }
    ;
    return shouldUpdate;
  }
  ;

  @Override
  public InventoryRow upsert(InventoryRow unsaved, Connection c) {
    map.put(unsaved.inventoryId(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public List<InventoryRow> upsertBatch(Iterator<InventoryRow> unsaved, Connection c) {
    var result = new ArrayList<InventoryRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.inventoryId(), row);
      result.add(row);
    }
    ;
    return result;
  }
  ;
}
