/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.customtypes

import com.fasterxml.jackson.annotation.JsonValue
import java.time.OffsetTime
import java.time.format.DateTimeFormatter
import java.time.format.DateTimeFormatterBuilder
import java.time.temporal.ChronoField
import java.time.temporal.ChronoUnit
import typo.dsl.Bijection
import typo.runtime.PgRead
import typo.runtime.PgText
import typo.runtime.PgType
import typo.runtime.PgTypes
import typo.runtime.PgWrite
import typo.runtime.internal.arrayMap

/** This is `java.time.OffsetTime`, but with microsecond precision and transferred to and from postgres as strings. The reason is that postgres driver and db libs are broken */
data class TypoOffsetTime(@JsonValue val value: OffsetTime) {
  companion object {
    fun apply(value: OffsetTime): TypoOffsetTime = TypoOffsetTime(value.truncatedTo(ChronoUnit.MICROS))

    fun apply(str: String): TypoOffsetTime = TypoOffsetTime.apply(OffsetTime.parse(str, parser))

    val bijection: Bijection<TypoOffsetTime, OffsetTime> =
      Bijection.of(TypoOffsetTime::value, ::TypoOffsetTime)

    fun now(): TypoOffsetTime = TypoOffsetTime.apply(OffsetTime.now())

    val parser: DateTimeFormatter = DateTimeFormatterBuilder().appendPattern("HH:mm:ss").appendFraction(ChronoField.MICRO_OF_SECOND, 0, 6, true).appendPattern("X").toFormatter()

    val pgText: PgText<TypoOffsetTime> =
      PgText.textString.contramap({ v -> v.value.toString() })

    val pgType: PgType<TypoOffsetTime> =
      PgTypes.text.bimap({ v: String -> TypoOffsetTime(OffsetTime.parse(v, parser)) }, { v: TypoOffsetTime -> v.value.toString() }).renamed("timetz")

    val pgTypeArray: PgType<Array<TypoOffsetTime>> =
      TypoOffsetTime.pgType.array(PgRead.massageJdbcArrayTo(Array<String>::class.java).map({ xs: Array<String> -> arrayMap.map(xs, { v: String -> TypoOffsetTime(OffsetTime.parse(v, parser)) }, TypoOffsetTime::class.java) }), PgWrite.passObjectToJdbc<String>().array(TypoOffsetTime.pgType.typename().`as`<String>()).contramap({ xs: Array<TypoOffsetTime> -> arrayMap.map(xs, { v: TypoOffsetTime -> v.value.toString() }, String::class.java) }))
  }
}