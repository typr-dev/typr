/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.customtypes

import com.fasterxml.jackson.annotation.JsonValue
import java.time.LocalDate
import typo.dsl.Bijection
import typo.runtime.PgRead
import typo.runtime.PgText
import typo.runtime.PgType
import typo.runtime.PgTypes
import typo.runtime.PgWrite
import typo.runtime.internal.arrayMap

/** This is `java.time.LocalDate`, but transferred to and from postgres as strings. The reason is that postgres driver and db libs are broken */
data class TypoLocalDate(@JsonValue val value: LocalDate) {
  companion object {
    fun apply(str: String): TypoLocalDate = TypoLocalDate(LocalDate.parse(str))

    val bijection: Bijection<TypoLocalDate, LocalDate> =
      Bijection.of(TypoLocalDate::value, ::TypoLocalDate)

    fun now(): TypoLocalDate = TypoLocalDate(LocalDate.now())

    val pgText: PgText<TypoLocalDate> =
      PgText.textString.contramap({ v -> v.value.toString() })

    val pgType: PgType<TypoLocalDate> =
      PgTypes.text.bimap({ v: String -> TypoLocalDate(LocalDate.parse(v)) }, { v: TypoLocalDate -> v.value.toString() }).renamed("date")

    val pgTypeArray: PgType<Array<TypoLocalDate>> =
      TypoLocalDate.pgType.array(PgRead.massageJdbcArrayTo(Array<String>::class.java).map({ xs: Array<String> -> arrayMap.map(xs, { v: String -> TypoLocalDate(LocalDate.parse(v)) }, TypoLocalDate::class.java) }), PgWrite.passObjectToJdbc<String>().array(TypoLocalDate.pgType.typename().`as`<String>()).contramap({ xs: Array<TypoLocalDate> -> arrayMap.map(xs, { v: TypoLocalDate -> v.value.toString() }, String::class.java) }))
  }
}