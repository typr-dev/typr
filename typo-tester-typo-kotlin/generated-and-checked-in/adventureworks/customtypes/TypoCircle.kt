/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.customtypes

import org.postgresql.geometric.PGcircle
import typo.runtime.PgRead
import typo.runtime.PgText
import typo.runtime.PgType
import typo.runtime.PgWrite
import typo.runtime.internal.arrayMap
import typo.runtime.internal.stringInterpolator.str

/** This represents circle datatype in PostgreSQL, consisting of a point and a radius */
data class TypoCircle(
  val center: TypoPoint,
  val radius: Double
) {
  companion object {
    val pgText: PgText<TypoCircle> =
      PgText.textString.contramap({ v -> str("<(", v.center.x.toString(), ",", v.center.y.toString(), "),", v.radius.toString(), ">") })

    val pgType: PgType<TypoCircle> =
      PgType.of<TypoCircle>(
        "circle",
        PgRead.castJdbcObjectTo(PGcircle::class.java).map({ v: PGcircle -> TypoCircle(TypoPoint(v.center!!.x, v.center!!.y), v.radius) }),
        PgWrite.passObjectToJdbc<PGcircle>().contramap({ v: TypoCircle -> PGcircle(v.center.x, v.center.y, v.radius) }),
        TypoCircle.pgText
      )

    val pgTypeArray: PgType<Array<TypoCircle>> =
      TypoCircle.pgType.array(PgRead.castJdbcArrayTo(PGcircle::class.java).map({ xs: Array<PGcircle> -> arrayMap.map(xs, { v: PGcircle -> TypoCircle(TypoPoint(v.center!!.x, v.center!!.y), v.radius) }, TypoCircle::class.java) }), PgWrite.passObjectToJdbc<PGcircle>().array(TypoCircle.pgType.typename().`as`<PGcircle>()).contramap({ xs: Array<TypoCircle> -> arrayMap.map(xs, { v: TypoCircle -> PGcircle(v.center.x, v.center.y, v.radius) }, PGcircle::class.java) }))
  }
}