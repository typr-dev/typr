/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.customtypes

import org.postgresql.geometric.PGbox
import typo.runtime.PgRead
import typo.runtime.PgText
import typo.runtime.PgType
import typo.runtime.PgWrite
import typo.runtime.internal.arrayMap
import typo.runtime.internal.stringInterpolator.str

/** This represents the box datatype in PostgreSQL */
data class TypoBox(
  val x1: Double,
  val y1: Double,
  val x2: Double,
  val y2: Double
) {
  companion object {
    val pgText: PgText<TypoBox> =
      PgText.textString.contramap({ v -> str("((", v.x1.toString(), ",", v.y1.toString(), "),(", v.x2.toString(), ",", v.y2.toString(), "))") })

    val pgType: PgType<TypoBox> =
      PgType.of<TypoBox>(
        "box",
        PgRead.castJdbcObjectTo(PGbox::class.java).map({ v: PGbox -> TypoBox(v.point!![0].x, v.point!![0].y, v.point!![1].x, v.point!![1].y) }),
        PgWrite.passObjectToJdbc<PGbox>().contramap({ v: TypoBox -> PGbox(v.x1, v.y1, v.x2, v.y2) }),
        TypoBox.pgText
      )

    val pgTypeArray: PgType<Array<TypoBox>> =
      TypoBox.pgType.array(PgRead.castJdbcArrayTo(PGbox::class.java).map({ xs: Array<PGbox> -> arrayMap.map(xs, { v: PGbox -> TypoBox(v.point!![0].x, v.point!![0].y, v.point!![1].x, v.point!![1].y) }, TypoBox::class.java) }), PgWrite.passObjectToJdbc<PGbox>().array(TypoBox.pgType.typename().`as`<PGbox>()).contramap({ xs: Array<TypoBox> -> arrayMap.map(xs, { v: TypoBox -> PGbox(v.x1, v.y1, v.x2, v.y2) }, PGbox::class.java) }))
  }
}