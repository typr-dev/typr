/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.customtypes

import com.fasterxml.jackson.annotation.JsonValue
import java.time.Instant
import java.time.OffsetDateTime
import java.time.format.DateTimeFormatter
import java.time.format.DateTimeFormatterBuilder
import java.time.temporal.ChronoField
import java.time.temporal.ChronoUnit
import typo.dsl.Bijection
import typo.runtime.PgRead
import typo.runtime.PgText
import typo.runtime.PgType
import typo.runtime.PgTypes
import typo.runtime.PgWrite
import typo.runtime.internal.arrayMap

/** This is `java.time.TypoInstant`, but with microsecond precision and transferred to and from postgres as strings. The reason is that postgres driver and db libs are broken */
data class TypoInstant(@JsonValue val value: Instant) {
  companion object {
    fun apply(value: Instant): TypoInstant = TypoInstant(value.truncatedTo(ChronoUnit.MICROS))

    fun apply(str: String): TypoInstant = TypoInstant.apply(OffsetDateTime.parse(str, parser).toInstant())

    val bijection: Bijection<TypoInstant, Instant> =
      Bijection.of(TypoInstant::value, ::TypoInstant)

    fun now(): TypoInstant = TypoInstant.apply(Instant.now())

    val parser: DateTimeFormatter = DateTimeFormatterBuilder().appendPattern("yyyy-MM-dd HH:mm:ss").appendFraction(ChronoField.MICRO_OF_SECOND, 0, 6, true).appendPattern("X").toFormatter()

    val pgText: PgText<TypoInstant> =
      PgText.textString.contramap({ v -> v.value.toString() })

    val pgType: PgType<TypoInstant> =
      PgTypes.text.bimap({ v: String -> apply(v) }, { v: TypoInstant -> v.value.toString() }).renamed("timestamptz")

    val pgTypeArray: PgType<Array<TypoInstant>> =
      TypoInstant.pgType.array(PgRead.massageJdbcArrayTo(Array<String>::class.java).map({ xs: Array<String> -> arrayMap.map(xs, { v: String -> apply(v) }, TypoInstant::class.java) }), PgWrite.passObjectToJdbc<String>().array(TypoInstant.pgType.typename().`as`<String>()).contramap({ xs: Array<TypoInstant> -> arrayMap.map(xs, { v: TypoInstant -> v.value.toString() }, String::class.java) }))
  }
}