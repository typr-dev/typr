/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.address

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.Optional
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableIterator
import kotlin.collections.MutableMap
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import typo.runtime.internal.stringInterpolator.str

data class AddressRepoMock(
  val toRow: (AddressRowUnsaved) -> AddressRow,
  val map: MutableMap<AddressId, AddressRow> = mutableMapOf<AddressId, AddressRow>()
) : AddressRepo {
  override fun delete(): DeleteBuilder<AddressFields, AddressRow> = DeleteBuilderMock(AddressFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.addressid }, { id -> map.remove(id) })

  override fun deleteById(
    addressid: AddressId,
    c: Connection
  ): Boolean = Optional.ofNullable(map.remove(addressid)).isPresent()

  override fun deleteByIds(
    addressids: Array<AddressId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in addressids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: AddressRow,
    c: Connection
  ): AddressRow {
    if (map.containsKey(unsaved.addressid)) {
      throw RuntimeException(str("id $unsaved.addressid already exists"))
    }
    map[unsaved.addressid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: AddressRowUnsaved,
    c: Connection
  ): AddressRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: MutableIterator<AddressRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.addressid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: MutableIterator<AddressRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.addressid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<AddressFields, AddressRow> = SelectBuilderMock(AddressFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<AddressRow> = map.values.toList()

  override fun selectById(
    addressid: AddressId,
    c: Connection
  ): Optional<AddressRow> = Optional.ofNullable(map[addressid])

  override fun selectByIds(
    addressids: Array<AddressId>,
    c: Connection
  ): List<AddressRow> {
    val result = ArrayList<AddressRow>()
    for (id in addressids) {
      val opt = Optional.ofNullable(map[id])
      if (opt.isPresent()) {
      result.add(opt.get())
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    addressids: Array<AddressId>,
    c: Connection
  ): Map<AddressId, AddressRow> = selectByIds(addressids, c).associateBy({ row: AddressRow -> row.addressid })

  override fun update(): UpdateBuilder<AddressFields, AddressRow> = UpdateBuilderMock(AddressFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: AddressRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = Optional.ofNullable(map[row.addressid]).filter({ oldRow -> (oldRow != row) }).isPresent()
    if (shouldUpdate) {
      map[row.addressid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: AddressRow,
    c: Connection
  ): AddressRow {
    map[unsaved.addressid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: MutableIterator<AddressRow>,
    c: Connection
  ): List<AddressRow> {
    val result = ArrayList<AddressRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.addressid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: MutableIterator<AddressRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.addressid] = row
      count = count + 1
    }
    return count
  }
}