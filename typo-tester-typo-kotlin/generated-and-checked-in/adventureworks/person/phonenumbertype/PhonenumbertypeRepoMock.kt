/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.phonenumbertype

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.Optional
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableIterator
import kotlin.collections.MutableMap
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import typo.runtime.internal.stringInterpolator.str

data class PhonenumbertypeRepoMock(
  val toRow: (PhonenumbertypeRowUnsaved) -> PhonenumbertypeRow,
  val map: MutableMap<PhonenumbertypeId, PhonenumbertypeRow> = mutableMapOf<PhonenumbertypeId, PhonenumbertypeRow>()
) : PhonenumbertypeRepo {
  override fun delete(): DeleteBuilder<PhonenumbertypeFields, PhonenumbertypeRow> = DeleteBuilderMock(PhonenumbertypeFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.phonenumbertypeid }, { id -> map.remove(id) })

  override fun deleteById(
    phonenumbertypeid: PhonenumbertypeId,
    c: Connection
  ): Boolean = Optional.ofNullable(map.remove(phonenumbertypeid)).isPresent()

  override fun deleteByIds(
    phonenumbertypeids: Array<PhonenumbertypeId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in phonenumbertypeids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: PhonenumbertypeRow,
    c: Connection
  ): PhonenumbertypeRow {
    if (map.containsKey(unsaved.phonenumbertypeid)) {
      throw RuntimeException(str("id $unsaved.phonenumbertypeid already exists"))
    }
    map[unsaved.phonenumbertypeid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: PhonenumbertypeRowUnsaved,
    c: Connection
  ): PhonenumbertypeRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: MutableIterator<PhonenumbertypeRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.phonenumbertypeid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: MutableIterator<PhonenumbertypeRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.phonenumbertypeid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<PhonenumbertypeFields, PhonenumbertypeRow> = SelectBuilderMock(PhonenumbertypeFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<PhonenumbertypeRow> = map.values.toList()

  override fun selectById(
    phonenumbertypeid: PhonenumbertypeId,
    c: Connection
  ): Optional<PhonenumbertypeRow> = Optional.ofNullable(map[phonenumbertypeid])

  override fun selectByIds(
    phonenumbertypeids: Array<PhonenumbertypeId>,
    c: Connection
  ): List<PhonenumbertypeRow> {
    val result = ArrayList<PhonenumbertypeRow>()
    for (id in phonenumbertypeids) {
      val opt = Optional.ofNullable(map[id])
      if (opt.isPresent()) {
      result.add(opt.get())
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    phonenumbertypeids: Array<PhonenumbertypeId>,
    c: Connection
  ): Map<PhonenumbertypeId, PhonenumbertypeRow> = selectByIds(phonenumbertypeids, c).associateBy({ row: PhonenumbertypeRow -> row.phonenumbertypeid })

  override fun update(): UpdateBuilder<PhonenumbertypeFields, PhonenumbertypeRow> = UpdateBuilderMock(PhonenumbertypeFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: PhonenumbertypeRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = Optional.ofNullable(map[row.phonenumbertypeid]).filter({ oldRow -> (oldRow != row) }).isPresent()
    if (shouldUpdate) {
      map[row.phonenumbertypeid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: PhonenumbertypeRow,
    c: Connection
  ): PhonenumbertypeRow {
    map[unsaved.phonenumbertypeid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: MutableIterator<PhonenumbertypeRow>,
    c: Connection
  ): List<PhonenumbertypeRow> {
    val result = ArrayList<PhonenumbertypeRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.phonenumbertypeid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: MutableIterator<PhonenumbertypeRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.phonenumbertypeid] = row
      count = count + 1
    }
    return count
  }
}