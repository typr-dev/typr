/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.contacttype

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.kotlindsl.DeleteBuilder
import typo.kotlindsl.DeleteBuilderMock
import typo.kotlindsl.DeleteParams
import typo.kotlindsl.SelectBuilder
import typo.kotlindsl.SelectBuilderMock
import typo.kotlindsl.SelectParams
import typo.kotlindsl.UpdateBuilder
import typo.kotlindsl.UpdateBuilderMock
import typo.kotlindsl.UpdateParams

data class ContacttypeRepoMock(
  val toRow: (ContacttypeRowUnsaved) -> ContacttypeRow,
  val map: MutableMap<ContacttypeId, ContacttypeRow> = mutableMapOf<ContacttypeId, ContacttypeRow>()
) : ContacttypeRepo {
  override fun delete(): DeleteBuilder<ContacttypeFields, ContacttypeRow> = DeleteBuilderMock(ContacttypeFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.contacttypeid }, { id -> map.remove(id) })

  override fun deleteById(
    contacttypeid: ContacttypeId,
    c: Connection
  ): Boolean = map.remove(contacttypeid) != null

  override fun deleteByIds(
    contacttypeids: Array<ContacttypeId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in contacttypeids) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: ContacttypeRow,
    c: Connection
  ): ContacttypeRow {
    if (map.containsKey(unsaved.contacttypeid)) {
      throw RuntimeException("id " + unsaved.contacttypeid + " already exists")
    }
    map[unsaved.contacttypeid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: ContacttypeRowUnsaved,
    c: Connection
  ): ContacttypeRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<ContacttypeRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.contacttypeid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<ContacttypeRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.contacttypeid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<ContacttypeFields, ContacttypeRow> = SelectBuilderMock(ContacttypeFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<ContacttypeRow> = map.values.toList()

  override fun selectById(
    contacttypeid: ContacttypeId,
    c: Connection
  ): ContacttypeRow? = map[contacttypeid]

  override fun selectByIds(
    contacttypeids: Array<ContacttypeId>,
    c: Connection
  ): List<ContacttypeRow> {
    val result = ArrayList<ContacttypeRow>()
    for (id in contacttypeids) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    contacttypeids: Array<ContacttypeId>,
    c: Connection
  ): Map<ContacttypeId, ContacttypeRow> = selectByIds(contacttypeids, c).associateBy({ row: ContacttypeRow -> row.contacttypeid })

  override fun update(): UpdateBuilder<ContacttypeFields, ContacttypeRow> = UpdateBuilderMock(ContacttypeFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: ContacttypeRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.contacttypeid]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.contacttypeid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: ContacttypeRow,
    c: Connection
  ): ContacttypeRow {
    map[unsaved.contacttypeid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<ContacttypeRow>,
    c: Connection
  ): List<ContacttypeRow> {
    val result = ArrayList<ContacttypeRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.contacttypeid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<ContacttypeRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.contacttypeid] = row
      count = count + 1
    }
    return count
  }
}