/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.emailaddress

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.kotlindsl.DeleteBuilder
import typo.kotlindsl.DeleteBuilderMock
import typo.kotlindsl.DeleteParams
import typo.kotlindsl.SelectBuilder
import typo.kotlindsl.SelectBuilderMock
import typo.kotlindsl.SelectParams
import typo.kotlindsl.UpdateBuilder
import typo.kotlindsl.UpdateBuilderMock
import typo.kotlindsl.UpdateParams

data class EmailaddressRepoMock(
  val toRow: (EmailaddressRowUnsaved) -> EmailaddressRow,
  val map: MutableMap<EmailaddressId, EmailaddressRow> = mutableMapOf<EmailaddressId, EmailaddressRow>()
) : EmailaddressRepo {
  override fun delete(): DeleteBuilder<EmailaddressFields, EmailaddressRow> = DeleteBuilderMock(EmailaddressFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.compositeId() }, { id -> map.remove(id) })

  override fun deleteById(
    compositeId: EmailaddressId,
    c: Connection
  ): Boolean = map.remove(compositeId) != null

  override fun deleteByIds(
    compositeIds: Array<EmailaddressId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in compositeIds) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: EmailaddressRow,
    c: Connection
  ): EmailaddressRow {
    if (map.containsKey(unsaved.compositeId())) {
      throw RuntimeException("id " + unsaved.compositeId() + " already exists")
    }
    map[unsaved.compositeId()] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: EmailaddressRowUnsaved,
    c: Connection
  ): EmailaddressRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<EmailaddressRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<EmailaddressRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.compositeId()] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<EmailaddressFields, EmailaddressRow> = SelectBuilderMock(EmailaddressFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<EmailaddressRow> = map.values.toList()

  override fun selectById(
    compositeId: EmailaddressId,
    c: Connection
  ): EmailaddressRow? = map[compositeId]

  override fun selectByIds(
    compositeIds: Array<EmailaddressId>,
    c: Connection
  ): List<EmailaddressRow> {
    val result = ArrayList<EmailaddressRow>()
    for (id in compositeIds) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    compositeIds: Array<EmailaddressId>,
    c: Connection
  ): Map<EmailaddressId, EmailaddressRow> = selectByIds(compositeIds, c).associateBy({ row: EmailaddressRow -> row.compositeId() })

  override fun update(): UpdateBuilder<EmailaddressFields, EmailaddressRow> = UpdateBuilderMock(EmailaddressFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: EmailaddressRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.compositeId()]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.compositeId()] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: EmailaddressRow,
    c: Connection
  ): EmailaddressRow {
    map[unsaved.compositeId()] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<EmailaddressRow>,
    c: Connection
  ): List<EmailaddressRow> {
    val result = ArrayList<EmailaddressRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<EmailaddressRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      count = count + 1
    }
    return count
  }
}