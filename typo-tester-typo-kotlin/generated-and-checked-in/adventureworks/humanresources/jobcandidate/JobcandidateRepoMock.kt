/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.humanresources.jobcandidate

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.Optional
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableIterator
import kotlin.collections.MutableMap
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import typo.runtime.internal.stringInterpolator.str

data class JobcandidateRepoMock(
  val toRow: (JobcandidateRowUnsaved) -> JobcandidateRow,
  val map: MutableMap<JobcandidateId, JobcandidateRow> = mutableMapOf<JobcandidateId, JobcandidateRow>()
) : JobcandidateRepo {
  override fun delete(): DeleteBuilder<JobcandidateFields, JobcandidateRow> = DeleteBuilderMock(JobcandidateFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.jobcandidateid }, { id -> map.remove(id) })

  override fun deleteById(
    jobcandidateid: JobcandidateId,
    c: Connection
  ): Boolean = Optional.ofNullable(map.remove(jobcandidateid)).isPresent()

  override fun deleteByIds(
    jobcandidateids: Array<JobcandidateId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in jobcandidateids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: JobcandidateRow,
    c: Connection
  ): JobcandidateRow {
    if (map.containsKey(unsaved.jobcandidateid)) {
      throw RuntimeException(str("id $unsaved.jobcandidateid already exists"))
    }
    map[unsaved.jobcandidateid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: JobcandidateRowUnsaved,
    c: Connection
  ): JobcandidateRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: MutableIterator<JobcandidateRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.jobcandidateid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: MutableIterator<JobcandidateRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.jobcandidateid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<JobcandidateFields, JobcandidateRow> = SelectBuilderMock(JobcandidateFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<JobcandidateRow> = map.values.toList()

  override fun selectById(
    jobcandidateid: JobcandidateId,
    c: Connection
  ): Optional<JobcandidateRow> = Optional.ofNullable(map[jobcandidateid])

  override fun selectByIds(
    jobcandidateids: Array<JobcandidateId>,
    c: Connection
  ): List<JobcandidateRow> {
    val result = ArrayList<JobcandidateRow>()
    for (id in jobcandidateids) {
      val opt = Optional.ofNullable(map[id])
      if (opt.isPresent()) {
      result.add(opt.get())
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    jobcandidateids: Array<JobcandidateId>,
    c: Connection
  ): Map<JobcandidateId, JobcandidateRow> = selectByIds(jobcandidateids, c).associateBy({ row: JobcandidateRow -> row.jobcandidateid })

  override fun update(): UpdateBuilder<JobcandidateFields, JobcandidateRow> = UpdateBuilderMock(JobcandidateFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: JobcandidateRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = Optional.ofNullable(map[row.jobcandidateid]).filter({ oldRow -> (oldRow != row) }).isPresent()
    if (shouldUpdate) {
      map[row.jobcandidateid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: JobcandidateRow,
    c: Connection
  ): JobcandidateRow {
    map[unsaved.jobcandidateid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: MutableIterator<JobcandidateRow>,
    c: Connection
  ): List<JobcandidateRow> {
    val result = ArrayList<JobcandidateRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.jobcandidateid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: MutableIterator<JobcandidateRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.jobcandidateid] = row
      count = count + 1
    }
    return count
  }
}