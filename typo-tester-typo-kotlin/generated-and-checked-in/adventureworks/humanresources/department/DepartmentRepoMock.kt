/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.humanresources.department

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.kotlindsl.DeleteBuilder
import typo.kotlindsl.DeleteBuilderMock
import typo.kotlindsl.DeleteParams
import typo.kotlindsl.SelectBuilder
import typo.kotlindsl.SelectBuilderMock
import typo.kotlindsl.SelectParams
import typo.kotlindsl.UpdateBuilder
import typo.kotlindsl.UpdateBuilderMock
import typo.kotlindsl.UpdateParams

data class DepartmentRepoMock(
  val toRow: (DepartmentRowUnsaved) -> DepartmentRow,
  val map: MutableMap<DepartmentId, DepartmentRow> = mutableMapOf<DepartmentId, DepartmentRow>()
) : DepartmentRepo {
  override fun delete(): DeleteBuilder<DepartmentFields, DepartmentRow> = DeleteBuilderMock(DepartmentFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.departmentid }, { id -> map.remove(id) })

  override fun deleteById(
    departmentid: DepartmentId,
    c: Connection
  ): Boolean = map.remove(departmentid) != null

  override fun deleteByIds(
    departmentids: Array<DepartmentId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in departmentids) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: DepartmentRow,
    c: Connection
  ): DepartmentRow {
    if (map.containsKey(unsaved.departmentid)) {
      throw RuntimeException("id " + unsaved.departmentid + " already exists")
    }
    map[unsaved.departmentid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: DepartmentRowUnsaved,
    c: Connection
  ): DepartmentRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<DepartmentRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.departmentid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<DepartmentRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.departmentid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<DepartmentFields, DepartmentRow> = SelectBuilderMock(DepartmentFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<DepartmentRow> = map.values.toList()

  override fun selectById(
    departmentid: DepartmentId,
    c: Connection
  ): DepartmentRow? = map[departmentid]

  override fun selectByIds(
    departmentids: Array<DepartmentId>,
    c: Connection
  ): List<DepartmentRow> {
    val result = ArrayList<DepartmentRow>()
    for (id in departmentids) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    departmentids: Array<DepartmentId>,
    c: Connection
  ): Map<DepartmentId, DepartmentRow> = selectByIds(departmentids, c).associateBy({ row: DepartmentRow -> row.departmentid })

  override fun update(): UpdateBuilder<DepartmentFields, DepartmentRow> = UpdateBuilderMock(DepartmentFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: DepartmentRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.departmentid]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.departmentid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: DepartmentRow,
    c: Connection
  ): DepartmentRow {
    map[unsaved.departmentid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<DepartmentRow>,
    c: Connection
  ): List<DepartmentRow> {
    val result = ArrayList<DepartmentRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.departmentid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<DepartmentRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.departmentid] = row
      count = count + 1
    }
    return count
  }
}