/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.humanresources.employeepayhistory

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.kotlindsl.DeleteBuilder
import typo.kotlindsl.DeleteBuilderMock
import typo.kotlindsl.DeleteParams
import typo.kotlindsl.SelectBuilder
import typo.kotlindsl.SelectBuilderMock
import typo.kotlindsl.SelectParams
import typo.kotlindsl.UpdateBuilder
import typo.kotlindsl.UpdateBuilderMock
import typo.kotlindsl.UpdateParams

data class EmployeepayhistoryRepoMock(
  val toRow: (EmployeepayhistoryRowUnsaved) -> EmployeepayhistoryRow,
  val map: MutableMap<EmployeepayhistoryId, EmployeepayhistoryRow> = mutableMapOf<EmployeepayhistoryId, EmployeepayhistoryRow>()
) : EmployeepayhistoryRepo {
  override fun delete(): DeleteBuilder<EmployeepayhistoryFields, EmployeepayhistoryRow> = DeleteBuilderMock(EmployeepayhistoryFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.compositeId() }, { id -> map.remove(id) })

  override fun deleteById(
    compositeId: EmployeepayhistoryId,
    c: Connection
  ): Boolean = map.remove(compositeId) != null

  override fun deleteByIds(
    compositeIds: Array<EmployeepayhistoryId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in compositeIds) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: EmployeepayhistoryRow,
    c: Connection
  ): EmployeepayhistoryRow {
    if (map.containsKey(unsaved.compositeId())) {
      throw RuntimeException("id " + unsaved.compositeId() + " already exists")
    }
    map[unsaved.compositeId()] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: EmployeepayhistoryRowUnsaved,
    c: Connection
  ): EmployeepayhistoryRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<EmployeepayhistoryRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<EmployeepayhistoryRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.compositeId()] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<EmployeepayhistoryFields, EmployeepayhistoryRow> = SelectBuilderMock(EmployeepayhistoryFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<EmployeepayhistoryRow> = map.values.toList()

  override fun selectById(
    compositeId: EmployeepayhistoryId,
    c: Connection
  ): EmployeepayhistoryRow? = map[compositeId]

  override fun selectByIds(
    compositeIds: Array<EmployeepayhistoryId>,
    c: Connection
  ): List<EmployeepayhistoryRow> {
    val result = ArrayList<EmployeepayhistoryRow>()
    for (id in compositeIds) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    compositeIds: Array<EmployeepayhistoryId>,
    c: Connection
  ): Map<EmployeepayhistoryId, EmployeepayhistoryRow> = selectByIds(compositeIds, c).associateBy({ row: EmployeepayhistoryRow -> row.compositeId() })

  override fun update(): UpdateBuilder<EmployeepayhistoryFields, EmployeepayhistoryRow> = UpdateBuilderMock(EmployeepayhistoryFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: EmployeepayhistoryRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.compositeId()]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.compositeId()] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: EmployeepayhistoryRow,
    c: Connection
  ): EmployeepayhistoryRow {
    map[unsaved.compositeId()] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<EmployeepayhistoryRow>,
    c: Connection
  ): List<EmployeepayhistoryRow> {
    val result = ArrayList<EmployeepayhistoryRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<EmployeepayhistoryRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      count = count + 1
    }
    return count
  }
}