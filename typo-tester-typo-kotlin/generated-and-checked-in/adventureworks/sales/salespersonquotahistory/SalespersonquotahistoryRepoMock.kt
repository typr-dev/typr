/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.salespersonquotahistory

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.kotlindsl.DeleteBuilder
import typo.kotlindsl.DeleteBuilderMock
import typo.kotlindsl.DeleteParams
import typo.kotlindsl.SelectBuilder
import typo.kotlindsl.SelectBuilderMock
import typo.kotlindsl.SelectParams
import typo.kotlindsl.UpdateBuilder
import typo.kotlindsl.UpdateBuilderMock
import typo.kotlindsl.UpdateParams

data class SalespersonquotahistoryRepoMock(
  val toRow: (SalespersonquotahistoryRowUnsaved) -> SalespersonquotahistoryRow,
  val map: MutableMap<SalespersonquotahistoryId, SalespersonquotahistoryRow> = mutableMapOf<SalespersonquotahistoryId, SalespersonquotahistoryRow>()
) : SalespersonquotahistoryRepo {
  override fun delete(): DeleteBuilder<SalespersonquotahistoryFields, SalespersonquotahistoryRow> = DeleteBuilderMock(SalespersonquotahistoryFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.compositeId() }, { id -> map.remove(id) })

  override fun deleteById(
    compositeId: SalespersonquotahistoryId,
    c: Connection
  ): Boolean = map.remove(compositeId) != null

  override fun deleteByIds(
    compositeIds: Array<SalespersonquotahistoryId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in compositeIds) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: SalespersonquotahistoryRow,
    c: Connection
  ): SalespersonquotahistoryRow {
    if (map.containsKey(unsaved.compositeId())) {
      throw RuntimeException("id " + unsaved.compositeId() + " already exists")
    }
    map[unsaved.compositeId()] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: SalespersonquotahistoryRowUnsaved,
    c: Connection
  ): SalespersonquotahistoryRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<SalespersonquotahistoryRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<SalespersonquotahistoryRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.compositeId()] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<SalespersonquotahistoryFields, SalespersonquotahistoryRow> = SelectBuilderMock(SalespersonquotahistoryFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<SalespersonquotahistoryRow> = map.values.toList()

  override fun selectById(
    compositeId: SalespersonquotahistoryId,
    c: Connection
  ): SalespersonquotahistoryRow? = map[compositeId]

  override fun selectByIds(
    compositeIds: Array<SalespersonquotahistoryId>,
    c: Connection
  ): List<SalespersonquotahistoryRow> {
    val result = ArrayList<SalespersonquotahistoryRow>()
    for (id in compositeIds) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    compositeIds: Array<SalespersonquotahistoryId>,
    c: Connection
  ): Map<SalespersonquotahistoryId, SalespersonquotahistoryRow> = selectByIds(compositeIds, c).associateBy({ row: SalespersonquotahistoryRow -> row.compositeId() })

  override fun update(): UpdateBuilder<SalespersonquotahistoryFields, SalespersonquotahistoryRow> = UpdateBuilderMock(SalespersonquotahistoryFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: SalespersonquotahistoryRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.compositeId()]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.compositeId()] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: SalespersonquotahistoryRow,
    c: Connection
  ): SalespersonquotahistoryRow {
    map[unsaved.compositeId()] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<SalespersonquotahistoryRow>,
    c: Connection
  ): List<SalespersonquotahistoryRow> {
    val result = ArrayList<SalespersonquotahistoryRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<SalespersonquotahistoryRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      count = count + 1
    }
    return count
  }
}