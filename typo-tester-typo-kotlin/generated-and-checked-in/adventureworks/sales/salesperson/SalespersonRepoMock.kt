/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.salesperson

import adventureworks.person.businessentity.BusinessentityId
import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.kotlindsl.DeleteBuilder
import typo.kotlindsl.DeleteBuilderMock
import typo.kotlindsl.DeleteParams
import typo.kotlindsl.SelectBuilder
import typo.kotlindsl.SelectBuilderMock
import typo.kotlindsl.SelectParams
import typo.kotlindsl.UpdateBuilder
import typo.kotlindsl.UpdateBuilderMock
import typo.kotlindsl.UpdateParams

data class SalespersonRepoMock(
  val toRow: (SalespersonRowUnsaved) -> SalespersonRow,
  val map: MutableMap<BusinessentityId, SalespersonRow> = mutableMapOf<BusinessentityId, SalespersonRow>()
) : SalespersonRepo {
  override fun delete(): DeleteBuilder<SalespersonFields, SalespersonRow> = DeleteBuilderMock(SalespersonFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.businessentityid }, { id -> map.remove(id) })

  override fun deleteById(
    businessentityid: BusinessentityId,
    c: Connection
  ): Boolean = map.remove(businessentityid) != null

  override fun deleteByIds(
    businessentityids: Array<BusinessentityId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in businessentityids) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: SalespersonRow,
    c: Connection
  ): SalespersonRow {
    if (map.containsKey(unsaved.businessentityid)) {
      throw RuntimeException("id " + unsaved.businessentityid + " already exists")
    }
    map[unsaved.businessentityid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: SalespersonRowUnsaved,
    c: Connection
  ): SalespersonRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<SalespersonRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.businessentityid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<SalespersonRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.businessentityid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<SalespersonFields, SalespersonRow> = SelectBuilderMock(SalespersonFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<SalespersonRow> = map.values.toList()

  override fun selectById(
    businessentityid: BusinessentityId,
    c: Connection
  ): SalespersonRow? = map[businessentityid]

  override fun selectByIds(
    businessentityids: Array<BusinessentityId>,
    c: Connection
  ): List<SalespersonRow> {
    val result = ArrayList<SalespersonRow>()
    for (id in businessentityids) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    businessentityids: Array<BusinessentityId>,
    c: Connection
  ): Map<BusinessentityId, SalespersonRow> = selectByIds(businessentityids, c).associateBy({ row: SalespersonRow -> row.businessentityid })

  override fun update(): UpdateBuilder<SalespersonFields, SalespersonRow> = UpdateBuilderMock(SalespersonFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: SalespersonRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.businessentityid]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.businessentityid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: SalespersonRow,
    c: Connection
  ): SalespersonRow {
    map[unsaved.businessentityid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<SalespersonRow>,
    c: Connection
  ): List<SalespersonRow> {
    val result = ArrayList<SalespersonRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.businessentityid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<SalespersonRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.businessentityid] = row
      count = count + 1
    }
    return count
  }
}