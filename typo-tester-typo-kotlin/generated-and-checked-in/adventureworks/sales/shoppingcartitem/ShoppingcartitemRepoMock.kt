/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.shoppingcartitem

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.Optional
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableIterator
import kotlin.collections.MutableMap
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import typo.runtime.internal.stringInterpolator.str

data class ShoppingcartitemRepoMock(
  val toRow: (ShoppingcartitemRowUnsaved) -> ShoppingcartitemRow,
  val map: MutableMap<ShoppingcartitemId, ShoppingcartitemRow> = mutableMapOf<ShoppingcartitemId, ShoppingcartitemRow>()
) : ShoppingcartitemRepo {
  override fun delete(): DeleteBuilder<ShoppingcartitemFields, ShoppingcartitemRow> = DeleteBuilderMock(ShoppingcartitemFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.shoppingcartitemid }, { id -> map.remove(id) })

  override fun deleteById(
    shoppingcartitemid: ShoppingcartitemId,
    c: Connection
  ): Boolean = Optional.ofNullable(map.remove(shoppingcartitemid)).isPresent()

  override fun deleteByIds(
    shoppingcartitemids: Array<ShoppingcartitemId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in shoppingcartitemids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: ShoppingcartitemRow,
    c: Connection
  ): ShoppingcartitemRow {
    if (map.containsKey(unsaved.shoppingcartitemid)) {
      throw RuntimeException(str("id $unsaved.shoppingcartitemid already exists"))
    }
    map[unsaved.shoppingcartitemid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: ShoppingcartitemRowUnsaved,
    c: Connection
  ): ShoppingcartitemRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: MutableIterator<ShoppingcartitemRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.shoppingcartitemid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: MutableIterator<ShoppingcartitemRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.shoppingcartitemid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<ShoppingcartitemFields, ShoppingcartitemRow> = SelectBuilderMock(ShoppingcartitemFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<ShoppingcartitemRow> = map.values.toList()

  override fun selectById(
    shoppingcartitemid: ShoppingcartitemId,
    c: Connection
  ): Optional<ShoppingcartitemRow> = Optional.ofNullable(map[shoppingcartitemid])

  override fun selectByIds(
    shoppingcartitemids: Array<ShoppingcartitemId>,
    c: Connection
  ): List<ShoppingcartitemRow> {
    val result = ArrayList<ShoppingcartitemRow>()
    for (id in shoppingcartitemids) {
      val opt = Optional.ofNullable(map[id])
      if (opt.isPresent()) {
      result.add(opt.get())
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    shoppingcartitemids: Array<ShoppingcartitemId>,
    c: Connection
  ): Map<ShoppingcartitemId, ShoppingcartitemRow> = selectByIds(shoppingcartitemids, c).associateBy({ row: ShoppingcartitemRow -> row.shoppingcartitemid })

  override fun update(): UpdateBuilder<ShoppingcartitemFields, ShoppingcartitemRow> = UpdateBuilderMock(ShoppingcartitemFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: ShoppingcartitemRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = Optional.ofNullable(map[row.shoppingcartitemid]).filter({ oldRow -> (oldRow != row) }).isPresent()
    if (shouldUpdate) {
      map[row.shoppingcartitemid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: ShoppingcartitemRow,
    c: Connection
  ): ShoppingcartitemRow {
    map[unsaved.shoppingcartitemid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: MutableIterator<ShoppingcartitemRow>,
    c: Connection
  ): List<ShoppingcartitemRow> {
    val result = ArrayList<ShoppingcartitemRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.shoppingcartitemid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: MutableIterator<ShoppingcartitemRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.shoppingcartitemid] = row
      count = count + 1
    }
    return count
  }
}