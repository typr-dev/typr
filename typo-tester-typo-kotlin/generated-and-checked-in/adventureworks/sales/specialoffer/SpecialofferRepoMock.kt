/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.specialoffer

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.kotlindsl.DeleteBuilder
import typo.kotlindsl.DeleteBuilderMock
import typo.kotlindsl.DeleteParams
import typo.kotlindsl.SelectBuilder
import typo.kotlindsl.SelectBuilderMock
import typo.kotlindsl.SelectParams
import typo.kotlindsl.UpdateBuilder
import typo.kotlindsl.UpdateBuilderMock
import typo.kotlindsl.UpdateParams

data class SpecialofferRepoMock(
  val toRow: (SpecialofferRowUnsaved) -> SpecialofferRow,
  val map: MutableMap<SpecialofferId, SpecialofferRow> = mutableMapOf<SpecialofferId, SpecialofferRow>()
) : SpecialofferRepo {
  override fun delete(): DeleteBuilder<SpecialofferFields, SpecialofferRow> = DeleteBuilderMock(SpecialofferFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.specialofferid }, { id -> map.remove(id) })

  override fun deleteById(
    specialofferid: SpecialofferId,
    c: Connection
  ): Boolean = map.remove(specialofferid) != null

  override fun deleteByIds(
    specialofferids: Array<SpecialofferId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in specialofferids) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: SpecialofferRow,
    c: Connection
  ): SpecialofferRow {
    if (map.containsKey(unsaved.specialofferid)) {
      throw RuntimeException("id " + unsaved.specialofferid + " already exists")
    }
    map[unsaved.specialofferid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: SpecialofferRowUnsaved,
    c: Connection
  ): SpecialofferRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<SpecialofferRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.specialofferid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<SpecialofferRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.specialofferid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<SpecialofferFields, SpecialofferRow> = SelectBuilderMock(SpecialofferFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<SpecialofferRow> = map.values.toList()

  override fun selectById(
    specialofferid: SpecialofferId,
    c: Connection
  ): SpecialofferRow? = map[specialofferid]

  override fun selectByIds(
    specialofferids: Array<SpecialofferId>,
    c: Connection
  ): List<SpecialofferRow> {
    val result = ArrayList<SpecialofferRow>()
    for (id in specialofferids) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    specialofferids: Array<SpecialofferId>,
    c: Connection
  ): Map<SpecialofferId, SpecialofferRow> = selectByIds(specialofferids, c).associateBy({ row: SpecialofferRow -> row.specialofferid })

  override fun update(): UpdateBuilder<SpecialofferFields, SpecialofferRow> = UpdateBuilderMock(SpecialofferFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: SpecialofferRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.specialofferid]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.specialofferid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: SpecialofferRow,
    c: Connection
  ): SpecialofferRow {
    map[unsaved.specialofferid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<SpecialofferRow>,
    c: Connection
  ): List<SpecialofferRow> {
    val result = ArrayList<SpecialofferRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.specialofferid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<SpecialofferRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.specialofferid] = row
      count = count + 1
    }
    return count
  }
}