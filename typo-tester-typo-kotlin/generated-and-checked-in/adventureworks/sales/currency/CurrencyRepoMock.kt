/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.currency

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.Optional
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableIterator
import kotlin.collections.MutableMap
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import typo.runtime.internal.stringInterpolator.str

data class CurrencyRepoMock(
  val toRow: (CurrencyRowUnsaved) -> CurrencyRow,
  val map: MutableMap<CurrencyId, CurrencyRow> = mutableMapOf<CurrencyId, CurrencyRow>()
) : CurrencyRepo {
  override fun delete(): DeleteBuilder<CurrencyFields, CurrencyRow> = DeleteBuilderMock(CurrencyFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.currencycode }, { id -> map.remove(id) })

  override fun deleteById(
    currencycode: CurrencyId,
    c: Connection
  ): Boolean = Optional.ofNullable(map.remove(currencycode)).isPresent()

  override fun deleteByIds(
    currencycodes: Array<CurrencyId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in currencycodes) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: CurrencyRow,
    c: Connection
  ): CurrencyRow {
    if (map.containsKey(unsaved.currencycode)) {
      throw RuntimeException(str("id $unsaved.currencycode already exists"))
    }
    map[unsaved.currencycode] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: CurrencyRowUnsaved,
    c: Connection
  ): CurrencyRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: MutableIterator<CurrencyRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.currencycode] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: MutableIterator<CurrencyRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.currencycode] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<CurrencyFields, CurrencyRow> = SelectBuilderMock(CurrencyFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<CurrencyRow> = map.values.toList()

  override fun selectById(
    currencycode: CurrencyId,
    c: Connection
  ): Optional<CurrencyRow> = Optional.ofNullable(map[currencycode])

  override fun selectByIds(
    currencycodes: Array<CurrencyId>,
    c: Connection
  ): List<CurrencyRow> {
    val result = ArrayList<CurrencyRow>()
    for (id in currencycodes) {
      val opt = Optional.ofNullable(map[id])
      if (opt.isPresent()) {
      result.add(opt.get())
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    currencycodes: Array<CurrencyId>,
    c: Connection
  ): Map<CurrencyId, CurrencyRow> = selectByIds(currencycodes, c).associateBy({ row: CurrencyRow -> row.currencycode })

  override fun update(): UpdateBuilder<CurrencyFields, CurrencyRow> = UpdateBuilderMock(CurrencyFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: CurrencyRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = Optional.ofNullable(map[row.currencycode]).filter({ oldRow -> (oldRow != row) }).isPresent()
    if (shouldUpdate) {
      map[row.currencycode] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: CurrencyRow,
    c: Connection
  ): CurrencyRow {
    map[unsaved.currencycode] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: MutableIterator<CurrencyRow>,
    c: Connection
  ): List<CurrencyRow> {
    val result = ArrayList<CurrencyRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.currencycode] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: MutableIterator<CurrencyRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.currencycode] = row
      count = count + 1
    }
    return count
  }
}