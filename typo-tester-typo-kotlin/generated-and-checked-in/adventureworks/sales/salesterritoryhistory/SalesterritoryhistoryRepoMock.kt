/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.salesterritoryhistory

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.Optional
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableIterator
import kotlin.collections.MutableMap
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import typo.runtime.internal.stringInterpolator.str

data class SalesterritoryhistoryRepoMock(
  val toRow: (SalesterritoryhistoryRowUnsaved) -> SalesterritoryhistoryRow,
  val map: MutableMap<SalesterritoryhistoryId, SalesterritoryhistoryRow> = mutableMapOf<SalesterritoryhistoryId, SalesterritoryhistoryRow>()
) : SalesterritoryhistoryRepo {
  override fun delete(): DeleteBuilder<SalesterritoryhistoryFields, SalesterritoryhistoryRow> = DeleteBuilderMock(SalesterritoryhistoryFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.compositeId() }, { id -> map.remove(id) })

  override fun deleteById(
    compositeId: SalesterritoryhistoryId,
    c: Connection
  ): Boolean = Optional.ofNullable(map.remove(compositeId)).isPresent()

  override fun deleteByIds(
    compositeIds: Array<SalesterritoryhistoryId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in compositeIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: SalesterritoryhistoryRow,
    c: Connection
  ): SalesterritoryhistoryRow {
    if (map.containsKey(unsaved.compositeId())) {
      throw RuntimeException(str("id $unsaved.compositeId() already exists"))
    }
    map[unsaved.compositeId()] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: SalesterritoryhistoryRowUnsaved,
    c: Connection
  ): SalesterritoryhistoryRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: MutableIterator<SalesterritoryhistoryRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: MutableIterator<SalesterritoryhistoryRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.compositeId()] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<SalesterritoryhistoryFields, SalesterritoryhistoryRow> = SelectBuilderMock(SalesterritoryhistoryFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<SalesterritoryhistoryRow> = map.values.toList()

  override fun selectById(
    compositeId: SalesterritoryhistoryId,
    c: Connection
  ): Optional<SalesterritoryhistoryRow> = Optional.ofNullable(map[compositeId])

  override fun selectByIds(
    compositeIds: Array<SalesterritoryhistoryId>,
    c: Connection
  ): List<SalesterritoryhistoryRow> {
    val result = ArrayList<SalesterritoryhistoryRow>()
    for (id in compositeIds) {
      val opt = Optional.ofNullable(map[id])
      if (opt.isPresent()) {
      result.add(opt.get())
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    compositeIds: Array<SalesterritoryhistoryId>,
    c: Connection
  ): Map<SalesterritoryhistoryId, SalesterritoryhistoryRow> = selectByIds(compositeIds, c).associateBy({ row: SalesterritoryhistoryRow -> row.compositeId() })

  override fun update(): UpdateBuilder<SalesterritoryhistoryFields, SalesterritoryhistoryRow> = UpdateBuilderMock(SalesterritoryhistoryFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: SalesterritoryhistoryRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = Optional.ofNullable(map[row.compositeId()]).filter({ oldRow -> (oldRow != row) }).isPresent()
    if (shouldUpdate) {
      map[row.compositeId()] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: SalesterritoryhistoryRow,
    c: Connection
  ): SalesterritoryhistoryRow {
    map[unsaved.compositeId()] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: MutableIterator<SalesterritoryhistoryRow>,
    c: Connection
  ): List<SalesterritoryhistoryRow> {
    val result = ArrayList<SalesterritoryhistoryRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: MutableIterator<SalesterritoryhistoryRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      count = count + 1
    }
    return count
  }
}