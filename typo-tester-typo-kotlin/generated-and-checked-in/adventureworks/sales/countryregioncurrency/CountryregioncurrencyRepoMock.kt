/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.countryregioncurrency

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.Optional
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableIterator
import kotlin.collections.MutableMap
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import typo.runtime.internal.stringInterpolator.str

data class CountryregioncurrencyRepoMock(
  val toRow: (CountryregioncurrencyRowUnsaved) -> CountryregioncurrencyRow,
  val map: MutableMap<CountryregioncurrencyId, CountryregioncurrencyRow> = mutableMapOf<CountryregioncurrencyId, CountryregioncurrencyRow>()
) : CountryregioncurrencyRepo {
  override fun delete(): DeleteBuilder<CountryregioncurrencyFields, CountryregioncurrencyRow> = DeleteBuilderMock(CountryregioncurrencyFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.compositeId() }, { id -> map.remove(id) })

  override fun deleteById(
    compositeId: CountryregioncurrencyId,
    c: Connection
  ): Boolean = Optional.ofNullable(map.remove(compositeId)).isPresent()

  override fun deleteByIds(
    compositeIds: Array<CountryregioncurrencyId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in compositeIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: CountryregioncurrencyRow,
    c: Connection
  ): CountryregioncurrencyRow {
    if (map.containsKey(unsaved.compositeId())) {
      throw RuntimeException(str("id $unsaved.compositeId() already exists"))
    }
    map[unsaved.compositeId()] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: CountryregioncurrencyRowUnsaved,
    c: Connection
  ): CountryregioncurrencyRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: MutableIterator<CountryregioncurrencyRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: MutableIterator<CountryregioncurrencyRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.compositeId()] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<CountryregioncurrencyFields, CountryregioncurrencyRow> = SelectBuilderMock(CountryregioncurrencyFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<CountryregioncurrencyRow> = map.values.toList()

  override fun selectById(
    compositeId: CountryregioncurrencyId,
    c: Connection
  ): Optional<CountryregioncurrencyRow> = Optional.ofNullable(map[compositeId])

  override fun selectByIds(
    compositeIds: Array<CountryregioncurrencyId>,
    c: Connection
  ): List<CountryregioncurrencyRow> {
    val result = ArrayList<CountryregioncurrencyRow>()
    for (id in compositeIds) {
      val opt = Optional.ofNullable(map[id])
      if (opt.isPresent()) {
      result.add(opt.get())
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    compositeIds: Array<CountryregioncurrencyId>,
    c: Connection
  ): Map<CountryregioncurrencyId, CountryregioncurrencyRow> = selectByIds(compositeIds, c).associateBy({ row: CountryregioncurrencyRow -> row.compositeId() })

  override fun update(): UpdateBuilder<CountryregioncurrencyFields, CountryregioncurrencyRow> = UpdateBuilderMock(CountryregioncurrencyFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: CountryregioncurrencyRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = Optional.ofNullable(map[row.compositeId()]).filter({ oldRow -> (oldRow != row) }).isPresent()
    if (shouldUpdate) {
      map[row.compositeId()] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: CountryregioncurrencyRow,
    c: Connection
  ): CountryregioncurrencyRow {
    map[unsaved.compositeId()] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: MutableIterator<CountryregioncurrencyRow>,
    c: Connection
  ): List<CountryregioncurrencyRow> {
    val result = ArrayList<CountryregioncurrencyRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: MutableIterator<CountryregioncurrencyRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      count = count + 1
    }
    return count
  }
}