/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.salesorderheader

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.kotlindsl.DeleteBuilder
import typo.kotlindsl.DeleteBuilderMock
import typo.kotlindsl.DeleteParams
import typo.kotlindsl.SelectBuilder
import typo.kotlindsl.SelectBuilderMock
import typo.kotlindsl.SelectParams
import typo.kotlindsl.UpdateBuilder
import typo.kotlindsl.UpdateBuilderMock
import typo.kotlindsl.UpdateParams

data class SalesorderheaderRepoMock(
  val toRow: (SalesorderheaderRowUnsaved) -> SalesorderheaderRow,
  val map: MutableMap<SalesorderheaderId, SalesorderheaderRow> = mutableMapOf<SalesorderheaderId, SalesorderheaderRow>()
) : SalesorderheaderRepo {
  override fun delete(): DeleteBuilder<SalesorderheaderFields, SalesorderheaderRow> = DeleteBuilderMock(SalesorderheaderFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.salesorderid }, { id -> map.remove(id) })

  override fun deleteById(
    salesorderid: SalesorderheaderId,
    c: Connection
  ): Boolean = map.remove(salesorderid) != null

  override fun deleteByIds(
    salesorderids: Array<SalesorderheaderId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in salesorderids) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: SalesorderheaderRow,
    c: Connection
  ): SalesorderheaderRow {
    if (map.containsKey(unsaved.salesorderid)) {
      throw RuntimeException("id " + unsaved.salesorderid + " already exists")
    }
    map[unsaved.salesorderid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: SalesorderheaderRowUnsaved,
    c: Connection
  ): SalesorderheaderRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<SalesorderheaderRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.salesorderid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<SalesorderheaderRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.salesorderid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<SalesorderheaderFields, SalesorderheaderRow> = SelectBuilderMock(SalesorderheaderFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<SalesorderheaderRow> = map.values.toList()

  override fun selectById(
    salesorderid: SalesorderheaderId,
    c: Connection
  ): SalesorderheaderRow? = map[salesorderid]

  override fun selectByIds(
    salesorderids: Array<SalesorderheaderId>,
    c: Connection
  ): List<SalesorderheaderRow> {
    val result = ArrayList<SalesorderheaderRow>()
    for (id in salesorderids) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    salesorderids: Array<SalesorderheaderId>,
    c: Connection
  ): Map<SalesorderheaderId, SalesorderheaderRow> = selectByIds(salesorderids, c).associateBy({ row: SalesorderheaderRow -> row.salesorderid })

  override fun update(): UpdateBuilder<SalesorderheaderFields, SalesorderheaderRow> = UpdateBuilderMock(SalesorderheaderFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: SalesorderheaderRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.salesorderid]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.salesorderid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: SalesorderheaderRow,
    c: Connection
  ): SalesorderheaderRow {
    map[unsaved.salesorderid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<SalesorderheaderRow>,
    c: Connection
  ): List<SalesorderheaderRow> {
    val result = ArrayList<SalesorderheaderRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.salesorderid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<SalesorderheaderRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.salesorderid] = row
      count = count + 1
    }
    return count
  }
}