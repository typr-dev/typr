/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.salestaxrate

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.Optional
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableIterator
import kotlin.collections.MutableMap
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import typo.runtime.internal.stringInterpolator.str

data class SalestaxrateRepoMock(
  val toRow: (SalestaxrateRowUnsaved) -> SalestaxrateRow,
  val map: MutableMap<SalestaxrateId, SalestaxrateRow> = mutableMapOf<SalestaxrateId, SalestaxrateRow>()
) : SalestaxrateRepo {
  override fun delete(): DeleteBuilder<SalestaxrateFields, SalestaxrateRow> = DeleteBuilderMock(SalestaxrateFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.salestaxrateid }, { id -> map.remove(id) })

  override fun deleteById(
    salestaxrateid: SalestaxrateId,
    c: Connection
  ): Boolean = Optional.ofNullable(map.remove(salestaxrateid)).isPresent()

  override fun deleteByIds(
    salestaxrateids: Array<SalestaxrateId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in salestaxrateids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: SalestaxrateRow,
    c: Connection
  ): SalestaxrateRow {
    if (map.containsKey(unsaved.salestaxrateid)) {
      throw RuntimeException(str("id $unsaved.salestaxrateid already exists"))
    }
    map[unsaved.salestaxrateid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: SalestaxrateRowUnsaved,
    c: Connection
  ): SalestaxrateRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: MutableIterator<SalestaxrateRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.salestaxrateid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: MutableIterator<SalestaxrateRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.salestaxrateid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<SalestaxrateFields, SalestaxrateRow> = SelectBuilderMock(SalestaxrateFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<SalestaxrateRow> = map.values.toList()

  override fun selectById(
    salestaxrateid: SalestaxrateId,
    c: Connection
  ): Optional<SalestaxrateRow> = Optional.ofNullable(map[salestaxrateid])

  override fun selectByIds(
    salestaxrateids: Array<SalestaxrateId>,
    c: Connection
  ): List<SalestaxrateRow> {
    val result = ArrayList<SalestaxrateRow>()
    for (id in salestaxrateids) {
      val opt = Optional.ofNullable(map[id])
      if (opt.isPresent()) {
      result.add(opt.get())
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    salestaxrateids: Array<SalestaxrateId>,
    c: Connection
  ): Map<SalestaxrateId, SalestaxrateRow> = selectByIds(salestaxrateids, c).associateBy({ row: SalestaxrateRow -> row.salestaxrateid })

  override fun update(): UpdateBuilder<SalestaxrateFields, SalestaxrateRow> = UpdateBuilderMock(SalestaxrateFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: SalestaxrateRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = Optional.ofNullable(map[row.salestaxrateid]).filter({ oldRow -> (oldRow != row) }).isPresent()
    if (shouldUpdate) {
      map[row.salestaxrateid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: SalestaxrateRow,
    c: Connection
  ): SalestaxrateRow {
    map[unsaved.salestaxrateid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: MutableIterator<SalestaxrateRow>,
    c: Connection
  ): List<SalestaxrateRow> {
    val result = ArrayList<SalestaxrateRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.salestaxrateid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: MutableIterator<SalestaxrateRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.salestaxrateid] = row
      count = count + 1
    }
    return count
  }
}