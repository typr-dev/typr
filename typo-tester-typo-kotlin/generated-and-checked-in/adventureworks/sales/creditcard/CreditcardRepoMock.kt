/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.creditcard

import adventureworks.userdefined.CustomCreditcardId
import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.kotlindsl.DeleteBuilder
import typo.kotlindsl.DeleteBuilderMock
import typo.kotlindsl.DeleteParams
import typo.kotlindsl.SelectBuilder
import typo.kotlindsl.SelectBuilderMock
import typo.kotlindsl.SelectParams
import typo.kotlindsl.UpdateBuilder
import typo.kotlindsl.UpdateBuilderMock
import typo.kotlindsl.UpdateParams

data class CreditcardRepoMock(
  val toRow: (CreditcardRowUnsaved) -> CreditcardRow,
  val map: MutableMap</* user-picked */ CustomCreditcardId, CreditcardRow> = mutableMapOf</* user-picked */ CustomCreditcardId, CreditcardRow>()
) : CreditcardRepo {
  override fun delete(): DeleteBuilder<CreditcardFields, CreditcardRow> = DeleteBuilderMock(CreditcardFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.creditcardid }, { id -> map.remove(id) })

  override fun deleteById(
    creditcardid: /* user-picked */ CustomCreditcardId,
    c: Connection
  ): Boolean = map.remove(creditcardid) != null

  override fun deleteByIds(
    creditcardids: Array</* user-picked */ CustomCreditcardId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in creditcardids) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: CreditcardRow,
    c: Connection
  ): CreditcardRow {
    if (map.containsKey(unsaved.creditcardid)) {
      throw RuntimeException("id " + unsaved.creditcardid + " already exists")
    }
    map[unsaved.creditcardid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: CreditcardRowUnsaved,
    c: Connection
  ): CreditcardRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<CreditcardRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.creditcardid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<CreditcardRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.creditcardid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<CreditcardFields, CreditcardRow> = SelectBuilderMock(CreditcardFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<CreditcardRow> = map.values.toList()

  override fun selectById(
    creditcardid: /* user-picked */ CustomCreditcardId,
    c: Connection
  ): CreditcardRow? = map[creditcardid]

  override fun selectByIds(
    creditcardids: Array</* user-picked */ CustomCreditcardId>,
    c: Connection
  ): List<CreditcardRow> {
    val result = ArrayList<CreditcardRow>()
    for (id in creditcardids) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    creditcardids: Array</* user-picked */ CustomCreditcardId>,
    c: Connection
  ): Map</* user-picked */ CustomCreditcardId, CreditcardRow> = selectByIds(creditcardids, c).associateBy({ row: CreditcardRow -> row.creditcardid })

  override fun update(): UpdateBuilder<CreditcardFields, CreditcardRow> = UpdateBuilderMock(CreditcardFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: CreditcardRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.creditcardid]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.creditcardid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: CreditcardRow,
    c: Connection
  ): CreditcardRow {
    map[unsaved.creditcardid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<CreditcardRow>,
    c: Connection
  ): List<CreditcardRow> {
    val result = ArrayList<CreditcardRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.creditcardid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<CreditcardRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.creditcardid] = row
      count = count + 1
    }
    return count
  }
}