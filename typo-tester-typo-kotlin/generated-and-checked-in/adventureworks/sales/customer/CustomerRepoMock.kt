/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.customer

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.Optional
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableIterator
import kotlin.collections.MutableMap
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import typo.runtime.internal.stringInterpolator.str

data class CustomerRepoMock(
  val toRow: (CustomerRowUnsaved) -> CustomerRow,
  val map: MutableMap<CustomerId, CustomerRow> = mutableMapOf<CustomerId, CustomerRow>()
) : CustomerRepo {
  override fun delete(): DeleteBuilder<CustomerFields, CustomerRow> = DeleteBuilderMock(CustomerFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.customerid }, { id -> map.remove(id) })

  override fun deleteById(
    customerid: CustomerId,
    c: Connection
  ): Boolean = Optional.ofNullable(map.remove(customerid)).isPresent()

  override fun deleteByIds(
    customerids: Array<CustomerId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in customerids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: CustomerRow,
    c: Connection
  ): CustomerRow {
    if (map.containsKey(unsaved.customerid)) {
      throw RuntimeException(str("id $unsaved.customerid already exists"))
    }
    map[unsaved.customerid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: CustomerRowUnsaved,
    c: Connection
  ): CustomerRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: MutableIterator<CustomerRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.customerid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: MutableIterator<CustomerRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.customerid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<CustomerFields, CustomerRow> = SelectBuilderMock(CustomerFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<CustomerRow> = map.values.toList()

  override fun selectById(
    customerid: CustomerId,
    c: Connection
  ): Optional<CustomerRow> = Optional.ofNullable(map[customerid])

  override fun selectByIds(
    customerids: Array<CustomerId>,
    c: Connection
  ): List<CustomerRow> {
    val result = ArrayList<CustomerRow>()
    for (id in customerids) {
      val opt = Optional.ofNullable(map[id])
      if (opt.isPresent()) {
      result.add(opt.get())
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    customerids: Array<CustomerId>,
    c: Connection
  ): Map<CustomerId, CustomerRow> = selectByIds(customerids, c).associateBy({ row: CustomerRow -> row.customerid })

  override fun update(): UpdateBuilder<CustomerFields, CustomerRow> = UpdateBuilderMock(CustomerFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: CustomerRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = Optional.ofNullable(map[row.customerid]).filter({ oldRow -> (oldRow != row) }).isPresent()
    if (shouldUpdate) {
      map[row.customerid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: CustomerRow,
    c: Connection
  ): CustomerRow {
    map[unsaved.customerid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: MutableIterator<CustomerRow>,
    c: Connection
  ): List<CustomerRow> {
    val result = ArrayList<CustomerRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.customerid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: MutableIterator<CustomerRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.customerid] = row
      count = count + 1
    }
    return count
  }
}