/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.salesterritory

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.kotlindsl.DeleteBuilder
import typo.kotlindsl.DeleteBuilderMock
import typo.kotlindsl.DeleteParams
import typo.kotlindsl.SelectBuilder
import typo.kotlindsl.SelectBuilderMock
import typo.kotlindsl.SelectParams
import typo.kotlindsl.UpdateBuilder
import typo.kotlindsl.UpdateBuilderMock
import typo.kotlindsl.UpdateParams

data class SalesterritoryRepoMock(
  val toRow: (SalesterritoryRowUnsaved) -> SalesterritoryRow,
  val map: MutableMap<SalesterritoryId, SalesterritoryRow> = mutableMapOf<SalesterritoryId, SalesterritoryRow>()
) : SalesterritoryRepo {
  override fun delete(): DeleteBuilder<SalesterritoryFields, SalesterritoryRow> = DeleteBuilderMock(SalesterritoryFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.territoryid }, { id -> map.remove(id) })

  override fun deleteById(
    territoryid: SalesterritoryId,
    c: Connection
  ): Boolean = map.remove(territoryid) != null

  override fun deleteByIds(
    territoryids: Array<SalesterritoryId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in territoryids) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: SalesterritoryRow,
    c: Connection
  ): SalesterritoryRow {
    if (map.containsKey(unsaved.territoryid)) {
      throw RuntimeException("id " + unsaved.territoryid + " already exists")
    }
    map[unsaved.territoryid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: SalesterritoryRowUnsaved,
    c: Connection
  ): SalesterritoryRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<SalesterritoryRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.territoryid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<SalesterritoryRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.territoryid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<SalesterritoryFields, SalesterritoryRow> = SelectBuilderMock(SalesterritoryFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<SalesterritoryRow> = map.values.toList()

  override fun selectById(
    territoryid: SalesterritoryId,
    c: Connection
  ): SalesterritoryRow? = map[territoryid]

  override fun selectByIds(
    territoryids: Array<SalesterritoryId>,
    c: Connection
  ): List<SalesterritoryRow> {
    val result = ArrayList<SalesterritoryRow>()
    for (id in territoryids) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    territoryids: Array<SalesterritoryId>,
    c: Connection
  ): Map<SalesterritoryId, SalesterritoryRow> = selectByIds(territoryids, c).associateBy({ row: SalesterritoryRow -> row.territoryid })

  override fun update(): UpdateBuilder<SalesterritoryFields, SalesterritoryRow> = UpdateBuilderMock(SalesterritoryFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: SalesterritoryRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.territoryid]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.territoryid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: SalesterritoryRow,
    c: Connection
  ): SalesterritoryRow {
    map[unsaved.territoryid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<SalesterritoryRow>,
    c: Connection
  ): List<SalesterritoryRow> {
    val result = ArrayList<SalesterritoryRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.territoryid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<SalesterritoryRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.territoryid] = row
      count = count + 1
    }
    return count
  }
}