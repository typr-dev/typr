/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.document

import adventureworks.customtypes.TypoUUID
import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.Optional
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableIterator
import kotlin.collections.MutableMap
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import typo.runtime.internal.stringInterpolator.str

data class DocumentRepoMock(
  val toRow: (DocumentRowUnsaved) -> DocumentRow,
  val map: MutableMap<DocumentId, DocumentRow> = mutableMapOf<DocumentId, DocumentRow>()
) : DocumentRepo {
  override fun delete(): DeleteBuilder<DocumentFields, DocumentRow> = DeleteBuilderMock(DocumentFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.documentnode }, { id -> map.remove(id) })

  override fun deleteById(
    documentnode: DocumentId,
    c: Connection
  ): Boolean = Optional.ofNullable(map.remove(documentnode)).isPresent()

  override fun deleteByIds(
    documentnodes: Array<DocumentId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in documentnodes) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: DocumentRow,
    c: Connection
  ): DocumentRow {
    if (map.containsKey(unsaved.documentnode)) {
      throw RuntimeException(str("id $unsaved.documentnode already exists"))
    }
    map[unsaved.documentnode] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: DocumentRowUnsaved,
    c: Connection
  ): DocumentRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: MutableIterator<DocumentRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.documentnode] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: MutableIterator<DocumentRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.documentnode] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<DocumentFields, DocumentRow> = SelectBuilderMock(DocumentFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<DocumentRow> = map.values.toList()

  override fun selectById(
    documentnode: DocumentId,
    c: Connection
  ): Optional<DocumentRow> = Optional.ofNullable(map[documentnode])

  override fun selectByIds(
    documentnodes: Array<DocumentId>,
    c: Connection
  ): List<DocumentRow> {
    val result = ArrayList<DocumentRow>()
    for (id in documentnodes) {
      val opt = Optional.ofNullable(map[id])
      if (opt.isPresent()) {
      result.add(opt.get())
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    documentnodes: Array<DocumentId>,
    c: Connection
  ): Map<DocumentId, DocumentRow> = selectByIds(documentnodes, c).associateBy({ row: DocumentRow -> row.documentnode })

  override fun selectByUniqueRowguid(
    rowguid: TypoUUID,
    c: Connection
  ): Optional<DocumentRow> = Optional.ofNullable(map.values.toList().find({ v -> (rowguid == v.rowguid) }))

  override fun update(): UpdateBuilder<DocumentFields, DocumentRow> = UpdateBuilderMock(DocumentFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: DocumentRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = Optional.ofNullable(map[row.documentnode]).filter({ oldRow -> (oldRow != row) }).isPresent()
    if (shouldUpdate) {
      map[row.documentnode] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: DocumentRow,
    c: Connection
  ): DocumentRow {
    map[unsaved.documentnode] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: MutableIterator<DocumentRow>,
    c: Connection
  ): List<DocumentRow> {
    val result = ArrayList<DocumentRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.documentnode] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: MutableIterator<DocumentRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.documentnode] = row
      count = count + 1
    }
    return count
  }
}