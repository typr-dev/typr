/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productreview

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.kotlindsl.DeleteBuilder
import typo.kotlindsl.DeleteBuilderMock
import typo.kotlindsl.DeleteParams
import typo.kotlindsl.SelectBuilder
import typo.kotlindsl.SelectBuilderMock
import typo.kotlindsl.SelectParams
import typo.kotlindsl.UpdateBuilder
import typo.kotlindsl.UpdateBuilderMock
import typo.kotlindsl.UpdateParams

data class ProductreviewRepoMock(
  val toRow: (ProductreviewRowUnsaved) -> ProductreviewRow,
  val map: MutableMap<ProductreviewId, ProductreviewRow> = mutableMapOf<ProductreviewId, ProductreviewRow>()
) : ProductreviewRepo {
  override fun delete(): DeleteBuilder<ProductreviewFields, ProductreviewRow> = DeleteBuilderMock(ProductreviewFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.productreviewid }, { id -> map.remove(id) })

  override fun deleteById(
    productreviewid: ProductreviewId,
    c: Connection
  ): Boolean = map.remove(productreviewid) != null

  override fun deleteByIds(
    productreviewids: Array<ProductreviewId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in productreviewids) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: ProductreviewRow,
    c: Connection
  ): ProductreviewRow {
    if (map.containsKey(unsaved.productreviewid)) {
      throw RuntimeException("id " + unsaved.productreviewid + " already exists")
    }
    map[unsaved.productreviewid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: ProductreviewRowUnsaved,
    c: Connection
  ): ProductreviewRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<ProductreviewRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.productreviewid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<ProductreviewRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.productreviewid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<ProductreviewFields, ProductreviewRow> = SelectBuilderMock(ProductreviewFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<ProductreviewRow> = map.values.toList()

  override fun selectById(
    productreviewid: ProductreviewId,
    c: Connection
  ): ProductreviewRow? = map[productreviewid]

  override fun selectByIds(
    productreviewids: Array<ProductreviewId>,
    c: Connection
  ): List<ProductreviewRow> {
    val result = ArrayList<ProductreviewRow>()
    for (id in productreviewids) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    productreviewids: Array<ProductreviewId>,
    c: Connection
  ): Map<ProductreviewId, ProductreviewRow> = selectByIds(productreviewids, c).associateBy({ row: ProductreviewRow -> row.productreviewid })

  override fun update(): UpdateBuilder<ProductreviewFields, ProductreviewRow> = UpdateBuilderMock(ProductreviewFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: ProductreviewRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.productreviewid]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.productreviewid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: ProductreviewRow,
    c: Connection
  ): ProductreviewRow {
    map[unsaved.productreviewid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<ProductreviewRow>,
    c: Connection
  ): List<ProductreviewRow> {
    val result = ArrayList<ProductreviewRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.productreviewid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<ProductreviewRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.productreviewid] = row
      count = count + 1
    }
    return count
  }
}