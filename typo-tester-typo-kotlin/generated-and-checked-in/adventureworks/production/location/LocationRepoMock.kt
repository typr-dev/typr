/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.location

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.Optional
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableIterator
import kotlin.collections.MutableMap
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import typo.runtime.internal.stringInterpolator.str

data class LocationRepoMock(
  val toRow: (LocationRowUnsaved) -> LocationRow,
  val map: MutableMap<LocationId, LocationRow> = mutableMapOf<LocationId, LocationRow>()
) : LocationRepo {
  override fun delete(): DeleteBuilder<LocationFields, LocationRow> = DeleteBuilderMock(LocationFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.locationid }, { id -> map.remove(id) })

  override fun deleteById(
    locationid: LocationId,
    c: Connection
  ): Boolean = Optional.ofNullable(map.remove(locationid)).isPresent()

  override fun deleteByIds(
    locationids: Array<LocationId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in locationids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: LocationRow,
    c: Connection
  ): LocationRow {
    if (map.containsKey(unsaved.locationid)) {
      throw RuntimeException(str("id $unsaved.locationid already exists"))
    }
    map[unsaved.locationid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: LocationRowUnsaved,
    c: Connection
  ): LocationRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: MutableIterator<LocationRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.locationid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: MutableIterator<LocationRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.locationid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<LocationFields, LocationRow> = SelectBuilderMock(LocationFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<LocationRow> = map.values.toList()

  override fun selectById(
    locationid: LocationId,
    c: Connection
  ): Optional<LocationRow> = Optional.ofNullable(map[locationid])

  override fun selectByIds(
    locationids: Array<LocationId>,
    c: Connection
  ): List<LocationRow> {
    val result = ArrayList<LocationRow>()
    for (id in locationids) {
      val opt = Optional.ofNullable(map[id])
      if (opt.isPresent()) {
      result.add(opt.get())
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    locationids: Array<LocationId>,
    c: Connection
  ): Map<LocationId, LocationRow> = selectByIds(locationids, c).associateBy({ row: LocationRow -> row.locationid })

  override fun update(): UpdateBuilder<LocationFields, LocationRow> = UpdateBuilderMock(LocationFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: LocationRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = Optional.ofNullable(map[row.locationid]).filter({ oldRow -> (oldRow != row) }).isPresent()
    if (shouldUpdate) {
      map[row.locationid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: LocationRow,
    c: Connection
  ): LocationRow {
    map[unsaved.locationid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: MutableIterator<LocationRow>,
    c: Connection
  ): List<LocationRow> {
    val result = ArrayList<LocationRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.locationid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: MutableIterator<LocationRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.locationid] = row
      count = count + 1
    }
    return count
  }
}