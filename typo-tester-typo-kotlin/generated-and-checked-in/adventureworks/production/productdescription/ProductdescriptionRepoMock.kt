/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productdescription

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.Optional
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableIterator
import kotlin.collections.MutableMap
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import typo.runtime.internal.stringInterpolator.str

data class ProductdescriptionRepoMock(
  val toRow: (ProductdescriptionRowUnsaved) -> ProductdescriptionRow,
  val map: MutableMap<ProductdescriptionId, ProductdescriptionRow> = mutableMapOf<ProductdescriptionId, ProductdescriptionRow>()
) : ProductdescriptionRepo {
  override fun delete(): DeleteBuilder<ProductdescriptionFields, ProductdescriptionRow> = DeleteBuilderMock(ProductdescriptionFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.productdescriptionid }, { id -> map.remove(id) })

  override fun deleteById(
    productdescriptionid: ProductdescriptionId,
    c: Connection
  ): Boolean = Optional.ofNullable(map.remove(productdescriptionid)).isPresent()

  override fun deleteByIds(
    productdescriptionids: Array<ProductdescriptionId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in productdescriptionids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: ProductdescriptionRow,
    c: Connection
  ): ProductdescriptionRow {
    if (map.containsKey(unsaved.productdescriptionid)) {
      throw RuntimeException(str("id $unsaved.productdescriptionid already exists"))
    }
    map[unsaved.productdescriptionid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: ProductdescriptionRowUnsaved,
    c: Connection
  ): ProductdescriptionRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: MutableIterator<ProductdescriptionRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.productdescriptionid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: MutableIterator<ProductdescriptionRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.productdescriptionid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<ProductdescriptionFields, ProductdescriptionRow> = SelectBuilderMock(ProductdescriptionFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<ProductdescriptionRow> = map.values.toList()

  override fun selectById(
    productdescriptionid: ProductdescriptionId,
    c: Connection
  ): Optional<ProductdescriptionRow> = Optional.ofNullable(map[productdescriptionid])

  override fun selectByIds(
    productdescriptionids: Array<ProductdescriptionId>,
    c: Connection
  ): List<ProductdescriptionRow> {
    val result = ArrayList<ProductdescriptionRow>()
    for (id in productdescriptionids) {
      val opt = Optional.ofNullable(map[id])
      if (opt.isPresent()) {
      result.add(opt.get())
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    productdescriptionids: Array<ProductdescriptionId>,
    c: Connection
  ): Map<ProductdescriptionId, ProductdescriptionRow> = selectByIds(productdescriptionids, c).associateBy({ row: ProductdescriptionRow -> row.productdescriptionid })

  override fun update(): UpdateBuilder<ProductdescriptionFields, ProductdescriptionRow> = UpdateBuilderMock(ProductdescriptionFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: ProductdescriptionRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = Optional.ofNullable(map[row.productdescriptionid]).filter({ oldRow -> (oldRow != row) }).isPresent()
    if (shouldUpdate) {
      map[row.productdescriptionid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: ProductdescriptionRow,
    c: Connection
  ): ProductdescriptionRow {
    map[unsaved.productdescriptionid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: MutableIterator<ProductdescriptionRow>,
    c: Connection
  ): List<ProductdescriptionRow> {
    val result = ArrayList<ProductdescriptionRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.productdescriptionid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: MutableIterator<ProductdescriptionRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.productdescriptionid] = row
      count = count + 1
    }
    return count
  }
}