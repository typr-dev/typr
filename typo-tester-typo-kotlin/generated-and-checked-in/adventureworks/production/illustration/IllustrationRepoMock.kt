/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.illustration

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.Optional
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableIterator
import kotlin.collections.MutableMap
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import typo.runtime.internal.stringInterpolator.str

data class IllustrationRepoMock(
  val toRow: (IllustrationRowUnsaved) -> IllustrationRow,
  val map: MutableMap<IllustrationId, IllustrationRow> = mutableMapOf<IllustrationId, IllustrationRow>()
) : IllustrationRepo {
  override fun delete(): DeleteBuilder<IllustrationFields, IllustrationRow> = DeleteBuilderMock(IllustrationFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.illustrationid }, { id -> map.remove(id) })

  override fun deleteById(
    illustrationid: IllustrationId,
    c: Connection
  ): Boolean = Optional.ofNullable(map.remove(illustrationid)).isPresent()

  override fun deleteByIds(
    illustrationids: Array<IllustrationId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in illustrationids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: IllustrationRow,
    c: Connection
  ): IllustrationRow {
    if (map.containsKey(unsaved.illustrationid)) {
      throw RuntimeException(str("id $unsaved.illustrationid already exists"))
    }
    map[unsaved.illustrationid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: IllustrationRowUnsaved,
    c: Connection
  ): IllustrationRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: MutableIterator<IllustrationRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.illustrationid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: MutableIterator<IllustrationRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.illustrationid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<IllustrationFields, IllustrationRow> = SelectBuilderMock(IllustrationFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<IllustrationRow> = map.values.toList()

  override fun selectById(
    illustrationid: IllustrationId,
    c: Connection
  ): Optional<IllustrationRow> = Optional.ofNullable(map[illustrationid])

  override fun selectByIds(
    illustrationids: Array<IllustrationId>,
    c: Connection
  ): List<IllustrationRow> {
    val result = ArrayList<IllustrationRow>()
    for (id in illustrationids) {
      val opt = Optional.ofNullable(map[id])
      if (opt.isPresent()) {
      result.add(opt.get())
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    illustrationids: Array<IllustrationId>,
    c: Connection
  ): Map<IllustrationId, IllustrationRow> = selectByIds(illustrationids, c).associateBy({ row: IllustrationRow -> row.illustrationid })

  override fun update(): UpdateBuilder<IllustrationFields, IllustrationRow> = UpdateBuilderMock(IllustrationFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: IllustrationRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = Optional.ofNullable(map[row.illustrationid]).filter({ oldRow -> (oldRow != row) }).isPresent()
    if (shouldUpdate) {
      map[row.illustrationid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: IllustrationRow,
    c: Connection
  ): IllustrationRow {
    map[unsaved.illustrationid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: MutableIterator<IllustrationRow>,
    c: Connection
  ): List<IllustrationRow> {
    val result = ArrayList<IllustrationRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.illustrationid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: MutableIterator<IllustrationRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.illustrationid] = row
      count = count + 1
    }
    return count
  }
}