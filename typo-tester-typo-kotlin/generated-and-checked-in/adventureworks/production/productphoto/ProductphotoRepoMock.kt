/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productphoto

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.kotlindsl.DeleteBuilder
import typo.kotlindsl.DeleteBuilderMock
import typo.kotlindsl.DeleteParams
import typo.kotlindsl.SelectBuilder
import typo.kotlindsl.SelectBuilderMock
import typo.kotlindsl.SelectParams
import typo.kotlindsl.UpdateBuilder
import typo.kotlindsl.UpdateBuilderMock
import typo.kotlindsl.UpdateParams

data class ProductphotoRepoMock(
  val toRow: (ProductphotoRowUnsaved) -> ProductphotoRow,
  val map: MutableMap<ProductphotoId, ProductphotoRow> = mutableMapOf<ProductphotoId, ProductphotoRow>()
) : ProductphotoRepo {
  override fun delete(): DeleteBuilder<ProductphotoFields, ProductphotoRow> = DeleteBuilderMock(ProductphotoFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.productphotoid }, { id -> map.remove(id) })

  override fun deleteById(
    productphotoid: ProductphotoId,
    c: Connection
  ): Boolean = map.remove(productphotoid) != null

  override fun deleteByIds(
    productphotoids: Array<ProductphotoId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in productphotoids) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: ProductphotoRow,
    c: Connection
  ): ProductphotoRow {
    if (map.containsKey(unsaved.productphotoid)) {
      throw RuntimeException("id " + unsaved.productphotoid + " already exists")
    }
    map[unsaved.productphotoid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: ProductphotoRowUnsaved,
    c: Connection
  ): ProductphotoRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<ProductphotoRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.productphotoid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<ProductphotoRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.productphotoid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<ProductphotoFields, ProductphotoRow> = SelectBuilderMock(ProductphotoFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<ProductphotoRow> = map.values.toList()

  override fun selectById(
    productphotoid: ProductphotoId,
    c: Connection
  ): ProductphotoRow? = map[productphotoid]

  override fun selectByIds(
    productphotoids: Array<ProductphotoId>,
    c: Connection
  ): List<ProductphotoRow> {
    val result = ArrayList<ProductphotoRow>()
    for (id in productphotoids) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    productphotoids: Array<ProductphotoId>,
    c: Connection
  ): Map<ProductphotoId, ProductphotoRow> = selectByIds(productphotoids, c).associateBy({ row: ProductphotoRow -> row.productphotoid })

  override fun update(): UpdateBuilder<ProductphotoFields, ProductphotoRow> = UpdateBuilderMock(ProductphotoFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: ProductphotoRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.productphotoid]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.productphotoid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: ProductphotoRow,
    c: Connection
  ): ProductphotoRow {
    map[unsaved.productphotoid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<ProductphotoRow>,
    c: Connection
  ): List<ProductphotoRow> {
    val result = ArrayList<ProductphotoRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.productphotoid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<ProductphotoRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.productphotoid] = row
      count = count + 1
    }
    return count
  }
}