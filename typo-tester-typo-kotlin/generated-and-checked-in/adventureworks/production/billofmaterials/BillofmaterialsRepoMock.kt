/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.billofmaterials

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.kotlindsl.DeleteBuilder
import typo.kotlindsl.DeleteBuilderMock
import typo.kotlindsl.DeleteParams
import typo.kotlindsl.SelectBuilder
import typo.kotlindsl.SelectBuilderMock
import typo.kotlindsl.SelectParams
import typo.kotlindsl.UpdateBuilder
import typo.kotlindsl.UpdateBuilderMock
import typo.kotlindsl.UpdateParams

data class BillofmaterialsRepoMock(
  val toRow: (BillofmaterialsRowUnsaved) -> BillofmaterialsRow,
  val map: MutableMap<Int, BillofmaterialsRow> = mutableMapOf<Int, BillofmaterialsRow>()
) : BillofmaterialsRepo {
  override fun delete(): DeleteBuilder<BillofmaterialsFields, BillofmaterialsRow> = DeleteBuilderMock(BillofmaterialsFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.billofmaterialsid }, { id -> map.remove(id) })

  override fun deleteById(
    billofmaterialsid: Int,
    c: Connection
  ): Boolean = map.remove(billofmaterialsid) != null

  override fun deleteByIds(
    billofmaterialsids: Array<Int>,
    c: Connection
  ): Int {
    var count = 0
    for (id in billofmaterialsids) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: BillofmaterialsRow,
    c: Connection
  ): BillofmaterialsRow {
    if (map.containsKey(unsaved.billofmaterialsid)) {
      throw RuntimeException("id " + unsaved.billofmaterialsid + " already exists")
    }
    map[unsaved.billofmaterialsid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: BillofmaterialsRowUnsaved,
    c: Connection
  ): BillofmaterialsRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<BillofmaterialsRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.billofmaterialsid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<BillofmaterialsRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.billofmaterialsid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<BillofmaterialsFields, BillofmaterialsRow> = SelectBuilderMock(BillofmaterialsFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<BillofmaterialsRow> = map.values.toList()

  override fun selectById(
    billofmaterialsid: Int,
    c: Connection
  ): BillofmaterialsRow? = map[billofmaterialsid]

  override fun selectByIds(
    billofmaterialsids: Array<Int>,
    c: Connection
  ): List<BillofmaterialsRow> {
    val result = ArrayList<BillofmaterialsRow>()
    for (id in billofmaterialsids) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    billofmaterialsids: Array<Int>,
    c: Connection
  ): Map<Int, BillofmaterialsRow> = selectByIds(billofmaterialsids, c).associateBy({ row: BillofmaterialsRow -> row.billofmaterialsid })

  override fun update(): UpdateBuilder<BillofmaterialsFields, BillofmaterialsRow> = UpdateBuilderMock(BillofmaterialsFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: BillofmaterialsRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.billofmaterialsid]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.billofmaterialsid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: BillofmaterialsRow,
    c: Connection
  ): BillofmaterialsRow {
    map[unsaved.billofmaterialsid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<BillofmaterialsRow>,
    c: Connection
  ): List<BillofmaterialsRow> {
    val result = ArrayList<BillofmaterialsRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.billofmaterialsid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<BillofmaterialsRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.billofmaterialsid] = row
      count = count + 1
    }
    return count
  }
}