/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productcategory

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.Optional
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableIterator
import kotlin.collections.MutableMap
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import typo.runtime.internal.stringInterpolator.str

data class ProductcategoryRepoMock(
  val toRow: (ProductcategoryRowUnsaved) -> ProductcategoryRow,
  val map: MutableMap<ProductcategoryId, ProductcategoryRow> = mutableMapOf<ProductcategoryId, ProductcategoryRow>()
) : ProductcategoryRepo {
  override fun delete(): DeleteBuilder<ProductcategoryFields, ProductcategoryRow> = DeleteBuilderMock(ProductcategoryFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.productcategoryid }, { id -> map.remove(id) })

  override fun deleteById(
    productcategoryid: ProductcategoryId,
    c: Connection
  ): Boolean = Optional.ofNullable(map.remove(productcategoryid)).isPresent()

  override fun deleteByIds(
    productcategoryids: Array<ProductcategoryId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in productcategoryids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: ProductcategoryRow,
    c: Connection
  ): ProductcategoryRow {
    if (map.containsKey(unsaved.productcategoryid)) {
      throw RuntimeException(str("id $unsaved.productcategoryid already exists"))
    }
    map[unsaved.productcategoryid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: ProductcategoryRowUnsaved,
    c: Connection
  ): ProductcategoryRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: MutableIterator<ProductcategoryRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.productcategoryid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: MutableIterator<ProductcategoryRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.productcategoryid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<ProductcategoryFields, ProductcategoryRow> = SelectBuilderMock(ProductcategoryFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<ProductcategoryRow> = map.values.toList()

  override fun selectById(
    productcategoryid: ProductcategoryId,
    c: Connection
  ): Optional<ProductcategoryRow> = Optional.ofNullable(map[productcategoryid])

  override fun selectByIds(
    productcategoryids: Array<ProductcategoryId>,
    c: Connection
  ): List<ProductcategoryRow> {
    val result = ArrayList<ProductcategoryRow>()
    for (id in productcategoryids) {
      val opt = Optional.ofNullable(map[id])
      if (opt.isPresent()) {
      result.add(opt.get())
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    productcategoryids: Array<ProductcategoryId>,
    c: Connection
  ): Map<ProductcategoryId, ProductcategoryRow> = selectByIds(productcategoryids, c).associateBy({ row: ProductcategoryRow -> row.productcategoryid })

  override fun update(): UpdateBuilder<ProductcategoryFields, ProductcategoryRow> = UpdateBuilderMock(ProductcategoryFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: ProductcategoryRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = Optional.ofNullable(map[row.productcategoryid]).filter({ oldRow -> (oldRow != row) }).isPresent()
    if (shouldUpdate) {
      map[row.productcategoryid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: ProductcategoryRow,
    c: Connection
  ): ProductcategoryRow {
    map[unsaved.productcategoryid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: MutableIterator<ProductcategoryRow>,
    c: Connection
  ): List<ProductcategoryRow> {
    val result = ArrayList<ProductcategoryRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.productcategoryid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: MutableIterator<ProductcategoryRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.productcategoryid] = row
      count = count + 1
    }
    return count
  }
}