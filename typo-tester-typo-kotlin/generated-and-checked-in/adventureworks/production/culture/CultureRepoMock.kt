/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.culture

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.kotlindsl.DeleteBuilder
import typo.kotlindsl.DeleteBuilderMock
import typo.kotlindsl.DeleteParams
import typo.kotlindsl.SelectBuilder
import typo.kotlindsl.SelectBuilderMock
import typo.kotlindsl.SelectParams
import typo.kotlindsl.UpdateBuilder
import typo.kotlindsl.UpdateBuilderMock
import typo.kotlindsl.UpdateParams

data class CultureRepoMock(
  val toRow: (CultureRowUnsaved) -> CultureRow,
  val map: MutableMap<CultureId, CultureRow> = mutableMapOf<CultureId, CultureRow>()
) : CultureRepo {
  override fun delete(): DeleteBuilder<CultureFields, CultureRow> = DeleteBuilderMock(CultureFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.cultureid }, { id -> map.remove(id) })

  override fun deleteById(
    cultureid: CultureId,
    c: Connection
  ): Boolean = map.remove(cultureid) != null

  override fun deleteByIds(
    cultureids: Array<CultureId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in cultureids) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: CultureRow,
    c: Connection
  ): CultureRow {
    if (map.containsKey(unsaved.cultureid)) {
      throw RuntimeException("id " + unsaved.cultureid + " already exists")
    }
    map[unsaved.cultureid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: CultureRowUnsaved,
    c: Connection
  ): CultureRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<CultureRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.cultureid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<CultureRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.cultureid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<CultureFields, CultureRow> = SelectBuilderMock(CultureFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<CultureRow> = map.values.toList()

  override fun selectById(
    cultureid: CultureId,
    c: Connection
  ): CultureRow? = map[cultureid]

  override fun selectByIds(
    cultureids: Array<CultureId>,
    c: Connection
  ): List<CultureRow> {
    val result = ArrayList<CultureRow>()
    for (id in cultureids) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    cultureids: Array<CultureId>,
    c: Connection
  ): Map<CultureId, CultureRow> = selectByIds(cultureids, c).associateBy({ row: CultureRow -> row.cultureid })

  override fun update(): UpdateBuilder<CultureFields, CultureRow> = UpdateBuilderMock(CultureFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: CultureRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.cultureid]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.cultureid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: CultureRow,
    c: Connection
  ): CultureRow {
    map[unsaved.cultureid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<CultureRow>,
    c: Connection
  ): List<CultureRow> {
    val result = ArrayList<CultureRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.cultureid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<CultureRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.cultureid] = row
      count = count + 1
    }
    return count
  }
}