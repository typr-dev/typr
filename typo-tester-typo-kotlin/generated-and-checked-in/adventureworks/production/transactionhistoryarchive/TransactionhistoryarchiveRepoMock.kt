/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.transactionhistoryarchive

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.kotlindsl.DeleteBuilder
import typo.kotlindsl.DeleteBuilderMock
import typo.kotlindsl.DeleteParams
import typo.kotlindsl.SelectBuilder
import typo.kotlindsl.SelectBuilderMock
import typo.kotlindsl.SelectParams
import typo.kotlindsl.UpdateBuilder
import typo.kotlindsl.UpdateBuilderMock
import typo.kotlindsl.UpdateParams

data class TransactionhistoryarchiveRepoMock(
  val toRow: (TransactionhistoryarchiveRowUnsaved) -> TransactionhistoryarchiveRow,
  val map: MutableMap<TransactionhistoryarchiveId, TransactionhistoryarchiveRow> = mutableMapOf<TransactionhistoryarchiveId, TransactionhistoryarchiveRow>()
) : TransactionhistoryarchiveRepo {
  override fun delete(): DeleteBuilder<TransactionhistoryarchiveFields, TransactionhistoryarchiveRow> = DeleteBuilderMock(TransactionhistoryarchiveFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.transactionid }, { id -> map.remove(id) })

  override fun deleteById(
    transactionid: TransactionhistoryarchiveId,
    c: Connection
  ): Boolean = map.remove(transactionid) != null

  override fun deleteByIds(
    transactionids: Array<TransactionhistoryarchiveId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in transactionids) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: TransactionhistoryarchiveRow,
    c: Connection
  ): TransactionhistoryarchiveRow {
    if (map.containsKey(unsaved.transactionid)) {
      throw RuntimeException("id " + unsaved.transactionid + " already exists")
    }
    map[unsaved.transactionid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: TransactionhistoryarchiveRowUnsaved,
    c: Connection
  ): TransactionhistoryarchiveRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<TransactionhistoryarchiveRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.transactionid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<TransactionhistoryarchiveRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.transactionid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<TransactionhistoryarchiveFields, TransactionhistoryarchiveRow> = SelectBuilderMock(TransactionhistoryarchiveFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<TransactionhistoryarchiveRow> = map.values.toList()

  override fun selectById(
    transactionid: TransactionhistoryarchiveId,
    c: Connection
  ): TransactionhistoryarchiveRow? = map[transactionid]

  override fun selectByIds(
    transactionids: Array<TransactionhistoryarchiveId>,
    c: Connection
  ): List<TransactionhistoryarchiveRow> {
    val result = ArrayList<TransactionhistoryarchiveRow>()
    for (id in transactionids) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    transactionids: Array<TransactionhistoryarchiveId>,
    c: Connection
  ): Map<TransactionhistoryarchiveId, TransactionhistoryarchiveRow> = selectByIds(transactionids, c).associateBy({ row: TransactionhistoryarchiveRow -> row.transactionid })

  override fun update(): UpdateBuilder<TransactionhistoryarchiveFields, TransactionhistoryarchiveRow> = UpdateBuilderMock(TransactionhistoryarchiveFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: TransactionhistoryarchiveRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.transactionid]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.transactionid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: TransactionhistoryarchiveRow,
    c: Connection
  ): TransactionhistoryarchiveRow {
    map[unsaved.transactionid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<TransactionhistoryarchiveRow>,
    c: Connection
  ): List<TransactionhistoryarchiveRow> {
    val result = ArrayList<TransactionhistoryarchiveRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.transactionid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<TransactionhistoryarchiveRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.transactionid] = row
      count = count + 1
    }
    return count
  }
}