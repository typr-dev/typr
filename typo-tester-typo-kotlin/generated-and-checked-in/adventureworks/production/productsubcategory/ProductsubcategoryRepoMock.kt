/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productsubcategory

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.kotlindsl.DeleteBuilder
import typo.kotlindsl.DeleteBuilderMock
import typo.kotlindsl.DeleteParams
import typo.kotlindsl.SelectBuilder
import typo.kotlindsl.SelectBuilderMock
import typo.kotlindsl.SelectParams
import typo.kotlindsl.UpdateBuilder
import typo.kotlindsl.UpdateBuilderMock
import typo.kotlindsl.UpdateParams

data class ProductsubcategoryRepoMock(
  val toRow: (ProductsubcategoryRowUnsaved) -> ProductsubcategoryRow,
  val map: MutableMap<ProductsubcategoryId, ProductsubcategoryRow> = mutableMapOf<ProductsubcategoryId, ProductsubcategoryRow>()
) : ProductsubcategoryRepo {
  override fun delete(): DeleteBuilder<ProductsubcategoryFields, ProductsubcategoryRow> = DeleteBuilderMock(ProductsubcategoryFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.productsubcategoryid }, { id -> map.remove(id) })

  override fun deleteById(
    productsubcategoryid: ProductsubcategoryId,
    c: Connection
  ): Boolean = map.remove(productsubcategoryid) != null

  override fun deleteByIds(
    productsubcategoryids: Array<ProductsubcategoryId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in productsubcategoryids) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: ProductsubcategoryRow,
    c: Connection
  ): ProductsubcategoryRow {
    if (map.containsKey(unsaved.productsubcategoryid)) {
      throw RuntimeException("id " + unsaved.productsubcategoryid + " already exists")
    }
    map[unsaved.productsubcategoryid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: ProductsubcategoryRowUnsaved,
    c: Connection
  ): ProductsubcategoryRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<ProductsubcategoryRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.productsubcategoryid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<ProductsubcategoryRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.productsubcategoryid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<ProductsubcategoryFields, ProductsubcategoryRow> = SelectBuilderMock(ProductsubcategoryFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<ProductsubcategoryRow> = map.values.toList()

  override fun selectById(
    productsubcategoryid: ProductsubcategoryId,
    c: Connection
  ): ProductsubcategoryRow? = map[productsubcategoryid]

  override fun selectByIds(
    productsubcategoryids: Array<ProductsubcategoryId>,
    c: Connection
  ): List<ProductsubcategoryRow> {
    val result = ArrayList<ProductsubcategoryRow>()
    for (id in productsubcategoryids) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    productsubcategoryids: Array<ProductsubcategoryId>,
    c: Connection
  ): Map<ProductsubcategoryId, ProductsubcategoryRow> = selectByIds(productsubcategoryids, c).associateBy({ row: ProductsubcategoryRow -> row.productsubcategoryid })

  override fun update(): UpdateBuilder<ProductsubcategoryFields, ProductsubcategoryRow> = UpdateBuilderMock(ProductsubcategoryFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: ProductsubcategoryRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.productsubcategoryid]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.productsubcategoryid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: ProductsubcategoryRow,
    c: Connection
  ): ProductsubcategoryRow {
    map[unsaved.productsubcategoryid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<ProductsubcategoryRow>,
    c: Connection
  ): List<ProductsubcategoryRow> {
    val result = ArrayList<ProductsubcategoryRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.productsubcategoryid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<ProductsubcategoryRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.productsubcategoryid] = row
      count = count + 1
    }
    return count
  }
}