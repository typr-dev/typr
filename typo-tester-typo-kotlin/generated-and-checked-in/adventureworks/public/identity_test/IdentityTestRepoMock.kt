/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.identity_test

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.Optional
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableIterator
import kotlin.collections.MutableMap
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import typo.runtime.internal.stringInterpolator.str

data class IdentityTestRepoMock(
  val toRow: (IdentityTestRowUnsaved) -> IdentityTestRow,
  val map: MutableMap<IdentityTestId, IdentityTestRow> = mutableMapOf<IdentityTestId, IdentityTestRow>()
) : IdentityTestRepo {
  override fun delete(): DeleteBuilder<IdentityTestFields, IdentityTestRow> = DeleteBuilderMock(IdentityTestFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.name }, { id -> map.remove(id) })

  override fun deleteById(
    name: IdentityTestId,
    c: Connection
  ): Boolean = Optional.ofNullable(map.remove(name)).isPresent()

  override fun deleteByIds(
    names: Array<IdentityTestId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in names) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: IdentityTestRow,
    c: Connection
  ): IdentityTestRow {
    if (map.containsKey(unsaved.name)) {
      throw RuntimeException(str("id $unsaved.name already exists"))
    }
    map[unsaved.name] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: IdentityTestRowUnsaved,
    c: Connection
  ): IdentityTestRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: MutableIterator<IdentityTestRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.name] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: MutableIterator<IdentityTestRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.name] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<IdentityTestFields, IdentityTestRow> = SelectBuilderMock(IdentityTestFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<IdentityTestRow> = map.values.toList()

  override fun selectById(
    name: IdentityTestId,
    c: Connection
  ): Optional<IdentityTestRow> = Optional.ofNullable(map[name])

  override fun selectByIds(
    names: Array<IdentityTestId>,
    c: Connection
  ): List<IdentityTestRow> {
    val result = ArrayList<IdentityTestRow>()
    for (id in names) {
      val opt = Optional.ofNullable(map[id])
      if (opt.isPresent()) {
      result.add(opt.get())
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    names: Array<IdentityTestId>,
    c: Connection
  ): Map<IdentityTestId, IdentityTestRow> = selectByIds(names, c).associateBy({ row: IdentityTestRow -> row.name })

  override fun update(): UpdateBuilder<IdentityTestFields, IdentityTestRow> = UpdateBuilderMock(IdentityTestFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: IdentityTestRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = Optional.ofNullable(map[row.name]).filter({ oldRow -> (oldRow != row) }).isPresent()
    if (shouldUpdate) {
      map[row.name] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: IdentityTestRow,
    c: Connection
  ): IdentityTestRow {
    map[unsaved.name] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: MutableIterator<IdentityTestRow>,
    c: Connection
  ): List<IdentityTestRow> {
    val result = ArrayList<IdentityTestRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.name] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: MutableIterator<IdentityTestRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.name] = row
      count = count + 1
    }
    return count
  }
}