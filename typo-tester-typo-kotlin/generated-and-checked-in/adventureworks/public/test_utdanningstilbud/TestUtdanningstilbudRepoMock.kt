/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.test_utdanningstilbud

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.Optional
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableIterator
import kotlin.collections.MutableMap
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import typo.runtime.internal.stringInterpolator.str

data class TestUtdanningstilbudRepoMock(val map: MutableMap<TestUtdanningstilbudId, TestUtdanningstilbudRow> = mutableMapOf<TestUtdanningstilbudId, TestUtdanningstilbudRow>()) : TestUtdanningstilbudRepo {
  override fun delete(): DeleteBuilder<TestUtdanningstilbudFields, TestUtdanningstilbudRow> = DeleteBuilderMock(TestUtdanningstilbudFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.compositeId() }, { id -> map.remove(id) })

  override fun deleteById(
    compositeId: TestUtdanningstilbudId,
    c: Connection
  ): Boolean = Optional.ofNullable(map.remove(compositeId)).isPresent()

  override fun deleteByIds(
    compositeIds: Array<TestUtdanningstilbudId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in compositeIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: TestUtdanningstilbudRow,
    c: Connection
  ): TestUtdanningstilbudRow {
    if (map.containsKey(unsaved.compositeId())) {
      throw RuntimeException(str("id $unsaved.compositeId() already exists"))
    }
    map[unsaved.compositeId()] = unsaved
    return unsaved
  }

  override fun insertStreaming(
    unsaved: MutableIterator<TestUtdanningstilbudRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<TestUtdanningstilbudFields, TestUtdanningstilbudRow> = SelectBuilderMock(TestUtdanningstilbudFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<TestUtdanningstilbudRow> = map.values.toList()

  override fun selectById(
    compositeId: TestUtdanningstilbudId,
    c: Connection
  ): Optional<TestUtdanningstilbudRow> = Optional.ofNullable(map[compositeId])

  override fun selectByIds(
    compositeIds: Array<TestUtdanningstilbudId>,
    c: Connection
  ): List<TestUtdanningstilbudRow> {
    val result = ArrayList<TestUtdanningstilbudRow>()
    for (id in compositeIds) {
      val opt = Optional.ofNullable(map[id])
      if (opt.isPresent()) {
      result.add(opt.get())
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    compositeIds: Array<TestUtdanningstilbudId>,
    c: Connection
  ): Map<TestUtdanningstilbudId, TestUtdanningstilbudRow> = selectByIds(compositeIds, c).associateBy({ row: TestUtdanningstilbudRow -> row.compositeId() })

  override fun update(): UpdateBuilder<TestUtdanningstilbudFields, TestUtdanningstilbudRow> = UpdateBuilderMock(TestUtdanningstilbudFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun upsert(
    unsaved: TestUtdanningstilbudRow,
    c: Connection
  ): TestUtdanningstilbudRow {
    map[unsaved.compositeId()] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: MutableIterator<TestUtdanningstilbudRow>,
    c: Connection
  ): List<TestUtdanningstilbudRow> {
    val result = ArrayList<TestUtdanningstilbudRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: MutableIterator<TestUtdanningstilbudRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.compositeId()] = row
      count = count + 1
    }
    return count
  }
}