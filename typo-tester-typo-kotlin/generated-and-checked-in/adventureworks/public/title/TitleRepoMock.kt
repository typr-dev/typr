/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.title

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.Optional
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableIterator
import kotlin.collections.MutableMap
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import typo.runtime.internal.stringInterpolator.str

data class TitleRepoMock(val map: MutableMap<TitleId, TitleRow> = mutableMapOf<TitleId, TitleRow>()) : TitleRepo {
  override fun delete(): DeleteBuilder<TitleFields, TitleRow> = DeleteBuilderMock(TitleFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.code }, { id -> map.remove(id) })

  override fun deleteById(
    code: TitleId,
    c: Connection
  ): Boolean = Optional.ofNullable(map.remove(code)).isPresent()

  override fun deleteByIds(
    codes: Array<TitleId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in codes) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: TitleRow,
    c: Connection
  ): TitleRow {
    if (map.containsKey(unsaved.code)) {
      throw RuntimeException(str("id $unsaved.code already exists"))
    }
    map[unsaved.code] = unsaved
    return unsaved
  }

  override fun insertStreaming(
    unsaved: MutableIterator<TitleRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.code] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<TitleFields, TitleRow> = SelectBuilderMock(TitleFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<TitleRow> = map.values.toList()

  override fun selectById(
    code: TitleId,
    c: Connection
  ): Optional<TitleRow> = Optional.ofNullable(map[code])

  override fun selectByIds(
    codes: Array<TitleId>,
    c: Connection
  ): List<TitleRow> {
    val result = ArrayList<TitleRow>()
    for (id in codes) {
      val opt = Optional.ofNullable(map[id])
      if (opt.isPresent()) {
      result.add(opt.get())
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    codes: Array<TitleId>,
    c: Connection
  ): Map<TitleId, TitleRow> = selectByIds(codes, c).associateBy({ row: TitleRow -> row.code })

  override fun update(): UpdateBuilder<TitleFields, TitleRow> = UpdateBuilderMock(TitleFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun upsert(
    unsaved: TitleRow,
    c: Connection
  ): TitleRow {
    map[unsaved.code] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: MutableIterator<TitleRow>,
    c: Connection
  ): List<TitleRow> {
    val result = ArrayList<TitleRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.code] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: MutableIterator<TitleRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.code] = row
      count = count + 1
    }
    return count
  }
}