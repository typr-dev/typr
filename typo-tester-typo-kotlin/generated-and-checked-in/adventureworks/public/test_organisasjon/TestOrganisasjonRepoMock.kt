/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.test_organisasjon

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.kotlindsl.DeleteBuilder
import typo.kotlindsl.DeleteBuilderMock
import typo.kotlindsl.DeleteParams
import typo.kotlindsl.SelectBuilder
import typo.kotlindsl.SelectBuilderMock
import typo.kotlindsl.SelectParams
import typo.kotlindsl.UpdateBuilder
import typo.kotlindsl.UpdateBuilderMock
import typo.kotlindsl.UpdateParams

data class TestOrganisasjonRepoMock(val map: MutableMap<TestOrganisasjonId, TestOrganisasjonRow> = mutableMapOf<TestOrganisasjonId, TestOrganisasjonRow>()) : TestOrganisasjonRepo {
  override fun delete(): DeleteBuilder<TestOrganisasjonFields, TestOrganisasjonRow> = DeleteBuilderMock(TestOrganisasjonFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.organisasjonskode }, { id -> map.remove(id) })

  override fun deleteById(
    organisasjonskode: TestOrganisasjonId,
    c: Connection
  ): Boolean = map.remove(organisasjonskode) != null

  override fun deleteByIds(
    organisasjonskodes: Array<TestOrganisasjonId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in organisasjonskodes) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: TestOrganisasjonRow,
    c: Connection
  ): TestOrganisasjonRow {
    if (map.containsKey(unsaved.organisasjonskode)) {
      throw RuntimeException("id " + unsaved.organisasjonskode + " already exists")
    }
    map[unsaved.organisasjonskode] = unsaved
    return unsaved
  }

  override fun insertStreaming(
    unsaved: Iterator<TestOrganisasjonRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.organisasjonskode] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<TestOrganisasjonFields, TestOrganisasjonRow> = SelectBuilderMock(TestOrganisasjonFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<TestOrganisasjonRow> = map.values.toList()

  override fun selectById(
    organisasjonskode: TestOrganisasjonId,
    c: Connection
  ): TestOrganisasjonRow? = map[organisasjonskode]

  override fun selectByIds(
    organisasjonskodes: Array<TestOrganisasjonId>,
    c: Connection
  ): List<TestOrganisasjonRow> {
    val result = ArrayList<TestOrganisasjonRow>()
    for (id in organisasjonskodes) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    organisasjonskodes: Array<TestOrganisasjonId>,
    c: Connection
  ): Map<TestOrganisasjonId, TestOrganisasjonRow> = selectByIds(organisasjonskodes, c).associateBy({ row: TestOrganisasjonRow -> row.organisasjonskode })

  override fun update(): UpdateBuilder<TestOrganisasjonFields, TestOrganisasjonRow> = UpdateBuilderMock(TestOrganisasjonFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun upsert(
    unsaved: TestOrganisasjonRow,
    c: Connection
  ): TestOrganisasjonRow {
    map[unsaved.organisasjonskode] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<TestOrganisasjonRow>,
    c: Connection
  ): List<TestOrganisasjonRow> {
    val result = ArrayList<TestOrganisasjonRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.organisasjonskode] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<TestOrganisasjonRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.organisasjonskode] = row
      count = count + 1
    }
    return count
  }
}