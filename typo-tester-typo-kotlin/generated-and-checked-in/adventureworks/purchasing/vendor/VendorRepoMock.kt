/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.purchasing.vendor

import adventureworks.person.businessentity.BusinessentityId
import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.Optional
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableIterator
import kotlin.collections.MutableMap
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import typo.runtime.internal.stringInterpolator.str

data class VendorRepoMock(
  val toRow: (VendorRowUnsaved) -> VendorRow,
  val map: MutableMap<BusinessentityId, VendorRow> = mutableMapOf<BusinessentityId, VendorRow>()
) : VendorRepo {
  override fun delete(): DeleteBuilder<VendorFields, VendorRow> = DeleteBuilderMock(VendorFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.businessentityid }, { id -> map.remove(id) })

  override fun deleteById(
    businessentityid: BusinessentityId,
    c: Connection
  ): Boolean = Optional.ofNullable(map.remove(businessentityid)).isPresent()

  override fun deleteByIds(
    businessentityids: Array<BusinessentityId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in businessentityids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: VendorRow,
    c: Connection
  ): VendorRow {
    if (map.containsKey(unsaved.businessentityid)) {
      throw RuntimeException(str("id $unsaved.businessentityid already exists"))
    }
    map[unsaved.businessentityid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: VendorRowUnsaved,
    c: Connection
  ): VendorRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: MutableIterator<VendorRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.businessentityid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: MutableIterator<VendorRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.businessentityid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<VendorFields, VendorRow> = SelectBuilderMock(VendorFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<VendorRow> = map.values.toList()

  override fun selectById(
    businessentityid: BusinessentityId,
    c: Connection
  ): Optional<VendorRow> = Optional.ofNullable(map[businessentityid])

  override fun selectByIds(
    businessentityids: Array<BusinessentityId>,
    c: Connection
  ): List<VendorRow> {
    val result = ArrayList<VendorRow>()
    for (id in businessentityids) {
      val opt = Optional.ofNullable(map[id])
      if (opt.isPresent()) {
      result.add(opt.get())
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    businessentityids: Array<BusinessentityId>,
    c: Connection
  ): Map<BusinessentityId, VendorRow> = selectByIds(businessentityids, c).associateBy({ row: VendorRow -> row.businessentityid })

  override fun update(): UpdateBuilder<VendorFields, VendorRow> = UpdateBuilderMock(VendorFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: VendorRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = Optional.ofNullable(map[row.businessentityid]).filter({ oldRow -> (oldRow != row) }).isPresent()
    if (shouldUpdate) {
      map[row.businessentityid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: VendorRow,
    c: Connection
  ): VendorRow {
    map[unsaved.businessentityid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: MutableIterator<VendorRow>,
    c: Connection
  ): List<VendorRow> {
    val result = ArrayList<VendorRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.businessentityid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: MutableIterator<VendorRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.businessentityid] = row
      count = count + 1
    }
    return count
  }
}