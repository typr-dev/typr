/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.purchasing.purchaseorderheader

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typo.kotlindsl.DeleteBuilder
import typo.kotlindsl.DeleteBuilderMock
import typo.kotlindsl.DeleteParams
import typo.kotlindsl.SelectBuilder
import typo.kotlindsl.SelectBuilderMock
import typo.kotlindsl.SelectParams
import typo.kotlindsl.UpdateBuilder
import typo.kotlindsl.UpdateBuilderMock
import typo.kotlindsl.UpdateParams

data class PurchaseorderheaderRepoMock(
  val toRow: (PurchaseorderheaderRowUnsaved) -> PurchaseorderheaderRow,
  val map: MutableMap<PurchaseorderheaderId, PurchaseorderheaderRow> = mutableMapOf<PurchaseorderheaderId, PurchaseorderheaderRow>()
) : PurchaseorderheaderRepo {
  override fun delete(): DeleteBuilder<PurchaseorderheaderFields, PurchaseorderheaderRow> = DeleteBuilderMock(PurchaseorderheaderFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.purchaseorderid }, { id -> map.remove(id) })

  override fun deleteById(
    purchaseorderid: PurchaseorderheaderId,
    c: Connection
  ): Boolean = map.remove(purchaseorderid) != null

  override fun deleteByIds(
    purchaseorderids: Array<PurchaseorderheaderId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in purchaseorderids) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: PurchaseorderheaderRow,
    c: Connection
  ): PurchaseorderheaderRow {
    if (map.containsKey(unsaved.purchaseorderid)) {
      throw RuntimeException("id " + unsaved.purchaseorderid + " already exists")
    }
    map[unsaved.purchaseorderid] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: PurchaseorderheaderRowUnsaved,
    c: Connection
  ): PurchaseorderheaderRow = insert(toRow(unsaved), c)

  override fun insertStreaming(
    unsaved: Iterator<PurchaseorderheaderRow>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.purchaseorderid] = row
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override fun insertUnsavedStreaming(
    unsaved: Iterator<PurchaseorderheaderRowUnsaved>,
    batchSize: Int,
    c: Connection
  ): Long {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map[row.purchaseorderid] = row
      count = count + 1L
    }
    return count
  }

  override fun select(): SelectBuilder<PurchaseorderheaderFields, PurchaseorderheaderRow> = SelectBuilderMock(PurchaseorderheaderFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<PurchaseorderheaderRow> = map.values.toList()

  override fun selectById(
    purchaseorderid: PurchaseorderheaderId,
    c: Connection
  ): PurchaseorderheaderRow? = map[purchaseorderid]

  override fun selectByIds(
    purchaseorderids: Array<PurchaseorderheaderId>,
    c: Connection
  ): List<PurchaseorderheaderRow> {
    val result = ArrayList<PurchaseorderheaderRow>()
    for (id in purchaseorderids) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    purchaseorderids: Array<PurchaseorderheaderId>,
    c: Connection
  ): Map<PurchaseorderheaderId, PurchaseorderheaderRow> = selectByIds(purchaseorderids, c).associateBy({ row: PurchaseorderheaderRow -> row.purchaseorderid })

  override fun update(): UpdateBuilder<PurchaseorderheaderFields, PurchaseorderheaderRow> = UpdateBuilderMock(PurchaseorderheaderFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: PurchaseorderheaderRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.purchaseorderid]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.purchaseorderid] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: PurchaseorderheaderRow,
    c: Connection
  ): PurchaseorderheaderRow {
    map[unsaved.purchaseorderid] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<PurchaseorderheaderRow>,
    c: Connection
  ): List<PurchaseorderheaderRow> {
    val result = ArrayList<PurchaseorderheaderRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.purchaseorderid] = row
      result.add(row)
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override fun upsertStreaming(
    unsaved: Iterator<PurchaseorderheaderRow>,
    batchSize: Int,
    c: Connection
  ): Int {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.purchaseorderid] = row
      count = count + 1
    }
    return count
  }
}