/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.transactionhistory

import adventureworks.production.product.ProductId
import java.sql.Connection
import scala.collection.mutable.ListBuffer
import typo.runtime.PgTypes
import typo.runtime.streamingInsert
import typo.scaladsl.DeleteBuilder
import typo.scaladsl.Dialect
import typo.scaladsl.Fragment
import typo.scaladsl.ScalaDbTypes
import typo.scaladsl.ScalaIteratorOps
import typo.scaladsl.SelectBuilder
import typo.scaladsl.UpdateBuilder
import typo.scaladsl.Fragment.sql

class TransactionhistoryRepoImpl extends TransactionhistoryRepo {
  override def delete: DeleteBuilder[TransactionhistoryFields, TransactionhistoryRow] = DeleteBuilder.of(""""production"."transactionhistory"""", TransactionhistoryFields.structure, Dialect.POSTGRESQL)

  override def deleteById(transactionid: TransactionhistoryId)(using c: Connection): Boolean = sql"""delete from "production"."transactionhistory" where "transactionid" = ${Fragment.encode(TransactionhistoryId.pgType, transactionid)}""".update().runUnchecked(c) > 0

  override def deleteByIds(transactionids: Array[TransactionhistoryId])(using c: Connection): Int = {
    sql"""delete
    from "production"."transactionhistory"
    where "transactionid" = ANY(${Fragment.encode(TransactionhistoryId.pgTypeArray, transactionids)})"""
      .update()
      .runUnchecked(c)
  }

  override def insert(unsaved: TransactionhistoryRow)(using c: Connection): TransactionhistoryRow = {
  sql"""insert into "production"."transactionhistory"("transactionid", "productid", "referenceorderid", "referenceorderlineid", "transactiondate", "transactiontype", "quantity", "actualcost", "modifieddate")
    values (${Fragment.encode(TransactionhistoryId.pgType, unsaved.transactionid)}::int4, ${Fragment.encode(ProductId.pgType, unsaved.productid)}::int4, ${Fragment.encode(ScalaDbTypes.PgTypes.int4, unsaved.referenceorderid)}::int4, ${Fragment.encode(ScalaDbTypes.PgTypes.int4, unsaved.referenceorderlineid)}::int4, ${Fragment.encode(PgTypes.timestamp, unsaved.transactiondate)}::timestamp, ${Fragment.encode(PgTypes.bpchar, unsaved.transactiontype)}::bpchar, ${Fragment.encode(ScalaDbTypes.PgTypes.int4, unsaved.quantity)}::int4, ${Fragment.encode(ScalaDbTypes.PgTypes.numeric, unsaved.actualcost)}::numeric, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    RETURNING "transactionid", "productid", "referenceorderid", "referenceorderlineid", "transactiondate", "transactiontype", "quantity", "actualcost", "modifieddate"
    """
    .updateReturning(TransactionhistoryRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: TransactionhistoryRowUnsaved)(using c: Connection): TransactionhistoryRow = {
    val columns: ListBuffer[Fragment] = ListBuffer()
    val values: ListBuffer[Fragment] = ListBuffer()
    columns.addOne(Fragment.lit(""""productid"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(ProductId.pgType, unsaved.productid)}::int4"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""referenceorderid"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(ScalaDbTypes.PgTypes.int4, unsaved.referenceorderid)}::int4"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""transactiontype"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(PgTypes.bpchar, unsaved.transactiontype)}::bpchar"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""quantity"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(ScalaDbTypes.PgTypes.int4, unsaved.quantity)}::int4"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""actualcost"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(ScalaDbTypes.PgTypes.numeric, unsaved.actualcost)}::numeric"): @scala.annotation.nowarn
    unsaved.transactionid.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""transactionid"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(TransactionhistoryId.pgType, value)}::int4"): @scala.annotation.nowarn }
    );
    unsaved.referenceorderlineid.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""referenceorderlineid"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(ScalaDbTypes.PgTypes.int4, value)}::int4"): @scala.annotation.nowarn }
    );
    unsaved.transactiondate.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""transactiondate"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.timestamp, value)}::timestamp"): @scala.annotation.nowarn }
    );
    unsaved.modifieddate.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.timestamp, value)}::timestamp"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      sql"""insert into "production"."transactionhistory"(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      RETURNING "transactionid", "productid", "referenceorderid", "referenceorderlineid", "transactiondate", "transactiontype", "quantity", "actualcost", "modifieddate"
      """
    }
    return q.updateReturning(TransactionhistoryRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: Iterator[TransactionhistoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "production"."transactionhistory"("transactionid", "productid", "referenceorderid", "referenceorderlineid", "transactiondate", "transactiontype", "quantity", "actualcost", "modifieddate") FROM STDIN""", batchSize, unsaved.toJavaIterator, c, TransactionhistoryRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[TransactionhistoryRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "production"."transactionhistory"("productid", "referenceorderid", "transactiontype", "quantity", "actualcost", "transactionid", "referenceorderlineid", "transactiondate", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved.toJavaIterator, c, TransactionhistoryRowUnsaved.pgText)

  override def select: SelectBuilder[TransactionhistoryFields, TransactionhistoryRow] = SelectBuilder.of(""""production"."transactionhistory"""", TransactionhistoryFields.structure, TransactionhistoryRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): List[TransactionhistoryRow] = {
    sql"""select "transactionid", "productid", "referenceorderid", "referenceorderlineid", "transactiondate", "transactiontype", "quantity", "actualcost", "modifieddate"
    from "production"."transactionhistory"
    """.query(TransactionhistoryRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(transactionid: TransactionhistoryId)(using c: Connection): Option[TransactionhistoryRow] = {
    sql"""select "transactionid", "productid", "referenceorderid", "referenceorderlineid", "transactiondate", "transactiontype", "quantity", "actualcost", "modifieddate"
    from "production"."transactionhistory"
    where "transactionid" = ${Fragment.encode(TransactionhistoryId.pgType, transactionid)}""".query(TransactionhistoryRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(transactionids: Array[TransactionhistoryId])(using c: Connection): List[TransactionhistoryRow] = {
    sql"""select "transactionid", "productid", "referenceorderid", "referenceorderlineid", "transactiondate", "transactiontype", "quantity", "actualcost", "modifieddate"
    from "production"."transactionhistory"
    where "transactionid" = ANY(${Fragment.encode(TransactionhistoryId.pgTypeArray, transactionids)})""".query(TransactionhistoryRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(transactionids: Array[TransactionhistoryId])(using c: Connection): Map[TransactionhistoryId, TransactionhistoryRow] = {
    val ret: scala.collection.mutable.Map[TransactionhistoryId, TransactionhistoryRow] = scala.collection.mutable.Map.empty[TransactionhistoryId, TransactionhistoryRow]
    selectByIds(transactionids)(using c).foreach(row => ret.put(row.transactionid, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[TransactionhistoryFields, TransactionhistoryRow] = UpdateBuilder.of(""""production"."transactionhistory"""", TransactionhistoryFields.structure, TransactionhistoryRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: TransactionhistoryRow)(using c: Connection): Boolean = {
    val transactionid: TransactionhistoryId = row.transactionid
    return sql"""update "production"."transactionhistory"
    set "productid" = ${Fragment.encode(ProductId.pgType, row.productid)}::int4,
    "referenceorderid" = ${Fragment.encode(ScalaDbTypes.PgTypes.int4, row.referenceorderid)}::int4,
    "referenceorderlineid" = ${Fragment.encode(ScalaDbTypes.PgTypes.int4, row.referenceorderlineid)}::int4,
    "transactiondate" = ${Fragment.encode(PgTypes.timestamp, row.transactiondate)}::timestamp,
    "transactiontype" = ${Fragment.encode(PgTypes.bpchar, row.transactiontype)}::bpchar,
    "quantity" = ${Fragment.encode(ScalaDbTypes.PgTypes.int4, row.quantity)}::int4,
    "actualcost" = ${Fragment.encode(ScalaDbTypes.PgTypes.numeric, row.actualcost)}::numeric,
    "modifieddate" = ${Fragment.encode(PgTypes.timestamp, row.modifieddate)}::timestamp
    where "transactionid" = ${Fragment.encode(TransactionhistoryId.pgType, transactionid)}""".update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: TransactionhistoryRow)(using c: Connection): TransactionhistoryRow = {
  sql"""insert into "production"."transactionhistory"("transactionid", "productid", "referenceorderid", "referenceorderlineid", "transactiondate", "transactiontype", "quantity", "actualcost", "modifieddate")
    values (${Fragment.encode(TransactionhistoryId.pgType, unsaved.transactionid)}::int4, ${Fragment.encode(ProductId.pgType, unsaved.productid)}::int4, ${Fragment.encode(ScalaDbTypes.PgTypes.int4, unsaved.referenceorderid)}::int4, ${Fragment.encode(ScalaDbTypes.PgTypes.int4, unsaved.referenceorderlineid)}::int4, ${Fragment.encode(PgTypes.timestamp, unsaved.transactiondate)}::timestamp, ${Fragment.encode(PgTypes.bpchar, unsaved.transactiontype)}::bpchar, ${Fragment.encode(ScalaDbTypes.PgTypes.int4, unsaved.quantity)}::int4, ${Fragment.encode(ScalaDbTypes.PgTypes.numeric, unsaved.actualcost)}::numeric, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    on conflict ("transactionid")
    do update set
      "productid" = EXCLUDED."productid",
    "referenceorderid" = EXCLUDED."referenceorderid",
    "referenceorderlineid" = EXCLUDED."referenceorderlineid",
    "transactiondate" = EXCLUDED."transactiondate",
    "transactiontype" = EXCLUDED."transactiontype",
    "quantity" = EXCLUDED."quantity",
    "actualcost" = EXCLUDED."actualcost",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "transactionid", "productid", "referenceorderid", "referenceorderlineid", "transactiondate", "transactiontype", "quantity", "actualcost", "modifieddate""""
    .updateReturning(TransactionhistoryRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: Iterator[TransactionhistoryRow])(using c: Connection): List[TransactionhistoryRow] = {
    sql"""insert into "production"."transactionhistory"("transactionid", "productid", "referenceorderid", "referenceorderlineid", "transactiondate", "transactiontype", "quantity", "actualcost", "modifieddate")
    values (?::int4, ?::int4, ?::int4, ?::int4, ?::timestamp, ?::bpchar, ?::int4, ?::numeric, ?::timestamp)
    on conflict ("transactionid")
    do update set
      "productid" = EXCLUDED."productid",
    "referenceorderid" = EXCLUDED."referenceorderid",
    "referenceorderlineid" = EXCLUDED."referenceorderlineid",
    "transactiondate" = EXCLUDED."transactiondate",
    "transactiontype" = EXCLUDED."transactiontype",
    "quantity" = EXCLUDED."quantity",
    "actualcost" = EXCLUDED."actualcost",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "transactionid", "productid", "referenceorderid", "referenceorderlineid", "transactiondate", "transactiontype", "quantity", "actualcost", "modifieddate""""
      .updateManyReturning(TransactionhistoryRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[TransactionhistoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    sql"""create temporary table transactionhistory_TEMP (like "production"."transactionhistory") on commit drop""".update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy transactionhistory_TEMP("transactionid", "productid", "referenceorderid", "referenceorderlineid", "transactiondate", "transactiontype", "quantity", "actualcost", "modifieddate") from stdin""", batchSize, unsaved.toJavaIterator, c, TransactionhistoryRow.pgText): @scala.annotation.nowarn
    return sql"""insert into "production"."transactionhistory"("transactionid", "productid", "referenceorderid", "referenceorderlineid", "transactiondate", "transactiontype", "quantity", "actualcost", "modifieddate")
    select * from transactionhistory_TEMP
    on conflict ("transactionid")
    do update set
      "productid" = EXCLUDED."productid",
    "referenceorderid" = EXCLUDED."referenceorderid",
    "referenceorderlineid" = EXCLUDED."referenceorderlineid",
    "transactiondate" = EXCLUDED."transactiondate",
    "transactiontype" = EXCLUDED."transactiontype",
    "quantity" = EXCLUDED."quantity",
    "actualcost" = EXCLUDED."actualcost",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table transactionhistory_TEMP;""".update().runUnchecked(c)
  }
}