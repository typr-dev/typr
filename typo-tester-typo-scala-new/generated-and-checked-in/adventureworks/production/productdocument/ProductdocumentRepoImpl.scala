/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productdocument

import adventureworks.production.document.DocumentId
import adventureworks.production.product.ProductId
import java.sql.Connection
import scala.collection.mutable.ListBuffer
import typo.runtime.PgTypes
import typo.runtime.streamingInsert
import typo.scaladsl.DeleteBuilder
import typo.scaladsl.Dialect
import typo.scaladsl.Fragment
import typo.scaladsl.ScalaIteratorOps
import typo.scaladsl.SelectBuilder
import typo.scaladsl.UpdateBuilder
import typo.scaladsl.Fragment.sql

class ProductdocumentRepoImpl extends ProductdocumentRepo {
  override def delete: DeleteBuilder[ProductdocumentFields, ProductdocumentRow] = DeleteBuilder.of(""""production"."productdocument"""", ProductdocumentFields.structure, Dialect.POSTGRESQL)

  override def deleteById(compositeId: ProductdocumentId)(using c: Connection): Boolean = sql"""delete from "production"."productdocument" where "productid" = ${Fragment.encode(ProductId.pgType, compositeId.productid)} AND "documentnode" = ${Fragment.encode(DocumentId.pgType, compositeId.documentnode)}""".update().runUnchecked(c) > 0

  override def deleteByIds(compositeIds: Array[ProductdocumentId])(using c: Connection): Int = {
    val productid: Array[ProductId] = compositeIds.map(_.productid)
    val documentnode: Array[DocumentId] = compositeIds.map(_.documentnode)
    return sql"""delete
    from "production"."productdocument"
    where ("productid", "documentnode")
    in (select * from unnest(${Fragment.encode(ProductId.pgTypeArray, productid)}, ${Fragment.encode(DocumentId.pgTypeArray, documentnode)}))
    """.update().runUnchecked(c)
  }

  override def insert(unsaved: ProductdocumentRow)(using c: Connection): ProductdocumentRow = {
  sql"""insert into "production"."productdocument"("productid", "modifieddate", "documentnode")
    values (${Fragment.encode(ProductId.pgType, unsaved.productid)}::int4, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp, ${Fragment.encode(DocumentId.pgType, unsaved.documentnode)})
    RETURNING "productid", "modifieddate", "documentnode"
    """
    .updateReturning(ProductdocumentRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: ProductdocumentRowUnsaved)(using c: Connection): ProductdocumentRow = {
    val columns: ListBuffer[Fragment] = ListBuffer()
    val values: ListBuffer[Fragment] = ListBuffer()
    columns.addOne(Fragment.lit(""""productid"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(ProductId.pgType, unsaved.productid)}::int4"): @scala.annotation.nowarn
    unsaved.modifieddate.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.timestamp, value)}::timestamp"): @scala.annotation.nowarn }
    );
    unsaved.documentnode.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""documentnode"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(DocumentId.pgType, value)}"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      sql"""insert into "production"."productdocument"(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      RETURNING "productid", "modifieddate", "documentnode"
      """
    }
    return q.updateReturning(ProductdocumentRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: Iterator[ProductdocumentRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "production"."productdocument"("productid", "modifieddate", "documentnode") FROM STDIN""", batchSize, unsaved.toJavaIterator, c, ProductdocumentRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[ProductdocumentRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "production"."productdocument"("productid", "modifieddate", "documentnode") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved.toJavaIterator, c, ProductdocumentRowUnsaved.pgText)

  override def select: SelectBuilder[ProductdocumentFields, ProductdocumentRow] = SelectBuilder.of(""""production"."productdocument"""", ProductdocumentFields.structure, ProductdocumentRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): List[ProductdocumentRow] = {
    sql"""select "productid", "modifieddate", "documentnode"
    from "production"."productdocument"
    """.query(ProductdocumentRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(compositeId: ProductdocumentId)(using c: Connection): Option[ProductdocumentRow] = {
    sql"""select "productid", "modifieddate", "documentnode"
    from "production"."productdocument"
    where "productid" = ${Fragment.encode(ProductId.pgType, compositeId.productid)} AND "documentnode" = ${Fragment.encode(DocumentId.pgType, compositeId.documentnode)}""".query(ProductdocumentRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(compositeIds: Array[ProductdocumentId])(using c: Connection): List[ProductdocumentRow] = {
    val productid: Array[ProductId] = compositeIds.map(_.productid)
    val documentnode: Array[DocumentId] = compositeIds.map(_.documentnode)
    return sql"""select "productid", "modifieddate", "documentnode"
    from "production"."productdocument"
    where ("productid", "documentnode")
    in (select * from unnest(${Fragment.encode(ProductId.pgTypeArray, productid)}, ${Fragment.encode(DocumentId.pgTypeArray, documentnode)}))
    """.query(ProductdocumentRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(compositeIds: Array[ProductdocumentId])(using c: Connection): Map[ProductdocumentId, ProductdocumentRow] = {
    val ret: scala.collection.mutable.Map[ProductdocumentId, ProductdocumentRow] = scala.collection.mutable.Map.empty[ProductdocumentId, ProductdocumentRow]
    selectByIds(compositeIds)(using c).foreach(row => ret.put(row.compositeId, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[ProductdocumentFields, ProductdocumentRow] = UpdateBuilder.of(""""production"."productdocument"""", ProductdocumentFields.structure, ProductdocumentRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: ProductdocumentRow)(using c: Connection): Boolean = {
    val compositeId: ProductdocumentId = row.compositeId
    return sql"""update "production"."productdocument"
    set "modifieddate" = ${Fragment.encode(PgTypes.timestamp, row.modifieddate)}::timestamp
    where "productid" = ${Fragment.encode(ProductId.pgType, compositeId.productid)} AND "documentnode" = ${Fragment.encode(DocumentId.pgType, compositeId.documentnode)}""".update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: ProductdocumentRow)(using c: Connection): ProductdocumentRow = {
  sql"""insert into "production"."productdocument"("productid", "modifieddate", "documentnode")
    values (${Fragment.encode(ProductId.pgType, unsaved.productid)}::int4, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp, ${Fragment.encode(DocumentId.pgType, unsaved.documentnode)})
    on conflict ("productid", "documentnode")
    do update set
      "modifieddate" = EXCLUDED."modifieddate"
    returning "productid", "modifieddate", "documentnode""""
    .updateReturning(ProductdocumentRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: Iterator[ProductdocumentRow])(using c: Connection): List[ProductdocumentRow] = {
    sql"""insert into "production"."productdocument"("productid", "modifieddate", "documentnode")
    values (?::int4, ?::timestamp, ?)
    on conflict ("productid", "documentnode")
    do update set
      "modifieddate" = EXCLUDED."modifieddate"
    returning "productid", "modifieddate", "documentnode""""
      .updateManyReturning(ProductdocumentRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[ProductdocumentRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    sql"""create temporary table productdocument_TEMP (like "production"."productdocument") on commit drop""".update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy productdocument_TEMP("productid", "modifieddate", "documentnode") from stdin""", batchSize, unsaved.toJavaIterator, c, ProductdocumentRow.pgText): @scala.annotation.nowarn
    return sql"""insert into "production"."productdocument"("productid", "modifieddate", "documentnode")
    select * from productdocument_TEMP
    on conflict ("productid", "documentnode")
    do update set
      "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table productdocument_TEMP;""".update().runUnchecked(c)
  }
}