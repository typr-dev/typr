/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.scrapreason

import adventureworks.public.Name
import java.sql.Connection
import scala.collection.mutable.ListBuffer
import typo.runtime.PgTypes
import typo.runtime.streamingInsert
import typo.scaladsl.DeleteBuilder
import typo.scaladsl.Dialect
import typo.scaladsl.Fragment
import typo.scaladsl.ScalaIteratorOps
import typo.scaladsl.SelectBuilder
import typo.scaladsl.UpdateBuilder
import typo.scaladsl.Fragment.sql

class ScrapreasonRepoImpl extends ScrapreasonRepo {
  override def delete: DeleteBuilder[ScrapreasonFields, ScrapreasonRow] = DeleteBuilder.of(""""production"."scrapreason"""", ScrapreasonFields.structure, Dialect.POSTGRESQL)

  override def deleteById(scrapreasonid: ScrapreasonId)(using c: Connection): Boolean = sql"""delete from "production"."scrapreason" where "scrapreasonid" = ${Fragment.encode(ScrapreasonId.pgType, scrapreasonid)}""".update().runUnchecked(c) > 0

  override def deleteByIds(scrapreasonids: Array[ScrapreasonId])(using c: Connection): Int = {
    sql"""delete
    from "production"."scrapreason"
    where "scrapreasonid" = ANY(${Fragment.encode(ScrapreasonId.pgTypeArray, scrapreasonids)})"""
      .update()
      .runUnchecked(c)
  }

  override def insert(unsaved: ScrapreasonRow)(using c: Connection): ScrapreasonRow = {
  sql"""insert into "production"."scrapreason"("scrapreasonid", "name", "modifieddate")
    values (${Fragment.encode(ScrapreasonId.pgType, unsaved.scrapreasonid)}::int4, ${Fragment.encode(Name.pgType, unsaved.name)}::varchar, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    RETURNING "scrapreasonid", "name", "modifieddate"
    """
    .updateReturning(ScrapreasonRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: ScrapreasonRowUnsaved)(using c: Connection): ScrapreasonRow = {
    val columns: ListBuffer[Fragment] = ListBuffer()
    val values: ListBuffer[Fragment] = ListBuffer()
    columns.addOne(Fragment.lit(""""name"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(Name.pgType, unsaved.name)}::varchar"): @scala.annotation.nowarn
    unsaved.scrapreasonid.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""scrapreasonid"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(ScrapreasonId.pgType, value)}::int4"): @scala.annotation.nowarn }
    );
    unsaved.modifieddate.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.timestamp, value)}::timestamp"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      sql"""insert into "production"."scrapreason"(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      RETURNING "scrapreasonid", "name", "modifieddate"
      """
    }
    return q.updateReturning(ScrapreasonRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: Iterator[ScrapreasonRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "production"."scrapreason"("scrapreasonid", "name", "modifieddate") FROM STDIN""", batchSize, unsaved.toJavaIterator, c, ScrapreasonRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[ScrapreasonRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "production"."scrapreason"("name", "scrapreasonid", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved.toJavaIterator, c, ScrapreasonRowUnsaved.pgText)

  override def select: SelectBuilder[ScrapreasonFields, ScrapreasonRow] = SelectBuilder.of(""""production"."scrapreason"""", ScrapreasonFields.structure, ScrapreasonRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): List[ScrapreasonRow] = {
    sql"""select "scrapreasonid", "name", "modifieddate"
    from "production"."scrapreason"
    """.query(ScrapreasonRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(scrapreasonid: ScrapreasonId)(using c: Connection): Option[ScrapreasonRow] = {
    sql"""select "scrapreasonid", "name", "modifieddate"
    from "production"."scrapreason"
    where "scrapreasonid" = ${Fragment.encode(ScrapreasonId.pgType, scrapreasonid)}""".query(ScrapreasonRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(scrapreasonids: Array[ScrapreasonId])(using c: Connection): List[ScrapreasonRow] = {
    sql"""select "scrapreasonid", "name", "modifieddate"
    from "production"."scrapreason"
    where "scrapreasonid" = ANY(${Fragment.encode(ScrapreasonId.pgTypeArray, scrapreasonids)})""".query(ScrapreasonRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(scrapreasonids: Array[ScrapreasonId])(using c: Connection): Map[ScrapreasonId, ScrapreasonRow] = {
    val ret: scala.collection.mutable.Map[ScrapreasonId, ScrapreasonRow] = scala.collection.mutable.Map.empty[ScrapreasonId, ScrapreasonRow]
    selectByIds(scrapreasonids)(using c).foreach(row => ret.put(row.scrapreasonid, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[ScrapreasonFields, ScrapreasonRow] = UpdateBuilder.of(""""production"."scrapreason"""", ScrapreasonFields.structure, ScrapreasonRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: ScrapreasonRow)(using c: Connection): Boolean = {
    val scrapreasonid: ScrapreasonId = row.scrapreasonid
    return sql"""update "production"."scrapreason"
    set "name" = ${Fragment.encode(Name.pgType, row.name)}::varchar,
    "modifieddate" = ${Fragment.encode(PgTypes.timestamp, row.modifieddate)}::timestamp
    where "scrapreasonid" = ${Fragment.encode(ScrapreasonId.pgType, scrapreasonid)}""".update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: ScrapreasonRow)(using c: Connection): ScrapreasonRow = {
  sql"""insert into "production"."scrapreason"("scrapreasonid", "name", "modifieddate")
    values (${Fragment.encode(ScrapreasonId.pgType, unsaved.scrapreasonid)}::int4, ${Fragment.encode(Name.pgType, unsaved.name)}::varchar, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    on conflict ("scrapreasonid")
    do update set
      "name" = EXCLUDED."name",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "scrapreasonid", "name", "modifieddate""""
    .updateReturning(ScrapreasonRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: Iterator[ScrapreasonRow])(using c: Connection): List[ScrapreasonRow] = {
    sql"""insert into "production"."scrapreason"("scrapreasonid", "name", "modifieddate")
    values (?::int4, ?::varchar, ?::timestamp)
    on conflict ("scrapreasonid")
    do update set
      "name" = EXCLUDED."name",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "scrapreasonid", "name", "modifieddate""""
      .updateManyReturning(ScrapreasonRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[ScrapreasonRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    sql"""create temporary table scrapreason_TEMP (like "production"."scrapreason") on commit drop""".update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy scrapreason_TEMP("scrapreasonid", "name", "modifieddate") from stdin""", batchSize, unsaved.toJavaIterator, c, ScrapreasonRow.pgText): @scala.annotation.nowarn
    return sql"""insert into "production"."scrapreason"("scrapreasonid", "name", "modifieddate")
    select * from scrapreason_TEMP
    on conflict ("scrapreasonid")
    do update set
      "name" = EXCLUDED."name",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table scrapreason_TEMP;""".update().runUnchecked(c)
  }
}