/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.location

import adventureworks.public.Name
import java.sql.Connection
import scala.collection.mutable.ListBuffer
import typo.runtime.PgTypes
import typo.runtime.streamingInsert
import typo.scaladsl.DeleteBuilder
import typo.scaladsl.Dialect
import typo.scaladsl.Fragment
import typo.scaladsl.ScalaDbTypes
import typo.scaladsl.ScalaIteratorOps
import typo.scaladsl.SelectBuilder
import typo.scaladsl.UpdateBuilder
import typo.scaladsl.Fragment.sql

class LocationRepoImpl extends LocationRepo {
  override def delete: DeleteBuilder[LocationFields, LocationRow] = DeleteBuilder.of(""""production"."location"""", LocationFields.structure, Dialect.POSTGRESQL)

  override def deleteById(locationid: LocationId)(using c: Connection): Boolean = sql"""delete from "production"."location" where "locationid" = ${Fragment.encode(LocationId.pgType, locationid)}""".update().runUnchecked(c) > 0

  override def deleteByIds(locationids: Array[LocationId])(using c: Connection): Int = {
    sql"""delete
    from "production"."location"
    where "locationid" = ANY(${Fragment.encode(LocationId.pgTypeArray, locationids)})"""
      .update()
      .runUnchecked(c)
  }

  override def insert(unsaved: LocationRow)(using c: Connection): LocationRow = {
  sql"""insert into "production"."location"("locationid", "name", "costrate", "availability", "modifieddate")
    values (${Fragment.encode(LocationId.pgType, unsaved.locationid)}::int4, ${Fragment.encode(Name.pgType, unsaved.name)}::varchar, ${Fragment.encode(ScalaDbTypes.PgTypes.numeric, unsaved.costrate)}::numeric, ${Fragment.encode(ScalaDbTypes.PgTypes.numeric, unsaved.availability)}::numeric, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    RETURNING "locationid", "name", "costrate", "availability", "modifieddate"
    """
    .updateReturning(LocationRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: LocationRowUnsaved)(using c: Connection): LocationRow = {
    val columns: ListBuffer[Fragment] = ListBuffer()
    val values: ListBuffer[Fragment] = ListBuffer()
    columns.addOne(Fragment.lit(""""name"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(Name.pgType, unsaved.name)}::varchar"): @scala.annotation.nowarn
    unsaved.locationid.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""locationid"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(LocationId.pgType, value)}::int4"): @scala.annotation.nowarn }
    );
    unsaved.costrate.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""costrate"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(ScalaDbTypes.PgTypes.numeric, value)}::numeric"): @scala.annotation.nowarn }
    );
    unsaved.availability.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""availability"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(ScalaDbTypes.PgTypes.numeric, value)}::numeric"): @scala.annotation.nowarn }
    );
    unsaved.modifieddate.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.timestamp, value)}::timestamp"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      sql"""insert into "production"."location"(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      RETURNING "locationid", "name", "costrate", "availability", "modifieddate"
      """
    }
    return q.updateReturning(LocationRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: Iterator[LocationRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "production"."location"("locationid", "name", "costrate", "availability", "modifieddate") FROM STDIN""", batchSize, unsaved.toJavaIterator, c, LocationRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[LocationRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "production"."location"("name", "locationid", "costrate", "availability", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved.toJavaIterator, c, LocationRowUnsaved.pgText)

  override def select: SelectBuilder[LocationFields, LocationRow] = SelectBuilder.of(""""production"."location"""", LocationFields.structure, LocationRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): List[LocationRow] = {
    sql"""select "locationid", "name", "costrate", "availability", "modifieddate"
    from "production"."location"
    """.query(LocationRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(locationid: LocationId)(using c: Connection): Option[LocationRow] = {
    sql"""select "locationid", "name", "costrate", "availability", "modifieddate"
    from "production"."location"
    where "locationid" = ${Fragment.encode(LocationId.pgType, locationid)}""".query(LocationRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(locationids: Array[LocationId])(using c: Connection): List[LocationRow] = {
    sql"""select "locationid", "name", "costrate", "availability", "modifieddate"
    from "production"."location"
    where "locationid" = ANY(${Fragment.encode(LocationId.pgTypeArray, locationids)})""".query(LocationRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(locationids: Array[LocationId])(using c: Connection): Map[LocationId, LocationRow] = {
    val ret: scala.collection.mutable.Map[LocationId, LocationRow] = scala.collection.mutable.Map.empty[LocationId, LocationRow]
    selectByIds(locationids)(using c).foreach(row => ret.put(row.locationid, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[LocationFields, LocationRow] = UpdateBuilder.of(""""production"."location"""", LocationFields.structure, LocationRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: LocationRow)(using c: Connection): Boolean = {
    val locationid: LocationId = row.locationid
    return sql"""update "production"."location"
    set "name" = ${Fragment.encode(Name.pgType, row.name)}::varchar,
    "costrate" = ${Fragment.encode(ScalaDbTypes.PgTypes.numeric, row.costrate)}::numeric,
    "availability" = ${Fragment.encode(ScalaDbTypes.PgTypes.numeric, row.availability)}::numeric,
    "modifieddate" = ${Fragment.encode(PgTypes.timestamp, row.modifieddate)}::timestamp
    where "locationid" = ${Fragment.encode(LocationId.pgType, locationid)}""".update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: LocationRow)(using c: Connection): LocationRow = {
  sql"""insert into "production"."location"("locationid", "name", "costrate", "availability", "modifieddate")
    values (${Fragment.encode(LocationId.pgType, unsaved.locationid)}::int4, ${Fragment.encode(Name.pgType, unsaved.name)}::varchar, ${Fragment.encode(ScalaDbTypes.PgTypes.numeric, unsaved.costrate)}::numeric, ${Fragment.encode(ScalaDbTypes.PgTypes.numeric, unsaved.availability)}::numeric, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    on conflict ("locationid")
    do update set
      "name" = EXCLUDED."name",
    "costrate" = EXCLUDED."costrate",
    "availability" = EXCLUDED."availability",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "locationid", "name", "costrate", "availability", "modifieddate""""
    .updateReturning(LocationRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: Iterator[LocationRow])(using c: Connection): List[LocationRow] = {
    sql"""insert into "production"."location"("locationid", "name", "costrate", "availability", "modifieddate")
    values (?::int4, ?::varchar, ?::numeric, ?::numeric, ?::timestamp)
    on conflict ("locationid")
    do update set
      "name" = EXCLUDED."name",
    "costrate" = EXCLUDED."costrate",
    "availability" = EXCLUDED."availability",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "locationid", "name", "costrate", "availability", "modifieddate""""
      .updateManyReturning(LocationRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[LocationRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    sql"""create temporary table location_TEMP (like "production"."location") on commit drop""".update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy location_TEMP("locationid", "name", "costrate", "availability", "modifieddate") from stdin""", batchSize, unsaved.toJavaIterator, c, LocationRow.pgText): @scala.annotation.nowarn
    return sql"""insert into "production"."location"("locationid", "name", "costrate", "availability", "modifieddate")
    select * from location_TEMP
    on conflict ("locationid")
    do update set
      "name" = EXCLUDED."name",
    "costrate" = EXCLUDED."costrate",
    "availability" = EXCLUDED."availability",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table location_TEMP;""".update().runUnchecked(c)
  }
}