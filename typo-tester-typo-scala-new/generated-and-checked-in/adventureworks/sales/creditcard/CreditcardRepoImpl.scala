/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.creditcard

import adventureworks.userdefined.CustomCreditcardId
import java.sql.Connection
import scala.collection.mutable.ListBuffer
import typo.runtime.PgTypes
import typo.runtime.streamingInsert
import typo.scaladsl.DeleteBuilder
import typo.scaladsl.Dialect
import typo.scaladsl.Fragment
import typo.scaladsl.ScalaDbTypes
import typo.scaladsl.ScalaIteratorOps
import typo.scaladsl.SelectBuilder
import typo.scaladsl.UpdateBuilder
import typo.scaladsl.Fragment.sql

class CreditcardRepoImpl extends CreditcardRepo {
  override def delete: DeleteBuilder[CreditcardFields, CreditcardRow] = DeleteBuilder.of(""""sales"."creditcard"""", CreditcardFields.structure, Dialect.POSTGRESQL)

  override def deleteById(creditcardid: /* user-picked */ CustomCreditcardId)(using c: Connection): Boolean = sql"""delete from "sales"."creditcard" where "creditcardid" = ${Fragment.encode(CustomCreditcardId.pgType, creditcardid)}""".update().runUnchecked(c) > 0

  override def deleteByIds(creditcardids: Array[/* user-picked */ CustomCreditcardId])(using c: Connection): Int = {
    sql"""delete
    from "sales"."creditcard"
    where "creditcardid" = ANY(${Fragment.encode(CustomCreditcardId.pgTypeArray, creditcardids)})"""
      .update()
      .runUnchecked(c)
  }

  override def insert(unsaved: CreditcardRow)(using c: Connection): CreditcardRow = {
  sql"""insert into "sales"."creditcard"("creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate")
    values (${Fragment.encode(CustomCreditcardId.pgType, unsaved.creditcardid)}::int4, ${Fragment.encode(PgTypes.text, unsaved.cardtype)}, ${Fragment.encode(PgTypes.text, unsaved.cardnumber)}, ${Fragment.encode(ScalaDbTypes.PgTypes.int2, unsaved.expmonth)}::int2, ${Fragment.encode(ScalaDbTypes.PgTypes.int2, unsaved.expyear)}::int2, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    RETURNING "creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate"
    """
    .updateReturning(CreditcardRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: CreditcardRowUnsaved)(using c: Connection): CreditcardRow = {
    val columns: ListBuffer[Fragment] = ListBuffer()
    val values: ListBuffer[Fragment] = ListBuffer()
    columns.addOne(Fragment.lit(""""cardtype"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(PgTypes.text, unsaved.cardtype)}"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""cardnumber"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(PgTypes.text, unsaved.cardnumber)}"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""expmonth"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(ScalaDbTypes.PgTypes.int2, unsaved.expmonth)}::int2"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""expyear"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(ScalaDbTypes.PgTypes.int2, unsaved.expyear)}::int2"): @scala.annotation.nowarn
    unsaved.creditcardid.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""creditcardid"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(CustomCreditcardId.pgType, value)}::int4"): @scala.annotation.nowarn }
    );
    unsaved.modifieddate.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.timestamp, value)}::timestamp"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      sql"""insert into "sales"."creditcard"(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      RETURNING "creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate"
      """
    }
    return q.updateReturning(CreditcardRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: Iterator[CreditcardRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "sales"."creditcard"("creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate") FROM STDIN""", batchSize, unsaved.toJavaIterator, c, CreditcardRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[CreditcardRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "sales"."creditcard"("cardtype", "cardnumber", "expmonth", "expyear", "creditcardid", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved.toJavaIterator, c, CreditcardRowUnsaved.pgText)

  override def select: SelectBuilder[CreditcardFields, CreditcardRow] = SelectBuilder.of(""""sales"."creditcard"""", CreditcardFields.structure, CreditcardRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): List[CreditcardRow] = {
    sql"""select "creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate"
    from "sales"."creditcard"
    """.query(CreditcardRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(creditcardid: /* user-picked */ CustomCreditcardId)(using c: Connection): Option[CreditcardRow] = {
    sql"""select "creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate"
    from "sales"."creditcard"
    where "creditcardid" = ${Fragment.encode(CustomCreditcardId.pgType, creditcardid)}""".query(CreditcardRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(creditcardids: Array[/* user-picked */ CustomCreditcardId])(using c: Connection): List[CreditcardRow] = {
    sql"""select "creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate"
    from "sales"."creditcard"
    where "creditcardid" = ANY(${Fragment.encode(CustomCreditcardId.pgTypeArray, creditcardids)})""".query(CreditcardRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(creditcardids: Array[/* user-picked */ CustomCreditcardId])(using c: Connection): Map[/* user-picked */ CustomCreditcardId, CreditcardRow] = {
    val ret: scala.collection.mutable.Map[/* user-picked */ CustomCreditcardId, CreditcardRow] = scala.collection.mutable.Map.empty[/* user-picked */ CustomCreditcardId, CreditcardRow]
    selectByIds(creditcardids)(using c).foreach(row => ret.put(row.creditcardid, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[CreditcardFields, CreditcardRow] = UpdateBuilder.of(""""sales"."creditcard"""", CreditcardFields.structure, CreditcardRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: CreditcardRow)(using c: Connection): Boolean = {
    val creditcardid: /* user-picked */ CustomCreditcardId = row.creditcardid
    return sql"""update "sales"."creditcard"
    set "cardtype" = ${Fragment.encode(PgTypes.text, row.cardtype)},
    "cardnumber" = ${Fragment.encode(PgTypes.text, row.cardnumber)},
    "expmonth" = ${Fragment.encode(ScalaDbTypes.PgTypes.int2, row.expmonth)}::int2,
    "expyear" = ${Fragment.encode(ScalaDbTypes.PgTypes.int2, row.expyear)}::int2,
    "modifieddate" = ${Fragment.encode(PgTypes.timestamp, row.modifieddate)}::timestamp
    where "creditcardid" = ${Fragment.encode(CustomCreditcardId.pgType, creditcardid)}""".update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: CreditcardRow)(using c: Connection): CreditcardRow = {
  sql"""insert into "sales"."creditcard"("creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate")
    values (${Fragment.encode(CustomCreditcardId.pgType, unsaved.creditcardid)}::int4, ${Fragment.encode(PgTypes.text, unsaved.cardtype)}, ${Fragment.encode(PgTypes.text, unsaved.cardnumber)}, ${Fragment.encode(ScalaDbTypes.PgTypes.int2, unsaved.expmonth)}::int2, ${Fragment.encode(ScalaDbTypes.PgTypes.int2, unsaved.expyear)}::int2, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    on conflict ("creditcardid")
    do update set
      "cardtype" = EXCLUDED."cardtype",
    "cardnumber" = EXCLUDED."cardnumber",
    "expmonth" = EXCLUDED."expmonth",
    "expyear" = EXCLUDED."expyear",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate""""
    .updateReturning(CreditcardRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: Iterator[CreditcardRow])(using c: Connection): List[CreditcardRow] = {
    sql"""insert into "sales"."creditcard"("creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate")
    values (?::int4, ?, ?, ?::int2, ?::int2, ?::timestamp)
    on conflict ("creditcardid")
    do update set
      "cardtype" = EXCLUDED."cardtype",
    "cardnumber" = EXCLUDED."cardnumber",
    "expmonth" = EXCLUDED."expmonth",
    "expyear" = EXCLUDED."expyear",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate""""
      .updateManyReturning(CreditcardRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[CreditcardRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    sql"""create temporary table creditcard_TEMP (like "sales"."creditcard") on commit drop""".update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy creditcard_TEMP("creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate") from stdin""", batchSize, unsaved.toJavaIterator, c, CreditcardRow.pgText): @scala.annotation.nowarn
    return sql"""insert into "sales"."creditcard"("creditcardid", "cardtype", "cardnumber", "expmonth", "expyear", "modifieddate")
    select * from creditcard_TEMP
    on conflict ("creditcardid")
    do update set
      "cardtype" = EXCLUDED."cardtype",
    "cardnumber" = EXCLUDED."cardnumber",
    "expmonth" = EXCLUDED."expmonth",
    "expyear" = EXCLUDED."expyear",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table creditcard_TEMP;""".update().runUnchecked(c)
  }
}