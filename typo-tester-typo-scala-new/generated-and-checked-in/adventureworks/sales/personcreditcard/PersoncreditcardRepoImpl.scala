/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.personcreditcard

import adventureworks.person.businessentity.BusinessentityId
import adventureworks.userdefined.CustomCreditcardId
import java.sql.Connection
import scala.collection.mutable.ListBuffer
import typo.runtime.PgTypes
import typo.runtime.streamingInsert
import typo.scaladsl.DeleteBuilder
import typo.scaladsl.Dialect
import typo.scaladsl.Fragment
import typo.scaladsl.ScalaIteratorOps
import typo.scaladsl.SelectBuilder
import typo.scaladsl.UpdateBuilder
import typo.scaladsl.Fragment.sql

class PersoncreditcardRepoImpl extends PersoncreditcardRepo {
  override def delete: DeleteBuilder[PersoncreditcardFields, PersoncreditcardRow] = DeleteBuilder.of(""""sales"."personcreditcard"""", PersoncreditcardFields.structure, Dialect.POSTGRESQL)

  override def deleteById(compositeId: PersoncreditcardId)(using c: Connection): Boolean = sql"""delete from "sales"."personcreditcard" where "businessentityid" = ${Fragment.encode(BusinessentityId.pgType, compositeId.businessentityid)} AND "creditcardid" = ${Fragment.encode(CustomCreditcardId.pgType, compositeId.creditcardid)}""".update().runUnchecked(c) > 0

  override def deleteByIds(compositeIds: Array[PersoncreditcardId])(using c: Connection): Int = {
    val businessentityid: Array[BusinessentityId] = compositeIds.map(_.businessentityid)
    val creditcardid: Array[/* user-picked */ CustomCreditcardId] = compositeIds.map(_.creditcardid)
    return sql"""delete
    from "sales"."personcreditcard"
    where ("businessentityid", "creditcardid")
    in (select * from unnest(${Fragment.encode(BusinessentityId.pgTypeArray, businessentityid)}, ${Fragment.encode(CustomCreditcardId.pgTypeArray, creditcardid)}))
    """.update().runUnchecked(c)
  }

  override def insert(unsaved: PersoncreditcardRow)(using c: Connection): PersoncreditcardRow = {
  sql"""insert into "sales"."personcreditcard"("businessentityid", "creditcardid", "modifieddate")
    values (${Fragment.encode(BusinessentityId.pgType, unsaved.businessentityid)}::int4, ${Fragment.encode(CustomCreditcardId.pgType, unsaved.creditcardid)}::int4, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    RETURNING "businessentityid", "creditcardid", "modifieddate"
    """
    .updateReturning(PersoncreditcardRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: PersoncreditcardRowUnsaved)(using c: Connection): PersoncreditcardRow = {
    val columns: ListBuffer[Fragment] = ListBuffer()
    val values: ListBuffer[Fragment] = ListBuffer()
    columns.addOne(Fragment.lit(""""businessentityid"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(BusinessentityId.pgType, unsaved.businessentityid)}::int4"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""creditcardid"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(CustomCreditcardId.pgType, unsaved.creditcardid)}::int4"): @scala.annotation.nowarn
    unsaved.modifieddate.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.timestamp, value)}::timestamp"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      sql"""insert into "sales"."personcreditcard"(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      RETURNING "businessentityid", "creditcardid", "modifieddate"
      """
    }
    return q.updateReturning(PersoncreditcardRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: Iterator[PersoncreditcardRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "sales"."personcreditcard"("businessentityid", "creditcardid", "modifieddate") FROM STDIN""", batchSize, unsaved.toJavaIterator, c, PersoncreditcardRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[PersoncreditcardRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "sales"."personcreditcard"("businessentityid", "creditcardid", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved.toJavaIterator, c, PersoncreditcardRowUnsaved.pgText)

  override def select: SelectBuilder[PersoncreditcardFields, PersoncreditcardRow] = SelectBuilder.of(""""sales"."personcreditcard"""", PersoncreditcardFields.structure, PersoncreditcardRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): List[PersoncreditcardRow] = {
    sql"""select "businessentityid", "creditcardid", "modifieddate"
    from "sales"."personcreditcard"
    """.query(PersoncreditcardRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(compositeId: PersoncreditcardId)(using c: Connection): Option[PersoncreditcardRow] = {
    sql"""select "businessentityid", "creditcardid", "modifieddate"
    from "sales"."personcreditcard"
    where "businessentityid" = ${Fragment.encode(BusinessentityId.pgType, compositeId.businessentityid)} AND "creditcardid" = ${Fragment.encode(CustomCreditcardId.pgType, compositeId.creditcardid)}""".query(PersoncreditcardRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(compositeIds: Array[PersoncreditcardId])(using c: Connection): List[PersoncreditcardRow] = {
    val businessentityid: Array[BusinessentityId] = compositeIds.map(_.businessentityid)
    val creditcardid: Array[/* user-picked */ CustomCreditcardId] = compositeIds.map(_.creditcardid)
    return sql"""select "businessentityid", "creditcardid", "modifieddate"
    from "sales"."personcreditcard"
    where ("businessentityid", "creditcardid")
    in (select * from unnest(${Fragment.encode(BusinessentityId.pgTypeArray, businessentityid)}, ${Fragment.encode(CustomCreditcardId.pgTypeArray, creditcardid)}))
    """.query(PersoncreditcardRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(compositeIds: Array[PersoncreditcardId])(using c: Connection): Map[PersoncreditcardId, PersoncreditcardRow] = {
    val ret: scala.collection.mutable.Map[PersoncreditcardId, PersoncreditcardRow] = scala.collection.mutable.Map.empty[PersoncreditcardId, PersoncreditcardRow]
    selectByIds(compositeIds)(using c).foreach(row => ret.put(row.compositeId, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[PersoncreditcardFields, PersoncreditcardRow] = UpdateBuilder.of(""""sales"."personcreditcard"""", PersoncreditcardFields.structure, PersoncreditcardRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: PersoncreditcardRow)(using c: Connection): Boolean = {
    val compositeId: PersoncreditcardId = row.compositeId
    return sql"""update "sales"."personcreditcard"
    set "modifieddate" = ${Fragment.encode(PgTypes.timestamp, row.modifieddate)}::timestamp
    where "businessentityid" = ${Fragment.encode(BusinessentityId.pgType, compositeId.businessentityid)} AND "creditcardid" = ${Fragment.encode(CustomCreditcardId.pgType, compositeId.creditcardid)}""".update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: PersoncreditcardRow)(using c: Connection): PersoncreditcardRow = {
  sql"""insert into "sales"."personcreditcard"("businessentityid", "creditcardid", "modifieddate")
    values (${Fragment.encode(BusinessentityId.pgType, unsaved.businessentityid)}::int4, ${Fragment.encode(CustomCreditcardId.pgType, unsaved.creditcardid)}::int4, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    on conflict ("businessentityid", "creditcardid")
    do update set
      "modifieddate" = EXCLUDED."modifieddate"
    returning "businessentityid", "creditcardid", "modifieddate""""
    .updateReturning(PersoncreditcardRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: Iterator[PersoncreditcardRow])(using c: Connection): List[PersoncreditcardRow] = {
    sql"""insert into "sales"."personcreditcard"("businessentityid", "creditcardid", "modifieddate")
    values (?::int4, ?::int4, ?::timestamp)
    on conflict ("businessentityid", "creditcardid")
    do update set
      "modifieddate" = EXCLUDED."modifieddate"
    returning "businessentityid", "creditcardid", "modifieddate""""
      .updateManyReturning(PersoncreditcardRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[PersoncreditcardRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    sql"""create temporary table personcreditcard_TEMP (like "sales"."personcreditcard") on commit drop""".update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy personcreditcard_TEMP("businessentityid", "creditcardid", "modifieddate") from stdin""", batchSize, unsaved.toJavaIterator, c, PersoncreditcardRow.pgText): @scala.annotation.nowarn
    return sql"""insert into "sales"."personcreditcard"("businessentityid", "creditcardid", "modifieddate")
    select * from personcreditcard_TEMP
    on conflict ("businessentityid", "creditcardid")
    do update set
      "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table personcreditcard_TEMP;""".update().runUnchecked(c)
  }
}