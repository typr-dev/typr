/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.salesreason

import adventureworks.public.Name
import java.sql.Connection
import scala.collection.mutable.ListBuffer
import typo.runtime.PgTypes
import typo.runtime.streamingInsert
import typo.scaladsl.DeleteBuilder
import typo.scaladsl.Dialect
import typo.scaladsl.Fragment
import typo.scaladsl.ScalaIteratorOps
import typo.scaladsl.SelectBuilder
import typo.scaladsl.UpdateBuilder
import typo.scaladsl.Fragment.sql

class SalesreasonRepoImpl extends SalesreasonRepo {
  override def delete: DeleteBuilder[SalesreasonFields, SalesreasonRow] = DeleteBuilder.of(""""sales"."salesreason"""", SalesreasonFields.structure, Dialect.POSTGRESQL)

  override def deleteById(salesreasonid: SalesreasonId)(using c: Connection): Boolean = sql"""delete from "sales"."salesreason" where "salesreasonid" = ${Fragment.encode(SalesreasonId.pgType, salesreasonid)}""".update().runUnchecked(c) > 0

  override def deleteByIds(salesreasonids: Array[SalesreasonId])(using c: Connection): Int = {
    sql"""delete
    from "sales"."salesreason"
    where "salesreasonid" = ANY(${Fragment.encode(SalesreasonId.pgTypeArray, salesreasonids)})"""
      .update()
      .runUnchecked(c)
  }

  override def insert(unsaved: SalesreasonRow)(using c: Connection): SalesreasonRow = {
  sql"""insert into "sales"."salesreason"("salesreasonid", "name", "reasontype", "modifieddate")
    values (${Fragment.encode(SalesreasonId.pgType, unsaved.salesreasonid)}::int4, ${Fragment.encode(Name.pgType, unsaved.name)}::varchar, ${Fragment.encode(Name.pgType, unsaved.reasontype)}::varchar, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    RETURNING "salesreasonid", "name", "reasontype", "modifieddate"
    """
    .updateReturning(SalesreasonRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: SalesreasonRowUnsaved)(using c: Connection): SalesreasonRow = {
    val columns: ListBuffer[Fragment] = ListBuffer()
    val values: ListBuffer[Fragment] = ListBuffer()
    columns.addOne(Fragment.lit(""""name"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(Name.pgType, unsaved.name)}::varchar"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""reasontype"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(Name.pgType, unsaved.reasontype)}::varchar"): @scala.annotation.nowarn
    unsaved.salesreasonid.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""salesreasonid"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(SalesreasonId.pgType, value)}::int4"): @scala.annotation.nowarn }
    );
    unsaved.modifieddate.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.timestamp, value)}::timestamp"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      sql"""insert into "sales"."salesreason"(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      RETURNING "salesreasonid", "name", "reasontype", "modifieddate"
      """
    }
    return q.updateReturning(SalesreasonRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: Iterator[SalesreasonRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "sales"."salesreason"("salesreasonid", "name", "reasontype", "modifieddate") FROM STDIN""", batchSize, unsaved.toJavaIterator, c, SalesreasonRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[SalesreasonRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "sales"."salesreason"("name", "reasontype", "salesreasonid", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved.toJavaIterator, c, SalesreasonRowUnsaved.pgText)

  override def select: SelectBuilder[SalesreasonFields, SalesreasonRow] = SelectBuilder.of(""""sales"."salesreason"""", SalesreasonFields.structure, SalesreasonRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): List[SalesreasonRow] = {
    sql"""select "salesreasonid", "name", "reasontype", "modifieddate"
    from "sales"."salesreason"
    """.query(SalesreasonRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(salesreasonid: SalesreasonId)(using c: Connection): Option[SalesreasonRow] = {
    sql"""select "salesreasonid", "name", "reasontype", "modifieddate"
    from "sales"."salesreason"
    where "salesreasonid" = ${Fragment.encode(SalesreasonId.pgType, salesreasonid)}""".query(SalesreasonRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(salesreasonids: Array[SalesreasonId])(using c: Connection): List[SalesreasonRow] = {
    sql"""select "salesreasonid", "name", "reasontype", "modifieddate"
    from "sales"."salesreason"
    where "salesreasonid" = ANY(${Fragment.encode(SalesreasonId.pgTypeArray, salesreasonids)})""".query(SalesreasonRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(salesreasonids: Array[SalesreasonId])(using c: Connection): Map[SalesreasonId, SalesreasonRow] = {
    val ret: scala.collection.mutable.Map[SalesreasonId, SalesreasonRow] = scala.collection.mutable.Map.empty[SalesreasonId, SalesreasonRow]
    selectByIds(salesreasonids)(using c).foreach(row => ret.put(row.salesreasonid, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[SalesreasonFields, SalesreasonRow] = UpdateBuilder.of(""""sales"."salesreason"""", SalesreasonFields.structure, SalesreasonRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: SalesreasonRow)(using c: Connection): Boolean = {
    val salesreasonid: SalesreasonId = row.salesreasonid
    return sql"""update "sales"."salesreason"
    set "name" = ${Fragment.encode(Name.pgType, row.name)}::varchar,
    "reasontype" = ${Fragment.encode(Name.pgType, row.reasontype)}::varchar,
    "modifieddate" = ${Fragment.encode(PgTypes.timestamp, row.modifieddate)}::timestamp
    where "salesreasonid" = ${Fragment.encode(SalesreasonId.pgType, salesreasonid)}""".update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: SalesreasonRow)(using c: Connection): SalesreasonRow = {
  sql"""insert into "sales"."salesreason"("salesreasonid", "name", "reasontype", "modifieddate")
    values (${Fragment.encode(SalesreasonId.pgType, unsaved.salesreasonid)}::int4, ${Fragment.encode(Name.pgType, unsaved.name)}::varchar, ${Fragment.encode(Name.pgType, unsaved.reasontype)}::varchar, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    on conflict ("salesreasonid")
    do update set
      "name" = EXCLUDED."name",
    "reasontype" = EXCLUDED."reasontype",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "salesreasonid", "name", "reasontype", "modifieddate""""
    .updateReturning(SalesreasonRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: Iterator[SalesreasonRow])(using c: Connection): List[SalesreasonRow] = {
    sql"""insert into "sales"."salesreason"("salesreasonid", "name", "reasontype", "modifieddate")
    values (?::int4, ?::varchar, ?::varchar, ?::timestamp)
    on conflict ("salesreasonid")
    do update set
      "name" = EXCLUDED."name",
    "reasontype" = EXCLUDED."reasontype",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "salesreasonid", "name", "reasontype", "modifieddate""""
      .updateManyReturning(SalesreasonRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[SalesreasonRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    sql"""create temporary table salesreason_TEMP (like "sales"."salesreason") on commit drop""".update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy salesreason_TEMP("salesreasonid", "name", "reasontype", "modifieddate") from stdin""", batchSize, unsaved.toJavaIterator, c, SalesreasonRow.pgText): @scala.annotation.nowarn
    return sql"""insert into "sales"."salesreason"("salesreasonid", "name", "reasontype", "modifieddate")
    select * from salesreason_TEMP
    on conflict ("salesreasonid")
    do update set
      "name" = EXCLUDED."name",
    "reasontype" = EXCLUDED."reasontype",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table salesreason_TEMP;""".update().runUnchecked(c)
  }
}