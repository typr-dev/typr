/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.personphone

import adventureworks.person.businessentity.BusinessentityId
import adventureworks.person.phonenumbertype.PhonenumbertypeId
import adventureworks.public.Phone
import java.sql.Connection
import scala.collection.mutable.ListBuffer
import typo.runtime.PgTypes
import typo.runtime.streamingInsert
import typo.scaladsl.DeleteBuilder
import typo.scaladsl.Dialect
import typo.scaladsl.Fragment
import typo.scaladsl.ScalaIteratorOps
import typo.scaladsl.SelectBuilder
import typo.scaladsl.UpdateBuilder
import typo.scaladsl.Fragment.sql

class PersonphoneRepoImpl extends PersonphoneRepo {
  override def delete: DeleteBuilder[PersonphoneFields, PersonphoneRow] = DeleteBuilder.of(""""person"."personphone"""", PersonphoneFields.structure, Dialect.POSTGRESQL)

  override def deleteById(compositeId: PersonphoneId)(using c: Connection): Boolean = sql"""delete from "person"."personphone" where "businessentityid" = ${Fragment.encode(BusinessentityId.pgType, compositeId.businessentityid)} AND "phonenumber" = ${Fragment.encode(Phone.pgType, compositeId.phonenumber)} AND "phonenumbertypeid" = ${Fragment.encode(PhonenumbertypeId.pgType, compositeId.phonenumbertypeid)}""".update().runUnchecked(c) > 0

  override def deleteByIds(compositeIds: Array[PersonphoneId])(using c: Connection): Int = {
    val businessentityid: Array[BusinessentityId] = compositeIds.map(_.businessentityid)
    val phonenumber: Array[Phone] = compositeIds.map(_.phonenumber)
    val phonenumbertypeid: Array[PhonenumbertypeId] = compositeIds.map(_.phonenumbertypeid)
    return sql"""delete
    from "person"."personphone"
    where ("businessentityid", "phonenumber", "phonenumbertypeid")
    in (select * from unnest(${Fragment.encode(BusinessentityId.pgTypeArray, businessentityid)}, ${Fragment.encode(Phone.pgTypeArray, phonenumber)}, ${Fragment.encode(PhonenumbertypeId.pgTypeArray, phonenumbertypeid)}))
    """.update().runUnchecked(c)
  }

  override def insert(unsaved: PersonphoneRow)(using c: Connection): PersonphoneRow = {
  sql"""insert into "person"."personphone"("businessentityid", "phonenumber", "phonenumbertypeid", "modifieddate")
    values (${Fragment.encode(BusinessentityId.pgType, unsaved.businessentityid)}::int4, ${Fragment.encode(Phone.pgType, unsaved.phonenumber)}::varchar, ${Fragment.encode(PhonenumbertypeId.pgType, unsaved.phonenumbertypeid)}::int4, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    RETURNING "businessentityid", "phonenumber", "phonenumbertypeid", "modifieddate"
    """
    .updateReturning(PersonphoneRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: PersonphoneRowUnsaved)(using c: Connection): PersonphoneRow = {
    val columns: ListBuffer[Fragment] = ListBuffer()
    val values: ListBuffer[Fragment] = ListBuffer()
    columns.addOne(Fragment.lit(""""businessentityid"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(BusinessentityId.pgType, unsaved.businessentityid)}::int4"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""phonenumber"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(Phone.pgType, unsaved.phonenumber)}::varchar"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""phonenumbertypeid"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(PhonenumbertypeId.pgType, unsaved.phonenumbertypeid)}::int4"): @scala.annotation.nowarn
    unsaved.modifieddate.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.timestamp, value)}::timestamp"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      sql"""insert into "person"."personphone"(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      RETURNING "businessentityid", "phonenumber", "phonenumbertypeid", "modifieddate"
      """
    }
    return q.updateReturning(PersonphoneRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: Iterator[PersonphoneRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "person"."personphone"("businessentityid", "phonenumber", "phonenumbertypeid", "modifieddate") FROM STDIN""", batchSize, unsaved.toJavaIterator, c, PersonphoneRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[PersonphoneRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "person"."personphone"("businessentityid", "phonenumber", "phonenumbertypeid", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved.toJavaIterator, c, PersonphoneRowUnsaved.pgText)

  override def select: SelectBuilder[PersonphoneFields, PersonphoneRow] = SelectBuilder.of(""""person"."personphone"""", PersonphoneFields.structure, PersonphoneRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): List[PersonphoneRow] = {
    sql"""select "businessentityid", "phonenumber", "phonenumbertypeid", "modifieddate"
    from "person"."personphone"
    """.query(PersonphoneRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(compositeId: PersonphoneId)(using c: Connection): Option[PersonphoneRow] = {
    sql"""select "businessentityid", "phonenumber", "phonenumbertypeid", "modifieddate"
    from "person"."personphone"
    where "businessentityid" = ${Fragment.encode(BusinessentityId.pgType, compositeId.businessentityid)} AND "phonenumber" = ${Fragment.encode(Phone.pgType, compositeId.phonenumber)} AND "phonenumbertypeid" = ${Fragment.encode(PhonenumbertypeId.pgType, compositeId.phonenumbertypeid)}""".query(PersonphoneRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(compositeIds: Array[PersonphoneId])(using c: Connection): List[PersonphoneRow] = {
    val businessentityid: Array[BusinessentityId] = compositeIds.map(_.businessentityid)
    val phonenumber: Array[Phone] = compositeIds.map(_.phonenumber)
    val phonenumbertypeid: Array[PhonenumbertypeId] = compositeIds.map(_.phonenumbertypeid)
    return sql"""select "businessentityid", "phonenumber", "phonenumbertypeid", "modifieddate"
    from "person"."personphone"
    where ("businessentityid", "phonenumber", "phonenumbertypeid")
    in (select * from unnest(${Fragment.encode(BusinessentityId.pgTypeArray, businessentityid)}, ${Fragment.encode(Phone.pgTypeArray, phonenumber)}, ${Fragment.encode(PhonenumbertypeId.pgTypeArray, phonenumbertypeid)}))
    """.query(PersonphoneRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(compositeIds: Array[PersonphoneId])(using c: Connection): Map[PersonphoneId, PersonphoneRow] = {
    val ret: scala.collection.mutable.Map[PersonphoneId, PersonphoneRow] = scala.collection.mutable.Map.empty[PersonphoneId, PersonphoneRow]
    selectByIds(compositeIds)(using c).foreach(row => ret.put(row.compositeId, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[PersonphoneFields, PersonphoneRow] = UpdateBuilder.of(""""person"."personphone"""", PersonphoneFields.structure, PersonphoneRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: PersonphoneRow)(using c: Connection): Boolean = {
    val compositeId: PersonphoneId = row.compositeId
    return sql"""update "person"."personphone"
    set "modifieddate" = ${Fragment.encode(PgTypes.timestamp, row.modifieddate)}::timestamp
    where "businessentityid" = ${Fragment.encode(BusinessentityId.pgType, compositeId.businessentityid)} AND "phonenumber" = ${Fragment.encode(Phone.pgType, compositeId.phonenumber)} AND "phonenumbertypeid" = ${Fragment.encode(PhonenumbertypeId.pgType, compositeId.phonenumbertypeid)}""".update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: PersonphoneRow)(using c: Connection): PersonphoneRow = {
  sql"""insert into "person"."personphone"("businessentityid", "phonenumber", "phonenumbertypeid", "modifieddate")
    values (${Fragment.encode(BusinessentityId.pgType, unsaved.businessentityid)}::int4, ${Fragment.encode(Phone.pgType, unsaved.phonenumber)}::varchar, ${Fragment.encode(PhonenumbertypeId.pgType, unsaved.phonenumbertypeid)}::int4, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    on conflict ("businessentityid", "phonenumber", "phonenumbertypeid")
    do update set
      "modifieddate" = EXCLUDED."modifieddate"
    returning "businessentityid", "phonenumber", "phonenumbertypeid", "modifieddate""""
    .updateReturning(PersonphoneRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: Iterator[PersonphoneRow])(using c: Connection): List[PersonphoneRow] = {
    sql"""insert into "person"."personphone"("businessentityid", "phonenumber", "phonenumbertypeid", "modifieddate")
    values (?::int4, ?::varchar, ?::int4, ?::timestamp)
    on conflict ("businessentityid", "phonenumber", "phonenumbertypeid")
    do update set
      "modifieddate" = EXCLUDED."modifieddate"
    returning "businessentityid", "phonenumber", "phonenumbertypeid", "modifieddate""""
      .updateManyReturning(PersonphoneRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[PersonphoneRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    sql"""create temporary table personphone_TEMP (like "person"."personphone") on commit drop""".update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy personphone_TEMP("businessentityid", "phonenumber", "phonenumbertypeid", "modifieddate") from stdin""", batchSize, unsaved.toJavaIterator, c, PersonphoneRow.pgText): @scala.annotation.nowarn
    return sql"""insert into "person"."personphone"("businessentityid", "phonenumber", "phonenumbertypeid", "modifieddate")
    select * from personphone_TEMP
    on conflict ("businessentityid", "phonenumber", "phonenumbertypeid")
    do update set
      "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table personphone_TEMP;""".update().runUnchecked(c)
  }
}