/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.contacttype

import adventureworks.public.Name
import java.sql.Connection
import scala.collection.mutable.ListBuffer
import typo.runtime.PgTypes
import typo.runtime.streamingInsert
import typo.scaladsl.DeleteBuilder
import typo.scaladsl.Dialect
import typo.scaladsl.Fragment
import typo.scaladsl.ScalaIteratorOps
import typo.scaladsl.SelectBuilder
import typo.scaladsl.UpdateBuilder
import typo.scaladsl.Fragment.sql

class ContacttypeRepoImpl extends ContacttypeRepo {
  override def delete: DeleteBuilder[ContacttypeFields, ContacttypeRow] = DeleteBuilder.of(""""person"."contacttype"""", ContacttypeFields.structure, Dialect.POSTGRESQL)

  override def deleteById(contacttypeid: ContacttypeId)(using c: Connection): Boolean = sql"""delete from "person"."contacttype" where "contacttypeid" = ${Fragment.encode(ContacttypeId.pgType, contacttypeid)}""".update().runUnchecked(c) > 0

  override def deleteByIds(contacttypeids: Array[ContacttypeId])(using c: Connection): Int = {
    sql"""delete
    from "person"."contacttype"
    where "contacttypeid" = ANY(${Fragment.encode(ContacttypeId.pgTypeArray, contacttypeids)})"""
      .update()
      .runUnchecked(c)
  }

  override def insert(unsaved: ContacttypeRow)(using c: Connection): ContacttypeRow = {
  sql"""insert into "person"."contacttype"("contacttypeid", "name", "modifieddate")
    values (${Fragment.encode(ContacttypeId.pgType, unsaved.contacttypeid)}::int4, ${Fragment.encode(Name.pgType, unsaved.name)}::varchar, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    RETURNING "contacttypeid", "name", "modifieddate"
    """
    .updateReturning(ContacttypeRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: ContacttypeRowUnsaved)(using c: Connection): ContacttypeRow = {
    val columns: ListBuffer[Fragment] = ListBuffer()
    val values: ListBuffer[Fragment] = ListBuffer()
    columns.addOne(Fragment.lit(""""name"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(Name.pgType, unsaved.name)}::varchar"): @scala.annotation.nowarn
    unsaved.contacttypeid.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""contacttypeid"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(ContacttypeId.pgType, value)}::int4"): @scala.annotation.nowarn }
    );
    unsaved.modifieddate.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.timestamp, value)}::timestamp"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      sql"""insert into "person"."contacttype"(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      RETURNING "contacttypeid", "name", "modifieddate"
      """
    }
    return q.updateReturning(ContacttypeRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: Iterator[ContacttypeRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "person"."contacttype"("contacttypeid", "name", "modifieddate") FROM STDIN""", batchSize, unsaved.toJavaIterator, c, ContacttypeRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[ContacttypeRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "person"."contacttype"("name", "contacttypeid", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved.toJavaIterator, c, ContacttypeRowUnsaved.pgText)

  override def select: SelectBuilder[ContacttypeFields, ContacttypeRow] = SelectBuilder.of(""""person"."contacttype"""", ContacttypeFields.structure, ContacttypeRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): List[ContacttypeRow] = {
    sql"""select "contacttypeid", "name", "modifieddate"
    from "person"."contacttype"
    """.query(ContacttypeRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(contacttypeid: ContacttypeId)(using c: Connection): Option[ContacttypeRow] = {
    sql"""select "contacttypeid", "name", "modifieddate"
    from "person"."contacttype"
    where "contacttypeid" = ${Fragment.encode(ContacttypeId.pgType, contacttypeid)}""".query(ContacttypeRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(contacttypeids: Array[ContacttypeId])(using c: Connection): List[ContacttypeRow] = {
    sql"""select "contacttypeid", "name", "modifieddate"
    from "person"."contacttype"
    where "contacttypeid" = ANY(${Fragment.encode(ContacttypeId.pgTypeArray, contacttypeids)})""".query(ContacttypeRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(contacttypeids: Array[ContacttypeId])(using c: Connection): Map[ContacttypeId, ContacttypeRow] = {
    val ret: scala.collection.mutable.Map[ContacttypeId, ContacttypeRow] = scala.collection.mutable.Map.empty[ContacttypeId, ContacttypeRow]
    selectByIds(contacttypeids)(using c).foreach(row => ret.put(row.contacttypeid, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[ContacttypeFields, ContacttypeRow] = UpdateBuilder.of(""""person"."contacttype"""", ContacttypeFields.structure, ContacttypeRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: ContacttypeRow)(using c: Connection): Boolean = {
    val contacttypeid: ContacttypeId = row.contacttypeid
    return sql"""update "person"."contacttype"
    set "name" = ${Fragment.encode(Name.pgType, row.name)}::varchar,
    "modifieddate" = ${Fragment.encode(PgTypes.timestamp, row.modifieddate)}::timestamp
    where "contacttypeid" = ${Fragment.encode(ContacttypeId.pgType, contacttypeid)}""".update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: ContacttypeRow)(using c: Connection): ContacttypeRow = {
  sql"""insert into "person"."contacttype"("contacttypeid", "name", "modifieddate")
    values (${Fragment.encode(ContacttypeId.pgType, unsaved.contacttypeid)}::int4, ${Fragment.encode(Name.pgType, unsaved.name)}::varchar, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    on conflict ("contacttypeid")
    do update set
      "name" = EXCLUDED."name",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "contacttypeid", "name", "modifieddate""""
    .updateReturning(ContacttypeRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: Iterator[ContacttypeRow])(using c: Connection): List[ContacttypeRow] = {
    sql"""insert into "person"."contacttype"("contacttypeid", "name", "modifieddate")
    values (?::int4, ?::varchar, ?::timestamp)
    on conflict ("contacttypeid")
    do update set
      "name" = EXCLUDED."name",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "contacttypeid", "name", "modifieddate""""
      .updateManyReturning(ContacttypeRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[ContacttypeRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    sql"""create temporary table contacttype_TEMP (like "person"."contacttype") on commit drop""".update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy contacttype_TEMP("contacttypeid", "name", "modifieddate") from stdin""", batchSize, unsaved.toJavaIterator, c, ContacttypeRow.pgText): @scala.annotation.nowarn
    return sql"""insert into "person"."contacttype"("contacttypeid", "name", "modifieddate")
    select * from contacttype_TEMP
    on conflict ("contacttypeid")
    do update set
      "name" = EXCLUDED."name",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table contacttype_TEMP;""".update().runUnchecked(c)
  }
}