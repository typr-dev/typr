/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.test_utdanningstilbud

import adventureworks.public.test_organisasjon.TestOrganisasjonId
import java.sql.Connection
import typo.runtime.PgTypes
import typo.runtime.streamingInsert
import typo.scaladsl.DeleteBuilder
import typo.scaladsl.Dialect
import typo.scaladsl.Fragment
import typo.scaladsl.ScalaIteratorOps
import typo.scaladsl.SelectBuilder
import typo.scaladsl.UpdateBuilder
import typo.scaladsl.Fragment.sql

class TestUtdanningstilbudRepoImpl extends TestUtdanningstilbudRepo {
  override def delete: DeleteBuilder[TestUtdanningstilbudFields, TestUtdanningstilbudRow] = DeleteBuilder.of(""""public"."test_utdanningstilbud"""", TestUtdanningstilbudFields.structure, Dialect.POSTGRESQL)

  override def deleteById(compositeId: TestUtdanningstilbudId)(using c: Connection): Boolean = sql"""delete from "public"."test_utdanningstilbud" where "organisasjonskode" = ${Fragment.encode(TestOrganisasjonId.pgType, compositeId.organisasjonskode)} AND "utdanningsmulighet_kode" = ${Fragment.encode(PgTypes.text, compositeId.utdanningsmulighetKode)}""".update().runUnchecked(c) > 0

  override def deleteByIds(compositeIds: Array[TestUtdanningstilbudId])(using c: Connection): Int = {
    val organisasjonskode: Array[TestOrganisasjonId] = compositeIds.map(_.organisasjonskode)
    val utdanningsmulighetKode: Array[String] = compositeIds.map(_.utdanningsmulighetKode)
    return sql"""delete
    from "public"."test_utdanningstilbud"
    where ("organisasjonskode", "utdanningsmulighet_kode")
    in (select * from unnest(${Fragment.encode(TestOrganisasjonId.pgTypeArray, organisasjonskode)}, ${Fragment.encode(PgTypes.textArray, utdanningsmulighetKode)}))
    """.update().runUnchecked(c)
  }

  override def insert(unsaved: TestUtdanningstilbudRow)(using c: Connection): TestUtdanningstilbudRow = {
  sql"""insert into "public"."test_utdanningstilbud"("organisasjonskode", "utdanningsmulighet_kode")
    values (${Fragment.encode(TestOrganisasjonId.pgType, unsaved.organisasjonskode)}, ${Fragment.encode(PgTypes.text, unsaved.utdanningsmulighetKode)})
    RETURNING "organisasjonskode", "utdanningsmulighet_kode"
    """
    .updateReturning(TestUtdanningstilbudRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: Iterator[TestUtdanningstilbudRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "public"."test_utdanningstilbud"("organisasjonskode", "utdanningsmulighet_kode") FROM STDIN""", batchSize, unsaved.toJavaIterator, c, TestUtdanningstilbudRow.pgText)

  override def select: SelectBuilder[TestUtdanningstilbudFields, TestUtdanningstilbudRow] = SelectBuilder.of(""""public"."test_utdanningstilbud"""", TestUtdanningstilbudFields.structure, TestUtdanningstilbudRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): List[TestUtdanningstilbudRow] = {
    sql"""select "organisasjonskode", "utdanningsmulighet_kode"
    from "public"."test_utdanningstilbud"
    """.query(TestUtdanningstilbudRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(compositeId: TestUtdanningstilbudId)(using c: Connection): Option[TestUtdanningstilbudRow] = {
    sql"""select "organisasjonskode", "utdanningsmulighet_kode"
    from "public"."test_utdanningstilbud"
    where "organisasjonskode" = ${Fragment.encode(TestOrganisasjonId.pgType, compositeId.organisasjonskode)} AND "utdanningsmulighet_kode" = ${Fragment.encode(PgTypes.text, compositeId.utdanningsmulighetKode)}""".query(TestUtdanningstilbudRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(compositeIds: Array[TestUtdanningstilbudId])(using c: Connection): List[TestUtdanningstilbudRow] = {
    val organisasjonskode: Array[TestOrganisasjonId] = compositeIds.map(_.organisasjonskode)
    val utdanningsmulighetKode: Array[String] = compositeIds.map(_.utdanningsmulighetKode)
    return sql"""select "organisasjonskode", "utdanningsmulighet_kode"
    from "public"."test_utdanningstilbud"
    where ("organisasjonskode", "utdanningsmulighet_kode")
    in (select * from unnest(${Fragment.encode(TestOrganisasjonId.pgTypeArray, organisasjonskode)}, ${Fragment.encode(PgTypes.textArray, utdanningsmulighetKode)}))
    """.query(TestUtdanningstilbudRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(compositeIds: Array[TestUtdanningstilbudId])(using c: Connection): Map[TestUtdanningstilbudId, TestUtdanningstilbudRow] = {
    val ret: scala.collection.mutable.Map[TestUtdanningstilbudId, TestUtdanningstilbudRow] = scala.collection.mutable.Map.empty[TestUtdanningstilbudId, TestUtdanningstilbudRow]
    selectByIds(compositeIds)(using c).foreach(row => ret.put(row.compositeId, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[TestUtdanningstilbudFields, TestUtdanningstilbudRow] = UpdateBuilder.of(""""public"."test_utdanningstilbud"""", TestUtdanningstilbudFields.structure, TestUtdanningstilbudRow.`_rowParser`, Dialect.POSTGRESQL)

  override def upsert(unsaved: TestUtdanningstilbudRow)(using c: Connection): TestUtdanningstilbudRow = {
  sql"""insert into "public"."test_utdanningstilbud"("organisasjonskode", "utdanningsmulighet_kode")
    values (${Fragment.encode(TestOrganisasjonId.pgType, unsaved.organisasjonskode)}, ${Fragment.encode(PgTypes.text, unsaved.utdanningsmulighetKode)})
    on conflict ("organisasjonskode", "utdanningsmulighet_kode")
    do update set "organisasjonskode" = EXCLUDED."organisasjonskode"
    returning "organisasjonskode", "utdanningsmulighet_kode""""
    .updateReturning(TestUtdanningstilbudRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: Iterator[TestUtdanningstilbudRow])(using c: Connection): List[TestUtdanningstilbudRow] = {
    sql"""insert into "public"."test_utdanningstilbud"("organisasjonskode", "utdanningsmulighet_kode")
    values (?, ?)
    on conflict ("organisasjonskode", "utdanningsmulighet_kode")
    do update set "organisasjonskode" = EXCLUDED."organisasjonskode"
    returning "organisasjonskode", "utdanningsmulighet_kode""""
      .updateManyReturning(TestUtdanningstilbudRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[TestUtdanningstilbudRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    sql"""create temporary table test_utdanningstilbud_TEMP (like "public"."test_utdanningstilbud") on commit drop""".update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy test_utdanningstilbud_TEMP("organisasjonskode", "utdanningsmulighet_kode") from stdin""", batchSize, unsaved.toJavaIterator, c, TestUtdanningstilbudRow.pgText): @scala.annotation.nowarn
    return sql"""insert into "public"."test_utdanningstilbud"("organisasjonskode", "utdanningsmulighet_kode")
    select * from test_utdanningstilbud_TEMP
    on conflict ("organisasjonskode", "utdanningsmulighet_kode")
    do nothing
    ;
    drop table test_utdanningstilbud_TEMP;""".update().runUnchecked(c)
  }
}