/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.reviews

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typr.kotlindsl.DeleteBuilder
import typr.kotlindsl.DeleteBuilderMock
import typr.kotlindsl.DeleteParams
import typr.kotlindsl.SelectBuilder
import typr.kotlindsl.SelectBuilderMock
import typr.kotlindsl.SelectParams
import typr.kotlindsl.UpdateBuilder
import typr.kotlindsl.UpdateBuilderMock
import typr.kotlindsl.UpdateParams

data class ReviewsRepoMock(
  val toRow: (ReviewsRowUnsaved) -> ReviewsRow,
  val map: MutableMap<ReviewsId, ReviewsRow> = mutableMapOf<ReviewsId, ReviewsRow>()
) : ReviewsRepo {
  override fun delete(): DeleteBuilder<ReviewsFields, ReviewsRow> = DeleteBuilderMock(ReviewsFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.reviewId }, { id -> map.remove(id) })

  override fun deleteById(
    reviewId: ReviewsId,
    c: Connection
  ): Boolean = map.remove(reviewId) != null

  override fun deleteByIds(
    reviewIds: Array<ReviewsId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in reviewIds) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: ReviewsRow,
    c: Connection
  ): ReviewsRow {
    if (map.containsKey(unsaved.reviewId)) {
      throw RuntimeException("id " + unsaved.reviewId + " already exists")
    }
    map[unsaved.reviewId] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: ReviewsRowUnsaved,
    c: Connection
  ): ReviewsRow = insert(toRow(unsaved), c)

  override fun select(): SelectBuilder<ReviewsFields, ReviewsRow> = SelectBuilderMock(ReviewsFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<ReviewsRow> = map.values.toList()

  override fun selectById(
    reviewId: ReviewsId,
    c: Connection
  ): ReviewsRow? = map[reviewId]

  override fun selectByIds(
    reviewIds: Array<ReviewsId>,
    c: Connection
  ): List<ReviewsRow> {
    val result = ArrayList<ReviewsRow>()
    for (id in reviewIds) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    reviewIds: Array<ReviewsId>,
    c: Connection
  ): Map<ReviewsId, ReviewsRow> = selectByIds(reviewIds, c).associateBy({ row: ReviewsRow -> row.reviewId })

  override fun update(): UpdateBuilder<ReviewsFields, ReviewsRow> = UpdateBuilderMock(ReviewsFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: ReviewsRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.reviewId]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.reviewId] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: ReviewsRow,
    c: Connection
  ): ReviewsRow {
    map[unsaved.reviewId] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<ReviewsRow>,
    c: Connection
  ): List<ReviewsRow> {
    val result = ArrayList<ReviewsRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.reviewId] = row
      result.add(row)
    }
    return result
  }
}