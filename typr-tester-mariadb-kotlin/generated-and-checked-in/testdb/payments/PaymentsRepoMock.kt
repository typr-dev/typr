/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.payments

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typr.kotlindsl.DeleteBuilder
import typr.kotlindsl.DeleteBuilderMock
import typr.kotlindsl.DeleteParams
import typr.kotlindsl.SelectBuilder
import typr.kotlindsl.SelectBuilderMock
import typr.kotlindsl.SelectParams
import typr.kotlindsl.UpdateBuilder
import typr.kotlindsl.UpdateBuilderMock
import typr.kotlindsl.UpdateParams

data class PaymentsRepoMock(
  val toRow: (PaymentsRowUnsaved) -> PaymentsRow,
  val map: MutableMap<PaymentsId, PaymentsRow> = mutableMapOf<PaymentsId, PaymentsRow>()
) : PaymentsRepo {
  override fun delete(): DeleteBuilder<PaymentsFields, PaymentsRow> = DeleteBuilderMock(PaymentsFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.paymentId }, { id -> map.remove(id) })

  override fun deleteById(
    paymentId: PaymentsId,
    c: Connection
  ): Boolean = map.remove(paymentId) != null

  override fun deleteByIds(
    paymentIds: Array<PaymentsId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in paymentIds) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: PaymentsRow,
    c: Connection
  ): PaymentsRow {
    if (map.containsKey(unsaved.paymentId)) {
      throw RuntimeException("id " + unsaved.paymentId + " already exists")
    }
    map[unsaved.paymentId] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: PaymentsRowUnsaved,
    c: Connection
  ): PaymentsRow = insert(toRow(unsaved), c)

  override fun select(): SelectBuilder<PaymentsFields, PaymentsRow> = SelectBuilderMock(PaymentsFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<PaymentsRow> = map.values.toList()

  override fun selectById(
    paymentId: PaymentsId,
    c: Connection
  ): PaymentsRow? = map[paymentId]

  override fun selectByIds(
    paymentIds: Array<PaymentsId>,
    c: Connection
  ): List<PaymentsRow> {
    val result = ArrayList<PaymentsRow>()
    for (id in paymentIds) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    paymentIds: Array<PaymentsId>,
    c: Connection
  ): Map<PaymentsId, PaymentsRow> = selectByIds(paymentIds, c).associateBy({ row: PaymentsRow -> row.paymentId })

  override fun update(): UpdateBuilder<PaymentsFields, PaymentsRow> = UpdateBuilderMock(PaymentsFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: PaymentsRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.paymentId]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.paymentId] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: PaymentsRow,
    c: Connection
  ): PaymentsRow {
    map[unsaved.paymentId] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<PaymentsRow>,
    c: Connection
  ): List<PaymentsRow> {
    val result = ArrayList<PaymentsRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.paymentId] = row
      result.add(row)
    }
    return result
  }
}