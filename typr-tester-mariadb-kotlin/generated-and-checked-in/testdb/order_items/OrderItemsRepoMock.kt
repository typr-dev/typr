/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.order_items

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typr.kotlindsl.DeleteBuilder
import typr.kotlindsl.DeleteBuilderMock
import typr.kotlindsl.DeleteParams
import typr.kotlindsl.SelectBuilder
import typr.kotlindsl.SelectBuilderMock
import typr.kotlindsl.SelectParams
import typr.kotlindsl.UpdateBuilder
import typr.kotlindsl.UpdateBuilderMock
import typr.kotlindsl.UpdateParams

data class OrderItemsRepoMock(
  val toRow: (OrderItemsRowUnsaved) -> OrderItemsRow,
  val map: MutableMap<OrderItemsId, OrderItemsRow> = mutableMapOf<OrderItemsId, OrderItemsRow>()
) : OrderItemsRepo {
  override fun delete(): DeleteBuilder<OrderItemsFields, OrderItemsRow> = DeleteBuilderMock(OrderItemsFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.itemId }, { id -> map.remove(id) })

  override fun deleteById(
    itemId: OrderItemsId,
    c: Connection
  ): Boolean = map.remove(itemId) != null

  override fun deleteByIds(
    itemIds: Array<OrderItemsId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in itemIds) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: OrderItemsRow,
    c: Connection
  ): OrderItemsRow {
    if (map.containsKey(unsaved.itemId)) {
      throw RuntimeException("id " + unsaved.itemId + " already exists")
    }
    map[unsaved.itemId] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: OrderItemsRowUnsaved,
    c: Connection
  ): OrderItemsRow = insert(toRow(unsaved), c)

  override fun select(): SelectBuilder<OrderItemsFields, OrderItemsRow> = SelectBuilderMock(OrderItemsFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<OrderItemsRow> = map.values.toList()

  override fun selectById(
    itemId: OrderItemsId,
    c: Connection
  ): OrderItemsRow? = map[itemId]

  override fun selectByIds(
    itemIds: Array<OrderItemsId>,
    c: Connection
  ): List<OrderItemsRow> {
    val result = ArrayList<OrderItemsRow>()
    for (id in itemIds) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    itemIds: Array<OrderItemsId>,
    c: Connection
  ): Map<OrderItemsId, OrderItemsRow> = selectByIds(itemIds, c).associateBy({ row: OrderItemsRow -> row.itemId })

  override fun update(): UpdateBuilder<OrderItemsFields, OrderItemsRow> = UpdateBuilderMock(OrderItemsFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: OrderItemsRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.itemId]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.itemId] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: OrderItemsRow,
    c: Connection
  ): OrderItemsRow {
    map[unsaved.itemId] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<OrderItemsRow>,
    c: Connection
  ): List<OrderItemsRow> {
    val result = ArrayList<OrderItemsRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.itemId] = row
      result.add(row)
    }
    return result
  }
}