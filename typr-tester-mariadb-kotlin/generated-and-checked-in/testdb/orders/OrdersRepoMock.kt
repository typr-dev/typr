/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.orders

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typr.kotlindsl.DeleteBuilder
import typr.kotlindsl.DeleteBuilderMock
import typr.kotlindsl.DeleteParams
import typr.kotlindsl.SelectBuilder
import typr.kotlindsl.SelectBuilderMock
import typr.kotlindsl.SelectParams
import typr.kotlindsl.UpdateBuilder
import typr.kotlindsl.UpdateBuilderMock
import typr.kotlindsl.UpdateParams

data class OrdersRepoMock(
  val toRow: (OrdersRowUnsaved) -> OrdersRow,
  val map: MutableMap<OrdersId, OrdersRow> = mutableMapOf<OrdersId, OrdersRow>()
) : OrdersRepo {
  override fun delete(): DeleteBuilder<OrdersFields, OrdersRow> = DeleteBuilderMock(OrdersFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.orderId }, { id -> map.remove(id) })

  override fun deleteById(
    orderId: OrdersId,
    c: Connection
  ): Boolean = map.remove(orderId) != null

  override fun deleteByIds(
    orderIds: Array<OrdersId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in orderIds) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: OrdersRow,
    c: Connection
  ): OrdersRow {
    if (map.containsKey(unsaved.orderId)) {
      throw RuntimeException("id " + unsaved.orderId + " already exists")
    }
    map[unsaved.orderId] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: OrdersRowUnsaved,
    c: Connection
  ): OrdersRow = insert(toRow(unsaved), c)

  override fun select(): SelectBuilder<OrdersFields, OrdersRow> = SelectBuilderMock(OrdersFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<OrdersRow> = map.values.toList()

  override fun selectById(
    orderId: OrdersId,
    c: Connection
  ): OrdersRow? = map[orderId]

  override fun selectByIds(
    orderIds: Array<OrdersId>,
    c: Connection
  ): List<OrdersRow> {
    val result = ArrayList<OrdersRow>()
    for (id in orderIds) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    orderIds: Array<OrdersId>,
    c: Connection
  ): Map<OrdersId, OrdersRow> = selectByIds(orderIds, c).associateBy({ row: OrdersRow -> row.orderId })

  override fun selectByUniqueOrderNumber(
    orderNumber: String,
    c: Connection
  ): OrdersRow? = map.values.toList().find({ v -> (orderNumber == v.orderNumber) })

  override fun update(): UpdateBuilder<OrdersFields, OrdersRow> = UpdateBuilderMock(OrdersFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: OrdersRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.orderId]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.orderId] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: OrdersRow,
    c: Connection
  ): OrdersRow {
    map[unsaved.orderId] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<OrdersRow>,
    c: Connection
  ): List<OrdersRow> {
    val result = ArrayList<OrdersRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.orderId] = row
      result.add(row)
    }
    return result
  }
}