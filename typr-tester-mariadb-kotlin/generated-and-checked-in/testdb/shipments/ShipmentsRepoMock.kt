/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package testdb.shipments

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import kotlin.collections.Iterator
import kotlin.collections.List
import kotlin.collections.Map
import kotlin.collections.MutableMap
import typr.kotlindsl.DeleteBuilder
import typr.kotlindsl.DeleteBuilderMock
import typr.kotlindsl.DeleteParams
import typr.kotlindsl.SelectBuilder
import typr.kotlindsl.SelectBuilderMock
import typr.kotlindsl.SelectParams
import typr.kotlindsl.UpdateBuilder
import typr.kotlindsl.UpdateBuilderMock
import typr.kotlindsl.UpdateParams

data class ShipmentsRepoMock(
  val toRow: (ShipmentsRowUnsaved) -> ShipmentsRow,
  val map: MutableMap<ShipmentsId, ShipmentsRow> = mutableMapOf<ShipmentsId, ShipmentsRow>()
) : ShipmentsRepo {
  override fun delete(): DeleteBuilder<ShipmentsFields, ShipmentsRow> = DeleteBuilderMock(ShipmentsFields.structure, { map.values.toList() }, DeleteParams.empty(), { row -> row.shipmentId }, { id -> map.remove(id) })

  override fun deleteById(
    shipmentId: ShipmentsId,
    c: Connection
  ): Boolean = map.remove(shipmentId) != null

  override fun deleteByIds(
    shipmentIds: Array<ShipmentsId>,
    c: Connection
  ): Int {
    var count = 0
    for (id in shipmentIds) {
      if (map.remove(id) != null) {
      count = count + 1
    }
    }
    return count
  }

  override fun insert(
    unsaved: ShipmentsRow,
    c: Connection
  ): ShipmentsRow {
    if (map.containsKey(unsaved.shipmentId)) {
      throw RuntimeException("id " + unsaved.shipmentId + " already exists")
    }
    map[unsaved.shipmentId] = unsaved
    return unsaved
  }

  override fun insert(
    unsaved: ShipmentsRowUnsaved,
    c: Connection
  ): ShipmentsRow = insert(toRow(unsaved), c)

  override fun select(): SelectBuilder<ShipmentsFields, ShipmentsRow> = SelectBuilderMock(ShipmentsFields.structure, { map.values.toList() }, SelectParams.empty())

  override fun selectAll(c: Connection): List<ShipmentsRow> = map.values.toList()

  override fun selectById(
    shipmentId: ShipmentsId,
    c: Connection
  ): ShipmentsRow? = map[shipmentId]

  override fun selectByIds(
    shipmentIds: Array<ShipmentsId>,
    c: Connection
  ): List<ShipmentsRow> {
    val result = ArrayList<ShipmentsRow>()
    for (id in shipmentIds) {
      val opt = map[id]
      if (opt != null) {
      result.add(opt!!)
    }
    }
    return result
  }

  override fun selectByIdsTracked(
    shipmentIds: Array<ShipmentsId>,
    c: Connection
  ): Map<ShipmentsId, ShipmentsRow> = selectByIds(shipmentIds, c).associateBy({ row: ShipmentsRow -> row.shipmentId })

  override fun update(): UpdateBuilder<ShipmentsFields, ShipmentsRow> = UpdateBuilderMock(ShipmentsFields.structure, { map.values.toList() }, UpdateParams.empty(), { row -> row })

  override fun update(
    row: ShipmentsRow,
    c: Connection
  ): Boolean {
    val shouldUpdate = map[row.shipmentId]?.takeIf({ oldRow -> (oldRow != row) }) != null
    if (shouldUpdate) {
      map[row.shipmentId] = row
    }
    return shouldUpdate
  }

  override fun upsert(
    unsaved: ShipmentsRow,
    c: Connection
  ): ShipmentsRow {
    map[unsaved.shipmentId] = unsaved
    return unsaved
  }

  override fun upsertBatch(
    unsaved: Iterator<ShipmentsRow>,
    c: Connection
  ): List<ShipmentsRow> {
    val result = ArrayList<ShipmentsRow>()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map[row.shipmentId] = row
      result.add(row)
    }
    return result
  }
}