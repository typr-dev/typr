/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.humanresources.jobcandidate

import adventureworks.Text
import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.Defaulted.UseDefault
import adventureworks.customtypes.TypoLocalDateTime
import adventureworks.customtypes.TypoXml
import adventureworks.person.businessentity.BusinessentityId
import zio.json.JsonDecoder
import zio.json.JsonEncoder
import zio.json.ast.Json
import zio.json.internal.Write

/** This class corresponds to a row in table `humanresources.jobcandidate` which has not been persisted yet */
case class JobcandidateRowUnsaved(
  /** Employee identification number if applicant was hired. Foreign key to Employee.BusinessEntityID.
   * Points to [[adventureworks.humanresources.employee.EmployeeRow.businessentityid]]
   */
  businessentityid: Option[BusinessentityId] = None,
  /** R̩sum̩ in XML format. */
  resume: Option[TypoXml] = None,
  /** Default: nextval('humanresources.jobcandidate_jobcandidateid_seq'::regclass)
   * Primary key for JobCandidate records.
   */
  jobcandidateid: Defaulted[JobcandidateId] = new UseDefault(),
  /** Default: now() */
  modifieddate: Defaulted[TypoLocalDateTime] = new UseDefault()
) {
  def toRow(
    jobcandidateidDefault: => JobcandidateId,
    modifieddateDefault: => TypoLocalDateTime
  ): JobcandidateRow = {
    new JobcandidateRow(
      jobcandidateid = jobcandidateid.getOrElse(jobcandidateidDefault),
      businessentityid = businessentityid,
      resume = resume,
      modifieddate = modifieddate.getOrElse(modifieddateDefault)
    )
  }
}

object JobcandidateRowUnsaved {
  given jsonDecoder: JsonDecoder[JobcandidateRowUnsaved] = {
    JsonDecoder[Json.Obj].mapOrFail { jsonObj =>
      val businessentityid = jsonObj.get("businessentityid").fold[Either[String, Option[BusinessentityId]]](Right(None))(_.as(using JsonDecoder.option(using BusinessentityId.jsonDecoder)))
      val resume = jsonObj.get("resume").fold[Either[String, Option[TypoXml]]](Right(None))(_.as(using JsonDecoder.option(using TypoXml.jsonDecoder)))
      val jobcandidateid = jsonObj.get("jobcandidateid").toRight("Missing field 'jobcandidateid'").flatMap(_.as(using Defaulted.jsonDecoder(using JobcandidateId.jsonDecoder)))
      val modifieddate = jsonObj.get("modifieddate").toRight("Missing field 'modifieddate'").flatMap(_.as(using Defaulted.jsonDecoder(using TypoLocalDateTime.jsonDecoder)))
      if (businessentityid.isRight && resume.isRight && jobcandidateid.isRight && modifieddate.isRight)
        Right(JobcandidateRowUnsaved(businessentityid = businessentityid.toOption.get, resume = resume.toOption.get, jobcandidateid = jobcandidateid.toOption.get, modifieddate = modifieddate.toOption.get))
      else Left(List[Either[String, Any]](businessentityid, resume, jobcandidateid, modifieddate).flatMap(_.left.toOption).mkString(", "))
    }
  }

  given jsonEncoder: JsonEncoder[JobcandidateRowUnsaved] = {
    new JsonEncoder[JobcandidateRowUnsaved] {
      override def unsafeEncode(a: JobcandidateRowUnsaved, indent: Option[Int], out: Write): Unit = {
        out.write("{")
        out.write(""""businessentityid":""")
        JsonEncoder.option(using BusinessentityId.jsonEncoder).unsafeEncode(a.businessentityid, indent, out)
        out.write(",")
        out.write(""""resume":""")
        JsonEncoder.option(using TypoXml.jsonEncoder).unsafeEncode(a.resume, indent, out)
        out.write(",")
        out.write(""""jobcandidateid":""")
        Defaulted.jsonEncoder(using JobcandidateId.jsonEncoder).unsafeEncode(a.jobcandidateid, indent, out)
        out.write(",")
        out.write(""""modifieddate":""")
        Defaulted.jsonEncoder(using TypoLocalDateTime.jsonEncoder).unsafeEncode(a.modifieddate, indent, out)
        out.write("}")
      }
    }
  }

  given pgText: Text[JobcandidateRowUnsaved] = {
    Text.instance[JobcandidateRowUnsaved]{ (row, sb) =>
      Text.option(using BusinessentityId.pgText).unsafeEncode(row.businessentityid, sb)
      sb.append(Text.DELIMETER)
      Text.option(using TypoXml.pgText).unsafeEncode(row.resume, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using JobcandidateId.pgText).unsafeEncode(row.jobcandidateid, sb)
      sb.append(Text.DELIMETER)
      Defaulted.pgText(using TypoLocalDateTime.pgText).unsafeEncode(row.modifieddate, sb)
    }
  }
}