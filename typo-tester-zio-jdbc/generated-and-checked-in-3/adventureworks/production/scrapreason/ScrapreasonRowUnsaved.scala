/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package production
package scrapreason

import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.TypoLocalDateTime
import adventureworks.public.Name
import zio.json.JsonDecoder
import zio.json.JsonEncoder
import zio.json.ast.Json
import zio.json.internal.Write

/** This class corresponds to a row in table `production.scrapreason` which has not been persisted yet */
case class ScrapreasonRowUnsaved(
  /** Failure description. */
  name: Name,
  /** Default: nextval('production.scrapreason_scrapreasonid_seq'::regclass)
      Primary key for ScrapReason records. */
  scrapreasonid: Defaulted[ScrapreasonId] = Defaulted.UseDefault,
  /** Default: now() */
  modifieddate: Defaulted[TypoLocalDateTime] = Defaulted.UseDefault
) {
  def toRow(scrapreasonidDefault: => ScrapreasonId, modifieddateDefault: => TypoLocalDateTime): ScrapreasonRow =
    ScrapreasonRow(
      name = name,
      scrapreasonid = scrapreasonid match {
                        case Defaulted.UseDefault => scrapreasonidDefault
                        case Defaulted.Provided(value) => value
                      },
      modifieddate = modifieddate match {
                       case Defaulted.UseDefault => modifieddateDefault
                       case Defaulted.Provided(value) => value
                     }
    )
}
object ScrapreasonRowUnsaved {
  given jsonDecoder: JsonDecoder[ScrapreasonRowUnsaved] = JsonDecoder[Json.Obj].mapOrFail { jsonObj =>
    val name = jsonObj.get("name").toRight("Missing field 'name'").flatMap(_.as(using Name.jsonDecoder))
    val scrapreasonid = jsonObj.get("scrapreasonid").toRight("Missing field 'scrapreasonid'").flatMap(_.as(using Defaulted.jsonDecoder(using ScrapreasonId.jsonDecoder)))
    val modifieddate = jsonObj.get("modifieddate").toRight("Missing field 'modifieddate'").flatMap(_.as(using Defaulted.jsonDecoder(using TypoLocalDateTime.jsonDecoder)))
    if (name.isRight && scrapreasonid.isRight && modifieddate.isRight)
      Right(ScrapreasonRowUnsaved(name = name.toOption.get, scrapreasonid = scrapreasonid.toOption.get, modifieddate = modifieddate.toOption.get))
    else Left(List[Either[String, Any]](name, scrapreasonid, modifieddate).flatMap(_.left.toOption).mkString(", "))
  }
  given jsonEncoder: JsonEncoder[ScrapreasonRowUnsaved] = new JsonEncoder[ScrapreasonRowUnsaved] {
    override def unsafeEncode(a: ScrapreasonRowUnsaved, indent: Option[Int], out: Write): Unit = {
      out.write("{")
      out.write(""""name":""")
      Name.jsonEncoder.unsafeEncode(a.name, indent, out)
      out.write(",")
      out.write(""""scrapreasonid":""")
      Defaulted.jsonEncoder(using ScrapreasonId.jsonEncoder).unsafeEncode(a.scrapreasonid, indent, out)
      out.write(",")
      out.write(""""modifieddate":""")
      Defaulted.jsonEncoder(using TypoLocalDateTime.jsonEncoder).unsafeEncode(a.modifieddate, indent, out)
      out.write("}")
    }
  }
  given text: Text[ScrapreasonRowUnsaved] = Text.instance[ScrapreasonRowUnsaved]{ (row, sb) =>
    Name.text.unsafeEncode(row.name, sb)
    sb.append(Text.DELIMETER)
    Defaulted.text(using ScrapreasonId.text).unsafeEncode(row.scrapreasonid, sb)
    sb.append(Text.DELIMETER)
    Defaulted.text(using TypoLocalDateTime.text).unsafeEncode(row.modifieddate, sb)
  }
}
