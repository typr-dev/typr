/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package public
package users

import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.TypoInstant
import adventureworks.customtypes.TypoUnknownCitext
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment
import zio.jdbc.SqlFragment.Segment
import zio.jdbc.SqlFragment.Setter
import zio.jdbc.UpdateResult
import zio.jdbc.ZConnection
import zio.jdbc.sqlInterpolator
import zio.stream.ZStream

class UsersRepoImpl extends UsersRepo {
  override def delete: DeleteBuilder[UsersFields, UsersRow] = {
    DeleteBuilder(""""public"."users"""", UsersFields.structure)
  }
  override def deleteById(userId: UsersId): ZIO[ZConnection, Throwable, Boolean] = {
    sql"""delete from "public"."users" where "user_id" = ${Segment.paramSegment(userId)(using UsersId.setter)}""".delete.map(_ > 0)
  }
  override def deleteByIds(userIds: Array[UsersId]): ZIO[ZConnection, Throwable, Long] = {
    sql"""delete from "public"."users" where "user_id" = ANY(${userIds})""".delete
  }
  override def insert(unsaved: UsersRow): ZIO[ZConnection, Throwable, UsersRow] = {
    sql"""insert into "public"."users"("user_id", "name", "last_name", "email", "password", "created_at", "verified_on")
          values (${Segment.paramSegment(unsaved.userId)(using UsersId.setter)}::uuid, ${Segment.paramSegment(unsaved.name)(using Setter.stringSetter)}, ${Segment.paramSegment(unsaved.lastName)(using Setter.optionParamSetter(using Setter.stringSetter))}, ${Segment.paramSegment(unsaved.email)(using TypoUnknownCitext.setter)}::citext, ${Segment.paramSegment(unsaved.password)(using Setter.stringSetter)}, ${Segment.paramSegment(unsaved.createdAt)(using TypoInstant.setter)}::timestamptz, ${Segment.paramSegment(unsaved.verifiedOn)(using Setter.optionParamSetter(using TypoInstant.setter))}::timestamptz)
          returning "user_id", "name", "last_name", "email"::text, "password", "created_at"::text, "verified_on"::text
       """.insertReturning(using UsersRow.jdbcDecoder).map(_.updatedKeys.head)
  }
  override def insert(unsaved: UsersRowUnsaved): ZIO[ZConnection, Throwable, UsersRow] = {
    val fs = List(
      Some((sql""""user_id"""", sql"${Segment.paramSegment(unsaved.userId)(using UsersId.setter)}::uuid")),
      Some((sql""""name"""", sql"${Segment.paramSegment(unsaved.name)(using Setter.stringSetter)}")),
      Some((sql""""last_name"""", sql"${Segment.paramSegment(unsaved.lastName)(using Setter.optionParamSetter(using Setter.stringSetter))}")),
      Some((sql""""email"""", sql"${Segment.paramSegment(unsaved.email)(using TypoUnknownCitext.setter)}::citext")),
      Some((sql""""password"""", sql"${Segment.paramSegment(unsaved.password)(using Setter.stringSetter)}")),
      Some((sql""""verified_on"""", sql"${Segment.paramSegment(unsaved.verifiedOn)(using Setter.optionParamSetter(using TypoInstant.setter))}::timestamptz")),
      unsaved.createdAt match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((sql""""created_at"""", sql"${Segment.paramSegment(value: TypoInstant)(using TypoInstant.setter)}::timestamptz"))
      }
    ).flatten
    
    val q = if (fs.isEmpty) {
      sql"""insert into "public"."users" default values
            returning "user_id", "name", "last_name", "email"::text, "password", "created_at"::text, "verified_on"::text
         """
    } else {
      val names  = fs.map { case (n, _) => n }.mkFragment(SqlFragment(", "))
      val values = fs.map { case (_, f) => f }.mkFragment(SqlFragment(", "))
      sql"""insert into "public"."users"($names) values ($values) returning "user_id", "name", "last_name", "email"::text, "password", "created_at"::text, "verified_on"::text"""
    }
    q.insertReturning(using UsersRow.jdbcDecoder).map(_.updatedKeys.head)
    
  }
  override def insertStreaming(unsaved: ZStream[ZConnection, Throwable, UsersRow], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    streamingInsert(s"""COPY "public"."users"("user_id", "name", "last_name", "email", "password", "created_at", "verified_on") FROM STDIN""", batchSize, unsaved)(using UsersRow.text)
  }
  /* NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(unsaved: ZStream[ZConnection, Throwable, UsersRowUnsaved], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    streamingInsert(s"""COPY "public"."users"("user_id", "name", "last_name", "email", "password", "verified_on", "created_at") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(using UsersRowUnsaved.text)
  }
  override def select: SelectBuilder[UsersFields, UsersRow] = {
    SelectBuilderSql(""""public"."users"""", UsersFields.structure, UsersRow.jdbcDecoder)
  }
  override def selectAll: ZStream[ZConnection, Throwable, UsersRow] = {
    sql"""select "user_id", "name", "last_name", "email"::text, "password", "created_at"::text, "verified_on"::text from "public"."users"""".query(using UsersRow.jdbcDecoder).selectStream()
  }
  override def selectById(userId: UsersId): ZIO[ZConnection, Throwable, Option[UsersRow]] = {
    sql"""select "user_id", "name", "last_name", "email"::text, "password", "created_at"::text, "verified_on"::text from "public"."users" where "user_id" = ${Segment.paramSegment(userId)(using UsersId.setter)}""".query(using UsersRow.jdbcDecoder).selectOne
  }
  override def selectByIds(userIds: Array[UsersId]): ZStream[ZConnection, Throwable, UsersRow] = {
    sql"""select "user_id", "name", "last_name", "email"::text, "password", "created_at"::text, "verified_on"::text from "public"."users" where "user_id" = ANY(${Segment.paramSegment(userIds)(using UsersId.arraySetter)})""".query(using UsersRow.jdbcDecoder).selectStream()
  }
  override def selectByIdsTracked(userIds: Array[UsersId]): ZIO[ZConnection, Throwable, Map[UsersId, UsersRow]] = {
    selectByIds(userIds).runCollect.map { rows =>
      val byId = rows.view.map(x => (x.userId, x)).toMap
      userIds.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
    }
  }
  override def selectByUniqueEmail(email: TypoUnknownCitext): ZIO[ZConnection, Throwable, Option[UsersRow]] = {
    sql"""select "user_id", "name", "last_name", "email"::text, "password", "created_at"::text, "verified_on"::text
          from "public"."users"
          where "email" = ${Segment.paramSegment(email)(using TypoUnknownCitext.setter)}
       """.query(using UsersRow.jdbcDecoder).selectOne
  }
  override def update: UpdateBuilder[UsersFields, UsersRow] = {
    UpdateBuilder(""""public"."users"""", UsersFields.structure, UsersRow.jdbcDecoder)
  }
  override def update(row: UsersRow): ZIO[ZConnection, Throwable, Option[UsersRow]] = {
    val userId = row.userId
    sql"""update "public"."users"
          set "name" = ${Segment.paramSegment(row.name)(using Setter.stringSetter)},
              "last_name" = ${Segment.paramSegment(row.lastName)(using Setter.optionParamSetter(using Setter.stringSetter))},
              "email" = ${Segment.paramSegment(row.email)(using TypoUnknownCitext.setter)}::citext,
              "password" = ${Segment.paramSegment(row.password)(using Setter.stringSetter)},
              "created_at" = ${Segment.paramSegment(row.createdAt)(using TypoInstant.setter)}::timestamptz,
              "verified_on" = ${Segment.paramSegment(row.verifiedOn)(using Setter.optionParamSetter(using TypoInstant.setter))}::timestamptz
          where "user_id" = ${Segment.paramSegment(userId)(using UsersId.setter)}
          returning "user_id", "name", "last_name", "email"::text, "password", "created_at"::text, "verified_on"::text"""
      .query(using UsersRow.jdbcDecoder)
      .selectOne
  }
  override def upsert(unsaved: UsersRow): ZIO[ZConnection, Throwable, UpdateResult[UsersRow]] = {
    sql"""insert into "public"."users"("user_id", "name", "last_name", "email", "password", "created_at", "verified_on")
          values (
            ${Segment.paramSegment(unsaved.userId)(using UsersId.setter)}::uuid,
            ${Segment.paramSegment(unsaved.name)(using Setter.stringSetter)},
            ${Segment.paramSegment(unsaved.lastName)(using Setter.optionParamSetter(using Setter.stringSetter))},
            ${Segment.paramSegment(unsaved.email)(using TypoUnknownCitext.setter)}::citext,
            ${Segment.paramSegment(unsaved.password)(using Setter.stringSetter)},
            ${Segment.paramSegment(unsaved.createdAt)(using TypoInstant.setter)}::timestamptz,
            ${Segment.paramSegment(unsaved.verifiedOn)(using Setter.optionParamSetter(using TypoInstant.setter))}::timestamptz
          )
          on conflict ("user_id")
          do update set
            "name" = EXCLUDED."name",
            "last_name" = EXCLUDED."last_name",
            "email" = EXCLUDED."email",
            "password" = EXCLUDED."password",
            "created_at" = EXCLUDED."created_at",
            "verified_on" = EXCLUDED."verified_on"
          returning "user_id", "name", "last_name", "email"::text, "password", "created_at"::text, "verified_on"::text""".insertReturning(using UsersRow.jdbcDecoder)
  }
  /* NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(unsaved: ZStream[ZConnection, Throwable, UsersRow], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    val created = sql"""create temporary table users_TEMP (like "public"."users") on commit drop""".execute
    val copied = streamingInsert(s"""copy users_TEMP("user_id", "name", "last_name", "email", "password", "created_at", "verified_on") from stdin""", batchSize, unsaved)(using UsersRow.text)
    val merged = sql"""insert into "public"."users"("user_id", "name", "last_name", "email", "password", "created_at", "verified_on")
                       select * from users_TEMP
                       on conflict ("user_id")
                       do update set
                         "name" = EXCLUDED."name",
                         "last_name" = EXCLUDED."last_name",
                         "email" = EXCLUDED."email",
                         "password" = EXCLUDED."password",
                         "created_at" = EXCLUDED."created_at",
                         "verified_on" = EXCLUDED."verified_on"
                       ;
                       drop table users_TEMP;""".update
    created *> copied *> merged
  }
}
