/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package person
package person

import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.TypoLocalDateTime
import adventureworks.customtypes.TypoUUID
import adventureworks.customtypes.TypoXml
import adventureworks.person.businessentity.BusinessentityId
import adventureworks.public.Name
import adventureworks.public.NameStyle
import adventureworks.userdefined.FirstName
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment
import zio.jdbc.SqlFragment.Segment
import zio.jdbc.SqlFragment.Setter
import zio.jdbc.UpdateResult
import zio.jdbc.ZConnection
import zio.jdbc.sqlInterpolator
import zio.stream.ZStream

class PersonRepoImpl extends PersonRepo {
  override def delete: DeleteBuilder[PersonFields, PersonRow] = {
    DeleteBuilder(""""person"."person"""", PersonFields.structure)
  }
  override def deleteById(businessentityid: BusinessentityId): ZIO[ZConnection, Throwable, Boolean] = {
    sql"""delete from "person"."person" where "businessentityid" = ${Segment.paramSegment(businessentityid)(using BusinessentityId.setter)}""".delete.map(_ > 0)
  }
  override def deleteByIds(businessentityids: Array[BusinessentityId]): ZIO[ZConnection, Throwable, Long] = {
    sql"""delete from "person"."person" where "businessentityid" = ANY(${businessentityids})""".delete
  }
  override def insert(unsaved: PersonRow): ZIO[ZConnection, Throwable, PersonRow] = {
    sql"""insert into "person"."person"("businessentityid", "persontype", "namestyle", "title", "firstname", "middlename", "lastname", "suffix", "emailpromotion", "additionalcontactinfo", "demographics", "rowguid", "modifieddate")
          values (${Segment.paramSegment(unsaved.businessentityid)(using BusinessentityId.setter)}::int4, ${Segment.paramSegment(unsaved.persontype)(using Setter.stringSetter)}::bpchar, ${Segment.paramSegment(unsaved.namestyle)(using NameStyle.setter)}::bool, ${Segment.paramSegment(unsaved.title)(using Setter.optionParamSetter(using Setter.stringSetter))}, ${Segment.paramSegment(unsaved.firstname)(using /* user-picked */ FirstName.setter)}::varchar, ${Segment.paramSegment(unsaved.middlename)(using Setter.optionParamSetter(using Name.setter))}::varchar, ${Segment.paramSegment(unsaved.lastname)(using Name.setter)}::varchar, ${Segment.paramSegment(unsaved.suffix)(using Setter.optionParamSetter(using Setter.stringSetter))}, ${Segment.paramSegment(unsaved.emailpromotion)(using Setter.intSetter)}::int4, ${Segment.paramSegment(unsaved.additionalcontactinfo)(using Setter.optionParamSetter(using TypoXml.setter))}::xml, ${Segment.paramSegment(unsaved.demographics)(using Setter.optionParamSetter(using TypoXml.setter))}::xml, ${Segment.paramSegment(unsaved.rowguid)(using TypoUUID.setter)}::uuid, ${Segment.paramSegment(unsaved.modifieddate)(using TypoLocalDateTime.setter)}::timestamp)
          returning "businessentityid", "persontype", "namestyle", "title", "firstname", "middlename", "lastname", "suffix", "emailpromotion", "additionalcontactinfo", "demographics", "rowguid", "modifieddate"::text
       """.insertReturning(using PersonRow.jdbcDecoder).map(_.updatedKeys.head)
  }
  override def insert(unsaved: PersonRowUnsaved): ZIO[ZConnection, Throwable, PersonRow] = {
    val fs = List(
      Some((sql""""businessentityid"""", sql"${Segment.paramSegment(unsaved.businessentityid)(using BusinessentityId.setter)}::int4")),
      Some((sql""""persontype"""", sql"${Segment.paramSegment(unsaved.persontype)(using Setter.stringSetter)}::bpchar")),
      Some((sql""""title"""", sql"${Segment.paramSegment(unsaved.title)(using Setter.optionParamSetter(using Setter.stringSetter))}")),
      Some((sql""""firstname"""", sql"${Segment.paramSegment(unsaved.firstname)(using /* user-picked */ FirstName.setter)}::varchar")),
      Some((sql""""middlename"""", sql"${Segment.paramSegment(unsaved.middlename)(using Setter.optionParamSetter(using Name.setter))}::varchar")),
      Some((sql""""lastname"""", sql"${Segment.paramSegment(unsaved.lastname)(using Name.setter)}::varchar")),
      Some((sql""""suffix"""", sql"${Segment.paramSegment(unsaved.suffix)(using Setter.optionParamSetter(using Setter.stringSetter))}")),
      Some((sql""""additionalcontactinfo"""", sql"${Segment.paramSegment(unsaved.additionalcontactinfo)(using Setter.optionParamSetter(using TypoXml.setter))}::xml")),
      Some((sql""""demographics"""", sql"${Segment.paramSegment(unsaved.demographics)(using Setter.optionParamSetter(using TypoXml.setter))}::xml")),
      unsaved.namestyle match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((sql""""namestyle"""", sql"${Segment.paramSegment(value: NameStyle)(using NameStyle.setter)}::bool"))
      },
      unsaved.emailpromotion match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((sql""""emailpromotion"""", sql"${Segment.paramSegment(value: Int)(using Setter.intSetter)}::int4"))
      },
      unsaved.rowguid match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((sql""""rowguid"""", sql"${Segment.paramSegment(value: TypoUUID)(using TypoUUID.setter)}::uuid"))
      },
      unsaved.modifieddate match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((sql""""modifieddate"""", sql"${Segment.paramSegment(value: TypoLocalDateTime)(using TypoLocalDateTime.setter)}::timestamp"))
      }
    ).flatten
    
    val q = if (fs.isEmpty) {
      sql"""insert into "person"."person" default values
            returning "businessentityid", "persontype", "namestyle", "title", "firstname", "middlename", "lastname", "suffix", "emailpromotion", "additionalcontactinfo", "demographics", "rowguid", "modifieddate"::text
         """
    } else {
      val names  = fs.map { case (n, _) => n }.mkFragment(SqlFragment(", "))
      val values = fs.map { case (_, f) => f }.mkFragment(SqlFragment(", "))
      sql"""insert into "person"."person"($names) values ($values) returning "businessentityid", "persontype", "namestyle", "title", "firstname", "middlename", "lastname", "suffix", "emailpromotion", "additionalcontactinfo", "demographics", "rowguid", "modifieddate"::text"""
    }
    q.insertReturning(using PersonRow.jdbcDecoder).map(_.updatedKeys.head)
    
  }
  override def insertStreaming(unsaved: ZStream[ZConnection, Throwable, PersonRow], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    streamingInsert(s"""COPY "person"."person"("businessentityid", "persontype", "namestyle", "title", "firstname", "middlename", "lastname", "suffix", "emailpromotion", "additionalcontactinfo", "demographics", "rowguid", "modifieddate") FROM STDIN""", batchSize, unsaved)(using PersonRow.text)
  }
  /* NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(unsaved: ZStream[ZConnection, Throwable, PersonRowUnsaved], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    streamingInsert(s"""COPY "person"."person"("businessentityid", "persontype", "title", "firstname", "middlename", "lastname", "suffix", "additionalcontactinfo", "demographics", "namestyle", "emailpromotion", "rowguid", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(using PersonRowUnsaved.text)
  }
  override def select: SelectBuilder[PersonFields, PersonRow] = {
    SelectBuilderSql(""""person"."person"""", PersonFields.structure, PersonRow.jdbcDecoder)
  }
  override def selectAll: ZStream[ZConnection, Throwable, PersonRow] = {
    sql"""select "businessentityid", "persontype", "namestyle", "title", "firstname", "middlename", "lastname", "suffix", "emailpromotion", "additionalcontactinfo", "demographics", "rowguid", "modifieddate"::text from "person"."person"""".query(using PersonRow.jdbcDecoder).selectStream()
  }
  override def selectById(businessentityid: BusinessentityId): ZIO[ZConnection, Throwable, Option[PersonRow]] = {
    sql"""select "businessentityid", "persontype", "namestyle", "title", "firstname", "middlename", "lastname", "suffix", "emailpromotion", "additionalcontactinfo", "demographics", "rowguid", "modifieddate"::text from "person"."person" where "businessentityid" = ${Segment.paramSegment(businessentityid)(using BusinessentityId.setter)}""".query(using PersonRow.jdbcDecoder).selectOne
  }
  override def selectByIds(businessentityids: Array[BusinessentityId]): ZStream[ZConnection, Throwable, PersonRow] = {
    sql"""select "businessentityid", "persontype", "namestyle", "title", "firstname", "middlename", "lastname", "suffix", "emailpromotion", "additionalcontactinfo", "demographics", "rowguid", "modifieddate"::text from "person"."person" where "businessentityid" = ANY(${Segment.paramSegment(businessentityids)(using BusinessentityId.arraySetter)})""".query(using PersonRow.jdbcDecoder).selectStream()
  }
  override def selectByIdsTracked(businessentityids: Array[BusinessentityId]): ZIO[ZConnection, Throwable, Map[BusinessentityId, PersonRow]] = {
    selectByIds(businessentityids).runCollect.map { rows =>
      val byId = rows.view.map(x => (x.businessentityid, x)).toMap
      businessentityids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
    }
  }
  override def update: UpdateBuilder[PersonFields, PersonRow] = {
    UpdateBuilder(""""person"."person"""", PersonFields.structure, PersonRow.jdbcDecoder)
  }
  override def update(row: PersonRow): ZIO[ZConnection, Throwable, Option[PersonRow]] = {
    val businessentityid = row.businessentityid
    sql"""update "person"."person"
          set "persontype" = ${Segment.paramSegment(row.persontype)(using Setter.stringSetter)}::bpchar,
              "namestyle" = ${Segment.paramSegment(row.namestyle)(using NameStyle.setter)}::bool,
              "title" = ${Segment.paramSegment(row.title)(using Setter.optionParamSetter(using Setter.stringSetter))},
              "firstname" = ${Segment.paramSegment(row.firstname)(using /* user-picked */ FirstName.setter)}::varchar,
              "middlename" = ${Segment.paramSegment(row.middlename)(using Setter.optionParamSetter(using Name.setter))}::varchar,
              "lastname" = ${Segment.paramSegment(row.lastname)(using Name.setter)}::varchar,
              "suffix" = ${Segment.paramSegment(row.suffix)(using Setter.optionParamSetter(using Setter.stringSetter))},
              "emailpromotion" = ${Segment.paramSegment(row.emailpromotion)(using Setter.intSetter)}::int4,
              "additionalcontactinfo" = ${Segment.paramSegment(row.additionalcontactinfo)(using Setter.optionParamSetter(using TypoXml.setter))}::xml,
              "demographics" = ${Segment.paramSegment(row.demographics)(using Setter.optionParamSetter(using TypoXml.setter))}::xml,
              "rowguid" = ${Segment.paramSegment(row.rowguid)(using TypoUUID.setter)}::uuid,
              "modifieddate" = ${Segment.paramSegment(row.modifieddate)(using TypoLocalDateTime.setter)}::timestamp
          where "businessentityid" = ${Segment.paramSegment(businessentityid)(using BusinessentityId.setter)}
          returning "businessentityid", "persontype", "namestyle", "title", "firstname", "middlename", "lastname", "suffix", "emailpromotion", "additionalcontactinfo", "demographics", "rowguid", "modifieddate"::text"""
      .query(using PersonRow.jdbcDecoder)
      .selectOne
  }
  override def upsert(unsaved: PersonRow): ZIO[ZConnection, Throwable, UpdateResult[PersonRow]] = {
    sql"""insert into "person"."person"("businessentityid", "persontype", "namestyle", "title", "firstname", "middlename", "lastname", "suffix", "emailpromotion", "additionalcontactinfo", "demographics", "rowguid", "modifieddate")
          values (
            ${Segment.paramSegment(unsaved.businessentityid)(using BusinessentityId.setter)}::int4,
            ${Segment.paramSegment(unsaved.persontype)(using Setter.stringSetter)}::bpchar,
            ${Segment.paramSegment(unsaved.namestyle)(using NameStyle.setter)}::bool,
            ${Segment.paramSegment(unsaved.title)(using Setter.optionParamSetter(using Setter.stringSetter))},
            ${Segment.paramSegment(unsaved.firstname)(using /* user-picked */ FirstName.setter)}::varchar,
            ${Segment.paramSegment(unsaved.middlename)(using Setter.optionParamSetter(using Name.setter))}::varchar,
            ${Segment.paramSegment(unsaved.lastname)(using Name.setter)}::varchar,
            ${Segment.paramSegment(unsaved.suffix)(using Setter.optionParamSetter(using Setter.stringSetter))},
            ${Segment.paramSegment(unsaved.emailpromotion)(using Setter.intSetter)}::int4,
            ${Segment.paramSegment(unsaved.additionalcontactinfo)(using Setter.optionParamSetter(using TypoXml.setter))}::xml,
            ${Segment.paramSegment(unsaved.demographics)(using Setter.optionParamSetter(using TypoXml.setter))}::xml,
            ${Segment.paramSegment(unsaved.rowguid)(using TypoUUID.setter)}::uuid,
            ${Segment.paramSegment(unsaved.modifieddate)(using TypoLocalDateTime.setter)}::timestamp
          )
          on conflict ("businessentityid")
          do update set
            "persontype" = EXCLUDED."persontype",
            "namestyle" = EXCLUDED."namestyle",
            "title" = EXCLUDED."title",
            "firstname" = EXCLUDED."firstname",
            "middlename" = EXCLUDED."middlename",
            "lastname" = EXCLUDED."lastname",
            "suffix" = EXCLUDED."suffix",
            "emailpromotion" = EXCLUDED."emailpromotion",
            "additionalcontactinfo" = EXCLUDED."additionalcontactinfo",
            "demographics" = EXCLUDED."demographics",
            "rowguid" = EXCLUDED."rowguid",
            "modifieddate" = EXCLUDED."modifieddate"
          returning "businessentityid", "persontype", "namestyle", "title", "firstname", "middlename", "lastname", "suffix", "emailpromotion", "additionalcontactinfo", "demographics", "rowguid", "modifieddate"::text""".insertReturning(using PersonRow.jdbcDecoder)
  }
  /* NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(unsaved: ZStream[ZConnection, Throwable, PersonRow], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    val created = sql"""create temporary table person_TEMP (like "person"."person") on commit drop""".execute
    val copied = streamingInsert(s"""copy person_TEMP("businessentityid", "persontype", "namestyle", "title", "firstname", "middlename", "lastname", "suffix", "emailpromotion", "additionalcontactinfo", "demographics", "rowguid", "modifieddate") from stdin""", batchSize, unsaved)(using PersonRow.text)
    val merged = sql"""insert into "person"."person"("businessentityid", "persontype", "namestyle", "title", "firstname", "middlename", "lastname", "suffix", "emailpromotion", "additionalcontactinfo", "demographics", "rowguid", "modifieddate")
                       select * from person_TEMP
                       on conflict ("businessentityid")
                       do update set
                         "persontype" = EXCLUDED."persontype",
                         "namestyle" = EXCLUDED."namestyle",
                         "title" = EXCLUDED."title",
                         "firstname" = EXCLUDED."firstname",
                         "middlename" = EXCLUDED."middlename",
                         "lastname" = EXCLUDED."lastname",
                         "suffix" = EXCLUDED."suffix",
                         "emailpromotion" = EXCLUDED."emailpromotion",
                         "additionalcontactinfo" = EXCLUDED."additionalcontactinfo",
                         "demographics" = EXCLUDED."demographics",
                         "rowguid" = EXCLUDED."rowguid",
                         "modifieddate" = EXCLUDED."modifieddate"
                       ;
                       drop table person_TEMP;""".update
    created *> copied *> merged
  }
}
