/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package customtypes

import java.sql.ResultSet
import java.sql.Types
import java.time.LocalDate
import typo.dsl.Bijection
import typo.dsl.PGType
import zio.jdbc.JdbcDecoder
import zio.jdbc.JdbcEncoder
import zio.jdbc.SqlFragment.Setter
import zio.json.JsonDecoder
import zio.json.JsonEncoder

/** This is `java.time.LocalDate`, but transferred to and from postgres as strings. The reason is that postgres driver and db libs are broken */
case class TypoLocalDate(value: LocalDate)

object TypoLocalDate {
  def now = TypoLocalDate(LocalDate.now)
  def apply(str: String): TypoLocalDate = TypoLocalDate(LocalDate.parse(str))
  given arrayJdbcDecoder: JdbcDecoder[Array[TypoLocalDate]] = JdbcDecoder[Array[TypoLocalDate]]((rs: ResultSet) => (i: Int) =>
    rs.getArray(i) match {
      case null => null
      case arr => arr.getArray.asInstanceOf[Array[AnyRef]].map(x => TypoLocalDate(LocalDate.parse(x.asInstanceOf[String])))
    },
    "Array[java.lang.String]"
  )
  given arrayJdbcEncoder: JdbcEncoder[Array[TypoLocalDate]] = JdbcEncoder.singleParamEncoder(using arraySetter)
  given arraySetter: Setter[Array[TypoLocalDate]] = Setter.forSqlType((ps, i, v) =>
    ps.setArray(
      i,
      ps.getConnection.createArrayOf(
        "date",
        v.map { vv =>
          vv.value.toString
        }
      )
    ),
    Types.ARRAY
  )
  given bijection: Bijection[TypoLocalDate, LocalDate] = Bijection[TypoLocalDate, LocalDate](_.value)(TypoLocalDate.apply)
  given jdbcDecoder: JdbcDecoder[TypoLocalDate] = JdbcDecoder[TypoLocalDate](
    (rs: ResultSet) => (i: Int) => {
      val v = rs.getObject(i)
      if (v eq null) null else TypoLocalDate(LocalDate.parse(v.asInstanceOf[String]))
    },
    "java.lang.String"
  )
  given jdbcEncoder: JdbcEncoder[TypoLocalDate] = JdbcEncoder.singleParamEncoder(using setter)
  given jsonDecoder: JsonDecoder[TypoLocalDate] = JsonDecoder.localDate.map(TypoLocalDate.apply)
  given jsonEncoder: JsonEncoder[TypoLocalDate] = JsonEncoder.localDate.contramap(_.value)
  given ordering: Ordering[TypoLocalDate] = Ordering.by(_.value)
  given pgType: PGType[TypoLocalDate] = PGType.instance[TypoLocalDate]("date", Types.OTHER)
  given setter: Setter[TypoLocalDate] = Setter.other(
    (ps, i, v) => {
      ps.setObject(
        i,
        v.value.toString
      )
    },
    "date"
  )
  given text: Text[TypoLocalDate] = new Text[TypoLocalDate] {
    override def unsafeEncode(v: TypoLocalDate, sb: StringBuilder): Unit = Text.stringInstance.unsafeEncode(v.value.toString, sb)
    override def unsafeArrayEncode(v: TypoLocalDate, sb: StringBuilder): Unit = Text.stringInstance.unsafeArrayEncode(v.value.toString, sb)
  }
}
