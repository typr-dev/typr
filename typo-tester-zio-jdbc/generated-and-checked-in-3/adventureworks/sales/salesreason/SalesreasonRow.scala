/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.salesreason

import adventureworks.Text
import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.TypoLocalDateTime
import adventureworks.public.Name
import java.sql.ResultSet
import zio.jdbc.JdbcDecoder
import zio.json.JsonDecoder
import zio.json.JsonEncoder
import zio.json.ast.Json
import zio.json.internal.Write

/** Table: sales.salesreason
 * Lookup table of customer purchase reasons.
 * Primary key: salesreasonid
 */
case class SalesreasonRow(
  /** Primary key for SalesReason records.
   * Default: nextval('sales.salesreason_salesreasonid_seq'::regclass)
   */
  salesreasonid: SalesreasonId,
  /** Sales reason description. */
  name: Name,
  /** Category the sales reason belongs to. */
  reasontype: Name,
  /** Default: now() */
  modifieddate: TypoLocalDateTime
) {
  def id: SalesreasonId = salesreasonid

  def toUnsavedRow(
    salesreasonid: Defaulted[SalesreasonId],
    modifieddate: Defaulted[TypoLocalDateTime] = Defaulted.Provided(this.modifieddate)
  ): SalesreasonRowUnsaved = {
    new SalesreasonRowUnsaved(
      name,
      reasontype,
      salesreasonid,
      modifieddate
    )
  }
}

object SalesreasonRow {
  given jdbcDecoder: JdbcDecoder[SalesreasonRow] = {
    new JdbcDecoder[SalesreasonRow] {
      override def unsafeDecode(columIndex: Int, rs: ResultSet): (Int, SalesreasonRow) =
        columIndex + 3 ->
          SalesreasonRow(
            salesreasonid = SalesreasonId.jdbcDecoder.unsafeDecode(columIndex + 0, rs)._2,
            name = Name.jdbcDecoder.unsafeDecode(columIndex + 1, rs)._2,
            reasontype = Name.jdbcDecoder.unsafeDecode(columIndex + 2, rs)._2,
            modifieddate = TypoLocalDateTime.jdbcDecoder.unsafeDecode(columIndex + 3, rs)._2
          )
    }
  }

  given jsonDecoder: JsonDecoder[SalesreasonRow] = {
    JsonDecoder[Json.Obj].mapOrFail { jsonObj =>
      val salesreasonid = jsonObj.get("salesreasonid").toRight("Missing field 'salesreasonid'").flatMap(_.as(using SalesreasonId.jsonDecoder))
      val name = jsonObj.get("name").toRight("Missing field 'name'").flatMap(_.as(using Name.jsonDecoder))
      val reasontype = jsonObj.get("reasontype").toRight("Missing field 'reasontype'").flatMap(_.as(using Name.jsonDecoder))
      val modifieddate = jsonObj.get("modifieddate").toRight("Missing field 'modifieddate'").flatMap(_.as(using TypoLocalDateTime.jsonDecoder))
      if (salesreasonid.isRight && name.isRight && reasontype.isRight && modifieddate.isRight)
        Right(SalesreasonRow(salesreasonid = salesreasonid.toOption.get, name = name.toOption.get, reasontype = reasontype.toOption.get, modifieddate = modifieddate.toOption.get))
      else Left(List[Either[String, Any]](salesreasonid, name, reasontype, modifieddate).flatMap(_.left.toOption).mkString(", "))
    }
  }

  given jsonEncoder: JsonEncoder[SalesreasonRow] = {
    new JsonEncoder[SalesreasonRow] {
      override def unsafeEncode(a: SalesreasonRow, indent: Option[Int], out: Write): Unit = {
        out.write("{")
        out.write(""""salesreasonid":""")
        SalesreasonId.jsonEncoder.unsafeEncode(a.salesreasonid, indent, out)
        out.write(",")
        out.write(""""name":""")
        Name.jsonEncoder.unsafeEncode(a.name, indent, out)
        out.write(",")
        out.write(""""reasontype":""")
        Name.jsonEncoder.unsafeEncode(a.reasontype, indent, out)
        out.write(",")
        out.write(""""modifieddate":""")
        TypoLocalDateTime.jsonEncoder.unsafeEncode(a.modifieddate, indent, out)
        out.write("}")
      }
    }
  }

  given pgText: Text[SalesreasonRow] = {
    Text.instance[SalesreasonRow]{ (row, sb) =>
      SalesreasonId.pgText.unsafeEncode(row.salesreasonid, sb)
      sb.append(Text.DELIMETER)
      Name.pgText.unsafeEncode(row.name, sb)
      sb.append(Text.DELIMETER)
      Name.pgText.unsafeEncode(row.reasontype, sb)
      sb.append(Text.DELIMETER)
      TypoLocalDateTime.pgText.unsafeEncode(row.modifieddate, sb)
    }
  }
}