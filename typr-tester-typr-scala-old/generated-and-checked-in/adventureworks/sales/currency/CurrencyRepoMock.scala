/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.currency

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.HashMap
import java.util.Optional
import java.util.function.Function
import java.util.stream.Collectors
import typr.dsl.DeleteBuilder
import typr.dsl.DeleteBuilderMock
import typr.dsl.DeleteParams
import typr.dsl.SelectBuilder
import typr.dsl.SelectBuilderMock
import typr.dsl.SelectParams
import typr.dsl.UpdateBuilder
import typr.dsl.UpdateBuilderMock
import typr.dsl.UpdateParams

case class CurrencyRepoMock(
  toRow: CurrencyRowUnsaved => CurrencyRow,
  map: HashMap[CurrencyId, CurrencyRow] = new HashMap[CurrencyId, CurrencyRow]()
) extends CurrencyRepo {
  override def delete: DeleteBuilder[CurrencyFields, CurrencyRow] = DeleteBuilderMock(CurrencyFields.structure, () => new ArrayList(map.values()), DeleteParams.empty(), row => row.currencycode, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(currencycode: CurrencyId)(using c: Connection): java.lang.Boolean = Optional.ofNullable(map.remove(currencycode)).isPresent()

  override def deleteByIds(currencycodes: Array[CurrencyId])(using c: Connection): Integer = {
    var count = 0
    currencycodes.foreach { id => if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: CurrencyRow)(using c: Connection): CurrencyRow = {
    if (map.containsKey(unsaved.currencycode)) {
      throw new RuntimeException(s"id ${unsaved.currencycode} already exists")
    }
    map.put(unsaved.currencycode, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: CurrencyRowUnsaved)(using c: Connection): CurrencyRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: java.util.Iterator[CurrencyRow],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.currencycode, row): @scala.annotation.nowarn
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: java.util.Iterator[CurrencyRowUnsaved],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map.put(row.currencycode, row): @scala.annotation.nowarn
      count = count + 1L
    }
    return count
  }

  override def select: SelectBuilder[CurrencyFields, CurrencyRow] = SelectBuilderMock(CurrencyFields.structure, () => new ArrayList(map.values()), SelectParams.empty())

  override def selectAll(using c: Connection): java.util.List[CurrencyRow] = new ArrayList(map.values())

  override def selectById(currencycode: CurrencyId)(using c: Connection): Optional[CurrencyRow] = Optional.ofNullable(map.get(currencycode))

  override def selectByIds(currencycodes: Array[CurrencyId])(using c: Connection): java.util.List[CurrencyRow] = {
    val result = new ArrayList[CurrencyRow]()
    currencycodes.foreach { id => val opt = Optional.ofNullable(map.get(id)); if (opt.isPresent()) {
      result.add(opt.get()): @scala.annotation.nowarn
    } }
    return result
  }

  override def selectByIdsTracked(currencycodes: Array[CurrencyId])(using c: Connection): java.util.Map[CurrencyId, CurrencyRow] = selectByIds(currencycodes)(using c).stream().collect(Collectors.toMap((row: CurrencyRow) => row.currencycode, Function.identity()))

  override def update: UpdateBuilder[CurrencyFields, CurrencyRow] = UpdateBuilderMock(CurrencyFields.structure, () => new ArrayList(map.values()), UpdateParams.empty(), row => row)

  override def update(row: CurrencyRow)(using c: Connection): java.lang.Boolean = {
    val shouldUpdate = Optional.ofNullable(map.get(row.currencycode)).filter(oldRow => (oldRow != row)).isPresent()
    if (shouldUpdate) {
      map.put(row.currencycode, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: CurrencyRow)(using c: Connection): CurrencyRow = {
    map.put(unsaved.currencycode, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: java.util.Iterator[CurrencyRow])(using c: Connection): java.util.List[CurrencyRow] = {
    val result = new ArrayList[CurrencyRow]()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.currencycode, row): @scala.annotation.nowarn
      result.add(row): @scala.annotation.nowarn
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: java.util.Iterator[CurrencyRow],
    batchSize: Integer = 10000
  )(using c: Connection): Integer = {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.currencycode, row): @scala.annotation.nowarn
      count = count + 1
    }
    return count
  }
}