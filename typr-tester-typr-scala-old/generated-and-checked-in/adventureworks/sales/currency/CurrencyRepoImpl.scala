/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.currency

import adventureworks.public.Name
import java.sql.Connection
import java.util.ArrayList
import java.util.HashMap
import java.util.Optional
import typr.dsl.DeleteBuilder
import typr.dsl.Dialect
import typr.dsl.SelectBuilder
import typr.dsl.UpdateBuilder
import typr.runtime.Fragment
import typr.runtime.PgTypes
import typr.runtime.streamingInsert
import typr.runtime.Fragment.interpolate

class CurrencyRepoImpl extends CurrencyRepo {
  override def delete: DeleteBuilder[CurrencyFields, CurrencyRow] = DeleteBuilder.of(""""sales"."currency"""", CurrencyFields.structure, Dialect.POSTGRESQL)

  override def deleteById(currencycode: CurrencyId)(using c: Connection): java.lang.Boolean = interpolate(Fragment.lit("""delete from "sales"."currency" where "currencycode" = """), Fragment.encode(CurrencyId.pgType, currencycode), Fragment.lit("")).update().runUnchecked(c) > 0

  override def deleteByIds(currencycodes: Array[CurrencyId])(using c: Connection): Integer = {
    interpolate(Fragment.lit("""delete
    from "sales"."currency"
    where "currencycode" = ANY("""), Fragment.encode(CurrencyId.pgTypeArray, currencycodes), Fragment.lit(")"))
      .update()
      .runUnchecked(c)
  }

  override def insert(unsaved: CurrencyRow)(using c: Connection): CurrencyRow = {
  interpolate(Fragment.lit("""insert into "sales"."currency"("currencycode", "name", "modifieddate")
    values ("""), Fragment.encode(CurrencyId.pgType, unsaved.currencycode), Fragment.lit("::bpchar, "), Fragment.encode(Name.pgType, unsaved.name), Fragment.lit("::varchar, "), Fragment.encode(PgTypes.timestamp, unsaved.modifieddate), Fragment.lit("""::timestamp)
    RETURNING "currencycode", "name", "modifieddate"
    """))
    .updateReturning(CurrencyRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: CurrencyRowUnsaved)(using c: Connection): CurrencyRow = {
    val columns: ArrayList[Fragment] = new ArrayList()
    val values: ArrayList[Fragment] = new ArrayList()
    columns.add(Fragment.lit(""""currencycode"""")): @scala.annotation.nowarn
    values.add(interpolate(Fragment.encode(CurrencyId.pgType, unsaved.currencycode), Fragment.lit("::bpchar"))): @scala.annotation.nowarn
    columns.add(Fragment.lit(""""name"""")): @scala.annotation.nowarn
    values.add(interpolate(Fragment.encode(Name.pgType, unsaved.name), Fragment.lit("::varchar"))): @scala.annotation.nowarn
    unsaved.modifieddate.visit(
      {  },
      value => { columns.add(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.add(interpolate(Fragment.encode(PgTypes.timestamp, value), Fragment.lit("::timestamp"))): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      interpolate(Fragment.lit("""insert into "sales"."currency"("""), Fragment.comma(columns), Fragment.lit(""")
      values ("""), Fragment.comma(values), Fragment.lit(""")
      RETURNING "currencycode", "name", "modifieddate"
      """))
    }
    return q.updateReturning(CurrencyRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: java.util.Iterator[CurrencyRow],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = streamingInsert.insertUnchecked(s"""COPY "sales"."currency"("currencycode", "name", "modifieddate") FROM STDIN""", batchSize, unsaved, c, CurrencyRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: java.util.Iterator[CurrencyRowUnsaved],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = streamingInsert.insertUnchecked(s"""COPY "sales"."currency"("currencycode", "name", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved, c, CurrencyRowUnsaved.pgText)

  override def select: SelectBuilder[CurrencyFields, CurrencyRow] = SelectBuilder.of(""""sales"."currency"""", CurrencyFields.structure, CurrencyRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): java.util.List[CurrencyRow] = {
    interpolate(Fragment.lit("""select "currencycode", "name", "modifieddate"
    from "sales"."currency"
    """)).query(CurrencyRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(currencycode: CurrencyId)(using c: Connection): Optional[CurrencyRow] = {
    interpolate(Fragment.lit("""select "currencycode", "name", "modifieddate"
    from "sales"."currency"
    where "currencycode" = """), Fragment.encode(CurrencyId.pgType, currencycode), Fragment.lit("")).query(CurrencyRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(currencycodes: Array[CurrencyId])(using c: Connection): java.util.List[CurrencyRow] = {
    interpolate(Fragment.lit("""select "currencycode", "name", "modifieddate"
    from "sales"."currency"
    where "currencycode" = ANY("""), Fragment.encode(CurrencyId.pgTypeArray, currencycodes), Fragment.lit(")")).query(CurrencyRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(currencycodes: Array[CurrencyId])(using c: Connection): java.util.Map[CurrencyId, CurrencyRow] = {
    val ret: HashMap[CurrencyId, CurrencyRow] = new HashMap[CurrencyId, CurrencyRow]()
    selectByIds(currencycodes)(using c).forEach(row => ret.put(row.currencycode, row): @scala.annotation.nowarn)
    return ret
  }

  override def update: UpdateBuilder[CurrencyFields, CurrencyRow] = UpdateBuilder.of(""""sales"."currency"""", CurrencyFields.structure, CurrencyRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: CurrencyRow)(using c: Connection): java.lang.Boolean = {
    val currencycode: CurrencyId = row.currencycode
    return interpolate(Fragment.lit("""update "sales"."currency"
    set "name" = """), Fragment.encode(Name.pgType, row.name), Fragment.lit("""::varchar,
    "modifieddate" = """), Fragment.encode(PgTypes.timestamp, row.modifieddate), Fragment.lit("""::timestamp
    where "currencycode" = """), Fragment.encode(CurrencyId.pgType, currencycode), Fragment.lit("")).update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: CurrencyRow)(using c: Connection): CurrencyRow = {
  interpolate(Fragment.lit("""insert into "sales"."currency"("currencycode", "name", "modifieddate")
    values ("""), Fragment.encode(CurrencyId.pgType, unsaved.currencycode), Fragment.lit("::bpchar, "), Fragment.encode(Name.pgType, unsaved.name), Fragment.lit("::varchar, "), Fragment.encode(PgTypes.timestamp, unsaved.modifieddate), Fragment.lit("""::timestamp)
    on conflict ("currencycode")
    do update set
      "name" = EXCLUDED."name",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "currencycode", "name", "modifieddate""""))
    .updateReturning(CurrencyRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: java.util.Iterator[CurrencyRow])(using c: Connection): java.util.List[CurrencyRow] = {
    interpolate(Fragment.lit("""insert into "sales"."currency"("currencycode", "name", "modifieddate")
    values (?::bpchar, ?::varchar, ?::timestamp)
    on conflict ("currencycode")
    do update set
      "name" = EXCLUDED."name",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "currencycode", "name", "modifieddate""""))
      .updateManyReturning(CurrencyRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: java.util.Iterator[CurrencyRow],
    batchSize: Integer = 10000
  )(using c: Connection): Integer = {
    interpolate(Fragment.lit("""create temporary table currency_TEMP (like "sales"."currency") on commit drop""")).update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy currency_TEMP("currencycode", "name", "modifieddate") from stdin""", batchSize, unsaved, c, CurrencyRow.pgText): @scala.annotation.nowarn
    return interpolate(Fragment.lit("""insert into "sales"."currency"("currencycode", "name", "modifieddate")
    select * from currency_TEMP
    on conflict ("currencycode")
    do update set
      "name" = EXCLUDED."name",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table currency_TEMP;""")).update().runUnchecked(c)
  }
}