/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productinventory

import adventureworks.production.location.LocationId
import adventureworks.production.product.ProductId
import java.sql.Connection
import java.util.ArrayList
import java.util.HashMap
import java.util.Optional
import typr.dsl.DeleteBuilder
import typr.dsl.Dialect
import typr.dsl.SelectBuilder
import typr.dsl.UpdateBuilder
import typr.runtime.Fragment
import typr.runtime.PgTypes
import typr.runtime.streamingInsert
import typr.runtime.Fragment.interpolate

class ProductinventoryRepoImpl extends ProductinventoryRepo {
  override def delete: DeleteBuilder[ProductinventoryFields, ProductinventoryRow] = DeleteBuilder.of(""""production"."productinventory"""", ProductinventoryFields.structure, Dialect.POSTGRESQL)

  override def deleteById(compositeId: ProductinventoryId)(using c: Connection): java.lang.Boolean = interpolate(Fragment.lit("""delete from "production"."productinventory" where "productid" = """), Fragment.encode(ProductId.pgType, compositeId.productid), Fragment.lit(""" AND "locationid" = """), Fragment.encode(LocationId.pgType, compositeId.locationid), Fragment.lit("")).update().runUnchecked(c) > 0

  override def deleteByIds(compositeIds: Array[ProductinventoryId])(using c: Connection): Integer = {
    val productid: Array[ProductId] = compositeIds.map(_.productid)
    val locationid: Array[LocationId] = compositeIds.map(_.locationid)
    return interpolate(Fragment.lit("""delete
    from "production"."productinventory"
    where ("productid", "locationid")
    in (select * from unnest("""), Fragment.encode(ProductId.pgTypeArray, productid), Fragment.lit(", "), Fragment.encode(LocationId.pgTypeArray, locationid), Fragment.lit("""))
    """)).update().runUnchecked(c)
  }

  override def insert(unsaved: ProductinventoryRow)(using c: Connection): ProductinventoryRow = {
  interpolate(Fragment.lit("""insert into "production"."productinventory"("productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate")
    values ("""), Fragment.encode(ProductId.pgType, unsaved.productid), Fragment.lit("::int4, "), Fragment.encode(LocationId.pgType, unsaved.locationid), Fragment.lit("::int2, "), Fragment.encode(PgTypes.text, unsaved.shelf), Fragment.lit(", "), Fragment.encode(PgTypes.int2, unsaved.bin), Fragment.lit("::int2, "), Fragment.encode(PgTypes.int2, unsaved.quantity), Fragment.lit("::int2, "), Fragment.encode(PgTypes.uuid, unsaved.rowguid), Fragment.lit("::uuid, "), Fragment.encode(PgTypes.timestamp, unsaved.modifieddate), Fragment.lit("""::timestamp)
    RETURNING "productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate"
    """))
    .updateReturning(ProductinventoryRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: ProductinventoryRowUnsaved)(using c: Connection): ProductinventoryRow = {
    val columns: ArrayList[Fragment] = new ArrayList()
    val values: ArrayList[Fragment] = new ArrayList()
    columns.add(Fragment.lit(""""productid"""")): @scala.annotation.nowarn
    values.add(interpolate(Fragment.encode(ProductId.pgType, unsaved.productid), Fragment.lit("::int4"))): @scala.annotation.nowarn
    columns.add(Fragment.lit(""""locationid"""")): @scala.annotation.nowarn
    values.add(interpolate(Fragment.encode(LocationId.pgType, unsaved.locationid), Fragment.lit("::int2"))): @scala.annotation.nowarn
    columns.add(Fragment.lit(""""shelf"""")): @scala.annotation.nowarn
    values.add(interpolate(Fragment.encode(PgTypes.text, unsaved.shelf), Fragment.lit(""))): @scala.annotation.nowarn
    columns.add(Fragment.lit(""""bin"""")): @scala.annotation.nowarn
    values.add(interpolate(Fragment.encode(PgTypes.int2, unsaved.bin), Fragment.lit("::int2"))): @scala.annotation.nowarn
    unsaved.quantity.visit(
      {  },
      value => { columns.add(Fragment.lit(""""quantity"""")): @scala.annotation.nowarn; values.add(interpolate(Fragment.encode(PgTypes.int2, value), Fragment.lit("::int2"))): @scala.annotation.nowarn }
    );
    unsaved.rowguid.visit(
      {  },
      value => { columns.add(Fragment.lit(""""rowguid"""")): @scala.annotation.nowarn; values.add(interpolate(Fragment.encode(PgTypes.uuid, value), Fragment.lit("::uuid"))): @scala.annotation.nowarn }
    );
    unsaved.modifieddate.visit(
      {  },
      value => { columns.add(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.add(interpolate(Fragment.encode(PgTypes.timestamp, value), Fragment.lit("::timestamp"))): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      interpolate(Fragment.lit("""insert into "production"."productinventory"("""), Fragment.comma(columns), Fragment.lit(""")
      values ("""), Fragment.comma(values), Fragment.lit(""")
      RETURNING "productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate"
      """))
    }
    return q.updateReturning(ProductinventoryRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: java.util.Iterator[ProductinventoryRow],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = streamingInsert.insertUnchecked(s"""COPY "production"."productinventory"("productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate") FROM STDIN""", batchSize, unsaved, c, ProductinventoryRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: java.util.Iterator[ProductinventoryRowUnsaved],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = streamingInsert.insertUnchecked(s"""COPY "production"."productinventory"("productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved, c, ProductinventoryRowUnsaved.pgText)

  override def select: SelectBuilder[ProductinventoryFields, ProductinventoryRow] = SelectBuilder.of(""""production"."productinventory"""", ProductinventoryFields.structure, ProductinventoryRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): java.util.List[ProductinventoryRow] = {
    interpolate(Fragment.lit("""select "productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate"
    from "production"."productinventory"
    """)).query(ProductinventoryRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(compositeId: ProductinventoryId)(using c: Connection): Optional[ProductinventoryRow] = {
    interpolate(Fragment.lit("""select "productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate"
    from "production"."productinventory"
    where "productid" = """), Fragment.encode(ProductId.pgType, compositeId.productid), Fragment.lit(""" AND "locationid" = """), Fragment.encode(LocationId.pgType, compositeId.locationid), Fragment.lit("")).query(ProductinventoryRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(compositeIds: Array[ProductinventoryId])(using c: Connection): java.util.List[ProductinventoryRow] = {
    val productid: Array[ProductId] = compositeIds.map(_.productid)
    val locationid: Array[LocationId] = compositeIds.map(_.locationid)
    return interpolate(Fragment.lit("""select "productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate"
    from "production"."productinventory"
    where ("productid", "locationid")
    in (select * from unnest("""), Fragment.encode(ProductId.pgTypeArray, productid), Fragment.lit(", "), Fragment.encode(LocationId.pgTypeArray, locationid), Fragment.lit("""))
    """)).query(ProductinventoryRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(compositeIds: Array[ProductinventoryId])(using c: Connection): java.util.Map[ProductinventoryId, ProductinventoryRow] = {
    val ret: HashMap[ProductinventoryId, ProductinventoryRow] = new HashMap[ProductinventoryId, ProductinventoryRow]()
    selectByIds(compositeIds)(using c).forEach(row => ret.put(row.compositeId, row): @scala.annotation.nowarn)
    return ret
  }

  override def update: UpdateBuilder[ProductinventoryFields, ProductinventoryRow] = UpdateBuilder.of(""""production"."productinventory"""", ProductinventoryFields.structure, ProductinventoryRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: ProductinventoryRow)(using c: Connection): java.lang.Boolean = {
    val compositeId: ProductinventoryId = row.compositeId
    return interpolate(Fragment.lit("""update "production"."productinventory"
    set "shelf" = """), Fragment.encode(PgTypes.text, row.shelf), Fragment.lit(""",
    "bin" = """), Fragment.encode(PgTypes.int2, row.bin), Fragment.lit("""::int2,
    "quantity" = """), Fragment.encode(PgTypes.int2, row.quantity), Fragment.lit("""::int2,
    "rowguid" = """), Fragment.encode(PgTypes.uuid, row.rowguid), Fragment.lit("""::uuid,
    "modifieddate" = """), Fragment.encode(PgTypes.timestamp, row.modifieddate), Fragment.lit("""::timestamp
    where "productid" = """), Fragment.encode(ProductId.pgType, compositeId.productid), Fragment.lit(""" AND "locationid" = """), Fragment.encode(LocationId.pgType, compositeId.locationid), Fragment.lit("")).update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: ProductinventoryRow)(using c: Connection): ProductinventoryRow = {
  interpolate(Fragment.lit("""insert into "production"."productinventory"("productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate")
    values ("""), Fragment.encode(ProductId.pgType, unsaved.productid), Fragment.lit("::int4, "), Fragment.encode(LocationId.pgType, unsaved.locationid), Fragment.lit("::int2, "), Fragment.encode(PgTypes.text, unsaved.shelf), Fragment.lit(", "), Fragment.encode(PgTypes.int2, unsaved.bin), Fragment.lit("::int2, "), Fragment.encode(PgTypes.int2, unsaved.quantity), Fragment.lit("::int2, "), Fragment.encode(PgTypes.uuid, unsaved.rowguid), Fragment.lit("::uuid, "), Fragment.encode(PgTypes.timestamp, unsaved.modifieddate), Fragment.lit("""::timestamp)
    on conflict ("productid", "locationid")
    do update set
      "shelf" = EXCLUDED."shelf",
    "bin" = EXCLUDED."bin",
    "quantity" = EXCLUDED."quantity",
    "rowguid" = EXCLUDED."rowguid",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate""""))
    .updateReturning(ProductinventoryRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: java.util.Iterator[ProductinventoryRow])(using c: Connection): java.util.List[ProductinventoryRow] = {
    interpolate(Fragment.lit("""insert into "production"."productinventory"("productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate")
    values (?::int4, ?::int2, ?, ?::int2, ?::int2, ?::uuid, ?::timestamp)
    on conflict ("productid", "locationid")
    do update set
      "shelf" = EXCLUDED."shelf",
    "bin" = EXCLUDED."bin",
    "quantity" = EXCLUDED."quantity",
    "rowguid" = EXCLUDED."rowguid",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate""""))
      .updateManyReturning(ProductinventoryRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: java.util.Iterator[ProductinventoryRow],
    batchSize: Integer = 10000
  )(using c: Connection): Integer = {
    interpolate(Fragment.lit("""create temporary table productinventory_TEMP (like "production"."productinventory") on commit drop""")).update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy productinventory_TEMP("productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate") from stdin""", batchSize, unsaved, c, ProductinventoryRow.pgText): @scala.annotation.nowarn
    return interpolate(Fragment.lit("""insert into "production"."productinventory"("productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate")
    select * from productinventory_TEMP
    on conflict ("productid", "locationid")
    do update set
      "shelf" = EXCLUDED."shelf",
    "bin" = EXCLUDED."bin",
    "quantity" = EXCLUDED."quantity",
    "rowguid" = EXCLUDED."rowguid",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table productinventory_TEMP;""")).update().runUnchecked(c)
  }
}