/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productinventory

import adventureworks.customtypes.Defaulted
import adventureworks.production.location.LocationId
import adventureworks.production.product.ProductId
import java.time.LocalDateTime
import java.util.UUID
import typr.runtime.PgText
import typr.runtime.PgTypes
import typr.runtime.RowParser
import typr.runtime.RowParsers

/** Table: production.productinventory
 * Product inventory information.
 * Composite primary key: productid, locationid
 */
case class ProductinventoryRow(
  /** Product identification number. Foreign key to Product.ProductID.
   * Points to [[adventureworks.production.product.ProductRow.productid]]
   */
  productid: ProductId,
  /** Inventory location identification number. Foreign key to Location.LocationID.
   * Points to [[adventureworks.production.location.LocationRow.locationid]]
   */
  locationid: LocationId,
  /** Storage compartment within an inventory location. */
  shelf: String,
  /** Storage container on a shelf in an inventory location.
   * Constraint CK_ProductInventory_Bin affecting columns bin: (((bin >= 0) AND (bin <= 100)))
   */
  bin: java.lang.Short,
  /** Quantity of products in the inventory location.
   * Default: 0
   */
  quantity: java.lang.Short,
  /** Default: uuid_generate_v1() */
  rowguid: UUID,
  /** Default: now() */
  modifieddate: LocalDateTime
) {
  def compositeId: ProductinventoryId = new ProductinventoryId(productid, locationid)

  def id: ProductinventoryId = this.compositeId

  def toUnsavedRow(
    quantity: Defaulted[java.lang.Short] = Defaulted.Provided(this.quantity),
    rowguid: Defaulted[UUID] = Defaulted.Provided(this.rowguid),
    modifieddate: Defaulted[LocalDateTime] = Defaulted.Provided(this.modifieddate)
  ): ProductinventoryRowUnsaved = {
    new ProductinventoryRowUnsaved(
      productid,
      locationid,
      shelf,
      bin,
      quantity,
      rowguid,
      modifieddate
    )
  }
}

object ProductinventoryRow {
  val `_rowParser`: RowParser[ProductinventoryRow] = RowParsers.of(ProductId.pgType, LocationId.pgType, PgTypes.text, PgTypes.int2, PgTypes.int2, PgTypes.uuid, PgTypes.timestamp, ProductinventoryRow.apply, row => Array[Any](row.productid, row.locationid, row.shelf, row.bin, row.quantity, row.rowguid, row.modifieddate))

  def apply(
    compositeId: ProductinventoryId,
    shelf: String,
    bin: java.lang.Short,
    quantity: java.lang.Short,
    rowguid: UUID,
    modifieddate: LocalDateTime
  ): ProductinventoryRow = {
    new ProductinventoryRow(
      compositeId.productid,
      compositeId.locationid,
      shelf,
      bin,
      quantity,
      rowguid,
      modifieddate
    )
  }

  given pgText: PgText[ProductinventoryRow] = PgText.from(`_rowParser`)
}