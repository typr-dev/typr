/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productreview

import adventureworks.production.product.ProductId
import adventureworks.public.Name
import java.sql.Connection
import java.util.ArrayList
import java.util.HashMap
import java.util.Optional
import typr.dsl.DeleteBuilder
import typr.dsl.Dialect
import typr.dsl.SelectBuilder
import typr.dsl.UpdateBuilder
import typr.runtime.Fragment
import typr.runtime.PgTypes
import typr.runtime.streamingInsert
import typr.runtime.Fragment.interpolate

class ProductreviewRepoImpl extends ProductreviewRepo {
  override def delete: DeleteBuilder[ProductreviewFields, ProductreviewRow] = DeleteBuilder.of(""""production"."productreview"""", ProductreviewFields.structure, Dialect.POSTGRESQL)

  override def deleteById(productreviewid: ProductreviewId)(using c: Connection): java.lang.Boolean = interpolate(Fragment.lit("""delete from "production"."productreview" where "productreviewid" = """), Fragment.encode(ProductreviewId.pgType, productreviewid), Fragment.lit("")).update().runUnchecked(c) > 0

  override def deleteByIds(productreviewids: Array[ProductreviewId])(using c: Connection): Integer = {
    interpolate(Fragment.lit("""delete
    from "production"."productreview"
    where "productreviewid" = ANY("""), Fragment.encode(ProductreviewId.pgTypeArray, productreviewids), Fragment.lit(")"))
      .update()
      .runUnchecked(c)
  }

  override def insert(unsaved: ProductreviewRow)(using c: Connection): ProductreviewRow = {
  interpolate(Fragment.lit("""insert into "production"."productreview"("productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate")
    values ("""), Fragment.encode(ProductreviewId.pgType, unsaved.productreviewid), Fragment.lit("::int4, "), Fragment.encode(ProductId.pgType, unsaved.productid), Fragment.lit("::int4, "), Fragment.encode(Name.pgType, unsaved.reviewername), Fragment.lit("::varchar, "), Fragment.encode(PgTypes.timestamp, unsaved.reviewdate), Fragment.lit("::timestamp, "), Fragment.encode(PgTypes.text, unsaved.emailaddress), Fragment.lit(", "), Fragment.encode(PgTypes.int4, unsaved.rating), Fragment.lit("::int4, "), Fragment.encode(PgTypes.text.opt(), unsaved.comments), Fragment.lit(", "), Fragment.encode(PgTypes.timestamp, unsaved.modifieddate), Fragment.lit("""::timestamp)
    RETURNING "productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate"
    """))
    .updateReturning(ProductreviewRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: ProductreviewRowUnsaved)(using c: Connection): ProductreviewRow = {
    val columns: ArrayList[Fragment] = new ArrayList()
    val values: ArrayList[Fragment] = new ArrayList()
    columns.add(Fragment.lit(""""productid"""")): @scala.annotation.nowarn
    values.add(interpolate(Fragment.encode(ProductId.pgType, unsaved.productid), Fragment.lit("::int4"))): @scala.annotation.nowarn
    columns.add(Fragment.lit(""""reviewername"""")): @scala.annotation.nowarn
    values.add(interpolate(Fragment.encode(Name.pgType, unsaved.reviewername), Fragment.lit("::varchar"))): @scala.annotation.nowarn
    columns.add(Fragment.lit(""""emailaddress"""")): @scala.annotation.nowarn
    values.add(interpolate(Fragment.encode(PgTypes.text, unsaved.emailaddress), Fragment.lit(""))): @scala.annotation.nowarn
    columns.add(Fragment.lit(""""rating"""")): @scala.annotation.nowarn
    values.add(interpolate(Fragment.encode(PgTypes.int4, unsaved.rating), Fragment.lit("::int4"))): @scala.annotation.nowarn
    columns.add(Fragment.lit(""""comments"""")): @scala.annotation.nowarn
    values.add(interpolate(Fragment.encode(PgTypes.text.opt(), unsaved.comments), Fragment.lit(""))): @scala.annotation.nowarn
    unsaved.productreviewid.visit(
      {  },
      value => { columns.add(Fragment.lit(""""productreviewid"""")): @scala.annotation.nowarn; values.add(interpolate(Fragment.encode(ProductreviewId.pgType, value), Fragment.lit("::int4"))): @scala.annotation.nowarn }
    );
    unsaved.reviewdate.visit(
      {  },
      value => { columns.add(Fragment.lit(""""reviewdate"""")): @scala.annotation.nowarn; values.add(interpolate(Fragment.encode(PgTypes.timestamp, value), Fragment.lit("::timestamp"))): @scala.annotation.nowarn }
    );
    unsaved.modifieddate.visit(
      {  },
      value => { columns.add(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.add(interpolate(Fragment.encode(PgTypes.timestamp, value), Fragment.lit("::timestamp"))): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      interpolate(Fragment.lit("""insert into "production"."productreview"("""), Fragment.comma(columns), Fragment.lit(""")
      values ("""), Fragment.comma(values), Fragment.lit(""")
      RETURNING "productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate"
      """))
    }
    return q.updateReturning(ProductreviewRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: java.util.Iterator[ProductreviewRow],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = streamingInsert.insertUnchecked(s"""COPY "production"."productreview"("productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate") FROM STDIN""", batchSize, unsaved, c, ProductreviewRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: java.util.Iterator[ProductreviewRowUnsaved],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = streamingInsert.insertUnchecked(s"""COPY "production"."productreview"("productid", "reviewername", "emailaddress", "rating", "comments", "productreviewid", "reviewdate", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved, c, ProductreviewRowUnsaved.pgText)

  override def select: SelectBuilder[ProductreviewFields, ProductreviewRow] = SelectBuilder.of(""""production"."productreview"""", ProductreviewFields.structure, ProductreviewRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): java.util.List[ProductreviewRow] = {
    interpolate(Fragment.lit("""select "productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate"
    from "production"."productreview"
    """)).query(ProductreviewRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(productreviewid: ProductreviewId)(using c: Connection): Optional[ProductreviewRow] = {
    interpolate(Fragment.lit("""select "productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate"
    from "production"."productreview"
    where "productreviewid" = """), Fragment.encode(ProductreviewId.pgType, productreviewid), Fragment.lit("")).query(ProductreviewRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(productreviewids: Array[ProductreviewId])(using c: Connection): java.util.List[ProductreviewRow] = {
    interpolate(Fragment.lit("""select "productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate"
    from "production"."productreview"
    where "productreviewid" = ANY("""), Fragment.encode(ProductreviewId.pgTypeArray, productreviewids), Fragment.lit(")")).query(ProductreviewRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(productreviewids: Array[ProductreviewId])(using c: Connection): java.util.Map[ProductreviewId, ProductreviewRow] = {
    val ret: HashMap[ProductreviewId, ProductreviewRow] = new HashMap[ProductreviewId, ProductreviewRow]()
    selectByIds(productreviewids)(using c).forEach(row => ret.put(row.productreviewid, row): @scala.annotation.nowarn)
    return ret
  }

  override def update: UpdateBuilder[ProductreviewFields, ProductreviewRow] = UpdateBuilder.of(""""production"."productreview"""", ProductreviewFields.structure, ProductreviewRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: ProductreviewRow)(using c: Connection): java.lang.Boolean = {
    val productreviewid: ProductreviewId = row.productreviewid
    return interpolate(Fragment.lit("""update "production"."productreview"
    set "productid" = """), Fragment.encode(ProductId.pgType, row.productid), Fragment.lit("""::int4,
    "reviewername" = """), Fragment.encode(Name.pgType, row.reviewername), Fragment.lit("""::varchar,
    "reviewdate" = """), Fragment.encode(PgTypes.timestamp, row.reviewdate), Fragment.lit("""::timestamp,
    "emailaddress" = """), Fragment.encode(PgTypes.text, row.emailaddress), Fragment.lit(""",
    "rating" = """), Fragment.encode(PgTypes.int4, row.rating), Fragment.lit("""::int4,
    "comments" = """), Fragment.encode(PgTypes.text.opt(), row.comments), Fragment.lit(""",
    "modifieddate" = """), Fragment.encode(PgTypes.timestamp, row.modifieddate), Fragment.lit("""::timestamp
    where "productreviewid" = """), Fragment.encode(ProductreviewId.pgType, productreviewid), Fragment.lit("")).update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: ProductreviewRow)(using c: Connection): ProductreviewRow = {
  interpolate(Fragment.lit("""insert into "production"."productreview"("productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate")
    values ("""), Fragment.encode(ProductreviewId.pgType, unsaved.productreviewid), Fragment.lit("::int4, "), Fragment.encode(ProductId.pgType, unsaved.productid), Fragment.lit("::int4, "), Fragment.encode(Name.pgType, unsaved.reviewername), Fragment.lit("::varchar, "), Fragment.encode(PgTypes.timestamp, unsaved.reviewdate), Fragment.lit("::timestamp, "), Fragment.encode(PgTypes.text, unsaved.emailaddress), Fragment.lit(", "), Fragment.encode(PgTypes.int4, unsaved.rating), Fragment.lit("::int4, "), Fragment.encode(PgTypes.text.opt(), unsaved.comments), Fragment.lit(", "), Fragment.encode(PgTypes.timestamp, unsaved.modifieddate), Fragment.lit("""::timestamp)
    on conflict ("productreviewid")
    do update set
      "productid" = EXCLUDED."productid",
    "reviewername" = EXCLUDED."reviewername",
    "reviewdate" = EXCLUDED."reviewdate",
    "emailaddress" = EXCLUDED."emailaddress",
    "rating" = EXCLUDED."rating",
    "comments" = EXCLUDED."comments",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate""""))
    .updateReturning(ProductreviewRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: java.util.Iterator[ProductreviewRow])(using c: Connection): java.util.List[ProductreviewRow] = {
    interpolate(Fragment.lit("""insert into "production"."productreview"("productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate")
    values (?::int4, ?::int4, ?::varchar, ?::timestamp, ?, ?::int4, ?, ?::timestamp)
    on conflict ("productreviewid")
    do update set
      "productid" = EXCLUDED."productid",
    "reviewername" = EXCLUDED."reviewername",
    "reviewdate" = EXCLUDED."reviewdate",
    "emailaddress" = EXCLUDED."emailaddress",
    "rating" = EXCLUDED."rating",
    "comments" = EXCLUDED."comments",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate""""))
      .updateManyReturning(ProductreviewRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: java.util.Iterator[ProductreviewRow],
    batchSize: Integer = 10000
  )(using c: Connection): Integer = {
    interpolate(Fragment.lit("""create temporary table productreview_TEMP (like "production"."productreview") on commit drop""")).update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy productreview_TEMP("productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate") from stdin""", batchSize, unsaved, c, ProductreviewRow.pgText): @scala.annotation.nowarn
    return interpolate(Fragment.lit("""insert into "production"."productreview"("productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate")
    select * from productreview_TEMP
    on conflict ("productreviewid")
    do update set
      "productid" = EXCLUDED."productid",
    "reviewername" = EXCLUDED."reviewername",
    "reviewdate" = EXCLUDED."reviewdate",
    "emailaddress" = EXCLUDED."emailaddress",
    "rating" = EXCLUDED."rating",
    "comments" = EXCLUDED."comments",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table productreview_TEMP;""")).update().runUnchecked(c)
  }
}