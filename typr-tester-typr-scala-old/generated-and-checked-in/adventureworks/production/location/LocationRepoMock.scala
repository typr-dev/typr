/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.location

import java.lang.RuntimeException
import java.sql.Connection
import java.util.ArrayList
import java.util.HashMap
import java.util.Optional
import java.util.function.Function
import java.util.stream.Collectors
import typr.dsl.DeleteBuilder
import typr.dsl.DeleteBuilderMock
import typr.dsl.DeleteParams
import typr.dsl.SelectBuilder
import typr.dsl.SelectBuilderMock
import typr.dsl.SelectParams
import typr.dsl.UpdateBuilder
import typr.dsl.UpdateBuilderMock
import typr.dsl.UpdateParams

case class LocationRepoMock(
  toRow: LocationRowUnsaved => LocationRow,
  map: HashMap[LocationId, LocationRow] = new HashMap[LocationId, LocationRow]()
) extends LocationRepo {
  override def delete: DeleteBuilder[LocationFields, LocationRow] = DeleteBuilderMock(LocationFields.structure, () => new ArrayList(map.values()), DeleteParams.empty(), row => row.locationid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(locationid: LocationId)(using c: Connection): java.lang.Boolean = Optional.ofNullable(map.remove(locationid)).isPresent()

  override def deleteByIds(locationids: Array[LocationId])(using c: Connection): Integer = {
    var count = 0
    locationids.foreach { id => if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: LocationRow)(using c: Connection): LocationRow = {
    if (map.containsKey(unsaved.locationid)) {
      throw new RuntimeException(s"id ${unsaved.locationid} already exists")
    }
    map.put(unsaved.locationid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: LocationRowUnsaved)(using c: Connection): LocationRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: java.util.Iterator[LocationRow],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = {
    var count = 0L
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.locationid, row): @scala.annotation.nowarn
      count = count + 1L
    }
    return count
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: java.util.Iterator[LocationRowUnsaved],
    batchSize: Integer = 10000
  )(using c: Connection): java.lang.Long = {
    var count = 0L
    while (unsaved.hasNext()) {
      val unsavedRow = unsaved.next()
      val row = toRow(unsavedRow)
      map.put(row.locationid, row): @scala.annotation.nowarn
      count = count + 1L
    }
    return count
  }

  override def select: SelectBuilder[LocationFields, LocationRow] = SelectBuilderMock(LocationFields.structure, () => new ArrayList(map.values()), SelectParams.empty())

  override def selectAll(using c: Connection): java.util.List[LocationRow] = new ArrayList(map.values())

  override def selectById(locationid: LocationId)(using c: Connection): Optional[LocationRow] = Optional.ofNullable(map.get(locationid))

  override def selectByIds(locationids: Array[LocationId])(using c: Connection): java.util.List[LocationRow] = {
    val result = new ArrayList[LocationRow]()
    locationids.foreach { id => val opt = Optional.ofNullable(map.get(id)); if (opt.isPresent()) {
      result.add(opt.get()): @scala.annotation.nowarn
    } }
    return result
  }

  override def selectByIdsTracked(locationids: Array[LocationId])(using c: Connection): java.util.Map[LocationId, LocationRow] = selectByIds(locationids)(using c).stream().collect(Collectors.toMap((row: LocationRow) => row.locationid, Function.identity()))

  override def update: UpdateBuilder[LocationFields, LocationRow] = UpdateBuilderMock(LocationFields.structure, () => new ArrayList(map.values()), UpdateParams.empty(), row => row)

  override def update(row: LocationRow)(using c: Connection): java.lang.Boolean = {
    val shouldUpdate = Optional.ofNullable(map.get(row.locationid)).filter(oldRow => (oldRow != row)).isPresent()
    if (shouldUpdate) {
      map.put(row.locationid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: LocationRow)(using c: Connection): LocationRow = {
    map.put(unsaved.locationid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: java.util.Iterator[LocationRow])(using c: Connection): java.util.List[LocationRow] = {
    val result = new ArrayList[LocationRow]()
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.locationid, row): @scala.annotation.nowarn
      result.add(row): @scala.annotation.nowarn
    }
    return result
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: java.util.Iterator[LocationRow],
    batchSize: Integer = 10000
  )(using c: Connection): Integer = {
    var count = 0
    while (unsaved.hasNext()) {
      val row = unsaved.next()
      map.put(row.locationid, row): @scala.annotation.nowarn
      count = count + 1
    }
    return count
  }
}