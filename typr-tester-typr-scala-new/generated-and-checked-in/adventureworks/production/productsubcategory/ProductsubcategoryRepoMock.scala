/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productsubcategory

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class ProductsubcategoryRepoMock(
  toRow: ProductsubcategoryRowUnsaved => ProductsubcategoryRow,
  map: scala.collection.mutable.Map[ProductsubcategoryId, ProductsubcategoryRow] = scala.collection.mutable.Map.empty[ProductsubcategoryId, ProductsubcategoryRow]
) extends ProductsubcategoryRepo {
  override def delete: DeleteBuilder[ProductsubcategoryFields, ProductsubcategoryRow] = DeleteBuilderMock(ProductsubcategoryFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.productsubcategoryid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(productsubcategoryid: ProductsubcategoryId)(using c: Connection): Boolean = map.remove(productsubcategoryid).isDefined

  override def deleteByIds(productsubcategoryids: Array[ProductsubcategoryId])(using c: Connection): Int = {
    var count = 0
    productsubcategoryids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: ProductsubcategoryRow)(using c: Connection): ProductsubcategoryRow = {
    if (map.contains(unsaved.productsubcategoryid)) {
      throw new RuntimeException(s"id ${unsaved.productsubcategoryid} already exists")
    }
    map.put(unsaved.productsubcategoryid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: ProductsubcategoryRowUnsaved)(using c: Connection): ProductsubcategoryRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[ProductsubcategoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.productsubcategoryid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[ProductsubcategoryRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.productsubcategoryid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[ProductsubcategoryFields, ProductsubcategoryRow] = SelectBuilderMock(ProductsubcategoryFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[ProductsubcategoryRow] = map.values.toList

  override def selectById(productsubcategoryid: ProductsubcategoryId)(using c: Connection): Option[ProductsubcategoryRow] = map.get(productsubcategoryid)

  override def selectByIds(productsubcategoryids: Array[ProductsubcategoryId])(using c: Connection): List[ProductsubcategoryRow] = productsubcategoryids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(productsubcategoryids: Array[ProductsubcategoryId])(using c: Connection): Map[ProductsubcategoryId, ProductsubcategoryRow] = selectByIds(productsubcategoryids)(using c).map(x => (((row: ProductsubcategoryRow) => row.productsubcategoryid).apply(x), x)).toMap

  override def update: UpdateBuilder[ProductsubcategoryFields, ProductsubcategoryRow] = UpdateBuilderMock(ProductsubcategoryFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: ProductsubcategoryRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.productsubcategoryid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.productsubcategoryid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: ProductsubcategoryRow)(using c: Connection): ProductsubcategoryRow = {
    map.put(unsaved.productsubcategoryid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[ProductsubcategoryRow])(using c: Connection): List[ProductsubcategoryRow] = {
    unsaved.map { row =>
      map.put(row.productsubcategoryid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[ProductsubcategoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.productsubcategoryid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}