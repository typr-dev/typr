/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productinventory

import adventureworks.production.location.LocationId
import adventureworks.production.product.ProductId
import java.sql.Connection
import scala.collection.mutable.ListBuffer
import typr.runtime.PgTypes
import typr.runtime.streamingInsert
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.Dialect
import typr.scaladsl.Fragment
import typr.scaladsl.ScalaDbTypes
import typr.scaladsl.ScalaIteratorOps
import typr.scaladsl.SelectBuilder
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.Fragment.sql

class ProductinventoryRepoImpl extends ProductinventoryRepo {
  override def delete: DeleteBuilder[ProductinventoryFields, ProductinventoryRow] = DeleteBuilder.of(""""production"."productinventory"""", ProductinventoryFields.structure, Dialect.POSTGRESQL)

  override def deleteById(compositeId: ProductinventoryId)(using c: Connection): Boolean = sql"""delete from "production"."productinventory" where "productid" = ${Fragment.encode(ProductId.pgType, compositeId.productid)} AND "locationid" = ${Fragment.encode(LocationId.pgType, compositeId.locationid)}""".update().runUnchecked(c) > 0

  override def deleteByIds(compositeIds: Array[ProductinventoryId])(using c: Connection): Int = {
    val productid: Array[ProductId] = compositeIds.map(_.productid)
    val locationid: Array[LocationId] = compositeIds.map(_.locationid)
    return sql"""delete
    from "production"."productinventory"
    where ("productid", "locationid")
    in (select * from unnest(${Fragment.encode(ProductId.pgTypeArray, productid)}, ${Fragment.encode(LocationId.pgTypeArray, locationid)}))
    """.update().runUnchecked(c)
  }

  override def insert(unsaved: ProductinventoryRow)(using c: Connection): ProductinventoryRow = {
  sql"""insert into "production"."productinventory"("productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate")
    values (${Fragment.encode(ProductId.pgType, unsaved.productid)}::int4, ${Fragment.encode(LocationId.pgType, unsaved.locationid)}::int2, ${Fragment.encode(PgTypes.text, unsaved.shelf)}, ${Fragment.encode(ScalaDbTypes.PgTypes.int2, unsaved.bin)}::int2, ${Fragment.encode(ScalaDbTypes.PgTypes.int2, unsaved.quantity)}::int2, ${Fragment.encode(PgTypes.uuid, unsaved.rowguid)}::uuid, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    RETURNING "productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate"
    """
    .updateReturning(ProductinventoryRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: ProductinventoryRowUnsaved)(using c: Connection): ProductinventoryRow = {
    val columns: ListBuffer[Fragment] = ListBuffer()
    val values: ListBuffer[Fragment] = ListBuffer()
    columns.addOne(Fragment.lit(""""productid"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(ProductId.pgType, unsaved.productid)}::int4"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""locationid"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(LocationId.pgType, unsaved.locationid)}::int2"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""shelf"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(PgTypes.text, unsaved.shelf)}"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""bin"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(ScalaDbTypes.PgTypes.int2, unsaved.bin)}::int2"): @scala.annotation.nowarn
    unsaved.quantity.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""quantity"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(ScalaDbTypes.PgTypes.int2, value)}::int2"): @scala.annotation.nowarn }
    );
    unsaved.rowguid.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""rowguid"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.uuid, value)}::uuid"): @scala.annotation.nowarn }
    );
    unsaved.modifieddate.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.timestamp, value)}::timestamp"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      sql"""insert into "production"."productinventory"(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      RETURNING "productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate"
      """
    }
    return q.updateReturning(ProductinventoryRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: Iterator[ProductinventoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "production"."productinventory"("productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate") FROM STDIN""", batchSize, unsaved.toJavaIterator, c, ProductinventoryRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[ProductinventoryRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "production"."productinventory"("productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved.toJavaIterator, c, ProductinventoryRowUnsaved.pgText)

  override def select: SelectBuilder[ProductinventoryFields, ProductinventoryRow] = SelectBuilder.of(""""production"."productinventory"""", ProductinventoryFields.structure, ProductinventoryRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): List[ProductinventoryRow] = {
    sql"""select "productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate"
    from "production"."productinventory"
    """.query(ProductinventoryRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(compositeId: ProductinventoryId)(using c: Connection): Option[ProductinventoryRow] = {
    sql"""select "productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate"
    from "production"."productinventory"
    where "productid" = ${Fragment.encode(ProductId.pgType, compositeId.productid)} AND "locationid" = ${Fragment.encode(LocationId.pgType, compositeId.locationid)}""".query(ProductinventoryRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(compositeIds: Array[ProductinventoryId])(using c: Connection): List[ProductinventoryRow] = {
    val productid: Array[ProductId] = compositeIds.map(_.productid)
    val locationid: Array[LocationId] = compositeIds.map(_.locationid)
    return sql"""select "productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate"
    from "production"."productinventory"
    where ("productid", "locationid")
    in (select * from unnest(${Fragment.encode(ProductId.pgTypeArray, productid)}, ${Fragment.encode(LocationId.pgTypeArray, locationid)}))
    """.query(ProductinventoryRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(compositeIds: Array[ProductinventoryId])(using c: Connection): Map[ProductinventoryId, ProductinventoryRow] = {
    val ret: scala.collection.mutable.Map[ProductinventoryId, ProductinventoryRow] = scala.collection.mutable.Map.empty[ProductinventoryId, ProductinventoryRow]
    selectByIds(compositeIds)(using c).foreach(row => ret.put(row.compositeId, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[ProductinventoryFields, ProductinventoryRow] = UpdateBuilder.of(""""production"."productinventory"""", ProductinventoryFields.structure, ProductinventoryRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: ProductinventoryRow)(using c: Connection): Boolean = {
    val compositeId: ProductinventoryId = row.compositeId
    return sql"""update "production"."productinventory"
    set "shelf" = ${Fragment.encode(PgTypes.text, row.shelf)},
    "bin" = ${Fragment.encode(ScalaDbTypes.PgTypes.int2, row.bin)}::int2,
    "quantity" = ${Fragment.encode(ScalaDbTypes.PgTypes.int2, row.quantity)}::int2,
    "rowguid" = ${Fragment.encode(PgTypes.uuid, row.rowguid)}::uuid,
    "modifieddate" = ${Fragment.encode(PgTypes.timestamp, row.modifieddate)}::timestamp
    where "productid" = ${Fragment.encode(ProductId.pgType, compositeId.productid)} AND "locationid" = ${Fragment.encode(LocationId.pgType, compositeId.locationid)}""".update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: ProductinventoryRow)(using c: Connection): ProductinventoryRow = {
  sql"""insert into "production"."productinventory"("productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate")
    values (${Fragment.encode(ProductId.pgType, unsaved.productid)}::int4, ${Fragment.encode(LocationId.pgType, unsaved.locationid)}::int2, ${Fragment.encode(PgTypes.text, unsaved.shelf)}, ${Fragment.encode(ScalaDbTypes.PgTypes.int2, unsaved.bin)}::int2, ${Fragment.encode(ScalaDbTypes.PgTypes.int2, unsaved.quantity)}::int2, ${Fragment.encode(PgTypes.uuid, unsaved.rowguid)}::uuid, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    on conflict ("productid", "locationid")
    do update set
      "shelf" = EXCLUDED."shelf",
    "bin" = EXCLUDED."bin",
    "quantity" = EXCLUDED."quantity",
    "rowguid" = EXCLUDED."rowguid",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate""""
    .updateReturning(ProductinventoryRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: Iterator[ProductinventoryRow])(using c: Connection): List[ProductinventoryRow] = {
    sql"""insert into "production"."productinventory"("productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate")
    values (?::int4, ?::int2, ?, ?::int2, ?::int2, ?::uuid, ?::timestamp)
    on conflict ("productid", "locationid")
    do update set
      "shelf" = EXCLUDED."shelf",
    "bin" = EXCLUDED."bin",
    "quantity" = EXCLUDED."quantity",
    "rowguid" = EXCLUDED."rowguid",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate""""
      .updateManyReturning(ProductinventoryRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[ProductinventoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    sql"""create temporary table productinventory_TEMP (like "production"."productinventory") on commit drop""".update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy productinventory_TEMP("productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate") from stdin""", batchSize, unsaved.toJavaIterator, c, ProductinventoryRow.pgText): @scala.annotation.nowarn
    return sql"""insert into "production"."productinventory"("productid", "locationid", "shelf", "bin", "quantity", "rowguid", "modifieddate")
    select * from productinventory_TEMP
    on conflict ("productid", "locationid")
    do update set
      "shelf" = EXCLUDED."shelf",
    "bin" = EXCLUDED."bin",
    "quantity" = EXCLUDED."quantity",
    "rowguid" = EXCLUDED."rowguid",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table productinventory_TEMP;""".update().runUnchecked(c)
  }
}