/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productcategory

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class ProductcategoryRepoMock(
  toRow: ProductcategoryRowUnsaved => ProductcategoryRow,
  map: scala.collection.mutable.Map[ProductcategoryId, ProductcategoryRow] = scala.collection.mutable.Map.empty[ProductcategoryId, ProductcategoryRow]
) extends ProductcategoryRepo {
  override def delete: DeleteBuilder[ProductcategoryFields, ProductcategoryRow] = DeleteBuilderMock(ProductcategoryFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.productcategoryid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(productcategoryid: ProductcategoryId)(using c: Connection): Boolean = map.remove(productcategoryid).isDefined

  override def deleteByIds(productcategoryids: Array[ProductcategoryId])(using c: Connection): Int = {
    var count = 0
    productcategoryids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: ProductcategoryRow)(using c: Connection): ProductcategoryRow = {
    if (map.contains(unsaved.productcategoryid)) {
      throw new RuntimeException(s"id ${unsaved.productcategoryid} already exists")
    }
    map.put(unsaved.productcategoryid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: ProductcategoryRowUnsaved)(using c: Connection): ProductcategoryRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[ProductcategoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.productcategoryid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[ProductcategoryRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.productcategoryid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[ProductcategoryFields, ProductcategoryRow] = SelectBuilderMock(ProductcategoryFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[ProductcategoryRow] = map.values.toList

  override def selectById(productcategoryid: ProductcategoryId)(using c: Connection): Option[ProductcategoryRow] = map.get(productcategoryid)

  override def selectByIds(productcategoryids: Array[ProductcategoryId])(using c: Connection): List[ProductcategoryRow] = productcategoryids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(productcategoryids: Array[ProductcategoryId])(using c: Connection): Map[ProductcategoryId, ProductcategoryRow] = selectByIds(productcategoryids)(using c).map(x => (((row: ProductcategoryRow) => row.productcategoryid).apply(x), x)).toMap

  override def update: UpdateBuilder[ProductcategoryFields, ProductcategoryRow] = UpdateBuilderMock(ProductcategoryFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: ProductcategoryRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.productcategoryid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.productcategoryid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: ProductcategoryRow)(using c: Connection): ProductcategoryRow = {
    map.put(unsaved.productcategoryid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[ProductcategoryRow])(using c: Connection): List[ProductcategoryRow] = {
    unsaved.map { row =>
      map.put(row.productcategoryid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[ProductcategoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.productcategoryid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}