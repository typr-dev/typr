/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.workorder

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class WorkorderRepoMock(
  toRow: WorkorderRowUnsaved => WorkorderRow,
  map: scala.collection.mutable.Map[WorkorderId, WorkorderRow] = scala.collection.mutable.Map.empty[WorkorderId, WorkorderRow]
) extends WorkorderRepo {
  override def delete: DeleteBuilder[WorkorderFields, WorkorderRow] = DeleteBuilderMock(WorkorderFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.workorderid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(workorderid: WorkorderId)(using c: Connection): Boolean = map.remove(workorderid).isDefined

  override def deleteByIds(workorderids: Array[WorkorderId])(using c: Connection): Int = {
    var count = 0
    workorderids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: WorkorderRow)(using c: Connection): WorkorderRow = {
    if (map.contains(unsaved.workorderid)) {
      throw new RuntimeException(s"id ${unsaved.workorderid} already exists")
    }
    map.put(unsaved.workorderid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: WorkorderRowUnsaved)(using c: Connection): WorkorderRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[WorkorderRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.workorderid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[WorkorderRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.workorderid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[WorkorderFields, WorkorderRow] = SelectBuilderMock(WorkorderFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[WorkorderRow] = map.values.toList

  override def selectById(workorderid: WorkorderId)(using c: Connection): Option[WorkorderRow] = map.get(workorderid)

  override def selectByIds(workorderids: Array[WorkorderId])(using c: Connection): List[WorkorderRow] = workorderids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(workorderids: Array[WorkorderId])(using c: Connection): Map[WorkorderId, WorkorderRow] = selectByIds(workorderids)(using c).map(x => (((row: WorkorderRow) => row.workorderid).apply(x), x)).toMap

  override def update: UpdateBuilder[WorkorderFields, WorkorderRow] = UpdateBuilderMock(WorkorderFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: WorkorderRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.workorderid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.workorderid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: WorkorderRow)(using c: Connection): WorkorderRow = {
    map.put(unsaved.workorderid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[WorkorderRow])(using c: Connection): List[WorkorderRow] = {
    unsaved.map { row =>
      map.put(row.workorderid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[WorkorderRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.workorderid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}