/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.transactionhistoryarchive

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class TransactionhistoryarchiveRepoMock(
  toRow: TransactionhistoryarchiveRowUnsaved => TransactionhistoryarchiveRow,
  map: scala.collection.mutable.Map[TransactionhistoryarchiveId, TransactionhistoryarchiveRow] = scala.collection.mutable.Map.empty[TransactionhistoryarchiveId, TransactionhistoryarchiveRow]
) extends TransactionhistoryarchiveRepo {
  override def delete: DeleteBuilder[TransactionhistoryarchiveFields, TransactionhistoryarchiveRow] = DeleteBuilderMock(TransactionhistoryarchiveFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.transactionid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(transactionid: TransactionhistoryarchiveId)(using c: Connection): Boolean = map.remove(transactionid).isDefined

  override def deleteByIds(transactionids: Array[TransactionhistoryarchiveId])(using c: Connection): Int = {
    var count = 0
    transactionids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: TransactionhistoryarchiveRow)(using c: Connection): TransactionhistoryarchiveRow = {
    if (map.contains(unsaved.transactionid)) {
      throw new RuntimeException(s"id ${unsaved.transactionid} already exists")
    }
    map.put(unsaved.transactionid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: TransactionhistoryarchiveRowUnsaved)(using c: Connection): TransactionhistoryarchiveRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[TransactionhistoryarchiveRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.transactionid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[TransactionhistoryarchiveRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.transactionid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[TransactionhistoryarchiveFields, TransactionhistoryarchiveRow] = SelectBuilderMock(TransactionhistoryarchiveFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[TransactionhistoryarchiveRow] = map.values.toList

  override def selectById(transactionid: TransactionhistoryarchiveId)(using c: Connection): Option[TransactionhistoryarchiveRow] = map.get(transactionid)

  override def selectByIds(transactionids: Array[TransactionhistoryarchiveId])(using c: Connection): List[TransactionhistoryarchiveRow] = transactionids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(transactionids: Array[TransactionhistoryarchiveId])(using c: Connection): Map[TransactionhistoryarchiveId, TransactionhistoryarchiveRow] = selectByIds(transactionids)(using c).map(x => (((row: TransactionhistoryarchiveRow) => row.transactionid).apply(x), x)).toMap

  override def update: UpdateBuilder[TransactionhistoryarchiveFields, TransactionhistoryarchiveRow] = UpdateBuilderMock(TransactionhistoryarchiveFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: TransactionhistoryarchiveRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.transactionid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.transactionid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: TransactionhistoryarchiveRow)(using c: Connection): TransactionhistoryarchiveRow = {
    map.put(unsaved.transactionid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[TransactionhistoryarchiveRow])(using c: Connection): List[TransactionhistoryarchiveRow] = {
    unsaved.map { row =>
      map.put(row.transactionid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[TransactionhistoryarchiveRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.transactionid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}