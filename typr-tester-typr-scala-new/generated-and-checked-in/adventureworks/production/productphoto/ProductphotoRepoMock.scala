/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productphoto

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class ProductphotoRepoMock(
  toRow: ProductphotoRowUnsaved => ProductphotoRow,
  map: scala.collection.mutable.Map[ProductphotoId, ProductphotoRow] = scala.collection.mutable.Map.empty[ProductphotoId, ProductphotoRow]
) extends ProductphotoRepo {
  override def delete: DeleteBuilder[ProductphotoFields, ProductphotoRow] = DeleteBuilderMock(ProductphotoFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.productphotoid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(productphotoid: ProductphotoId)(using c: Connection): Boolean = map.remove(productphotoid).isDefined

  override def deleteByIds(productphotoids: Array[ProductphotoId])(using c: Connection): Int = {
    var count = 0
    productphotoids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: ProductphotoRow)(using c: Connection): ProductphotoRow = {
    if (map.contains(unsaved.productphotoid)) {
      throw new RuntimeException(s"id ${unsaved.productphotoid} already exists")
    }
    map.put(unsaved.productphotoid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: ProductphotoRowUnsaved)(using c: Connection): ProductphotoRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[ProductphotoRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.productphotoid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[ProductphotoRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.productphotoid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[ProductphotoFields, ProductphotoRow] = SelectBuilderMock(ProductphotoFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[ProductphotoRow] = map.values.toList

  override def selectById(productphotoid: ProductphotoId)(using c: Connection): Option[ProductphotoRow] = map.get(productphotoid)

  override def selectByIds(productphotoids: Array[ProductphotoId])(using c: Connection): List[ProductphotoRow] = productphotoids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(productphotoids: Array[ProductphotoId])(using c: Connection): Map[ProductphotoId, ProductphotoRow] = selectByIds(productphotoids)(using c).map(x => (((row: ProductphotoRow) => row.productphotoid).apply(x), x)).toMap

  override def update: UpdateBuilder[ProductphotoFields, ProductphotoRow] = UpdateBuilderMock(ProductphotoFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: ProductphotoRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.productphotoid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.productphotoid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: ProductphotoRow)(using c: Connection): ProductphotoRow = {
    map.put(unsaved.productphotoid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[ProductphotoRow])(using c: Connection): List[ProductphotoRow] = {
    unsaved.map { row =>
      map.put(row.productphotoid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[ProductphotoRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.productphotoid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}