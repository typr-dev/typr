/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.culture

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class CultureRepoMock(
  toRow: CultureRowUnsaved => CultureRow,
  map: scala.collection.mutable.Map[CultureId, CultureRow] = scala.collection.mutable.Map.empty[CultureId, CultureRow]
) extends CultureRepo {
  override def delete: DeleteBuilder[CultureFields, CultureRow] = DeleteBuilderMock(CultureFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.cultureid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(cultureid: CultureId)(using c: Connection): Boolean = map.remove(cultureid).isDefined

  override def deleteByIds(cultureids: Array[CultureId])(using c: Connection): Int = {
    var count = 0
    cultureids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: CultureRow)(using c: Connection): CultureRow = {
    if (map.contains(unsaved.cultureid)) {
      throw new RuntimeException(s"id ${unsaved.cultureid} already exists")
    }
    map.put(unsaved.cultureid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: CultureRowUnsaved)(using c: Connection): CultureRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[CultureRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.cultureid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[CultureRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.cultureid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[CultureFields, CultureRow] = SelectBuilderMock(CultureFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[CultureRow] = map.values.toList

  override def selectById(cultureid: CultureId)(using c: Connection): Option[CultureRow] = map.get(cultureid)

  override def selectByIds(cultureids: Array[CultureId])(using c: Connection): List[CultureRow] = cultureids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(cultureids: Array[CultureId])(using c: Connection): Map[CultureId, CultureRow] = selectByIds(cultureids)(using c).map(x => (((row: CultureRow) => row.cultureid).apply(x), x)).toMap

  override def update: UpdateBuilder[CultureFields, CultureRow] = UpdateBuilderMock(CultureFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: CultureRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.cultureid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.cultureid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: CultureRow)(using c: Connection): CultureRow = {
    map.put(unsaved.cultureid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[CultureRow])(using c: Connection): List[CultureRow] = {
    unsaved.map { row =>
      map.put(row.cultureid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[CultureRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.cultureid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}