/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.culture

import adventureworks.public.Name
import java.sql.Connection
import scala.collection.mutable.ListBuffer
import typr.runtime.PgTypes
import typr.runtime.streamingInsert
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.Dialect
import typr.scaladsl.Fragment
import typr.scaladsl.ScalaIteratorOps
import typr.scaladsl.SelectBuilder
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.Fragment.sql

class CultureRepoImpl extends CultureRepo {
  override def delete: DeleteBuilder[CultureFields, CultureRow] = DeleteBuilder.of(""""production"."culture"""", CultureFields.structure, Dialect.POSTGRESQL)

  override def deleteById(cultureid: CultureId)(using c: Connection): Boolean = sql"""delete from "production"."culture" where "cultureid" = ${Fragment.encode(CultureId.pgType, cultureid)}""".update().runUnchecked(c) > 0

  override def deleteByIds(cultureids: Array[CultureId])(using c: Connection): Int = {
    sql"""delete
    from "production"."culture"
    where "cultureid" = ANY(${Fragment.encode(CultureId.pgTypeArray, cultureids)})"""
      .update()
      .runUnchecked(c)
  }

  override def insert(unsaved: CultureRow)(using c: Connection): CultureRow = {
  sql"""insert into "production"."culture"("cultureid", "name", "modifieddate")
    values (${Fragment.encode(CultureId.pgType, unsaved.cultureid)}::bpchar, ${Fragment.encode(Name.pgType, unsaved.name)}::varchar, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    RETURNING "cultureid", "name", "modifieddate"
    """
    .updateReturning(CultureRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: CultureRowUnsaved)(using c: Connection): CultureRow = {
    val columns: ListBuffer[Fragment] = ListBuffer()
    val values: ListBuffer[Fragment] = ListBuffer()
    columns.addOne(Fragment.lit(""""cultureid"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(CultureId.pgType, unsaved.cultureid)}::bpchar"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""name"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(Name.pgType, unsaved.name)}::varchar"): @scala.annotation.nowarn
    unsaved.modifieddate.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.timestamp, value)}::timestamp"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      sql"""insert into "production"."culture"(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      RETURNING "cultureid", "name", "modifieddate"
      """
    }
    return q.updateReturning(CultureRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: Iterator[CultureRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "production"."culture"("cultureid", "name", "modifieddate") FROM STDIN""", batchSize, unsaved.toJavaIterator, c, CultureRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[CultureRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "production"."culture"("cultureid", "name", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved.toJavaIterator, c, CultureRowUnsaved.pgText)

  override def select: SelectBuilder[CultureFields, CultureRow] = SelectBuilder.of(""""production"."culture"""", CultureFields.structure, CultureRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): List[CultureRow] = {
    sql"""select "cultureid", "name", "modifieddate"
    from "production"."culture"
    """.query(CultureRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(cultureid: CultureId)(using c: Connection): Option[CultureRow] = {
    sql"""select "cultureid", "name", "modifieddate"
    from "production"."culture"
    where "cultureid" = ${Fragment.encode(CultureId.pgType, cultureid)}""".query(CultureRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(cultureids: Array[CultureId])(using c: Connection): List[CultureRow] = {
    sql"""select "cultureid", "name", "modifieddate"
    from "production"."culture"
    where "cultureid" = ANY(${Fragment.encode(CultureId.pgTypeArray, cultureids)})""".query(CultureRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(cultureids: Array[CultureId])(using c: Connection): Map[CultureId, CultureRow] = {
    val ret: scala.collection.mutable.Map[CultureId, CultureRow] = scala.collection.mutable.Map.empty[CultureId, CultureRow]
    selectByIds(cultureids)(using c).foreach(row => ret.put(row.cultureid, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[CultureFields, CultureRow] = UpdateBuilder.of(""""production"."culture"""", CultureFields.structure, CultureRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: CultureRow)(using c: Connection): Boolean = {
    val cultureid: CultureId = row.cultureid
    return sql"""update "production"."culture"
    set "name" = ${Fragment.encode(Name.pgType, row.name)}::varchar,
    "modifieddate" = ${Fragment.encode(PgTypes.timestamp, row.modifieddate)}::timestamp
    where "cultureid" = ${Fragment.encode(CultureId.pgType, cultureid)}""".update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: CultureRow)(using c: Connection): CultureRow = {
  sql"""insert into "production"."culture"("cultureid", "name", "modifieddate")
    values (${Fragment.encode(CultureId.pgType, unsaved.cultureid)}::bpchar, ${Fragment.encode(Name.pgType, unsaved.name)}::varchar, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    on conflict ("cultureid")
    do update set
      "name" = EXCLUDED."name",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "cultureid", "name", "modifieddate""""
    .updateReturning(CultureRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: Iterator[CultureRow])(using c: Connection): List[CultureRow] = {
    sql"""insert into "production"."culture"("cultureid", "name", "modifieddate")
    values (?::bpchar, ?::varchar, ?::timestamp)
    on conflict ("cultureid")
    do update set
      "name" = EXCLUDED."name",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "cultureid", "name", "modifieddate""""
      .updateManyReturning(CultureRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[CultureRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    sql"""create temporary table culture_TEMP (like "production"."culture") on commit drop""".update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy culture_TEMP("cultureid", "name", "modifieddate") from stdin""", batchSize, unsaved.toJavaIterator, c, CultureRow.pgText): @scala.annotation.nowarn
    return sql"""insert into "production"."culture"("cultureid", "name", "modifieddate")
    select * from culture_TEMP
    on conflict ("cultureid")
    do update set
      "name" = EXCLUDED."name",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table culture_TEMP;""".update().runUnchecked(c)
  }
}