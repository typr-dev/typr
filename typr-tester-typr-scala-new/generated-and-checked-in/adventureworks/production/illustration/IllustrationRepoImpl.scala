/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.illustration

import java.sql.Connection
import scala.collection.mutable.ListBuffer
import typr.runtime.PgTypes
import typr.runtime.streamingInsert
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.Dialect
import typr.scaladsl.Fragment
import typr.scaladsl.PgTypeOps
import typr.scaladsl.ScalaIteratorOps
import typr.scaladsl.SelectBuilder
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.Fragment.sql

class IllustrationRepoImpl extends IllustrationRepo {
  override def delete: DeleteBuilder[IllustrationFields, IllustrationRow] = DeleteBuilder.of(""""production"."illustration"""", IllustrationFields.structure, Dialect.POSTGRESQL)

  override def deleteById(illustrationid: IllustrationId)(using c: Connection): Boolean = sql"""delete from "production"."illustration" where "illustrationid" = ${Fragment.encode(IllustrationId.pgType, illustrationid)}""".update().runUnchecked(c) > 0

  override def deleteByIds(illustrationids: Array[IllustrationId])(using c: Connection): Int = {
    sql"""delete
    from "production"."illustration"
    where "illustrationid" = ANY(${Fragment.encode(IllustrationId.pgTypeArray, illustrationids)})"""
      .update()
      .runUnchecked(c)
  }

  override def insert(unsaved: IllustrationRow)(using c: Connection): IllustrationRow = {
  sql"""insert into "production"."illustration"("illustrationid", "diagram", "modifieddate")
    values (${Fragment.encode(IllustrationId.pgType, unsaved.illustrationid)}::int4, ${Fragment.encode(PgTypes.xml.nullable, unsaved.diagram)}::xml, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    RETURNING "illustrationid", "diagram", "modifieddate"
    """
    .updateReturning(IllustrationRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: IllustrationRowUnsaved)(using c: Connection): IllustrationRow = {
    val columns: ListBuffer[Fragment] = ListBuffer()
    val values: ListBuffer[Fragment] = ListBuffer()
    columns.addOne(Fragment.lit(""""diagram"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(PgTypes.xml.nullable, unsaved.diagram)}::xml"): @scala.annotation.nowarn
    unsaved.illustrationid.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""illustrationid"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(IllustrationId.pgType, value)}::int4"): @scala.annotation.nowarn }
    );
    unsaved.modifieddate.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.timestamp, value)}::timestamp"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      sql"""insert into "production"."illustration"(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      RETURNING "illustrationid", "diagram", "modifieddate"
      """
    }
    return q.updateReturning(IllustrationRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: Iterator[IllustrationRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "production"."illustration"("illustrationid", "diagram", "modifieddate") FROM STDIN""", batchSize, unsaved.toJavaIterator, c, IllustrationRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[IllustrationRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "production"."illustration"("diagram", "illustrationid", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved.toJavaIterator, c, IllustrationRowUnsaved.pgText)

  override def select: SelectBuilder[IllustrationFields, IllustrationRow] = SelectBuilder.of(""""production"."illustration"""", IllustrationFields.structure, IllustrationRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): List[IllustrationRow] = {
    sql"""select "illustrationid", "diagram", "modifieddate"
    from "production"."illustration"
    """.query(IllustrationRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(illustrationid: IllustrationId)(using c: Connection): Option[IllustrationRow] = {
    sql"""select "illustrationid", "diagram", "modifieddate"
    from "production"."illustration"
    where "illustrationid" = ${Fragment.encode(IllustrationId.pgType, illustrationid)}""".query(IllustrationRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(illustrationids: Array[IllustrationId])(using c: Connection): List[IllustrationRow] = {
    sql"""select "illustrationid", "diagram", "modifieddate"
    from "production"."illustration"
    where "illustrationid" = ANY(${Fragment.encode(IllustrationId.pgTypeArray, illustrationids)})""".query(IllustrationRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(illustrationids: Array[IllustrationId])(using c: Connection): Map[IllustrationId, IllustrationRow] = {
    val ret: scala.collection.mutable.Map[IllustrationId, IllustrationRow] = scala.collection.mutable.Map.empty[IllustrationId, IllustrationRow]
    selectByIds(illustrationids)(using c).foreach(row => ret.put(row.illustrationid, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[IllustrationFields, IllustrationRow] = UpdateBuilder.of(""""production"."illustration"""", IllustrationFields.structure, IllustrationRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: IllustrationRow)(using c: Connection): Boolean = {
    val illustrationid: IllustrationId = row.illustrationid
    return sql"""update "production"."illustration"
    set "diagram" = ${Fragment.encode(PgTypes.xml.nullable, row.diagram)}::xml,
    "modifieddate" = ${Fragment.encode(PgTypes.timestamp, row.modifieddate)}::timestamp
    where "illustrationid" = ${Fragment.encode(IllustrationId.pgType, illustrationid)}""".update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: IllustrationRow)(using c: Connection): IllustrationRow = {
  sql"""insert into "production"."illustration"("illustrationid", "diagram", "modifieddate")
    values (${Fragment.encode(IllustrationId.pgType, unsaved.illustrationid)}::int4, ${Fragment.encode(PgTypes.xml.nullable, unsaved.diagram)}::xml, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    on conflict ("illustrationid")
    do update set
      "diagram" = EXCLUDED."diagram",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "illustrationid", "diagram", "modifieddate""""
    .updateReturning(IllustrationRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: Iterator[IllustrationRow])(using c: Connection): List[IllustrationRow] = {
    sql"""insert into "production"."illustration"("illustrationid", "diagram", "modifieddate")
    values (?::int4, ?::xml, ?::timestamp)
    on conflict ("illustrationid")
    do update set
      "diagram" = EXCLUDED."diagram",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "illustrationid", "diagram", "modifieddate""""
      .updateManyReturning(IllustrationRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[IllustrationRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    sql"""create temporary table illustration_TEMP (like "production"."illustration") on commit drop""".update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy illustration_TEMP("illustrationid", "diagram", "modifieddate") from stdin""", batchSize, unsaved.toJavaIterator, c, IllustrationRow.pgText): @scala.annotation.nowarn
    return sql"""insert into "production"."illustration"("illustrationid", "diagram", "modifieddate")
    select * from illustration_TEMP
    on conflict ("illustrationid")
    do update set
      "diagram" = EXCLUDED."diagram",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table illustration_TEMP;""".update().runUnchecked(c)
  }
}