/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.illustration

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class IllustrationRepoMock(
  toRow: IllustrationRowUnsaved => IllustrationRow,
  map: scala.collection.mutable.Map[IllustrationId, IllustrationRow] = scala.collection.mutable.Map.empty[IllustrationId, IllustrationRow]
) extends IllustrationRepo {
  override def delete: DeleteBuilder[IllustrationFields, IllustrationRow] = DeleteBuilderMock(IllustrationFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.illustrationid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(illustrationid: IllustrationId)(using c: Connection): Boolean = map.remove(illustrationid).isDefined

  override def deleteByIds(illustrationids: Array[IllustrationId])(using c: Connection): Int = {
    var count = 0
    illustrationids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: IllustrationRow)(using c: Connection): IllustrationRow = {
    if (map.contains(unsaved.illustrationid)) {
      throw new RuntimeException(s"id ${unsaved.illustrationid} already exists")
    }
    map.put(unsaved.illustrationid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: IllustrationRowUnsaved)(using c: Connection): IllustrationRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[IllustrationRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.illustrationid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[IllustrationRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.illustrationid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[IllustrationFields, IllustrationRow] = SelectBuilderMock(IllustrationFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[IllustrationRow] = map.values.toList

  override def selectById(illustrationid: IllustrationId)(using c: Connection): Option[IllustrationRow] = map.get(illustrationid)

  override def selectByIds(illustrationids: Array[IllustrationId])(using c: Connection): List[IllustrationRow] = illustrationids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(illustrationids: Array[IllustrationId])(using c: Connection): Map[IllustrationId, IllustrationRow] = selectByIds(illustrationids)(using c).map(x => (((row: IllustrationRow) => row.illustrationid).apply(x), x)).toMap

  override def update: UpdateBuilder[IllustrationFields, IllustrationRow] = UpdateBuilderMock(IllustrationFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: IllustrationRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.illustrationid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.illustrationid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: IllustrationRow)(using c: Connection): IllustrationRow = {
    map.put(unsaved.illustrationid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[IllustrationRow])(using c: Connection): List[IllustrationRow] = {
    unsaved.map { row =>
      map.put(row.illustrationid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[IllustrationRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.illustrationid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}