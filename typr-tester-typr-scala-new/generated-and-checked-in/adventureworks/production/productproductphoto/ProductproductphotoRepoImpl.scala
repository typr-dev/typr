/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productproductphoto

import adventureworks.production.product.ProductId
import adventureworks.production.productphoto.ProductphotoId
import adventureworks.public.Flag
import java.sql.Connection
import scala.collection.mutable.ListBuffer
import typr.runtime.PgTypes
import typr.runtime.streamingInsert
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.Dialect
import typr.scaladsl.Fragment
import typr.scaladsl.ScalaIteratorOps
import typr.scaladsl.SelectBuilder
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.Fragment.sql

class ProductproductphotoRepoImpl extends ProductproductphotoRepo {
  override def delete: DeleteBuilder[ProductproductphotoFields, ProductproductphotoRow] = DeleteBuilder.of(""""production"."productproductphoto"""", ProductproductphotoFields.structure, Dialect.POSTGRESQL)

  override def deleteById(compositeId: ProductproductphotoId)(using c: Connection): Boolean = sql"""delete from "production"."productproductphoto" where "productid" = ${Fragment.encode(ProductId.pgType, compositeId.productid)} AND "productphotoid" = ${Fragment.encode(ProductphotoId.pgType, compositeId.productphotoid)}""".update().runUnchecked(c) > 0

  override def deleteByIds(compositeIds: Array[ProductproductphotoId])(using c: Connection): Int = {
    val productid: Array[ProductId] = compositeIds.map(_.productid)
    val productphotoid: Array[ProductphotoId] = compositeIds.map(_.productphotoid)
    return sql"""delete
    from "production"."productproductphoto"
    where ("productid", "productphotoid")
    in (select * from unnest(${Fragment.encode(ProductId.pgTypeArray, productid)}, ${Fragment.encode(ProductphotoId.pgTypeArray, productphotoid)}))
    """.update().runUnchecked(c)
  }

  override def insert(unsaved: ProductproductphotoRow)(using c: Connection): ProductproductphotoRow = {
  sql"""insert into "production"."productproductphoto"("productid", "productphotoid", "primary", "modifieddate")
    values (${Fragment.encode(ProductId.pgType, unsaved.productid)}::int4, ${Fragment.encode(ProductphotoId.pgType, unsaved.productphotoid)}::int4, ${Fragment.encode(Flag.pgType, unsaved.primary)}::bool, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    RETURNING "productid", "productphotoid", "primary", "modifieddate"
    """
    .updateReturning(ProductproductphotoRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: ProductproductphotoRowUnsaved)(using c: Connection): ProductproductphotoRow = {
    val columns: ListBuffer[Fragment] = ListBuffer()
    val values: ListBuffer[Fragment] = ListBuffer()
    columns.addOne(Fragment.lit(""""productid"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(ProductId.pgType, unsaved.productid)}::int4"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""productphotoid"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(ProductphotoId.pgType, unsaved.productphotoid)}::int4"): @scala.annotation.nowarn
    unsaved.primary.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""primary"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(Flag.pgType, value)}::bool"): @scala.annotation.nowarn }
    );
    unsaved.modifieddate.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.timestamp, value)}::timestamp"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      sql"""insert into "production"."productproductphoto"(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      RETURNING "productid", "productphotoid", "primary", "modifieddate"
      """
    }
    return q.updateReturning(ProductproductphotoRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: Iterator[ProductproductphotoRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "production"."productproductphoto"("productid", "productphotoid", "primary", "modifieddate") FROM STDIN""", batchSize, unsaved.toJavaIterator, c, ProductproductphotoRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[ProductproductphotoRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "production"."productproductphoto"("productid", "productphotoid", "primary", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved.toJavaIterator, c, ProductproductphotoRowUnsaved.pgText)

  override def select: SelectBuilder[ProductproductphotoFields, ProductproductphotoRow] = SelectBuilder.of(""""production"."productproductphoto"""", ProductproductphotoFields.structure, ProductproductphotoRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): List[ProductproductphotoRow] = {
    sql"""select "productid", "productphotoid", "primary", "modifieddate"
    from "production"."productproductphoto"
    """.query(ProductproductphotoRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(compositeId: ProductproductphotoId)(using c: Connection): Option[ProductproductphotoRow] = {
    sql"""select "productid", "productphotoid", "primary", "modifieddate"
    from "production"."productproductphoto"
    where "productid" = ${Fragment.encode(ProductId.pgType, compositeId.productid)} AND "productphotoid" = ${Fragment.encode(ProductphotoId.pgType, compositeId.productphotoid)}""".query(ProductproductphotoRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(compositeIds: Array[ProductproductphotoId])(using c: Connection): List[ProductproductphotoRow] = {
    val productid: Array[ProductId] = compositeIds.map(_.productid)
    val productphotoid: Array[ProductphotoId] = compositeIds.map(_.productphotoid)
    return sql"""select "productid", "productphotoid", "primary", "modifieddate"
    from "production"."productproductphoto"
    where ("productid", "productphotoid")
    in (select * from unnest(${Fragment.encode(ProductId.pgTypeArray, productid)}, ${Fragment.encode(ProductphotoId.pgTypeArray, productphotoid)}))
    """.query(ProductproductphotoRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(compositeIds: Array[ProductproductphotoId])(using c: Connection): Map[ProductproductphotoId, ProductproductphotoRow] = {
    val ret: scala.collection.mutable.Map[ProductproductphotoId, ProductproductphotoRow] = scala.collection.mutable.Map.empty[ProductproductphotoId, ProductproductphotoRow]
    selectByIds(compositeIds)(using c).foreach(row => ret.put(row.compositeId, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[ProductproductphotoFields, ProductproductphotoRow] = UpdateBuilder.of(""""production"."productproductphoto"""", ProductproductphotoFields.structure, ProductproductphotoRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: ProductproductphotoRow)(using c: Connection): Boolean = {
    val compositeId: ProductproductphotoId = row.compositeId
    return sql"""update "production"."productproductphoto"
    set "primary" = ${Fragment.encode(Flag.pgType, row.primary)}::bool,
    "modifieddate" = ${Fragment.encode(PgTypes.timestamp, row.modifieddate)}::timestamp
    where "productid" = ${Fragment.encode(ProductId.pgType, compositeId.productid)} AND "productphotoid" = ${Fragment.encode(ProductphotoId.pgType, compositeId.productphotoid)}""".update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: ProductproductphotoRow)(using c: Connection): ProductproductphotoRow = {
  sql"""insert into "production"."productproductphoto"("productid", "productphotoid", "primary", "modifieddate")
    values (${Fragment.encode(ProductId.pgType, unsaved.productid)}::int4, ${Fragment.encode(ProductphotoId.pgType, unsaved.productphotoid)}::int4, ${Fragment.encode(Flag.pgType, unsaved.primary)}::bool, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    on conflict ("productid", "productphotoid")
    do update set
      "primary" = EXCLUDED."primary",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "productid", "productphotoid", "primary", "modifieddate""""
    .updateReturning(ProductproductphotoRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: Iterator[ProductproductphotoRow])(using c: Connection): List[ProductproductphotoRow] = {
    sql"""insert into "production"."productproductphoto"("productid", "productphotoid", "primary", "modifieddate")
    values (?::int4, ?::int4, ?::bool, ?::timestamp)
    on conflict ("productid", "productphotoid")
    do update set
      "primary" = EXCLUDED."primary",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "productid", "productphotoid", "primary", "modifieddate""""
      .updateManyReturning(ProductproductphotoRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[ProductproductphotoRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    sql"""create temporary table productproductphoto_TEMP (like "production"."productproductphoto") on commit drop""".update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy productproductphoto_TEMP("productid", "productphotoid", "primary", "modifieddate") from stdin""", batchSize, unsaved.toJavaIterator, c, ProductproductphotoRow.pgText): @scala.annotation.nowarn
    return sql"""insert into "production"."productproductphoto"("productid", "productphotoid", "primary", "modifieddate")
    select * from productproductphoto_TEMP
    on conflict ("productid", "productphotoid")
    do update set
      "primary" = EXCLUDED."primary",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table productproductphoto_TEMP;""".update().runUnchecked(c)
  }
}