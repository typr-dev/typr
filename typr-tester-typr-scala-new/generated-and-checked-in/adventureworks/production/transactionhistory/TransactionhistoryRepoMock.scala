/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.transactionhistory

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class TransactionhistoryRepoMock(
  toRow: TransactionhistoryRowUnsaved => TransactionhistoryRow,
  map: scala.collection.mutable.Map[TransactionhistoryId, TransactionhistoryRow] = scala.collection.mutable.Map.empty[TransactionhistoryId, TransactionhistoryRow]
) extends TransactionhistoryRepo {
  override def delete: DeleteBuilder[TransactionhistoryFields, TransactionhistoryRow] = DeleteBuilderMock(TransactionhistoryFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.transactionid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(transactionid: TransactionhistoryId)(using c: Connection): Boolean = map.remove(transactionid).isDefined

  override def deleteByIds(transactionids: Array[TransactionhistoryId])(using c: Connection): Int = {
    var count = 0
    transactionids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: TransactionhistoryRow)(using c: Connection): TransactionhistoryRow = {
    if (map.contains(unsaved.transactionid)) {
      throw new RuntimeException(s"id ${unsaved.transactionid} already exists")
    }
    map.put(unsaved.transactionid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: TransactionhistoryRowUnsaved)(using c: Connection): TransactionhistoryRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[TransactionhistoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.transactionid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[TransactionhistoryRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.transactionid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[TransactionhistoryFields, TransactionhistoryRow] = SelectBuilderMock(TransactionhistoryFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[TransactionhistoryRow] = map.values.toList

  override def selectById(transactionid: TransactionhistoryId)(using c: Connection): Option[TransactionhistoryRow] = map.get(transactionid)

  override def selectByIds(transactionids: Array[TransactionhistoryId])(using c: Connection): List[TransactionhistoryRow] = transactionids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(transactionids: Array[TransactionhistoryId])(using c: Connection): Map[TransactionhistoryId, TransactionhistoryRow] = selectByIds(transactionids)(using c).map(x => (((row: TransactionhistoryRow) => row.transactionid).apply(x), x)).toMap

  override def update: UpdateBuilder[TransactionhistoryFields, TransactionhistoryRow] = UpdateBuilderMock(TransactionhistoryFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: TransactionhistoryRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.transactionid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.transactionid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: TransactionhistoryRow)(using c: Connection): TransactionhistoryRow = {
    map.put(unsaved.transactionid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[TransactionhistoryRow])(using c: Connection): List[TransactionhistoryRow] = {
    unsaved.map { row =>
      map.put(row.transactionid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[TransactionhistoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.transactionid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}