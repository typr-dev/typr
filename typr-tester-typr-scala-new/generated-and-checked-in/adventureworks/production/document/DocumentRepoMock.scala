/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.document

import java.lang.RuntimeException
import java.sql.Connection
import java.util.UUID
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class DocumentRepoMock(
  toRow: DocumentRowUnsaved => DocumentRow,
  map: scala.collection.mutable.Map[DocumentId, DocumentRow] = scala.collection.mutable.Map.empty[DocumentId, DocumentRow]
) extends DocumentRepo {
  override def delete: DeleteBuilder[DocumentFields, DocumentRow] = DeleteBuilderMock(DocumentFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.documentnode, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(documentnode: DocumentId)(using c: Connection): Boolean = map.remove(documentnode).isDefined

  override def deleteByIds(documentnodes: Array[DocumentId])(using c: Connection): Int = {
    var count = 0
    documentnodes.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: DocumentRow)(using c: Connection): DocumentRow = {
    if (map.contains(unsaved.documentnode)) {
      throw new RuntimeException(s"id ${unsaved.documentnode} already exists")
    }
    map.put(unsaved.documentnode, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: DocumentRowUnsaved)(using c: Connection): DocumentRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[DocumentRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.documentnode, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[DocumentRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.documentnode, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[DocumentFields, DocumentRow] = SelectBuilderMock(DocumentFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[DocumentRow] = map.values.toList

  override def selectById(documentnode: DocumentId)(using c: Connection): Option[DocumentRow] = map.get(documentnode)

  override def selectByIds(documentnodes: Array[DocumentId])(using c: Connection): List[DocumentRow] = documentnodes.flatMap(map.get(_)).toList

  override def selectByIdsTracked(documentnodes: Array[DocumentId])(using c: Connection): Map[DocumentId, DocumentRow] = selectByIds(documentnodes)(using c).map(x => (((row: DocumentRow) => row.documentnode).apply(x), x)).toMap

  override def selectByUniqueRowguid(rowguid: UUID)(using c: Connection): Option[DocumentRow] = map.values.toList.find(v => (rowguid == v.rowguid))

  override def update: UpdateBuilder[DocumentFields, DocumentRow] = UpdateBuilderMock(DocumentFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: DocumentRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.documentnode).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.documentnode, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: DocumentRow)(using c: Connection): DocumentRow = {
    map.put(unsaved.documentnode, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[DocumentRow])(using c: Connection): List[DocumentRow] = {
    unsaved.map { row =>
      map.put(row.documentnode, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[DocumentRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.documentnode, row): @scala.annotation.nowarn
      1
    }.sum
  }
}