/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.scrapreason

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class ScrapreasonRepoMock(
  toRow: ScrapreasonRowUnsaved => ScrapreasonRow,
  map: scala.collection.mutable.Map[ScrapreasonId, ScrapreasonRow] = scala.collection.mutable.Map.empty[ScrapreasonId, ScrapreasonRow]
) extends ScrapreasonRepo {
  override def delete: DeleteBuilder[ScrapreasonFields, ScrapreasonRow] = DeleteBuilderMock(ScrapreasonFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.scrapreasonid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(scrapreasonid: ScrapreasonId)(using c: Connection): Boolean = map.remove(scrapreasonid).isDefined

  override def deleteByIds(scrapreasonids: Array[ScrapreasonId])(using c: Connection): Int = {
    var count = 0
    scrapreasonids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: ScrapreasonRow)(using c: Connection): ScrapreasonRow = {
    if (map.contains(unsaved.scrapreasonid)) {
      throw new RuntimeException(s"id ${unsaved.scrapreasonid} already exists")
    }
    map.put(unsaved.scrapreasonid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: ScrapreasonRowUnsaved)(using c: Connection): ScrapreasonRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[ScrapreasonRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.scrapreasonid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[ScrapreasonRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.scrapreasonid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[ScrapreasonFields, ScrapreasonRow] = SelectBuilderMock(ScrapreasonFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[ScrapreasonRow] = map.values.toList

  override def selectById(scrapreasonid: ScrapreasonId)(using c: Connection): Option[ScrapreasonRow] = map.get(scrapreasonid)

  override def selectByIds(scrapreasonids: Array[ScrapreasonId])(using c: Connection): List[ScrapreasonRow] = scrapreasonids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(scrapreasonids: Array[ScrapreasonId])(using c: Connection): Map[ScrapreasonId, ScrapreasonRow] = selectByIds(scrapreasonids)(using c).map(x => (((row: ScrapreasonRow) => row.scrapreasonid).apply(x), x)).toMap

  override def update: UpdateBuilder[ScrapreasonFields, ScrapreasonRow] = UpdateBuilderMock(ScrapreasonFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: ScrapreasonRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.scrapreasonid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.scrapreasonid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: ScrapreasonRow)(using c: Connection): ScrapreasonRow = {
    map.put(unsaved.scrapreasonid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[ScrapreasonRow])(using c: Connection): List[ScrapreasonRow] = {
    unsaved.map { row =>
      map.put(row.scrapreasonid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[ScrapreasonRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.scrapreasonid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}