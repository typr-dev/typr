/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.billofmaterials

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class BillofmaterialsRepoMock(
  toRow: BillofmaterialsRowUnsaved => BillofmaterialsRow,
  map: scala.collection.mutable.Map[Int, BillofmaterialsRow] = scala.collection.mutable.Map.empty[Int, BillofmaterialsRow]
) extends BillofmaterialsRepo {
  override def delete: DeleteBuilder[BillofmaterialsFields, BillofmaterialsRow] = DeleteBuilderMock(BillofmaterialsFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.billofmaterialsid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(billofmaterialsid: Int)(using c: Connection): Boolean = map.remove(billofmaterialsid).isDefined

  override def deleteByIds(billofmaterialsids: Array[Int])(using c: Connection): Int = {
    var count = 0
    billofmaterialsids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: BillofmaterialsRow)(using c: Connection): BillofmaterialsRow = {
    if (map.contains(unsaved.billofmaterialsid)) {
      throw new RuntimeException(s"id ${unsaved.billofmaterialsid} already exists")
    }
    map.put(unsaved.billofmaterialsid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: BillofmaterialsRowUnsaved)(using c: Connection): BillofmaterialsRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[BillofmaterialsRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.billofmaterialsid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[BillofmaterialsRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.billofmaterialsid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[BillofmaterialsFields, BillofmaterialsRow] = SelectBuilderMock(BillofmaterialsFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[BillofmaterialsRow] = map.values.toList

  override def selectById(billofmaterialsid: Int)(using c: Connection): Option[BillofmaterialsRow] = map.get(billofmaterialsid)

  override def selectByIds(billofmaterialsids: Array[Int])(using c: Connection): List[BillofmaterialsRow] = billofmaterialsids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(billofmaterialsids: Array[Int])(using c: Connection): Map[Int, BillofmaterialsRow] = selectByIds(billofmaterialsids)(using c).map(x => (((row: BillofmaterialsRow) => row.billofmaterialsid).apply(x), x)).toMap

  override def update: UpdateBuilder[BillofmaterialsFields, BillofmaterialsRow] = UpdateBuilderMock(BillofmaterialsFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: BillofmaterialsRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.billofmaterialsid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.billofmaterialsid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: BillofmaterialsRow)(using c: Connection): BillofmaterialsRow = {
    map.put(unsaved.billofmaterialsid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[BillofmaterialsRow])(using c: Connection): List[BillofmaterialsRow] = {
    unsaved.map { row =>
      map.put(row.billofmaterialsid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[BillofmaterialsRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.billofmaterialsid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}