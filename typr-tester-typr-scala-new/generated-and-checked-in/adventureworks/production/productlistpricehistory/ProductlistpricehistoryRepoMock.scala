/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productlistpricehistory

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class ProductlistpricehistoryRepoMock(
  toRow: ProductlistpricehistoryRowUnsaved => ProductlistpricehistoryRow,
  map: scala.collection.mutable.Map[ProductlistpricehistoryId, ProductlistpricehistoryRow] = scala.collection.mutable.Map.empty[ProductlistpricehistoryId, ProductlistpricehistoryRow]
) extends ProductlistpricehistoryRepo {
  override def delete: DeleteBuilder[ProductlistpricehistoryFields, ProductlistpricehistoryRow] = DeleteBuilderMock(ProductlistpricehistoryFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.compositeId, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(compositeId: ProductlistpricehistoryId)(using c: Connection): Boolean = map.remove(compositeId).isDefined

  override def deleteByIds(compositeIds: Array[ProductlistpricehistoryId])(using c: Connection): Int = {
    var count = 0
    compositeIds.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: ProductlistpricehistoryRow)(using c: Connection): ProductlistpricehistoryRow = {
    if (map.contains(unsaved.compositeId)) {
      throw new RuntimeException(s"id ${unsaved.compositeId} already exists")
    }
    map.put(unsaved.compositeId, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: ProductlistpricehistoryRowUnsaved)(using c: Connection): ProductlistpricehistoryRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[ProductlistpricehistoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.compositeId, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[ProductlistpricehistoryRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.compositeId, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[ProductlistpricehistoryFields, ProductlistpricehistoryRow] = SelectBuilderMock(ProductlistpricehistoryFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[ProductlistpricehistoryRow] = map.values.toList

  override def selectById(compositeId: ProductlistpricehistoryId)(using c: Connection): Option[ProductlistpricehistoryRow] = map.get(compositeId)

  override def selectByIds(compositeIds: Array[ProductlistpricehistoryId])(using c: Connection): List[ProductlistpricehistoryRow] = compositeIds.flatMap(map.get(_)).toList

  override def selectByIdsTracked(compositeIds: Array[ProductlistpricehistoryId])(using c: Connection): Map[ProductlistpricehistoryId, ProductlistpricehistoryRow] = selectByIds(compositeIds)(using c).map(x => (((row: ProductlistpricehistoryRow) => row.compositeId).apply(x), x)).toMap

  override def update: UpdateBuilder[ProductlistpricehistoryFields, ProductlistpricehistoryRow] = UpdateBuilderMock(ProductlistpricehistoryFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: ProductlistpricehistoryRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.compositeId).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.compositeId, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: ProductlistpricehistoryRow)(using c: Connection): ProductlistpricehistoryRow = {
    map.put(unsaved.compositeId, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[ProductlistpricehistoryRow])(using c: Connection): List[ProductlistpricehistoryRow] = {
    unsaved.map { row =>
      map.put(row.compositeId, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[ProductlistpricehistoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.compositeId, row): @scala.annotation.nowarn
      1
    }.sum
  }
}