/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productlistpricehistory

import adventureworks.production.product.ProductId
import java.sql.Connection
import java.time.LocalDateTime
import scala.collection.mutable.ListBuffer
import typr.runtime.PgTypes
import typr.runtime.streamingInsert
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.Dialect
import typr.scaladsl.Fragment
import typr.scaladsl.PgTypeOps
import typr.scaladsl.ScalaDbTypes
import typr.scaladsl.ScalaIteratorOps
import typr.scaladsl.SelectBuilder
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.Fragment.sql

class ProductlistpricehistoryRepoImpl extends ProductlistpricehistoryRepo {
  override def delete: DeleteBuilder[ProductlistpricehistoryFields, ProductlistpricehistoryRow] = DeleteBuilder.of(""""production"."productlistpricehistory"""", ProductlistpricehistoryFields.structure, Dialect.POSTGRESQL)

  override def deleteById(compositeId: ProductlistpricehistoryId)(using c: Connection): Boolean = sql"""delete from "production"."productlistpricehistory" where "productid" = ${Fragment.encode(ProductId.pgType, compositeId.productid)} AND "startdate" = ${Fragment.encode(PgTypes.timestamp, compositeId.startdate)}""".update().runUnchecked(c) > 0

  override def deleteByIds(compositeIds: Array[ProductlistpricehistoryId])(using c: Connection): Int = {
    val productid: Array[ProductId] = compositeIds.map(_.productid)
    val startdate: Array[LocalDateTime] = compositeIds.map(_.startdate)
    return sql"""delete
    from "production"."productlistpricehistory"
    where ("productid", "startdate")
    in (select * from unnest(${Fragment.encode(ProductId.pgTypeArray, productid)}, ${Fragment.encode(PgTypes.timestampArray, startdate)}))
    """.update().runUnchecked(c)
  }

  override def insert(unsaved: ProductlistpricehistoryRow)(using c: Connection): ProductlistpricehistoryRow = {
  sql"""insert into "production"."productlistpricehistory"("productid", "startdate", "enddate", "listprice", "modifieddate")
    values (${Fragment.encode(ProductId.pgType, unsaved.productid)}::int4, ${Fragment.encode(PgTypes.timestamp, unsaved.startdate)}::timestamp, ${Fragment.encode(PgTypes.timestamp.nullable, unsaved.enddate)}::timestamp, ${Fragment.encode(ScalaDbTypes.PgTypes.numeric, unsaved.listprice)}::numeric, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    RETURNING "productid", "startdate", "enddate", "listprice", "modifieddate"
    """
    .updateReturning(ProductlistpricehistoryRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: ProductlistpricehistoryRowUnsaved)(using c: Connection): ProductlistpricehistoryRow = {
    val columns: ListBuffer[Fragment] = ListBuffer()
    val values: ListBuffer[Fragment] = ListBuffer()
    columns.addOne(Fragment.lit(""""productid"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(ProductId.pgType, unsaved.productid)}::int4"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""startdate"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(PgTypes.timestamp, unsaved.startdate)}::timestamp"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""enddate"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(PgTypes.timestamp.nullable, unsaved.enddate)}::timestamp"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""listprice"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(ScalaDbTypes.PgTypes.numeric, unsaved.listprice)}::numeric"): @scala.annotation.nowarn
    unsaved.modifieddate.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.timestamp, value)}::timestamp"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      sql"""insert into "production"."productlistpricehistory"(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      RETURNING "productid", "startdate", "enddate", "listprice", "modifieddate"
      """
    }
    return q.updateReturning(ProductlistpricehistoryRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: Iterator[ProductlistpricehistoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "production"."productlistpricehistory"("productid", "startdate", "enddate", "listprice", "modifieddate") FROM STDIN""", batchSize, unsaved.toJavaIterator, c, ProductlistpricehistoryRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[ProductlistpricehistoryRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "production"."productlistpricehistory"("productid", "startdate", "enddate", "listprice", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved.toJavaIterator, c, ProductlistpricehistoryRowUnsaved.pgText)

  override def select: SelectBuilder[ProductlistpricehistoryFields, ProductlistpricehistoryRow] = SelectBuilder.of(""""production"."productlistpricehistory"""", ProductlistpricehistoryFields.structure, ProductlistpricehistoryRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): List[ProductlistpricehistoryRow] = {
    sql"""select "productid", "startdate", "enddate", "listprice", "modifieddate"
    from "production"."productlistpricehistory"
    """.query(ProductlistpricehistoryRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(compositeId: ProductlistpricehistoryId)(using c: Connection): Option[ProductlistpricehistoryRow] = {
    sql"""select "productid", "startdate", "enddate", "listprice", "modifieddate"
    from "production"."productlistpricehistory"
    where "productid" = ${Fragment.encode(ProductId.pgType, compositeId.productid)} AND "startdate" = ${Fragment.encode(PgTypes.timestamp, compositeId.startdate)}""".query(ProductlistpricehistoryRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(compositeIds: Array[ProductlistpricehistoryId])(using c: Connection): List[ProductlistpricehistoryRow] = {
    val productid: Array[ProductId] = compositeIds.map(_.productid)
    val startdate: Array[LocalDateTime] = compositeIds.map(_.startdate)
    return sql"""select "productid", "startdate", "enddate", "listprice", "modifieddate"
    from "production"."productlistpricehistory"
    where ("productid", "startdate")
    in (select * from unnest(${Fragment.encode(ProductId.pgTypeArray, productid)}, ${Fragment.encode(PgTypes.timestampArray, startdate)}))
    """.query(ProductlistpricehistoryRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(compositeIds: Array[ProductlistpricehistoryId])(using c: Connection): Map[ProductlistpricehistoryId, ProductlistpricehistoryRow] = {
    val ret: scala.collection.mutable.Map[ProductlistpricehistoryId, ProductlistpricehistoryRow] = scala.collection.mutable.Map.empty[ProductlistpricehistoryId, ProductlistpricehistoryRow]
    selectByIds(compositeIds)(using c).foreach(row => ret.put(row.compositeId, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[ProductlistpricehistoryFields, ProductlistpricehistoryRow] = UpdateBuilder.of(""""production"."productlistpricehistory"""", ProductlistpricehistoryFields.structure, ProductlistpricehistoryRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: ProductlistpricehistoryRow)(using c: Connection): Boolean = {
    val compositeId: ProductlistpricehistoryId = row.compositeId
    return sql"""update "production"."productlistpricehistory"
    set "enddate" = ${Fragment.encode(PgTypes.timestamp.nullable, row.enddate)}::timestamp,
    "listprice" = ${Fragment.encode(ScalaDbTypes.PgTypes.numeric, row.listprice)}::numeric,
    "modifieddate" = ${Fragment.encode(PgTypes.timestamp, row.modifieddate)}::timestamp
    where "productid" = ${Fragment.encode(ProductId.pgType, compositeId.productid)} AND "startdate" = ${Fragment.encode(PgTypes.timestamp, compositeId.startdate)}""".update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: ProductlistpricehistoryRow)(using c: Connection): ProductlistpricehistoryRow = {
  sql"""insert into "production"."productlistpricehistory"("productid", "startdate", "enddate", "listprice", "modifieddate")
    values (${Fragment.encode(ProductId.pgType, unsaved.productid)}::int4, ${Fragment.encode(PgTypes.timestamp, unsaved.startdate)}::timestamp, ${Fragment.encode(PgTypes.timestamp.nullable, unsaved.enddate)}::timestamp, ${Fragment.encode(ScalaDbTypes.PgTypes.numeric, unsaved.listprice)}::numeric, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    on conflict ("productid", "startdate")
    do update set
      "enddate" = EXCLUDED."enddate",
    "listprice" = EXCLUDED."listprice",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "productid", "startdate", "enddate", "listprice", "modifieddate""""
    .updateReturning(ProductlistpricehistoryRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: Iterator[ProductlistpricehistoryRow])(using c: Connection): List[ProductlistpricehistoryRow] = {
    sql"""insert into "production"."productlistpricehistory"("productid", "startdate", "enddate", "listprice", "modifieddate")
    values (?::int4, ?::timestamp, ?::timestamp, ?::numeric, ?::timestamp)
    on conflict ("productid", "startdate")
    do update set
      "enddate" = EXCLUDED."enddate",
    "listprice" = EXCLUDED."listprice",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "productid", "startdate", "enddate", "listprice", "modifieddate""""
      .updateManyReturning(ProductlistpricehistoryRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[ProductlistpricehistoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    sql"""create temporary table productlistpricehistory_TEMP (like "production"."productlistpricehistory") on commit drop""".update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy productlistpricehistory_TEMP("productid", "startdate", "enddate", "listprice", "modifieddate") from stdin""", batchSize, unsaved.toJavaIterator, c, ProductlistpricehistoryRow.pgText): @scala.annotation.nowarn
    return sql"""insert into "production"."productlistpricehistory"("productid", "startdate", "enddate", "listprice", "modifieddate")
    select * from productlistpricehistory_TEMP
    on conflict ("productid", "startdate")
    do update set
      "enddate" = EXCLUDED."enddate",
    "listprice" = EXCLUDED."listprice",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table productlistpricehistory_TEMP;""".update().runUnchecked(c)
  }
}