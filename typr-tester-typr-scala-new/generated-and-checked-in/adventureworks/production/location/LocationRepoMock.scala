/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.location

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class LocationRepoMock(
  toRow: LocationRowUnsaved => LocationRow,
  map: scala.collection.mutable.Map[LocationId, LocationRow] = scala.collection.mutable.Map.empty[LocationId, LocationRow]
) extends LocationRepo {
  override def delete: DeleteBuilder[LocationFields, LocationRow] = DeleteBuilderMock(LocationFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.locationid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(locationid: LocationId)(using c: Connection): Boolean = map.remove(locationid).isDefined

  override def deleteByIds(locationids: Array[LocationId])(using c: Connection): Int = {
    var count = 0
    locationids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: LocationRow)(using c: Connection): LocationRow = {
    if (map.contains(unsaved.locationid)) {
      throw new RuntimeException(s"id ${unsaved.locationid} already exists")
    }
    map.put(unsaved.locationid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: LocationRowUnsaved)(using c: Connection): LocationRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[LocationRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.locationid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[LocationRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.locationid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[LocationFields, LocationRow] = SelectBuilderMock(LocationFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[LocationRow] = map.values.toList

  override def selectById(locationid: LocationId)(using c: Connection): Option[LocationRow] = map.get(locationid)

  override def selectByIds(locationids: Array[LocationId])(using c: Connection): List[LocationRow] = locationids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(locationids: Array[LocationId])(using c: Connection): Map[LocationId, LocationRow] = selectByIds(locationids)(using c).map(x => (((row: LocationRow) => row.locationid).apply(x), x)).toMap

  override def update: UpdateBuilder[LocationFields, LocationRow] = UpdateBuilderMock(LocationFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: LocationRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.locationid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.locationid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: LocationRow)(using c: Connection): LocationRow = {
    map.put(unsaved.locationid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[LocationRow])(using c: Connection): List[LocationRow] = {
    unsaved.map { row =>
      map.put(row.locationid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[LocationRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.locationid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}