/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productreview

import adventureworks.production.product.ProductId
import adventureworks.public.Name
import java.sql.Connection
import scala.collection.mutable.ListBuffer
import typr.runtime.PgTypes
import typr.runtime.streamingInsert
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.Dialect
import typr.scaladsl.Fragment
import typr.scaladsl.PgTypeOps
import typr.scaladsl.ScalaDbTypes
import typr.scaladsl.ScalaIteratorOps
import typr.scaladsl.SelectBuilder
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.Fragment.sql

class ProductreviewRepoImpl extends ProductreviewRepo {
  override def delete: DeleteBuilder[ProductreviewFields, ProductreviewRow] = DeleteBuilder.of(""""production"."productreview"""", ProductreviewFields.structure, Dialect.POSTGRESQL)

  override def deleteById(productreviewid: ProductreviewId)(using c: Connection): Boolean = sql"""delete from "production"."productreview" where "productreviewid" = ${Fragment.encode(ProductreviewId.pgType, productreviewid)}""".update().runUnchecked(c) > 0

  override def deleteByIds(productreviewids: Array[ProductreviewId])(using c: Connection): Int = {
    sql"""delete
    from "production"."productreview"
    where "productreviewid" = ANY(${Fragment.encode(ProductreviewId.pgTypeArray, productreviewids)})"""
      .update()
      .runUnchecked(c)
  }

  override def insert(unsaved: ProductreviewRow)(using c: Connection): ProductreviewRow = {
  sql"""insert into "production"."productreview"("productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate")
    values (${Fragment.encode(ProductreviewId.pgType, unsaved.productreviewid)}::int4, ${Fragment.encode(ProductId.pgType, unsaved.productid)}::int4, ${Fragment.encode(Name.pgType, unsaved.reviewername)}::varchar, ${Fragment.encode(PgTypes.timestamp, unsaved.reviewdate)}::timestamp, ${Fragment.encode(PgTypes.text, unsaved.emailaddress)}, ${Fragment.encode(ScalaDbTypes.PgTypes.int4, unsaved.rating)}::int4, ${Fragment.encode(PgTypes.text.nullable, unsaved.comments)}, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    RETURNING "productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate"
    """
    .updateReturning(ProductreviewRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: ProductreviewRowUnsaved)(using c: Connection): ProductreviewRow = {
    val columns: ListBuffer[Fragment] = ListBuffer()
    val values: ListBuffer[Fragment] = ListBuffer()
    columns.addOne(Fragment.lit(""""productid"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(ProductId.pgType, unsaved.productid)}::int4"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""reviewername"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(Name.pgType, unsaved.reviewername)}::varchar"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""emailaddress"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(PgTypes.text, unsaved.emailaddress)}"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""rating"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(ScalaDbTypes.PgTypes.int4, unsaved.rating)}::int4"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""comments"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(PgTypes.text.nullable, unsaved.comments)}"): @scala.annotation.nowarn
    unsaved.productreviewid.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""productreviewid"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(ProductreviewId.pgType, value)}::int4"): @scala.annotation.nowarn }
    );
    unsaved.reviewdate.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""reviewdate"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.timestamp, value)}::timestamp"): @scala.annotation.nowarn }
    );
    unsaved.modifieddate.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.timestamp, value)}::timestamp"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      sql"""insert into "production"."productreview"(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      RETURNING "productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate"
      """
    }
    return q.updateReturning(ProductreviewRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: Iterator[ProductreviewRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "production"."productreview"("productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate") FROM STDIN""", batchSize, unsaved.toJavaIterator, c, ProductreviewRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[ProductreviewRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "production"."productreview"("productid", "reviewername", "emailaddress", "rating", "comments", "productreviewid", "reviewdate", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved.toJavaIterator, c, ProductreviewRowUnsaved.pgText)

  override def select: SelectBuilder[ProductreviewFields, ProductreviewRow] = SelectBuilder.of(""""production"."productreview"""", ProductreviewFields.structure, ProductreviewRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): List[ProductreviewRow] = {
    sql"""select "productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate"
    from "production"."productreview"
    """.query(ProductreviewRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(productreviewid: ProductreviewId)(using c: Connection): Option[ProductreviewRow] = {
    sql"""select "productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate"
    from "production"."productreview"
    where "productreviewid" = ${Fragment.encode(ProductreviewId.pgType, productreviewid)}""".query(ProductreviewRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(productreviewids: Array[ProductreviewId])(using c: Connection): List[ProductreviewRow] = {
    sql"""select "productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate"
    from "production"."productreview"
    where "productreviewid" = ANY(${Fragment.encode(ProductreviewId.pgTypeArray, productreviewids)})""".query(ProductreviewRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(productreviewids: Array[ProductreviewId])(using c: Connection): Map[ProductreviewId, ProductreviewRow] = {
    val ret: scala.collection.mutable.Map[ProductreviewId, ProductreviewRow] = scala.collection.mutable.Map.empty[ProductreviewId, ProductreviewRow]
    selectByIds(productreviewids)(using c).foreach(row => ret.put(row.productreviewid, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[ProductreviewFields, ProductreviewRow] = UpdateBuilder.of(""""production"."productreview"""", ProductreviewFields.structure, ProductreviewRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: ProductreviewRow)(using c: Connection): Boolean = {
    val productreviewid: ProductreviewId = row.productreviewid
    return sql"""update "production"."productreview"
    set "productid" = ${Fragment.encode(ProductId.pgType, row.productid)}::int4,
    "reviewername" = ${Fragment.encode(Name.pgType, row.reviewername)}::varchar,
    "reviewdate" = ${Fragment.encode(PgTypes.timestamp, row.reviewdate)}::timestamp,
    "emailaddress" = ${Fragment.encode(PgTypes.text, row.emailaddress)},
    "rating" = ${Fragment.encode(ScalaDbTypes.PgTypes.int4, row.rating)}::int4,
    "comments" = ${Fragment.encode(PgTypes.text.nullable, row.comments)},
    "modifieddate" = ${Fragment.encode(PgTypes.timestamp, row.modifieddate)}::timestamp
    where "productreviewid" = ${Fragment.encode(ProductreviewId.pgType, productreviewid)}""".update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: ProductreviewRow)(using c: Connection): ProductreviewRow = {
  sql"""insert into "production"."productreview"("productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate")
    values (${Fragment.encode(ProductreviewId.pgType, unsaved.productreviewid)}::int4, ${Fragment.encode(ProductId.pgType, unsaved.productid)}::int4, ${Fragment.encode(Name.pgType, unsaved.reviewername)}::varchar, ${Fragment.encode(PgTypes.timestamp, unsaved.reviewdate)}::timestamp, ${Fragment.encode(PgTypes.text, unsaved.emailaddress)}, ${Fragment.encode(ScalaDbTypes.PgTypes.int4, unsaved.rating)}::int4, ${Fragment.encode(PgTypes.text.nullable, unsaved.comments)}, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    on conflict ("productreviewid")
    do update set
      "productid" = EXCLUDED."productid",
    "reviewername" = EXCLUDED."reviewername",
    "reviewdate" = EXCLUDED."reviewdate",
    "emailaddress" = EXCLUDED."emailaddress",
    "rating" = EXCLUDED."rating",
    "comments" = EXCLUDED."comments",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate""""
    .updateReturning(ProductreviewRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: Iterator[ProductreviewRow])(using c: Connection): List[ProductreviewRow] = {
    sql"""insert into "production"."productreview"("productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate")
    values (?::int4, ?::int4, ?::varchar, ?::timestamp, ?, ?::int4, ?, ?::timestamp)
    on conflict ("productreviewid")
    do update set
      "productid" = EXCLUDED."productid",
    "reviewername" = EXCLUDED."reviewername",
    "reviewdate" = EXCLUDED."reviewdate",
    "emailaddress" = EXCLUDED."emailaddress",
    "rating" = EXCLUDED."rating",
    "comments" = EXCLUDED."comments",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate""""
      .updateManyReturning(ProductreviewRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[ProductreviewRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    sql"""create temporary table productreview_TEMP (like "production"."productreview") on commit drop""".update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy productreview_TEMP("productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate") from stdin""", batchSize, unsaved.toJavaIterator, c, ProductreviewRow.pgText): @scala.annotation.nowarn
    return sql"""insert into "production"."productreview"("productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate")
    select * from productreview_TEMP
    on conflict ("productreviewid")
    do update set
      "productid" = EXCLUDED."productid",
    "reviewername" = EXCLUDED."reviewername",
    "reviewdate" = EXCLUDED."reviewdate",
    "emailaddress" = EXCLUDED."emailaddress",
    "rating" = EXCLUDED."rating",
    "comments" = EXCLUDED."comments",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table productreview_TEMP;""".update().runUnchecked(c)
  }
}