/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productreview

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class ProductreviewRepoMock(
  toRow: ProductreviewRowUnsaved => ProductreviewRow,
  map: scala.collection.mutable.Map[ProductreviewId, ProductreviewRow] = scala.collection.mutable.Map.empty[ProductreviewId, ProductreviewRow]
) extends ProductreviewRepo {
  override def delete: DeleteBuilder[ProductreviewFields, ProductreviewRow] = DeleteBuilderMock(ProductreviewFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.productreviewid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(productreviewid: ProductreviewId)(using c: Connection): Boolean = map.remove(productreviewid).isDefined

  override def deleteByIds(productreviewids: Array[ProductreviewId])(using c: Connection): Int = {
    var count = 0
    productreviewids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: ProductreviewRow)(using c: Connection): ProductreviewRow = {
    if (map.contains(unsaved.productreviewid)) {
      throw new RuntimeException(s"id ${unsaved.productreviewid} already exists")
    }
    map.put(unsaved.productreviewid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: ProductreviewRowUnsaved)(using c: Connection): ProductreviewRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[ProductreviewRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.productreviewid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[ProductreviewRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.productreviewid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[ProductreviewFields, ProductreviewRow] = SelectBuilderMock(ProductreviewFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[ProductreviewRow] = map.values.toList

  override def selectById(productreviewid: ProductreviewId)(using c: Connection): Option[ProductreviewRow] = map.get(productreviewid)

  override def selectByIds(productreviewids: Array[ProductreviewId])(using c: Connection): List[ProductreviewRow] = productreviewids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(productreviewids: Array[ProductreviewId])(using c: Connection): Map[ProductreviewId, ProductreviewRow] = selectByIds(productreviewids)(using c).map(x => (((row: ProductreviewRow) => row.productreviewid).apply(x), x)).toMap

  override def update: UpdateBuilder[ProductreviewFields, ProductreviewRow] = UpdateBuilderMock(ProductreviewFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: ProductreviewRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.productreviewid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.productreviewid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: ProductreviewRow)(using c: Connection): ProductreviewRow = {
    map.put(unsaved.productreviewid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[ProductreviewRow])(using c: Connection): List[ProductreviewRow] = {
    unsaved.map { row =>
      map.put(row.productreviewid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[ProductreviewRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.productreviewid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}