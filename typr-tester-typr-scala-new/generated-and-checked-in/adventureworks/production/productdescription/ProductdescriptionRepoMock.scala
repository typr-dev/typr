/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productdescription

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class ProductdescriptionRepoMock(
  toRow: ProductdescriptionRowUnsaved => ProductdescriptionRow,
  map: scala.collection.mutable.Map[ProductdescriptionId, ProductdescriptionRow] = scala.collection.mutable.Map.empty[ProductdescriptionId, ProductdescriptionRow]
) extends ProductdescriptionRepo {
  override def delete: DeleteBuilder[ProductdescriptionFields, ProductdescriptionRow] = DeleteBuilderMock(ProductdescriptionFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.productdescriptionid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(productdescriptionid: ProductdescriptionId)(using c: Connection): Boolean = map.remove(productdescriptionid).isDefined

  override def deleteByIds(productdescriptionids: Array[ProductdescriptionId])(using c: Connection): Int = {
    var count = 0
    productdescriptionids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: ProductdescriptionRow)(using c: Connection): ProductdescriptionRow = {
    if (map.contains(unsaved.productdescriptionid)) {
      throw new RuntimeException(s"id ${unsaved.productdescriptionid} already exists")
    }
    map.put(unsaved.productdescriptionid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: ProductdescriptionRowUnsaved)(using c: Connection): ProductdescriptionRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[ProductdescriptionRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.productdescriptionid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[ProductdescriptionRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.productdescriptionid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[ProductdescriptionFields, ProductdescriptionRow] = SelectBuilderMock(ProductdescriptionFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[ProductdescriptionRow] = map.values.toList

  override def selectById(productdescriptionid: ProductdescriptionId)(using c: Connection): Option[ProductdescriptionRow] = map.get(productdescriptionid)

  override def selectByIds(productdescriptionids: Array[ProductdescriptionId])(using c: Connection): List[ProductdescriptionRow] = productdescriptionids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(productdescriptionids: Array[ProductdescriptionId])(using c: Connection): Map[ProductdescriptionId, ProductdescriptionRow] = selectByIds(productdescriptionids)(using c).map(x => (((row: ProductdescriptionRow) => row.productdescriptionid).apply(x), x)).toMap

  override def update: UpdateBuilder[ProductdescriptionFields, ProductdescriptionRow] = UpdateBuilderMock(ProductdescriptionFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: ProductdescriptionRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.productdescriptionid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.productdescriptionid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: ProductdescriptionRow)(using c: Connection): ProductdescriptionRow = {
    map.put(unsaved.productdescriptionid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[ProductdescriptionRow])(using c: Connection): List[ProductdescriptionRow] = {
    unsaved.map { row =>
      map.put(row.productdescriptionid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[ProductdescriptionRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.productdescriptionid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}