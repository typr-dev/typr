/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.address

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class AddressRepoMock(
  toRow: AddressRowUnsaved => AddressRow,
  map: scala.collection.mutable.Map[AddressId, AddressRow] = scala.collection.mutable.Map.empty[AddressId, AddressRow]
) extends AddressRepo {
  override def delete: DeleteBuilder[AddressFields, AddressRow] = DeleteBuilderMock(AddressFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.addressid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(addressid: AddressId)(using c: Connection): Boolean = map.remove(addressid).isDefined

  override def deleteByIds(addressids: Array[AddressId])(using c: Connection): Int = {
    var count = 0
    addressids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: AddressRow)(using c: Connection): AddressRow = {
    if (map.contains(unsaved.addressid)) {
      throw new RuntimeException(s"id ${unsaved.addressid} already exists")
    }
    map.put(unsaved.addressid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: AddressRowUnsaved)(using c: Connection): AddressRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[AddressRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.addressid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[AddressRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.addressid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[AddressFields, AddressRow] = SelectBuilderMock(AddressFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[AddressRow] = map.values.toList

  override def selectById(addressid: AddressId)(using c: Connection): Option[AddressRow] = map.get(addressid)

  override def selectByIds(addressids: Array[AddressId])(using c: Connection): List[AddressRow] = addressids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(addressids: Array[AddressId])(using c: Connection): Map[AddressId, AddressRow] = selectByIds(addressids)(using c).map(x => (((row: AddressRow) => row.addressid).apply(x), x)).toMap

  override def update: UpdateBuilder[AddressFields, AddressRow] = UpdateBuilderMock(AddressFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: AddressRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.addressid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.addressid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: AddressRow)(using c: Connection): AddressRow = {
    map.put(unsaved.addressid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[AddressRow])(using c: Connection): List[AddressRow] = {
    unsaved.map { row =>
      map.put(row.addressid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[AddressRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.addressid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}