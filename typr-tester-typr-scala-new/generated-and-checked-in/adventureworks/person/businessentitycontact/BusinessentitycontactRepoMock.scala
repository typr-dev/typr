/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.businessentitycontact

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class BusinessentitycontactRepoMock(
  toRow: BusinessentitycontactRowUnsaved => BusinessentitycontactRow,
  map: scala.collection.mutable.Map[BusinessentitycontactId, BusinessentitycontactRow] = scala.collection.mutable.Map.empty[BusinessentitycontactId, BusinessentitycontactRow]
) extends BusinessentitycontactRepo {
  override def delete: DeleteBuilder[BusinessentitycontactFields, BusinessentitycontactRow] = DeleteBuilderMock(BusinessentitycontactFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.compositeId, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(compositeId: BusinessentitycontactId)(using c: Connection): Boolean = map.remove(compositeId).isDefined

  override def deleteByIds(compositeIds: Array[BusinessentitycontactId])(using c: Connection): Int = {
    var count = 0
    compositeIds.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: BusinessentitycontactRow)(using c: Connection): BusinessentitycontactRow = {
    if (map.contains(unsaved.compositeId)) {
      throw new RuntimeException(s"id ${unsaved.compositeId} already exists")
    }
    map.put(unsaved.compositeId, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: BusinessentitycontactRowUnsaved)(using c: Connection): BusinessentitycontactRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[BusinessentitycontactRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.compositeId, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[BusinessentitycontactRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.compositeId, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[BusinessentitycontactFields, BusinessentitycontactRow] = SelectBuilderMock(BusinessentitycontactFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[BusinessentitycontactRow] = map.values.toList

  override def selectById(compositeId: BusinessentitycontactId)(using c: Connection): Option[BusinessentitycontactRow] = map.get(compositeId)

  override def selectByIds(compositeIds: Array[BusinessentitycontactId])(using c: Connection): List[BusinessentitycontactRow] = compositeIds.flatMap(map.get(_)).toList

  override def selectByIdsTracked(compositeIds: Array[BusinessentitycontactId])(using c: Connection): Map[BusinessentitycontactId, BusinessentitycontactRow] = selectByIds(compositeIds)(using c).map(x => (((row: BusinessentitycontactRow) => row.compositeId).apply(x), x)).toMap

  override def update: UpdateBuilder[BusinessentitycontactFields, BusinessentitycontactRow] = UpdateBuilderMock(BusinessentitycontactFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: BusinessentitycontactRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.compositeId).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.compositeId, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: BusinessentitycontactRow)(using c: Connection): BusinessentitycontactRow = {
    map.put(unsaved.compositeId, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[BusinessentitycontactRow])(using c: Connection): List[BusinessentitycontactRow] = {
    unsaved.map { row =>
      map.put(row.compositeId, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[BusinessentitycontactRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.compositeId, row): @scala.annotation.nowarn
      1
    }.sum
  }
}