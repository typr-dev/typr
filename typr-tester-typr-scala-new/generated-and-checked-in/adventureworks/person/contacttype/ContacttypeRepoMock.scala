/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.contacttype

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class ContacttypeRepoMock(
  toRow: ContacttypeRowUnsaved => ContacttypeRow,
  map: scala.collection.mutable.Map[ContacttypeId, ContacttypeRow] = scala.collection.mutable.Map.empty[ContacttypeId, ContacttypeRow]
) extends ContacttypeRepo {
  override def delete: DeleteBuilder[ContacttypeFields, ContacttypeRow] = DeleteBuilderMock(ContacttypeFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.contacttypeid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(contacttypeid: ContacttypeId)(using c: Connection): Boolean = map.remove(contacttypeid).isDefined

  override def deleteByIds(contacttypeids: Array[ContacttypeId])(using c: Connection): Int = {
    var count = 0
    contacttypeids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: ContacttypeRow)(using c: Connection): ContacttypeRow = {
    if (map.contains(unsaved.contacttypeid)) {
      throw new RuntimeException(s"id ${unsaved.contacttypeid} already exists")
    }
    map.put(unsaved.contacttypeid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: ContacttypeRowUnsaved)(using c: Connection): ContacttypeRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[ContacttypeRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.contacttypeid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[ContacttypeRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.contacttypeid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[ContacttypeFields, ContacttypeRow] = SelectBuilderMock(ContacttypeFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[ContacttypeRow] = map.values.toList

  override def selectById(contacttypeid: ContacttypeId)(using c: Connection): Option[ContacttypeRow] = map.get(contacttypeid)

  override def selectByIds(contacttypeids: Array[ContacttypeId])(using c: Connection): List[ContacttypeRow] = contacttypeids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(contacttypeids: Array[ContacttypeId])(using c: Connection): Map[ContacttypeId, ContacttypeRow] = selectByIds(contacttypeids)(using c).map(x => (((row: ContacttypeRow) => row.contacttypeid).apply(x), x)).toMap

  override def update: UpdateBuilder[ContacttypeFields, ContacttypeRow] = UpdateBuilderMock(ContacttypeFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: ContacttypeRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.contacttypeid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.contacttypeid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: ContacttypeRow)(using c: Connection): ContacttypeRow = {
    map.put(unsaved.contacttypeid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[ContacttypeRow])(using c: Connection): List[ContacttypeRow] = {
    unsaved.map { row =>
      map.put(row.contacttypeid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[ContacttypeRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.contacttypeid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}