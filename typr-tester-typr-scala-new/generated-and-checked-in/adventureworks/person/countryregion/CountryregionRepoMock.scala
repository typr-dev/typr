/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.countryregion

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class CountryregionRepoMock(
  toRow: CountryregionRowUnsaved => CountryregionRow,
  map: scala.collection.mutable.Map[CountryregionId, CountryregionRow] = scala.collection.mutable.Map.empty[CountryregionId, CountryregionRow]
) extends CountryregionRepo {
  override def delete: DeleteBuilder[CountryregionFields, CountryregionRow] = DeleteBuilderMock(CountryregionFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.countryregioncode, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(countryregioncode: CountryregionId)(using c: Connection): Boolean = map.remove(countryregioncode).isDefined

  override def deleteByIds(countryregioncodes: Array[CountryregionId])(using c: Connection): Int = {
    var count = 0
    countryregioncodes.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: CountryregionRow)(using c: Connection): CountryregionRow = {
    if (map.contains(unsaved.countryregioncode)) {
      throw new RuntimeException(s"id ${unsaved.countryregioncode} already exists")
    }
    map.put(unsaved.countryregioncode, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: CountryregionRowUnsaved)(using c: Connection): CountryregionRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[CountryregionRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.countryregioncode, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[CountryregionRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.countryregioncode, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[CountryregionFields, CountryregionRow] = SelectBuilderMock(CountryregionFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[CountryregionRow] = map.values.toList

  override def selectById(countryregioncode: CountryregionId)(using c: Connection): Option[CountryregionRow] = map.get(countryregioncode)

  override def selectByIds(countryregioncodes: Array[CountryregionId])(using c: Connection): List[CountryregionRow] = countryregioncodes.flatMap(map.get(_)).toList

  override def selectByIdsTracked(countryregioncodes: Array[CountryregionId])(using c: Connection): Map[CountryregionId, CountryregionRow] = selectByIds(countryregioncodes)(using c).map(x => (((row: CountryregionRow) => row.countryregioncode).apply(x), x)).toMap

  override def update: UpdateBuilder[CountryregionFields, CountryregionRow] = UpdateBuilderMock(CountryregionFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: CountryregionRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.countryregioncode).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.countryregioncode, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: CountryregionRow)(using c: Connection): CountryregionRow = {
    map.put(unsaved.countryregioncode, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[CountryregionRow])(using c: Connection): List[CountryregionRow] = {
    unsaved.map { row =>
      map.put(row.countryregioncode, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[CountryregionRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.countryregioncode, row): @scala.annotation.nowarn
      1
    }.sum
  }
}