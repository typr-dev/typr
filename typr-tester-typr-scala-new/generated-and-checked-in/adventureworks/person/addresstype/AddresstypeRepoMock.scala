/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.addresstype

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class AddresstypeRepoMock(
  toRow: AddresstypeRowUnsaved => AddresstypeRow,
  map: scala.collection.mutable.Map[AddresstypeId, AddresstypeRow] = scala.collection.mutable.Map.empty[AddresstypeId, AddresstypeRow]
) extends AddresstypeRepo {
  override def delete: DeleteBuilder[AddresstypeFields, AddresstypeRow] = DeleteBuilderMock(AddresstypeFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.addresstypeid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(addresstypeid: AddresstypeId)(using c: Connection): Boolean = map.remove(addresstypeid).isDefined

  override def deleteByIds(addresstypeids: Array[AddresstypeId])(using c: Connection): Int = {
    var count = 0
    addresstypeids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: AddresstypeRow)(using c: Connection): AddresstypeRow = {
    if (map.contains(unsaved.addresstypeid)) {
      throw new RuntimeException(s"id ${unsaved.addresstypeid} already exists")
    }
    map.put(unsaved.addresstypeid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: AddresstypeRowUnsaved)(using c: Connection): AddresstypeRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[AddresstypeRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.addresstypeid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[AddresstypeRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.addresstypeid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[AddresstypeFields, AddresstypeRow] = SelectBuilderMock(AddresstypeFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[AddresstypeRow] = map.values.toList

  override def selectById(addresstypeid: AddresstypeId)(using c: Connection): Option[AddresstypeRow] = map.get(addresstypeid)

  override def selectByIds(addresstypeids: Array[AddresstypeId])(using c: Connection): List[AddresstypeRow] = addresstypeids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(addresstypeids: Array[AddresstypeId])(using c: Connection): Map[AddresstypeId, AddresstypeRow] = selectByIds(addresstypeids)(using c).map(x => (((row: AddresstypeRow) => row.addresstypeid).apply(x), x)).toMap

  override def update: UpdateBuilder[AddresstypeFields, AddresstypeRow] = UpdateBuilderMock(AddresstypeFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: AddresstypeRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.addresstypeid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.addresstypeid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: AddresstypeRow)(using c: Connection): AddresstypeRow = {
    map.put(unsaved.addresstypeid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[AddresstypeRow])(using c: Connection): List[AddresstypeRow] = {
    unsaved.map { row =>
      map.put(row.addresstypeid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[AddresstypeRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.addresstypeid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}