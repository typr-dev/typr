/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.stateprovince

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class StateprovinceRepoMock(
  toRow: StateprovinceRowUnsaved => StateprovinceRow,
  map: scala.collection.mutable.Map[StateprovinceId, StateprovinceRow] = scala.collection.mutable.Map.empty[StateprovinceId, StateprovinceRow]
) extends StateprovinceRepo {
  override def delete: DeleteBuilder[StateprovinceFields, StateprovinceRow] = DeleteBuilderMock(StateprovinceFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.stateprovinceid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(stateprovinceid: StateprovinceId)(using c: Connection): Boolean = map.remove(stateprovinceid).isDefined

  override def deleteByIds(stateprovinceids: Array[StateprovinceId])(using c: Connection): Int = {
    var count = 0
    stateprovinceids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: StateprovinceRow)(using c: Connection): StateprovinceRow = {
    if (map.contains(unsaved.stateprovinceid)) {
      throw new RuntimeException(s"id ${unsaved.stateprovinceid} already exists")
    }
    map.put(unsaved.stateprovinceid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: StateprovinceRowUnsaved)(using c: Connection): StateprovinceRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[StateprovinceRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.stateprovinceid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[StateprovinceRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.stateprovinceid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[StateprovinceFields, StateprovinceRow] = SelectBuilderMock(StateprovinceFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[StateprovinceRow] = map.values.toList

  override def selectById(stateprovinceid: StateprovinceId)(using c: Connection): Option[StateprovinceRow] = map.get(stateprovinceid)

  override def selectByIds(stateprovinceids: Array[StateprovinceId])(using c: Connection): List[StateprovinceRow] = stateprovinceids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(stateprovinceids: Array[StateprovinceId])(using c: Connection): Map[StateprovinceId, StateprovinceRow] = selectByIds(stateprovinceids)(using c).map(x => (((row: StateprovinceRow) => row.stateprovinceid).apply(x), x)).toMap

  override def update: UpdateBuilder[StateprovinceFields, StateprovinceRow] = UpdateBuilderMock(StateprovinceFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: StateprovinceRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.stateprovinceid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.stateprovinceid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: StateprovinceRow)(using c: Connection): StateprovinceRow = {
    map.put(unsaved.stateprovinceid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[StateprovinceRow])(using c: Connection): List[StateprovinceRow] = {
    unsaved.map { row =>
      map.put(row.stateprovinceid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[StateprovinceRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.stateprovinceid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}