/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.phonenumbertype

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class PhonenumbertypeRepoMock(
  toRow: PhonenumbertypeRowUnsaved => PhonenumbertypeRow,
  map: scala.collection.mutable.Map[PhonenumbertypeId, PhonenumbertypeRow] = scala.collection.mutable.Map.empty[PhonenumbertypeId, PhonenumbertypeRow]
) extends PhonenumbertypeRepo {
  override def delete: DeleteBuilder[PhonenumbertypeFields, PhonenumbertypeRow] = DeleteBuilderMock(PhonenumbertypeFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.phonenumbertypeid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(phonenumbertypeid: PhonenumbertypeId)(using c: Connection): Boolean = map.remove(phonenumbertypeid).isDefined

  override def deleteByIds(phonenumbertypeids: Array[PhonenumbertypeId])(using c: Connection): Int = {
    var count = 0
    phonenumbertypeids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: PhonenumbertypeRow)(using c: Connection): PhonenumbertypeRow = {
    if (map.contains(unsaved.phonenumbertypeid)) {
      throw new RuntimeException(s"id ${unsaved.phonenumbertypeid} already exists")
    }
    map.put(unsaved.phonenumbertypeid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: PhonenumbertypeRowUnsaved)(using c: Connection): PhonenumbertypeRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[PhonenumbertypeRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.phonenumbertypeid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[PhonenumbertypeRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.phonenumbertypeid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[PhonenumbertypeFields, PhonenumbertypeRow] = SelectBuilderMock(PhonenumbertypeFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[PhonenumbertypeRow] = map.values.toList

  override def selectById(phonenumbertypeid: PhonenumbertypeId)(using c: Connection): Option[PhonenumbertypeRow] = map.get(phonenumbertypeid)

  override def selectByIds(phonenumbertypeids: Array[PhonenumbertypeId])(using c: Connection): List[PhonenumbertypeRow] = phonenumbertypeids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(phonenumbertypeids: Array[PhonenumbertypeId])(using c: Connection): Map[PhonenumbertypeId, PhonenumbertypeRow] = selectByIds(phonenumbertypeids)(using c).map(x => (((row: PhonenumbertypeRow) => row.phonenumbertypeid).apply(x), x)).toMap

  override def update: UpdateBuilder[PhonenumbertypeFields, PhonenumbertypeRow] = UpdateBuilderMock(PhonenumbertypeFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: PhonenumbertypeRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.phonenumbertypeid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.phonenumbertypeid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: PhonenumbertypeRow)(using c: Connection): PhonenumbertypeRow = {
    map.put(unsaved.phonenumbertypeid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[PhonenumbertypeRow])(using c: Connection): List[PhonenumbertypeRow] = {
    unsaved.map { row =>
      map.put(row.phonenumbertypeid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[PhonenumbertypeRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.phonenumbertypeid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}