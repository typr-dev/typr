/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.purchasing.vendor

import adventureworks.person.businessentity.BusinessentityId
import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class VendorRepoMock(
  toRow: VendorRowUnsaved => VendorRow,
  map: scala.collection.mutable.Map[BusinessentityId, VendorRow] = scala.collection.mutable.Map.empty[BusinessentityId, VendorRow]
) extends VendorRepo {
  override def delete: DeleteBuilder[VendorFields, VendorRow] = DeleteBuilderMock(VendorFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.businessentityid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(businessentityid: BusinessentityId)(using c: Connection): Boolean = map.remove(businessentityid).isDefined

  override def deleteByIds(businessentityids: Array[BusinessentityId])(using c: Connection): Int = {
    var count = 0
    businessentityids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: VendorRow)(using c: Connection): VendorRow = {
    if (map.contains(unsaved.businessentityid)) {
      throw new RuntimeException(s"id ${unsaved.businessentityid} already exists")
    }
    map.put(unsaved.businessentityid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: VendorRowUnsaved)(using c: Connection): VendorRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[VendorRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.businessentityid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[VendorRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.businessentityid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[VendorFields, VendorRow] = SelectBuilderMock(VendorFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[VendorRow] = map.values.toList

  override def selectById(businessentityid: BusinessentityId)(using c: Connection): Option[VendorRow] = map.get(businessentityid)

  override def selectByIds(businessentityids: Array[BusinessentityId])(using c: Connection): List[VendorRow] = businessentityids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(businessentityids: Array[BusinessentityId])(using c: Connection): Map[BusinessentityId, VendorRow] = selectByIds(businessentityids)(using c).map(x => (((row: VendorRow) => row.businessentityid).apply(x), x)).toMap

  override def update: UpdateBuilder[VendorFields, VendorRow] = UpdateBuilderMock(VendorFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: VendorRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.businessentityid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.businessentityid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: VendorRow)(using c: Connection): VendorRow = {
    map.put(unsaved.businessentityid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[VendorRow])(using c: Connection): List[VendorRow] = {
    unsaved.map { row =>
      map.put(row.businessentityid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[VendorRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.businessentityid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}