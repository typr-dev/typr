/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.purchasing.purchaseorderheader

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class PurchaseorderheaderRepoMock(
  toRow: PurchaseorderheaderRowUnsaved => PurchaseorderheaderRow,
  map: scala.collection.mutable.Map[PurchaseorderheaderId, PurchaseorderheaderRow] = scala.collection.mutable.Map.empty[PurchaseorderheaderId, PurchaseorderheaderRow]
) extends PurchaseorderheaderRepo {
  override def delete: DeleteBuilder[PurchaseorderheaderFields, PurchaseorderheaderRow] = DeleteBuilderMock(PurchaseorderheaderFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.purchaseorderid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(purchaseorderid: PurchaseorderheaderId)(using c: Connection): Boolean = map.remove(purchaseorderid).isDefined

  override def deleteByIds(purchaseorderids: Array[PurchaseorderheaderId])(using c: Connection): Int = {
    var count = 0
    purchaseorderids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: PurchaseorderheaderRow)(using c: Connection): PurchaseorderheaderRow = {
    if (map.contains(unsaved.purchaseorderid)) {
      throw new RuntimeException(s"id ${unsaved.purchaseorderid} already exists")
    }
    map.put(unsaved.purchaseorderid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: PurchaseorderheaderRowUnsaved)(using c: Connection): PurchaseorderheaderRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[PurchaseorderheaderRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.purchaseorderid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[PurchaseorderheaderRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.purchaseorderid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[PurchaseorderheaderFields, PurchaseorderheaderRow] = SelectBuilderMock(PurchaseorderheaderFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[PurchaseorderheaderRow] = map.values.toList

  override def selectById(purchaseorderid: PurchaseorderheaderId)(using c: Connection): Option[PurchaseorderheaderRow] = map.get(purchaseorderid)

  override def selectByIds(purchaseorderids: Array[PurchaseorderheaderId])(using c: Connection): List[PurchaseorderheaderRow] = purchaseorderids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(purchaseorderids: Array[PurchaseorderheaderId])(using c: Connection): Map[PurchaseorderheaderId, PurchaseorderheaderRow] = selectByIds(purchaseorderids)(using c).map(x => (((row: PurchaseorderheaderRow) => row.purchaseorderid).apply(x), x)).toMap

  override def update: UpdateBuilder[PurchaseorderheaderFields, PurchaseorderheaderRow] = UpdateBuilderMock(PurchaseorderheaderFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: PurchaseorderheaderRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.purchaseorderid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.purchaseorderid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: PurchaseorderheaderRow)(using c: Connection): PurchaseorderheaderRow = {
    map.put(unsaved.purchaseorderid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[PurchaseorderheaderRow])(using c: Connection): List[PurchaseorderheaderRow] = {
    unsaved.map { row =>
      map.put(row.purchaseorderid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[PurchaseorderheaderRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.purchaseorderid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}