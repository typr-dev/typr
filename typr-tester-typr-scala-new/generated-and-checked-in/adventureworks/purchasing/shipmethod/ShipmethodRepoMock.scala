/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.purchasing.shipmethod

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class ShipmethodRepoMock(
  toRow: ShipmethodRowUnsaved => ShipmethodRow,
  map: scala.collection.mutable.Map[ShipmethodId, ShipmethodRow] = scala.collection.mutable.Map.empty[ShipmethodId, ShipmethodRow]
) extends ShipmethodRepo {
  override def delete: DeleteBuilder[ShipmethodFields, ShipmethodRow] = DeleteBuilderMock(ShipmethodFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.shipmethodid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(shipmethodid: ShipmethodId)(using c: Connection): Boolean = map.remove(shipmethodid).isDefined

  override def deleteByIds(shipmethodids: Array[ShipmethodId])(using c: Connection): Int = {
    var count = 0
    shipmethodids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: ShipmethodRow)(using c: Connection): ShipmethodRow = {
    if (map.contains(unsaved.shipmethodid)) {
      throw new RuntimeException(s"id ${unsaved.shipmethodid} already exists")
    }
    map.put(unsaved.shipmethodid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: ShipmethodRowUnsaved)(using c: Connection): ShipmethodRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[ShipmethodRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.shipmethodid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[ShipmethodRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.shipmethodid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[ShipmethodFields, ShipmethodRow] = SelectBuilderMock(ShipmethodFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[ShipmethodRow] = map.values.toList

  override def selectById(shipmethodid: ShipmethodId)(using c: Connection): Option[ShipmethodRow] = map.get(shipmethodid)

  override def selectByIds(shipmethodids: Array[ShipmethodId])(using c: Connection): List[ShipmethodRow] = shipmethodids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(shipmethodids: Array[ShipmethodId])(using c: Connection): Map[ShipmethodId, ShipmethodRow] = selectByIds(shipmethodids)(using c).map(x => (((row: ShipmethodRow) => row.shipmethodid).apply(x), x)).toMap

  override def update: UpdateBuilder[ShipmethodFields, ShipmethodRow] = UpdateBuilderMock(ShipmethodFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: ShipmethodRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.shipmethodid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.shipmethodid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: ShipmethodRow)(using c: Connection): ShipmethodRow = {
    map.put(unsaved.shipmethodid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[ShipmethodRow])(using c: Connection): List[ShipmethodRow] = {
    unsaved.map { row =>
      map.put(row.shipmethodid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[ShipmethodRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.shipmethodid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}