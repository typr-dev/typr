/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.salesorderdetail

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class SalesorderdetailRepoMock(
  toRow: SalesorderdetailRowUnsaved => SalesorderdetailRow,
  map: scala.collection.mutable.Map[SalesorderdetailId, SalesorderdetailRow] = scala.collection.mutable.Map.empty[SalesorderdetailId, SalesorderdetailRow]
) extends SalesorderdetailRepo {
  override def delete: DeleteBuilder[SalesorderdetailFields, SalesorderdetailRow] = DeleteBuilderMock(SalesorderdetailFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.compositeId, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(compositeId: SalesorderdetailId)(using c: Connection): Boolean = map.remove(compositeId).isDefined

  override def deleteByIds(compositeIds: Array[SalesorderdetailId])(using c: Connection): Int = {
    var count = 0
    compositeIds.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: SalesorderdetailRow)(using c: Connection): SalesorderdetailRow = {
    if (map.contains(unsaved.compositeId)) {
      throw new RuntimeException(s"id ${unsaved.compositeId} already exists")
    }
    map.put(unsaved.compositeId, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: SalesorderdetailRowUnsaved)(using c: Connection): SalesorderdetailRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[SalesorderdetailRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.compositeId, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[SalesorderdetailRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.compositeId, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[SalesorderdetailFields, SalesorderdetailRow] = SelectBuilderMock(SalesorderdetailFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[SalesorderdetailRow] = map.values.toList

  override def selectById(compositeId: SalesorderdetailId)(using c: Connection): Option[SalesorderdetailRow] = map.get(compositeId)

  override def selectByIds(compositeIds: Array[SalesorderdetailId])(using c: Connection): List[SalesorderdetailRow] = compositeIds.flatMap(map.get(_)).toList

  override def selectByIdsTracked(compositeIds: Array[SalesorderdetailId])(using c: Connection): Map[SalesorderdetailId, SalesorderdetailRow] = selectByIds(compositeIds)(using c).map(x => (((row: SalesorderdetailRow) => row.compositeId).apply(x), x)).toMap

  override def update: UpdateBuilder[SalesorderdetailFields, SalesorderdetailRow] = UpdateBuilderMock(SalesorderdetailFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: SalesorderdetailRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.compositeId).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.compositeId, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: SalesorderdetailRow)(using c: Connection): SalesorderdetailRow = {
    map.put(unsaved.compositeId, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[SalesorderdetailRow])(using c: Connection): List[SalesorderdetailRow] = {
    unsaved.map { row =>
      map.put(row.compositeId, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[SalesorderdetailRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.compositeId, row): @scala.annotation.nowarn
      1
    }.sum
  }
}