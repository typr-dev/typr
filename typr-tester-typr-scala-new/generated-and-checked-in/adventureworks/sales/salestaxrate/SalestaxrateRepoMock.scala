/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.salestaxrate

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class SalestaxrateRepoMock(
  toRow: SalestaxrateRowUnsaved => SalestaxrateRow,
  map: scala.collection.mutable.Map[SalestaxrateId, SalestaxrateRow] = scala.collection.mutable.Map.empty[SalestaxrateId, SalestaxrateRow]
) extends SalestaxrateRepo {
  override def delete: DeleteBuilder[SalestaxrateFields, SalestaxrateRow] = DeleteBuilderMock(SalestaxrateFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.salestaxrateid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(salestaxrateid: SalestaxrateId)(using c: Connection): Boolean = map.remove(salestaxrateid).isDefined

  override def deleteByIds(salestaxrateids: Array[SalestaxrateId])(using c: Connection): Int = {
    var count = 0
    salestaxrateids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: SalestaxrateRow)(using c: Connection): SalestaxrateRow = {
    if (map.contains(unsaved.salestaxrateid)) {
      throw new RuntimeException(s"id ${unsaved.salestaxrateid} already exists")
    }
    map.put(unsaved.salestaxrateid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: SalestaxrateRowUnsaved)(using c: Connection): SalestaxrateRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[SalestaxrateRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.salestaxrateid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[SalestaxrateRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.salestaxrateid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[SalestaxrateFields, SalestaxrateRow] = SelectBuilderMock(SalestaxrateFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[SalestaxrateRow] = map.values.toList

  override def selectById(salestaxrateid: SalestaxrateId)(using c: Connection): Option[SalestaxrateRow] = map.get(salestaxrateid)

  override def selectByIds(salestaxrateids: Array[SalestaxrateId])(using c: Connection): List[SalestaxrateRow] = salestaxrateids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(salestaxrateids: Array[SalestaxrateId])(using c: Connection): Map[SalestaxrateId, SalestaxrateRow] = selectByIds(salestaxrateids)(using c).map(x => (((row: SalestaxrateRow) => row.salestaxrateid).apply(x), x)).toMap

  override def update: UpdateBuilder[SalestaxrateFields, SalestaxrateRow] = UpdateBuilderMock(SalestaxrateFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: SalestaxrateRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.salestaxrateid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.salestaxrateid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: SalestaxrateRow)(using c: Connection): SalestaxrateRow = {
    map.put(unsaved.salestaxrateid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[SalestaxrateRow])(using c: Connection): List[SalestaxrateRow] = {
    unsaved.map { row =>
      map.put(row.salestaxrateid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[SalestaxrateRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.salestaxrateid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}