/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.personcreditcard

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class PersoncreditcardRepoMock(
  toRow: PersoncreditcardRowUnsaved => PersoncreditcardRow,
  map: scala.collection.mutable.Map[PersoncreditcardId, PersoncreditcardRow] = scala.collection.mutable.Map.empty[PersoncreditcardId, PersoncreditcardRow]
) extends PersoncreditcardRepo {
  override def delete: DeleteBuilder[PersoncreditcardFields, PersoncreditcardRow] = DeleteBuilderMock(PersoncreditcardFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.compositeId, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(compositeId: PersoncreditcardId)(using c: Connection): Boolean = map.remove(compositeId).isDefined

  override def deleteByIds(compositeIds: Array[PersoncreditcardId])(using c: Connection): Int = {
    var count = 0
    compositeIds.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: PersoncreditcardRow)(using c: Connection): PersoncreditcardRow = {
    if (map.contains(unsaved.compositeId)) {
      throw new RuntimeException(s"id ${unsaved.compositeId} already exists")
    }
    map.put(unsaved.compositeId, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: PersoncreditcardRowUnsaved)(using c: Connection): PersoncreditcardRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[PersoncreditcardRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.compositeId, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[PersoncreditcardRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.compositeId, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[PersoncreditcardFields, PersoncreditcardRow] = SelectBuilderMock(PersoncreditcardFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[PersoncreditcardRow] = map.values.toList

  override def selectById(compositeId: PersoncreditcardId)(using c: Connection): Option[PersoncreditcardRow] = map.get(compositeId)

  override def selectByIds(compositeIds: Array[PersoncreditcardId])(using c: Connection): List[PersoncreditcardRow] = compositeIds.flatMap(map.get(_)).toList

  override def selectByIdsTracked(compositeIds: Array[PersoncreditcardId])(using c: Connection): Map[PersoncreditcardId, PersoncreditcardRow] = selectByIds(compositeIds)(using c).map(x => (((row: PersoncreditcardRow) => row.compositeId).apply(x), x)).toMap

  override def update: UpdateBuilder[PersoncreditcardFields, PersoncreditcardRow] = UpdateBuilderMock(PersoncreditcardFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: PersoncreditcardRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.compositeId).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.compositeId, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: PersoncreditcardRow)(using c: Connection): PersoncreditcardRow = {
    map.put(unsaved.compositeId, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[PersoncreditcardRow])(using c: Connection): List[PersoncreditcardRow] = {
    unsaved.map { row =>
      map.put(row.compositeId, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[PersoncreditcardRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.compositeId, row): @scala.annotation.nowarn
      1
    }.sum
  }
}