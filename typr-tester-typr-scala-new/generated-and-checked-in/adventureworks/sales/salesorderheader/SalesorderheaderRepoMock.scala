/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.salesorderheader

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class SalesorderheaderRepoMock(
  toRow: SalesorderheaderRowUnsaved => SalesorderheaderRow,
  map: scala.collection.mutable.Map[SalesorderheaderId, SalesorderheaderRow] = scala.collection.mutable.Map.empty[SalesorderheaderId, SalesorderheaderRow]
) extends SalesorderheaderRepo {
  override def delete: DeleteBuilder[SalesorderheaderFields, SalesorderheaderRow] = DeleteBuilderMock(SalesorderheaderFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.salesorderid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(salesorderid: SalesorderheaderId)(using c: Connection): Boolean = map.remove(salesorderid).isDefined

  override def deleteByIds(salesorderids: Array[SalesorderheaderId])(using c: Connection): Int = {
    var count = 0
    salesorderids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: SalesorderheaderRow)(using c: Connection): SalesorderheaderRow = {
    if (map.contains(unsaved.salesorderid)) {
      throw new RuntimeException(s"id ${unsaved.salesorderid} already exists")
    }
    map.put(unsaved.salesorderid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: SalesorderheaderRowUnsaved)(using c: Connection): SalesorderheaderRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[SalesorderheaderRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.salesorderid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[SalesorderheaderRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.salesorderid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[SalesorderheaderFields, SalesorderheaderRow] = SelectBuilderMock(SalesorderheaderFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[SalesorderheaderRow] = map.values.toList

  override def selectById(salesorderid: SalesorderheaderId)(using c: Connection): Option[SalesorderheaderRow] = map.get(salesorderid)

  override def selectByIds(salesorderids: Array[SalesorderheaderId])(using c: Connection): List[SalesorderheaderRow] = salesorderids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(salesorderids: Array[SalesorderheaderId])(using c: Connection): Map[SalesorderheaderId, SalesorderheaderRow] = selectByIds(salesorderids)(using c).map(x => (((row: SalesorderheaderRow) => row.salesorderid).apply(x), x)).toMap

  override def update: UpdateBuilder[SalesorderheaderFields, SalesorderheaderRow] = UpdateBuilderMock(SalesorderheaderFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: SalesorderheaderRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.salesorderid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.salesorderid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: SalesorderheaderRow)(using c: Connection): SalesorderheaderRow = {
    map.put(unsaved.salesorderid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[SalesorderheaderRow])(using c: Connection): List[SalesorderheaderRow] = {
    unsaved.map { row =>
      map.put(row.salesorderid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[SalesorderheaderRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.salesorderid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}