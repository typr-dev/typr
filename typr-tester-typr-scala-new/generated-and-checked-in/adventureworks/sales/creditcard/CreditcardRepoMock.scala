/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.creditcard

import adventureworks.userdefined.CustomCreditcardId
import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class CreditcardRepoMock(
  toRow: CreditcardRowUnsaved => CreditcardRow,
  map: scala.collection.mutable.Map[/* user-picked */ CustomCreditcardId, CreditcardRow] = scala.collection.mutable.Map.empty[/* user-picked */ CustomCreditcardId, CreditcardRow]
) extends CreditcardRepo {
  override def delete: DeleteBuilder[CreditcardFields, CreditcardRow] = DeleteBuilderMock(CreditcardFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.creditcardid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(creditcardid: /* user-picked */ CustomCreditcardId)(using c: Connection): Boolean = map.remove(creditcardid).isDefined

  override def deleteByIds(creditcardids: Array[/* user-picked */ CustomCreditcardId])(using c: Connection): Int = {
    var count = 0
    creditcardids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: CreditcardRow)(using c: Connection): CreditcardRow = {
    if (map.contains(unsaved.creditcardid)) {
      throw new RuntimeException(s"id ${unsaved.creditcardid} already exists")
    }
    map.put(unsaved.creditcardid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: CreditcardRowUnsaved)(using c: Connection): CreditcardRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[CreditcardRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.creditcardid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[CreditcardRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.creditcardid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[CreditcardFields, CreditcardRow] = SelectBuilderMock(CreditcardFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[CreditcardRow] = map.values.toList

  override def selectById(creditcardid: /* user-picked */ CustomCreditcardId)(using c: Connection): Option[CreditcardRow] = map.get(creditcardid)

  override def selectByIds(creditcardids: Array[/* user-picked */ CustomCreditcardId])(using c: Connection): List[CreditcardRow] = creditcardids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(creditcardids: Array[/* user-picked */ CustomCreditcardId])(using c: Connection): Map[/* user-picked */ CustomCreditcardId, CreditcardRow] = selectByIds(creditcardids)(using c).map(x => (((row: CreditcardRow) => row.creditcardid).apply(x), x)).toMap

  override def update: UpdateBuilder[CreditcardFields, CreditcardRow] = UpdateBuilderMock(CreditcardFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: CreditcardRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.creditcardid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.creditcardid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: CreditcardRow)(using c: Connection): CreditcardRow = {
    map.put(unsaved.creditcardid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[CreditcardRow])(using c: Connection): List[CreditcardRow] = {
    unsaved.map { row =>
      map.put(row.creditcardid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[CreditcardRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.creditcardid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}