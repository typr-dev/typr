/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.salesorderheadersalesreason

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class SalesorderheadersalesreasonRepoMock(
  toRow: SalesorderheadersalesreasonRowUnsaved => SalesorderheadersalesreasonRow,
  map: scala.collection.mutable.Map[SalesorderheadersalesreasonId, SalesorderheadersalesreasonRow] = scala.collection.mutable.Map.empty[SalesorderheadersalesreasonId, SalesorderheadersalesreasonRow]
) extends SalesorderheadersalesreasonRepo {
  override def delete: DeleteBuilder[SalesorderheadersalesreasonFields, SalesorderheadersalesreasonRow] = DeleteBuilderMock(SalesorderheadersalesreasonFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.compositeId, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(compositeId: SalesorderheadersalesreasonId)(using c: Connection): Boolean = map.remove(compositeId).isDefined

  override def deleteByIds(compositeIds: Array[SalesorderheadersalesreasonId])(using c: Connection): Int = {
    var count = 0
    compositeIds.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: SalesorderheadersalesreasonRow)(using c: Connection): SalesorderheadersalesreasonRow = {
    if (map.contains(unsaved.compositeId)) {
      throw new RuntimeException(s"id ${unsaved.compositeId} already exists")
    }
    map.put(unsaved.compositeId, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: SalesorderheadersalesreasonRowUnsaved)(using c: Connection): SalesorderheadersalesreasonRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[SalesorderheadersalesreasonRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.compositeId, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[SalesorderheadersalesreasonRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.compositeId, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[SalesorderheadersalesreasonFields, SalesorderheadersalesreasonRow] = SelectBuilderMock(SalesorderheadersalesreasonFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[SalesorderheadersalesreasonRow] = map.values.toList

  override def selectById(compositeId: SalesorderheadersalesreasonId)(using c: Connection): Option[SalesorderheadersalesreasonRow] = map.get(compositeId)

  override def selectByIds(compositeIds: Array[SalesorderheadersalesreasonId])(using c: Connection): List[SalesorderheadersalesreasonRow] = compositeIds.flatMap(map.get(_)).toList

  override def selectByIdsTracked(compositeIds: Array[SalesorderheadersalesreasonId])(using c: Connection): Map[SalesorderheadersalesreasonId, SalesorderheadersalesreasonRow] = selectByIds(compositeIds)(using c).map(x => (((row: SalesorderheadersalesreasonRow) => row.compositeId).apply(x), x)).toMap

  override def update: UpdateBuilder[SalesorderheadersalesreasonFields, SalesorderheadersalesreasonRow] = UpdateBuilderMock(SalesorderheadersalesreasonFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: SalesorderheadersalesreasonRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.compositeId).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.compositeId, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: SalesorderheadersalesreasonRow)(using c: Connection): SalesorderheadersalesreasonRow = {
    map.put(unsaved.compositeId, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[SalesorderheadersalesreasonRow])(using c: Connection): List[SalesorderheadersalesreasonRow] = {
    unsaved.map { row =>
      map.put(row.compositeId, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[SalesorderheadersalesreasonRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.compositeId, row): @scala.annotation.nowarn
      1
    }.sum
  }
}