/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.store

import adventureworks.person.businessentity.BusinessentityId
import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class StoreRepoMock(
  toRow: StoreRowUnsaved => StoreRow,
  map: scala.collection.mutable.Map[BusinessentityId, StoreRow] = scala.collection.mutable.Map.empty[BusinessentityId, StoreRow]
) extends StoreRepo {
  override def delete: DeleteBuilder[StoreFields, StoreRow] = DeleteBuilderMock(StoreFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.businessentityid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(businessentityid: BusinessentityId)(using c: Connection): Boolean = map.remove(businessentityid).isDefined

  override def deleteByIds(businessentityids: Array[BusinessentityId])(using c: Connection): Int = {
    var count = 0
    businessentityids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: StoreRow)(using c: Connection): StoreRow = {
    if (map.contains(unsaved.businessentityid)) {
      throw new RuntimeException(s"id ${unsaved.businessentityid} already exists")
    }
    map.put(unsaved.businessentityid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: StoreRowUnsaved)(using c: Connection): StoreRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[StoreRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.businessentityid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[StoreRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.businessentityid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[StoreFields, StoreRow] = SelectBuilderMock(StoreFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[StoreRow] = map.values.toList

  override def selectById(businessentityid: BusinessentityId)(using c: Connection): Option[StoreRow] = map.get(businessentityid)

  override def selectByIds(businessentityids: Array[BusinessentityId])(using c: Connection): List[StoreRow] = businessentityids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(businessentityids: Array[BusinessentityId])(using c: Connection): Map[BusinessentityId, StoreRow] = selectByIds(businessentityids)(using c).map(x => (((row: StoreRow) => row.businessentityid).apply(x), x)).toMap

  override def update: UpdateBuilder[StoreFields, StoreRow] = UpdateBuilderMock(StoreFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: StoreRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.businessentityid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.businessentityid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: StoreRow)(using c: Connection): StoreRow = {
    map.put(unsaved.businessentityid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[StoreRow])(using c: Connection): List[StoreRow] = {
    unsaved.map { row =>
      map.put(row.businessentityid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[StoreRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.businessentityid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}