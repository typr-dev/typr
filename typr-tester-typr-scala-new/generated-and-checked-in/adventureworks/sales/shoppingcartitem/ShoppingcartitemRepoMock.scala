/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.shoppingcartitem

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class ShoppingcartitemRepoMock(
  toRow: ShoppingcartitemRowUnsaved => ShoppingcartitemRow,
  map: scala.collection.mutable.Map[ShoppingcartitemId, ShoppingcartitemRow] = scala.collection.mutable.Map.empty[ShoppingcartitemId, ShoppingcartitemRow]
) extends ShoppingcartitemRepo {
  override def delete: DeleteBuilder[ShoppingcartitemFields, ShoppingcartitemRow] = DeleteBuilderMock(ShoppingcartitemFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.shoppingcartitemid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(shoppingcartitemid: ShoppingcartitemId)(using c: Connection): Boolean = map.remove(shoppingcartitemid).isDefined

  override def deleteByIds(shoppingcartitemids: Array[ShoppingcartitemId])(using c: Connection): Int = {
    var count = 0
    shoppingcartitemids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: ShoppingcartitemRow)(using c: Connection): ShoppingcartitemRow = {
    if (map.contains(unsaved.shoppingcartitemid)) {
      throw new RuntimeException(s"id ${unsaved.shoppingcartitemid} already exists")
    }
    map.put(unsaved.shoppingcartitemid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: ShoppingcartitemRowUnsaved)(using c: Connection): ShoppingcartitemRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[ShoppingcartitemRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.shoppingcartitemid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[ShoppingcartitemRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.shoppingcartitemid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[ShoppingcartitemFields, ShoppingcartitemRow] = SelectBuilderMock(ShoppingcartitemFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[ShoppingcartitemRow] = map.values.toList

  override def selectById(shoppingcartitemid: ShoppingcartitemId)(using c: Connection): Option[ShoppingcartitemRow] = map.get(shoppingcartitemid)

  override def selectByIds(shoppingcartitemids: Array[ShoppingcartitemId])(using c: Connection): List[ShoppingcartitemRow] = shoppingcartitemids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(shoppingcartitemids: Array[ShoppingcartitemId])(using c: Connection): Map[ShoppingcartitemId, ShoppingcartitemRow] = selectByIds(shoppingcartitemids)(using c).map(x => (((row: ShoppingcartitemRow) => row.shoppingcartitemid).apply(x), x)).toMap

  override def update: UpdateBuilder[ShoppingcartitemFields, ShoppingcartitemRow] = UpdateBuilderMock(ShoppingcartitemFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: ShoppingcartitemRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.shoppingcartitemid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.shoppingcartitemid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: ShoppingcartitemRow)(using c: Connection): ShoppingcartitemRow = {
    map.put(unsaved.shoppingcartitemid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[ShoppingcartitemRow])(using c: Connection): List[ShoppingcartitemRow] = {
    unsaved.map { row =>
      map.put(row.shoppingcartitemid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[ShoppingcartitemRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.shoppingcartitemid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}