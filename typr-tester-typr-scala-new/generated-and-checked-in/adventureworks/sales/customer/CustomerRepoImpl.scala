/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.customer

import adventureworks.person.businessentity.BusinessentityId
import adventureworks.sales.salesterritory.SalesterritoryId
import java.sql.Connection
import scala.collection.mutable.ListBuffer
import typr.runtime.PgTypes
import typr.runtime.streamingInsert
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.Dialect
import typr.scaladsl.Fragment
import typr.scaladsl.PgTypeOps
import typr.scaladsl.ScalaIteratorOps
import typr.scaladsl.SelectBuilder
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.Fragment.sql

class CustomerRepoImpl extends CustomerRepo {
  override def delete: DeleteBuilder[CustomerFields, CustomerRow] = DeleteBuilder.of(""""sales"."customer"""", CustomerFields.structure, Dialect.POSTGRESQL)

  override def deleteById(customerid: CustomerId)(using c: Connection): Boolean = sql"""delete from "sales"."customer" where "customerid" = ${Fragment.encode(CustomerId.pgType, customerid)}""".update().runUnchecked(c) > 0

  override def deleteByIds(customerids: Array[CustomerId])(using c: Connection): Int = {
    sql"""delete
    from "sales"."customer"
    where "customerid" = ANY(${Fragment.encode(CustomerId.pgTypeArray, customerids)})"""
      .update()
      .runUnchecked(c)
  }

  override def insert(unsaved: CustomerRow)(using c: Connection): CustomerRow = {
  sql"""insert into "sales"."customer"("customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate")
    values (${Fragment.encode(CustomerId.pgType, unsaved.customerid)}::int4, ${Fragment.encode(BusinessentityId.pgType.nullable, unsaved.personid)}::int4, ${Fragment.encode(BusinessentityId.pgType.nullable, unsaved.storeid)}::int4, ${Fragment.encode(SalesterritoryId.pgType.nullable, unsaved.territoryid)}::int4, ${Fragment.encode(PgTypes.uuid, unsaved.rowguid)}::uuid, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    RETURNING "customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate"
    """
    .updateReturning(CustomerRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: CustomerRowUnsaved)(using c: Connection): CustomerRow = {
    val columns: ListBuffer[Fragment] = ListBuffer()
    val values: ListBuffer[Fragment] = ListBuffer()
    columns.addOne(Fragment.lit(""""personid"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(BusinessentityId.pgType.nullable, unsaved.personid)}::int4"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""storeid"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(BusinessentityId.pgType.nullable, unsaved.storeid)}::int4"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""territoryid"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(SalesterritoryId.pgType.nullable, unsaved.territoryid)}::int4"): @scala.annotation.nowarn
    unsaved.customerid.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""customerid"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(CustomerId.pgType, value)}::int4"): @scala.annotation.nowarn }
    );
    unsaved.rowguid.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""rowguid"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.uuid, value)}::uuid"): @scala.annotation.nowarn }
    );
    unsaved.modifieddate.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.timestamp, value)}::timestamp"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      sql"""insert into "sales"."customer"(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      RETURNING "customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate"
      """
    }
    return q.updateReturning(CustomerRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: Iterator[CustomerRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "sales"."customer"("customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate") FROM STDIN""", batchSize, unsaved.toJavaIterator, c, CustomerRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[CustomerRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "sales"."customer"("personid", "storeid", "territoryid", "customerid", "rowguid", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved.toJavaIterator, c, CustomerRowUnsaved.pgText)

  override def select: SelectBuilder[CustomerFields, CustomerRow] = SelectBuilder.of(""""sales"."customer"""", CustomerFields.structure, CustomerRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): List[CustomerRow] = {
    sql"""select "customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate"
    from "sales"."customer"
    """.query(CustomerRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(customerid: CustomerId)(using c: Connection): Option[CustomerRow] = {
    sql"""select "customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate"
    from "sales"."customer"
    where "customerid" = ${Fragment.encode(CustomerId.pgType, customerid)}""".query(CustomerRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(customerids: Array[CustomerId])(using c: Connection): List[CustomerRow] = {
    sql"""select "customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate"
    from "sales"."customer"
    where "customerid" = ANY(${Fragment.encode(CustomerId.pgTypeArray, customerids)})""".query(CustomerRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(customerids: Array[CustomerId])(using c: Connection): Map[CustomerId, CustomerRow] = {
    val ret: scala.collection.mutable.Map[CustomerId, CustomerRow] = scala.collection.mutable.Map.empty[CustomerId, CustomerRow]
    selectByIds(customerids)(using c).foreach(row => ret.put(row.customerid, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[CustomerFields, CustomerRow] = UpdateBuilder.of(""""sales"."customer"""", CustomerFields.structure, CustomerRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: CustomerRow)(using c: Connection): Boolean = {
    val customerid: CustomerId = row.customerid
    return sql"""update "sales"."customer"
    set "personid" = ${Fragment.encode(BusinessentityId.pgType.nullable, row.personid)}::int4,
    "storeid" = ${Fragment.encode(BusinessentityId.pgType.nullable, row.storeid)}::int4,
    "territoryid" = ${Fragment.encode(SalesterritoryId.pgType.nullable, row.territoryid)}::int4,
    "rowguid" = ${Fragment.encode(PgTypes.uuid, row.rowguid)}::uuid,
    "modifieddate" = ${Fragment.encode(PgTypes.timestamp, row.modifieddate)}::timestamp
    where "customerid" = ${Fragment.encode(CustomerId.pgType, customerid)}""".update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: CustomerRow)(using c: Connection): CustomerRow = {
  sql"""insert into "sales"."customer"("customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate")
    values (${Fragment.encode(CustomerId.pgType, unsaved.customerid)}::int4, ${Fragment.encode(BusinessentityId.pgType.nullable, unsaved.personid)}::int4, ${Fragment.encode(BusinessentityId.pgType.nullable, unsaved.storeid)}::int4, ${Fragment.encode(SalesterritoryId.pgType.nullable, unsaved.territoryid)}::int4, ${Fragment.encode(PgTypes.uuid, unsaved.rowguid)}::uuid, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    on conflict ("customerid")
    do update set
      "personid" = EXCLUDED."personid",
    "storeid" = EXCLUDED."storeid",
    "territoryid" = EXCLUDED."territoryid",
    "rowguid" = EXCLUDED."rowguid",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate""""
    .updateReturning(CustomerRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: Iterator[CustomerRow])(using c: Connection): List[CustomerRow] = {
    sql"""insert into "sales"."customer"("customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate")
    values (?::int4, ?::int4, ?::int4, ?::int4, ?::uuid, ?::timestamp)
    on conflict ("customerid")
    do update set
      "personid" = EXCLUDED."personid",
    "storeid" = EXCLUDED."storeid",
    "territoryid" = EXCLUDED."territoryid",
    "rowguid" = EXCLUDED."rowguid",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate""""
      .updateManyReturning(CustomerRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[CustomerRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    sql"""create temporary table customer_TEMP (like "sales"."customer") on commit drop""".update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy customer_TEMP("customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate") from stdin""", batchSize, unsaved.toJavaIterator, c, CustomerRow.pgText): @scala.annotation.nowarn
    return sql"""insert into "sales"."customer"("customerid", "personid", "storeid", "territoryid", "rowguid", "modifieddate")
    select * from customer_TEMP
    on conflict ("customerid")
    do update set
      "personid" = EXCLUDED."personid",
    "storeid" = EXCLUDED."storeid",
    "territoryid" = EXCLUDED."territoryid",
    "rowguid" = EXCLUDED."rowguid",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table customer_TEMP;""".update().runUnchecked(c)
  }
}