/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.customer

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class CustomerRepoMock(
  toRow: CustomerRowUnsaved => CustomerRow,
  map: scala.collection.mutable.Map[CustomerId, CustomerRow] = scala.collection.mutable.Map.empty[CustomerId, CustomerRow]
) extends CustomerRepo {
  override def delete: DeleteBuilder[CustomerFields, CustomerRow] = DeleteBuilderMock(CustomerFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.customerid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(customerid: CustomerId)(using c: Connection): Boolean = map.remove(customerid).isDefined

  override def deleteByIds(customerids: Array[CustomerId])(using c: Connection): Int = {
    var count = 0
    customerids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: CustomerRow)(using c: Connection): CustomerRow = {
    if (map.contains(unsaved.customerid)) {
      throw new RuntimeException(s"id ${unsaved.customerid} already exists")
    }
    map.put(unsaved.customerid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: CustomerRowUnsaved)(using c: Connection): CustomerRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[CustomerRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.customerid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[CustomerRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.customerid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[CustomerFields, CustomerRow] = SelectBuilderMock(CustomerFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[CustomerRow] = map.values.toList

  override def selectById(customerid: CustomerId)(using c: Connection): Option[CustomerRow] = map.get(customerid)

  override def selectByIds(customerids: Array[CustomerId])(using c: Connection): List[CustomerRow] = customerids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(customerids: Array[CustomerId])(using c: Connection): Map[CustomerId, CustomerRow] = selectByIds(customerids)(using c).map(x => (((row: CustomerRow) => row.customerid).apply(x), x)).toMap

  override def update: UpdateBuilder[CustomerFields, CustomerRow] = UpdateBuilderMock(CustomerFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: CustomerRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.customerid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.customerid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: CustomerRow)(using c: Connection): CustomerRow = {
    map.put(unsaved.customerid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[CustomerRow])(using c: Connection): List[CustomerRow] = {
    unsaved.map { row =>
      map.put(row.customerid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[CustomerRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.customerid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}