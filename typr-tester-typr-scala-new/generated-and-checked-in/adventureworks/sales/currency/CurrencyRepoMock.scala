/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.currency

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class CurrencyRepoMock(
  toRow: CurrencyRowUnsaved => CurrencyRow,
  map: scala.collection.mutable.Map[CurrencyId, CurrencyRow] = scala.collection.mutable.Map.empty[CurrencyId, CurrencyRow]
) extends CurrencyRepo {
  override def delete: DeleteBuilder[CurrencyFields, CurrencyRow] = DeleteBuilderMock(CurrencyFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.currencycode, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(currencycode: CurrencyId)(using c: Connection): Boolean = map.remove(currencycode).isDefined

  override def deleteByIds(currencycodes: Array[CurrencyId])(using c: Connection): Int = {
    var count = 0
    currencycodes.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: CurrencyRow)(using c: Connection): CurrencyRow = {
    if (map.contains(unsaved.currencycode)) {
      throw new RuntimeException(s"id ${unsaved.currencycode} already exists")
    }
    map.put(unsaved.currencycode, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: CurrencyRowUnsaved)(using c: Connection): CurrencyRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[CurrencyRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.currencycode, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[CurrencyRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.currencycode, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[CurrencyFields, CurrencyRow] = SelectBuilderMock(CurrencyFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[CurrencyRow] = map.values.toList

  override def selectById(currencycode: CurrencyId)(using c: Connection): Option[CurrencyRow] = map.get(currencycode)

  override def selectByIds(currencycodes: Array[CurrencyId])(using c: Connection): List[CurrencyRow] = currencycodes.flatMap(map.get(_)).toList

  override def selectByIdsTracked(currencycodes: Array[CurrencyId])(using c: Connection): Map[CurrencyId, CurrencyRow] = selectByIds(currencycodes)(using c).map(x => (((row: CurrencyRow) => row.currencycode).apply(x), x)).toMap

  override def update: UpdateBuilder[CurrencyFields, CurrencyRow] = UpdateBuilderMock(CurrencyFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: CurrencyRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.currencycode).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.currencycode, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: CurrencyRow)(using c: Connection): CurrencyRow = {
    map.put(unsaved.currencycode, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[CurrencyRow])(using c: Connection): List[CurrencyRow] = {
    unsaved.map { row =>
      map.put(row.currencycode, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[CurrencyRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.currencycode, row): @scala.annotation.nowarn
      1
    }.sum
  }
}