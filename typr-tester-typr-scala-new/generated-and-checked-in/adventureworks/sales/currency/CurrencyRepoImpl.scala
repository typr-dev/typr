/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.currency

import adventureworks.public.Name
import java.sql.Connection
import scala.collection.mutable.ListBuffer
import typr.runtime.PgTypes
import typr.runtime.streamingInsert
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.Dialect
import typr.scaladsl.Fragment
import typr.scaladsl.ScalaIteratorOps
import typr.scaladsl.SelectBuilder
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.Fragment.sql

class CurrencyRepoImpl extends CurrencyRepo {
  override def delete: DeleteBuilder[CurrencyFields, CurrencyRow] = DeleteBuilder.of(""""sales"."currency"""", CurrencyFields.structure, Dialect.POSTGRESQL)

  override def deleteById(currencycode: CurrencyId)(using c: Connection): Boolean = sql"""delete from "sales"."currency" where "currencycode" = ${Fragment.encode(CurrencyId.pgType, currencycode)}""".update().runUnchecked(c) > 0

  override def deleteByIds(currencycodes: Array[CurrencyId])(using c: Connection): Int = {
    sql"""delete
    from "sales"."currency"
    where "currencycode" = ANY(${Fragment.encode(CurrencyId.pgTypeArray, currencycodes)})"""
      .update()
      .runUnchecked(c)
  }

  override def insert(unsaved: CurrencyRow)(using c: Connection): CurrencyRow = {
  sql"""insert into "sales"."currency"("currencycode", "name", "modifieddate")
    values (${Fragment.encode(CurrencyId.pgType, unsaved.currencycode)}::bpchar, ${Fragment.encode(Name.pgType, unsaved.name)}::varchar, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    RETURNING "currencycode", "name", "modifieddate"
    """
    .updateReturning(CurrencyRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: CurrencyRowUnsaved)(using c: Connection): CurrencyRow = {
    val columns: ListBuffer[Fragment] = ListBuffer()
    val values: ListBuffer[Fragment] = ListBuffer()
    columns.addOne(Fragment.lit(""""currencycode"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(CurrencyId.pgType, unsaved.currencycode)}::bpchar"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""name"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(Name.pgType, unsaved.name)}::varchar"): @scala.annotation.nowarn
    unsaved.modifieddate.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.timestamp, value)}::timestamp"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      sql"""insert into "sales"."currency"(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      RETURNING "currencycode", "name", "modifieddate"
      """
    }
    return q.updateReturning(CurrencyRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: Iterator[CurrencyRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "sales"."currency"("currencycode", "name", "modifieddate") FROM STDIN""", batchSize, unsaved.toJavaIterator, c, CurrencyRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[CurrencyRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "sales"."currency"("currencycode", "name", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved.toJavaIterator, c, CurrencyRowUnsaved.pgText)

  override def select: SelectBuilder[CurrencyFields, CurrencyRow] = SelectBuilder.of(""""sales"."currency"""", CurrencyFields.structure, CurrencyRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): List[CurrencyRow] = {
    sql"""select "currencycode", "name", "modifieddate"
    from "sales"."currency"
    """.query(CurrencyRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(currencycode: CurrencyId)(using c: Connection): Option[CurrencyRow] = {
    sql"""select "currencycode", "name", "modifieddate"
    from "sales"."currency"
    where "currencycode" = ${Fragment.encode(CurrencyId.pgType, currencycode)}""".query(CurrencyRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(currencycodes: Array[CurrencyId])(using c: Connection): List[CurrencyRow] = {
    sql"""select "currencycode", "name", "modifieddate"
    from "sales"."currency"
    where "currencycode" = ANY(${Fragment.encode(CurrencyId.pgTypeArray, currencycodes)})""".query(CurrencyRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(currencycodes: Array[CurrencyId])(using c: Connection): Map[CurrencyId, CurrencyRow] = {
    val ret: scala.collection.mutable.Map[CurrencyId, CurrencyRow] = scala.collection.mutable.Map.empty[CurrencyId, CurrencyRow]
    selectByIds(currencycodes)(using c).foreach(row => ret.put(row.currencycode, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[CurrencyFields, CurrencyRow] = UpdateBuilder.of(""""sales"."currency"""", CurrencyFields.structure, CurrencyRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: CurrencyRow)(using c: Connection): Boolean = {
    val currencycode: CurrencyId = row.currencycode
    return sql"""update "sales"."currency"
    set "name" = ${Fragment.encode(Name.pgType, row.name)}::varchar,
    "modifieddate" = ${Fragment.encode(PgTypes.timestamp, row.modifieddate)}::timestamp
    where "currencycode" = ${Fragment.encode(CurrencyId.pgType, currencycode)}""".update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: CurrencyRow)(using c: Connection): CurrencyRow = {
  sql"""insert into "sales"."currency"("currencycode", "name", "modifieddate")
    values (${Fragment.encode(CurrencyId.pgType, unsaved.currencycode)}::bpchar, ${Fragment.encode(Name.pgType, unsaved.name)}::varchar, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    on conflict ("currencycode")
    do update set
      "name" = EXCLUDED."name",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "currencycode", "name", "modifieddate""""
    .updateReturning(CurrencyRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: Iterator[CurrencyRow])(using c: Connection): List[CurrencyRow] = {
    sql"""insert into "sales"."currency"("currencycode", "name", "modifieddate")
    values (?::bpchar, ?::varchar, ?::timestamp)
    on conflict ("currencycode")
    do update set
      "name" = EXCLUDED."name",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "currencycode", "name", "modifieddate""""
      .updateManyReturning(CurrencyRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[CurrencyRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    sql"""create temporary table currency_TEMP (like "sales"."currency") on commit drop""".update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy currency_TEMP("currencycode", "name", "modifieddate") from stdin""", batchSize, unsaved.toJavaIterator, c, CurrencyRow.pgText): @scala.annotation.nowarn
    return sql"""insert into "sales"."currency"("currencycode", "name", "modifieddate")
    select * from currency_TEMP
    on conflict ("currencycode")
    do update set
      "name" = EXCLUDED."name",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table currency_TEMP;""".update().runUnchecked(c)
  }
}