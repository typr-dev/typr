/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.specialoffer

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class SpecialofferRepoMock(
  toRow: SpecialofferRowUnsaved => SpecialofferRow,
  map: scala.collection.mutable.Map[SpecialofferId, SpecialofferRow] = scala.collection.mutable.Map.empty[SpecialofferId, SpecialofferRow]
) extends SpecialofferRepo {
  override def delete: DeleteBuilder[SpecialofferFields, SpecialofferRow] = DeleteBuilderMock(SpecialofferFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.specialofferid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(specialofferid: SpecialofferId)(using c: Connection): Boolean = map.remove(specialofferid).isDefined

  override def deleteByIds(specialofferids: Array[SpecialofferId])(using c: Connection): Int = {
    var count = 0
    specialofferids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: SpecialofferRow)(using c: Connection): SpecialofferRow = {
    if (map.contains(unsaved.specialofferid)) {
      throw new RuntimeException(s"id ${unsaved.specialofferid} already exists")
    }
    map.put(unsaved.specialofferid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: SpecialofferRowUnsaved)(using c: Connection): SpecialofferRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[SpecialofferRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.specialofferid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[SpecialofferRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.specialofferid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[SpecialofferFields, SpecialofferRow] = SelectBuilderMock(SpecialofferFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[SpecialofferRow] = map.values.toList

  override def selectById(specialofferid: SpecialofferId)(using c: Connection): Option[SpecialofferRow] = map.get(specialofferid)

  override def selectByIds(specialofferids: Array[SpecialofferId])(using c: Connection): List[SpecialofferRow] = specialofferids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(specialofferids: Array[SpecialofferId])(using c: Connection): Map[SpecialofferId, SpecialofferRow] = selectByIds(specialofferids)(using c).map(x => (((row: SpecialofferRow) => row.specialofferid).apply(x), x)).toMap

  override def update: UpdateBuilder[SpecialofferFields, SpecialofferRow] = UpdateBuilderMock(SpecialofferFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: SpecialofferRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.specialofferid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.specialofferid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: SpecialofferRow)(using c: Connection): SpecialofferRow = {
    map.put(unsaved.specialofferid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[SpecialofferRow])(using c: Connection): List[SpecialofferRow] = {
    unsaved.map { row =>
      map.put(row.specialofferid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[SpecialofferRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.specialofferid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}