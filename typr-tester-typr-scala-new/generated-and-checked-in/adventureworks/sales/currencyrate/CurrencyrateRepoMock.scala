/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.currencyrate

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class CurrencyrateRepoMock(
  toRow: CurrencyrateRowUnsaved => CurrencyrateRow,
  map: scala.collection.mutable.Map[CurrencyrateId, CurrencyrateRow] = scala.collection.mutable.Map.empty[CurrencyrateId, CurrencyrateRow]
) extends CurrencyrateRepo {
  override def delete: DeleteBuilder[CurrencyrateFields, CurrencyrateRow] = DeleteBuilderMock(CurrencyrateFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.currencyrateid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(currencyrateid: CurrencyrateId)(using c: Connection): Boolean = map.remove(currencyrateid).isDefined

  override def deleteByIds(currencyrateids: Array[CurrencyrateId])(using c: Connection): Int = {
    var count = 0
    currencyrateids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: CurrencyrateRow)(using c: Connection): CurrencyrateRow = {
    if (map.contains(unsaved.currencyrateid)) {
      throw new RuntimeException(s"id ${unsaved.currencyrateid} already exists")
    }
    map.put(unsaved.currencyrateid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: CurrencyrateRowUnsaved)(using c: Connection): CurrencyrateRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[CurrencyrateRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.currencyrateid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[CurrencyrateRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.currencyrateid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[CurrencyrateFields, CurrencyrateRow] = SelectBuilderMock(CurrencyrateFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[CurrencyrateRow] = map.values.toList

  override def selectById(currencyrateid: CurrencyrateId)(using c: Connection): Option[CurrencyrateRow] = map.get(currencyrateid)

  override def selectByIds(currencyrateids: Array[CurrencyrateId])(using c: Connection): List[CurrencyrateRow] = currencyrateids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(currencyrateids: Array[CurrencyrateId])(using c: Connection): Map[CurrencyrateId, CurrencyrateRow] = selectByIds(currencyrateids)(using c).map(x => (((row: CurrencyrateRow) => row.currencyrateid).apply(x), x)).toMap

  override def update: UpdateBuilder[CurrencyrateFields, CurrencyrateRow] = UpdateBuilderMock(CurrencyrateFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: CurrencyrateRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.currencyrateid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.currencyrateid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: CurrencyrateRow)(using c: Connection): CurrencyrateRow = {
    map.put(unsaved.currencyrateid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[CurrencyrateRow])(using c: Connection): List[CurrencyrateRow] = {
    unsaved.map { row =>
      map.put(row.currencyrateid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[CurrencyrateRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.currencyrateid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}