/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.identity_test

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class IdentityTestRepoMock(
  toRow: IdentityTestRowUnsaved => IdentityTestRow,
  map: scala.collection.mutable.Map[IdentityTestId, IdentityTestRow] = scala.collection.mutable.Map.empty[IdentityTestId, IdentityTestRow]
) extends IdentityTestRepo {
  override def delete: DeleteBuilder[IdentityTestFields, IdentityTestRow] = DeleteBuilderMock(IdentityTestFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.name, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(name: IdentityTestId)(using c: Connection): Boolean = map.remove(name).isDefined

  override def deleteByIds(names: Array[IdentityTestId])(using c: Connection): Int = {
    var count = 0
    names.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: IdentityTestRow)(using c: Connection): IdentityTestRow = {
    if (map.contains(unsaved.name)) {
      throw new RuntimeException(s"id ${unsaved.name} already exists")
    }
    map.put(unsaved.name, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: IdentityTestRowUnsaved)(using c: Connection): IdentityTestRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[IdentityTestRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.name, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[IdentityTestRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.name, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[IdentityTestFields, IdentityTestRow] = SelectBuilderMock(IdentityTestFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[IdentityTestRow] = map.values.toList

  override def selectById(name: IdentityTestId)(using c: Connection): Option[IdentityTestRow] = map.get(name)

  override def selectByIds(names: Array[IdentityTestId])(using c: Connection): List[IdentityTestRow] = names.flatMap(map.get(_)).toList

  override def selectByIdsTracked(names: Array[IdentityTestId])(using c: Connection): Map[IdentityTestId, IdentityTestRow] = selectByIds(names)(using c).map(x => (((row: IdentityTestRow) => row.name).apply(x), x)).toMap

  override def update: UpdateBuilder[IdentityTestFields, IdentityTestRow] = UpdateBuilderMock(IdentityTestFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: IdentityTestRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.name).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.name, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: IdentityTestRow)(using c: Connection): IdentityTestRow = {
    map.put(unsaved.name, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[IdentityTestRow])(using c: Connection): List[IdentityTestRow] = {
    unsaved.map { row =>
      map.put(row.name, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[IdentityTestRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.name, row): @scala.annotation.nowarn
      1
    }.sum
  }
}