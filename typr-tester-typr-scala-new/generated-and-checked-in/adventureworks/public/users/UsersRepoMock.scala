/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.users

import java.lang.RuntimeException
import java.sql.Connection
import typr.data.Unknown
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class UsersRepoMock(
  toRow: UsersRowUnsaved => UsersRow,
  map: scala.collection.mutable.Map[UsersId, UsersRow] = scala.collection.mutable.Map.empty[UsersId, UsersRow]
) extends UsersRepo {
  override def delete: DeleteBuilder[UsersFields, UsersRow] = DeleteBuilderMock(UsersFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.userId, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(userId: UsersId)(using c: Connection): Boolean = map.remove(userId).isDefined

  override def deleteByIds(userIds: Array[UsersId])(using c: Connection): Int = {
    var count = 0
    userIds.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: UsersRow)(using c: Connection): UsersRow = {
    if (map.contains(unsaved.userId)) {
      throw new RuntimeException(s"id ${unsaved.userId} already exists")
    }
    map.put(unsaved.userId, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: UsersRowUnsaved)(using c: Connection): UsersRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[UsersRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.userId, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[UsersRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.userId, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[UsersFields, UsersRow] = SelectBuilderMock(UsersFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[UsersRow] = map.values.toList

  override def selectById(userId: UsersId)(using c: Connection): Option[UsersRow] = map.get(userId)

  override def selectByIds(userIds: Array[UsersId])(using c: Connection): List[UsersRow] = userIds.flatMap(map.get(_)).toList

  override def selectByIdsTracked(userIds: Array[UsersId])(using c: Connection): Map[UsersId, UsersRow] = selectByIds(userIds)(using c).map(x => (((row: UsersRow) => row.userId).apply(x), x)).toMap

  override def selectByUniqueEmail(email: Unknown)(using c: Connection): Option[UsersRow] = map.values.toList.find(v => (email == v.email))

  override def update: UpdateBuilder[UsersFields, UsersRow] = UpdateBuilderMock(UsersFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: UsersRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.userId).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.userId, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: UsersRow)(using c: Connection): UsersRow = {
    map.put(unsaved.userId, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[UsersRow])(using c: Connection): List[UsersRow] = {
    unsaved.map { row =>
      map.put(row.userId, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[UsersRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.userId, row): @scala.annotation.nowarn
      1
    }.sum
  }
}