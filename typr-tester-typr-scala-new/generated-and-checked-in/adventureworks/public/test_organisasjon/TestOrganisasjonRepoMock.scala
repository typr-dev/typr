/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public.test_organisasjon

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class TestOrganisasjonRepoMock(map: scala.collection.mutable.Map[TestOrganisasjonId, TestOrganisasjonRow] = scala.collection.mutable.Map.empty[TestOrganisasjonId, TestOrganisasjonRow]) extends TestOrganisasjonRepo {
  override def delete: DeleteBuilder[TestOrganisasjonFields, TestOrganisasjonRow] = DeleteBuilderMock(TestOrganisasjonFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.organisasjonskode, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(organisasjonskode: TestOrganisasjonId)(using c: Connection): Boolean = map.remove(organisasjonskode).isDefined

  override def deleteByIds(organisasjonskodes: Array[TestOrganisasjonId])(using c: Connection): Int = {
    var count = 0
    organisasjonskodes.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: TestOrganisasjonRow)(using c: Connection): TestOrganisasjonRow = {
    if (map.contains(unsaved.organisasjonskode)) {
      throw new RuntimeException(s"id ${unsaved.organisasjonskode} already exists")
    }
    map.put(unsaved.organisasjonskode, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insertStreaming(
    unsaved: Iterator[TestOrganisasjonRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.organisasjonskode, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[TestOrganisasjonFields, TestOrganisasjonRow] = SelectBuilderMock(TestOrganisasjonFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[TestOrganisasjonRow] = map.values.toList

  override def selectById(organisasjonskode: TestOrganisasjonId)(using c: Connection): Option[TestOrganisasjonRow] = map.get(organisasjonskode)

  override def selectByIds(organisasjonskodes: Array[TestOrganisasjonId])(using c: Connection): List[TestOrganisasjonRow] = organisasjonskodes.flatMap(map.get(_)).toList

  override def selectByIdsTracked(organisasjonskodes: Array[TestOrganisasjonId])(using c: Connection): Map[TestOrganisasjonId, TestOrganisasjonRow] = selectByIds(organisasjonskodes)(using c).map(x => (((row: TestOrganisasjonRow) => row.organisasjonskode).apply(x), x)).toMap

  override def update: UpdateBuilder[TestOrganisasjonFields, TestOrganisasjonRow] = UpdateBuilderMock(TestOrganisasjonFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def upsert(unsaved: TestOrganisasjonRow)(using c: Connection): TestOrganisasjonRow = {
    map.put(unsaved.organisasjonskode, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[TestOrganisasjonRow])(using c: Connection): List[TestOrganisasjonRow] = {
    unsaved.map { row =>
      map.put(row.organisasjonskode, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[TestOrganisasjonRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.organisasjonskode, row): @scala.annotation.nowarn
      1
    }.sum
  }
}