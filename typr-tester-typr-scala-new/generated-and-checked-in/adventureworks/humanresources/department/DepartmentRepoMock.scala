/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.humanresources.department

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class DepartmentRepoMock(
  toRow: DepartmentRowUnsaved => DepartmentRow,
  map: scala.collection.mutable.Map[DepartmentId, DepartmentRow] = scala.collection.mutable.Map.empty[DepartmentId, DepartmentRow]
) extends DepartmentRepo {
  override def delete: DeleteBuilder[DepartmentFields, DepartmentRow] = DeleteBuilderMock(DepartmentFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.departmentid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(departmentid: DepartmentId)(using c: Connection): Boolean = map.remove(departmentid).isDefined

  override def deleteByIds(departmentids: Array[DepartmentId])(using c: Connection): Int = {
    var count = 0
    departmentids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: DepartmentRow)(using c: Connection): DepartmentRow = {
    if (map.contains(unsaved.departmentid)) {
      throw new RuntimeException(s"id ${unsaved.departmentid} already exists")
    }
    map.put(unsaved.departmentid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: DepartmentRowUnsaved)(using c: Connection): DepartmentRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[DepartmentRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.departmentid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[DepartmentRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.departmentid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[DepartmentFields, DepartmentRow] = SelectBuilderMock(DepartmentFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[DepartmentRow] = map.values.toList

  override def selectById(departmentid: DepartmentId)(using c: Connection): Option[DepartmentRow] = map.get(departmentid)

  override def selectByIds(departmentids: Array[DepartmentId])(using c: Connection): List[DepartmentRow] = departmentids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(departmentids: Array[DepartmentId])(using c: Connection): Map[DepartmentId, DepartmentRow] = selectByIds(departmentids)(using c).map(x => (((row: DepartmentRow) => row.departmentid).apply(x), x)).toMap

  override def update: UpdateBuilder[DepartmentFields, DepartmentRow] = UpdateBuilderMock(DepartmentFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: DepartmentRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.departmentid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.departmentid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: DepartmentRow)(using c: Connection): DepartmentRow = {
    map.put(unsaved.departmentid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[DepartmentRow])(using c: Connection): List[DepartmentRow] = {
    unsaved.map { row =>
      map.put(row.departmentid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[DepartmentRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.departmentid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}