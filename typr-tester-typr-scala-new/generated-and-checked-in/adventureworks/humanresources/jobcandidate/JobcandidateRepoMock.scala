/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.humanresources.jobcandidate

import java.lang.RuntimeException
import java.sql.Connection
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.DeleteBuilderMock
import typr.scaladsl.DeleteParams
import typr.scaladsl.SelectBuilder
import typr.scaladsl.SelectBuilderMock
import typr.scaladsl.SelectParams
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.UpdateBuilderMock
import typr.scaladsl.UpdateParams

case class JobcandidateRepoMock(
  toRow: JobcandidateRowUnsaved => JobcandidateRow,
  map: scala.collection.mutable.Map[JobcandidateId, JobcandidateRow] = scala.collection.mutable.Map.empty[JobcandidateId, JobcandidateRow]
) extends JobcandidateRepo {
  override def delete: DeleteBuilder[JobcandidateFields, JobcandidateRow] = DeleteBuilderMock(JobcandidateFields.structure, () => map.values.toList, DeleteParams.empty(), row => row.jobcandidateid, id => map.remove(id): @scala.annotation.nowarn)

  override def deleteById(jobcandidateid: JobcandidateId)(using c: Connection): Boolean = map.remove(jobcandidateid).isDefined

  override def deleteByIds(jobcandidateids: Array[JobcandidateId])(using c: Connection): Int = {
    var count = 0
    jobcandidateids.foreach { id => if (map.remove(id).isDefined) {
      count = count + 1
    } }
    return count
  }

  override def insert(unsaved: JobcandidateRow)(using c: Connection): JobcandidateRow = {
    if (map.contains(unsaved.jobcandidateid)) {
      throw new RuntimeException(s"id ${unsaved.jobcandidateid} already exists")
    }
    map.put(unsaved.jobcandidateid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def insert(unsaved: JobcandidateRowUnsaved)(using c: Connection): JobcandidateRow = insert(toRow(unsaved))(using c)

  override def insertStreaming(
    unsaved: Iterator[JobcandidateRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { row =>
      map.put(row.jobcandidateid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[JobcandidateRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = {
    unsaved.map { unsavedRow =>
      val row = toRow(unsavedRow)
      map.put(row.jobcandidateid, row): @scala.annotation.nowarn
      1L
    }.sum
  }

  override def select: SelectBuilder[JobcandidateFields, JobcandidateRow] = SelectBuilderMock(JobcandidateFields.structure, () => map.values.toList, SelectParams.empty())

  override def selectAll(using c: Connection): List[JobcandidateRow] = map.values.toList

  override def selectById(jobcandidateid: JobcandidateId)(using c: Connection): Option[JobcandidateRow] = map.get(jobcandidateid)

  override def selectByIds(jobcandidateids: Array[JobcandidateId])(using c: Connection): List[JobcandidateRow] = jobcandidateids.flatMap(map.get(_)).toList

  override def selectByIdsTracked(jobcandidateids: Array[JobcandidateId])(using c: Connection): Map[JobcandidateId, JobcandidateRow] = selectByIds(jobcandidateids)(using c).map(x => (((row: JobcandidateRow) => row.jobcandidateid).apply(x), x)).toMap

  override def update: UpdateBuilder[JobcandidateFields, JobcandidateRow] = UpdateBuilderMock(JobcandidateFields.structure, () => map.values.toList, UpdateParams.empty(), row => row)

  override def update(row: JobcandidateRow)(using c: Connection): Boolean = {
    val shouldUpdate = map.get(row.jobcandidateid).filter(oldRow => (oldRow != row)).isDefined
    if (shouldUpdate) {
      map.put(row.jobcandidateid, row): @scala.annotation.nowarn
    }
    return shouldUpdate
  }

  override def upsert(unsaved: JobcandidateRow)(using c: Connection): JobcandidateRow = {
    map.put(unsaved.jobcandidateid, unsaved): @scala.annotation.nowarn
    return unsaved
  }

  override def upsertBatch(unsaved: Iterator[JobcandidateRow])(using c: Connection): List[JobcandidateRow] = {
    unsaved.map { row =>
      map.put(row.jobcandidateid, row): @scala.annotation.nowarn
      row
    }.toList
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[JobcandidateRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    unsaved.map { row =>
      map.put(row.jobcandidateid, row): @scala.annotation.nowarn
      1
    }.sum
  }
}