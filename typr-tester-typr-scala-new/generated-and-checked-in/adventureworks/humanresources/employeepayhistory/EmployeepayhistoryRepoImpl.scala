/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.humanresources.employeepayhistory

import adventureworks.person.businessentity.BusinessentityId
import java.sql.Connection
import java.time.LocalDateTime
import scala.collection.mutable.ListBuffer
import typr.runtime.PgTypes
import typr.runtime.streamingInsert
import typr.scaladsl.DeleteBuilder
import typr.scaladsl.Dialect
import typr.scaladsl.Fragment
import typr.scaladsl.ScalaDbTypes
import typr.scaladsl.ScalaIteratorOps
import typr.scaladsl.SelectBuilder
import typr.scaladsl.UpdateBuilder
import typr.scaladsl.Fragment.sql

class EmployeepayhistoryRepoImpl extends EmployeepayhistoryRepo {
  override def delete: DeleteBuilder[EmployeepayhistoryFields, EmployeepayhistoryRow] = DeleteBuilder.of(""""humanresources"."employeepayhistory"""", EmployeepayhistoryFields.structure, Dialect.POSTGRESQL)

  override def deleteById(compositeId: EmployeepayhistoryId)(using c: Connection): Boolean = sql"""delete from "humanresources"."employeepayhistory" where "businessentityid" = ${Fragment.encode(BusinessentityId.pgType, compositeId.businessentityid)} AND "ratechangedate" = ${Fragment.encode(PgTypes.timestamp, compositeId.ratechangedate)}""".update().runUnchecked(c) > 0

  override def deleteByIds(compositeIds: Array[EmployeepayhistoryId])(using c: Connection): Int = {
    val businessentityid: Array[BusinessentityId] = compositeIds.map(_.businessentityid)
    val ratechangedate: Array[LocalDateTime] = compositeIds.map(_.ratechangedate)
    return sql"""delete
    from "humanresources"."employeepayhistory"
    where ("businessentityid", "ratechangedate")
    in (select * from unnest(${Fragment.encode(BusinessentityId.pgTypeArray, businessentityid)}, ${Fragment.encode(PgTypes.timestampArray, ratechangedate)}))
    """.update().runUnchecked(c)
  }

  override def insert(unsaved: EmployeepayhistoryRow)(using c: Connection): EmployeepayhistoryRow = {
  sql"""insert into "humanresources"."employeepayhistory"("businessentityid", "ratechangedate", "rate", "payfrequency", "modifieddate")
    values (${Fragment.encode(BusinessentityId.pgType, unsaved.businessentityid)}::int4, ${Fragment.encode(PgTypes.timestamp, unsaved.ratechangedate)}::timestamp, ${Fragment.encode(ScalaDbTypes.PgTypes.numeric, unsaved.rate)}::numeric, ${Fragment.encode(ScalaDbTypes.PgTypes.int2, unsaved.payfrequency)}::int2, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    RETURNING "businessentityid", "ratechangedate", "rate", "payfrequency", "modifieddate"
    """
    .updateReturning(EmployeepayhistoryRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insert(unsaved: EmployeepayhistoryRowUnsaved)(using c: Connection): EmployeepayhistoryRow = {
    val columns: ListBuffer[Fragment] = ListBuffer()
    val values: ListBuffer[Fragment] = ListBuffer()
    columns.addOne(Fragment.lit(""""businessentityid"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(BusinessentityId.pgType, unsaved.businessentityid)}::int4"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""ratechangedate"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(PgTypes.timestamp, unsaved.ratechangedate)}::timestamp"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""rate"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(ScalaDbTypes.PgTypes.numeric, unsaved.rate)}::numeric"): @scala.annotation.nowarn
    columns.addOne(Fragment.lit(""""payfrequency"""")): @scala.annotation.nowarn
    values.addOne(sql"${Fragment.encode(ScalaDbTypes.PgTypes.int2, unsaved.payfrequency)}::int2"): @scala.annotation.nowarn
    unsaved.modifieddate.visit(
      {  },
      value => { columns.addOne(Fragment.lit(""""modifieddate"""")): @scala.annotation.nowarn; values.addOne(sql"${Fragment.encode(PgTypes.timestamp, value)}::timestamp"): @scala.annotation.nowarn }
    );
    val q: Fragment = {
      sql"""insert into "humanresources"."employeepayhistory"(${Fragment.comma(columns)})
      values (${Fragment.comma(values)})
      RETURNING "businessentityid", "ratechangedate", "rate", "payfrequency", "modifieddate"
      """
    }
    return q.updateReturning(EmployeepayhistoryRow.`_rowParser`.exactlyOne()).runUnchecked(c)
  }

  override def insertStreaming(
    unsaved: Iterator[EmployeepayhistoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "humanresources"."employeepayhistory"("businessentityid", "ratechangedate", "rate", "payfrequency", "modifieddate") FROM STDIN""", batchSize, unsaved.toJavaIterator, c, EmployeepayhistoryRow.pgText)

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(
    unsaved: Iterator[EmployeepayhistoryRowUnsaved],
    batchSize: Int = 10000
  )(using c: Connection): Long = streamingInsert.insertUnchecked(s"""COPY "humanresources"."employeepayhistory"("businessentityid", "ratechangedate", "rate", "payfrequency", "modifieddate") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved.toJavaIterator, c, EmployeepayhistoryRowUnsaved.pgText)

  override def select: SelectBuilder[EmployeepayhistoryFields, EmployeepayhistoryRow] = SelectBuilder.of(""""humanresources"."employeepayhistory"""", EmployeepayhistoryFields.structure, EmployeepayhistoryRow.`_rowParser`, Dialect.POSTGRESQL)

  override def selectAll(using c: Connection): List[EmployeepayhistoryRow] = {
    sql"""select "businessentityid", "ratechangedate", "rate", "payfrequency", "modifieddate"
    from "humanresources"."employeepayhistory"
    """.query(EmployeepayhistoryRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectById(compositeId: EmployeepayhistoryId)(using c: Connection): Option[EmployeepayhistoryRow] = {
    sql"""select "businessentityid", "ratechangedate", "rate", "payfrequency", "modifieddate"
    from "humanresources"."employeepayhistory"
    where "businessentityid" = ${Fragment.encode(BusinessentityId.pgType, compositeId.businessentityid)} AND "ratechangedate" = ${Fragment.encode(PgTypes.timestamp, compositeId.ratechangedate)}""".query(EmployeepayhistoryRow.`_rowParser`.first()).runUnchecked(c)
  }

  override def selectByIds(compositeIds: Array[EmployeepayhistoryId])(using c: Connection): List[EmployeepayhistoryRow] = {
    val businessentityid: Array[BusinessentityId] = compositeIds.map(_.businessentityid)
    val ratechangedate: Array[LocalDateTime] = compositeIds.map(_.ratechangedate)
    return sql"""select "businessentityid", "ratechangedate", "rate", "payfrequency", "modifieddate"
    from "humanresources"."employeepayhistory"
    where ("businessentityid", "ratechangedate")
    in (select * from unnest(${Fragment.encode(BusinessentityId.pgTypeArray, businessentityid)}, ${Fragment.encode(PgTypes.timestampArray, ratechangedate)}))
    """.query(EmployeepayhistoryRow.`_rowParser`.all()).runUnchecked(c)
  }

  override def selectByIdsTracked(compositeIds: Array[EmployeepayhistoryId])(using c: Connection): Map[EmployeepayhistoryId, EmployeepayhistoryRow] = {
    val ret: scala.collection.mutable.Map[EmployeepayhistoryId, EmployeepayhistoryRow] = scala.collection.mutable.Map.empty[EmployeepayhistoryId, EmployeepayhistoryRow]
    selectByIds(compositeIds)(using c).foreach(row => ret.put(row.compositeId, row): @scala.annotation.nowarn)
    return ret.toMap
  }

  override def update: UpdateBuilder[EmployeepayhistoryFields, EmployeepayhistoryRow] = UpdateBuilder.of(""""humanresources"."employeepayhistory"""", EmployeepayhistoryFields.structure, EmployeepayhistoryRow.`_rowParser`, Dialect.POSTGRESQL)

  override def update(row: EmployeepayhistoryRow)(using c: Connection): Boolean = {
    val compositeId: EmployeepayhistoryId = row.compositeId
    return sql"""update "humanresources"."employeepayhistory"
    set "rate" = ${Fragment.encode(ScalaDbTypes.PgTypes.numeric, row.rate)}::numeric,
    "payfrequency" = ${Fragment.encode(ScalaDbTypes.PgTypes.int2, row.payfrequency)}::int2,
    "modifieddate" = ${Fragment.encode(PgTypes.timestamp, row.modifieddate)}::timestamp
    where "businessentityid" = ${Fragment.encode(BusinessentityId.pgType, compositeId.businessentityid)} AND "ratechangedate" = ${Fragment.encode(PgTypes.timestamp, compositeId.ratechangedate)}""".update().runUnchecked(c) > 0
  }

  override def upsert(unsaved: EmployeepayhistoryRow)(using c: Connection): EmployeepayhistoryRow = {
  sql"""insert into "humanresources"."employeepayhistory"("businessentityid", "ratechangedate", "rate", "payfrequency", "modifieddate")
    values (${Fragment.encode(BusinessentityId.pgType, unsaved.businessentityid)}::int4, ${Fragment.encode(PgTypes.timestamp, unsaved.ratechangedate)}::timestamp, ${Fragment.encode(ScalaDbTypes.PgTypes.numeric, unsaved.rate)}::numeric, ${Fragment.encode(ScalaDbTypes.PgTypes.int2, unsaved.payfrequency)}::int2, ${Fragment.encode(PgTypes.timestamp, unsaved.modifieddate)}::timestamp)
    on conflict ("businessentityid", "ratechangedate")
    do update set
      "rate" = EXCLUDED."rate",
    "payfrequency" = EXCLUDED."payfrequency",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "businessentityid", "ratechangedate", "rate", "payfrequency", "modifieddate""""
    .updateReturning(EmployeepayhistoryRow.`_rowParser`.exactlyOne())
    .runUnchecked(c)
  }

  override def upsertBatch(unsaved: Iterator[EmployeepayhistoryRow])(using c: Connection): List[EmployeepayhistoryRow] = {
    sql"""insert into "humanresources"."employeepayhistory"("businessentityid", "ratechangedate", "rate", "payfrequency", "modifieddate")
    values (?::int4, ?::timestamp, ?::numeric, ?::int2, ?::timestamp)
    on conflict ("businessentityid", "ratechangedate")
    do update set
      "rate" = EXCLUDED."rate",
    "payfrequency" = EXCLUDED."payfrequency",
    "modifieddate" = EXCLUDED."modifieddate"
    returning "businessentityid", "ratechangedate", "rate", "payfrequency", "modifieddate""""
      .updateManyReturning(EmployeepayhistoryRow.`_rowParser`, unsaved)
    .runUnchecked(c)
  }

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(
    unsaved: Iterator[EmployeepayhistoryRow],
    batchSize: Int = 10000
  )(using c: Connection): Int = {
    sql"""create temporary table employeepayhistory_TEMP (like "humanresources"."employeepayhistory") on commit drop""".update().runUnchecked(c): @scala.annotation.nowarn
    streamingInsert.insertUnchecked(s"""copy employeepayhistory_TEMP("businessentityid", "ratechangedate", "rate", "payfrequency", "modifieddate") from stdin""", batchSize, unsaved.toJavaIterator, c, EmployeepayhistoryRow.pgText): @scala.annotation.nowarn
    return sql"""insert into "humanresources"."employeepayhistory"("businessentityid", "ratechangedate", "rate", "payfrequency", "modifieddate")
    select * from employeepayhistory_TEMP
    on conflict ("businessentityid", "ratechangedate")
    do update set
      "rate" = EXCLUDED."rate",
    "payfrequency" = EXCLUDED."payfrequency",
    "modifieddate" = EXCLUDED."modifieddate"
    ;
    drop table employeepayhistory_TEMP;""".update().runUnchecked(c)
  }
}