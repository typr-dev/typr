/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public_.issue142_2;

import adventureworks.public_.issue142.Issue142Id;
import java.lang.RuntimeException;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record Issue1422RepoMock(HashMap<Issue142Id, Issue1422Row> map) implements Issue1422Repo {
  public Issue1422RepoMock() {
    this(new HashMap<Issue142Id, Issue1422Row>());
  };

  public Issue1422RepoMock withMap(HashMap<Issue142Id, Issue1422Row> map) {
    return new Issue1422RepoMock(map);
  };

  @Override
  public DeleteBuilder<Issue1422Fields, Issue1422Row> delete() {
    return new DeleteBuilderMock<>(Issue1422Fields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.tabellkode(), id -> map.remove(id));
  };

  @Override
  public Boolean deleteById(
    Issue142Id tabellkode,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(tabellkode)).isPresent();
  };

  @Override
  public Integer deleteByIds(
    Issue142Id[] tabellkodes,
    Connection c
  ) {
    var count = 0;
    for (var id : tabellkodes) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    };
    };
    return count;
  };

  @Override
  public Issue1422Row insert(
    Issue1422Row unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.tabellkode())) {
      throw new RuntimeException(str("id $unsaved.tabellkode() already exists"));
    };
    map.put(unsaved.tabellkode(), unsaved);
    return unsaved;
  };

  @Override
  public Long insertStreaming(
    Iterator<Issue1422Row> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.tabellkode(), row);
      count = count + 1L;
    };
    return count;
  };

  @Override
  public SelectBuilder<Issue1422Fields, Issue1422Row> select() {
    return new SelectBuilderMock<>(Issue1422Fields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  @Override
  public List<Issue1422Row> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  @Override
  public Optional<Issue1422Row> selectById(
    Issue142Id tabellkode,
    Connection c
  ) {
    return Optional.ofNullable(map.get(tabellkode));
  };

  @Override
  public List<Issue1422Row> selectByIds(
    Issue142Id[] tabellkodes,
    Connection c
  ) {
    var result = new ArrayList<Issue1422Row>();
    for (var id : tabellkodes) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    };
    };
    return result;
  };

  @Override
  public Map<Issue142Id, Issue1422Row> selectByIdsTracked(
    Issue142Id[] tabellkodes,
    Connection c
  ) {
    return selectByIds(tabellkodes, c).stream().collect(Collectors.toMap((Issue1422Row row) -> row.tabellkode(), Function.identity()));
  };

  @Override
  public UpdateBuilder<Issue1422Fields, Issue1422Row> update() {
    return new UpdateBuilderMock<>(Issue1422Fields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  @Override
  public Issue1422Row upsert(
    Issue1422Row unsaved,
    Connection c
  ) {
    map.put(unsaved.tabellkode(), unsaved);
    return unsaved;
  };

  @Override
  public List<Issue1422Row> upsertBatch(
    Iterator<Issue1422Row> unsaved,
    Connection c
  ) {
    var result = new ArrayList<Issue1422Row>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.tabellkode(), row);
      result.add(row);
    };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
    Iterator<Issue1422Row> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.tabellkode(), row);
      count = count + 1;
    };
    return count;
  };
}