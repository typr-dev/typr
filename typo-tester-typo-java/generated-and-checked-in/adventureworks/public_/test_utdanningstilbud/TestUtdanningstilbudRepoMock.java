/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.public_.test_utdanningstilbud;

import java.lang.RuntimeException;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record TestUtdanningstilbudRepoMock(HashMap<TestUtdanningstilbudId, TestUtdanningstilbudRow> map) implements TestUtdanningstilbudRepo {
  public TestUtdanningstilbudRepoMock() {
    this(new HashMap<TestUtdanningstilbudId, TestUtdanningstilbudRow>());
  };

  public TestUtdanningstilbudRepoMock withMap(HashMap<TestUtdanningstilbudId, TestUtdanningstilbudRow> map) {
    return new TestUtdanningstilbudRepoMock(map);
  };

  @Override
  public DeleteBuilder<TestUtdanningstilbudFields, TestUtdanningstilbudRow> delete() {
    return new DeleteBuilderMock<>(TestUtdanningstilbudFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.compositeId(), id -> map.remove(id));
  };

  @Override
  public Boolean deleteById(
    TestUtdanningstilbudId compositeId,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(compositeId)).isPresent();
  };

  @Override
  public Integer deleteByIds(
    TestUtdanningstilbudId[] compositeIds,
    Connection c
  ) {
    var count = 0;
    for (var id : compositeIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    };
    };
    return count;
  };

  @Override
  public TestUtdanningstilbudRow insert(
    TestUtdanningstilbudRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.compositeId())) {
      throw new RuntimeException(str("id $unsaved.compositeId() already exists"));
    };
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  };

  @Override
  public Long insertStreaming(
    Iterator<TestUtdanningstilbudRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      count = count + 1L;
    };
    return count;
  };

  @Override
  public SelectBuilder<TestUtdanningstilbudFields, TestUtdanningstilbudRow> select() {
    return new SelectBuilderMock<>(TestUtdanningstilbudFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  @Override
  public List<TestUtdanningstilbudRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  @Override
  public Optional<TestUtdanningstilbudRow> selectById(
    TestUtdanningstilbudId compositeId,
    Connection c
  ) {
    return Optional.ofNullable(map.get(compositeId));
  };

  @Override
  public List<TestUtdanningstilbudRow> selectByIds(
    TestUtdanningstilbudId[] compositeIds,
    Connection c
  ) {
    var result = new ArrayList<TestUtdanningstilbudRow>();
    for (var id : compositeIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    };
    };
    return result;
  };

  @Override
  public Map<TestUtdanningstilbudId, TestUtdanningstilbudRow> selectByIdsTracked(
    TestUtdanningstilbudId[] compositeIds,
    Connection c
  ) {
    return selectByIds(compositeIds, c).stream().collect(Collectors.toMap((TestUtdanningstilbudRow row) -> row.compositeId(), Function.identity()));
  };

  @Override
  public UpdateBuilder<TestUtdanningstilbudFields, TestUtdanningstilbudRow> update() {
    return new UpdateBuilderMock<>(TestUtdanningstilbudFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  @Override
  public TestUtdanningstilbudRow upsert(
    TestUtdanningstilbudRow unsaved,
    Connection c
  ) {
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  };

  @Override
  public List<TestUtdanningstilbudRow> upsertBatch(
    Iterator<TestUtdanningstilbudRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<TestUtdanningstilbudRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      result.add(row);
    };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
    Iterator<TestUtdanningstilbudRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      count = count + 1;
    };
    return count;
  };
}