/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.person.phonenumbertype;

import java.lang.RuntimeException;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record PhonenumbertypeRepoMock(
  java.util.function.Function<PhonenumbertypeRowUnsaved, PhonenumbertypeRow> toRow,
  HashMap<PhonenumbertypeId, PhonenumbertypeRow> map
) implements PhonenumbertypeRepo {
  public PhonenumbertypeRepoMock(java.util.function.Function<PhonenumbertypeRowUnsaved, PhonenumbertypeRow> toRow) {
    this(toRow, new HashMap<PhonenumbertypeId, PhonenumbertypeRow>());
  };

  public PhonenumbertypeRepoMock withToRow(java.util.function.Function<PhonenumbertypeRowUnsaved, PhonenumbertypeRow> toRow) {
    return new PhonenumbertypeRepoMock(toRow, map);
  };

  public PhonenumbertypeRepoMock withMap(HashMap<PhonenumbertypeId, PhonenumbertypeRow> map) {
    return new PhonenumbertypeRepoMock(toRow, map);
  };

  @Override
  public DeleteBuilder<PhonenumbertypeFields, PhonenumbertypeRow> delete() {
    return new DeleteBuilderMock<>(PhonenumbertypeFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.phonenumbertypeid(), id -> map.remove(id));
  };

  @Override
  public Boolean deleteById(
    PhonenumbertypeId phonenumbertypeid,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(phonenumbertypeid)).isPresent();
  };

  @Override
  public Integer deleteByIds(
    PhonenumbertypeId[] phonenumbertypeids,
    Connection c
  ) {
    var count = 0;
    for (var id : phonenumbertypeids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    };
    };
    return count;
  };

  @Override
  public PhonenumbertypeRow insert(
    PhonenumbertypeRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.phonenumbertypeid())) {
      throw new RuntimeException(str("id $unsaved.phonenumbertypeid() already exists"));
    };
    map.put(unsaved.phonenumbertypeid(), unsaved);
    return unsaved;
  };

  @Override
  public PhonenumbertypeRow insert(
    PhonenumbertypeRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  @Override
  public Long insertStreaming(
    Iterator<PhonenumbertypeRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.phonenumbertypeid(), row);
      count = count + 1L;
    };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  @Override
  public Long insertUnsavedStreaming(
    Iterator<PhonenumbertypeRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var unsavedRow = unsaved.next();
      var row = toRow.apply(unsavedRow);
      map.put(row.phonenumbertypeid(), row);
      count = count + 1L;
    };
    return count;
  };

  @Override
  public SelectBuilder<PhonenumbertypeFields, PhonenumbertypeRow> select() {
    return new SelectBuilderMock<>(PhonenumbertypeFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  @Override
  public List<PhonenumbertypeRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  @Override
  public Optional<PhonenumbertypeRow> selectById(
    PhonenumbertypeId phonenumbertypeid,
    Connection c
  ) {
    return Optional.ofNullable(map.get(phonenumbertypeid));
  };

  @Override
  public List<PhonenumbertypeRow> selectByIds(
    PhonenumbertypeId[] phonenumbertypeids,
    Connection c
  ) {
    var result = new ArrayList<PhonenumbertypeRow>();
    for (var id : phonenumbertypeids) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    };
    };
    return result;
  };

  @Override
  public Map<PhonenumbertypeId, PhonenumbertypeRow> selectByIdsTracked(
    PhonenumbertypeId[] phonenumbertypeids,
    Connection c
  ) {
    return selectByIds(phonenumbertypeids, c).stream().collect(Collectors.toMap((PhonenumbertypeRow row) -> row.phonenumbertypeid(), Function.identity()));
  };

  @Override
  public UpdateBuilder<PhonenumbertypeFields, PhonenumbertypeRow> update() {
    return new UpdateBuilderMock<>(PhonenumbertypeFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  @Override
  public Boolean update(
    PhonenumbertypeRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.phonenumbertypeid())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.phonenumbertypeid(), row);;
    };
    return shouldUpdate;
  };

  @Override
  public PhonenumbertypeRow upsert(
    PhonenumbertypeRow unsaved,
    Connection c
  ) {
    map.put(unsaved.phonenumbertypeid(), unsaved);
    return unsaved;
  };

  @Override
  public List<PhonenumbertypeRow> upsertBatch(
    Iterator<PhonenumbertypeRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<PhonenumbertypeRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.phonenumbertypeid(), row);
      result.add(row);
    };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
    Iterator<PhonenumbertypeRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.phonenumbertypeid(), row);
      count = count + 1;
    };
    return count;
  };
}