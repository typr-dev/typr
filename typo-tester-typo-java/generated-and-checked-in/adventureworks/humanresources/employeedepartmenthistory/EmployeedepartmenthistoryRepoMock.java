/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.humanresources.employeedepartmenthistory;

import java.lang.RuntimeException;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record EmployeedepartmenthistoryRepoMock(
  java.util.function.Function<EmployeedepartmenthistoryRowUnsaved, EmployeedepartmenthistoryRow> toRow,
  HashMap<EmployeedepartmenthistoryId, EmployeedepartmenthistoryRow> map
) implements EmployeedepartmenthistoryRepo {
  public EmployeedepartmenthistoryRepoMock(java.util.function.Function<EmployeedepartmenthistoryRowUnsaved, EmployeedepartmenthistoryRow> toRow) {
    this(toRow, new HashMap<EmployeedepartmenthistoryId, EmployeedepartmenthistoryRow>());
  };

  public EmployeedepartmenthistoryRepoMock withToRow(java.util.function.Function<EmployeedepartmenthistoryRowUnsaved, EmployeedepartmenthistoryRow> toRow) {
    return new EmployeedepartmenthistoryRepoMock(toRow, map);
  };

  public EmployeedepartmenthistoryRepoMock withMap(HashMap<EmployeedepartmenthistoryId, EmployeedepartmenthistoryRow> map) {
    return new EmployeedepartmenthistoryRepoMock(toRow, map);
  };

  @Override
  public DeleteBuilder<EmployeedepartmenthistoryFields, EmployeedepartmenthistoryRow> delete() {
    return new DeleteBuilderMock<>(EmployeedepartmenthistoryFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.compositeId(), id -> map.remove(id));
  };

  @Override
  public Boolean deleteById(
    EmployeedepartmenthistoryId compositeId,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(compositeId)).isPresent();
  };

  @Override
  public Integer deleteByIds(
    EmployeedepartmenthistoryId[] compositeIds,
    Connection c
  ) {
    var count = 0;
    for (var id : compositeIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    };
    };
    return count;
  };

  @Override
  public EmployeedepartmenthistoryRow insert(
    EmployeedepartmenthistoryRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.compositeId())) {
      throw new RuntimeException(str("id $unsaved.compositeId() already exists"));
    };
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  };

  @Override
  public EmployeedepartmenthistoryRow insert(
    EmployeedepartmenthistoryRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  @Override
  public Long insertStreaming(
    Iterator<EmployeedepartmenthistoryRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      count = count + 1L;
    };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  @Override
  public Long insertUnsavedStreaming(
    Iterator<EmployeedepartmenthistoryRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var unsavedRow = unsaved.next();
      var row = toRow.apply(unsavedRow);
      map.put(row.compositeId(), row);
      count = count + 1L;
    };
    return count;
  };

  @Override
  public SelectBuilder<EmployeedepartmenthistoryFields, EmployeedepartmenthistoryRow> select() {
    return new SelectBuilderMock<>(EmployeedepartmenthistoryFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  @Override
  public List<EmployeedepartmenthistoryRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  @Override
  public Optional<EmployeedepartmenthistoryRow> selectById(
    EmployeedepartmenthistoryId compositeId,
    Connection c
  ) {
    return Optional.ofNullable(map.get(compositeId));
  };

  @Override
  public List<EmployeedepartmenthistoryRow> selectByIds(
    EmployeedepartmenthistoryId[] compositeIds,
    Connection c
  ) {
    var result = new ArrayList<EmployeedepartmenthistoryRow>();
    for (var id : compositeIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    };
    };
    return result;
  };

  @Override
  public Map<EmployeedepartmenthistoryId, EmployeedepartmenthistoryRow> selectByIdsTracked(
    EmployeedepartmenthistoryId[] compositeIds,
    Connection c
  ) {
    return selectByIds(compositeIds, c).stream().collect(Collectors.toMap((EmployeedepartmenthistoryRow row) -> row.compositeId(), Function.identity()));
  };

  @Override
  public UpdateBuilder<EmployeedepartmenthistoryFields, EmployeedepartmenthistoryRow> update() {
    return new UpdateBuilderMock<>(EmployeedepartmenthistoryFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  @Override
  public Boolean update(
    EmployeedepartmenthistoryRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.compositeId())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.compositeId(), row);;
    };
    return shouldUpdate;
  };

  @Override
  public EmployeedepartmenthistoryRow upsert(
    EmployeedepartmenthistoryRow unsaved,
    Connection c
  ) {
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  };

  @Override
  public List<EmployeedepartmenthistoryRow> upsertBatch(
    Iterator<EmployeedepartmenthistoryRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<EmployeedepartmenthistoryRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      result.add(row);
    };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
    Iterator<EmployeedepartmenthistoryRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      count = count + 1;
    };
    return count;
  };
}