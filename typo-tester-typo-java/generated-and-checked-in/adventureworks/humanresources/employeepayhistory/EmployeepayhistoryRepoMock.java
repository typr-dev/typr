/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.humanresources.employeepayhistory;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilderMock;
import typo.dsl.UpdateParams;

public record EmployeepayhistoryRepoMock(
    java.util.function.Function<EmployeepayhistoryRowUnsaved, EmployeepayhistoryRow> toRow,
    HashMap<EmployeepayhistoryId, EmployeepayhistoryRow> map)
    implements EmployeepayhistoryRepo {
  public EmployeepayhistoryRepoMock(
      java.util.function.Function<EmployeepayhistoryRowUnsaved, EmployeepayhistoryRow> toRow) {
    this(toRow, new HashMap<EmployeepayhistoryId, EmployeepayhistoryRow>());
  }
  ;

  public EmployeepayhistoryRepoMock withToRow(
      java.util.function.Function<EmployeepayhistoryRowUnsaved, EmployeepayhistoryRow> toRow) {
    return new EmployeepayhistoryRepoMock(toRow, map);
  }
  ;

  public EmployeepayhistoryRepoMock withMap(
      HashMap<EmployeepayhistoryId, EmployeepayhistoryRow> map) {
    return new EmployeepayhistoryRepoMock(toRow, map);
  }
  ;

  @Override
  public DeleteBuilder<EmployeepayhistoryFields, EmployeepayhistoryRow> delete() {
    return new DeleteBuilderMock<>(
        EmployeepayhistoryFields.structure(),
        () -> new ArrayList<>(map.values()),
        DeleteParams.empty(),
        row -> row.compositeId(),
        id -> map.remove(id));
  }
  ;

  @Override
  public Boolean deleteById(EmployeepayhistoryId compositeId, Connection c) {
    return Optional.ofNullable(map.remove(compositeId)).isPresent();
  }
  ;

  @Override
  public Integer deleteByIds(EmployeepayhistoryId[] compositeIds, Connection c) {
    var count = 0;
    for (var id : compositeIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
        ;
      }
      ;
    }
    ;
    return count;
  }
  ;

  @Override
  public EmployeepayhistoryRow insert(EmployeepayhistoryRow unsaved, Connection c) {
    if (map.containsKey(unsaved.compositeId())) {
      throw new RuntimeException("id " + unsaved.compositeId() + " already exists");
    }
    ;
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public EmployeepayhistoryRow insert(EmployeepayhistoryRowUnsaved unsaved, Connection c) {
    return insert(toRow.apply(unsaved), c);
  }
  ;

  @Override
  public Long insertStreaming(
      Iterator<EmployeepayhistoryRow> unsaved, Integer batchSize, Connection c) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      count = count + 1L;
    }
    ;
    return count;
  }
  ;

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  @Override
  public Long insertUnsavedStreaming(
      Iterator<EmployeepayhistoryRowUnsaved> unsaved, Integer batchSize, Connection c) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var unsavedRow = unsaved.next();
      var row = toRow.apply(unsavedRow);
      map.put(row.compositeId(), row);
      count = count + 1L;
    }
    ;
    return count;
  }
  ;

  @Override
  public SelectBuilder<EmployeepayhistoryFields, EmployeepayhistoryRow> select() {
    return new SelectBuilderMock<>(
        EmployeepayhistoryFields.structure(),
        () -> new ArrayList<>(map.values()),
        SelectParams.empty());
  }
  ;

  @Override
  public List<EmployeepayhistoryRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  }
  ;

  @Override
  public Optional<EmployeepayhistoryRow> selectById(
      EmployeepayhistoryId compositeId, Connection c) {
    return Optional.ofNullable(map.get(compositeId));
  }
  ;

  @Override
  public List<EmployeepayhistoryRow> selectByIds(
      EmployeepayhistoryId[] compositeIds, Connection c) {
    var result = new ArrayList<EmployeepayhistoryRow>();
    for (var id : compositeIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
        result.add(opt.get());
      }
      ;
    }
    ;
    return result;
  }
  ;

  @Override
  public Map<EmployeepayhistoryId, EmployeepayhistoryRow> selectByIdsTracked(
      EmployeepayhistoryId[] compositeIds, Connection c) {
    return selectByIds(compositeIds, c).stream()
        .collect(
            Collectors.toMap(
                (EmployeepayhistoryRow row) -> row.compositeId(), Function.identity()));
  }
  ;

  @Override
  public UpdateBuilder<EmployeepayhistoryFields, EmployeepayhistoryRow> update() {
    return new UpdateBuilderMock<>(
        EmployeepayhistoryFields.structure(),
        () -> new ArrayList<>(map.values()),
        UpdateParams.empty(),
        row -> row);
  }
  ;

  @Override
  public Boolean update(EmployeepayhistoryRow row, Connection c) {
    var shouldUpdate =
        Optional.ofNullable(map.get(row.compositeId()))
            .filter(oldRow -> !oldRow.equals(row))
            .isPresent();
    if (shouldUpdate) {
      map.put(row.compositeId(), row);
      ;
    }
    ;
    return shouldUpdate;
  }
  ;

  @Override
  public EmployeepayhistoryRow upsert(EmployeepayhistoryRow unsaved, Connection c) {
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public List<EmployeepayhistoryRow> upsertBatch(
      Iterator<EmployeepayhistoryRow> unsaved, Connection c) {
    var result = new ArrayList<EmployeepayhistoryRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      result.add(row);
    }
    ;
    return result;
  }
  ;

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
      Iterator<EmployeepayhistoryRow> unsaved, Integer batchSize, Connection c) {
    var count = 0;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      count = count + 1;
    }
    ;
    return count;
  }
  ;
}
