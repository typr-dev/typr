/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.humanresources.shift;

import java.lang.RuntimeException;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilderMock;
import typo.dsl.UpdateParams;

public record ShiftRepoMock(
  java.util.function.Function<ShiftRowUnsaved, ShiftRow> toRow,
  HashMap<ShiftId, ShiftRow> map
) implements ShiftRepo {
  public ShiftRepoMock(java.util.function.Function<ShiftRowUnsaved, ShiftRow> toRow) {
    this(toRow, new HashMap<ShiftId, ShiftRow>());
  };

  public ShiftRepoMock withToRow(java.util.function.Function<ShiftRowUnsaved, ShiftRow> toRow) {
    return new ShiftRepoMock(toRow, map);
  };

  public ShiftRepoMock withMap(HashMap<ShiftId, ShiftRow> map) {
    return new ShiftRepoMock(toRow, map);
  };

  @Override
  public DeleteBuilder<ShiftFields, ShiftRow> delete() {
    return new DeleteBuilderMock<>(ShiftFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.shiftid(), id -> map.remove(id));
  };

  @Override
  public Boolean deleteById(
    ShiftId shiftid,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(shiftid)).isPresent();
  };

  @Override
  public Integer deleteByIds(
    ShiftId[] shiftids,
    Connection c
  ) {
    var count = 0;
    for (var id : shiftids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    };
    };
    return count;
  };

  @Override
  public ShiftRow insert(
    ShiftRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.shiftid())) {
      throw new RuntimeException("id " + unsaved.shiftid() + " already exists");
    };
    map.put(unsaved.shiftid(), unsaved);
    return unsaved;
  };

  @Override
  public ShiftRow insert(
    ShiftRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  @Override
  public Long insertStreaming(
    Iterator<ShiftRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.shiftid(), row);
      count = count + 1L;
    };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  @Override
  public Long insertUnsavedStreaming(
    Iterator<ShiftRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var unsavedRow = unsaved.next();
      var row = toRow.apply(unsavedRow);
      map.put(row.shiftid(), row);
      count = count + 1L;
    };
    return count;
  };

  @Override
  public SelectBuilder<ShiftFields, ShiftRow> select() {
    return new SelectBuilderMock<>(ShiftFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  @Override
  public List<ShiftRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  @Override
  public Optional<ShiftRow> selectById(
    ShiftId shiftid,
    Connection c
  ) {
    return Optional.ofNullable(map.get(shiftid));
  };

  @Override
  public List<ShiftRow> selectByIds(
    ShiftId[] shiftids,
    Connection c
  ) {
    var result = new ArrayList<ShiftRow>();
    for (var id : shiftids) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    };
    };
    return result;
  };

  @Override
  public Map<ShiftId, ShiftRow> selectByIdsTracked(
    ShiftId[] shiftids,
    Connection c
  ) {
    return selectByIds(shiftids, c).stream().collect(Collectors.toMap((ShiftRow row) -> row.shiftid(), Function.identity()));
  };

  @Override
  public UpdateBuilder<ShiftFields, ShiftRow> update() {
    return new UpdateBuilderMock<>(ShiftFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  @Override
  public Boolean update(
    ShiftRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.shiftid())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.shiftid(), row);;
    };
    return shouldUpdate;
  };

  @Override
  public ShiftRow upsert(
    ShiftRow unsaved,
    Connection c
  ) {
    map.put(unsaved.shiftid(), unsaved);
    return unsaved;
  };

  @Override
  public List<ShiftRow> upsertBatch(
    Iterator<ShiftRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<ShiftRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.shiftid(), row);
      result.add(row);
    };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
    Iterator<ShiftRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.shiftid(), row);
      count = count + 1;
    };
    return count;
  };
}