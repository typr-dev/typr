/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.salespersonquotahistory;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilderMock;
import typo.dsl.UpdateParams;

public record SalespersonquotahistoryRepoMock(
    java.util.function.Function<SalespersonquotahistoryRowUnsaved, SalespersonquotahistoryRow>
        toRow,
    HashMap<SalespersonquotahistoryId, SalespersonquotahistoryRow> map)
    implements SalespersonquotahistoryRepo {
  public SalespersonquotahistoryRepoMock(
      java.util.function.Function<SalespersonquotahistoryRowUnsaved, SalespersonquotahistoryRow>
          toRow) {
    this(toRow, new HashMap<SalespersonquotahistoryId, SalespersonquotahistoryRow>());
  }
  ;

  public SalespersonquotahistoryRepoMock withToRow(
      java.util.function.Function<SalespersonquotahistoryRowUnsaved, SalespersonquotahistoryRow>
          toRow) {
    return new SalespersonquotahistoryRepoMock(toRow, map);
  }
  ;

  public SalespersonquotahistoryRepoMock withMap(
      HashMap<SalespersonquotahistoryId, SalespersonquotahistoryRow> map) {
    return new SalespersonquotahistoryRepoMock(toRow, map);
  }
  ;

  @Override
  public DeleteBuilder<SalespersonquotahistoryFields, SalespersonquotahistoryRow> delete() {
    return new DeleteBuilderMock<>(
        SalespersonquotahistoryFields.structure(),
        () -> new ArrayList<>(map.values()),
        DeleteParams.empty(),
        row -> row.compositeId(),
        id -> map.remove(id));
  }
  ;

  @Override
  public Boolean deleteById(SalespersonquotahistoryId compositeId, Connection c) {
    return Optional.ofNullable(map.remove(compositeId)).isPresent();
  }
  ;

  @Override
  public Integer deleteByIds(SalespersonquotahistoryId[] compositeIds, Connection c) {
    var count = 0;
    for (var id : compositeIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
        ;
      }
      ;
    }
    ;
    return count;
  }
  ;

  @Override
  public SalespersonquotahistoryRow insert(SalespersonquotahistoryRow unsaved, Connection c) {
    if (map.containsKey(unsaved.compositeId())) {
      throw new RuntimeException("id " + unsaved.compositeId() + " already exists");
    }
    ;
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public SalespersonquotahistoryRow insert(
      SalespersonquotahistoryRowUnsaved unsaved, Connection c) {
    return insert(toRow.apply(unsaved), c);
  }
  ;

  @Override
  public Long insertStreaming(
      Iterator<SalespersonquotahistoryRow> unsaved, Integer batchSize, Connection c) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      count = count + 1L;
    }
    ;
    return count;
  }
  ;

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  @Override
  public Long insertUnsavedStreaming(
      Iterator<SalespersonquotahistoryRowUnsaved> unsaved, Integer batchSize, Connection c) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var unsavedRow = unsaved.next();
      var row = toRow.apply(unsavedRow);
      map.put(row.compositeId(), row);
      count = count + 1L;
    }
    ;
    return count;
  }
  ;

  @Override
  public SelectBuilder<SalespersonquotahistoryFields, SalespersonquotahistoryRow> select() {
    return new SelectBuilderMock<>(
        SalespersonquotahistoryFields.structure(),
        () -> new ArrayList<>(map.values()),
        SelectParams.empty());
  }
  ;

  @Override
  public List<SalespersonquotahistoryRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  }
  ;

  @Override
  public Optional<SalespersonquotahistoryRow> selectById(
      SalespersonquotahistoryId compositeId, Connection c) {
    return Optional.ofNullable(map.get(compositeId));
  }
  ;

  @Override
  public List<SalespersonquotahistoryRow> selectByIds(
      SalespersonquotahistoryId[] compositeIds, Connection c) {
    var result = new ArrayList<SalespersonquotahistoryRow>();
    for (var id : compositeIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
        result.add(opt.get());
      }
      ;
    }
    ;
    return result;
  }
  ;

  @Override
  public Map<SalespersonquotahistoryId, SalespersonquotahistoryRow> selectByIdsTracked(
      SalespersonquotahistoryId[] compositeIds, Connection c) {
    return selectByIds(compositeIds, c).stream()
        .collect(
            Collectors.toMap(
                (SalespersonquotahistoryRow row) -> row.compositeId(), Function.identity()));
  }
  ;

  @Override
  public UpdateBuilder<SalespersonquotahistoryFields, SalespersonquotahistoryRow> update() {
    return new UpdateBuilderMock<>(
        SalespersonquotahistoryFields.structure(),
        () -> new ArrayList<>(map.values()),
        UpdateParams.empty(),
        row -> row);
  }
  ;

  @Override
  public Boolean update(SalespersonquotahistoryRow row, Connection c) {
    var shouldUpdate =
        Optional.ofNullable(map.get(row.compositeId()))
            .filter(oldRow -> !oldRow.equals(row))
            .isPresent();
    if (shouldUpdate) {
      map.put(row.compositeId(), row);
      ;
    }
    ;
    return shouldUpdate;
  }
  ;

  @Override
  public SalespersonquotahistoryRow upsert(SalespersonquotahistoryRow unsaved, Connection c) {
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public List<SalespersonquotahistoryRow> upsertBatch(
      Iterator<SalespersonquotahistoryRow> unsaved, Connection c) {
    var result = new ArrayList<SalespersonquotahistoryRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      result.add(row);
    }
    ;
    return result;
  }
  ;

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
      Iterator<SalespersonquotahistoryRow> unsaved, Integer batchSize, Connection c) {
    var count = 0;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      count = count + 1;
    }
    ;
    return count;
  }
  ;
}
