/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.salesorderdetail;

import java.lang.RuntimeException;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;
import static typo.runtime.internal.stringInterpolator.str;

public record SalesorderdetailRepoMock(
  java.util.function.Function<SalesorderdetailRowUnsaved, SalesorderdetailRow> toRow,
  HashMap<SalesorderdetailId, SalesorderdetailRow> map
) implements SalesorderdetailRepo {
  public SalesorderdetailRepoMock(java.util.function.Function<SalesorderdetailRowUnsaved, SalesorderdetailRow> toRow) {
    this(toRow, new HashMap<SalesorderdetailId, SalesorderdetailRow>());
  };

  public SalesorderdetailRepoMock withToRow(java.util.function.Function<SalesorderdetailRowUnsaved, SalesorderdetailRow> toRow) {
    return new SalesorderdetailRepoMock(toRow, map);
  };

  public SalesorderdetailRepoMock withMap(HashMap<SalesorderdetailId, SalesorderdetailRow> map) {
    return new SalesorderdetailRepoMock(toRow, map);
  };

  @Override
  public DeleteBuilder<SalesorderdetailFields, SalesorderdetailRow> delete() {
    return new DeleteBuilderMock<>(SalesorderdetailFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.compositeId(), id -> map.remove(id));
  };

  @Override
  public Boolean deleteById(
    SalesorderdetailId compositeId,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(compositeId)).isPresent();
  };

  @Override
  public Integer deleteByIds(
    SalesorderdetailId[] compositeIds,
    Connection c
  ) {
    var count = 0;
    for (var id : compositeIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    };
    };
    return count;
  };

  @Override
  public SalesorderdetailRow insert(
    SalesorderdetailRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.compositeId())) {
      throw new RuntimeException(str("id $unsaved.compositeId() already exists"));
    };
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  };

  @Override
  public SalesorderdetailRow insert(
    SalesorderdetailRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  @Override
  public Long insertStreaming(
    Iterator<SalesorderdetailRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      count = count + 1L;
    };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  @Override
  public Long insertUnsavedStreaming(
    Iterator<SalesorderdetailRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var unsavedRow = unsaved.next();
      var row = toRow.apply(unsavedRow);
      map.put(row.compositeId(), row);
      count = count + 1L;
    };
    return count;
  };

  @Override
  public SelectBuilder<SalesorderdetailFields, SalesorderdetailRow> select() {
    return new SelectBuilderMock<>(SalesorderdetailFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  @Override
  public List<SalesorderdetailRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  @Override
  public Optional<SalesorderdetailRow> selectById(
    SalesorderdetailId compositeId,
    Connection c
  ) {
    return Optional.ofNullable(map.get(compositeId));
  };

  @Override
  public List<SalesorderdetailRow> selectByIds(
    SalesorderdetailId[] compositeIds,
    Connection c
  ) {
    var result = new ArrayList<SalesorderdetailRow>();
    for (var id : compositeIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    };
    };
    return result;
  };

  @Override
  public Map<SalesorderdetailId, SalesorderdetailRow> selectByIdsTracked(
    SalesorderdetailId[] compositeIds,
    Connection c
  ) {
    return selectByIds(compositeIds, c).stream().collect(Collectors.toMap((SalesorderdetailRow row) -> row.compositeId(), Function.identity()));
  };

  @Override
  public UpdateBuilder<SalesorderdetailFields, SalesorderdetailRow> update() {
    return new UpdateBuilderMock<>(SalesorderdetailFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  @Override
  public Boolean update(
    SalesorderdetailRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.compositeId())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.compositeId(), row);;
    };
    return shouldUpdate;
  };

  @Override
  public SalesorderdetailRow upsert(
    SalesorderdetailRow unsaved,
    Connection c
  ) {
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  };

  @Override
  public List<SalesorderdetailRow> upsertBatch(
    Iterator<SalesorderdetailRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<SalesorderdetailRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      result.add(row);
    };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
    Iterator<SalesorderdetailRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      count = count + 1;
    };
    return count;
  };
}