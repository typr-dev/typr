/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.currency;

import java.lang.RuntimeException;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilderMock;
import typo.dsl.UpdateParams;

public record CurrencyRepoMock(
  java.util.function.Function<CurrencyRowUnsaved, CurrencyRow> toRow,
  HashMap<CurrencyId, CurrencyRow> map
) implements CurrencyRepo {
  public CurrencyRepoMock(java.util.function.Function<CurrencyRowUnsaved, CurrencyRow> toRow) {
    this(toRow, new HashMap<CurrencyId, CurrencyRow>());
  };

  public CurrencyRepoMock withToRow(java.util.function.Function<CurrencyRowUnsaved, CurrencyRow> toRow) {
    return new CurrencyRepoMock(toRow, map);
  };

  public CurrencyRepoMock withMap(HashMap<CurrencyId, CurrencyRow> map) {
    return new CurrencyRepoMock(toRow, map);
  };

  @Override
  public DeleteBuilder<CurrencyFields, CurrencyRow> delete() {
    return new DeleteBuilderMock<>(CurrencyFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.currencycode(), id -> map.remove(id));
  };

  @Override
  public Boolean deleteById(
    CurrencyId currencycode,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(currencycode)).isPresent();
  };

  @Override
  public Integer deleteByIds(
    CurrencyId[] currencycodes,
    Connection c
  ) {
    var count = 0;
    for (var id : currencycodes) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    };
    };
    return count;
  };

  @Override
  public CurrencyRow insert(
    CurrencyRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.currencycode())) {
      throw new RuntimeException("id " + unsaved.currencycode() + " already exists");
    };
    map.put(unsaved.currencycode(), unsaved);
    return unsaved;
  };

  @Override
  public CurrencyRow insert(
    CurrencyRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  @Override
  public Long insertStreaming(
    Iterator<CurrencyRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.currencycode(), row);
      count = count + 1L;
    };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  @Override
  public Long insertUnsavedStreaming(
    Iterator<CurrencyRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var unsavedRow = unsaved.next();
      var row = toRow.apply(unsavedRow);
      map.put(row.currencycode(), row);
      count = count + 1L;
    };
    return count;
  };

  @Override
  public SelectBuilder<CurrencyFields, CurrencyRow> select() {
    return new SelectBuilderMock<>(CurrencyFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  @Override
  public List<CurrencyRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  @Override
  public Optional<CurrencyRow> selectById(
    CurrencyId currencycode,
    Connection c
  ) {
    return Optional.ofNullable(map.get(currencycode));
  };

  @Override
  public List<CurrencyRow> selectByIds(
    CurrencyId[] currencycodes,
    Connection c
  ) {
    var result = new ArrayList<CurrencyRow>();
    for (var id : currencycodes) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    };
    };
    return result;
  };

  @Override
  public Map<CurrencyId, CurrencyRow> selectByIdsTracked(
    CurrencyId[] currencycodes,
    Connection c
  ) {
    return selectByIds(currencycodes, c).stream().collect(Collectors.toMap((CurrencyRow row) -> row.currencycode(), Function.identity()));
  };

  @Override
  public UpdateBuilder<CurrencyFields, CurrencyRow> update() {
    return new UpdateBuilderMock<>(CurrencyFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  @Override
  public Boolean update(
    CurrencyRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.currencycode())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.currencycode(), row);;
    };
    return shouldUpdate;
  };

  @Override
  public CurrencyRow upsert(
    CurrencyRow unsaved,
    Connection c
  ) {
    map.put(unsaved.currencycode(), unsaved);
    return unsaved;
  };

  @Override
  public List<CurrencyRow> upsertBatch(
    Iterator<CurrencyRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<CurrencyRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.currencycode(), row);
      result.add(row);
    };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
    Iterator<CurrencyRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.currencycode(), row);
      count = count + 1;
    };
    return count;
  };
}