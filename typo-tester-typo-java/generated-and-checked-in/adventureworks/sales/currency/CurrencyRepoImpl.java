/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.sales.currency;

import static typo.runtime.Fragment.interpolate;

import adventureworks.public_.Name;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import typo.dsl.DeleteBuilder;
import typo.dsl.Dialect;
import typo.dsl.SelectBuilder;
import typo.dsl.UpdateBuilder;
import typo.runtime.Fragment;
import typo.runtime.PgTypes;
import typo.runtime.streamingInsert;

public class CurrencyRepoImpl implements CurrencyRepo {
  @Override
  public DeleteBuilder<CurrencyFields, CurrencyRow> delete() {
    return DeleteBuilder.of(
        "\"sales\".\"currency\"", CurrencyFields.structure(), Dialect.POSTGRESQL);
  }
  ;

  @Override
  public Boolean deleteById(CurrencyId currencycode, Connection c) {
    return interpolate(
                Fragment.lit("delete from \"sales\".\"currency\" where \"currencycode\" = "),
                Fragment.encode(CurrencyId.pgType, currencycode),
                Fragment.lit(""))
            .update()
            .runUnchecked(c)
        > 0;
  }
  ;

  @Override
  public Integer deleteByIds(CurrencyId[] currencycodes, Connection c) {
    return interpolate(
            Fragment.lit("delete\nfrom \"sales\".\"currency\"\nwhere \"currencycode\" = ANY("),
            Fragment.encode(CurrencyId.pgTypeArray, currencycodes),
            Fragment.lit(")"))
        .update()
        .runUnchecked(c);
  }
  ;

  @Override
  public CurrencyRow insert(CurrencyRow unsaved, Connection c) {
    return interpolate(
            Fragment.lit(
                "insert into \"sales\".\"currency\"(\"currencycode\", \"name\", \"modifieddate\")\n"
                    + "values ("),
            Fragment.encode(CurrencyId.pgType, unsaved.currencycode()),
            Fragment.lit("::bpchar, "),
            Fragment.encode(Name.pgType, unsaved.name()),
            Fragment.lit("::varchar, "),
            Fragment.encode(PgTypes.timestamp, unsaved.modifieddate()),
            Fragment.lit("::timestamp)\nRETURNING \"currencycode\", \"name\", \"modifieddate\"\n"))
        .updateReturning(CurrencyRow._rowParser.exactlyOne())
        .runUnchecked(c);
  }
  ;

  @Override
  public CurrencyRow insert(CurrencyRowUnsaved unsaved, Connection c) {
    ArrayList<Fragment> columns = new ArrayList<>();
    ;
    ArrayList<Fragment> values = new ArrayList<>();
    ;
    columns.add(Fragment.lit("\"currencycode\""));
    values.add(
        interpolate(
            Fragment.encode(CurrencyId.pgType, unsaved.currencycode()), Fragment.lit("::bpchar")));
    columns.add(Fragment.lit("\"name\""));
    values.add(
        interpolate(Fragment.encode(Name.pgType, unsaved.name()), Fragment.lit("::varchar")));
    unsaved
        .modifieddate()
        .visit(
            () -> {},
            value -> {
              columns.add(Fragment.lit("\"modifieddate\""));
              values.add(
                  interpolate(
                      Fragment.encode(PgTypes.timestamp, value), Fragment.lit("::timestamp")));
            });
    ;
    Fragment q =
        interpolate(
            Fragment.lit("insert into \"sales\".\"currency\"("),
            Fragment.comma(columns),
            Fragment.lit(")\nvalues ("),
            Fragment.comma(values),
            Fragment.lit(")\nRETURNING \"currencycode\", \"name\", \"modifieddate\"\n"));
    ;
    return q.updateReturning(CurrencyRow._rowParser.exactlyOne()).runUnchecked(c);
  }
  ;

  @Override
  public Long insertStreaming(Iterator<CurrencyRow> unsaved, Integer batchSize, Connection c) {
    return streamingInsert.insertUnchecked(
        "COPY \"sales\".\"currency\"(\"currencycode\", \"name\", \"modifieddate\") FROM STDIN",
        batchSize,
        unsaved,
        c,
        CurrencyRow.pgText);
  }
  ;

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  @Override
  public Long insertUnsavedStreaming(
      Iterator<CurrencyRowUnsaved> unsaved, Integer batchSize, Connection c) {
    return streamingInsert.insertUnchecked(
        "COPY \"sales\".\"currency\"(\"currencycode\", \"name\", \"modifieddate\") FROM STDIN"
            + " (DEFAULT '__DEFAULT_VALUE__')",
        batchSize,
        unsaved,
        c,
        CurrencyRowUnsaved.pgText);
  }
  ;

  @Override
  public SelectBuilder<CurrencyFields, CurrencyRow> select() {
    return SelectBuilder.of(
        "\"sales\".\"currency\"",
        CurrencyFields.structure(),
        CurrencyRow._rowParser,
        Dialect.POSTGRESQL);
  }
  ;

  @Override
  public List<CurrencyRow> selectAll(Connection c) {
    return interpolate(
            Fragment.lit(
                "select \"currencycode\", \"name\", \"modifieddate\"\n"
                    + "from \"sales\".\"currency\"\n"))
        .query(CurrencyRow._rowParser.all())
        .runUnchecked(c);
  }
  ;

  @Override
  public Optional<CurrencyRow> selectById(CurrencyId currencycode, Connection c) {
    return interpolate(
            Fragment.lit(
                "select \"currencycode\", \"name\", \"modifieddate\"\n"
                    + "from \"sales\".\"currency\"\n"
                    + "where \"currencycode\" = "),
            Fragment.encode(CurrencyId.pgType, currencycode),
            Fragment.lit(""))
        .query(CurrencyRow._rowParser.first())
        .runUnchecked(c);
  }
  ;

  @Override
  public List<CurrencyRow> selectByIds(CurrencyId[] currencycodes, Connection c) {
    return interpolate(
            Fragment.lit(
                "select \"currencycode\", \"name\", \"modifieddate\"\n"
                    + "from \"sales\".\"currency\"\n"
                    + "where \"currencycode\" = ANY("),
            Fragment.encode(CurrencyId.pgTypeArray, currencycodes),
            Fragment.lit(")"))
        .query(CurrencyRow._rowParser.all())
        .runUnchecked(c);
  }
  ;

  @Override
  public Map<CurrencyId, CurrencyRow> selectByIdsTracked(CurrencyId[] currencycodes, Connection c) {
    HashMap<CurrencyId, CurrencyRow> ret = new HashMap<CurrencyId, CurrencyRow>();
    selectByIds(currencycodes, c).forEach(row -> ret.put(row.currencycode(), row));
    return ret;
  }
  ;

  @Override
  public UpdateBuilder<CurrencyFields, CurrencyRow> update() {
    return UpdateBuilder.of(
        "\"sales\".\"currency\"",
        CurrencyFields.structure(),
        CurrencyRow._rowParser,
        Dialect.POSTGRESQL);
  }
  ;

  @Override
  public Boolean update(CurrencyRow row, Connection c) {
    CurrencyId currencycode = row.currencycode();
    ;
    return interpolate(
                Fragment.lit("update \"sales\".\"currency\"\nset \"name\" = "),
                Fragment.encode(Name.pgType, row.name()),
                Fragment.lit("::varchar,\n\"modifieddate\" = "),
                Fragment.encode(PgTypes.timestamp, row.modifieddate()),
                Fragment.lit("::timestamp\nwhere \"currencycode\" = "),
                Fragment.encode(CurrencyId.pgType, currencycode),
                Fragment.lit(""))
            .update()
            .runUnchecked(c)
        > 0;
  }
  ;

  @Override
  public CurrencyRow upsert(CurrencyRow unsaved, Connection c) {
    return interpolate(
            Fragment.lit(
                "insert into \"sales\".\"currency\"(\"currencycode\", \"name\", \"modifieddate\")\n"
                    + "values ("),
            Fragment.encode(CurrencyId.pgType, unsaved.currencycode()),
            Fragment.lit("::bpchar, "),
            Fragment.encode(Name.pgType, unsaved.name()),
            Fragment.lit("::varchar, "),
            Fragment.encode(PgTypes.timestamp, unsaved.modifieddate()),
            Fragment.lit(
                "::timestamp)\n"
                    + "on conflict (\"currencycode\")\n"
                    + "do update set\n"
                    + "  \"name\" = EXCLUDED.\"name\",\n"
                    + "\"modifieddate\" = EXCLUDED.\"modifieddate\"\n"
                    + "returning \"currencycode\", \"name\", \"modifieddate\""))
        .updateReturning(CurrencyRow._rowParser.exactlyOne())
        .runUnchecked(c);
  }
  ;

  @Override
  public List<CurrencyRow> upsertBatch(Iterator<CurrencyRow> unsaved, Connection c) {
    return interpolate(
            Fragment.lit(
                "insert into \"sales\".\"currency\"(\"currencycode\", \"name\", \"modifieddate\")\n"
                    + "values (?::bpchar, ?::varchar, ?::timestamp)\n"
                    + "on conflict (\"currencycode\")\n"
                    + "do update set\n"
                    + "  \"name\" = EXCLUDED.\"name\",\n"
                    + "\"modifieddate\" = EXCLUDED.\"modifieddate\"\n"
                    + "returning \"currencycode\", \"name\", \"modifieddate\""))
        .updateManyReturning(CurrencyRow._rowParser, unsaved)
        .runUnchecked(c);
  }
  ;

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(Iterator<CurrencyRow> unsaved, Integer batchSize, Connection c) {
    interpolate(
            Fragment.lit(
                "create temporary table currency_TEMP (like \"sales\".\"currency\") on commit"
                    + " drop"))
        .update()
        .runUnchecked(c);
    streamingInsert.insertUnchecked(
        "copy currency_TEMP(\"currencycode\", \"name\", \"modifieddate\") from stdin",
        batchSize,
        unsaved,
        c,
        CurrencyRow.pgText);
    return interpolate(
            Fragment.lit(
                "insert into \"sales\".\"currency\"(\"currencycode\", \"name\", \"modifieddate\")\n"
                    + "select * from currency_TEMP\n"
                    + "on conflict (\"currencycode\")\n"
                    + "do update set\n"
                    + "  \"name\" = EXCLUDED.\"name\",\n"
                    + "\"modifieddate\" = EXCLUDED.\"modifieddate\"\n"
                    + ";\n"
                    + "drop table currency_TEMP;"))
        .update()
        .runUnchecked(c);
  }
  ;
}
