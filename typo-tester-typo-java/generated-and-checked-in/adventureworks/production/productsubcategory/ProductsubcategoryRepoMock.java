/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productsubcategory;

import java.lang.RuntimeException;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilderMock;
import typo.dsl.UpdateParams;

public record ProductsubcategoryRepoMock(
  java.util.function.Function<ProductsubcategoryRowUnsaved, ProductsubcategoryRow> toRow,
  HashMap<ProductsubcategoryId, ProductsubcategoryRow> map
) implements ProductsubcategoryRepo {
  public ProductsubcategoryRepoMock(java.util.function.Function<ProductsubcategoryRowUnsaved, ProductsubcategoryRow> toRow) {
    this(toRow, new HashMap<ProductsubcategoryId, ProductsubcategoryRow>());
  };

  public ProductsubcategoryRepoMock withToRow(java.util.function.Function<ProductsubcategoryRowUnsaved, ProductsubcategoryRow> toRow) {
    return new ProductsubcategoryRepoMock(toRow, map);
  };

  public ProductsubcategoryRepoMock withMap(HashMap<ProductsubcategoryId, ProductsubcategoryRow> map) {
    return new ProductsubcategoryRepoMock(toRow, map);
  };

  @Override
  public DeleteBuilder<ProductsubcategoryFields, ProductsubcategoryRow> delete() {
    return new DeleteBuilderMock<>(ProductsubcategoryFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.productsubcategoryid(), id -> map.remove(id));
  };

  @Override
  public Boolean deleteById(
    ProductsubcategoryId productsubcategoryid,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(productsubcategoryid)).isPresent();
  };

  @Override
  public Integer deleteByIds(
    ProductsubcategoryId[] productsubcategoryids,
    Connection c
  ) {
    var count = 0;
    for (var id : productsubcategoryids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    };
    };
    return count;
  };

  @Override
  public ProductsubcategoryRow insert(
    ProductsubcategoryRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.productsubcategoryid())) {
      throw new RuntimeException("id " + unsaved.productsubcategoryid() + " already exists");
    };
    map.put(unsaved.productsubcategoryid(), unsaved);
    return unsaved;
  };

  @Override
  public ProductsubcategoryRow insert(
    ProductsubcategoryRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  @Override
  public Long insertStreaming(
    Iterator<ProductsubcategoryRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.productsubcategoryid(), row);
      count = count + 1L;
    };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  @Override
  public Long insertUnsavedStreaming(
    Iterator<ProductsubcategoryRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var unsavedRow = unsaved.next();
      var row = toRow.apply(unsavedRow);
      map.put(row.productsubcategoryid(), row);
      count = count + 1L;
    };
    return count;
  };

  @Override
  public SelectBuilder<ProductsubcategoryFields, ProductsubcategoryRow> select() {
    return new SelectBuilderMock<>(ProductsubcategoryFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  @Override
  public List<ProductsubcategoryRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  @Override
  public Optional<ProductsubcategoryRow> selectById(
    ProductsubcategoryId productsubcategoryid,
    Connection c
  ) {
    return Optional.ofNullable(map.get(productsubcategoryid));
  };

  @Override
  public List<ProductsubcategoryRow> selectByIds(
    ProductsubcategoryId[] productsubcategoryids,
    Connection c
  ) {
    var result = new ArrayList<ProductsubcategoryRow>();
    for (var id : productsubcategoryids) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    };
    };
    return result;
  };

  @Override
  public Map<ProductsubcategoryId, ProductsubcategoryRow> selectByIdsTracked(
    ProductsubcategoryId[] productsubcategoryids,
    Connection c
  ) {
    return selectByIds(productsubcategoryids, c).stream().collect(Collectors.toMap((ProductsubcategoryRow row) -> row.productsubcategoryid(), Function.identity()));
  };

  @Override
  public UpdateBuilder<ProductsubcategoryFields, ProductsubcategoryRow> update() {
    return new UpdateBuilderMock<>(ProductsubcategoryFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  @Override
  public Boolean update(
    ProductsubcategoryRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.productsubcategoryid())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.productsubcategoryid(), row);;
    };
    return shouldUpdate;
  };

  @Override
  public ProductsubcategoryRow upsert(
    ProductsubcategoryRow unsaved,
    Connection c
  ) {
    map.put(unsaved.productsubcategoryid(), unsaved);
    return unsaved;
  };

  @Override
  public List<ProductsubcategoryRow> upsertBatch(
    Iterator<ProductsubcategoryRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<ProductsubcategoryRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.productsubcategoryid(), row);
      result.add(row);
    };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
    Iterator<ProductsubcategoryRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.productsubcategoryid(), row);
      count = count + 1;
    };
    return count;
  };
}