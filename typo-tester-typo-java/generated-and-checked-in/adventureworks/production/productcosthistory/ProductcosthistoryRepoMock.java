/**
 * File has been automatically generated by `typo`.
 *
 * <p>IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.productcosthistory;

import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilderMock;
import typo.dsl.UpdateParams;

public record ProductcosthistoryRepoMock(
    java.util.function.Function<ProductcosthistoryRowUnsaved, ProductcosthistoryRow> toRow,
    HashMap<ProductcosthistoryId, ProductcosthistoryRow> map)
    implements ProductcosthistoryRepo {
  public ProductcosthistoryRepoMock(
      java.util.function.Function<ProductcosthistoryRowUnsaved, ProductcosthistoryRow> toRow) {
    this(toRow, new HashMap<ProductcosthistoryId, ProductcosthistoryRow>());
  }
  ;

  public ProductcosthistoryRepoMock withToRow(
      java.util.function.Function<ProductcosthistoryRowUnsaved, ProductcosthistoryRow> toRow) {
    return new ProductcosthistoryRepoMock(toRow, map);
  }
  ;

  public ProductcosthistoryRepoMock withMap(
      HashMap<ProductcosthistoryId, ProductcosthistoryRow> map) {
    return new ProductcosthistoryRepoMock(toRow, map);
  }
  ;

  @Override
  public DeleteBuilder<ProductcosthistoryFields, ProductcosthistoryRow> delete() {
    return new DeleteBuilderMock<>(
        ProductcosthistoryFields.structure(),
        () -> new ArrayList<>(map.values()),
        DeleteParams.empty(),
        row -> row.compositeId(),
        id -> map.remove(id));
  }
  ;

  @Override
  public Boolean deleteById(ProductcosthistoryId compositeId, Connection c) {
    return Optional.ofNullable(map.remove(compositeId)).isPresent();
  }
  ;

  @Override
  public Integer deleteByIds(ProductcosthistoryId[] compositeIds, Connection c) {
    var count = 0;
    for (var id : compositeIds) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
        count = count + 1;
        ;
      }
      ;
    }
    ;
    return count;
  }
  ;

  @Override
  public ProductcosthistoryRow insert(ProductcosthistoryRow unsaved, Connection c) {
    if (map.containsKey(unsaved.compositeId())) {
      throw new RuntimeException("id " + unsaved.compositeId() + " already exists");
    }
    ;
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public ProductcosthistoryRow insert(ProductcosthistoryRowUnsaved unsaved, Connection c) {
    return insert(toRow.apply(unsaved), c);
  }
  ;

  @Override
  public Long insertStreaming(
      Iterator<ProductcosthistoryRow> unsaved, Integer batchSize, Connection c) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      count = count + 1L;
    }
    ;
    return count;
  }
  ;

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  @Override
  public Long insertUnsavedStreaming(
      Iterator<ProductcosthistoryRowUnsaved> unsaved, Integer batchSize, Connection c) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var unsavedRow = unsaved.next();
      var row = toRow.apply(unsavedRow);
      map.put(row.compositeId(), row);
      count = count + 1L;
    }
    ;
    return count;
  }
  ;

  @Override
  public SelectBuilder<ProductcosthistoryFields, ProductcosthistoryRow> select() {
    return new SelectBuilderMock<>(
        ProductcosthistoryFields.structure(),
        () -> new ArrayList<>(map.values()),
        SelectParams.empty());
  }
  ;

  @Override
  public List<ProductcosthistoryRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  }
  ;

  @Override
  public Optional<ProductcosthistoryRow> selectById(
      ProductcosthistoryId compositeId, Connection c) {
    return Optional.ofNullable(map.get(compositeId));
  }
  ;

  @Override
  public List<ProductcosthistoryRow> selectByIds(
      ProductcosthistoryId[] compositeIds, Connection c) {
    var result = new ArrayList<ProductcosthistoryRow>();
    for (var id : compositeIds) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
        result.add(opt.get());
      }
      ;
    }
    ;
    return result;
  }
  ;

  @Override
  public Map<ProductcosthistoryId, ProductcosthistoryRow> selectByIdsTracked(
      ProductcosthistoryId[] compositeIds, Connection c) {
    return selectByIds(compositeIds, c).stream()
        .collect(
            Collectors.toMap(
                (ProductcosthistoryRow row) -> row.compositeId(), Function.identity()));
  }
  ;

  @Override
  public UpdateBuilder<ProductcosthistoryFields, ProductcosthistoryRow> update() {
    return new UpdateBuilderMock<>(
        ProductcosthistoryFields.structure(),
        () -> new ArrayList<>(map.values()),
        UpdateParams.empty(),
        row -> row);
  }
  ;

  @Override
  public Boolean update(ProductcosthistoryRow row, Connection c) {
    var shouldUpdate =
        Optional.ofNullable(map.get(row.compositeId()))
            .filter(oldRow -> !oldRow.equals(row))
            .isPresent();
    if (shouldUpdate) {
      map.put(row.compositeId(), row);
      ;
    }
    ;
    return shouldUpdate;
  }
  ;

  @Override
  public ProductcosthistoryRow upsert(ProductcosthistoryRow unsaved, Connection c) {
    map.put(unsaved.compositeId(), unsaved);
    return unsaved;
  }
  ;

  @Override
  public List<ProductcosthistoryRow> upsertBatch(
      Iterator<ProductcosthistoryRow> unsaved, Connection c) {
    var result = new ArrayList<ProductcosthistoryRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      result.add(row);
    }
    ;
    return result;
  }
  ;

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
      Iterator<ProductcosthistoryRow> unsaved, Integer batchSize, Connection c) {
    var count = 0;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.compositeId(), row);
      count = count + 1;
    }
    ;
    return count;
  }
  ;
}
