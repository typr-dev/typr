/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.transactionhistory;

import java.lang.RuntimeException;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilderMock;
import typo.dsl.UpdateParams;

public record TransactionhistoryRepoMock(
  java.util.function.Function<TransactionhistoryRowUnsaved, TransactionhistoryRow> toRow,
  HashMap<TransactionhistoryId, TransactionhistoryRow> map
) implements TransactionhistoryRepo {
  public TransactionhistoryRepoMock(java.util.function.Function<TransactionhistoryRowUnsaved, TransactionhistoryRow> toRow) {
    this(toRow, new HashMap<TransactionhistoryId, TransactionhistoryRow>());
  };

  public TransactionhistoryRepoMock withToRow(java.util.function.Function<TransactionhistoryRowUnsaved, TransactionhistoryRow> toRow) {
    return new TransactionhistoryRepoMock(toRow, map);
  };

  public TransactionhistoryRepoMock withMap(HashMap<TransactionhistoryId, TransactionhistoryRow> map) {
    return new TransactionhistoryRepoMock(toRow, map);
  };

  @Override
  public DeleteBuilder<TransactionhistoryFields, TransactionhistoryRow> delete() {
    return new DeleteBuilderMock<>(TransactionhistoryFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.transactionid(), id -> map.remove(id));
  };

  @Override
  public Boolean deleteById(
    TransactionhistoryId transactionid,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(transactionid)).isPresent();
  };

  @Override
  public Integer deleteByIds(
    TransactionhistoryId[] transactionids,
    Connection c
  ) {
    var count = 0;
    for (var id : transactionids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    };
    };
    return count;
  };

  @Override
  public TransactionhistoryRow insert(
    TransactionhistoryRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.transactionid())) {
      throw new RuntimeException("id " + unsaved.transactionid() + " already exists");
    };
    map.put(unsaved.transactionid(), unsaved);
    return unsaved;
  };

  @Override
  public TransactionhistoryRow insert(
    TransactionhistoryRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  @Override
  public Long insertStreaming(
    Iterator<TransactionhistoryRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.transactionid(), row);
      count = count + 1L;
    };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  @Override
  public Long insertUnsavedStreaming(
    Iterator<TransactionhistoryRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var unsavedRow = unsaved.next();
      var row = toRow.apply(unsavedRow);
      map.put(row.transactionid(), row);
      count = count + 1L;
    };
    return count;
  };

  @Override
  public SelectBuilder<TransactionhistoryFields, TransactionhistoryRow> select() {
    return new SelectBuilderMock<>(TransactionhistoryFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  @Override
  public List<TransactionhistoryRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  @Override
  public Optional<TransactionhistoryRow> selectById(
    TransactionhistoryId transactionid,
    Connection c
  ) {
    return Optional.ofNullable(map.get(transactionid));
  };

  @Override
  public List<TransactionhistoryRow> selectByIds(
    TransactionhistoryId[] transactionids,
    Connection c
  ) {
    var result = new ArrayList<TransactionhistoryRow>();
    for (var id : transactionids) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    };
    };
    return result;
  };

  @Override
  public Map<TransactionhistoryId, TransactionhistoryRow> selectByIdsTracked(
    TransactionhistoryId[] transactionids,
    Connection c
  ) {
    return selectByIds(transactionids, c).stream().collect(Collectors.toMap((TransactionhistoryRow row) -> row.transactionid(), Function.identity()));
  };

  @Override
  public UpdateBuilder<TransactionhistoryFields, TransactionhistoryRow> update() {
    return new UpdateBuilderMock<>(TransactionhistoryFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  @Override
  public Boolean update(
    TransactionhistoryRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.transactionid())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.transactionid(), row);;
    };
    return shouldUpdate;
  };

  @Override
  public TransactionhistoryRow upsert(
    TransactionhistoryRow unsaved,
    Connection c
  ) {
    map.put(unsaved.transactionid(), unsaved);
    return unsaved;
  };

  @Override
  public List<TransactionhistoryRow> upsertBatch(
    Iterator<TransactionhistoryRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<TransactionhistoryRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.transactionid(), row);
      result.add(row);
    };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
    Iterator<TransactionhistoryRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.transactionid(), row);
      count = count + 1;
    };
    return count;
  };
}