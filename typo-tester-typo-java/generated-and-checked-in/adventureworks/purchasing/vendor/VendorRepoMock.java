/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.purchasing.vendor;

import adventureworks.person.businessentity.BusinessentityId;
import java.lang.RuntimeException;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.function.Function;
import java.util.stream.Collectors;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilderMock;
import typo.dsl.UpdateParams;

public record VendorRepoMock(
  java.util.function.Function<VendorRowUnsaved, VendorRow> toRow,
  HashMap<BusinessentityId, VendorRow> map
) implements VendorRepo {
  public VendorRepoMock(java.util.function.Function<VendorRowUnsaved, VendorRow> toRow) {
    this(toRow, new HashMap<BusinessentityId, VendorRow>());
  };

  public VendorRepoMock withToRow(java.util.function.Function<VendorRowUnsaved, VendorRow> toRow) {
    return new VendorRepoMock(toRow, map);
  };

  public VendorRepoMock withMap(HashMap<BusinessentityId, VendorRow> map) {
    return new VendorRepoMock(toRow, map);
  };

  @Override
  public DeleteBuilder<VendorFields, VendorRow> delete() {
    return new DeleteBuilderMock<>(VendorFields.structure(), () -> new ArrayList<>(map.values()), DeleteParams.empty(), row -> row.businessentityid(), id -> map.remove(id));
  };

  @Override
  public Boolean deleteById(
    BusinessentityId businessentityid,
    Connection c
  ) {
    return Optional.ofNullable(map.remove(businessentityid)).isPresent();
  };

  @Override
  public Integer deleteByIds(
    BusinessentityId[] businessentityids,
    Connection c
  ) {
    var count = 0;
    for (var id : businessentityids) {
      if (Optional.ofNullable(map.remove(id)).isPresent()) {
      count = count + 1;;
    };
    };
    return count;
  };

  @Override
  public VendorRow insert(
    VendorRow unsaved,
    Connection c
  ) {
    if (map.containsKey(unsaved.businessentityid())) {
      throw new RuntimeException("id " + unsaved.businessentityid() + " already exists");
    };
    map.put(unsaved.businessentityid(), unsaved);
    return unsaved;
  };

  @Override
  public VendorRow insert(
    VendorRowUnsaved unsaved,
    Connection c
  ) {
    return insert(toRow.apply(unsaved), c);
  };

  @Override
  public Long insertStreaming(
    Iterator<VendorRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.businessentityid(), row);
      count = count + 1L;
    };
    return count;
  };

  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  @Override
  public Long insertUnsavedStreaming(
    Iterator<VendorRowUnsaved> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0L;
    while (unsaved.hasNext()) {
      var unsavedRow = unsaved.next();
      var row = toRow.apply(unsavedRow);
      map.put(row.businessentityid(), row);
      count = count + 1L;
    };
    return count;
  };

  @Override
  public SelectBuilder<VendorFields, VendorRow> select() {
    return new SelectBuilderMock<>(VendorFields.structure(), () -> new ArrayList<>(map.values()), SelectParams.empty());
  };

  @Override
  public List<VendorRow> selectAll(Connection c) {
    return new ArrayList<>(map.values());
  };

  @Override
  public Optional<VendorRow> selectById(
    BusinessentityId businessentityid,
    Connection c
  ) {
    return Optional.ofNullable(map.get(businessentityid));
  };

  @Override
  public List<VendorRow> selectByIds(
    BusinessentityId[] businessentityids,
    Connection c
  ) {
    var result = new ArrayList<VendorRow>();
    for (var id : businessentityids) {
      var opt = Optional.ofNullable(map.get(id));
      if (opt.isPresent()) {
      result.add(opt.get());
    };
    };
    return result;
  };

  @Override
  public Map<BusinessentityId, VendorRow> selectByIdsTracked(
    BusinessentityId[] businessentityids,
    Connection c
  ) {
    return selectByIds(businessentityids, c).stream().collect(Collectors.toMap((VendorRow row) -> row.businessentityid(), Function.identity()));
  };

  @Override
  public UpdateBuilder<VendorFields, VendorRow> update() {
    return new UpdateBuilderMock<>(VendorFields.structure(), () -> new ArrayList<>(map.values()), UpdateParams.empty(), row -> row);
  };

  @Override
  public Boolean update(
    VendorRow row,
    Connection c
  ) {
    var shouldUpdate = Optional.ofNullable(map.get(row.businessentityid())).filter(oldRow -> !oldRow.equals(row)).isPresent();
    if (shouldUpdate) {
      map.put(row.businessentityid(), row);;
    };
    return shouldUpdate;
  };

  @Override
  public VendorRow upsert(
    VendorRow unsaved,
    Connection c
  ) {
    map.put(unsaved.businessentityid(), unsaved);
    return unsaved;
  };

  @Override
  public List<VendorRow> upsertBatch(
    Iterator<VendorRow> unsaved,
    Connection c
  ) {
    var result = new ArrayList<VendorRow>();
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.businessentityid(), row);
      result.add(row);
    };
    return result;
  };

  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  @Override
  public Integer upsertStreaming(
    Iterator<VendorRow> unsaved,
    Integer batchSize,
    Connection c
  ) {
    var count = 0;
    while (unsaved.hasNext()) {
      var row = unsaved.next();
      map.put(row.businessentityid(), row);
      count = count + 1;
    };
    return count;
  };
}